---
type: change-plan
status: in-progress
date: "2026-09-09"
---
# Persistent Live Acceptance

## Intent Epoch 1

The engineer requests architecture, technical design, canonical documentation, implementation and actual deployment of two complementary real-use paths: tasks and Agent benchmark work running in OpenKit with its product records and diagnostics; and a desktop Agent operating the same deployed product through a refreshed, capable OpenKit Skill plus separately authorized SSH/tools. A co-deployed Codex/Pi Agent and customer autonomous maintenance are excluded. The primary personally authors documentation, consults an independent Claude Code Agent, and obtains independent reviewer/auditor scrutiny. Commit documentation before implementation. Delegate all concrete implementation and testing work to Cursor CLI Agents using Grok 4.6. Deploy to the expressly authorized SSH alias `a2`, using existing `~/.openkit` test data and Codex subscription configuration; the engineer explicitly permits test-data loss, while credential confidentiality remains strict. Continue until both modes actually run. The purpose is to reduce repeated environment construction and enable pending feature plans to obtain their own real acceptance, not to declare all roadmap plans complete in this change.

## Intent Epoch 2

During canonical writing the engineer asks to state whether these modes primarily cover L6 Stories and real-user Agent task/benchmark suites, or whether all L6 must execute internally. The working interpretation is explicit scope plus default placement: these two modes are the default for ordinary L6; the Actor may be external, and cold-start/installation/destructive/isolation stories retain a justified dedicated environment. L1-L5 retain their existing owners. This clarifies placement without broadening test-platform scope.

## Intent Epoch 3

The engineer now explicitly authorizes that if any current token, login information, or Credential is unavailable, it may be rebuilt or reinjected. During the single planned a2 deployment this uses existing administrator recovery, provider subscription renewal or injection, and NanoHost transport credential reissue as necessary, without a repeated permission request. Confidentiality and existing authorities remain: no secret output or argv, no direct database authentication-flag mutation, and no invented authorization path. Source: user steering on 2026-09-09 during implementation.

## Owners

- [Architecture](../../core/architecture.md) owns observation versus execution authority.
- [Persistent deployment acceptance](../../specs/20260909-persistent_deployment_acceptance.md) owns the two-mode composition and initial acceptance.
- [L6](../../specs/20260529-l6_story_acceptance.md) owns open-ended Actor and independent verdict semantics.
- [Skill interface](../../specs/20260713-openkit_agent_skill_interface.md) owns discovery and public operation projection.
- [Operational telemetry](../../specs/20260731-operational_telemetry_standardization.md) and [boot diagnostics](../../specs/20260704-nanocore_bootstrap_readiness.md) retain optional signals and process observations.
- [Verification instruments](../../verification-instruments.md) retains strict fixture qualification while admitting ordinary persistent product use.

## Method And Decisions

Reuse existing Task/Goal, public work reads, Audit, Usage, Evidence, Skill packaging and deployment tooling. Do not add an evidence aggregation service, evaluation database, runner or automatic repair daemon. Restore useful existing public read projections in the CLI, refresh guidance and story admission, and add bounded process/request diagnostics only within the explicit initial slice. External host operations stay outside the Skill. Cold and destructive qualification do not operate implicitly on the shared instance.

Claude consultation is through Herdr `live-consultant`; canonical writing is primary-only. Independent registered reviewer/auditor contexts inspect actual bytes. Cursor `live-deployment` first inspects a2 read-only and later receives exclusive deployment/test ownership when documents are committed. No other agent may mutate that deployment concurrently. Primary owns this plan and all documentation; implementation ownership is dispatched by explicit paths after the documentation commit.

## Checkpoint

Baseline `a592b199`, initially clean. Canonical owners define persistent ordinary acceptance without cold-start of a new instance. Intent Epoch 3 now authorizes rebuilding or reinjecting unavailable credentials during the one planned deployment through existing recovery, subscription, and NanoHost transport procedures. The existing catalog retains its mappings-plus-exclusions oracle. The initial new read is NanoHost runtime-target; search/dashboard additions remain deferred. Product Vision is untouched.

Local implementation now passes focused NanoCore, schema, Core Client and catalog checks. Web typecheck and build pass after the existing frozen workspace install restored absent package links; no dependency change was needed for Web. Independent review identified optional exporter shutdown gating listener closure; the correction now starts best-effort flush concurrently. An independent controlled probe of the actual entrypoint function observes both listeners and the canonical close callback completing with telemetry still pending. The durable regression executes that same extracted function. The two Web diagnostics fixtures are aligned and their 49 focused tests pass.

Current deployment checkpoint: App and externally mounted Web now use exact commit `3a3986d4964a1f5ff26e9ed3245d83f795a36503`, image `sha256:4c5eb418b4c45e40571c858bd80597952bdec3419f71d621f65a4c86a622b799`, boot `boot_4a69c5ef-7b49-4258-a65b-36cb97e6de74`. App smoke passed; the update retained Data Root, configuration, credentials and prior scenario records. Worker and NanoHost retain the verified unchanged artifacts from the earlier build. Public diagnostics accept product work; the retained boot note `storage.index-rebuilt` is non-blocking. The Collector is running after its second bounded outage/recovery proof. The corrected boot exports owned HTTP spans only, including after an ordinary Better Auth session read. The unrelated R058 deployment remains intact.

External Actor A remains independently inconclusive for its invalid admin-credential precondition, outside the revised admission. Actors C, D and E settled the three-run Thread story on `ee58fc38`; its independent accumulating verifier accepted their retained sequence. Actor F created and read `th_6` in `ws_2` on the final build, and its fresh Judge returned `passed` after its own public read. NanoHost's initial provisioning failure remains preserved in Artifact `ar_import_6fcd7e813cbdbe616f421418`. The accepted terminal operator route completed full disposal, image index verification and exact-pair reenrollment before one start; public generation 5 is fenced, ready and fresh-empty at `2026-09-09T14:49:39.033Z`. The earlier failed interval is not reclassified. Mode one still requires an actual Worker Task and meaningful Artifact. The configured Task path uses Gateway inference rather than native Worker Vault authentication; a separate native Codex bootstrap cannot restore its Provider access. No Task AEP has been generated or executed in this attempt. The configured Codex subscription device login expired without completion; the human account holder must complete a new flow. Availability has been requested without minting another expiring code.

Fresh Claude context `db7ee554-8287-4bc9-93c6-499208e316a2` scrutinized this primary's Intent, checkpoint, Git and named evidence before the completed external effects and returned Continue. Final-build Skill adjudication and the separate read-only review of Collector and public readiness observations are complete. The remaining acceptance is human-completed subscription login followed by the real Worker Task. Once login completes, submit the held ordinary Task input in `temp/live-tester/mode-one-prep.md`, then require its actual terminal state and independently checked Artifact totals. No further product reset, App restart, fresh Provider account or new native Worker credential is planned. The plan remains in progress until that mode-one observation exists.

## Acceptance And Evidence

Both real modes must complete on one persistent authorized deployment with exact build attribution, meaningful output and public terminal-state evidence. The external Skill L6 retains its required independent judgment and repeated-run admission, with current host context isolated from hidden answers. A repeated attempt must not recreate the product or Provider account. Record focused check results and real observations here before closure. Other plans close only against evidence that satisfies their own conditions.

Temporary source/inspection/consultation evidence stays under `temp/live-*` and `temp/reviews/20260909-persistent-deployment-consultant/`. Implementation and deployment observations do not close the remaining real Worker acceptance.

## Story Admission Argument

The revised Skill story asks for one new named project Thread in a supplied authorized Workspace and its identity. A competent Actor with only that goal and the installed product interface can satisfy the required product assertions; no exact CLI sequence or hidden answer is required. A unique attempt-owned name provides isolation on the persistent deployment. Scope/tool and confidentiality assertions preserve ordinary permitted use; discovery friction remains non-blocking. Actor A and the former Workspace-create revision cannot count toward the revised story's repeated-run admission. Internal real Task completion is the mode-one seam proof and does not claim a benchmark comparison or the separate Worker MCP story's full acceptance.

## Design Review Evidence

Claude Consultant returned Continue with no consequential unresolved objection after inspecting actual owners and source. It withdrew an unnecessary repeat-permission request because the engineer already authorized persistent reuse and a2 deployment. Direct code inspection corrected two proposed detours: operation coverage already exists, and NanoHost qualification teardown must not run as ordinary product preflight. The amended verification owner is 4591 words, within its 4600-word ceiling. Optional HTTP telemetry remains requested scope, separate from acceptance authority. No new build identity is invented; deployed artifacts remain the attribution source.

Observed document checks: documentation model passed (248 documents), spec lifecycle passed, story schema passed (2 stories), `git diff --check` passed, and skill-creator validation passed using an isolated uv PyYAML environment after system Python lacked that optional dependency. Independent reviewer final reinspection found no actionable documentation findings; Auditor found no blocking intent, authority-consistency or protected-path finding. Their corrected-byte reports are retained at `temp/live-design-review.md` and `temp/live-acceptance-auditor.md`. Production checks remain Cursor-owned implementation work.

## Deployment Observation

Cursor read-only inspection found the persistent a2 NanoCore container healthy at its existing HTTPS endpoint, using the existing test Data Root. Its configured NanoHost is running but the observed target is not ready/fresh-empty, and the configured Codex Worker image is absent. These are real implementation/deployment blockers to mode one; liveness does not discharge them. The existing host deploy script only accepts origin/main, so deployment must either use an authorized publication or transport the exact committed snapshot and reuse the existing build/service procedure with truthful identity. No Git push is implied merely by the script's preference. A separate prior test container exists and is outside this change's owned deployment.

## Review Corrections

Independent review separated Actor admission from product assertions, restored required Skill discovery triggers, clarified telemetry configuration truth conditions, corrected local indexes and Markdown, and preserved the desktop Agent operator phase. Search and dashboard mappings are deferred: their existing handlers check Workspace access but do not yet implement the accepted private-thread visibility contract. Only the admin NanoHost runtime-target read is added now; the three existing exclusions receive accurate dependency reasons. This avoids expanding a known content-visibility gap while leaving both requested execution modes achievable through attempt-owned public records.

## Implementation Dispatch

Documentation committed as `cbab8ace` after independent review and audit. Cursor `live-builder` owns the bounded runtime/schema/client/catalog code and focused tests; primary retains all Markdown. Cursor `live-tester` independently derives the real-use evidence procedure read-only; Cursor `live-deployment` checks the actual NanoHost backend and image-import readiness before any remote mutation. Expected observations are contract regressions and the real readiness cause, not success inferred from process liveness. The release-tree and fixture-wording test projections intentionally require reconciliation with the newly committed owners during implementation.

Fresh independent Claude `live-direction` (session `35cd8033-7ea2-4fea-a228-4bab64596c24`) inspected source intent, current diff and host evidence after primary compaction and returned Continue. It confirmed ordinary Task admission requires the actual NanoHost handshake and warned that host Docker image inventory does not prove the private backend image is absent. Directly changing readiness database flags is forbidden; fix image supply or the actual runtime owner and observe a fresh handshake. If readiness requires a NanoHost code fix, handle the demonstrated defect under its accepted owner, not qualification teardown. Preserve the exact successful snapshot transport/build/restart procedure in the cookbook after observation; a local commit plus deployed digest is attributable without claiming origin publication.

## Corrected Host Observation

The private NanoHost image store contains the staging images; the earlier host-Docker absence inference was false. Actual cause: the single live NanoHost unit still connects to the retained R058 fixture on rendezvous 4328, not staging on 8081. The fixture has zero live leases, all 24 backend sessions cleaned, stale September 6 activity, and verified/completed owning plans. There is no active concurrent work to preserve in that session. Restore the existing staging configuration during the single planned deployment, retaining the unrelated fixture container and data. Read-only evidence: `temp/live-deployment/nanohost-readiness-probe.md` and `temp/live-deployment/r058-occupancy.md`.

## Ownership Split

After confirming its release, `live-builder` retains NanoCore process/telemetry, App API schemas and dependency lock ownership. Cursor `live-tester` now owns Core Client, Skill operation catalog and the existing Skill/verification contract tests, preserving peer-written fixtures. This independent seam can progress in parallel; a later independent reviewer will inspect both final slices. Primary still owns all Markdown and no agent may alter a2 until the committed build is ready.

## External Interface Preparation

Cursor `live-deployment` stored the existing a2 file grant in the local Skill encrypted store for `https://ai.simonxu.net`. That grant is invalid: the same in-memory token returned HTTP 401 from both `127.0.0.1:7080` and public HTTPS `GET /api/app/diagnostics`. Doctor store presence is not auth proof. Intent Epoch 3 now authorizes existing stopped-server administrator recovery, provider subscription renewal or injection, and NanoHost transport credential reissue during the single deployment, without repeat permission. Pipe recovery envelopes through stdin only. Do not print secrets, guess tokens, or write authentication flags in SQLite. Remote service mutation still waits for the final committed build. Evidence: `temp/live-deployment/auth-store-outcome.md`.

## Telemetry Configuration Correction

Claude inspected the installed stock exporter merge implementation and confirmed there is no documented override that removes inherited environment header names. Rejecting unsupported header configuration is independent of SDK evaluation timing and replaces the proposed temporary process-environment mutation. The telemetry owner now defines this explicit disabled case and safe variable-name diagnostic; endpoint validity remains separately observable. Stock TLS transport remains operator configuration. This adds no exporter wrapper or alternative transport.

Optional same-deployment Collector prep is ready and not started: pin RepoDigest `otel/opentelemetry-collector-contrib@sha256:799dc6cf12c96192af37b5bdba804da8c10b3bc563b43cb90c3f3c58d9572ad6` (manifest-list; image Id recorded separately), loopback OTLP/HTTP on free `127.0.0.1:14318`, rotated file export under `/home/ubuntu/openkit/otelcol` outside Data Root, `--restart unless-stopped`, config already installed, YAML `validate` exited 0 in a disposable `--network none` run. Start and product `OTEL_EXPORTER_OTLP_ENDPOINT` wait for the one deployment. Evidence: `temp/live-deployment/collector-slice.md`.

## Local Implementation Evidence

Observed App API schema suite: 116 tests passed; Core Client: 78 tests passed; Skill and verification contracts: 34 tests passed; initial NanoCore telemetry/process/diagnostics/boot checks: 26 passed; OpenAPI projection: 24 passed. Relevant lint, typecheck, schema/client/NanoCore builds and Skill bundle build passed. Web typecheck and build also passed after `pnpm install --frozen-lockfile` supplied the missing workspace package links. Independent Cursor review reproduced the Core Client/catalog results and inspected generated bytes. Sources: `temp/live-builder-checks.md`, `temp/live-tester/implementation-checks.md`, `temp/live-tester/build-env/report.md`, and `temp/live-review/skill-seam-review.md`. These are local evidence, not live acceptance.

Final correction checks: shutdown/phase/telemetry suite 18 passed; the behavior-based phase-order file 7 passed after replacing the source-shape assertion; both touched Web suites 49 passed; focused lint and NanoCore build passed. Independent documentation reinspection withdrew an inferred completed-flush requirement: normal exit may cut off best-effort export, while the existing shutdown owner retains exit authority. No actionable documentation finding remains. `temp/live-review/close-listeners-telemetry-probe.txt` records the independent hanging-exporter observation. Documentation model (248), lifecycle, generated index and diff-whitespace checks pass.

## Deployment Dispatch

Implementation committed as `ee58fc3892bc78202e235be4be5cac77ca122be7` with a clean tree. Fresh Claude `live-deploy-direction` (session `fb4b5064-7ae9-4373-a5e0-034565de1284`) read the current primary Intent, artifacts and host observations after compaction and returned Continue. It identified the existing script's unconditional origin update; the chosen route already avoids invoking that script and instead uses the exact archive with its underlying commands. Actual Worker, supervisor and OpenShell image inventory plus credential mount compatibility will be inspected before Task admission. Optional Collector enablement remains in the one deployment: failure is explicitly non-authoritative, and a separate outage observation will prove that boundary without a second product restart. No new permission or product owner is required. Cursor `live-deployment` now owns the authorized host effects and existing credential recovery; primary retains all canonical documentation. Expected observation: exact deployed identities, restored staging NanoHost handshake, authenticated public process diagnostics and real-provider configuration, followed by both real-use proofs. A missing runtime dependency changes the local deployment method; it cannot be repaired by database readiness flags.

## Initial Data Root Correction

New App boot reached scheduler restart recovery and failed because the old test Core database records `core_0000_setup` from August 31 but lacks the current baseline `worker_capability_token_hash` column. Read-only schema inspection confirms the current baseline SQL already owns that column; this is obsolete internal test state, not an accepted backward-migration requirement. The engineer expressly permits test-data loss and unavailable credential rebuilding. The operator therefore preserves the old state in a restricted archive and uses existing initialization/recovery/import owners to establish the current baseline, retaining supported configuration and encrypted credential files and reinjecting subscriptions if needed. No ad hoc ALTER, readiness flag, old-table copy or product compatibility path is added. No acceptance Actor or Task has started. Both modes will reuse the resulting baseline, and the unrelated R058 root/container stays untouched.


## Skill Fixture Reframe

Independent Claude `live-skill-direction` (`d7fc1d59-bf4d-4792-bce2-35d02a2b6a94`) traced the actual authorizer and returned Continue: server-admin denial matches the multi-user owner, while Thread creation is Workspace-bound. Independent Auditor adjudicated this as instrument repair and requires preservation of Actor A and the original story revision. Sealed evidence is under `temp/live-tester/actor-a-20260909/`, with admission details in `temp/live-tester/actor-a-seal.md`; a fresh Judge retains its own verdict. No product auth changes are made.

Consultation also found that the `workspace.create` catalog annotation says generic authenticated user without exposing its canonical-user/session restriction. This is a separate Skill interface projection finding, received by `docs/specs/20260713-openkit_agent_skill_interface.md` for a later catalog access-annotation correction; it is not silently repaired by broadening authority or expanding this acceptance change.


Fresh Claude `live-resume-direction` (`22900809-07ee-41ba-906b-f86b0a651d49`) checked the current primary context, source Intent, actual diff and authorization path and returned Continue. Its two corrections are applied: owner/editor membership is explicit, and product Provider/Codex flags are false for Thread CRUD. Auditor withdrew its earlier run-wide Provider interpretation after examining the governing Test Strategy and L6 examples; three fresh consecutive runs now govern admission. Independent reviewer found no actionable issue in the Thread reframe. Cursor documentation checks passed: model 248 documents, lifecycle, story schema 2 stories, current generated index and diff whitespace. Actor A's independent Judge returned `inconclusive`; it remains outside the revised count.

## Observed Skill Admission And Telemetry Correction

Fresh Cursor/Grok Actors C, D and E each created and read the requested Thread through the installed Skill on build `ee58fc38`: `th_2`, `th_3`, `th_4` in `ws_2`. Each fresh Judge performed its own public read and returned `passed`; these are the three consecutive admitted runs for story revision `d29554f0`. The installed Skill digest is `e7d51fd8b49dc5f81be3b7d2a11afc03a24ae5bf8468206f851b75aac604b215`. Sealed evidence is in `temp/live-tester/actor-{c,d,e}-20260910/` and `judge-{c,d,e}-seal.md`; the original invalid-credential attempt remains excluded. This settles that story's admission, not mode-one Worker completion.

Independent Cursor accumulating verifier `live-review` (`f0d21789-e35a-45d9-ae7a-461da4438e6a`) inspected the actual Actor/Judge transcripts and admission observations and accepted the C/D/E sequence without a rerun; its evidence is `temp/live-review/l6-d29554f0-accumulation.md`. Native reviewer found no actionable issue in the exact-source update recipe, accurate Partial implementation projection and refreshed checkpoint. Cursor documentation checks passed: model 248 documents, lifecycle, current generated index and diff whitespace (`temp/live-review/doc-checks-20260910.txt`).

Collector stop/resume observations preserve the same App boot and successful public diagnostics, with matching request log/export trace ids before and after the outage. Actual export inspection also found 17 library spans among 87 spans: global provider registration enrolled Better Auth instrumentation, whose source can record exception and context content. The bounded HTTP slice must not collect those fields. The correction uses the existing private stock provider, with a stock non-recording span when disabled or stopped; it removes global registration, global disable and the fallback to a foreign tracer. No dependency, filter or wrapper is added. Two regressions cover library error/context exclusion and disabled behavior in the presence of a foreign registered provider. Cursor independently inspected the final bytes and reran all 11 telemetry tests successfully; the seven phase-order tests, focused lint, typecheck and build also pass. Document model, lifecycle, index and whitespace checks pass; independent documentation review found no remaining issue. Old live observations remain attributed to `ee58fc38`; the correction still needs deployment and a fresh export observation.

## NanoHost Recovery Observation

The local first-fence marker records `2026-09-09T13:39:53.145772Z`. Later starts at 13:55 and 13:59 exceed the accepted 300-second unresolved interval, so NanoHost correctly remains non-ready before sending the readiness request. Core generation 4 records predecessor fenced but no readiness. The marker is create-once and survives failures by design; deleting or replacing it is not a recovery procedure and cannot make the old interval pass. The operator is retaining the initial failure and investigating its pre-expiry cause.

Independent Auditor identifies the existing terminal lifecycle in `docs/specs/20260802-nanohost_runtime_and_transport.md`: complete decommission followed by explicit reinstall of the exact retained identity/deployment pair is permitted. Changing deployment identity is not the same-host reinstall route. Before any such effect, Cursor must verify that the supported decommission/uninstall implementation terminates the entire old installation, credentials and effect domain, preserves required failure evidence, and permits ordinary reenrollment. A standalone marker reset is excluded; missing terminal cleanup is corrected under that owner instead. App data, build and external Thread acceptance remain unchanged by this narrowly scoped NanoHost work.

Auditor clarification removes an inferred single-CLI requirement: the owner explicitly permits a composed operator path. Fresh Claude session `7db631d6-732a-4966-b397-b63da2dd716c` independently returned Continue after reading the actual owner and marker lifecycle. The original failure was admission 401 at 13:40:38, before valid enrollment at 13:44:20; later manual starts inherited that unresolved interval. This provisioning-order failure does not establish a stock epoch-rebuild performance defect. The corrected procedure stops and proves the complete group absent before Core decommission clears raw slots, retires every old effect and installation state, preserves the failed evidence, and reenrolls the exact pair before first start. The maintained cookbook now records that order. Cursor independently reviews the actual operator commands before their effects.

Required failed-interval facts were imported and independently read through public Artifact operations: `ws_2` / `ar_import_6fcd7e813cbdbe616f421418`, version 1, digest `sha256:98fb702a6510a632030529b5450f005a94cd435ad02939c9370d028219b6bdff`. This durable owner preserves the old failed outcome before private NanoHost evidence disposal; it is not a Worker execution result. Operator path and live alignment proofs are under `temp/live-deployment/nanohost-terminal-decommission-procedure.md`.

## Final Build Deployment And Remaining Acceptance

The tracked `3a3986d4` archive has SHA-256 `ff2d36c7b780045086038611c86e821dabbb560cb17532b12acfd26fd23e6dd8`; App build and image smoke completed at 14:45:06 and 14:45:14 UTC. App container `20b719304ffa` started at `2026-09-09T14:46:10.08289984Z`, image `sha256:4c5eb418b4c45e40571c858bd80597952bdec3419f71d621f65a4c86a622b799`. Public boot is `boot_4a69c5ef-7b49-4258-a65b-36cb97e6de74`; Web uses the same commit. Data Root and transport sink inodes remain 262643 and 2885362; previous App containers remain stopped. A preflight file-read permission error was corrected with existing sudo before replacement, without loosening file permissions. Actual safe observations are retained in `temp/live-deployment/app-3a3986d4-observation.json` and `temp/live-builder-3a3986d4.md`.

After full NanoHost group absence, decommission request `c44d419e-ced5-4ed4-ba3a-dc0c17260bf3` revoked the old token. Complete state disposal and four-digest Image Store index verification preceded fresh enrollment at `2026-09-09T14:48:45.847Z` and one start. The retained pair `staging-nanohost-a2` / `staging-a2` became publicly ready and fresh-empty in generation 5 at `2026-09-09T14:49:39.033Z`. Evidence is `temp/live-deployment/nanohost-terminal-execution-evidence.md`; the original failed interval remains separately preserved.

On this final boot, actual OTLP output contains only scope `nanocore`, span `http.server.request` and the four admitted attribute keys. The ordinary Better Auth session read adds no library span. Collector stopped at 14:49:27 UTC and resumed at 14:50:27.721837376 UTC; public diagnostics succeeded during the outage with the same App boot, and export resumed. Before and resumed trace ids are `6466d9425a1b23930a6f7c681f52d69e` and `6b4785513306f64bcc73db28aeb0caa3`; outage trace `66f208df652c2a27e9ad33d4f4cd08f0` was not exported. Collector and unrelated R058 container remain running. Evidence is `temp/live-collector/proof-3a3986d4.md`.

Fresh Actor F (`7751575a-93bb-463d-81b2-1b3ada157c20`) created and read `th_6` / `ws_2` / `Cedar Ridge field notes 20260910-F` on the final build. Fresh Judge F (`1d7d7bef-6124-4e24-a85a-a4c3859b728e`) performed its own public `thread.read`, request `ef2c4609-7f26-4151-849f-25bea34b924d`, and returned `passed`. Its deciding output is retained in `temp/live-tester/judge-f-pane.redacted.txt`; the earlier C/D/E admission remains unchanged. No real Task has been submitted: configured Codex subscription device login is still awaiting the human account holder. Therefore mode two is observed on the final build, but the two-mode composition and this plan remain incomplete.

Independent Cursor live review recomputed the exact App/image/boot, public generation-5 readiness and HTTP-only export shape. It recovered the before/resume trace ids from the size-rotated native export and matched both to App logs; the initial current-file absence was a rotation-window observation, not a product failure. Sources are `temp/live-review/live-observation-3a3986d4.md` and `named-id-followup-3a3986d4.md`; no outage was repeated. Native reviewer and Auditor closed the two stale/overstated checkpoint phrases and found no remaining issue in their bounded scope. Final documentation model (248 documents), lifecycle, generated index and whitespace checks passed (`temp/live-builder-final-doc-checks.txt`). Product Vision is unchanged. All authorized effects are complete except the human-dependent subscription login and the ensuing real Task acceptance; no plan closure or Git push is claimed.
