---
type: change-plan
status: in-progress
---
# Develop And Maintain OpenKit Through OpenKit

## Intent Epoch 1

Source: the engineer's 2026-09-16 request after redeploying the latest code on SSH host a2 at https://ai.simonxu.net. Exercise every page and its useful operations using OrcaRouter / DeepSeek Flash Free, fix observed problems and make justified improvements, commit each completed correction, and continue until OpenKit development and maintenance can move into Workspace ws_7. Changes beyond Web presentation must also reach the public OpenKit Skill and be exercised there. The engineer authorizes live product testing and Skill installation. Existing human approval gates, credential protections, and unrelated work remain intact.

## Intent Epoch 2

Source: the engineer's subsequent 2026-09-16 browser comments and deployment confirmation. Replace the overflowing Task header text badge with an icon. Improve Account section spacing and relationships, prioritizing Workspace names over ids in invitations. Make Overview a useful current-Workspace summary of ongoing Tasks and Goals and required decisions, including direct Allow and Deny for approvals. Continue parallel corrections and live checks after the engineer's A2 redeployment; commit each correction separately. Cursor CLI and Claude Code authentication are available again; use Cursor for bounded implementation and Claude Code for consequential consultation.

## Owners

- `docs/core/work-model.md` owns the user-facing work backbone and execution narratives.
- `docs/specs/20260628-web_product_surface_projection.md` and `DESIGN.md` own Web projections.
- `docs/specs/20260713-openkit_agent_skill_interface.md` owns the public Skill and operation parity.
- `docs/specs/20260704-task_mode_worker_delegation.md` and the applicable Goal, repository, approval, and workspace-sync specifications own execution and delivery.
- `docs/specs/20260909-persistent_deployment_acceptance.md` and `docs/cookbooks/persistent-live-acceptance.md` own live-use attribution and repair boundaries.

## Method And Acceptance

Start with the deployed UI and public Skill, preserve concrete failures, and reduce deterministic defects into focused regressions. Prioritize the development loop: submit a bounded task with the requested model, observe execution, inspect exact changes, handle required human decisions, and locate durable results through both interfaces. Inspect every published page and safe meaningful controls; do not exercise destructive administration merely for coverage. Preserve existing data and services. Local fixes are committed individually; deployed and locally verified behavior are distinguished. Broad migration success requires a real completed development loop and an identified supported maintenance path, not only passing UI tests.

## Checkpoint

Goal acceptance now has a workspace-visible test Thread ws_4/th_7 and generated plan it_goal_plan_tu_goal_plan_a5a17c022a8fbe8724979069, durably awaiting_plan_approval with no dispatched Tasks. The requested provider is available through logical model orcarouter, but goal.step currently carries no model selection and the current Workspace Agent prefers gpt-6. The user-preference fallback exists internally but has no public mutation surface, so it is not an end-user workaround. Do not approve or execute this plan until model selection and the exact human gate are resolved. The durable-plan reader fc00be38 and Goal Web recovery e2fd174b are deployed and verified on A2. The original plan and approval controls survive reload through both Web and the public Skill without new Turns or Items. No per-Goal model contract has been accepted.

A2 App and mounted Web assets both run 2cd35062e28924ff11576aef48aa3a241858416f, including complete pre-approval Plan disclosures and truthful initial-draft copy. Both original accounts work; restored configuration, credentials and the approved certificate-fix Worker image support shared OpenKit Workspace ws_4, which replaces disposable ws_7. Worker source remains pinned separately at 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9. Backup, cross-deployment import, restoration and deployed Workspace review UI evidence are retained below.

The real th_6 Worker produced the exact one-file README patch with digest sha256:7ce33d501bbfe09e510706397848da7321cf217eee3572977608a58fb5f16c70. Its staged human decision remains pending; read-only apply checks passed, but no acceptance, apply or publication is claimed. A later read-only continuation emitted a duplicate review of unchanged bytes. Readable structured Task input is committed and verified on A2; a missing apply target truthfully leaves the original handoff failed; explicit retained-checkout recovery through a new Task is verified, while a new non-actionable output record is deferred. The complete development workflow is not yet verified.

Explicit retained-storage selection is fixed, committed and verified on A2. The original th_5 association advanced from idle revision 4/generation 1 to attached revision 6/generation 2, and a new read-only OrcaRouter Task observed the original one-line README edit without recreating it. Normal closeout now produced a pending review, and Web opened its typed diff. The public Skill recovery recipe is synchronized and exercised. The bounded Task input presentation correction is implemented through the verified dashboard projection, with unchanged immutable bytes available in a native disclosure; the real five-Turn conversation passed Web and public Skill proof. Source inspection resolved the model-selector observation as the configured default for the next submission after remount, not incorrect Worker model execution. Exact th_6 review acceptance remains dependent on the engineer's pending decision; no review was accepted or applied.

## First Live Observations

The fixed deployed App is container openkit-staging, image tag openkit/app:staging-bdd94349e96fb0e6017a5b0f8d086c2e60fa6367, image ID sha256:f034d9f996d424a12c6e1122ee8bcc6baeaa9ee7a1b15cfcb470b3fa867c3e69, started 2026-09-16T04:51:13.371942156Z. This is earlier than local e44c78f0; do not claim the last local icon correction is deployed. Non-secret Docker identity was read through authorized SSH a2 with sudo. No service was changed.

The installed complete Skill at /Users/m5pro/.codex/skills/openkit passes doctor and workspace.list with the existing protected endpoint credential. Web uses the signed-in editor while the stored Skill credential has a different effective scope; parity observations must preserve that difference. AI interface displays OrcaRouter model deepseek/deepseek-v4-flash-free. A bounded Web Chat produced three relevant Chinese acceptance-evidence bullets in ws_7/th_81. This proves Chat only, not Worker execution.

Overview displays seven Workspace Review attention rows with no Open controls. The existing openHrefForRow omitted workspace_review and workspace_recovery, which have no Thread. Both now route to the existing selected-Workspace changes surface with an accurate accessible label; decisions remain there. Two focused regressions failed before the correction; 152 Workspace and Workspace Sync tests passed afterward. Web build passed with the existing chunk-size warning. Evidence: /tmp/openkit-overview-red.log, /tmp/openkit-overview-green.log, /tmp/openkit-overview-build.log. This is a Web projection correction; existing public sync review operations are already discoverable through the installed Skill.

Independent Consultant check_self_hosted_route returned Continue: prioritize the real development loop over page polish, reuse public Skill operations and existing App update receipts, and retain exact model/build/result attribution. Obtain the next direction observation after the first real development loop, before committing to a further batch of cross-backend changes.

Pages observed so far: existing Task history, Chat starter and successful Chat, Overview, Agents (empty even though Worker targets exist elsewhere; Refresh health exercised), Knowledge, Artifacts (internal reviews correctly excluded), Account, AI interface, Workspace changes, and Repositories. Repository loading fails after Retry; cross-interface diagnosis is next. Workspace changes exposes exact patches but foregrounds long internal IDs and duplicates operational inventories; retain as a usability finding rather than pretending it is a user-ready review experience.

## Current Development Attempt And Shared Contract Repair

The Web-created Task ws_7/th_82, Turn tu_conversation_e103462daef02515e9eee9ee, started at 2026-09-16T05:08:54.229Z to fix Recent conversation ordering and failed at 05:21:24.892Z: `worker_governance_turn_failed`, "Last worker inference stream failed before completion." It retained an accepted status Item but produced no code changes or Artifact. Its execution snapshot selects orcarouter, while the remounted composer shows the default GPT-6; these are different observations. Runtime evidence attributes 226 Gateway calls to this Turn, records successful transcript collection and teardown, and does not establish why inference failed. No push, approval, merge, cancellation or deployment was performed.

Repository diagnostics report missing local directories. The host repository /home/ubuntu/openkit/workspaces-repos/openkit exists at 3b85590; the live App container has no repository bind mount and /srv/repos/openkit is absent. The existing deployment script requires workspaces-repos bound at /srv/repos. The Worker separately materialized its Git source at older pin 4879812a5c705d46d04f293692a7edec5f810558; the missing Core mount is not evidence of the model-stream failure's cause. NanoHost is active and reports the configured target ready; readiness is not successful Task evidence.

The public usage.read operation and Web Usage page fail because CapabilityUsageCallSchema omits network although the canonical ledger and accepted Git push contract already require it. The schema now admits that existing family, with a Git push regression. Package tests pass 143/143, typecheck and lint pass, and Web plus Skill builds pass. The freshly built Skill interface suite exposes three separate existing parity/harness failures (missing listConversationNavigation and two token-delivery checks); retain /tmp/openkit-usage-skill-test.log and correct them rather than claiming a full Skill PASS.

Additional pages exercised: General draft edit and Reset without saving, Catalog native file-picker entry, Recovery (empty), Archived threads, Vault (read errors), Usage (read error), and Appearance (Spectrum/Noir/Paper switched and original Paper restored). The file picker caused no accessibility-tree change; that is not evidence of a Catalog defect and the speculative UI change was removed. Configuration draft validation, App update form choices, administration environment status, access-token metadata, audit records, backup controls and Workspace access recovery were inspected without effectful administration. No token, role, deployment, backup or environment change was performed. Authentication is restored for Cursor CLI; Herdr agents implement bounded corrections while the primary owns integration, the generated Skill, documentation, and commits.

## Verified Corrections And Remaining Blockers

Committed corrections include Overview review links (674ddca8), network capability usage (c968c174), typed repository access-denial presentation (b15cdde5), recorded Task failure after reload (c415442), public Skill conversation navigation with isolated credential test storage (fda08c23), Recent activity ordering with mode-correct routes (32e4e5d), public content slash preservation (b4476128), and retained Worker stream diagnostics (3f9065c3). Focused evidence: 152 Workspace tests, 143 App schema tests, 2 failed-Turn tests, 92 Chat tests, and 41 public Skill interface tests passed. Web typecheck and builds passed for the relevant slices; these local results await deployment verification.

Explicit Skill import created ar_import_60c324cd46d488f58441f088 version 1, "Dogfood report — 2026-09-16". Web opened its Markdown body correctly and added its exact-version reference to the test Chat th_81 without starting Agent work. Skill artifact.read incorrectly redacted the standalone slash in "OrcaRouter / DeepSeek Flash Free"; the shared public redactor now preserves that punctuation with path and credential protection retained. The 42-test Skill suite passed, the installed package was refreshed, and artifact.read returned the original body and digest.

The stored Skill credential belongs to user_local, while the browser session is a different user. On the test-created private Task th_82, Core Thread/Turn/Item reads returned content but the App dashboard correctly returned 404. Independent source review confirmed missing Core audience gates under docs/core/permissions.md and docs/specs/20260909-thread_visibility_and_sharing.md. No further cross-user private reads are needed. Restore the existing visibility helper at shared authorization seams and audit indirect identifiers before acceptance; this correction does not grant new rights or redefine private ownership.


## Bounded Worker Probe And Current Review

An explicit New Shard + Worker request through Web created workspace-visible Thread th_task_4a3e323ace91782dfd9f8559 and Turn tu_conversation_45b375d0ce20e450997343cb. It completed from 2026-09-16T06:04:47.869Z to 06:05:09.819Z (21,950 ms), running only git rev-parse HEAD and git status --short. Web and Skill both exposed the completed result. The authoritative package snapshot selected orcarouter; the Worker reported clean source revision 4879812a5c705d46d04f293692a7edec5f810558. The remounted composer displayed GPT-6 Astra, a separate unresolved selection/display defect. This proves bounded execution, not a code-change/review/apply loop.

The failed earlier Task had 226 attributed Gateway calls: 215 successful and 11 stream failures. Existing evidence does not distinguish transport loss from missing provider completion. A classifier collapsed the known provider_stream_truncated and provider_stream_failed diagnostics; the committed correction preserves these on durable capability calls while keeping the fixed redacted public Gateway JSON/SSE vocabulary and failed terminal state. All 204 focused Gateway, Worker inference, and executor tests passed; staged lint passed. The public Skill recovery reference describes how to inspect the same evidence without inferring an unsupported cause.

Independent review of the private-audience correction identified enforcement-order gaps in Generative UI and Action Center, opaque queue-id enumeration, and unfiltered Workspace Thread counts. Corrections and exact regressions are in progress. Existing invalid token fixtures and old missing/foreign Thread response expectations are being aligned with durable token validation and the nondisclosing 404 boundary; shared cross-user behavior and no-effect checks remain required. Workspace export, private-to-shared Worker handoff, and runtime context assembly remain separately identified surfaces, not claims of completed privacy coverage.

An authority conflict remains pending the engineer: canonical membership rules prohibit server-admin Workspace bypass, while accepted and merged Issue 13 / PR 12 and Issue 58 / PR 63 explicitly implement full-system server-admin bearer access without membership. The existing bypass is unchanged while the engineer chooses the governing rule. Both interpretations preserve exact private Thread ownership. Claude Code consultation through Herdr remains unavailable because its login is expired; no authority decision is inferred from that failure.


The complete NanoCore suite was exercised during integration: 2,975 passed, 70 failed, one skipped across 275 files. A detached 3f9065c3 baseline reproduced 66 failures across the ten affected pre-existing test files (272 tests passed there), so those failures are not attributed to the uncommitted Thread correction. The four newly failing cases were the in-progress Action Center audience regression and three Quick Chat admission fixtures, assigned to their owners for correction without weakening mode restrictions. The generated OpenAPI drift was the missing network usage projection from the earlier schema correction; regeneration and validation plus 25 OpenAPI tests passed, committed as f710243. Baseline comparison evidence: /tmp/openkit-audience-full-test.log and /tmp/openkit-audience-baseline-test.log. The broad suite is not claimed green.


The independent Consultant rechecked the merged Issue 13/58 and PR 12/63 evidence and withdrew the earlier recommendation to remove server-admin bypass from canonical text alone. Continue with the current authority question pending. Finish and commit the bounded audience correction, then stop expanding local implementation: request the concrete App revision, existing repository bind mount and obtainable exact Worker source revision, and use the next deployment window for a real code-change/review/apply loop.


## Accepted Audience Correction

Independent review accepted the bounded Thread-audience restoration after inspecting final production/test bytes. Central guard/list/count/create-replay/SSE checks passed 87 tests across 12 files; three Quick Chat tests retain their original mode rejection with valid owner-private fixtures. Action Center, scheduler, recovery and generated presentation tests passed 40 tests across ten files; the retained private-source/shared-target projection additionally covers both a different member and the source owner, with seven Generative UI tests passing. A foreign-Workspace Approval using a visible Thread id reproduced 403 instead of nondisclosing 404 before the exact Workspace-owner check and passed after it. NanoCore typecheck and build passed. Cursor service High Load interrupted the final two builders; the primary completed their bounded remaining changes and the independent reviewer accepted those final bytes.

The final full NanoCore run completed with 2,980 passed, 65 failed and one skipped across 275 files. Every remaining failure name also occurs in the detached 3f9065c3 baseline; the generated OpenAPI failure is resolved, and there are no new failure names. Evidence: /tmp/openkit-core-audience-final-test.log, /tmp/openkit-projection-audience-final-test.log, /tmp/openkit-source-audience-final-test.log, /tmp/openkit-audience-final-typecheck.log, /tmp/openkit-audience-final-build.log, /tmp/openkit-audience-final-full-test.log. This is bounded correction acceptance, not a green release gate or completed development migration.

## Next Live Window

Redeploy the final committed batch to the existing A2 staging App, restore the existing bind mount from /home/ubuntu/openkit/workspaces-repos to /srv/repos, and make the selected exact Worker repository source revision obtainable through its configured Git source. Reconfirm App image identity, repository mount and Worker checkout HEAD before starting a new code-changing Task. Use OrcaRouter / DeepSeek Flash Free, inspect the same resulting patch through Web and Skill, and exercise the existing human review/apply path. Do not infer that deploying the App updates the separately pinned Worker source. No push, service change or deployment was performed by this task.

The engineer reported redeployment. SSH inspection confirms openkit/app:staging-96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9, image sha256:cbae64053b485dcaccfd38cae5741284b1a518b2749843eb2737910cfe88154d, started 2026-09-16T06:44:15.993197577Z. The mount list still omits /srv/repos. Skill repository.diagnostics reports both existing repository resources missing; runtime.file-read reports the Worker source commit still 4879812a5c705d46d04f293692a7edec5f810558. Skill doctor and usage.read succeed. Web Overview exposes the seven repaired Open workspace changes links, and Web Usage now renders usage, audit and permission records. These are read-only observations; no approval, deployment or configuration mutation occurred.

The Task icon correction uses the existing Worker glyph with a nonshrinking container, accessible Task name and hover title. The changed Chat suite passed 92 tests; Web build and staged lint passed. The initial updated test exposed an asynchronous fixture race because the icon appears before stream data; waiting for the existing approval-card assertion fixed the test without changing its meaning. Evidence: /tmp/openkit-task-icon-final-test.log and /tmp/openkit-task-icon-build.log.

The Composer model observation was reframed after a bounded read-only Cursor investigation and primary source inspection. The accepted unified Composer specification makes logicalModelId a per-submission preference; omitted future input uses the target's configured preference chain. The existing Worker catalog defaults to gpt-6 while the inspected completed Turn accepted orcarouter. Remounting displays the next submission default, not the preceding Turn's model. No sticky-model or last-AEP hydration change is authorized by that contract, and no such code was added. Live acceptance must explicitly select OrcaRouter on every new submission.


## Intent Epoch 3: Authorized A2 Repair

The engineer explicitly authorized SSH repair of the missing repository mount and stale Worker source, requested a distinction between deployment drift and a reusable product defect, and requested verification of Web, CLI/API and built-in administration-agent repair paths. The primary owns host mutations and evidence; Cursor owns a bounded deployment mount-check regression, while existing Account and Overview ownership remains unchanged. Preserve the current Data Root, credentials, image, network and rollback container; do not infer Git publication authority or synthesize human-gated activation approval. The public source revision selected for this repair is the obtainable origin/main commit 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9.


## A2 Repository Repair Evidence

The installed host helper lacked both the repository mount and linked-checkout synchronization although public origin/main already contained them. The primary preserved that helper as deploy.sh.before-repository-repair-20260916, installed the canonical copy, fast-forwarded the clean linked checkout to 96a6b73d, and recreated only the existing App using its original image, Data Root, protected environment, credential mounts, network and service primitives. Before maintenance all 49 Worker backend sessions were cleaned and all leases released or failed. The replacement is healthy; both repository resources are ready and NanoHost generation 56 is fenced and ready. The source pin was updated through revision-bound public runtime.file-update and safe runtime.reload; the reload reported future-session scope and no restart requirement. A new Web Task th_task_24d2832453c1f23f70822c39 using OrcaRouter reported exact HEAD 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9 and a clean tree. This proves the repaired mount and new-session source, not the full code-change/review/apply loop.

The server-admin Skill actor user_local has no ws_7 membership. Its separate Task probe reached an older scheduler recheck without server-admin request authority and was cancelled before producing a Turn; the wrapper reported scheduler_admission_deferred, obscuring the denial. Existing authority disagreement remains pending, so the bounded correction preserves permissions and reports exact-queue denial truthfully. This is separate from the successful configuration repair and member-authenticated Worker probe.


## Intent Epoch 4: Server Administrator Task Authority

Source: the engineer explicitly selected "server-admin may start Tasks across Workspaces, preserving audit and private conversation isolation" in response to the membership conflict. This resolves the pending authority question. Align the existing Permissions, Identity, remote-auth, multi-user and scheduler owners; retain only the presented non-secret Token ID on existing admission and revalidate it at each later governed Worker effect. Both built-in and Herdr Claude consultants inspected the current chain; a dispatch-only repair is insufficient. Existing sessions and automated triggers do not acquire bearer authority. The primary owns canonical docs and A2 deployment; builders receive disjoint implementation paths.

## Current Repair Checkpoint

The A2 mount and exact-source repair passed a real Web Worker at 96a6b73d. Deployment helper correction 0c5e3984 is installed on A2; full App remains 96a6b73d. Account 776c8723, truthful scheduler denial 6a9da94f and Overview fceaa321 are committed locally. Account and shell browser regressions passed; stale synthetic fixture data needed current model context management and explicit Thread visibility. The next acceptance target is a real nonmember server-admin CLI Task using OrcaRouter. Preserve the single baseline schema contract; upgrade this internal deployment with a backed-up one-time additive column change, not runtime compatibility machinery. Expected observation: the exact credential survives queuing/restart and invalid current credentials cannot cause later effects; foreign private content stays inaccessible.


## Administrator Task Verification

The core admission, authorization, dispatch, Turn route and baseline schema regressions passed 135 tests. Worker inference and MCP regressions passed 51 tests using actual admission, placement and lease records; they cover a nonmember administrator, token revocation and contradictory package lineage. The public Skill interface passed 41 tests, and documentation validation passed 277 documents. Downstream Git and Worker publication review identified late-effect lease checks, exact credential-materialization lineage, private approval-source visibility and provenance publication ordering; those paths were corrected and independently re-inspected before bounded acceptance. User-confirmed Cursor account switching was handled by restarting two existing sessions with their original conversation ids, assigning nonoverlapping read-only checks, and verifying successful provider responses.

The one-time A2 SQLite cutover was exercised against isolated synthetic databases: successful additive upgrade, unchanged backup after subsequent live writes, repeated-attempt refusal, rollback after an injected post-ALTER failure, and private 0600 backup permissions on both success and failure passed. Deployment must retain the old immutable image identity, Web link and protected container configuration, recheck quiescence after both processes stop, and preserve the database backup without overwriting later user writes during rollback. This is operational preparation, not evidence that the new App is deployed.


Final administrator Task slice verification: Worker credential/executor suites passed 142 tests; Git executor, repository routes and Worker repository MCP passed 50 tests. NanoCore typecheck, build, all 661-file package lint, Skill interface checks and documentation validation passed. The full NanoCore run recorded 3,001 passed, 65 failed and one skipped out of 3,067; its exact 65 failing test names equal the previous full-run baseline, including identical existing Git server-fixture assertions. Evidence: /tmp/openkit-admin-full-test.log, /tmp/openkit-audience-final-full-test.log, /tmp/openkit-git-focused-tests.log and /tmp/openkit-admin-effects-final-rows.log. Independent review inspected actual source, regression oracles and these outputs. Revalidation covers current caller Token and Worker lease before later Git network effects; it does not add mid-attempt Vault grant revalidation after material was already resolved. No live Git push was performed.


## Live Administrator Follow-Up

Commit 9788b666 was built from a verified Git archive (SHA256 71ce60028f1cab4bf26102e1ba49c25c50e15800af4bb72c077bd6406c17d98b) on A2. The App image sha256:24fd2ca3e3847088a6b4bbdfcfdcb94dff29822447a5647fd8db85aa10224c79 passed its smoke command. The offline Core backup and additive column upgrade preserved 72 admissions; Web and App switched to that commit, NanoHost became ready, and Skill doctor passed under Node 24.

The real nonmember administrator submission created Task th_task_bae072bc6f47a445f49cd87d and Turn tu_conversation_755286c5c663bc6165254010 with the exact administrator Token origin retained on admission. It exposed another member-only check in retained Worker storage, failing before sandbox creation. A rolled-back local storage reservation was outside the existing pre-sandbox failure marker, so cleanup attempted an absent Sandbox and became fenced. The only unresolved lease belonged to this failed probe; all prior 50 backends were cleaned. An authorized NanoHost-only stop/start produced generation 58 and different fenced, ready, fresh-empty proof. Existing periodic maintenance cleaned the exact backend at 2026-09-16T08:22:05.760Z and terminalized its lease without operator database writes or a Core restart. The primary owns the factory storage authorization and cleanup correction; Cursor owns its exact-lineage regressions, and Claude Code independently reviews the bounded pre-sandbox cleanup correction. The real administrator Task is not yet accepted as successful.


## Bootstrap Inclusion and Storage Follow-Up

The engineer requested review and inclusion of their commit 7d2a0a95 (login-capable first bootstrap owner) in the next deployment. Its real login and bootstrap tests passed 14 cases; App API schemas passed 143 and Core client passed 84. The generated Skill CLI still reflected the previous schema, so c786826 rebuilt it and documented first-owner email/password setup; 42 Skill interface and redaction tests passed. Independent review identified a hash-time expiry race for a bounded follow-up before deployment. Preserve the engineer's commit and use separate corrective commits.

Worker storage now resolves the exact current administrator admission and live lease, preserves same-user whole-volume reuse, and checks each current Thread audience at reservation/reuse. A fully rolled-back pre-Sandbox reservation consumes process-local cleanup proof without touching an absent Sandbox; physical-effect uncertainty still requires existing recovery proof. Related storage/restart checks passed 59 cases after correcting the old fixture's clock only around its initial materialization. No production reconnect or expiry check was relaxed.

Final storage follow-up regression result: 114 passed across factory, storage binding, and restart recovery suites (/tmp/openkit-storage-final-tests.log). The private-audience test deliberately seeds same-user retained provenance for another owner's private Thread; it verifies the visibility gate independently of the existing same-user restriction and preserves the idle binding revision on denial. Cursor released the factory tests; primary inspected and linted their final bytes.

The Bootstrap follow-up refreshes consumption time inside the transaction after current settings and user reads, so asynchronous hashing or lock acquisition cannot preserve a stale expiry decision. The deterministic regression advances only Date while using real password hashing and SQLite and verifies zero partial account state. Final bootstrap and real-login checks passed 15 tests (/tmp/openkit-bootstrap-final-tests.log); focused lint and NanoCore typecheck passed.

## Current Live Acceptance Checkpoint

A2 App and Web now run 238edc47, including the engineer's Bootstrap change, generated Skill contract, live storage authority, and expiry correction. The exact Git archive SHA256 is 4e0e20d64d4833fc158867dad7e405bb261c16884e2c611833b0a6c68dac19e0 and the immutable App image is sha256:709114c8e4c0ddbe78acd0b1f2caa2c0baad5e75a7a972e27bbdddc126ab2381. Production build, image smoke, health, and Skill doctor passed. Activation preserved the existing mounts and protected configuration, verified quiescence before and after stopping services, and needed no additional schema change or rollback. The initialized deployment was not bootstrapped again.

Nonmember server-admin actor user_local successfully submitted OrcaRouter Task th_task_0301e8f99cd6597ea828d913 through the public Skill. Turn tu_conversation_556213e221d9af89ed94dd42 completed in 37,970 ms and reported exact Worker HEAD 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9 and an empty git status. Independent turn.read and the Web Task page display the same completed result. Evidence is retained in /tmp/openkit-storage-live-task.json, /tmp/openkit-storage-live-turn.json, /tmp/openkit-a2-storage-build.log, and /tmp/openkit-a2-storage-activate.log. This settles the administrator admission/storage probe, not the complete code-change/review/apply/publication workflow.

The Worker source remains separately pinned to public 96a6b73d. Source inspection confirms that importing Git objects into the NanoCore linked checkout cannot update the Worker's HTTPS clone. GitHub publication of tested 238edc47 to a separate codex/a2-self-hosted-repair branch was proposed for explicit engineer authorization before the next source update. The remote Bootstrap commit 9c020796 and local engineer commit 7d2a0a95 have identical stable patch ids; preserve both histories without force pushing. Cursor is completing the separately owned Web Workspace selection persistence correction and its account/portability isolation regressions.

## Web Selection Follow-Up Acceptance

The explicit Workspace selection now survives reload as a browser-local UI preference scoped to the current identity and validated against authorized discovery. Account and Portability use that shared validated selection; identity transitions and confirmed membership removal clear departed selection. Route synchronization retains its current authorized Workspace across identity changes. Failed Portability discovery shows the existing error and retry surface with user-scoped Import, withholding project actions rather than claiming no project was selected. This implements the existing DESIGN error alternative without inventing another cache or authorization owner.

The real account browser journey exposed two additional paths within this correction. Creating a Workspace now reconciles the authoritative create response into the existing Workspace query before selection/navigation, avoiding clearance against the old list. Personal Quick Chat retains its owner role but no longer issues sharing-management reads or presents invitations/member controls; its existing owner-only contract and server sharing rejection already require that distinction. The unchanged end-to-end creation and post-leave assertions then passed. UI-only storage assertions allow only the exact selection envelope while continuing to exclude credentials, content, and departed Workspace identifiers.

Final verification passed 372 focused tests across six files: 82 Account tests and 290 Chat, Workspace, selection, and Portability tests. The isolated server account-sharing browser journey passed, Web TypeScript/Vite build passed, and all 11 changed code/test files passed Biome. Evidence: /tmp/openkit-account-selection-final-tests.log, /tmp/openkit-selection-final-tests.log, /tmp/openkit-selection-account-e2e.log, /tmp/openkit-workspace-selection-build.log, and /tmp/openkit-selection-lint.log. Independent review inspected the final production and test bytes and accepted the bounded correction. These Web-only fixes introduce no new API or Skill operation and have not yet been deployed to A2.

## Next Deployment And Development Attempt

The preceding goal turn made verified progress through committed fixes and live administrator Task evidence. Current SSH observation still identifies App/Web 238edc47 and a ready Skill connection. Prepare a Web-only build of committed 5b55aa30 using the existing A2 Web Dockerfile and publish primitive; expected observation is the exact new Web asset identity with the same App image, container, and NanoHost interval. Retain the old asset target for rollback and do not restart services or synchronize linked repositories for this UI-only update. The source archive SHA256 is 1a7b4bd6bc60f97afb589226b747b24bfbf93b0d3abc2ab356d5e1d01e79c8fb. GitHub publication remains pending explicit authorization; continue independent work and do not treat it as proof that the whole goal is blocked.

The next meaningful workload should correct an observed product defect through a bounded Worker Task and inspect its resulting review surface. Investigate the current Task instruction rendered as raw serialized Worker input before choosing that workload. The publicly obtainable 96a6b73d source remains valid for a defect whose owning paths are unchanged; compare those exact paths before dispatch. Do not call this a complete development loop until actual code, checks, review, and any required human decisions are observed.

## Web Deployment And First Code Task

Web-only publication of 5b55aa30 succeeded using the installed Web image publish primitive. Image sha256:e921bd8ff241e269832d40da6d4e30b264c1310e9a87c3d9a3ae5d326300508e serves the verified archive; the App container id and start time, App image 238edc47, and ready NanoHost generation 59 remained unchanged. Health and Skill doctor passed. Live hard reload preserves OpenKit selection on Overview and Account. General settings Reset restored an unsaved draft without saving; Catalog transport selection changed its relevant input without creating configuration; Recovery was empty; Archived opened existing history without restoring it. Theme selection exercised Noir and Spectrum and restored Paper; the Noir navigation icons remained visible. Configuration, App update, Debug, Administration, access-token listing, server audit, Vault backend, deployment backup, access recovery, and user-disable surfaces loaded; sensitive controls were inspected without activation, rotation, deletion, or access changes. Debug dialog opening and dismissal succeeded. These are bounded observations, not comprehensive functional acceptance of those administration effects.

Portability reproduced an enabled Export action returning access denied and a Vault references permission denial presented as a generic retryable load failure. The selected browser actor is an editor; this is distinct from the Skill administrator bearer. Claude Code recommended a truthful typed-error presentation correction while preserving backend policy; no client-side role-policy duplicate or permission widening was selected. The new Task request 524efcf1-970c-4942-8d31-47b7df8041ec explicitly chooses orcarouter, owns only the bounded Web correction inside its Worker checkout, prohibits publication/deployment, and requires actual edits plus focused verification. Durable Task th_task_b5ee815ea53b3d0bc444718e and Turn tu_conversation_5464ed54f169cad8ea95ba01 are running, visible in Web and Skill. Expected next evidence is a real reviewable patch; a prose-only completion or unavailable patch reframes the development-loop premise.

The fresh direction consultation returned Continue for this first bounded code Task, with the next independent direction observation required when its patch reaches public review/apply. Raw structured Task input remains an observed presentation problem, but separating its display from execution bytes was not implemented during this attempt. The mid-slice continuation re-read the checkpoint, current Git, Skill guidance, and the completed Claude recommendation; it did not dispatch another redundant direction check. Additional read-only findings remain to assess after this patch: server audit presents old events without visible timestamps, token ownership is shown as raw user ids, and Vault backend status is available while its inventory reports access denied.

## First Code Task Failure And Reframe

The first code Task failed after 442,425 ms with worker_governance_turn_failed and no patch. Its initial Skill call returned connection_failed after roughly five minutes while durable state remained running; no duplicate submission was made. Public usage retained 129 OrcaRouter inference calls, 120 successful and nine failed. The last successful request reported 128,000 cached input tokens plus 526 uncached input tokens, followed by immediate failures without input usage. The native transcript confirms 121 shell calls, primarily exploration, and a final stream-disconnected error. This strongly suggests an exhausted context window, but the provider's actual rejection detail was not retained, so the exact upstream cause remains unproven. The failure also produced a misleading Worker Turn accepted status Item; a separate bounded regression and copy correction is assigned to Cursor.

Live Provider metadata declares context 111,000 and gateway policy sets compactThreshold 8,000. Exact AEP version 4 has no context-management projection, and retained native Codex configuration has no explicit context window or compaction threshold. Owner inspection corrects the initial missing-field-bug hypothesis: the accepted compaction specification explicitly permits native defaults until a coordinated AEP/adapter change. Claude Code recommended the already-owned first conformance step: prove pinned Codex 0.153.4 threshold control in an isolated experiment before implementing that projection. No ambient Worker configuration workaround or unsupported conformance claim was made.

The synchronous submit/receipt lifecycle is separately confirmed by independent review. The current Composer owner waits for the complete downstream Task tuple and has no post-response outer settlement path. A concrete engineer question proposes a durable launch receipt followed by status/SSE with exact request-id recovery; dependent redesign waits for that answer. Local patch review/apply and GitHub publication are separate effects: the former uses the existing Workspace Sync Review decision, the latter still awaits publication authorization.

Claude Code returned Continue for an independent, smaller edit-to-review attempt while native compaction proof proceeds. Request 375ec5d4-85d2-40f1-82a3-824a90e77448 targets only the known Vault permission error render and its existing focused regression, with explicit OrcaRouter selection and no backend/query redesign. Its expected observable is an actual small patch in public review; success would prove only bounded patch collection, never long-session compaction. The previous broad exploration method is not repeated.

Exact replay after the failed Task became terminal returned the original Task/Turn and accepted command receipt without creating another Task. Its returned Turn still correctly reports failed; this confirms why command acceptance must not be reported as execution success. The public Skill loop reference now states that distinction and the disconnect inspection path. The narrower Task is th_task_8a9987faf1f1c619b32fc84e and was independently observed running.

Read-only host inspection confirms the failed Task left its exact Worker checkout at 96a6b73d with an empty porcelain status; this failure produced no code changes to collect. The Skill reference update passed all 41 interface checks and was copied to the installed Skill. Evidence: /tmp/openkit-task-status-skill-tests.log.

## Worker Result Presentation Acceptance

The selected-Worker conversation now persists truthful failed and interrupted status Items while preserving accepted command receipts. Summaries use fixed product-safe text rather than copying raw Worker diagnostics. Exact replay validates the persisted Item against the durable Turn and returns that Item; contradiction returns the existing recovery_required result without another execution. Independent review caught and corrected synthetic replay fields and raw-error propagation before acceptance. The final focused route and RuntimeEvidence checks passed four cases with 170 unrelated cases skipped; NanoCore typecheck and focused Biome passed. Evidence: /tmp/openkit-worker-status-final-tests.log and /tmp/openkit-worker-status-typecheck.log. The matching Skill command-versus-execution guidance is already committed in 50ce632b. This code is not deployed while the live narrow Task remains running.

The isolated pinned Codex 0.153.4 probe reports 9,000 input tokens, below the authored physical window of 111,000. Native threshold 8,000 triggers compaction before resumed inference and continues the same native thread; the otherwise-identical 16,000 control does not compact. Primary inspection of the actual paired evidence supports configurable native threshold control on resume, not yet within one long tool-running Turn or through the OpenKit adapter. The next bounded probe tests that missing within-Turn observation using only a harmless local tool and localhost inference. Evidence stays under temp/changes/202609160700000000-self_hosted_development/codex-compaction-probe/. No AEP or production runtime projection changed.

## Dependency Download Reframe

The narrower Task spent over 17 minutes waiting on its own pnpm install behind a buffering tail command. Its exact retained checkout had no tracked edits. Public turn.interrupt succeeded and durable state became interrupted at 2026-09-16T10:16:14.239Z; the deployment was not restarted and no new Task duplicated the running attempt. The current App still displays the old accepted status Item, as expected before deploying 5851ab3e. The package manager process had proxy variables but no NODE_EXTRA_CA_CERTS or NPM_CONFIG_CAFILE; this is a diagnostic clue, not proof of a trust failure. AEP inspection confirms the npm registry grant, and Gateway evidence records sixteen allowed Node connections to registry.npmjs.org at 09:57, refuting missing egress permission. Subsequent denied Codex telemetry connections are unrelated. Evidence: /tmp/openkit-small-task-interrupt.json, /tmp/openkit-small-turn-current.json, /tmp/openkit-small-aep.json, and /tmp/openkit-small-gateway-log.txt.

Fresh Claude consultation returned Continue for one bounded package-metadata download through the public Worker interface. Run pnpm view first with a 20-second process limit, disabled retries, and unbuffered output; the expected observable is an actual network result or error class, which distinguishes slow installation from a broken runtime network path. Do not disable TLS, widen policy, alter ambient configuration, or launch another code-edit workload before this premise is resolved. Native compaction proof proceeds independently; it does not repair package installation.

The bounded public diagnostic completed on the same Task as Turn tu_conversation_787ccb0575b678826b760aab. `timeout 20s pnpm view react@19.2.0 version --fetch-retries=0 --fetch-timeout=10000` returned exit 1 and SELF_SIGNED_CERT_IN_CHAIN in about half a second; the native function-call output independently corroborates the public report. The live Harness supplies a readable SSL_CERT_FILE at /etc/openshell-tls/ca-bundle.pem with 151 certificates, but the native child has no Node additional CA mapping. This defeats the inference that an allowed flow proves successful TLS or completed package fetching.

Claude confirmed the existing backend trust propagation and Git mapping as the owner for the bounded Node repair. The shared Worker child environment will derive NODE_EXTRA_CA_CERTS only from its already-admitted SSL_CERT_FILE, while reserving that Node control key against runtime credential declarations. It remains excluded from ambient inheritance; no TLS verification, host grant, or AEP schema changes. The runtime credential name path adds only the declared names and fixed OpenKit route-token names, and the live AEP declares only GITHUB_TOKEN. Cursor owns worker-shim cli.ts, its existing native-child regression, and the package guide; primary owns the Skill recovery reference and this record. Expected evidence is derived CA in the real child environment, ambient and credential injection rejection, followed by the identical package-download probe succeeding on a rebuilt Worker image.

## Node Trust Repair Acceptance

The shared child-environment repair passed independent diff review, all 89 Worker CLI regressions, Worker typecheck, Biome over 28 files, Worker build, and all 42 Skill interface/redaction checks. The native-child regression proves that the admitted SSL_CERT_FILE wins over ambient NODE_EXTRA_CA_CERTS, directory-only configuration invents no file, and runtime credentials cannot target the derived Node control key. These checks establish environment propagation, not a live TLS handshake; rebuilding the Worker image and repeating the same bounded pnpm metadata request remain required. Evidence: /tmp/openkit-node-ca-tests.log and /tmp/openkit-node-ca-skill-tests.log.

The completed isolated Codex 0.153.4 probe also verifies within-Turn compaction: with 9,000 reported input tokens and physical context 111,000, threshold 8,000 compacts after a successful local tool result and continues the same Turn, while threshold 16,000 does not. Independent inspection confirmed the raw request sequence. The final oracle includes the tool result carried in the compaction request, preserves the earlier rejected oracle in the report, and both final paired observations pass. This proves bounded native configurability only; no production AEP projection or adapter conformance is claimed.

## Worker Image And Administration Follow-up

CA repair 02cf9c06 is committed and its recovery reference is installed. An exact Git archive built on A2 through the canonical worker-codex build script passed the canonical image smoke. Docker's default multi-manifest provenance archive was rejected by the existing single-platform image importer; exporting the same built image with docker image save --platform linux/arm64 produced the accepted manifest sha256:b1ce07de7838b7fbeaadfd8046d809a4e85abf5549ff30b63e66f7f99f190514. NanoHost import succeeded without service restart. The image is available but not activated; the original App/Web and Worker Agent configuration remain in use. Host evidence is under /home/ubuntu/openkit/artifacts/worker-node-ca-02cf9c06*, including build and smoke logs. An earlier import met the independent inventory read's store lock and was retried only after that read completed.

The supported preparation path exposed an independent administration prerequisite defect. First administration.conversation-submit with the active user_local server-admin bearer returned workspace_access_denied before creating its private Thread (request 28c2e4e1-551c-40a5-aceb-e411ba8edb37). Independent read-only Core inspection confirms the actor is active and owns no registered Quick Chat home; server-mode bearer authentication does not invoke the existing home provisioner, unlike browser session authentication. The primary implements the bounded shared-resolver repair using ensureUserQuickChatWorkspace for the validated actor only; Cursor owns its regressions, preserving private Thread isolation and denying invalid credentials before provisioning. No database repair, membership grant, alternate administration Thread, or activation bypass was attempted. Evidence: /tmp/openkit-node-ca-admin-input.json and /tmp/openkit-node-ca-admin-result.json.

The first foreign-Thread regression incorrectly expected the central authorizer stub to enforce administration body validation. Its observed 200 response is a harness/oracle mismatch: the actual administration route restricts lookup to the actor home and returns 409 target_missing before creating a Turn. Independent review resolved the owner; move that assertion to the real route instead of introducing redundant generic parsing. The fresh-actor provision and removed-membership tombstone checks remain on the shared authorizer.

Independent review accepted the final narrow resolver and corrected real-route oracle. Both focused files pass all 37 tests, focused Biome passes three files, and the Skill interface passes 41 checks; documentation validation passes 277 documents. Evidence: /tmp/openkit-admin-home-final-tests.log, /tmp/openkit-admin-home-skill-final.log, and /tmp/openkit-admin-home-doc-check.log. The exact committed App will be built and smoke-tested before an idle A2 maintenance cutover, retaining the current Web bundle and existing Bootstrap fixes.

## Administrator Home Live Verification

App e3e8f4310e632a023cbec68f484c0e93a7ce8c41 passed NanoCore typecheck, canonical A2 build and App smoke, and was deployed as image sha256:abd89ffd1ee77c644e5821b86882b15fbfc03b77cc9c80a4917bd60f01fc7a63. The exact source archive SHA-256 is 875af16aec9621dd9050623e8c44b99ca25b78a3ef2f9b785b696576d3d20621. All three quiescence checks were zero, the App health and refreshed Skill doctor are ready, and the externally mounted Web remains 5b55aa3027ab569be5dd9375e6e0b0f58b3cc8d7. The user Bootstrap commit 7d2a0a95 and expiry correction 238edc47 are ancestors of this exact deployment. A connection check during replacement returned 502; the post-cutover check succeeded. Build, smoke, cutover and recovery metadata are retained under /home/ubuntu/openkit/artifacts/admin-home-*.

Retrying original administration request 28c2e4e1-551c-40a5-aceb-e411ba8edb37 now creates the actor-owned active ws_quick_chat and private administration Thread th_administration_4d6dcf637baed70d5399dd4d, replacing the prior guard 403 with the actual model-admission result. Its Turn is truthfully failed with context_compaction_unavailable for OrcaRouter; no inference success is claimed. Cursor is tracing that separate model prerequisite while public worker-environment.prepare uses the now-valid private administration Thread. Evidence: /tmp/openkit-admin-home-live-result.json and /tmp/openkit-admin-home-doctor-ready.json.

Public Worker preparation request 2c1f42b8-8b11-4750-ad4d-8b24805f6e44 then failed with effect_failed because worker_image_settlements is absent. Read-only Core inspection confirms no image settlement table and only core_0000_setup plus core_0001_physical_epoch_cutover in schema_migrations. No image activation occurred. The next bounded investigation traces the owning scoped migration and actual prepare connection before selecting an authorized product correction; no ad hoc SQL repair is accepted. Evidence: /tmp/openkit-node-ca-prepare-input.json and /tmp/openkit-node-ca-prepared.json.

## Administration Admission Diagnostics

OrcaRouter's free-model alias had an authoritative physical context limit but no tool_call metadata. The existing admission guard conflated missing capabilities with missing compaction policy. The narrow route correction reports model selection or capability refusal using administration_execution_failed and retains context_compaction_unavailable for a missing policy after capabilities admit. The actual route regressions pass six tests and focused Biome passes both files. The public Skill reference and NanoCore guide describe the same distinction; a successful command envelope still does not imply a successful Turn.

Public runtime.file-update added only tool_call=true to the existing model metadata under the exact revision guard. Readback revision is sha256:07c89dc3fe625482ad7752bdded212b0adb8140b27d611e0eb49e9b9c4b3fd7c. Dry-run reload required a Provider restart; the later bounded schema-repair restart loaded that configuration. Fresh administration inference verification remains outstanding.

## Intent Epoch 5: Remove Six Old Workspaces

Source: the engineer's instruction to delete the six old Workspaces identified by schema comparison. Live inspection distinguishes two fixture project Workspaces, ws_2 and ws_3, from four still-active personal Quick Chat homes belonging to Administrator, Staging Administrator, Fixture Skill Owner, and Grok Bot. The personal-home scope question remains pending because deleting deterministic homes can block subsequent sign-in; none of those four homes was modified. The two fixture projects follow the existing documented administrator ownership-recovery then owner-authorized deletion path.

## Schema Restoration And Deletion Checkpoint

Read-only comparison of sixteen scoped databases against exact e3e8f431 setup found one missing Core worker_image_settlements table and six older goal_records tables missing nullable worker_storage_choice_json. Remaining SQL text differences are formatting or existing column order, not missing semantics. Claude consultation and independent script review accepted only the bounded additive restoration under existing authority, preserving every old table's row-count/content digest and migration ledger. After the deletion request, the write scope narrowed to Core plus ws_2 and ws_3; four personal homes remain unchanged. The exact three-database repair passed a private-copy rehearsal, refuses optimized Python, requires real paths inside the data root and the exact unique operation set, and ran only with NanoCore/NanoHost stopped after zero active leases, pending backends, or queued admissions.

The full cold backup at A2 /home/ubuntu/openkit/artifacts/schema-repair-delete-two-cold-backup/data-root.tar was compared against the stopped source before mutation; SHA-256 is d8170ce26fee56797665cbaedf0ae8a482298d5e84edce690336a7c3510cb80a. repair-result.json in that protected directory retains per-table before/after equality evidence: 41 Core tables and 37 tables in each project, all unchanged; only the empty Core table and nullable columns were added. Same App e3e8f431 restarted successfully, NanoHost resumed, and public doctor reports ready. No migration ledger entry or generalized upgrade mechanism was invented.

ws_2 deletion request cae8ee19-e909-42b4-8612-52f37638d568 reached deleting revision 3 but recovery export initially failed on the old schema. After repair, exact retry exposed a separate central-authorizer defect: its non-content original-owner retry rejects server-admin bearer scope. Cursor owns the bounded guard/regression correction. ws_3 deletion request 3f1bb14d-9263-4b77-b5aa-666764820037 remains fenced, registry active revision 2, because five failed leases retain needs-evidence despite cleaned backends. No deletion has completed. The proposed containment-only refinement requires an engineer decision before implementation; unknown historical results must not be fabricated or cleared.

## Original-Owner Deletion Retry Correction

Independent review accepted the central-authorizer correction: a durably validated server-admin bearer can enter only the non-content deletion retry path when its active canonical user equals the retained original registry owner. The ordinary deletion route still binds request id, original owner, expected revision and path-specific confirmation. Foreign, revoked and disabled-owner tests deny entry; ordinary dashboard content remains denied. Focused authorizer and deletion-route suites pass 50 tests across two files, focused Biome passes, and final NanoCore typecheck passes. The public Skill recovery guide and NanoCore guide describe the same boundary. This restores S15's existing retry contract rather than granting a new administrator deletion bypass. Evidence: /tmp/openkit-deletion-retry-final-tests.log and /tmp/openkit-deletion-retry-final-typecheck.log.

## Deletion Retry Deployment And Live Result

App b9e031f13e1a3625d6b0458d4ae42aba0de4e36e built from source archive SHA-256 e26e036dc889e788813ac23be31a4a32532d03b51df491df97e6b81e8ef924f3 and passed the canonical App smoke. A2 now runs image sha256:ee1641ade6c15e4274e0e84f7d80130afe3c763523038210ff8c256a0e38842a; the existing idle-maintenance helper preserved Web and storage, App health and public doctor are ready, and NanoHost is active. Build/smoke/cutover evidence is retained in /private/tmp/openkit-deletion-retry-{build,smoke,cutover}.log; A2 keeps the exact source tree and container recovery metadata under its protected artifacts directory.

Exact original ws_2 request cae8ee19-e909-42b4-8612-52f37638d568 now completes through the public Skill. Its authoritative response and read-only durable verification agree on deleted registry revision 4, phase cleaned, absent canonical Workspace root, no retained staging, recovery export wsexp_delete_cae8ee19-e909-42b4-8612-52f37638d568, and closure wsclose_delete_cae8ee19-e909-42b4-8612-52f37638d568. Cleanup completed at 2026-09-16T11:42:38.701Z. This verifies the deployed retry correction, not merely a simulated guard.

ws_3 remains active revision 2 with its existing fenced deletion request; five failed needs-evidence leases are the known blocker. The four personal homes remain active revision 1 with their roots intact, and ws_7 remains active revision 1. No unknown historical result was cleared, no private home was deleted or reassigned, and no deletion-safety predicate was changed while the engineer questions remain pending.

## Intent Epoch 6: Authorized Cleanup

The engineer explicitly authorized deletion after the two cleanup-scope questions. Two project Workspaces use permanent deletion; four personal Quick Chat homes receive a one-time operator content reset while preserving their deterministic identities, Core ownership and usable login provisioning. The user-facing report must distinguish deletion from reset. Protected cold backup and exact-target verification precede the reset. No general personal-home reset endpoint or administrator private-content grant is introduced.

S15 now permits a failed needs-evidence lease to stop fencing deletion only with existing exact-lineage cleaned backend evidence and physical_cleaned_at. The runtime predicate and route regressions implement that boundary without changing the unknown result. Independent review checks the actual diff; focused tests and deployed exact-request ws_3 replay provide separate implementation and live evidence.

Independent final review accepted the exact predicate, the five-lease positive case and one-missing-proof negative case. All 23 focused deletion-route tests, NanoCore typecheck, two-file Biome and 277-document validation pass. Evidence: /private/tmp/openkit-quiescence-tests.log, /private/tmp/openkit-quiescence-typecheck.log and /private/tmp/openkit-quiescence-docs.log. The staging-only helper now admits canonical /staging after realpath and ancestor guards; its container mount must point exclusively to the separate empty artifacts staging directory.

## Authorized Cleanup Live Checkpoint

App fd3aaf9de0f0d6d90365f85ac0500f6154f43182 passed canonical build and smoke and is deployed as sha256:e71e726c4534c65304ca499645cd0d43aa2c227befb0a91066341c7ac4b4feaa. Its exact archive digest is d50d0405851dfed4fda28a4edd22857394118e356aa03b93745cd0595f7878f0. ws_3 exact deletion request advanced from fenced to deleting revision 3, proving the cleanup predicate accepts the retained matching evidence, but portable export now refuses Worker Context Package backend handoff mismatch. An isolated source-copy reproduction confirms that separate failure; no history was discarded or rewritten.

The four authorized personal-home content resets completed after independent script review, a protected-copy eight-rename rehearsal, canonical staging under the deployed image and isolated owner-provision checks preserving all copied Core rows. Stopped-service preflight verified exact active owners, membership, no retained evidence or legal holds, no unrelated Core Workspace records, and no active runtime. The full cold backup was compared to its stopped source before swapping; SHA-256 is 1e8c0e02f9f0574b693ceae23a6bc9641898e397240b5b4215c938ef57346229 at A2 /home/ubuntu/openkit/artifacts/four-quickchat-reset-20260916/data-root.tar. Original roots remain under that protected directory's reset/old-roots. All four homes now contain zero Threads and zero Artifacts with current schema; registry identities, active owners and revision 1 remain unchanged. Every non-target file hash matched before restart, including Core, User and ws_7 data. App health and public doctor are ready after restart. This is content reset, not deletion of four personal identities. Evidence: /private/tmp/openkit-four-home-rehearsal.log, /private/tmp/openkit-four-home-cutover.log and the protected A2 reset journal.

The OrcaRouter alias compaction threshold was raised from 8,000 to 80,000 under exact runtime.file-update revision guard, retaining its 111,000 physical limit, and hot reload applied successfully. The prior 8,000 threshold was below the approximately 11,132-token initial administration context. The subsequent read-only diagnostic reached internal_agent_model_turns_limit after 36.5 seconds instead of context_compaction_unavailable; this does not prove successful administration completion or an implemented compactor. Result: /private/tmp/openkit-admin-orca-threshold-result.json.

## Intent Epoch 7: Full Backup And Fresh Rebuild Rehearsal

The engineer superseded the six-Workspace cleanup strategy. Preserve the current deployment user data in a separate protected archive, then treat A2 as a fresh OpenKit server. Initialize the two accounts from /Users/m5pro/Downloads/openkit-a2-admin-accounts-20260911.json with their existing email/password pairs. Restore useful server, LLM Provider, MCP, Skill, Knowledge, GitHub Vault and related credential configuration through supported mechanisms; credentials never enter reports or prompts. Old conversation history may be used only for bounded export/import tests and need not be fully imported. Do not invest in compatibility fixes for historical development-only schema drift; correct general backup, restoration and setup defects that affect supported user workflows, including the public Skill. ws_7 need not survive as an identity.

Execution now distinguishes a static same-deployment restoration check from fresh-deployment resource restoration. Cursor read-only lanes own backup/restore contract discovery, credential/configuration portability and initial-user bootstrap; Claude provides independent direction review before the destructive rebuild. Primary owns live inventory, all credentials, snapshots, deployment effects and acceptance. The earlier direct six-Workspace purge was never executed; current state is App fd3aaf9d, ws_2 deleted, ws_3 deleting with export blocked, and four personal homes reset empty.

## Rebuild Rehearsal Checkpoint

The authorized concurrent merge of 9c020796 completed as 76cd1753. Its Bootstrap account creation was already present; both resolved files retain the later post-hash expiry check. Independent diff review, 15 focused Bootstrap tests, NanoCore typecheck and focused Biome passed. Packaged stopped-server restore is committed as 50b847c1, reusing the existing restore engine and documenting the required writable parent/child bind mount; source CLI help/error checks and four focused restore tests pass. Runtime image acceptance remains pending.

Public hot backup drb_6f410a91-e7b2-4406-90a7-a984a0e0e2d7 passed create and manifest verification, but the deployment omitted its sibling backup mount. Its 980-file tree was rescued from the stopped container to the protected rebuild directory. Full static backup at A2 /home/ubuntu/openkit/rebuild-20260916/server-static.tar includes Data Root, external credentials, linked repositories, Web assets, host configuration and all /var/lib/openkit runtime storage. SHA-256 is 4b138eca8b34c7d112f6c0f16bbc7ab2e72a9fc9cfe9fcd135c37e48e06acf9a. Comparison against stopped sources and isolated extraction passed; a static restored App booted healthy with no network. The original App and NanoHost restarted, and public doctor reports ready. No fresh-server cutover has occurred.

The public hot-backup restore copied 980 files successfully but failed boot because recursive Node cpSync changed Vault directories from 0700 to 0755. Static tar preserved 0700. This is a general backup defect, not historical schema drift; the source hot/cold/restore copy seam is being corrected and must pass a real restored boot before acceptance. A separate launcher correction persists the sibling backup mount through dogfood, local Docker and App-update replacement paths. Cursor owns bounded implementation; primary owns acceptance and all host effects.

An isolated fresh root now contains only copied runtime configuration, three validated server credential references and the existing subscription slot with its matching encrypted store header, reference directories and original key. It contains zero Users, access Tokens and Vault grants. The archived ws_7 GitHub token will be resolved through the audited backend into a new Workspace secret; no old Workspace grant is copied. NanoHost retains its matching configured transport identity and performs its own physical-epoch replacement on start. A current-format ws_12 rehearsal export contains a single immutable marker Artifact, digest sha256:0f9717932d8c4d927e66c848a30b3b658e33e10dcab8f8f09a692919fe070ac5, and passed public export/dry-run. Its protected export is retained separately for portability testing; old conversation compatibility remains excluded.


## Fresh Deployment Cutover And Restoration Evidence

Commits 9999ce00 and ad75976b fix persistent sibling backup mounts and preserve directory permissions through the shared backup/restore copy seam. Fourteen backup/restore tests and focused Biome pass; launcher checks pass 14 dogfood, 10 Docker-run, 51 update-helper and three fixture tests. The actual packaged image restored a 979-file hot backup, preserved Vault mode 0700, and booted healthy with network disabled. Evidence: /private/tmp/openkit-fixed-hot-proof.log and A2 /home/ubuntu/openkit/rebuild-20260916/fixed-hot-restore-probe/data. Historical defective backup bytes remain unchanged as evidence.

The fresh A2 cutover now runs App ad75976b, image sha256:0ec4e5676fe1e96f9f3d531f349ef1b3582c930b23bfc46bf4009823935de01a, and Web bcc82ec4. Its new Data Root deployment marker is dep_4259eb63-a942-4bfc-ac45-30f6052bbc57. The old root, Web and stopped container remain separate rollback material; the 8.2 GB static archive and its verified hash above remain intact. The first Web request exposed an operator packaging mismatch: staging Caddy roots /srv/web/current while the copied dist was flat. Moving the bundle into the required current directory restored public HTTP 200; the cutover preflight now checks that layout and HTTP result. This was not presented as a product-code defect.

Both original accounts were recreated through public first-owner bootstrap and signup with their original password pairs, followed by canonical session and administrator-access checks. No old User, access Token or Workspace authority rows were copied. The new shared project is OpenKit, ws_4, with the new administrator as owner and staging account as editor through the canonical invitation/acceptance flow. Three server credential references and their matching encrypted Vault material and external key were restored; the subscription reports logged_in. The archived GitHub credential was resolved through the audited backend and enrolled as a fresh ws_4 secret, with separate host-push and Worker runtime-env grants. The repository is publicly diagnosed ready at /srv/repos/openkit. Configuration and Knowledge source files were restored; inventory found no additional published MCP or Skill catalog contents to migrate. Runtime reload applied successfully. OrcaRouter completed a public Assistant conversation in th_1 with the exact requested RESTORE_OK reply.

The canonical-session current-format source export wsexp_5a8d6cdc-84e9-4cc4-8600-0da6475bfb81 downloaded as a 2487-byte portable archive, SHA-256 5db4aa433f44142aaf0690dea16ae989948e62c650c985977d3b8ea8d0076655. Public dry-run and import into the fresh deployment succeeded, preserving source lineage and reminting the Artifact as ar_imported_ws_13_1. Its exact content and digest sha256:0f9717932d8c4d927e66c848a30b3b658e33e10dcab8f8f09a692919fe070ac5 match the source marker. The first read incorrectly used the pre-import Artifact ID and returned 404; collection inspection found the reminted identity and verified it without repeating import. Evidence: /private/tmp/a2-portable-inspect.mjs.result.json. Old historical conversation compatibility remains deliberately excluded.

Web commit bcc82ec4 adds native archive download and file selection to the existing Portability operations, binds review to the exact selected File, preserves request identity on retry, and prevents source changes during import. Forty-seven focused tests, Web typecheck, Biome, build and independent review passed. The Skill documents canonical-session server authority and local-only CLI archive operations. Browser verification of the new file controls remains pending a fresh sign-in; API archive behavior above is verified separately.

Fresh public backup drb_5d69bcfc-2576-4b6c-ad80-f138df053a4f verified all 73 files with manifest sha256:6c69c5e14abe47838f909e27346353b91b3475e3b1e8bec41ebd1019ee30f25d. Its actual files persist on /home/ubuntu/.openkit.backups and Vault mode remains 0700. This snapshot precedes subsequent NanoHost re-enrollment. Public Skill doctor reports ready through the new stored administrator credential.

A fresh Core intentionally lacks the old NanoHost transport authority. The initial host admission returned 401. After stopping NanoHost and archiving its exact old slot pair under previous-nanohost-slots, public nanohost.enroll wrote the empty A slot and durably issued the new transport record. Its last-used evidence confirms successful admission after restart. The subsequent readiness check still fails: runtime-target reports ready=false, freshEmpty=false, predecessorFenced=true at connection generation 1. Cursor is tracing that separate execution-environment prerequisite; no Worker readiness or complete self-hosted development loop is claimed. Commit dd1bb43c documents the distinction between same-deployment restoration and fresh trust enrollment in both operator interfaces. Documentation model validation, index check and Skill reachability pass.


## NanoHost Rebuild Budget Finding

Read-only host inspection and Cursor source tracing identify the exact readiness refusal: /var/lib/openkit/nanohost/.rebuild-fence-started retains 2026-09-16T13:31:16.054110Z, created at the first cutover startup before fresh transport enrollment. Its age was 1080 seconds when inspected, beyond the existing 300-second hard rebuild bound. Startup itself reaches healthy Gateway in approximately two seconds, but main.rs rejects the retained unresolved interval before publishing readiness. The public false flags are pre-readiness defaults, not evidence of missing Worker images. The existing Display deliberately omits its internal reason; a regression explicitly protects that omission, so no producer weakened that check merely to improve logging.

The accepted runtime owner requires preserving the first timestamp across failures and permits its consumption only after readiness acknowledgement within the remaining deadline. No marker was deleted, rewritten or bypassed. Claude is reviewing whether the authorized full fresh-server installation supplies an existing recovery path; a general explicit operator retry would require an accepted owner decision before production changes. The live App, accounts, restored configuration and Assistant remain usable; the execution host is stopped and non-ready.

A second fresh public backup now includes the completed transport enrollment and provider conversation: drb_56e77cbf-5edb-4380-8bfe-0e3c017bb245, 83 files, 3307874 bytes, manifest sha256:fbb8a4a2e0d8e7069bf6fec0c64d806e88d5553890799f82953d7ca12e83ee67. Public verification passed, the host-mounted manifest exists, and the backed-up Vault directory is mode 0700. Evidence: /private/tmp/openkit-fresh-final-backup.json and /private/tmp/openkit-fresh-final-backup-verify.json. This is a truthful non-ready execution-host checkpoint, not a claim of Worker acceptance.


## Complete NanoHost Retirement And Successful Reinstallation

Claude consultation reconciled the expired-marker rule with the already accepted complete installation retirement and exact-pair reinstall lifecycle in docs/cookbooks/persistent-live-acceptance.md. The engineer's fresh-server rebuild authorization covers that whole lifecycle; it does not authorize pretending that an old unresolved interval completed. No new operator reset command or clock-reset rule was introduced.

The stopped service had MainPID=0, an empty control group, no epoch processes, active epoch sockets, Gateway listener or mounted epoch paths. The non-secret failure report was imported and read as ws_4 Artifact ar_import_3d7218f74e04f5a18144b6ea version 1. Public nanohost.decommission revoked the sole transport Token and fenced the current connection. Independent review accepted the archive helper after adding an explicit Python optimized-mode refusal and exact-root mount checks. It archived all retired NanoHost state, image, evidence and persistent-work roots under /home/ubuntu/openkit/rebuild-20260916/retired-nanohost-installation, and the dead runtime root under /run/openkit/retired-nanohost-20260916. The live bind-mounted credential directory retained its inode and was proved empty. The original full static archive also retains the pre-rebuild installation.

Public enrollment then recreated only the configured staging-nanohost-a2 / staging-a2 pair before first service start. On 2026-09-16T14:00:21.713Z the authoritative runtime-target read reported connectionGeneration=2, predecessorFenced=true, ready=true and freshEmpty=true. Verified local image import restored the configured sha256:8497c302fdd90b4a67b45f699b3924ea6768c64f3babdff1f6ba0da82bc9439d and the separately built certificate-fix candidate sha256:b1ce07de7838b7fbeaadfd8046d809a4e85abf5549ff30b63e66f7f99f190514. Import did not activate the candidate or silently change the Agent manifest.

Public worker-environment.prepare now succeeds against the fresh schema and ready execution host, request b50a681f-ede6-4121-bd76-533be308ad70. The resolved candidate is ar_worker_environment_resolved_1519df55574fa1fd00c1b939 version 1, content digest sha256:2cac91c1dab30532b51b98872eca5f839f763d899c3258097f605171b987ddb6, targeting agent_codex_host with configuration revision sha256:901db93bf746df482c7362c803641f552f44ed866e9c2601d8465b2f55348bf4. affectedStorage is empty and replaceNow is null. The complete preparation result was shown to the engineer and exact-payload activation confirmation remains pending. No Worker acceptance is claimed yet.

A private OrcaRouter administration probe completed but answered incorrectly that NanoHost was unconfigured because there was no same-named LLM Provider. This contradicts the authoritative ready target above. Cursor implemented the bounded administration prompt correction and deterministic regression, and the primary inspected the final diff; successful inference alone is not accepted as a correct diagnostic. Evidence: /private/tmp/openkit-fresh-admin-probe.json. Browser archive-control verification still awaits fresh browser sign-in.


The ready-installation checkpoint is public backup drb_58afbc1b-1195-4541-9771-4c999755e47c: all 96 files verify, with manifest sha256:9abd6ea8963e216951e594066a4514a508e84aaf6efd3d6383b3b52d7bb85fca. Its matching host-side external-secrets.tar is separately retained under the protected rebuild ready-installation directory, SHA-256 7c08c7f89620a0f2b78af179c1c7fec6497913da07387b3e84e4aaa1810e3142. backup-pair.json records that pairing. The archive is permission-protected (directory 0700, file 0600), not additionally archive-encrypted. Public backup preserves Vault mode 0700. This checkpoint includes the current re-enrolled transport authority and prepared image candidate.

The administration owner explicitly limits the private Assistant to six Tools, none of which reads NanoHost readiness. Adding the existing public read to that Tool set therefore awaits the engineer's scoped design decision. The immediate correction stays inside current authority: explain that NanoHost is an execution host and that Provider or retained-environment inventory cannot prove host readiness. The public Skill receives the same limitation and points authorized operators at nanohost.runtime-target. The separate image activation question remains pending the already shown exact candidate; no activation effect was executed.


The administration correction preserves the exact six-Tool assembly and directs readiness requests to the existing public observation. The two successful-route fixtures now use a 10000 compaction threshold within their 20000 physical context, allowing the larger prompt and six Tool schemas; refusal fixtures and production compaction logic remain unchanged. Primary verification passed all 18 tests across administration-routes, administration-tools, internal-agent-loop and gateway-provider, NanoCore typecheck, focused Biome, documentation-model validation (277 documents), generated documentation index, public Skill reachability and git diff whitespace checks. The Skill reference is synchronized to the installed client. This is source-only acceptance; live administration inference must be repeated after deployment.


## Accepted Read-Only Administration Diagnostics

The engineer accepted adding the existing read-only NanoHost status observation to the private administration Agent. The Assistant specification now owns seven fixed Tools, with the new Tool using the same configured-target projection as Web and Skill, empty input, current administrator rechecks, explicit unavailable outcomes and no host effects or extra durable state. Cursor owns the bounded implementation and regressions; the primary owns the specification, Skill reference and live acceptance. Expected evidence is identical redacted readiness data through both adapters, rejection of selected-target input and revoked authority, followed by focused tests and a deployed read-only administration probe.

The engineer separately approved the exact prepared certificate-fix image candidate for later Tasks only and reported fresh browser sign-in. The next public activation will copy the previously shown candidate and confirmation unchanged, with affectedStorage=[] and replaceNow=null; configuration is re-read afterward before a new Worker probe.


Exact approved activation succeeded with request 525070b2-555d-46a4-91e5-701e3ad04c0a, no affected resident storage and no replacement. The durable Agent configuration now has revision sha256:82c067b0d8261b71d36f9a6b496403305e0a6aec46d3f9401783df31311526fb and the approved b1ce07de image digest. A new ws_4/th_3 Worker Turn tu_conversation_3a1ef01ba64ebb69fdb9c705 completed using the explicitly selected OrcaRouter model. Its durable response reports the configured 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9 checkout and a clean git status. The same answer is visible in Web. Evidence: /private/tmp/openkit-fresh-image-activate.json, /private/tmp/openkit-fresh-codex-after-activation.json, /private/tmp/openkit-fresh-worker-submit.json and /private/tmp/openkit-fresh-worker-items.json. This proves new Worker execution, not Git publication or the full code-review/apply loop.

Browser archive selection, dry-run and import completed after fresh sign-in. Import remapped the colliding ws_13 to ws_imported_ws_13. Public read of ar_imported_ws_imported_ws_13_1 confirms the source marker and sha256:0f9717932d8c4d927e66c848a30b3b658e33e10dcab8f8f09a692919fe070ac5. Browser export created wsexp_36c51985-5d42-456f-b595-1b4853c0fe25 with 26 files; download click produced no observed browser download or error, so local file delivery remains unverified. Cursor investigates that boundary, and a separate Web builder improves the ambiguous completed-import presentation and prevents accidental duplicate application after success.


Independent review accepted the seven-Tool owner amendment and production diff. Primary verification passed 51 tests across eight administration, configuration, RuntimeTarget, transport and internal-loop files, NanoCore typecheck, focused Biome (16 files), documentation-model validation (277 documents), documentation index and Skill reachability. The shared reader preserves startup configuration identity and exact redaction, and the Tool wrapper rechecks durable administrator authority on every call. The installed Skill reference is synchronized. Before the next deployment, public backup drb_a509009f-1975-42bf-a1d4-8548e9cb25fc was created and verified, manifest sha256:fc19c1c90817bd0a6a366bf8fa5043bb1b4bc8fc4172c2ab608d610ecb037182; matching external credentials remain in the previously verified ready-installation archive. The reviewed operator update helper preserves the current Data Root, mounts, credential directory and prior container, and uses an immutable built image ID for both App and Web.


The successful Worker exposed a separate remaining presentation issue: the user-message Item contains the structured delegation JSON, and Web renders it verbatim. That same Item is recovery authority in mode-entry, turn and approval routes, so rewriting its stored text would break recovery. Cursor confirmed ItemView lacks explicit Worker Turn context and the accepted delegation schema currently lives only in NanoCore. No heuristic Web parser or durable-format change was added. A later bounded projection correction must carry explicit Worker context and reuse the schema through an appropriate shared owner, preserving full details and ordinary user JSON. This does not block the current readiness/portability deployment.

## Browser Portability Completion

The signed-in browser imported the retained portable archive and the public artifact read verified its exact content digest. The old Blob-based download did not yield observable file delivery in the in-app browser, so this observation is not presented as a proven browser-engine cause. The Web correction delegates attachment delivery to the existing authenticated same-origin archive GET, eliminating full-archive buffering and object-URL lifetime handling. Completed import now announces the actual imported identity, prevents re-review/re-import of that bound successful source, and offers a direct Workspace entry. Independent review found a slow-list-refresh race; both import hooks now synchronously add the returned Workspace to the existing list cache before invalidation. A regression holds the list refetch pending and verifies the new Overview's actual Workspace-scoped requests.

Independent final source review found no remaining actionable Portability issue. Primary corrected the link's exact accessible name, awaited asynchronous completion announcements in existing tests, and made the no-project import fixture return the newly created Workspace on refresh; its no-Vault-before-import assertion remains before the new Workspace exists. Final focused verification passed 48/48 Portability tests, Web TypeScript build and Vite production build, focused Biome and git diff whitespace checks. The existing bundle-size warning remains informational. Evidence: /private/tmp/openkit-portability-final-tests.log and /private/tmp/openkit-web-final-build.log. Pre-update public ws_4 dashboard has activeWork=[], and the retained Worker environment is attached after its completed Turn.

## Live Acceptance On f8543dbe

The clean committed f8543dbe839bd4d2d806a7226faa3c8f1d64d2d9 snapshot was transferred by git archive; both local and A2 archive SHA-256 are 8d6311116b39bea32c80a502aa865135ae3bcc293e98b58c5cdbe0a34b2ef20c. The A2 App image built and its smoke command passed with Node 24.18.0, pnpm 10.33.3 and Caddy 2.6.2. The immutable image is sha256:910cdd09cb8f8ac8d75efc4a647d3a21304e311e3db8a86a2cffb493b868f05a. The reviewed updater preserved Data Root, mounts and credentials, retained openkit-before-f8543dbe839b stopped, and switched Web to the exact commit directory. Public health and Skill doctor pass; browser DOM loads index-BAsSEcAM.js. Build, smoke and replacement evidence are /private/tmp/openkit-a2-f8543dbe-build.log, /private/tmp/openkit-a2-f8543dbe-smoke.log and /private/tmp/openkit-a2-f8543dbe-update.json.

Public NanoHost readiness after the update reports staging-nanohost-a2 / staging-a2, connection generation 3, predecessorFenced=true, ready=true and freshEmpty=true at 2026-09-16T14:50:03.042Z. The private administration Assistant, explicitly using OrcaRouter, returned all seven identical fields and correctly explained that this is Core's stored observation rather than a live host probe; it made no configuration or Worker mutation. Evidence: /private/tmp/openkit-after-update-runtime-target.json and /private/tmp/openkit-admin-readiness-after-update.json. The prior incorrect provider-based readiness diagnosis is now corrected in deployed behavior.

In the signed-in browser, export wsexp_de02953b-3bf9-4d43-b892-0c2029bf6c91 from ws_13 produced a real 3730-byte archive under Downloads, SHA-256 e0f8534ea718a6ff30a43495839170f47cb76b58f57ded6ba471ea69b2e70677. The link opened a separate attachment tab and the originating-tab download-event wait timed out; actual file delivery, subsequent successful upload and import are the deciding evidence. That empty temporary attachment tab was closed. Uploading the just-downloaded bytes, reviewing and importing completed as ws_imported_ws_13_2; Web announced that actual identity, disabled both Review and Import for the successful source, and Open workspace navigated into its Overview. Its Artifacts page opens the marker body. Independent public artifact read confirms ar_imported_ws_imported_ws_13_2_1 with unchanged content digest sha256:0f9717932d8c4d927e66c848a30b3b658e33e10dcab8f8f09a692919fe070ac5. Evidence: /private/tmp/openkit-web-download-roundtrip-proof.json. The native download, browser round-trip and imported Workspace entry are verified; this does not close the separate full development/review/apply/Git-publication acceptance loop.

## Unified Sidebar Settings Gear

The engineer requested removing the separate bottom Settings row and merging it into the first gear shortcut. The existing React Aria Menu now supports a compact icon trigger; Sidebar uses one gear with Workspace settings when selection is validated and the global Settings entry regardless of Workspace availability. The destinations retain their existing routes, scope and authorization. DESIGN.md, the Web projection owner and local guide reflect that placement. Independent source/authority review accepted the bounded change. The initial regression failed on the old sidebar as expected; the final App, Sidebar, Portability and Workspace Sync run passed 121 tests, and the final navigation run after adding both route assertions passed six selected tests. Web typecheck/production build, focused Biome over seven files and whitespace checks pass. Four broader operations/search failures reproduce on the prior HEAD Sidebar/Menu bytes and are not attributed to this change. Logs: /private/tmp/openkit-gear-tests-final.log, /private/tmp/openkit-gear-final-navigation.log, /private/tmp/openkit-gear-operations-baseline.log and /private/tmp/openkit-gear-build.log. No backend contract changed, so no Skill operation change is required.

The Web-only b18fda76d532b51f1469aee4419b22162caed921 build was installed in its own A2 asset directory and the current symlink atomically switched under the deployment lock after checking the expected previous target. The App image and NanoHost were not restarted. Transfer SHA-256 matched e48dc969c8e95882b4ede7650935bb2098f74c41d70612a04b5a8f90a4f86fbb. In the signed-in browser the footer contains one icon-only Settings menu, with no separate row; selecting Workspace settings opens /workspace, and selecting Settings opens /settings/account with the existing scope groups. Both destinations are live-verified, and the browser was returned to /workspace.


The four previously reproduced operations/search failures were stale test fixtures after recent conversations and Overview moved to listConversationNavigation. Cursor updated only the operations test file; primary inspected the diff and current consumers, preserving pending-discovery and cross-Workspace read-isolation assertions. The complete file now passes 58 tests; focused Biome and whitespace checks pass. Evidence: /private/tmp/openkit-operations-fixtures-final.log. No production behavior or Skill contract changed.


## Real Worker Edit And Repository Handoff

A real OrcaRouter Task in ws_4/th_5 edited only apps/web/README.md, but post-execution ingress failed with git_repository_missing because its input resource task-mode-repository had no exact host apply target; repo_default was the only linked row. The public repository.set-default operation linked the already authorized /srv/repos/openkit path under the exact input resource ID while preserving Git policy and the Vault grant. Catalog/source authority was unchanged. Public reread confirms the exact row is ready. A new request in th_5 then failed because a current NanoHost AgentSession binding remained; no public interrupted checkpoint, review, change-set or reconciliation record was available. This lifecycle defect remains under investigation without deleting runtime/session records.

A separate bounded Task in ws_4/th_6, request 749b762a-104f-4301-a62d-fec84da8721f, completed on the explicitly selected OrcaRouter model after the binding repair. It produced a real one-file README patch based on 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9, digest sha256:7ce33d501bbfe09e510706397848da7321cf217eee3572977608a58fb5f16c70, and a pending staged review under Turn tu_conversation_6a420ce2a231ffe18862c25f. Both the public Skill and Web Workspace changes page expose the exact diff and review controls. Human acceptance was requested before apply; no acceptance, apply, commit or push is inferred from Worker completion. Evidence: /private/tmp/openkit-maintenance-bound-submit.json and /private/tmp/openkit-maintenance-bound-reviews.json.

The public administration Skill now documents exact input-resource binding and separates local-directory readiness from review actionability. Independent review verified the actual source identity and upsert behavior; documentation-model validation (277 documents), documentation index and whitespace checks pass. The optional skill-creator metadata validator cannot run in the system Python because PyYAML is absent; metadata was not changed. The installed Skill reference was synchronized.


The exact th_6 patch also passes a read-only git apply --check against the linked A2 checkout. Public usage evidence records five succeeded OrcaRouter Worker inference calls. A subsequent read-only continuation through the discovered running-worker:th_6:agent_codex_host target completed and reported git diff --check exit 0 with only apps/web/README.md modified, preserving the pending patch. It also emitted a second review for the unchanged patch; duplicate review production remains a separate finding. Evidence: /private/tmp/openkit-maintenance-usage.json and /private/tmp/openkit-maintenance-continuation.json. No review has been accepted or applied.

Failure investigation confirmed two distinct boundaries. Host apply-target absence correctly does not prohibit catalog-backed Task launch, but closeout currently rejects collected bytes before durable review ingress. The existing blocked review state is a terminal human decision and cannot be reused as a repairable hold. Separately, terminal Codex inspection can mark a native session reusable before closeout fails; cleanup then retains the Harness binding while Core marks its AgentSession failed. The assigned correction uses the existing verified session.close path for failed closeout, without deleting binding rows or weakening unique Thread admission. Claude consultation supports reusing this lifecycle; broader output retention remains unresolved.

The pre-backend-cleanup failure correction now forwards failed closeout through live and restart cleanup and closes the exact native AgentSession using the existing verified Harness result. Regression coverage proves ordinary success retains its binding, failed handoff closes it and permits a successor, and refused close rejects cleanup while retaining the admission barrier. The executor regression rejects invalid Workspace-change lineage before cleanup; it does not claim to reach the missing-target diagnostic. Independent review accepted this bounded scope. Failures after cleanup, including event emission or terminal product writes, remain open because cleanup has already retained the reusable session; no broader lifecycle fix is claimed. Collected-output retention and duplicate unchanged-patch reviews also remain open.

Verification: both runtime test files pass 176 tests, focused Biome passes, NanoCore typecheck and build pass, and documentation-model validation passes 277 documents. The full NanoCore suite reports 64 failures. Restoring only the three changed production files to HEAD reproduces all 64 with no fixed-only failures, plus the three new closeout regressions fail as expected; exact modified bytes were then restored. Evidence: /private/tmp/openkit-closeout-focused-final.log, /private/tmp/openkit-closeout-full-tests.log, /private/tmp/openkit-closeout-baseline.log, /private/tmp/openkit-closeout-typecheck.log and /private/tmp/openkit-closeout-build.log. This correction is not yet deployed to A2 and does not repair the already stranded th_5 binding.

A fresh Claude direction review prioritized completing the pending real apply/publication loop and deploying the reviewed closeout fix over another broad UI sweep. The exact th_6 patch again passes git apply --check on the unchanged A2 repository base 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9; its human approval remains pending. Source inspection confirms existing terminal Core sessions are filtered out before exact-ID continuity inspection, although native admission rejects any binding for the Thread. After correcting an initial conflation with same-attempt reconnect, consultation confirmed that the accepted AgentSession continuity successor rules already authorize verified predecessor close during separately admitted later work. A bounded correction is delegated without changing historical sessions, adding a public repair API or weakening native admission.

The 12 worker-environment App/preparation baseline failures came from fixtures creating private administration Threads for the default local identity inside another User's Quick Chat Workspace. Cursor changed only the four fixture calls to name the actual owner-private audience; primary inspected the diff against the owning Quick Chat rules and independently ran both complete test files: 12 tests pass. Production authorization and negative assertions are unchanged. Focused Biome passes. Evidence: /private/tmp/openkit-worker-environment-fixtures-final.log.

The exact committed d4d39dcef66222841a3c810cbc10c46f69a26bfc source archive (SHA-256 625857c1373d22a226bd376308f99a4c074360ba71874caaef24d77f47d7f475) built successfully on A2 and passed openkit-app-smoke. The reviewed updater installed immutable App image sha256:16bc69b22d992ae7eaea1ac6d87c6d34464597ddb36cff11ba361fc3b3b1de04 with matching Web assets, retaining openkit-before-d4d39dcef662 stopped and preserving all mounts, credentials and Data Root. The pre-update scheduler had no active leases. Public health and installed Skill doctor pass; public NanoHost status reports connectionGeneration 4, predecessorFenced=true, ready=true and freshEmpty=true at 2026-09-16T15:46:50.534Z. No Worker image, repository source pin or pending review decision changed. Evidence: /private/tmp/openkit-a2-d4d39dce-build.log, /private/tmp/openkit-a2-d4d39dce-smoke.log and /private/tmp/openkit-a2-d4d39dce-update.json.

The signed-in browser reload loads the new index-D92UB8o5.js bundle and retains both pending review rows. A new read-only OrcaRouter continuation in ws_4/th_3 on d4d39dce completed in 23.858 seconds, reporting pinned source HEAD 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9 and a clean working tree. Public request 8f01f210-f9d2-4390-b127-5c15c9554573 produced completed Turn tu_conversation_a003ca1b399e757077d09fb5; evidence: /private/tmp/openkit-d4-readonly-result.json. This proves post-upgrade Worker execution, not pending review acceptance or publication.

The export-lineage baseline failures shared a missing required Thread visibility field in the common project-history fixture. The fixture now explicitly uses workspace visibility, matching its formal Goal/Task history; production parsing still rejects missing or contradictory audience data. Primary inspected the one-line diff and ran the complete export-lineage and Thread-envelope files: 60 tests pass, including existing negative lineage and audience assertions. Evidence: /private/tmp/openkit-export-lineage-fixture-final.log.


After the post-upgrade read-only Task, a read-only native binding inventory contained only th_3/as_eff07cf75566baca/open. The old th_5 binding is therefore no longer stranded after the existing restart and fresh-admission path. Historical failure remains unchanged; the bounded same-process successor correction continues against a regression rather than a remaining live victim.


## Workspace Review Origin In Overview

Overview could not name the conversation behind otherwise identical Workspace Review rows because the backend omitted optional Thread and Turn ids. The Action Center now reads the exact backing Artifact and reuses the existing Artifact audience predicate; visible turn-output lineage supplies those ids, while missing, private or imported origins leave the Workspace-owned row and its actions intact. The existing Web navigation join requires no UI change. The NanoCore guide and public Skill reference describe the same scope. Independent review accepted the actual diff and evidence. With production restored temporarily to HEAD, the visible-origin assertion fails as expected while both omission cases pass; final Action Center tests pass 21/21, focused Biome and NanoCore typecheck pass, and documentation-model validation passes 277 documents. Evidence: /private/tmp/openkit-action-center-origin-red.log, /private/tmp/openkit-action-center-origin-final.log and /private/tmp/openkit-origin-typecheck.log. This backend change awaits the next App deployment.


## Terminal Predecessor Admission

The same-process successor correction resolves the exact native binding by Workspace and Thread before reusing the existing read-only continuity preview and post-lease close path. It preserves terminal Core history and rejects active, foreign, contradictory or unproved ownership. Independent review rejected a proposed lifecycle filter: native inspect can label a row closed or failed while retaining a running child or lease, so every remaining row continues to block native open until verified close or wider retirement removes it. The final diff keeps the original any-row admission guard. Regressions drive actual native inspect results, exact binding selection and refused cleanup; all three affected runtime test files pass 196 tests. Restoring only the executor production file to HEAD produces five expected failures, with the absent and foreign-history cases passing; exact final bytes were restored afterward. NanoCore typecheck, build, focused Biome and whitespace checks pass. Evidence: /private/tmp/openkit-terminal-successor-final.log, /private/tmp/openkit-terminal-successor-red.log, /private/tmp/openkit-terminal-successor-typecheck.log and /private/tmp/openkit-terminal-successor-build.log. The NanoCore guides and public Skill recovery reference are aligned. Independent review accepted the actual diff and named outputs.

Fresh Claude direction review returned Continue for reviewed commit and immutable App deployment. The real th_6 decision remains a parallel human-owned dependency. Regression evidence closes the bounded runtime correction; a post-deployment read-only Task can prove execution availability, not the uninduced same-process failure/recovery path. The next discriminating live observation would be an actual failed closeout followed by a separately authorized successor in one App lifetime. Concurrent fixture changes remain separately owned and excluded from this runtime commit.


## Remaining Fixture Alignment

Export fixtures now declare shared project-history visibility; the legacy AEP migration fixture supplies its actual worker Turn so the existing historical classifier can infer a shared audience without a current envelope. Catalog tests now follow the accepted presented server-admin bearer authority while retaining scoped-token isolation assertions. The Goal scope fixture explicitly creates the shared Thread required by Goal Mode. The POSIX export cancellation oracle retains Linux procfs and uses lsof on Darwin; a held staging descriptor is positively detected before asserting that abort cleanup leaves none. Primary inspected these diffs and independently ran the two export files (54 passed), migration file (16 passed), and catalog/server-auth files (5 passed), with focused Biome and whitespace checks passing. Evidence: /private/tmp/openkit-export-fixtures-final.log, /private/tmp/openkit-migration-fixture-review.log and /private/tmp/openkit-authority-fixtures-review.log. The separate Git push barrier failures remain under investigation; their assertions are not weakened.


## Reviewed App Deployment 81f74ccf

The tracked source archive for 81f74ccff6d738184e0adbb26ed7b64ab85ade81 has SHA-256 2039bc927f07c0f8d697490ba0211ff4e466cb5eaaa510cd36463143b48f9482. An initial extraction before transfer completion failed without touching the live App; after transfer completion, the digest matched before extraction and build. A2 built immutable image sha256:11521e960b7ca39b4b3ea15e75685b41c68c576bb558e9793ba5adda6cfd7585 and passed openkit-app-smoke. Fresh pre-replacement reads found zero active leases, queued admissions or uncleaned backends. The existing reviewed updater preserved Data Root, credentials and mounts and retained openkit-before-81f74ccff6d7 stopped. Public health and installed Skill doctor pass; NanoHost generation 5 reports fenced, ready and fresh-empty at 2026-09-16T16:21:54.163Z. Evidence: /private/tmp/openkit-a2-81f74ccf-build.log, /private/tmp/openkit-a2-81f74ccf-smoke.log and /private/tmp/openkit-a2-81f74ccf-update.json.

Signed-in Web Overview now labels both pending review cards with their originating conversation, Verify repaired repository review handoff. Installed Skill attention.list independently returns th_6 and the exact originating Turn for each pending review. The read-only OrcaRouter continuation request db182761-54a2-4ca5-8680-348e88f2b1a9 completed Turn tu_conversation_aecf53b7d2807d8a2350764b in 21.857 seconds, reporting source HEAD 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9 and a clean working tree. Evidence: /private/tmp/openkit-81f-attention.json and /private/tmp/openkit-81f-readonly-result.json. This verifies post-upgrade execution and origin projection, not same-process failed-closeout recovery or human review/apply/publication. Both review decisions remain pending.


## Git Push Review Evidence

Host-exempt pushes and repositories with linkage enforcement disabled returned no review ids even when the requested commits had known apply or staged-review linkage. The shared evaluator now collects those existing ids before applying the unchanged enforcement predicate. No permission decision, approval, branch, Vault or remote-effect gate changes. The server barrier fixture no longer corrupts its granted permission subject before an expected-success call; independent review confirmed that this was not a removed negative authorization assertion. Both existing barrier modes and the new disabled/exempt metadata regressions pass. Primary independently ran the 181 linkage/server tests, NanoCore typecheck and build, focused Biome, and the complete NanoCore suite: 3114 passed, one existing skipped, no failures. Evidence: /private/tmp/openkit-push-evidence-final.log, /private/tmp/openkit-push-evidence-typecheck.log, /private/tmp/openkit-push-evidence-build.log and /private/tmp/openkit-closeout-full-tests-second.log.

Claude independently reconciled the existing host exemption against engineer-authored issue #85 and merged f0090263, and accepted the owning Git workflow specification alignment as preserving the already merged predicate and every other gate. The primary separately read the issue body and inspected the implementation. The specification, runtime guide and public Skill now state that known review ids remain recorded even when missing-link enforcement is disabled or exempt. No new exemption state or field is introduced. The code, accepted specification alignment and Skill documentation land together; this evidence correction is not yet deployed on A2.


## Verified Evidence Deployment And Next UI Seam

A2 now runs 5fe875f3959fa0298ae0a5b2ed3485aeca7bdb80 as immutable image sha256:d1b07df2756507a61169938e3c117a3ef73759608f407802a37bc92a1ece4910. The compressed tracked archive SHA-256 is e9cd0aa98ad3de27d3448ac5d5011c96cf6a13fe9dd9f0bc2a58dabafe9b85f3; build and image smoke pass. Fresh pre-replacement scheduler/backend checks were empty. The same updater preserved mounts, credentials and Data Root and retained openkit-before-5fe875f3959f stopped. Public health and Skill doctor pass; NanoHost generation 6 is fenced, ready and fresh-empty at 2026-09-16T16:30:54.198Z. Public attention still reports the two pending th_6 reviews with exact origin. Evidence: /private/tmp/openkit-a2-5fe875f3-build.log, /private/tmp/openkit-a2-5fe875f3-smoke.log, /private/tmp/openkit-a2-5fe875f3-update.json and /private/tmp/openkit-5fe-attention.json. No live push was performed; the audit correction is proved by regression tests, not a publication claim. Independent Claude review also accepted the stale App update specification sentence correction, committed separately as 87c63e14 without behavior changes.

Live Overview Open navigation reaches Workspace changes and exposes the actual one-file patch with available review actions. Its cards still lead with opaque review/change-set/Artifact ids, while snapshots, materializations and backend handles are always expanded. Cursor's bounded read-only map identifies apps/web/src/screens/workspace-sync/WorkspaceChangesScreen.tsx as the existing owner, with ChangedPathPreview and changedPathSummary already available. ArtifactReference provides an existing native details pattern and ItemCard supplies a title/meta/action shell. A narrow next presentation correction can prioritize filename, status and summary and retain diagnostics under disclosure; no new API or approval behavior is needed. Overview currently opens only /workspace-changes with no exact-review route parameter; no deep-link contract has been added. This is the next implementation seam, not a completed change.


## Workspace Changes Presentation

The preceding goal turn made concrete progress through reviewed commits, two verified App deployments and full NanoCore regression evidence. Current live inspection shows the remaining Workspace changes hierarchy problem: opaque ids precede useful review content and backend inventories overwhelm the decision surface. This presentation-only correction follows DESIGN.md plain-language and progressive-disclosure guidance and the existing Workspace Sync Web projection owner. Cursor exclusively owns WorkspaceChangesScreen.tsx and workspace-sync.test.tsx; primary owns the Web guide, acceptance and deployment. Review callbacks, target identity, disabled/error behavior and recovery decisions remain unchanged. The expected observation is meaningful changed-path/status summaries, expandable exact ids and patch content, and collapsed low-level diagnostics without hiding actionable outcomes.

The live 819px viewport also exposes a concrete flex-layout failure: review actions consume the row and squeeze the body into roughly one character per line. The bounded correction gives review and recovery bodies their own full-width column and wraps actions on a separate row. Browser rendering, rather than class-name assertions, is the layout oracle.

Parallel read-only investigation traced the two pending th_6 candidates to retained dirty Git work being collected against the repository base on successive Turns. Claude initially applied the Non-Git Filesystem Strategy's pre-execution manifest rule to Git; primary checked the section boundary and the consultant withdrew that claim. The Git Strategy explicitly preserves retained dirty work and prefers base-relative patches. No automatic deduplication or baseline change is included: the current owners do not specify a cross-Turn suppression predicate, and failed collection before durable ingress still needs separate public-interface recovery evidence. Both candidate identities and pending human decisions remain intact.

Cursor implemented the two-file presentation change and primary inspected the exact diff. Independent reviewer review_conversation_targets found no remaining production blocker after simplifying duplicate markup and correcting the disclosure regression. Primary observed 14 passing Workspace Sync tests, clean focused Biome and a successful Web build; evidence is /private/tmp/openkit-review-ui-tests.log and /private/tmp/openkit-workspace-review-web-build.log. The original failing disclosure assertion confused DOM presence with visibility; the final regression checks closed/open state, hidden/visible content, exposed review actions and exact decision routing. No API, Skill behavior, authority, durable records or live review decisions change. Browser layout acceptance remains a separate post-deployment check.

## Verified Review Presentation Deployment

A2 runs fd4174666b35bcb502eb0e5164418bbfe21d1fc2 as immutable image sha256:4ff9b25f1c595d48acbbeea19ca555ec93e536e5179508f163bc596ea28db605. Its tracked archive SHA-256 is 4e1096ee233386e5d720c8d60fb0c89257069a8342d6ceb34c5c38e3958457f4. Image build and smoke passed; fresh pre-replacement checks found zero active leases, queued admissions and uncleaned backends. The reviewed updater retained openkit-before-fd4174666b35 and preserved existing mounts, credentials and Data Root. Public health and installed Skill doctor passed. Evidence: /private/tmp/openkit-a2-fd417466-build.log, /private/tmp/openkit-a2-fd417466-smoke.log and /private/tmp/openkit-a2-fd417466-update.json.

Primary verified signed-in rendering at the normal 819px viewport and the accepted 800x600 floor. The review body now occupies the card width and decision buttons occupy their own row. Opening View diff, Technical details and Diagnostics preserves readable patch text, exact record ids and all six diagnostic inventories. At 800px the document scroll width was 800px and the review region's client and scroll widths were both 488px. The initial 750px probe exposed the intentional 800px shell minimum owned by DESIGN.md section 12, not a regression in this slice; smaller-window support is outside this acceptance claim. Temporary viewport overrides were reset. The unique Settings gear opens Workspace settings and Settings, and the latter successfully navigates to /settings/account; no redundant bottom Settings row remains. Public attention still exposes the same two pending th_6 reviews. No review, apply, recovery or push action was performed. The end-to-end development goal remains open pending the previously requested exact review decision and subsequent apply/publication verification.

## Explicit Retained Storage Recovery Probe

The default th_5 continuation request f23b680a-189e-4095-adad-23fdaf67f8ab completed on a clean 96a6b73d source and does not prove the old dirty worktree was lost. Public worker-environment.list/status/select report the original wst_d4a6b3e4fc1b4ab7a9bfbb9e195b2b35 idle at revision 4, available and without an attachment. The prior AEP exposes its exact workSlotRef wsl_b2257a77e8f80c9e727949ee6eb03bd5. A read-only selection eligibility check succeeded. No retained bytes were inspected through SSH or deleted.

The first explicit task.start probe f712f048-2a5d-47c4-b891-bf2897410565 was refused as task_mode_not_delegated because incidental human-review wording matched the deterministic coordinator review classifier. Rephrasing the same bounded read-only command request as fd2779e4-f387-41cc-bc1c-354623436187 completed, but again observed the clean current volume. Its Core scheduler_admission_entries.worker_storage_choice_json contains the exact selected old ref, expectedRevision 4 and reuseWorkSlotRef. Its new backend session as_e5ce4056b99cee8a completed cleanup; the selected old ref still has revision 4, no attachment and its original update time. Source inspection localizes the silent choice loss to the shared Sandbox existing-contributor path, which skips the selected-ref comparison and reservation. This is a violated existing persistent-volume contract, not evidence that retention itself deleted the bytes.

Evidence files: /private/tmp/openkit-th5-recovery-probe-result.json, /private/tmp/openkit-th5-old-environment-status.json, /private/tmp/openkit-th5-old-environment-select.json, /private/tmp/openkit-th5-selected-recovery-result.json, /private/tmp/openkit-th5-selected-command-input.json, /private/tmp/openkit-th5-selected-command-result.json and /private/tmp/openkit-th5-old-environment-after.json. The latest installed Skill doctor passed with request 02a47d1a-6bf9-425d-a63c-be53320cb5d8. These probes neither accepted pending reviews nor applied or published code. The prior continuation only reconfirmed the already completed Settings consolidation; this iteration resumes the next supported recovery path rather than claiming new progress from that restatement.

Claude consultation confirmed the existing-contract repair: honor explicit selected and fresh choices, check caller revisions across both resident and new contributors, and preserve omitted-choice continuation. Independent reviewer review_terminal_successor requires stale selected refs to fail before any destructive resident eviction and the ordinary CAS to recheck afterward. The secondary review-word classifier finding remains open: both a blanket execution-keyword override and stripping the human-review phrase can misclassify prohibited execution, so neither was implemented. Public environment.snapshot-list already exposes the authorized prior work slot; no new summary schema is needed for the Skill recovery recipe.


## Selected Storage Regression Evidence

The first delegated test failed in lease setup and therefore did not establish the defect. A later conditional missing-resident fallback was rejected as a weak oracle. The final fixture requires the same live idle resident and attached association before and after stale selection. It covers omitted reuse, successful same-ref selection, stale revision and missing slot for existing and new contributors, busy replacement refusal, another selected association, same-ref alternate-slot fenced reattachment, and explicit fresh storage while a resident remains. The existing native-session fixture also proves stale selection fails before closing the current session.

The old factory source fails the new stale-revision regression because materialization resolves instead of rejecting (`/private/tmp/openkit-storage-selection-baseline-red.log`); the fixed factory passes all 59 tests (`/private/tmp/openkit-storage-factory-final.log`). The old planner fails explicit fresh selection by inheriting the resident predecessor slot, while omitted continuation passes (`/private/tmp/openkit-fresh-slot-baseline-red.log`); the corrected two-case regression passes (`/private/tmp/openkit-fresh-slot-regression.log`). Temporary baseline substitutions restored fixed source byte-for-byte. Independent review confirmed that two older expectations for explicit fresh inheriting a predecessor slot contradicted the fresh association's default slot; they now assert the Thread default while preserving omitted-continuation expectations.

Focused Biome checks pass for all four changed TypeScript files; typecheck and build pass; documentation validation passes for 277 documents. Package-wide lint reports an existing format error and unnecessary escape in unchanged `apps/nanocore/src/docker/app-run-script.test.ts`; the file is byte-identical to HEAD and is outside this correction. The complete NanoCore suite passes: 3117 passed, one existing skipped test, zero failures, exit 0 (`/private/tmp/openkit-storage-full-tests.log`). Claude's fresh direction check returned Continue after reading the actual diff and named red/green evidence: an App-only update preserves NanoHost and retained roots, and the live success predicate requires both the exact old association revision/attachment change and its original README bytes. Deployment remains pending.


## Deployed Selected-Storage Recovery

Commit c166524590c9eb8977e19deea073811b865fc25f was transferred as a tracked archive with matching SHA-256 ab09e0e7540831c502cfd9d63e86d2d13d8de3eb0978e9003f7fcc05fd344fd3. A2 built immutable App image sha256:64c2654b6707518ac133e4a0702c6dba8dd3c4dc5e0d06a2e0a7dffbf6567d3c; the image smoke passed. Fresh pre-replacement checks found zero active leases, queued admissions and uncleaned backends. The reviewed updater preserved credentials, Data Root and mounts, retained openkit-before-c166524590c9 stopped, and did not restart NanoHost. Public health and installed Skill doctor passed. Evidence: /private/tmp/openkit-a2-c1665245-build.log, /private/tmp/openkit-a2-c1665245-smoke.log, /private/tmp/openkit-a2-c1665245-update.json and /private/tmp/openkit-storage-fix-doctor-after.json.

The public pre-read confirmed wst_d4a6b3e4fc1b4ab7a9bfbb9e195b2b35 remained available, idle at revision 4/generation 1 without an attachment. Eligibility succeeded. New Task request 26d2d276-4fb9-473f-8b59-fa0a1bf951c7 selected that exact current revision and slot wsl_b2257a77e8f80c9e727949ee6eb03bd5 with model orcarouter. Turn turn_26d2d276-4fb9-473f-8b59-fa0a1bf951c7_ff04f983c403c2c2 completed in 23,882 ms. The same association now reports attached revision 6/generation 2. Read-only commands found HEAD 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9, one modified apps/web/README.md, clean diff-check, and the original replacement of the default-model/Agent/Skill claim with display-name/Knowledge controls. This distinguishes recovered retained bytes from the previous clean unrelated volume.

Normal handoff created pending review swr_wcs_aepsnap_turn_26d2d276-4fb9-473f-8b59-fa0a1bf951c7_ff04f983c403c2c2_as_b37fae7f298accab_task-mode-repository, with one addition and one deletion. Public sync.staged-review-list confirms it. The signed-in Web task displays Turn 5, and View content opens Type: Workspace change review and the exact README diff, with Git blob identities 072e8f4e3389badcbabeb6d8ed038a10604e9bbd to 74e7a1ec1cd4ce3435332f0ebd2dd706c3c866cd. Evidence: /private/tmp/openkit-th5-fixed-selection-submit.json, /private/tmp/openkit-th5-old-environment-fixed-before.json, /private/tmp/openkit-th5-old-environment-fixed-after.json and /private/tmp/openkit-th5-fixed-reviews.json. No Worker file edit, review decision, apply, commit or publication was performed in the probe. The earlier failed Turn stays historical; its retained output is now inspectable through the existing recovery route.

## Task Message Presentation Investigation

The deployed sidebar has one Settings gear with Workspace settings and Settings menu entries; no separate Settings row remains. Live browser inspection on ws_4/th_5 confirmed the exact menu. Task initiating messages still expose complete Coordinator JSON. Preserve those immutable bytes under S12/S15; presentation must identify the actual worker request, not guess from arbitrary user JSON.

Source inspection refuted a presumed Turn mode discriminator: ProductTurn has no mode field, and its projection intentionally removes AgentSession identity. The proposed broad delegation-schema move was rejected after independent Claude consultation. The existing Context Package trace verifies exact workerRequestItemId and workerRequestDigest through the shared authority reader; the next bounded proposal is an objective-only dashboard projection from that verified existing owner, with raw-message fallback when provenance is unavailable. Expected evidence is a readable real Task objective, unchanged stored bytes, and unchanged human-pasted JSON; no new durable record or delivery claim is intended.

The Composer observation has a separate disposition: the accepted per-submission logical-model preference does not persist across mounts. Composer initializes the next message from the target catalog default, while task.start forwards its explicit modelId independently. Therefore a completed OrcaRouter Task followed by a GPT-6 Astra default selector is not evidence of incorrect execution. Adding a remembered next-message preference is outside this correction; no model-selection implementation changed.

Claude accepted the verified-trace route after inspecting its Item identity checks. Cursor owns disjoint backend/schema and Web implementation slices; root owns guides, Skill packaging, integration and review. The expected change is objective-first rendering through one shared dashboard field, with focused negative cases preserving arbitrary JSON and missing-proof messages.

## Verified Task Input Presentation

The shared dashboard now derives only Item identity and objective from the existing strict Context Package verifier and internal structured request schema. It also compares the exact displayed Item snapshot digest with the verified request digest. Independent review found this second byte check necessary because the verifier re-reads the Item after the projection snapshot. The new mismatch check failed against the initial implementation, then passed with the guard. The public client likewise failed its new preservation check before the shared schema field was added, then passed. Missing or invalid proof leaves the original Item visible. Web keeps the full original text in a native closed disclosure, and the public Skill bundle and reference consume the same field. No runtime identity, new durable owner, model preference, or Worker effect is introduced.

Observed focused evidence: Web chat 95 passed; client 79 passed; App API schemas 143 passed; backend projection, verifier and dashboard 36 passed; final projection recheck 10 passed; Skill plus documentation checks 138 passed on Node 24.18.0. Web and NanoCore typechecks and builds passed; OpenAPI generation and validation passed; focused Biome and diff whitespace checks passed. Logs use /private/tmp/openkit-task-presentation-*. Independent review inspected the actual diff and red/green logs and reported no remaining actionable finding. The mock-based projection checks prove the join and failure behavior, not end-to-end provenance; the existing verifier suite and planned A2 public/browser read provide the real verifier evidence. The required dashboard field, producer, client and consumers will be committed together to avoid a broken intermediate public contract.

Claude's fresh deployment direction check found a request-scoped WorkspaceDb handle missing cleanup in the new dashboard path. A regression inspected the actual opened SQLite handle and failed before the fix (handle-red.log); an inner finally now closes it on success and failure, and the 36 backend checks plus NanoCore build passed again. The requested negative cases are covered by `omits a summary when the shared verifier throws missing or corrupt proof` (fully conforming structured JSON without provenance), the mismatched digest assertion in `maps the objective from a verified trace through the exact request Item`, and Web's ordinary-human-JSON fallback. Claude accepted the deployment direction conditional on this local cleanup and reviewer confirmation.

## Deployed Task Input Presentation

Commit 299b3ed3318180bf3b79b2661b85bd6e3885f1f2 was transferred as a tracked gzip archive with SHA-256 7e5c05b25a95f4527a11fe455e43d5dff14baf2bd6f3ca44d23685af76112f07. A2 built immutable App image sha256:2705bea57d267dafbc11031d057658be4ca2dc5a1d047d20761ddbb58b4c8335 and passed its image smoke. Immediately before replacement there were zero active leases, uncleaned backends, and queued admissions. The reviewed updater retained openkit-before-299b3ed33181, preserved environment and mounts, updated Web assets, and returned healthy. NanoHost and pending review decisions were untouched. Evidence: /private/tmp/openkit-a2-task-presentation-{build,smoke,update}.log.

The complete installed public Skill was refreshed and doctor passed. Public thread.dashboard on ws_4/th_5 now returns five taskInputs for five existing Turns. Every objective equals the objective parsed from its exact original Item, only itemId and objective are emitted, and all five original Item text SHA-256 values match the saved pre-update response. The signed-in browser displays all five objectives with closed View request details disclosures; opening the first reveals the complete JSON and closing it restores the compact view. DOM checks confirm all five objectives equal their preserved disclosure text and no message overflows horizontally. The screenshot confirms readable wrapped layout and only the merged Settings gear in the sidebar. Evidence: /private/tmp/openkit-task-presentation-dashboard-{before,after}.json, /private/tmp/openkit-task-presentation-message-digests-before.json and /private/tmp/openkit-task-presentation-doctor-after.json. The pending human review/apply gate remains open. A separate visible follow-up is that assistant replies still show literal Markdown markers; it is not included in this Task-input correction.

## Review Closure And Audit Timestamp Investigation

The previous goal turn made direct progress: verified Task request presentation was committed, deployed, and proved through Web and Skill against all five existing requests without changing their bytes. The next priority remains real review-to-apply closure. Fresh sync.review-read confirms the original th_6 review is still pending with digest sha256:7ce33d501bbfe09e510706397848da7321cf217eee3572977608a58fb5f16c70. Its exact public patch bytes match the digest. Read-only git apply --check passes on the clean linked checkout at base 96a6b73d9229cf11f03cfaf7e0b1d457c041f1b9 when run as actual repository owner UID 1001; an earlier root probe hit Git ownership protection and an incorrect UID 1000 probe could not access the checkout. Neither probe changed Git trust configuration. The complete one-line README diff is opened in /private/tmp/openkit-review-closure-approval.md and the exact human acceptance/application decision is pending; no push is requested.

While that reserved decision is pending, the deployed Server audit page visibly omits event times. Public audit.server-list returns 105 complete events including recorded createdAt and occurredAt; the Web has zero time elements. Cursor traced the omission to the shared projectAuditAndDecisions whitelist and AuditRows renderers, affecting both deployment-admin Server audit and selected-Workspace Usage & audit. Server lists are complete and oldest-first, with no API limit/cursor; this is not evidence of hidden or lost events. The bounded fix restores the existing timestamp authority to both shared renderers without changing server ordering, pagination, permissions, or record storage. Cursor owns the Web projection/rendering/tests, root owns guides, review and integration.

The separate literal-Markdown observation is a missing rich-text presentation contract, not a dropped timestamp or Task-summary regression. There is no existing Chat Markdown renderer or installed Markdown rendering dependency. A hand-written partial inline dialect was not accepted as a shortcut; no Markdown parser or dependency was introduced in this slice.

The timestamp correction is implemented at the shared whitelist and row renderer. One focused regression verifies occurredAt precedence, createdAt fallback, exact semantic datetime/title, permission-decision time, explicit absence and unchanged private-payload redaction; the existing Workspace Usage and audit story verifies the same shared rendering. Primary reviewed the actual diff, corrected misplaced function comments, and independently ran both Web suites (46 passed), Web build/typecheck, focused Biome, documentation checks (97 passed), and git diff --check. Evidence: /private/tmp/openkit-audit-timestamps-{tests,build,lint,docs}.log. Public Skill reads returned 105 server events and 60 Workspace events with recorded time, proving existing API parity without a schema or CLI binary change.

The prior Vault inventory observation does not reproduce for the current signed-in canonical owner: /settings/vault-backend shows the available encrypted-file backend, one active github-token reference and two active grants. Both Refresh status and Refresh inventory complete successfully. No secret value, grant, lock state or permission was mutated; this observation does not claim coverage for another identity.

## Deployed Audit Timestamp Verification

Commit 9ff2d17cdfbe142af79ad412fb092cf926c72ad4 was transferred as a tracked gzip archive with SHA-256 375ce5b0f638244cb4c8dcf0c83126b4f47bc274c8282831d967a91b100dc25e. A2 built immutable App image sha256:670e2ed4d93c8039892992befa8a74f414f65e8bdda108a384b23fb4f8c91cfd and passed its image smoke. Pre-replacement checks found zero active leases, uncleaned backends and queued admissions. The reviewed updater retained openkit-before-9ff2d17cdfbe, preserved environment and mounts, updated Web assets and returned healthy without restarting NanoHost. Evidence: /private/tmp/openkit-a2-audit-timestamps-{build,smoke,update}.log. The installed Skill manual was refreshed and doctor passed.

Signed-in Server audit now renders 110 audit timestamps and 20 permission-decision timestamps. Its first and last audit datetime values exactly match public audit.server-list: 2026-09-16T13:21:44.477Z and 2026-09-16T18:47:36.678Z. Workspace Usage & audit renders 60 audit timestamps and 10 permission-decision timestamps. Neither audit surface displays Not recorded for these complete real records. The screenshot confirms readable local date/time with GMT+10 and intact status alignment. The sidebar still exposes one Settings gear, whose menu contains Workspace settings and Settings; no duplicate bottom row is present. Evidence: /private/tmp/openkit-audit-after.json and /private/tmp/openkit-audit-timestamps-doctor.json, plus direct browser DOM and screenshot observations. The exact pending review/application decision remains untouched; end-to-end publication is not claimed.

## Remaining Development Blocker Investigation

The preceding turn completed timestamp deployment and live verification, so it was progress. Current Git is clean at da06d65f and public doctor is ready. The reserved th_6 human review decision remains pending. Two bounded read-only Cursor investigations now test whether existing owners already cover the remaining observed defects: missing public handoff after git_repository_missing, and the empty Agents roster despite a reusable Codex Agent in the conversation target list. Neither agent owns writable paths or product mutations. Expected evidence is the exact producer/consumer and failure path, with a smallest existing-contract correction or a finding that a governing decision is missing; no new recovery lifecycle or catalog owner is presumed.

Fresh Web Recovery shows no interrupted Workers or scheduler admissions; public recovery.worker-list and sync.reconciliation-list also return empty items. The retained-checkout recovery proved earlier does not prove the original failed closeout is retryable. Fresh agent.list and workspace.resources both return empty agent lists, and Refresh health leaves the Agents screen empty. The composer nevertheless offers Codex Agent as reusable Worker supply for this Workspace. Evidence: /private/tmp/openkit-agent-roster-global.json, /private/tmp/openkit-agent-roster-workspace.json and direct browser observations. The current primary re-read the checkpoint and accepted execution guidance after compaction; these are read-only investigations within the existing development route, with no new direction-bearing commitment yet.

Independent Claude consultation reframed the handoff finding: valid output with a missing apply target is not invalid material, so the closed quarantine reasons cannot truthfully represent it; current reconciliation triggers also do not describe ingress refusal. Existing tests require no actionable review or change set. The original Turn remains failed, while retained-storage recovery through an explicit new Task is already live-proven. A new non-actionable Core output record is deferred unless a real use case requires inspection independently of retained storage; no enum, review state or recovery owner changes. The public Skill now documents the supported recovery and its limits. A fresh thread.dashboard read still exposes the exact original git_repository_missing error, but identifies only the opaque review id. Cursor now owns only worker-governance-turn-executor.ts and its existing test to improve that diagnostic with the exact missing repository resource and supported next step, preserving all refusal and no-effect assertions. Expected evidence is a failing diagnostic assertion before the change and the same ingress rejection with actionable text afterward.

The Agent roster investigation found a genuine missing projection: Core Agent Supply requires server manifests to appear in the Workspace-visible catalog by default. Claude corrected the earlier pin-filter recommendation; Workspace bindings select launch composition, not catalog membership. A remaining governing gap is that manifests have no role while the catalog requires a closed role enum. The concrete proposal in temp/changes/202609160700000000-self_hosted_development/agent-catalog-role-proposal.md admits null and displays Worker without guessing from the runtime. The engineer decision is pending under AUTH-001; dependent schema and roster implementation are paused. This is separate from the pending th_6 review decision.

A further live comparison changes the next action: thread.dashboard still returns two failed th_5 Turns with their recorded errors, followed by three completed Turns, but the Web history renders neither failure and leaves the first assistant's Done text unqualified. ThreadScreen.tsx selects failedTurn only from latestTurn. Cursor's Web builder owns only ThreadScreen.tsx, ThreadStream.tsx and turn-failure.test.tsx to restore each historical failure at its owning Turn without duplicate latest/live errors or retry effects. The expected regression loads a failed earlier Turn followed by a successful Turn and requires the original error to remain visible. This is an existing durable-state projection correction, independent of the paused roster contract decision.


## Missing Repository Diagnostic Verification

The bounded executor correction identifies the exact missing repository resource and links the supported new-Task recovery path without changing rejection or replay semantics. Primary inspected the actual two-file diff and preserved the existing no-effect oracle. The old implementation failed the new diagnostic assertion. The complete executor file passed 127 tests; the independently rerun focused ingress checks passed 17 tests with 110 unrelated cases skipped, recorded in /private/tmp/openkit-handoff-diagnostic-tests.log. Focused Biome passed both files; documentation checks passed 97 tests and validated 277 documents. The NanoCore guide and public Skill recovery reference now distinguish target linking, retained-file recovery, new review and human-gated apply. Deployment remains pending.


## Intent Epoch 8: Unspecified Agent Role

The engineer selected the recommended generic Worker proposal: absent authored role is `kind: null`, rendered as Worker, while configured supply remains discoverable. Do not infer roles from runtime names or add an authored role field or a second durable Agent entity. Core Agent Supply owns the clarified summary; the current manifest remains the authored source. This decision releases the dependent roster projection, Web and Skill synchronization. It does not decide the separately pending exact th_6 review/application gate.


## Historical Failure Correction

Commit 8049ef9e restores historical failed-Turn errors from the existing dashboard, including no-Item failures, without retry or durable-state changes. Independent reviewer review_conversation_targets found that the initial per-Turn map dropped earlier Item groups when events interleave. The corrected implementation preserves Item-log order and places each recorded error once after its Turn’s final group. A chronological interleaving fixture reproduced the dropped message before correction. Primary observed 103 passing Chat tests, six final focused failed-Turn tests, clean Biome and a passing Web typecheck; the reviewer inspected final bytes and the six-test output without remaining findings. Evidence: /private/tmp/openkit-history-interleaved-red.log, /private/tmp/openkit-history-failure-tests.log, /private/tmp/openkit-history-final-tests.log and /private/tmp/openkit-history-web-typecheck.log. Deployment verification remains pending.


## Catalog Health Boundary

Claude independently reviewed the nullable-role authority and current projection. Missing live health evidence remains unknown with no check time, and raw authored readiness messages stay private. The existing catalog status can carry disabled supply, but cannot separately express authored blocked or degraded readiness without conflating it with health. That explanation gap is recorded for a separate concrete use case; this bounded roster restoration adds no readiness field, runtime probe or lifecycle. Core Agent Supply’s discovery requirement and the engineer-approved generic Worker behavior remain the acceptance scope.


## Catalog Restoration Verification

The shared FsStore read projection now derives inventory from the current runtime manifest snapshot; list/detail, Workspace resources, refresh, dashboard and Action Center use that seam without persisting manifest summaries. Nullable role, Web Worker labeling, safe fields and same-release public Skill/OpenAPI are synchronized. Missing or removed manifests do not leave stale catalog rows. Independent review inspected final source, private-field exclusions and Workspace authorization without remaining findings.

The first delegated red run failed in missing-executor setup, not the catalog assertion, and is not regression evidence. The prior live empty resource/catalog responses establish the failure; final focused tests exercise manifest-backed reads, unknown health, private-field exclusion, scope and snapshot replacement. The broad NanoCore run recorded 3118 passes, five failures from obsolete upsert-seeded fixtures and one existing skip. Health fixtures now use real manifests, while Action Center fixtures inject explicit product health summaries after App wiring and retain every existing audience and attention assertion. The final five affected files pass all 30 tests; no production fallback or assertion weakening was introduced. Evidence: /private/tmp/openkit-catalog-nanocore-full.log and /private/tmp/openkit-catalog-final-regressions.log.

Protocol tests pass 171, App schema tests 143, public Skill tests 41 and focused Web Agents tests 12; the complete Workspace test file passed 153. NanoCore typecheck/build, Web typecheck/build, focused Biome, OpenAPI generation/validation and 97 documentation checks pass. Evidence is retained in /private/tmp/openkit-catalog-*.log. Installed Skill refresh and A2 deployment/live verification follow the commit; the original th_6 review decision is still pending.


## Deployed Catalog And Historical Failure Verification

Commit 0a6cb3a3ef541b1d19a5c335820901ce4518b9a2 was transferred as a tracked gzip archive with SHA-256 6b9179b11895ff8dca54a2e8978f1864d8cee5d191da4a0be9ac9bb34d658a10. A2 built immutable App image sha256:1841310e8d30b24e2f76475d8b70892f5c1b8edc26a5f00ba7edaa56029883ea and passed its image smoke. Fresh pre-replacement checks found zero active leases, uncleaned backends and queued admissions. The reviewed updater retained openkit-before-0a6cb3a3ef54, preserved existing environment and mounts, switched Web assets and returned healthy without restarting NanoHost. Evidence: /private/tmp/openkit-a2-catalog-{build,smoke,update}.log. The complete installed public Skill was refreshed and doctor passed with pinned Node 24.18.0; an initial invocation under the shell's unpinned Node correctly refused the host and was corrected by selecting the repository toolchain.

Before replacement the signed-in Agents page still displayed No agents yet. After replacement, public agent.list and ws_4 workspace.resources return exactly equal three-entry inventories: Codex Agent, OpenCode Agent and disabled Pi Agent. Every unspecified role is null, every health summary remains unknown with no probe message or stored check time, and agent.read for Codex exactly equals its list entry. The Web shows three Worker labels, Unknown for Codex and OpenCode, and Resting for disabled Pi. Refresh health completes without hiding entries or inventing successful runtime health. Its public response records the refresh time with unknown status; that acknowledgment is not a probe. Expanding Codex's native View details disclosure displays unknown health and model gpt-6. Evidence: /private/tmp/openkit-catalog-{doctor,list,resources,detail,refresh}-after.json and direct browser observations.

On ws_4/th_5, the two recorded failed Turns followed by three completed Turns now render exactly two history alerts, one at each failed Turn, matching thread.dashboard byte-for-byte. The missing-repository alert and current-AgentSession alert appear once each; neither the alerts nor the document overflow horizontally at the current viewport. Old stored diagnostics are not rewritten, so the newly improved missing-repository guidance applies to future failures only. Evidence: /private/tmp/openkit-history-dashboard-after.json and direct DOM inspection. The separate exact th_6 review/application decision remains pending; no review acceptance, apply or publication was performed.


## Maintenance Coverage And App-update Mount Recovery

The preceding goal turn deployed and proved the Agent catalog and historical failures, so it made progress. Claude consultation and Cursor's route audit separated remaining coverage from polish: Goal Mode and product-driven App replacement are not yet verified; Material is a published route-only surface, while the Generative directory is not a published route and is not an extra acceptance obligation. The exact th_6 review/apply decision remains the development-loop gate. No further UI redesign is inferred from this audit.

Live Web Knowledge created the explicitly marked acceptance record mem_1 in ws_4, and the public Skill returned its exact title and content. Web retrieval and public knowledge.retrieve both selected mem_1; Web Manager Answer returned the recorded content. This proves save/read/retrieval, not Worker context delivery or proposal promotion. Material mat_2539d3beab2b2e475f4712b5 was created through its published Web route with two immutable revisions, mrev_8f9094091a689d7af66a96a5 and mrev_41e3612755faaa9b12dba110. Web comparison showed the added rollback-check line, and public revision-list/read returned both revisions and their parent link. The Material remains unbound and neither test started Agent work. Evidence: /private/tmp/openkit-knowledge-{baseline,created,retrieval}.json and /private/tmp/openkit-material-{revisions,v1,v2}.json.

Public backup.create and backup.verify produced and verified drb_a3f6a86a-d450-4957-b6fc-0e6e27fd5a98. A read-only inspection of the backed-up ws_4 SQLite database confirms the exact pending th_6 review, its change-set row and patch bytes with original digest sha256:7ce33d501bbfe09e510706397848da7321cf217eee3572977608a58fb5f16c70; SQLite quick_check returned ok. The backup is labelled hot/crash-consistent, not a cold or Worker-storage snapshot. Evidence: /private/tmp/openkit-current-backup-{create,verify,review-proof}.json.

Both Web App-update Prepare and public app-update.status failed with app_update_unavailable because the configured SSH identity was not a usable regular file. Host inspection found the original protected credentials and restricted helper intact, but the fresh App container omitted their two read-only binds. This was a deployment omission; generic local app:run must not guess A2 paths or create host-update authority. After independent Claude review, a bounded root-owned recovery script checked the exact running image, root ownership/readability, zero active leases/backends/queued admissions and mount overlap; it recreated only the App with the two required file mounts, retained openkit-before-update-mount-20260917, preserved all existing mounts and environment, and checked both App/Web health plus the exact resulting mount set. NanoHost was not restarted. The immutable App image remains sha256:1841310e8d30b24e2f76475d8b70892f5c1b8edc26a5f00ba7edaa56029883ea. Evidence: /private/tmp/openkit-a2-restore-update-mount.py and /private/tmp/openkit-a2-restore-update-mount.log.

The consumer probe now reaches the host helper, and app-update.prepare successfully created receipt 44e5ff51-7b62-459e-b2ef-6737012d0f58 for the current exact commit 0a6cb3a3ef541b1d19a5c335820901ce4518b9a2. Public status and Web Refresh status agree on prepared, source and expected image. Web exposes the unchecked maintenance-consent switch and disabled Start update. No replacement was started through the product and no candidate acquisition or successful deployment is inferred from preparation. Doctor passes. The cookbook and installed public Skill now describe the existing file-mount diagnostic and recovery boundary. Evidence: /private/tmp/openkit-app-update-{status-probe,status-after-mount,prepare-after-mount,prepared-status}.json and /private/tmp/openkit-mount-restoration-doctor.json.

## Goal Plan And Model Selection Probe

The public Skill created shared ws_4/th_7, and Web Goal start created goal_c5ae00d575b3e030abfc5f07 for a read-only repository and maintenance-documentation audit using only OrcaRouter / DeepSeek Flash Free. Opening the Plan lens automatically generated a one-step plan. A subsequent redundant public goal.plan call returned goal_not_planning; the durable read correctly showed awaiting_plan_approval, not a planning failure. thread.dashboard preserves the exact objective and plan Item. No plan approval or bounded execution occurred. Evidence: /private/tmp/openkit-goal-probe-{thread,started,plan,current,dashboard}.json.

Cursor traced Goal dispatch to the current request actor, first-ready Agent selection, and setup resolution without an explicit model. Claude initially identified user.logicalModelId as a possible fallback, but the focused follow-up found no public current-user preference mutation; the Chat composer selects only the submitted Turn's model and runtime.file-list excludes user files. Live public configuration confirms Workspace default agent_codex_host prefers gpt-6 and allows orcarouter; the gateway maps orcarouter to deepseek/deepseek-v4-flash-free. Neither objective text nor an empty composer selector binds execution to that model. Claude confirmed that a per-step override would not constrain automatic steps and would require a separate public-command decision. The smaller acceptance route reuses existing Workspace model configuration: a temporary orcarouter preference passed public runtime.validate without restart, but was not written. The exact read-only plan and temporary Workspace-wide default change are awaiting the engineer response; no model contract extension is being implemented. Evidence: /private/tmp/openkit-goal-{runtime-files,codex-config,gateway-config,workspace-config}.json and read-only Herdr consultations.

Web observation found a stale planning label after plan creation and steering controls offering empty Agent, model and attachment selectors. Reload also lost the plan and approval controls because the read path repeated goal.plan and hid its rejection. These are projection defects independent of the unaccepted model-contract extension. The accessibility snapshot appeared to repeat the phase strip, but direct live DOM inspection found exactly one rendered ol; no duplicate-strip correction is justified. The public Skill loop reference's obsolete agent.get name was corrected to agent.read in f1c81ef9 and synchronized to the installed reference after operation discovery.


## Current Plan Recovery Correction

The reload failure originates in the Web plan query calling plan creation for awaiting_plan_approval rather than reading the existing owner. The bounded correction adds GET on the existing Goal plan path, the typed Core Client reader and public goal.plan-read, all using the existing thread.read policy and exact durable Goal plan pointer. It adds no plan lifecycle, model selection or approval authority. Missing storage returns 503; a contradictory plan record returns recovery_required. Independent Claude review found that the original new read and the existing summary selected the most recently updated terminal Goal ahead of an active one. Both now use active-first selection while exact-id command replay is unchanged; with no active Goal the prior last-updated terminal fallback remains. Private-Thread and no-write regressions cover the new reader. Final Web verification and commit follow.

Primary inspected the exact backend, schema, client, metadata, Skill and regression diffs. The independent review findings are closed, including a regression asserting active-over-terminal selection through both summary and plan reads. Direct reruns passed 32 NanoCore Goal tests, 143 App schema tests, 84 Core Client tests and 42 public Skill tests. Dependency builds and OpenAPI generation/validation pass; focused Biome has no errors and retains one pre-existing unused-parameter warning in an unrelated Vault operation. Documentation validation passes for 277 documents. Evidence: /private/tmp/openkit-goal-{plan-read-tests,schemas-tests,client-tests,skill-tests,reader-build,reader-lint,openapi,openapi-validation}.log. The backend/Skill correction is ready for its own commit; Web corrections remain independently owned and uncommitted. A2 has not received this reader yet.


## Goal Web Recovery Verification

Primary inspected the final six-file Web diff. The plan query reads the durable plan before creating, checks the freshly returned Goal state, scopes plan and uncertain-create request identity to the Goal within the existing QueryClient, and refreshes the summary after recovery or creation. Both Plan and Board consume the nullable read projection. Failed reads expose retry instead of an empty plan, and steering retains multiline input after a failed send without offering unsupported Agent, model or attachment controls. No phase-strip duplication existed in the actual DOM, so no mount workaround remains.

The final Goal Web file passes all 59 tests, including reload without plan creation, stable request identity after uncertain creation, replacement Goal isolation, fresh-state gating and multiline steering failure recovery. Focused Biome passes five files, Web typecheck and production build pass, and NanoCore build passes. The Web build retains its existing large-chunk advisory. Evidence: /private/tmp/openkit-goal-web-{tests,lint,build}.log and /private/tmp/openkit-goal-nanocore-build.log. A speculative running-phase plan projection check was removed because it would expand this pre-approval recovery correction into execution-status projection; it is not evidence for the accepted fix. A2 still runs the earlier build, and both exact human gates remain unanswered.


## Deployed Goal Recovery Proof

The exact e2fd174b71831df6e832b3c41f131e03a464d4e8 tracked archive has SHA-256 109c58f40d24cbef4bab16be900ff301f66840eedf3ecd197250160379cf50e0. A2 built and smoke-tested App image sha256:e16ed8a1679bb8aa4774c18fa020157d22fb13eef81a7354cf66c6f3ac0265f9. Fresh drain checks found zero active leases, uncleaned backends and queued admissions. The reviewed replacement retained stopped container openkit-before-e2fd174b7183 and preserved all nine mounts, exact environment, restart policy and log rotation; App/Web health passed. Worker source and NanoHost were not replaced. Evidence: /private/tmp/openkit-a2-goal-recovery-{build,smoke,update}.log and /private/tmp/openkit-a2-goal-recovery-preservation.json.

The complete installed Skill was rebuilt and synchronized; doctor passes and goal.plan-read returns the original Goal, exact plan Item and awaiting_plan_approval status. Browser reload now shows the original planned step plus Adjust plan and Approve plan; Board projects the same planned step. Direct DOM inspection confirms one phase strip, one steering textarea, one approval button and no horizontal document overflow at 809 pixels. Agent, model and attachment controls are absent from steering. The pre/post deployment public dashboards have exactly equal two Turns. The initial collector mistook the itemLog href object for an Item list; that count was invalid. A corrected public thread.items comparison around another verified browser reload returns exactly equal two Items and no next cursor, proving that this recovery did not regenerate the plan. The existing App-update receipt is still readable after replacement. Evidence: /private/tmp/openkit-goal-{doctor-after,plan-read-after,dashboard-predeploy,dashboard-postdeploy,app-update-after}.json, /private/tmp/openkit-goal-items-{before,after}-reload.json and direct browser observations. No Goal approval, Workspace model mutation or th_6 review acceptance/application was performed.

The live screenshot also exposes an independent presentation defect: the full Goal objective is used as a large heading and repeated beneath it, displacing the actual plan and approval controls. The bounded title/summary correction is committed and verified below; no durable Goal semantics or autonomy changes are inferred.


## Duplicate Goal Objective Heading Correction

Cursor traced the live oversized heading to existing Goal creation deriving its title from the first objective line. A single-paragraph objective therefore equals its title; Plan and completed closeout rendered that entire string twice. The narrow Web projection now uses a short contextual heading only when title equals objective, preserving distinct titles and showing the complete objective once as whitespace-preserving body text. Durable titles, objective bytes, approval gates and the public Skill contract are unchanged. Primary inspected both consumers and the exact diff. The new regression covers Plan and completed closeout; the full Goal test file passes 60 tests, focused Biome passes three files, and Web typecheck/production build pass. Evidence: /private/tmp/openkit-goal-heading-{tests,lint,build}.log. The static bundle is ready for an independent Web-only publication, without restarting App or NanoHost.


## Deployed Goal Heading Proof

The committed cddeee1831203637f69a2668852170a6241acaeb Web bundle has archive SHA-256 c6de09295779fd9d90f0912a71465fc5d2d8ed821c8213669efe14f7732b96f9. Under the existing deployment lock, it was extracted into a new commit-named Web directory and the current symlink was atomically replaced, preserving the prior e2fd174b assets. The served index exactly equals the new file; public health passes; the App container id and start timestamp are unchanged. Evidence: /private/tmp/openkit-a2-goal-heading-publish.json. App image remains sha256:e16ed8a1679bb8aa4774c18fa020157d22fb13eef81a7354cf66c6f3ac0265f9.

Browser reload displays Goal plan as a roughly 32-pixel heading, the complete objective in exactly one paragraph, and a usable Approve plan entry after the durable read resolves. The document does not overflow horizontally. Adjust plan opens its editor and Cancel dismisses it without submission. Both exact human decisions remain pending: the bounded th_7 plan plus temporary OrcaRouter default, and the th_6 README review/application. No autonomous approval, model configuration write or workspace change application occurred.


## Goal Planning Truth And Approval Visibility

The preceding goal turn made progress through deployed plan recovery and heading correction. Fresh public reads still show th_7 awaiting_plan_approval and the exact th_6 Workspace review pending. Evidence: /private/tmp/openkit-goal-current-gate.json and /private/tmp/openkit-readme-current-review.json. Neither pending decision has been answered or inferred from the broad work request.

The stored th_7 plan labels its assumptions as deterministic fallback for test support. Cursor traced the production App route through Workflow Coordinator into the existing shared one-task builder. The accepted Coordinator owner explicitly requires deterministic V1 behavior and does not require ML-based planning; no missing LLM injection or new planner is inferred. The correction changes only misleading user-visible assumptions, risks, review instructions and verification wording. Pre-approval decomposition review and post-Worker result review remain distinct. Task graph, fields, limits, authority and approval policy are unchanged; old immutable plans are not rewritten. The public Skill loop reference now explains the actual single-task drafting limitation.

Primary inspected the final code and test diff and directly reran 55 Goal-plan and Coordinator tests; all pass. Focused Biome passes three files. Evidence: /private/tmp/openkit-goal-draft-copy-{tests,lint}.log. The optional Skill quick validator could not import yaml in the local Python environment; no metadata or executable Skill changes were made, and repository documentation validation remains the deciding documentation check.

A separate source-to-live comparison confirms that PlanLens discards most full-plan fields before the approval gate: assumptions, risks, verification approach and task acceptance, resources, review, budget, dependencies and escalation facts are inaccessible. Goal Mode owner line 251 requires these executable-plan facts to remain explicit, and the Web projection owner uses the existing Core Client authority. Cursor owns the bounded Web correction using the current plan read and native disclosures; no public contract, second plan authority or mutation is added.


## Goal Plan Disclosure Verification

Primary resumed after compaction and reconciled the checkpoint with Git, delegated output and current owner. This remains the existing read-projection correction; no fresh direction check was needed for its ordinary commit. The earlier nullable-role answer is already implemented and deployed, as recorded above; no duplicate catalog work was started.

Primary inspected the final PlanLens, test and guide diff. Native disclosures expose all current pre-approval plan facts without a new contract or mutation. Duplicate-allowed arrays retain distinct keys; cached immutable plans cannot replace current Goal Task rows after approval. Direct verification passes 62 Goal Web tests, focused Biome on two files, and the Web production build including TypeScript; the existing chunk-size advisory remains. Evidence: /private/tmp/openkit-plan-details-{tests,lint,build}.log. The next effect is an exact-source App/Web deployment to A2 through the existing reviewed updater, preserving current mounts, environment, retained data and NanoHost. A fresh zero-work preflight must precede replacement; image smoke and live public reads will decide whether to retain or roll back.

A separate investigation confirmed that pre-approval goal.plan.revise records the human instruction but goal.plan drafts again from unchanged title and objective, ignoring that instruction. Claude independently confirmed this conflicts with material Plan revision. Its proposed repair reuses the accepted Goal-scoped Orchestrator and internal-agent runtime, retaining deterministic control and explicit human approval. Role/profile selection and failure behavior are still under read-only investigation; no semantic revision implementation or automatic approval is included in this commit.


## Deployed Goal Plan Disclosure Proof

Claude independently inspected the resumed deployment direction, existing updater and current change and found no intent or route objection. The exact 2cd35062e28924ff11576aef48aa3a241858416f archive has SHA-256 d8919101ea71d76921e6d3c5ce22fcd6aef41671a83972398a4e2b1e86fed396. A2 built and smoke-tested App image sha256:c48367ac2af414b53e9788469c5aaf8de78971a79e8e34f0234cfdf229b32a7d. The initial invocation incorrectly used the Docker build config digest; the updater rejected it before stopping the service. Its empty attempt directory was removed with rmdir, and a fresh drain check again returned zero active leases, uncleaned backends and queued admissions before replacement with the actual inspected image ID. The previous App remains stopped as openkit-before-2cd35062e289. App/Web health, exact environment, all nine mounts, restart policy, network and log configuration pass; NanoHost and Worker source are unchanged. Evidence: /private/tmp/openkit-plan-details-a2-{build,smoke,update,update-corrected}.log and /private/tmp/openkit-plan-details-preservation.json.

The installed Skill doctor is ready, and goal.plan-read retains the exact original plan Item it_goal_plan_tu_goal_plan_a5a17c022a8fbe8724979069 with awaiting_plan_approval. Browser Plan details and Task details both expand to labeled facts, including assumptions, risk, verification, full objective, acceptance, budget, resources, outputs, checks, review and escalation. At 824 pixels the document width equals scroll width and no main paragraph or list item overflows. The before/after thread.items payloads are exactly equal, with two Items and no next cursor. Evidence: /private/tmp/openkit-plan-details-{doctor-after,plan-after,items-before,items-after}.json and direct browser DOM/screenshot observations. The immutable old Plan intentionally retains its historical fallback wording; the new production copy applies only to newly created drafts and does not repair ignored revision instructions. No approval or configuration mutation was performed. A separate browser check again confirmed all three configured Agents carry the Worker role label.

## Pending Pre-approval Orchestrator Decision

Source inspection confirms goal-orchestrator already exists as a product target, role IDs are open strings, and internal-role-profiles.jsonc has public Web/Skill read, schema, validation, update and deferred-reload paths. Configuration alone cannot implement planning. Claude and primary agree that the exact eight-Tool role contract needs an explicit pre-approval-only exception before shipping a planning-only assembly with goal.plan.propose; the full eight remain required for post-approval progression. The concrete proposal is retained at temp/changes/202609160700000000-self_hosted_development/goal-preapproval-revision-proposal.md and has been presented to the engineer. No dependent implementation has begun. The proposal must also revisit deterministic-copy assumptions if model-produced multi-task drafts become possible; current documentation is not evidence that semantic revision works. The existing exact th_7 approval/model-default and th_6 review/application decisions remain unanswered and separate.


## Artifact Report Acceptance Probe

The preceding turn made progress through commit 2cd35062, exact A2 deployment and live no-mutation Plan disclosure proof. The three separate engineer decisions remain pending. The next independent observable is a normal maintenance report imported through Web, read verbatim through the public Skill, introduced as a reference into a dedicated acceptance Thread and opened there. This exercises actual reusable development output rather than internal change-review records. The report contains only synthetic acceptance text and already-public build facts; no credentials or protected file content. Cursor is read-only on the existing Artifact and Thread owners to identify whether reference introduction admits model work. No model invocation or unrelated Thread mutation is presumed.


## Artifact Handoff And Chat Refusal Finding

Web imported ar_import_df8dc1fd3b51cbea8aebc042 v1, then introduced it into dedicated shared Thread ws_4/th_8. Public read verifies exact content digest sha256:d05430850440b4db5353a6bad3166b6a095b2282bdf217ef3911544a3868e833 and unchanged imported origin, null Thread/Turn ownership and immutable bytes. Introduction created only completed Turn tu_artifact_08a235242f5212beefd29ae5 with durationMs=0 and one exact artifact-reference Item; Web opened the same report from the Thread side panel. Evidence: /private/tmp/openkit-artifact-roundtrip-{list,read,after,thread,dashboard,items}.json and direct browser observations. The Conversation picker initially lacked the new Skill-created Thread; source inspection shows a one-shot Core list distinct from foreground-polled navigation, so one reload completed this probe. No new polling mechanism is inferred.

A subsequent Web Assistant submission explicitly attached that report and selected OrcaRouter. The Chinese request asked only for its marker and Goal Web test count. Turn tu_19 completed with Chat Mode request refused / External search is not enabled for Chat Mode. The sole shared isExternalSearchChatPrompt predicate incorrectly matches standalone web before Artifact/provider assembly. The Chat owner requires refusal of explicit external search, not topic mentions. Cursor owns only mode-entry-routes.ts and server.test.ts for the smallest phrase-based correction and exact live-shaped failing regression, preserving explicit-search refusal even with attachments. Primary owns guides, public Skill, live verification and this record. No external capability, model router or public contract change is introduced.


## Artifact Search Navigation Correction

Live Search correctly finds the maintenance report by title. Body-only marker search has no hit because the existing Artifact matcher indexes title and summary, not content; no full-text expansion is included. Clicking the exact Artifact result currently discards its id and opens only the inventory, requiring another manual selection. The bounded Web improvement preserves the exact authorized search hit in existing preview selection and navigates within the hit Workspace. Cursor owns AppSearch, ArtifactsScreen and their existing focused tests; primary owns guides and this plan. Missing or inaccessible targets must remain unavailable without displaying unrelated content. The observable regression is that selecting a result opens its exact content, while ordinary import, reference introduction and output-catalog exclusions remain unchanged.


## Explicit Artifact Replaced By Knowledge Finding

A second live diagnostic removed the Web topic word while retaining the exact attachment and OrcaRouter selection. Turn tu_20 answered with an unrelated prior Knowledge acceptance marker instead of the attached report marker and count. Public thread.items and browser agree; evidence is /private/tmp/openkit-artifact-diagnostic-items.json. The shared Assistant branch invokes answerFromWorkspaceKnowledge with the entire conversationPrompt before provider delivery, allowing lexical matches from attached content to replace the requested source. This is a separate correctness failure, not evidence that Artifact-to-model delivery passed. Claude is checking the accepted source-selection and Composer owners before a bounded correction; no Knowledge storage or retrieval engine redesign is inferred.

Claude confirmed the admitted-Artifact source substitution is ordinary correctness under Chat owner line 268 and the unified Composer contract; the bounded repair skips only the Assistant knowledge-only shortcut for explicit admitted Artifacts. The separate Knowledge Manager target remains intact. Inspection also rejects a speculative zero-token Chinese explanation: index-rebuild.ts uses Unicode letter/number tokens, while the actual query includes the English attachment body and therefore shares maintenance/acceptance terms with unrelated Knowledge. No retrieval-engine change is justified by this probe.

The exact original request exposes another independent topic/action error after search classification: shared Worker Coordinator isGoalPrompt matches the bare Goal word in Goal Web and would create a planning handoff. Claude confirmed that tightening the existing deterministic predicate toward explicit planning intent restores its accepted contract and follows the prior concrete-PR heuristic correction. A separate Cursor writer owns only worker-coordinator.ts and its tests; actual planning and multi-step work must retain their existing route, and no attachment blanket bypass is allowed.


## External Search Classification Verification

Primary inspected the released predicate and all new regression bytes, then directly reran 15 focused server regressions with all passing and focused Biome with two files clean. Cursor also reports a passing NanoCore TypeScript check; its exact output was inspected. Evidence: /private/tmp/openkit-chat-search-{green,lint}.log. The exact live Chinese prompt reaches the provider with its admitted Artifact in a Quick Chat Workspace; this deliberately isolates external-search classification and does not claim the separate project-Workspace Goal false positive is repaired. Explicit external requests, including requests carrying attachments, remain refused. The matcher remains a bounded English phrase heuristic, not general natural-language intent classification. No external search capability was introduced.


## Artifact Source Precedence Verification

The new public-import regression first reproduced a successful but wrong Knowledge-only answer in place of an explicitly attached report; earlier fixture storage/membership failures were corrected and are not regression evidence. The one-condition repair skips the Assistant shortcut only when admitted Artifacts exist. It preserves the explicit Knowledge Manager target, authorization, external-search and delegation paths. All 24 quick-chat tests pass, including the unchanged no-attachment Knowledge answer, and NanoCore TypeScript passes. Cursor independently inspected the actual diff, both shortcut callers and the named red output and reported no findings. Evidence: /private/tmp/openkit-artifact-precedence-{red,green,typecheck}.log. The app guide and public Skill now state source precedence; real OrcaRouter verification remains due after deployment.


## Goal Topic Routing Verification

The final shared Coordinator predicate distinguishes planning requests and actionable multi-step execution from Goal/roadmap/strategy topic mentions. Primary rejected an intermediate builder version that downgraded actionable multi-step execution to a direct Worker Turn; the final positive keeps Goal planning and no Worker request. The original mixed-script live prompt now reaches the provider in the shared project Workspace even with competing Knowledge, as observed by the amended public-route regression. All 51 Coordinator and 24 quick-chat tests pass, and focused Biome passes three files. Evidence: /private/tmp/openkit-goal-topic-{green,lint}.log. The internal guide and public Skill name the bounded English inference ceiling and explicit Goal entry; no model classifier, capability, durable lifecycle or human-gate change is introduced.


## Artifact Search Navigation Verification

The final Web change uses the existing shared search-route helper, authorized preferred-Workspace resolver and Artifact preview. Both Workspace and Artifact identity survive reload; unknown or inaccessible identities show unavailable states. The existing sidebar switch action clears Artifact identity when selecting another Workspace, avoiding a duplicate route-synchronization mechanism. Primary rejected intermediate competing effects and inspected the final producer/consumer diff. All 110 Sidebar, search/operations and Artifact tests pass; focused Biome passes six changed files, Web TypeScript/production build and NanoCore build pass, and documentation validation passes 277 documents. The Web build retains its existing chunk-size advisory. Evidence: /private/tmp/openkit-artifact-navigation-{tests,lint}.log, /private/tmp/openkit-artifact-{web-build,nanocore-build,docs}.log. The next effect is an exact-source App/Web replacement on A2 after fresh zero-work drain and image smoke, preserving all mounts and NanoHost; live tests must validate the original question with OrcaRouter and exact search preview navigation.

## Artifact Reference Restart Incident

The exact 8d692810 App image built and passed image smoke, and fresh drain reported zero active leases, uncleaned backends and queued admissions. Deployment failed health and rolled back, but the old image also could not boot: both Chat and selected Worker attachment producers used index-based Item ids while canonical restart validation requires artifactReferenceItemId. Existing ws_4/th_8 Turns tu_19 and tu_20 from the preceding live acceptance exposed this latent producer defect. The loader correctly retained its integrity refusal.

Cursor corrected both producers to reuse the existing helper. Primary inspected the actual diff and removed source-string assertions in favor of the real public-submit, persistence and FsStore reload regression. All 24 quick-chat tests and focused Biome pass. Admission accepts only the current Artifact version, so two distinct versions cannot both pass into the same Turn. No new identity algorithm or compatibility loader was added.

Claude independently reviewed the repair approach. With the App stopped, a full data-root reference scan found only the two Item logs, derived search index and historical boot-audit error descriptions. The operator preserved the three touched files outside the data root under /home/ubuntu/openkit/rebuild-20260916/artifact-id-repair-20260916220754, replaced only the two proven malformed identities using the canonical hash formula, and retained audit history unchanged. The old App restarted successfully; public health and Skill doctor confirm readiness. User content, Artifact origin/version/body and runtime records were not changed. Evidence: /private/tmp/openkit-artifact-{a2-update,rollback-logs,id-tests}.log and /private/tmp/openkit-artifact-doctor-recovered.json. The public Skill now explains this boot-failure boundary and the required operator recovery without offering a validation bypass.

## Repaired Deployment And Live Artifact Acceptance

Committed 9db8e0ccb278532f7dd57a00e06e8ce0fc0482d5 was archived from Git, verified remotely with SHA-256 b1422893f5da043e056621b510f05c66e86fc170be0e272cd690f1f9c78f3de8, built as immutable App image sha256:b42af9bf2ba1dd6db2c791c794f0f68ad93f227f392c42b906046225394c563e and passed image smoke. Fresh drain again returned zero active leases, uncleaned backends and queued admissions. App/Web replacement succeeded with all existing mounts and NanoHost preserved; prior container openkit-before-9db8e0ccb278 remains retained. Evidence: /private/tmp/openkit-reference-fix-{build,smoke,deploy,doctor}.log or .json as applicable.

Public Skill conversation.submit request 4332a71a-eb90-4772-90ad-70ef6a1fcd5f created tu_21, and Web request b96e6da7-8cee-45fb-9628-473d32860c90 created tu_22 in ws_4/th_8. Both used the original Chinese Goal Web question, exact report v1 and OrcaRouter. Both returned the correct artifact-roundtrip-c7f46a19 marker and 62 count without a Goal handoff, external-search refusal or Knowledge substitution. Public usage records attribute successful calls to orcarouter, with 451 and 394 reported tokens respectively. These records identify the logical model/provider, not an independently observed upstream model. Both new references use canonical identities. A fresh zero-work drain, real App restart, public health and post-restart thread.items proved both complete answers survive recovery. Evidence: /private/tmp/openkit-reference-{skill-answer,live-items,live-usage,postrestart-items}.json.

Web global Search for maintenance report now navigates directly to /artifacts?workspaceId=ws_4&artifact=ar_import_df8dc1fd3b51cbea8aebc042 and displays its exact body; refresh preserves the same identity. The repaired older Item logs were compared field-by-field with preserved originals, proving only the two reference ids changed. The public knowledge.indexes read regenerated derived indexes after recovery, request 98374e90-6222-4c77-81de-f4e25c9543d7. A full Workspace JSON/JSONL prefix scan found no remaining malformed conversation-reference identities. No pending Goal plan, repository apply or workspace model change was approved during these checks.


## Output Index And Product Update Acceptance

The preceding turn made verified progress through four conversation/search corrections, canonical attachment identity repair, exact deployment and successful Web/Skill/restart acceptance. The next independent maintenance seam is the deployed public App-update path; the three existing Goal/review decisions remain unanswered. Read-only host probes found the installed helper predates the committed backup-mount preservation and its protected administrator credential still refers to the retired pre-rebuild server. The current host helper receives 401 for diagnostics. The existing credential slot was refreshed with the already authorized current endpoint administrator credential over SSH stdin, with no plaintext output or scope expansion; diagnostics now returns the observed core_0000_setup migration. The current helper suite passes 51 tests. These are operator deployment repairs, not a new product privilege or automatic credential migration.

Live th_8 Side panel also repeats the same Artifact v1 for introduction and subsequent attachments. DESIGN.md section 3.3 owns an auxiliary index, while the stream owns history. Cursor corrected only the local panel projection to keep the first row per Artifact/version and every file-change record. The regression keeps duplicate references in the stream and distinct versions in the panel. Core records and public Skill semantics remain unchanged. Primary inspected the actual two-file diff; focused verification and candidate preparation follow.

The entire Chat Web file passes 95 tests, focused Biome passes both changed files, Web TypeScript/production build passes with the existing chunk-size advisory, and documentation validation passes 277 documents. Evidence: /private/tmp/openkit-output-index-{tests,build}.log. Host helper installation now matches the committed backup-aware implementation; the old helper is retained outside Data Root. Product start requires consent bound to the prepared source/current-image receipt under the App-update owner; operator repair does not satisfy that separate decision.

## App Update Repository Mount Blocker

A direct read-only call to the current helper shape guard refused the live authorized /srv/repos bind before candidate staging or App interruption. The fixed mount inventory supports Data Root, backup, transport sink, Web, Caddy, Vault and update SSH files, but omits the host-linked repository required by this development Workspace. Removing that bind would defeat the goal. Claude rejected silently accepting any observed repository source because the protected operator configuration owns mount authority. The bounded correction adds optional repositoryDirectory only to that protected host configuration: when configured, require the exact existing read/write /srv/repos bind; otherwise retain the existing rejection. Public requests gain no paths, mount choices or privilege. Cursor owns helper and regression files; primary owns operator/public Skill guidance, review and host acceptance. The existing preservation contract remains the owner, and no arbitrary-mount support is added.

Primary reviewed the complete helper and test diff, including the configured source, bind type, read/write requirement, no-config path and pre-stop refusal assertions. The helper suite passes 54 tests; six always-on host-fixture checks pass and the opt-in Linux/systemd/Docker effect test remains skipped. These local checks do not establish a real App replacement. Documentation validation passes 277 documents. Evidence: /private/tmp/openkit-repository-mount-{tests,host-tests}.log. Operator and public Skill references now describe the existing mount declaration and stale helper-credential recovery without granting host authority to a public request.
