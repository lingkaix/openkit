---
type: change-plan
status: in-progress
started: 2026-09-03
branch: codex/r058-worker-mcp
---
# R058 Worker MCP

## Intent Epoch 1

- **Source:** Engineer instruction in the current task, `/home/ubuntu/handoff-a2-testenv-20260903.md`, and `docs/roadmap.md` R058.
- **Requested outcome:** Deliver the accepted R058 worker MCP plane so a governed Codex Worker can discover and invoke Workspace-enabled stdio and Streamable HTTP MCP tools through the existing Gateway path.
- **Non-negotiables:** Preserve accepted lifecycle, schema, credential, policy, approval, Item, usage, audit, bounded-failure, and teardown semantics; keep R005 parked and independently mergeable; use no compatibility path; make the smallest coherent change; do not restart, replace, or lifecycle-manage the A1 Codex App Server or any A1 service; use A2 only as the authorized test and staging host and attribute every retained result to machine id `34b95b09f8c1475ea242800759411fea` and the exact product commit.
- **Acceptance observations:** A packaged Codex Worker discovers and calls a credential-free stdio tool through the single Sandbox Integration path; an HTTP tool receives a gateway-only Vault credential without leaking credential or upstream topology; an approval-required call ends before upstream contact and only a fresh call in a new Turn can execute after approval; every product-visible call has coherent Item, schema snapshot, CapabilityCall, usage when upstream was contacted, audit, and typed bounded outcome evidence; teardown leaves no owned child process or live credential material.
- **Exclusions:** Marketplace, registry discovery, OAuth browser flow, HTTP+SSE compatibility, arbitrary API proxying, server sandboxing, Web catalog editing, OpenCode and Pi delivery, multi-worker concurrency, HA, durable process registry, streaming results, automatic schema repin, generic connector or capability frameworks, and A1/A2 lifecycle effects.
- **Effect boundary:** Repository edits, package installation, temporary local child processes, isolated local test data, candidate commit and push, PR submission, and one rollback-protected A2 staging deployment are allowed; tests may terminate only their own children; the completed pin probe used the engineer-authorized Codex subscription in an isolated temporary `CODEX_HOME`, while deciding feature checks use deterministic local inference and MCP stubs; no A1 service lifecycle effect is allowed, and merge remains a separate decision.

## Intent Epoch 2

- **Source:** Engineer steering on 2026-09-05 referencing PR #4 and its handoff.
- **Requested outcome:** Finish and merge PR #4 first; then update only the Codex and PI Agent Workers to current stable releases and Open Knowledge Format to upstream v0.2, and pause subsequent roadmap features.
- **Effect boundary:** The engineer now authorizes merging the completed PR. The existing A2 test/staging authorization and credential handling rules continue; no A1 lifecycle effect is authorized. Preserve staging data with a verified recoverable backup before deployment changes.
- **Acceptance:** Retain the existing R058 acceptance requirements. The subsequent version refresh is a separate change after this PR is merged.

## Owning Documents

- `docs/core/agent-capability.md` owns capability terminal semantics, Item projection, Gateway mediation, and credential boundary.
- `docs/core/vault.md` owns injection plans, uses, receipts, uncertainty, revocation, and teardown.
- `docs/specs/20260704-worker_mcp_tool_supply.md` owns the R058 MCP catalog, transports, tools, schema snapshots, policy, approval, usage, failures, and acceptance.
- `docs/specs/20260703-worker_agent_capability.md` owns the worker-local capability route and independent credential boundary.
- `docs/specs/20260704-capability_usage_gateway_foundation.md` owns shared CapabilityCall, UsageRecord, idempotency, terminal, and recovery behavior.
- `docs/specs/20260531-human_attention_intervention_model.md` owns approval gates and forbids implicit current-call or AgentSession resumption.
- `docs/specs/20260629-worker_runtime_communication_model.md` owns the shared Worker Shim contract and single Sandbox Integration path.
- `docs/specs/20260716-codex_worker_adapter.md` owns the pinned Codex adapter and inline native configuration projection.
- `docs/specs/20260802-nanohost_runtime_and_transport.md` owns NanoHost carriage, stream, and byte bounds.
- `docs/specs/20260529-test_strategy.md` and `docs/verification-instruments.md` own test level and deciding-evidence quality.

## Accepted Direction

- The independent Consultant accepted ignored proposal SHA-256 `b2bc88232b392501f660b65656d9d72a47d1a0bc80bcb574ddcb4e42d7c18504` after two correction rounds.
- R058 starts from `origin/main`; the parked R005 ref remains untouched, while R058 ports and independently accepts only the minimum shared CapabilityCall terminal vocabulary, atomic terminal AuditEvent, and restart-`unknown` seam required by accepted Core semantics.
- The Workspace MCP catalog mirrors the existing deployment-admin runtime-config pattern and is the sole owner of stdio commands, HTTP endpoints, finite transport-specific credential bindings, tool rules, approval marks, timeouts, and schema policy.
- AEP freezes only worker-safe catalog digest, server id, tool rules, approval marks, schema policy, and an optional existing pinned snapshot id; it never projects command, endpoint, credential reference, or live schema guesses.
- NanoCore uses the official TypeScript MCP SDK directly, while Worker Shim transparently relays its bytes through the existing Sandbox Integration stream and fixed loopback listener; the process-local NanoCore supervisor has no durable registry; Codex receives only a fixed loopback MCP URL through inline `-c` arguments.
- The third Turn token has its own raw-token injection, durable hash and active-lease lineage binding, restart verification, terminal cleanup, and cross-family rejection.
- MCP policy reuses `tool.use` with server and tool resource context; approval ends the current call as typed denied before upstream contact, expires after one hour, then a later new-Turn call must reauthorize and may durably claim exactly one granted, unexpired, unconsumed proposed-effect tuple through a CapabilityCall id derived from the Approval id without receiving that id or resuming the prior AgentSession; its fresh allow decision links the granted terminal decision and current call through context without reusing the Approval id.
- A deterministic Item id binds each product-visible call; CapabilityCall and Audit terminalize before terminal Item publication, and startup reconciliation fills only a missing Item from the durable terminal winner.
- Upstream contact creates exactly one `tool_calls=1` UsageRecord even when the later outcome fails, interrupts, or becomes unknown; pre-effect invalid or denied calls create no usage.
- Vault handling follows Plan, audited Use, and proved-sink Receipt order; only a possibly transmitted tool request with unprovable outcome becomes `unknown`; exact resolved credential bytes cause whole-result rejection if echoed.
- Schema snapshots reuse the existing table with canonical tool and object-key ordering, deterministic digests, a fixed tracking retention bound, explicit current selection, pinned comparison, and snapshot id attribution on CapabilityCall.

## Current Evidence

- Git branch `codex/r058-worker-mcp` was created from `origin/main@9a87cee9028fbe4dd985c24b384ca4b81ddd25d0`; parked R005 remains at `origin/codex/phase1-host-recovery-campaign@b0871db3df2518283d57d1fda5335968ecddad2f`.
- The pre-branch fresh-context Verifier returned `Continue`; the Consultant then returned `DISAGREE`, direct artifact inspection confirmed its owner conflicts, and the corrected proposal received Consultant `AGREE` at the exact hash above.
- The post-Reframe Verifier accepted the separate-main branch and narrow R005 seam, and required branch creation, bounded pin probe, this plan, then production changes in that order.
- The bounded probe installed stock Codex CLI `0.144.1` under ignored `temp/r058-pin-probe`, ran it with `--ignore-user-config --strict-config`, and projected `mcp_servers.probe.url` through inline `-c`.
- The probe completed Streamable HTTP initialize, tool discovery, and exactly one `echo({text:"pin-ok"})` call, returned `echo:pin-ok`, and left the call observation at `temp/r058-pin-probe/run1/calls.jsonl`; its owned MCP child was terminated by exact PID after completion.
- Current `origin/main` still has the documented CapabilityCall terminal/recovery divergence; the candidate seam exists in R005 commit `582ac99` but that commit also contains unrelated Phase 1 paths and will not be cherry-picked wholesale.
- The first independent review blocked unsound message-based timeout inference and provider-originated `AbortError` cancellation inference; the corrected classifier now requires typed timeout evidence or the current caller signal's exact reason, with attributed regressions for both negative cases and genuine caller cancellation.
- The post-compaction Verifier confirmed that correction and then found the committed App API OpenAPI projection stale; the candidate now preserves the canonical lifecycle metadata through the capability usage read model, tests its representable constraint with AJV, and regenerates the tracked artifact.
- The second independent review accepted both corrections but found three stale owning regressions; Worker inference now asserts CapabilityCall `aborted` while retaining AuditEvent `cancelled`, and boot recovery asserts `unknown`, with both complete affected test files green.
- The post-compaction Verifier rejected premature capability enablement and arbitrary inline stdio environment values; the corrected catalog uses only Vault-binding slot templates, scheduler dispatch carries the actual dequeued Workspace catalog to worker startup, and capability mode remains disabled until the executable plane exists.
- The correction review found two remaining premature contracts; the prerequisite now keeps the public AEP capability schema disabled-only and permits schema snapshot recording only from an explicit live source.
- The normal Reviewer then found that loop-global catalog inputs could attach the initiating request's Workspace catalog to an older dequeued Turn and left periodic retry without any MCP catalog. The corrected scheduler selects both catalogs by the dequeued entry's Workspace, uses its durable roots and working directory, derives source references from its selected manifest, and removes the dead pre-admission catalog-context helper.
- The fresh post-Reframe Verifier accepted that selection but found the periodic service still closed over the process-start snapshot, contradicting session-scoped safe reload. The service now reads one complete current snapshot per retry iteration, without catalog-specific state or queue persistence, and its regression proves a catalogless first attempt succeeds after the snapshot gains the required Workspace catalog.
- The renewed Verifier returned `Continue`, and the final normal Reviewer returned `ACCEPT` after independently rerunning the scheduler, Config Schema, Worker Shim, and diff checks and reproducing the single unrelated NanoCore baseline failure.
- The executable-plane candidate now includes the SDK-backed stdio and Streamable HTTP gateway, catalog and schema snapshot handling, Vault injection and teardown, governed calls, usage and Item projections, one-shot approval claims, and startup Item reconciliation; focused NanoCore typecheck and 24 route, ledger, approval, concurrency, expiry, and recovery tests pass.
- The latest independent Consultant reviewed proposal SHA-256 `c942773ba677a696cb32b430b2a1c06965fcc74f381977bb883f83e69f74478e` and returned `DISAGREE`: the candidate does not yet connect an approval-required MCP call to the production Worker Gate stop and mode checkpoint lifecycle.
- A fresh Verifier returned `Reframe`: Core Protocol and Worker Reliability already resolve human denial to Product Turn `interrupted` and checkpoint `aborted`, while Task `cancelled` is only a mode projection and Goal Mode disclaims Gate mechanics; current `cancelled` implementation bytes are an authority divergence, not contrary authority. The proposal is corrected at SHA-256 `0340b3d373c371c036a72f6cee75b70c854e6486ffa9b2c62dc1ffe8acf41a2d` and awaits renewed independent agreement.
- After two further Consultant correction rounds, proposal SHA-256 `96aede4a718a018a0e03e4899bb9a1c47eec4f58b805ab70715d0891b15b20a5` received `AGREE`: the accepted scope is only the delivered Codex session-continuity path, `turn.interrupt` purpose is exactly `interrupt | human-gate`, its stop admission CAS covers only the existing Core DB projections, and source AgentSession closure is explicit. The corrected independent Auditor returned `ACCEPT`, and the renewed independent Verifier returned `Continue` after confirming the narrow purpose schema, truthful transaction boundary, asynchronous settlement, and Core `interrupted` mapping.
- The completed Gate slice now admits an actionable `tool.use` decision only for one exact Worker lease, preserves partial cross-store state as `recovery_required`, accepts `blocked/ask_user` only after exact Gate and cleanup evidence, closes a granted source Turn and AgentSession, closes a denied source Turn and AgentSession as interrupted, and lets only a fresh successor Turn claim the exact granted effect once.
- Product and verification bytes are committed through exact candidate `16bc6b5427113ca067e766a3db2475467af485de`; exact story SHA-256 `83f6941d6df5f8e9bc1b7aca0814301336a5e7c955de1cccc3a78fe4772e75cc` received independent L6 admission acceptance, while final feature acceptance remains open.
- The admitted story's sole actor ask is "Find and report the repository details and current issue list for this workspace." A competent actor needs only that ask and the declared persona: the two requested results make the available repository-detail and issue-list capabilities relevant without naming MCP, Gate, call order, or retry behavior; task completion, the visible approval, redaction, and the named public AEP, call, approval, teardown, usage, audit, and schema records are end-state or invariant observations produced by completing that intent rather than a prescribed trajectory, so every required assertion is satisfiable from the persona and sole ask.
- Exact-candidate focused evidence is green without an A1 lifecycle effect: 383/383 selected NanoCore action, approval, recovery, gateway, route, simulator, server, image-contract, migration, and OpenAPI tests pass; the gateway file separately passed 28/28 on three consecutive cold invocations; 185/185 Worker Shim tests, 159/159 Config Schema tests, and 169/169 Protocol tests pass; NanoCore, Worker Shim, Config Schema, Protocol, and NanoHost builds pass.
- The full NanoCore suite has 2,553 passing tests and five failures independently reproduced as the same 5/69 subset on `origin/main@9a87cee`: one stale startup-phase expectation, three pre-existing unsupported non-file filesystem fixtures, and one Vault fixture without `workspace_registry`; no R058 test fails.
- Repository governance and formatting pass over 214 documents and 976 files, NanoCore lint, typecheck, and build pass, NanoHost formatting and Clippy pass, and `git diff --check` passes.
- The built NanoCore L5 smoke and its exact shared adjudicator at script SHA-256 `6072addacb4dfac2c2967fd6ea6f71fd2e4c00c73c695589d1057fc98f09f969` are admitted on 2026-09-04. `node tests/smoke/nanocore-worker-mcp-smoke.mjs --self-check` is the check; its stable complete observation is the pass target, `cleanup.listenerClosed=false` is the deliberate deciding-observation intervention, and a never-settling observation provider is the 25 ms timeout intervention; all three pass through `runHarnessAdmissionSelfCheck` → `settleSmokeAdjudication` → `expectedSmokeObservation`, the same `main` → `settleSmokeAdjudication` → `expectedSmokeObservation` path used by the product run; observed outcomes were respectively `pass`, `fail`, and `timeout`, the stand-in marker remained present until adjudication completed, and the following full root smoke reported Harness Admission, Worker MCP product, NanoCore health, and Web preview PASS.
- The admitted L5 adjudicator oracle is `Prior`: its expected object and accepted owners predate the adjudicator and the falsifier confirmed the expectation would not change with different product bytes; `Bounded`: the falsifier confirmed it compares one finite exact observation object and can answer all fields; `Reproducible`: independent producer and reviewer runs returned the same three self-check outcomes and product verdict from the same observations; and `Re-runnable`: measured self-check execution is sub-second and the full product smoke completed in 15.3 seconds, so both can run after every adjudicator change.
- A2 machine id `34b95b09f8c1475ea242800759411fea` still runs the healthy staging App at `c478f4dd984d758772dcd565b963bcf9041b76d1`, but its current NanoHost baseline is fail-stopped at runtime target `97:1:0:0`; two explicit A2-only start attempts on 2026-09-04 completed the private runtime bootstrap but did not publish readiness. No candidate deployment or L6 run has begun, and the App, Caddy, data, and deployment pointers remain unchanged.
- The full NanoHost package run has 82 passing tests plus five unrelated baseline/environment failures: four stale source-string assertions expect an absent `Starts the fixed NanoHost` marker in unmodified `main.rs`, and one recovery test reads the live host's root-only `/var/lib/openkit/nanohost`; the exact changed capability carriage and outer-projection tests pass independently.

## Current Checkpoint

- **Current intent:** Intent Epoch 2 controls sequencing: finish and merge PR #4, then refresh only Codex, PI Agent, and Open Knowledge Format v0.2. Further roadmap features remain paused.
- **Current Git:** The corrective product candidate is committed and pushed at `09d8b77cd0febacc63dc5e9906cf389496455f11`. The following documentation reconciliation changes no product bytes; L6 attribution remains this immutable product commit.
- **Correction and direct evidence:** Independent review reproduced two blockers: Linux-only `setsid`/`setpriv` broke the supported macOS source path, and a query credential echoed in URLSearchParams wire encoding bypassed exact raw-value filtering. The corrected Gateway uses native detached Node process groups and a private IPC parent-death channel, reuses the official SDK's role-neutral stream transport, preserves bounded group-absence proof, and rejects raw and transmitted query credential forms. The App image no longer installs or asserts obsolete Linux helper commands. Descendant-written credential digest receipts preserve the inherited-secret oracle on supported POSIX hosts.
- **Independent design scrutiny:** The portable-supervision Consultant accepted the smaller native IPC design after actual SDK connect/call/error/overflow/cleanup probes and 20/20 parent-death repetitions. The normal Reviewer independently accepted the query-credential fix and smoke cleanup guard and observed the portable stdio session plus the three credential-bearing cleanup tests pass on macOS; the final independent Reviewer accepted the exact corrective diff with no actionable finding.
- **Verification correction:** The original smoke cleanup awaited an already-emitted child exit and masked the primary failure with exit 13; a terminal-child guard now preserves the original error. The idle-use test mixed Undici dispatch with a simulated clock and failed in the full file despite an isolated pass. Its corrected lowest-sufficient oracle holds one SDK tool-call promise unresolved, explicitly checks that it is still unresolved at 60 seconds, then resolves it and observes eventual idle cleanup. The real HTTP transport, concurrency, credential, timeout, and byte-bound tests remain separate deciding checks. Diagnostic markers and unsuccessful polling experiments are absent from the final bytes.
- **Current local checks:** On pinned macOS Node `24.18.0`, Gateway tests pass 29/29 and NanoCore typecheck and lint pass. The complete NanoCore suite reports 2,557 passing tests and three failures: the unchanged startup-phase expectation, an unchanged archive fixture reading Linux `/proc/self/fd`, and the unchanged Vault authentication fixture missing `workspace_registry`. The independent Reviewer reproduced all three exact failures on an isolated detached `origin/main@9a87cee9` checkout after an offline frozen install and dependency build, retained in `candidate-review/baseline-three.log`; this is not a full-suite PASS. Raw logs are under `temp/changes/202609031230162442-r058_worker_mcp/resume-checks/`.
- **Current built checks:** Both the normal `pnpm test:smoke` and the explicitly labelled `OPENKIT_TEST_USE_IMAGE=1 pnpm test:smoke` complete with exit 0 and all four observations passing: NanoCore health, Worker MCP Harness Admission, Worker MCP product, and Web preview. Both include the corrected smoke's shared pass/fail/timeout self-check at script SHA-256 `b97a4c957a5e0293ef030a98dd66de120bafe6533d8f7914dd8153c3dad610a8` before product adjudication. `pnpm check:repo` also passes over 214 documents and 976 files. Current focused evidence does not substitute for the unrun real-Codex L6 story.
- **A2 preservation:** Machine id `34b95b09f8c1475ea242800759411fea` matches the handoff. A stopped-state 6,118,692,176-byte backup preserves App state, NanoHost state, exact App image, configuration, and pointers; its archive hashes, 64 SQLite integrity checks, and `restore.sh --check` pass. The primary independently reran the restore check. Recovery assets remain under `/home/ubuntu/openkit/r058-l6-20260905/backup/` with the restore script at the attempt root. The standalone historical handoff file is absent, but the owned deployment data is preserved.
- **A2 prerequisite:** Fresh independent direction returned `Continue` for the exact host-manifest Docker prerequisite. A bounded three-package upgrade changed Docker CE, CLI, and rootless extras from `29.6.1` to `29.7.2`; six upgrade and rollback package hashes passed, zero unrelated packages were installed or removed, and supporting package versions remained unchanged. The unchanged `openkit-staging` App was deliberately stopped and restarted, returned to healthy at original image `sha256:ba559df858fd42e9dc916768dc05f2b04773eac667c19dc8408a113b5732bb8d`, and public health returned HTTP 200. The primary independently observed the exact Docker version, App image and health, and public health. Existing App and Web pointers remain `c478f4dd984d758772dcd565b963bcf9041b76d1`; the original NanoHost was failed/stopped and the candidate had not yet started at that prerequisite receipt.
- **Prepared L6 scene boundary:** The separate attempt owns a fresh data root, identity, enrollment slots, distinct listeners, and public origin `https://r058-l6.150-136-34-197.sslip.io`. The exact product build passed App and Worker image smoke, release preflight, and artifact hash checks: App image `sha256:678b058126912415dd249b6e372c2af3fc49a6efe507086c0e08cbca61bcf1bf`, NanoHost binary SHA-256 `132dea3ea1b3073dd228be927b17b8130f1377438f3b01765c314010a8212305`, and Codex Worker manifest `sha256:daf33e081875545aefaa8c8a30ecfb3afe8633a1041fede149195857f702b7ea`. Deployment and scene preparation use public configuration, authentication, and enrollment owners; no historical private database or runtime token is rewritten or copied. Deterministic preparation is separate from actor execution.
- **Fresh direction:** After primary compaction, the independent Verifier read Intent Epoch 2, the checkpoint, Git `09d8b77`, the five-doc correction, and named A2 receipts and returned `Continue` for documentation reconciliation and the remaining L6 frontier. The A2 builder remains the sole authorized transition owner. Its raw pi-ai probe established separate clean actor and judge payloads without the coding harness envelope. Actor scaffolding now checks the exact initial persona and user content, bounds navigation to the application origin, closes popups, and keeps public read-model capture outside the actor context.
- **Documentation reconciliation:** Independent review, verification, and audit inspected the final five-file correction: owning specifications now distinguish the implemented selected-MCP slice from deferred capability families and pending L6 closure. Usage wording explicitly includes contacted and unknown effects and excludes only proved pre-contact outcomes, matching `worker-mcp-routes.ts`. The generated documentation index is aligned.
- **Scene reframe:** The fresh-user Agent Catalog check failed because its production manifest projection is absent; `findings.md` records `R058-FND-001`. Independent Reviewer, Verifier, and Auditor traced the actual Web Task selector to the existing current-config conversation-target route, so the unrelated catalog check is removed from setup without expanding R058. A fresh direction check returned `Continue`. Preparation reuses the already-created empty `ws_2`, selects an exact provider model in the pinned catalog, binds the declared remote Git source through the authored manifest, and validates actual conversation targets after the candidate restart. The single A2 transition owner keeps effects paused until that coherent setup audit is complete.
- **Post-failure fresh direction:** The independent Verifier read current Intent, checkpoint, Git, exact producer/consumer bytes, the true RED regression, and the retained journal and returned `Continue`. The correction follows the unique Worker Control owner and its explicit implemented V1 projection; no wire-version migration is authorized by this repair. NanoHost's pre-existing “no body” prose is stale cross-owner duplication and does not override that protocol owner. Timers, transport, image packaging, and the already-correct local guide remain unchanged.
- **Run 1 observation and reframe:** The exact candidate scene reached real Sandbox creation, Git and Context imports, and the Worker Shim entry marker. The actor ended after 209,339 ms and 30 browser calls with no repository or issue result; final public CapabilityCalls and UsageRecords are empty. Evidence is sealed at `temp/r058-l6-run1/sealed/package.json`, SHA-256 `a36112c3194488a9d6dd0949716ac9d055d24b914f5783b016172e45f3069e66`, and awaits two clean per-run judges. Image signature-cache warnings were nonfatal. Direct journal chronology and independent source/history inspection instead establish that NanoHost requires the retired two-member initial poll while current Worker Shim and NanoCore use exact `{ "schemaVersion": 1 }`. The readiness latch never signals, its 120-second deadline starts teardown, and the later missing Exec exit is a consequence. The corrected regression fails on the first valid-poll assertion before production changes; `resume-checks/nanohost-initial-poll-red.log` records exit 101 and one deciding failure. An earlier invocation ran the unchanged test after a working-directory error and is retained separately as `nanohost-initial-poll-unchanged.log`, not RED evidence.
- **Run 1 adjudication:** Three isolated raw judge instances read only the unchanged story and sealed package and independently returned overall `failed`; each recomputed final CapabilityCalls as zero. Judges 1 and 3 cover all nine assertions and agree on the deciding terminal-success and allowed-call failures. Judge 2 omitted assertion 9, and judges 1 and 3 differ between passed and inconclusive for the unobserved payload-redaction assertion 8. Those limitations remain in the retained artifacts and do not become an accepted pass or trigger further judge selection. The Auditor independently verified all 70 sealed package/manifest hashes and accepted retention and cleanup as a failed attempt; it cannot count toward the consecutive-pass requirement.
- **Poll correction acceptance:** The normal Reviewer accepted the exact source and one-line specification reconciliation after independently observing the focused predicate pass with Rust `1.97.1`. The Verifier and Auditor separately accepted deleting the stale NanoHost body rule in favor of the unique Worker Control owner. Corrected full NanoHost tests report 82 passing and four `main.rs` source-string failures; the Reviewer reproduced all four identical `end of NanoHost run path` panics on detached `origin/main@9a87cee9`, retained in `candidate-review/nanohost-four-baseline.log`. This is not a full-suite pass. NanoHost format and Clippy pass, and `pnpm check:repo` passes over 215 documents and 976 files. These checks do not substitute for the next real bridge and MCP observations.
- **Next action:** Commit the independently accepted NanoHost correction, rebuild only its exact binary after proving unchanged App/Web and Worker Git inputs, and retain the packaged artifact hashes. Then clean the sealed failed attempt through the inspected public decommission and allowlisted cleanup path and prepare a fresh scene. Two consecutive passing real packaged-Codex runs remain required before final review, audit, roadmap closure, and merge.
- **Predicted observable:** Two consecutive admitted L6 runs demonstrate selected tool discovery, one directly allowed call, the visible approval Gate before the protected effect, a fresh successor Turn claiming the grant, and redacted public lineage and usage. Cleanup restores the healthy original App and its pointers while preserving the explicitly failed/stopped original NanoHost baseline.
- **Reframe evidence:** Any proof that a required path exists only in documentation or unit mocks, source closeout can be bypassed, credentials or upstream topology reach Worker-visible bytes, or owned descendants survive teardown requires a local correction or Reframe before roadmap closure.

## Acceptance And Closeout

- A normal independent Reviewer must inspect the exact final diff and named execution output before feature acceptance.
- Fresh verification or audit is used at owner reconciliation, major direction changes, and final closure where consequence or uncertainty warrants it; producer reports alone never accept their artifacts.
- Focused checks run during each slice; final scope determines package lint, typecheck, tests, build, packaged smoke, and deterministic story evidence.
- Closeout records commits, exact check results, external effects, unresolved findings, and surviving conclusions promoted to their owning documents; the roadmap changes only after the whole accepted R058 plane passes.
