---
type: change-plan
status: verified
started: 2026-09-03
branch: codex/r058-worker-mcp
---
# R058 Worker MCP

## Intent Epoch 1

- **Source:** Engineer instruction in the current task, `/home/ubuntu/handoff-a2-testenv-20260903.md`, and `docs/roadmap.md` R058.
- **Requested outcome:** Deliver the accepted R058 worker MCP plane so a governed Codex Worker can discover and invoke Workspace-enabled stdio and Streamable HTTP MCP tools through the existing Gateway path.
- **Non-negotiables:** Preserve accepted lifecycle, schema, credential, policy, approval, Item, usage, audit, bounded-failure, and teardown semantics; keep R005 parked and independently mergeable; use no compatibility path; make the smallest coherent change; do not restart, replace, or lifecycle-manage the A1 Codex App Server or any A1 service; use A2 only as the authorized test and staging host and attribute every retained result to machine id `34b95b09f8c1475ea242800759411fea` and the exact product commit.
- **Acceptance observations:** A packaged Codex Worker discovers and calls a credential-free stdio tool through the single Sandbox Integration path; an HTTP tool receives a gateway-only Vault credential without leaking credential or upstream topology; an approval-required call ends before upstream contact and only a fresh call in a new Turn can execute after approval; every product-visible call has coherent Item, schema snapshot, CapabilityCall, usage when upstream was contacted, audit, and typed bounded outcome evidence; teardown leaves no owned child process or live credential material.
- **Exclusions:** Marketplace, registry discovery, OAuth browser flow, HTTP+SSE compatibility, arbitrary API proxying, server sandboxing, Web catalog editing, OpenCode and Pi delivery, multi-worker concurrency, HA, durable process registry, streaming results, automatic schema repin, generic connector or capability frameworks, and A1/A2 lifecycle effects.
- **Effect boundary:** Repository edits, package installation, temporary local child processes, isolated local test data, candidate commit and push, PR submission, and one rollback-protected A2 staging deployment are allowed; tests may terminate only their own children; the completed pin probe used the engineer-authorized Codex subscription in an isolated temporary `CODEX_HOME`, while deciding feature checks use deterministic local inference and MCP stubs; no A1 service lifecycle effect is allowed, and merge remains a separate decision.

## Intent Epoch 2

- **Source:** Engineer steering on 2026-09-05 referencing PR #4 and its handoff.
- **Requested outcome:** Finish and merge PR #4 first; then update only the Codex and PI Agent Workers to current stable releases and Open Knowledge Format to upstream v0.2, and pause subsequent roadmap features.
- **Effect boundary:** The engineer now authorizes merging the completed PR. The existing A2 test/staging authorization and credential handling rules continue; no A1 lifecycle effect is authorized. Preserve staging data with a verified recoverable backup before deployment changes.
- **Acceptance:** Retain the existing R058 acceptance requirements. The subsequent version refresh is a separate change after this PR is merged.

## Intent Epoch 3

- **Source:** Engineer steering on 2026-09-05 asking whether inference failed because of an invalid token, directing use of the server's `~/.codex/auth.json`, naming `gpt-5.6-luna` with `low` reasoning, and explicitly authorizing moving the tests to SSH `a2` using that server's Codex subscription.
- **Requested outcome:** Verify the server credential through a real minimal request, then run the next browser actor and adjudication processes on A2 and configure the tested Worker through the existing real Codex subscription provider path with the exact requested model and reasoning effort.
- **Effect boundary:** The A2 subscription credential may be consumed through the existing credential owner; it stays on A2 and never appears in output, retained evidence, or committed files. Preserve completed failed-run evidence before changing the scene. No A1 effect is authorized.
- **Acceptance and sequencing:** Existing R058 acceptance remains unchanged. Finish and merge PR #4 before updating only Codex, PI Agent, and Open Knowledge Format v0.2; later roadmap features remain paused.

## Intent Epoch 4

- **Source:** Engineer steering on 2026-09-05 explicitly authorizing command-based direct insertion of the existing server token into the OpenKit OpenAI subscription system for this test, without API import or a login step.
- **Requested outcome:** Seed a fresh isolated test subscription slot from the A2-local credential file through the existing credential and encryption implementation before the actor starts, then use the normal product inference relay.
- **Effect boundary:** This is an explicit test-only exception to the prior public-only setup method. It does not authorize a production import API, new authentication transport, direct Worker-to-provider bypass, manual database mutation when the existing store owner suffices, changes to the original staging account, or credential disclosure. Source credentials stay on A2 and remain unchanged; disposable copied secret material belongs to cleanup.

## Intent Epoch 5

- **Source:** Engineer steering on 2026-09-05 requesting `$herdr`, Cursor CLI, and Grok 4.6 to perform Codex and PI Agent version/schema compatibility work concurrently during testing.
- **Requested outcome:** Prepare and verify the Codex and PI stable-version refresh in an isolated branch while R058 acceptance continues. Compatibility means alignment with the new upstream contracts and does not introduce backward compatibility.
- **Sequencing and ownership:** PR #4 still completes and merges first. The explicitly parallel refresh is limited to Codex and PI; Open Knowledge Format v0.2 remains part of the subsequent refresh, and later roadmap features remain paused. The delegated Cursor process owns only `temp/worktrees/codex-pi-stable-refresh` on branch `codex/codex-pi-stable-refresh`, based on exact PR candidate `edc9e31f`; it has no A2, credential, publication, or PR-merge responsibility.

## Intent Epoch 6

- **Source:** Engineer reply on 2026-09-06, "批准授权", to the explicit question naming Run 9 NanoHost credential enrollment, startup of the currently stopped shared `openkit-nanohost` service on SSH A2, temporary service configuration/identity switching, and post-run credential cleanup and restoration.
- **Effect boundary:** The engineer provides the informed additional approval required by automatic review for those exact prepared Run 9 lifecycle effects. The existing A2-only, unchanged source-auth, preserved staging, and once-only cleanup boundaries remain. No A1 effect, additional service topology, or approval bypass is authorized.
- **Acceptance and sequencing:** R058 product acceptance and PR-first sequencing remain unchanged.

## Intent Epoch 7

- **Source:** Engineer steering on 2026-09-06: do not restore the old A2 state; operate A2 as a continuously used system; retain normal `~/.openkit` data, users, configuration, credentials, and injected Codex subscriptions for later feature and version staging.
- **Requested outcome and effect boundary:** Keep normal App and NanoHost service state and credentials after verification. Stop only disposable test processes when finished. Do not restore old routing, revoke ordinary credentials, decommission the configured NanoHost, or delete product data as per-run cleanup. Preserve both the original data and the currently exercised candidate data until their normal-use disposition is established without loss. This supersedes the restoration and disposable-product-state directions of earlier epochs; it does not waive product acceptance or confidentiality.

## Intent Epoch 8

- **Source:** Engineer steering on 2026-09-06 to finish the current two major tasks, then close or merge all PRs, retain every change in the main branch, and remove local, remote-server, and GitHub branches and worktrees.
- **Requested outcome and sequence:** Finish PR #4 and the Codex/PI plus OKF v0.2 updates first. Then reconcile all OpenKit changes into `main`, verify their integration, resolve every repository PR, and remove the remaining non-main branch and worktree references locally, on A2, and on GitHub only after their changes are preserved. A2 normal-use data and credentials are not Git cleanup targets. Later roadmap development remains paused.

## Owning Documents

- `docs/core/agent-capability.md` owns capability terminal semantics, Item projection, Gateway mediation, and credential boundary.
- `docs/core/vault.md` owns injection plans, uses, receipts, uncertainty, revocation, and teardown.
- `docs/specs/20260704-worker_mcp_tool_supply.md` owns the R058 MCP catalog, transports, tools, schema snapshots, policy, approval, usage, failures, and acceptance.
- `docs/specs/20260703-worker_agent_capability.md` owns the worker-local capability route and independent credential boundary.
- `docs/specs/20260704-capability_usage_gateway_foundation.md` owns shared CapabilityCall, UsageRecord, idempotency, terminal, and recovery behavior.
- `docs/specs/20260531-human_attention_intervention_model.md` owns approval gates and forbids implicit current-call or AgentSession resumption.
- `docs/specs/20260629-worker_runtime_communication_model.md` owns the shared Worker Shim contract and single Sandbox Integration path.
- `docs/specs/20260716-codex_worker_adapter.md` owns the pinned Codex adapter and inline native configuration projection.
- `docs/specs/20260802-nanohost_runtime_and_transport.md` owns NanoHost carriage, stream, and byte bounds.
- `docs/specs/20260529-test_strategy.md` and `docs/verification-instruments.md` own test level and deciding-evidence quality.

## Accepted Direction

- The independent Consultant accepted ignored proposal SHA-256 `b2bc88232b392501f660b65656d9d72a47d1a0bc80bcb574ddcb4e42d7c18504` after two correction rounds.
- R058 starts from `origin/main`; the parked R005 ref remains untouched, while R058 ports and independently accepts only the minimum shared CapabilityCall terminal vocabulary, atomic terminal AuditEvent, and restart-`unknown` seam required by accepted Core semantics.
- The Workspace MCP catalog mirrors the existing deployment-admin runtime-config pattern and is the sole owner of stdio commands, HTTP endpoints, finite transport-specific credential bindings, tool rules, approval marks, timeouts, and schema policy.
- AEP freezes only worker-safe catalog digest, server id, tool rules, approval marks, schema policy, and an optional existing pinned snapshot id; it never projects command, endpoint, credential reference, or live schema guesses.
- NanoCore uses the official TypeScript MCP SDK directly, while Worker Shim transparently relays its bytes through the existing Sandbox Integration stream and fixed loopback listener; the process-local NanoCore supervisor has no durable registry; Codex receives only a fixed loopback MCP URL through inline `-c` arguments.
- The third Turn token has its own raw-token injection, durable hash and active-lease lineage binding, restart verification, terminal cleanup, and cross-family rejection.
- MCP policy reuses `tool.use` with server and tool resource context; approval ends the current call as typed denied before upstream contact, expires after one hour, then a later new-Turn call must reauthorize and may durably claim exactly one granted, unexpired, unconsumed proposed-effect tuple through a CapabilityCall id derived from the Approval id without receiving that id or resuming the prior AgentSession; its fresh allow decision links the granted terminal decision and current call through context without reusing the Approval id.
- A deterministic Item id binds each product-visible call; CapabilityCall and Audit terminalize before terminal Item publication, and startup reconciliation fills only a missing Item from the durable terminal winner.
- Upstream contact creates exactly one `tool_calls=1` UsageRecord even when the later outcome fails, interrupts, or becomes unknown; pre-effect invalid or denied calls create no usage.
- Vault handling follows Plan, audited Use, and proved-sink Receipt order; only a possibly transmitted tool request with unprovable outcome becomes `unknown`; exact resolved credential bytes cause whole-result rejection if echoed.
- Schema snapshots reuse the existing table with canonical tool and object-key ordering, deterministic digests, a fixed tracking retention bound, explicit current selection, pinned comparison, and snapshot id attribution on CapabilityCall.

## Current Evidence

- Git branch `codex/r058-worker-mcp` was created from `origin/main@9a87cee9028fbe4dd985c24b384ca4b81ddd25d0`; parked R005 remains at `origin/codex/phase1-host-recovery-campaign@b0871db3df2518283d57d1fda5335968ecddad2f`.
- The pre-branch fresh-context Verifier returned `Continue`; the Consultant then returned `DISAGREE`, direct artifact inspection confirmed its owner conflicts, and the corrected proposal received Consultant `AGREE` at the exact hash above.
- The post-Reframe Verifier accepted the separate-main branch and narrow R005 seam, and required branch creation, bounded pin probe, this plan, then production changes in that order.
- The bounded probe installed stock Codex CLI `0.144.1` under ignored `temp/r058-pin-probe`, ran it with `--ignore-user-config --strict-config`, and projected `mcp_servers.probe.url` through inline `-c`.
- The probe completed Streamable HTTP initialize, tool discovery, and exactly one `echo({text:"pin-ok"})` call, returned `echo:pin-ok`, and left the call observation at `temp/r058-pin-probe/run1/calls.jsonl`; its owned MCP child was terminated by exact PID after completion.
- Current `origin/main` still has the documented CapabilityCall terminal/recovery divergence; the candidate seam exists in R005 commit `582ac99` but that commit also contains unrelated Phase 1 paths and will not be cherry-picked wholesale.
- The first independent review blocked unsound message-based timeout inference and provider-originated `AbortError` cancellation inference; the corrected classifier now requires typed timeout evidence or the current caller signal's exact reason, with attributed regressions for both negative cases and genuine caller cancellation.
- The post-compaction Verifier confirmed that correction and then found the committed App API OpenAPI projection stale; the candidate now preserves the canonical lifecycle metadata through the capability usage read model, tests its representable constraint with AJV, and regenerates the tracked artifact.
- The second independent review accepted both corrections but found three stale owning regressions; Worker inference now asserts CapabilityCall `aborted` while retaining AuditEvent `cancelled`, and boot recovery asserts `unknown`, with both complete affected test files green.
- The post-compaction Verifier rejected premature capability enablement and arbitrary inline stdio environment values; the corrected catalog uses only Vault-binding slot templates, scheduler dispatch carries the actual dequeued Workspace catalog to worker startup, and capability mode remains disabled until the executable plane exists.
- The correction review found two remaining premature contracts; the prerequisite now keeps the public AEP capability schema disabled-only and permits schema snapshot recording only from an explicit live source.
- The normal Reviewer then found that loop-global catalog inputs could attach the initiating request's Workspace catalog to an older dequeued Turn and left periodic retry without any MCP catalog. The corrected scheduler selects both catalogs by the dequeued entry's Workspace, uses its durable roots and working directory, derives source references from its selected manifest, and removes the dead pre-admission catalog-context helper.
- The fresh post-Reframe Verifier accepted that selection but found the periodic service still closed over the process-start snapshot, contradicting session-scoped safe reload. The service now reads one complete current snapshot per retry iteration, without catalog-specific state or queue persistence, and its regression proves a catalogless first attempt succeeds after the snapshot gains the required Workspace catalog.
- The renewed Verifier returned `Continue`, and the final normal Reviewer returned `ACCEPT` after independently rerunning the scheduler, Config Schema, Worker Shim, and diff checks and reproducing the single unrelated NanoCore baseline failure.
- The executable-plane candidate now includes the SDK-backed stdio and Streamable HTTP gateway, catalog and schema snapshot handling, Vault injection and teardown, governed calls, usage and Item projections, one-shot approval claims, and startup Item reconciliation; focused NanoCore typecheck and 24 route, ledger, approval, concurrency, expiry, and recovery tests pass.
- The latest independent Consultant reviewed proposal SHA-256 `c942773ba677a696cb32b430b2a1c06965fcc74f381977bb883f83e69f74478e` and returned `DISAGREE`: the candidate does not yet connect an approval-required MCP call to the production Worker Gate stop and mode checkpoint lifecycle.
- A fresh Verifier returned `Reframe`: Core Protocol and Worker Reliability already resolve human denial to Product Turn `interrupted` and checkpoint `aborted`, while Task `cancelled` is only a mode projection and Goal Mode disclaims Gate mechanics; current `cancelled` implementation bytes are an authority divergence, not contrary authority. The proposal is corrected at SHA-256 `0340b3d373c371c036a72f6cee75b70c854e6486ffa9b2c62dc1ffe8acf41a2d` and awaits renewed independent agreement.
- After two further Consultant correction rounds, proposal SHA-256 `96aede4a718a018a0e03e4899bb9a1c47eec4f58b805ab70715d0891b15b20a5` received `AGREE`: the accepted scope is only the delivered Codex session-continuity path, `turn.interrupt` purpose is exactly `interrupt | human-gate`, its stop admission CAS covers only the existing Core DB projections, and source AgentSession closure is explicit. The corrected independent Auditor returned `ACCEPT`, and the renewed independent Verifier returned `Continue` after confirming the narrow purpose schema, truthful transaction boundary, asynchronous settlement, and Core `interrupted` mapping.
- The completed Gate slice now admits an actionable `tool.use` decision only for one exact Worker lease, preserves partial cross-store state as `recovery_required`, accepts `blocked/ask_user` only after exact Gate and cleanup evidence, closes a granted source Turn and AgentSession, closes a denied source Turn and AgentSession as interrupted, and lets only a fresh successor Turn claim the exact granted effect once.
- Product and verification bytes are committed through exact candidate `16bc6b5427113ca067e766a3db2475467af485de`; exact story SHA-256 `83f6941d6df5f8e9bc1b7aca0814301336a5e7c955de1cccc3a78fe4772e75cc` received independent L6 admission acceptance, while final feature acceptance remains open.
- The admitted story's sole actor ask is "Find and report the repository details and current issue list for this workspace." A competent actor needs only that ask and the declared persona: the two requested results make the available repository-detail and issue-list capabilities relevant without naming MCP, Gate, call order, or retry behavior; task completion, the visible approval, redaction, and the named public AEP, call, approval, teardown, usage, audit, and schema records are end-state or invariant observations produced by completing that intent rather than a prescribed trajectory, so every required assertion is satisfiable from the persona and sole ask.
- Exact-candidate focused evidence is green without an A1 lifecycle effect: 383/383 selected NanoCore action, approval, recovery, gateway, route, simulator, server, image-contract, migration, and OpenAPI tests pass; the gateway file separately passed 28/28 on three consecutive cold invocations; 185/185 Worker Shim tests, 159/159 Config Schema tests, and 169/169 Protocol tests pass; NanoCore, Worker Shim, Config Schema, Protocol, and NanoHost builds pass.
- The full NanoCore suite has 2,553 passing tests and five failures independently reproduced as the same 5/69 subset on `origin/main@9a87cee`: one stale startup-phase expectation, three pre-existing unsupported non-file filesystem fixtures, and one Vault fixture without `workspace_registry`; no R058 test fails.
- Repository governance and formatting pass over 214 documents and 976 files, NanoCore lint, typecheck, and build pass, NanoHost formatting and Clippy pass, and `git diff --check` passes.
- The built NanoCore L5 smoke and its exact shared adjudicator at script SHA-256 `6072addacb4dfac2c2967fd6ea6f71fd2e4c00c73c695589d1057fc98f09f969` are admitted on 2026-09-04. `node tests/smoke/nanocore-worker-mcp-smoke.mjs --self-check` is the check; its stable complete observation is the pass target, `cleanup.listenerClosed=false` is the deliberate deciding-observation intervention, and a never-settling observation provider is the 25 ms timeout intervention; all three pass through `runHarnessAdmissionSelfCheck` → `settleSmokeAdjudication` → `expectedSmokeObservation`, the same `main` → `settleSmokeAdjudication` → `expectedSmokeObservation` path used by the product run; observed outcomes were respectively `pass`, `fail`, and `timeout`, the stand-in marker remained present until adjudication completed, and the following full root smoke reported Harness Admission, Worker MCP product, NanoCore health, and Web preview PASS.
- The admitted L5 adjudicator oracle is `Prior`: its expected object and accepted owners predate the adjudicator and the falsifier confirmed the expectation would not change with different product bytes; `Bounded`: the falsifier confirmed it compares one finite exact observation object and can answer all fields; `Reproducible`: independent producer and reviewer runs returned the same three self-check outcomes and product verdict from the same observations; and `Re-runnable`: measured self-check execution is sub-second and the full product smoke completed in 15.3 seconds, so both can run after every adjudicator change.
- A2 machine id `34b95b09f8c1475ea242800759411fea` still runs the healthy staging App at `c478f4dd984d758772dcd565b963bcf9041b76d1`, but its current NanoHost baseline is fail-stopped at runtime target `97:1:0:0`; two explicit A2-only start attempts on 2026-09-04 completed the private runtime bootstrap but did not publish readiness. No candidate deployment or L6 run has begun, and the App, Caddy, data, and deployment pointers remain unchanged.
- The full NanoHost package run has 82 passing tests plus five unrelated baseline/environment failures: four stale source-string assertions expect an absent `Starts the fixed NanoHost` marker in unmodified `main.rs`, and one recovery test reads the live host's root-only `/var/lib/openkit/nanohost`; the exact changed capability carriage and outer-projection tests pass independently.

## Current Checkpoint

- **Current intent:** Epochs 3–6 authorize A2, its unchanged source Codex subscription, Luna-low where supported, command-based credential configuration without login, parallel Cursor/Grok Codex/PI preparation, and the informed NanoHost service transition. Epoch 7 makes A2 persistent normal-use staging and retains data, users, configuration, credentials, and services. PR #4 merges first; Codex/PI and OKF v0.2 updates follow; Epoch 8 then requires integration of all OpenKit changes into main and cleanup of repository PRs, branches, and worktrees. Later roadmap features remain paused.
- **Current Git:** The accepted private-input correction is production commit `5162a0951b8eb737f068e4b8e6868c7a91da525f`, pushed to PR #4. It supersedes the incomplete package-only fix. Subsequent changes are limited to the existing route regression and this execution checkpoint; deciding App and Worker artifacts retain exact production commit `5162a095`, with no production-source change. The shared root contains unrelated history and is not a push source.
- **Correction and direct evidence:** Independent review reproduced two blockers: Linux-only `setsid`/`setpriv` broke the supported macOS source path, and a query credential echoed in URLSearchParams wire encoding bypassed exact raw-value filtering. The corrected Gateway uses native detached Node process groups and a private IPC parent-death channel, reuses the official SDK's role-neutral stream transport, preserves bounded group-absence proof, and rejects raw and transmitted query credential forms. The App image no longer installs or asserts obsolete Linux helper commands. Descendant-written credential digest receipts preserve the inherited-secret oracle on supported POSIX hosts.
- **Independent design scrutiny:** The portable-supervision Consultant accepted the smaller native IPC design after actual SDK connect/call/error/overflow/cleanup probes and 20/20 parent-death repetitions. The normal Reviewer independently accepted the query-credential fix and smoke cleanup guard and observed the portable stdio session plus the three credential-bearing cleanup tests pass on macOS; the final independent Reviewer accepted the exact corrective diff with no actionable finding.
- **Verification correction:** The original smoke cleanup awaited an already-emitted child exit and masked the primary failure with exit 13; a terminal-child guard now preserves the original error. The idle-use test mixed Undici dispatch with a simulated clock and failed in the full file despite an isolated pass. Its corrected lowest-sufficient oracle holds one SDK tool-call promise unresolved, explicitly checks that it is still unresolved at 60 seconds, then resolves it and observes eventual idle cleanup. The real HTTP transport, concurrency, credential, timeout, and byte-bound tests remain separate deciding checks. Diagnostic markers and unsuccessful polling experiments are absent from the final bytes.
- **Current local checks:** On pinned macOS Node `24.18.0`, Gateway tests pass 29/29 and NanoCore typecheck and lint pass. The complete NanoCore suite reports 2,557 passing tests and three failures: the unchanged startup-phase expectation, an unchanged archive fixture reading Linux `/proc/self/fd`, and the unchanged Vault authentication fixture missing `workspace_registry`. The independent Reviewer reproduced all three exact failures on an isolated detached `origin/main@9a87cee9` checkout after an offline frozen install and dependency build, retained in `candidate-review/baseline-three.log`; this is not a full-suite PASS. Raw logs are under `temp/changes/202609031230162442-r058_worker_mcp/resume-checks/`.
- **Current built checks:** Both the normal `pnpm test:smoke` and the explicitly labelled `OPENKIT_TEST_USE_IMAGE=1 pnpm test:smoke` complete with exit 0 and all four observations passing: NanoCore health, Worker MCP Harness Admission, Worker MCP product, and Web preview. Both include the corrected smoke's shared pass/fail/timeout self-check at script SHA-256 `b97a4c957a5e0293ef030a98dd66de120bafe6533d8f7914dd8153c3dad610a8` before product adjudication. `pnpm check:repo` also passes over 214 documents and 976 files. Current focused evidence does not substitute for the unrun real-Codex L6 story.
- **A2 preservation:** Machine id `34b95b09f8c1475ea242800759411fea` matches the handoff. A stopped-state 6,118,692,176-byte backup preserves App state, NanoHost state, exact App image, configuration, and pointers; its archive hashes, 64 SQLite integrity checks, and `restore.sh --check` pass. The primary independently reran the restore check. Recovery assets remain under `/home/ubuntu/openkit/r058-l6-20260905/backup/` with the restore script at the attempt root. The standalone historical handoff file is absent, but the owned deployment data is preserved.
- **A2 prerequisite:** Fresh independent direction returned `Continue` for the exact host-manifest Docker prerequisite. A bounded three-package upgrade changed Docker CE, CLI, and rootless extras from `29.6.1` to `29.7.2`; six upgrade and rollback package hashes passed, zero unrelated packages were installed or removed, and supporting package versions remained unchanged. The unchanged `openkit-staging` App was deliberately stopped and restarted, returned to healthy at original image `sha256:ba559df858fd42e9dc916768dc05f2b04773eac667c19dc8408a113b5732bb8d`, and public health returned HTTP 200. The primary independently observed the exact Docker version, App image and health, and public health. Existing App and Web pointers remain `c478f4dd984d758772dcd565b963bcf9041b76d1`; the original NanoHost was failed/stopped and the candidate had not yet started at that prerequisite receipt.
- **Prepared L6 scene boundary:** The separate attempt owns a fresh data root, identity, enrollment slots, distinct listeners, and public origin `https://r058-l6.150-136-34-197.sslip.io`. The exact product build passed App and Worker image smoke, release preflight, and artifact hash checks: App image `sha256:678b058126912415dd249b6e372c2af3fc49a6efe507086c0e08cbca61bcf1bf`, NanoHost binary SHA-256 `132dea3ea1b3073dd228be927b17b8130f1377438f3b01765c314010a8212305`, and Codex Worker manifest `sha256:daf33e081875545aefaa8c8a30ecfb3afe8633a1041fede149195857f702b7ea`. Deployment and scene preparation use public configuration, authentication, and enrollment owners; no historical private database or runtime token is rewritten or copied. Deterministic preparation is separate from actor execution.
- **Fresh direction:** After primary compaction, the independent Verifier read Intent Epoch 2, the checkpoint, Git `09d8b77`, the five-doc correction, and named A2 receipts and returned `Continue` for documentation reconciliation and the remaining L6 frontier. The A2 builder remains the sole authorized transition owner. Its raw pi-ai probe established separate clean actor and judge payloads without the coding harness envelope. Actor scaffolding now checks the exact initial persona and user content, bounds navigation to the application origin, closes popups, and keeps public read-model capture outside the actor context.
- **Documentation reconciliation:** Independent review, verification, and audit inspected the final five-file correction: owning specifications now distinguish the implemented selected-MCP slice from deferred capability families and pending L6 closure. Usage wording explicitly includes contacted and unknown effects and excludes only proved pre-contact outcomes, matching `worker-mcp-routes.ts`. The generated documentation index is aligned.
- **Scene reframe:** The fresh-user Agent Catalog check failed because its production manifest projection is absent; `findings.md` records `R058-FND-001`. Independent Reviewer, Verifier, and Auditor traced the actual Web Task selector to the existing current-config conversation-target route, so the unrelated catalog check is removed from setup without expanding R058. A fresh direction check returned `Continue`. Preparation reuses the already-created empty `ws_2`, selects an exact provider model in the pinned catalog, binds the declared remote Git source through the authored manifest, and validates actual conversation targets after the candidate restart. The single A2 transition owner keeps effects paused until that coherent setup audit is complete.
- **Post-failure fresh direction:** The independent Verifier read current Intent, checkpoint, Git, exact producer/consumer bytes, the true RED regression, and the retained journal and returned `Continue`. The correction follows the unique Worker Control owner and its explicit implemented V1 projection; no wire-version migration is authorized by this repair. NanoHost's pre-existing “no body” prose is stale cross-owner duplication and does not override that protocol owner. Timers, transport, image packaging, and the already-correct local guide remain unchanged.
- **Run 1 observation and reframe:** The exact candidate scene reached real Sandbox creation, Git and Context imports, and the Worker Shim entry marker. The actor ended after 209,339 ms and 30 browser calls with no repository or issue result; final public CapabilityCalls and UsageRecords are empty. Evidence is sealed at `temp/r058-l6-run1/sealed/package.json`, SHA-256 `a36112c3194488a9d6dd0949716ac9d055d24b914f5783b016172e45f3069e66`, and awaits two clean per-run judges. Image signature-cache warnings were nonfatal. Direct journal chronology and independent source/history inspection instead establish that NanoHost requires the retired two-member initial poll while current Worker Shim and NanoCore use exact `{ "schemaVersion": 1 }`. The readiness latch never signals, its 120-second deadline starts teardown, and the later missing Exec exit is a consequence. The corrected regression fails on the first valid-poll assertion before production changes; `resume-checks/nanohost-initial-poll-red.log` records exit 101 and one deciding failure. An earlier invocation ran the unchanged test after a working-directory error and is retained separately as `nanohost-initial-poll-unchanged.log`, not RED evidence.
- **Run 1 adjudication:** Three isolated raw judge instances read only the unchanged story and sealed package and independently returned overall `failed`; each recomputed final CapabilityCalls as zero. Judges 1 and 3 cover all nine assertions and agree on the deciding terminal-success and allowed-call failures. Judge 2 omitted assertion 9, and judges 1 and 3 differ between passed and inconclusive for the unobserved payload-redaction assertion 8. Those limitations remain in the retained artifacts and do not become an accepted pass or trigger further judge selection. The Auditor independently verified all 70 sealed package/manifest hashes and accepted retention and cleanup as a failed attempt; it cannot count toward the consecutive-pass requirement.
- **Poll correction acceptance:** The normal Reviewer accepted the exact source and one-line specification reconciliation after independently observing the focused predicate pass with Rust `1.97.1`. The Verifier and Auditor separately accepted deleting the stale NanoHost body rule in favor of the unique Worker Control owner. Corrected full NanoHost tests report 82 passing and four `main.rs` source-string failures; the Reviewer reproduced all four identical `end of NanoHost run path` panics on detached `origin/main@9a87cee9`, retained in `candidate-review/nanohost-four-baseline.log`. This is not a full-suite pass. NanoHost format and Clippy pass, and `pnpm check:repo` passes over 215 documents and 976 files. These checks do not substitute for the next real bridge and MCP observations.
- **Exact rebuilt artifact:** A2 built and packaged the clean corrected candidate `edc9e31f1366897f69152328e26aaf0fe24bf737`, tree `5fc6e172864de9a3b178721fc243d9e06788d549`, after exact App/Web and Worker Git object equality checks. The focused poll regression passes on Linux; release build, preflight, checksums, package verification, and contained install pass. Built, packaged, and staged NanoHost SHA-256 is `d686916995aac114fb767e15e809a67172613149a45003ff1c362103189189a1`; archive SHA-256 is `d7357c105a4d01678d0aa3a8593a3f7f9a9fbbe95d2134fd3d1e8aa23bb86f6c`. The primary inspected the actual remote build output and local receipt `a2-prepare/nanohost-only-build-receipt.md`, SHA-256 `7f6344880f9ef41a526bb50a25c8e5cc825c49028bfcfc0edce730f90aa08da6`.
- **Run 1 cleanup:** Public decommission returned HTTP 200 and revoked one token; owned credential slots, container, stub, DATA_ROOT, credentials, and public route were removed, while the original App, pointers, Caddy bytes, rollback files, Worker object, and full backup were preserved. The script exited 1 because it expected literal `inactive` after stopping a historically failed service; direct observation proved `MainPID=0`, failed/failed service state, inactive/dead slice, and listener absence. The stopped postcondition was corrected without replaying any effect or hiding the failure. The cleanup receipt at `a2-prepare/run1-cleanup-receipt.md`, SHA-256 `33bf3fb5185f7a41393a52691e836ec45cb5d00e1213ae87bac715ecddc2721e`, preserves this limitation. The primary deleted its local disposable browser login. The Auditor and named accumulating Verifier independently accepted cleanup completeness; passing count remains 0/2.
- **Run 2 termination:** The isolated edc candidate passed initial readiness and MCP discovery, then the first inference call failed with `worker_inference_failed`. The browser actor ended at its 600,002 ms bound without a final answer; final observation took 2,458 ms, with 57 browser calls, two tool errors, one recovery, and no guidance. Final public records contain three CapabilityCalls (`mcp.list_tools` succeeded, `responses` failed, and `worker.checkpoint.terminal` failed), one inference UsageRecord, and zero `mcp.call_tool` records. This is not successful MCP tool use. The sealed package is `temp/r058-l6-run2/sealed/package.json`, SHA-256 `83c09426fafbfee10c5b655b5e0ea964269accda055b26ece72530362ad1dff0`; its manifest contains 131 files. The independent Auditor verified every hash and recomputed zero MCP tool calls.
- **Run 2 cause boundary:** Independent source inspection proves the deterministic fixture lacks Codex 0.144.1 tool-search sequencing, but retained terminal diagnostics do not prove its actual HTTP 400 response. The live cause remains unconfirmed, so no `environment_failure` or accepted product verdict is assigned. Two clean raw Luna-low judges returned FAIL prose but did not execute their available recomputation tool; both processes failed `verifiedCounts > 0`. Their complete outputs remain retained as incomplete adjudication, including one unsupported schema assertion against `mcp.list_tools`; no judge-shopping or synthesized pass follows.
- **Server subscription probe:** The A2 owner read `/home/ubuntu/.codex/auth.json` only in memory and completed one real Codex subscription request with HTTP 200, exact model `gpt-5.6-luna`, reasoning `low`, 2,219 ms latency, and the expected minimal answer. The source credential file was unchanged. The primary independently read the redacted result at `/home/ubuntu/openkit/r058-l6-20260905/codex-auth-probe-result.json`, SHA-256 `b633cc1d2128e2658efb9a31c52de696725ede2120106ce3426434aee2cf2376`. This proves credential and model access, not readiness of the product's NanoCore subscription route.
- **PR lineage:** Concurrent local work advanced the shared checkout to `2b09b2d1` with unrelated OpenShell runtime changes, while PR #4 remains at `edc9e31f`. The primary moved this plan's pending edits into `temp/worktrees/r058-pr4-closeout`, based on exact PR head edc, and restored the shared checkout's plan to its committed bytes. Unrelated commits will not be pushed into this PR. Any eventual doc-only closeout must prove executable/configuration/story input equality to the tested edc product.
- **Run 2 cleanup:** Inspected cleanup executed once with exit 0; receipt `a2-prepare/run2-cleanup-receipt.md`, SHA-256 `549f8c174df77470807d1ec2d947f9eda8f63631a41ed10f50a2e7d1b9e76403`, records public decommission and removal of the disposable root, container, route, credentials, and guarded incidental root mise trees. The primary independently verified original staging health and image, restored Caddy bytes, stopped NanoHost PID, and all four backup hashes, then removed the local browser credential. Full sealed evidence and incomplete judge outputs remain retained.
- **Run 3 preparation:** The fresh A2 actor uses Luna-low through the installed host SDK; accepted actor SHA-256 is `cadd23c0d191e99284abadf579c4562613d9a54fb06829f23bd7572f5e9cb80e`. The final scene source SHA-256 is `81ee0d31758c2be1af5a6abf4a885c3ecce33dc8e3f02ecd365d62a2218cf78b`; materialized scene SHA-256 is `f377abcc64ea5e1419d559cc5cae734b8baea96ef38fb4285dd7686bcfcb76dc`. It selects the real subscription provider and verifies its state with the existing deployment-admin token. The deterministic Responses draft was deleted. Test-only credential seeding reuses the bundled secure reader, account manager, and encrypted Vault in a fresh DATA_ROOT, without login or an import API. The native Worker remains at its model-default medium effort because the current accepted Worker route has no effort field; this limitation was reported to the engineer.
- **A2 prerequisites:** Playwright 1.61.1 and Chromium passed an actual title probe after 37 required host-library packages were added with zero upgrades, removals, or service restarts. The exact package receipt is retained for bounded final cleanup. The reviewed one-off seed ran once in the exact App image with network disabled, returned `logged_in`, and left the source auth unchanged. The primary independently read and hash-checked the sanitized remote seed and verification receipts; App deployment and Task execution were still absent at that boundary.
- **Parallel refresh:** Cursor CLI with Grok 4.6 completed an uncommitted Codex 0.153.4 and PI 0.85.0 pin/schema/consumer update in its isolated worktree. Focused checks and both changed-leaf local arm64 image smokes pass. Independent review requires correction of stale historical evidence wording, individual snapshot checksums and consumed-surface dispositions, and completion of the owning repin gates before integration. Nothing from that worktree is part of PR #4 yet.
- **Run 3 readiness:** The primary independently verified exact remote actor, scene, invocation, and readiness receipt hashes; candidate and original App image identity and health; public HTTP 200; and actual setup, self-check, and initial empty-record outputs. `a2-prepare/run3-ready/environment-ready.txt`, SHA-256 `00a5946089c61b34a28b85e3940a166f7c1d67214e1482ce058f4272218e05fc`, records generation 1 ready, predecessor fenced, and fresh-empty NanoHost. The root-owned actor is now running as ubuntu on the frozen A2 candidate, with output retained under the disposable `run3-actor/evidence` directory and no source-auth copy.
- **Run 3 termination:** The actor exited 0 in 71,803 ms with 16 browser calls and no tool errors, recoveries, or guidance. It submitted the sole ask from the Chat route using the default Assistant/reasoning target, then observed the send-failure banner. No Turn was admitted; all deciding public lifecycle arrays remain empty, and the exact HTTP rejection is unavailable. The sealed package SHA-256 is `a590d0716f4da3c38e19ecdbee92911b11b5cedbec7bdadcce22df2597eb7f16`, with 87 retained files. Judge 1 timed out with no successful recomputation; Judge 2 completed four recomputations and returned FAIL prose. Both complete outputs were retained and independently matched A2 hashes. Adjudication is incomplete, not an accepted two-judge product verdict. The primary corrected its earlier inference that no send was attempted after reading the complete transcript.
- **Run 4 direction:** An independent Verifier returned Reframe after inspecting the actual send attempt and public Task owner. The next fresh run starts at the existing `/tasks/{workspaceId}/{threadId}` entrypoint with the same persona and sole ask. A read-only scene preflight must first prove the expected Worker and Luna are selected and available; it neither sends a request nor scripts a dropdown choice. Task mode's existing default selection, rather than Chat's Assistant default, supplies the correct route. No story or product bytes change.
- **Run 3 cleanup and Run 4 readiness:** Run 3 cleanup completed with exit 0 and full private log retention; the primary independently verified original staging health, Caddy bytes, stopped NanoHost, removed disposable paths, and all four backup hashes. The sole accumulator accepted cleanup with passing count 0/2. Run 4 is deployed with unchanged edc App, Worker, NanoHost, and story bytes. Its read-only Task preflight visibly selected the expected warm Codex Worker and Luna; initial public records are empty. The primary verified remote actor, scene, preflight, invocation, and image identities; actor execution has not started.
- **CI process-reaping correction:** The separately pushed Worker refresh candidate `14d228f080e662bb055c0c006fd8b19746ab847d` passed all eight catalog Worker/platform image build and smoke jobs in CI run `33957292369`. The additional L5 check failed because its MCP PID remained addressable; two Worker Shim process-group tests also failed. Independent reproduction of the unchanged supervisor under the CI tail-PID-1 container produced an unreaped zombie, while Docker `--init` produced PID absence. The correction adds `--init` to all nine existing test-image jobs, matching the local container execution path, and leaves product code and PID absence assertions unchanged. The configuration regression failed for missing init and passed after correction; focused workflow/toolchain checks pass 21/21. Repository checks pass. The required local release suite retains six BSD-tar failures for unsupported `--mtime=@0`; Linux CI decides that environment-dependent result. The closeout worktree is now `/Users/m5pro/Documents/AI/openkit-r058-pr4-closeout`, outside the ignored temp directory.
- **Hosted init result:** The three-file independently reviewed correction was committed and pushed as `d0d160afd10e64207f29248de07af935d2176f11`. CI run `33958187582` used Docker `--init` and passed unchanged L5 health, Harness Admission, public Task Worker MCP, Web, L3, portability, and app-image checks. Its L5 log SHA-256 is `e820baf389b36c9ef97a6f0b92c5877018fc9737f0b467e7992439a406f304d2`. Both previous Worker Shim process-group failures pass. L0-L2 then exposed a Core Client fixture missing the PR-added default `schemaSnapshotId: null`; that single expected field was restored, and all 76 Core Client tests pass. The upgrade branch's identical protocol source hit the 5-second timeout while repeatedly compiling the same large generated event schema; the corresponding test passed in this PR run at 4,959 ms. Reusing one compiled validator keeps all positive and negative assertions and removes repeated setup cost; all 169 protocol tests and both package typechecks pass. The independent Reviewer accepted both exact corrections, committed as `cc542278` and cherry-picked into the isolated Worker refresh. Hosted run `33958826082` passes the corrected JavaScript surface and reaches the four previously reproduced NanoHost source-string baseline failures: 83 Rust tests pass and four fail. This remains a failed full gate, with the same four failures independently retained on main; no contract check was weakened.
- **Run 4 termination:** After the hosted L5 pass and remote identity checks, the primary launched the frozen actor with no guidance. It exited 0 after 89,379 ms, 15 provider calls, 14 browser calls, one tool timeout and one recovery. Scanned terminal evidence contains one Turn, one AEP, 12 CapabilityCalls, 37 UsageRecords, four permission decisions, and five runtime records. The actor reported repository details unavailable and issues awaiting approval; complete evidence, adjudication, and causal interpretation are still pending.
- **Run 4 findings:** Public records prove a successful `repos.get`, denied `issues.list`, durable approval grant, and source Turn/session closeout, but no approved successor call. Exact source diagnosis found that checkpoint cleanup accepted only `goal.step` or `task.start` despite closeout already accepting the exact `conversation.submit` owner. It therefore returned HTTP 409 after persisting the grant. The Web retained active controls for the resolved approval. The correction reuses the existing exact conversation-owner helper and marks only matching resolved approval history read-only; it adds no automatic resumption or successor command. The independent Reviewer accepted the final six-file correction, including one shared approval-owner predicate and both local guides. Root inspected the actual diff and named outputs: backend 8/8, Web 74/74, both package lint/typecheck, and repository validation over 215 documents and 976 files pass. The retained review is `candidate-review/run4-approval-fix-review.md`. Sidebar navigation and Task message presentation remain outside this correction.
- **Run 4 adjudication:** The final 86-file manifest is `fc3c8c786518e4782d95a8dc8ee4ed4eb6d6fa97c8aabd218f0c80a1a8c16f3e`, package `3cdd0f0dcf5e618a20db175fcfb3cc48f83c81fa8323183675bbddc03c087863`, and judge input `2ae534130b9472aa2b9d4ee0fbd3381c85920a8c1eaf99fafcf15bce405a6af6`. The Auditor verified all hashes and lossless reconstruction of all 30 captures before two fixed judges ran. Judge 2 returned FAIL with six verified count calls; Judge 1 ended with a provider-aborted timeout. Both complete raw judge sets were scanned with zero credential occurrences and all 13 files matched the A2 inventory. The failed judge used a dotted string instead of separate `count_array.keys` segments twice; the next instrument will clarify that input description without changing its oracle. This is not an accepted two-judge product verdict. Cleanup is in progress; the first script entry stopped at relative manifest resolution before any runtime stop or decommission, leaving only empty retained directories. Root inspected the exact two-hunk correction and authorized continuation with all hashes and preservation guards unchanged.
- **Resume reconciliation:** The primary re-read the current intent, checkpoint, actual isolated four-file diff, Run 4 receipt, and bounded cleanup script after ordinary mid-slice compaction. No fresh direction check ran; this line is self-reported continuity, not independent proof.
- **Run 4 closure and current lower gates:** Cleanup completed with exit 0 after the retained manifest-cwd correction. Root independently observed the original staging image/health and pointers, baseline Caddy, d686 NanoHost stopped, absent disposable roots/listeners, both retained private manifests, and all four backup hashes. The sole accumulator accepted complete cleanup for all four retained nonpassing attempts, with no accepted Run 4 two-judge product verdict and passing count 0/2; its updated artifact SHA-256 is `5a8c546301c0ec7d0bcdb6c034b5979d06be7530591aa0c3288d6c3092c6cec8`. Exact 8772249a PR checks and focused hosted L5 run `33960658844` pass: health, Harness Admission, public Task MCP, Web, and stopped-server App recovery. The raw log SHA-256 is `59391d002628292298ff89e20541f8e197886f090263b33e6473fd6d979ee438`.
- **Run 5 preparation:** The independently reviewed instruments preserve the persona, sole ask, story, count oracle, model and low reasoning, bounds, and public-only actor actions. The neutral count_array key description fixes the failed dotted-key input without supplying an expected result. A same-8772249a App rebuild under normal public-source permissions removed the nonroot seed read failures; its image is `sha256:57b5dbef96d81613988df0afbf1ced33a07e71bfd65588dd6a6a0e513d5f9aaa`, with unchanged Dockerfile and Web index. Actor SHA-256 remains `33ab36b04cfe364d18e36066335952cc8b179e70cf40e17420dc6f823495ae1d`; judge is `6b3967e0469b9b0f8c59a3404520ef75e016bb73697ac32367ebf7da3b97b30f`; scene source is `6a3f93d84bdf5766614788d040ed5d8445bce973bd052199c305f0bbc0c416f5`. Root directly observed exact healthy candidate and original App identities, the readiness output, and remote actor/scene hashes. Target checks pass, subscription is logged in, initial story arrays are empty, and the Task preflight selects the expected warm Worker and Luna. The real actor is the next action.
- **Run 5 observation:** The actor terminated with exit 0 after 70,677 ms, 17 provider calls, 16 browser calls, and zero tool errors, recoveries, or guidance. It visited Repositories, returned through the sidebar to Chat, recovered from the default Assistant send failure by selecting the Worker, then finished before the Worker settled. The actual Worker completed from local Git and roadmap inspection; settled public records show 12 CapabilityCalls, 51 UsageRecords, one AEP, no `mcp.call_tool`, and no approval. The 81-file confidentiality scan found zero occurrences in all seven private-value categories. The sealed package is `temp/r058-l6-run5/sealed/package.json`, SHA-256 `b0bfeff055c5198fedfef9762ff67d71664d73b2f80a52235c9ee620d2267198`; input SHA-256 is `3aefeca1448a79594a279020406c325b60983ce18544f9d39c5784d9ab823d30`. Both fixed clean judges exited 0 and returned FAIL; their raw schemaSnapshotId claims incorrectly include `mcp.list_tools`, and judge 2 adds an unsupported non-actor-trigger inference. Those limitations remain unmodified in raw evidence and do not become product facts. Both judges completed their count tool (one and two successful calls respectively). Direct cleanup exited 0, decommissioned the candidate, retained private logs on A2, removed disposable state, and restored baseline Caddy and healthy original staging; root read the actual output at `temp/r058-l6-run5/cleanup-observed.log`, SHA-256 `016034f6cc5b31dc025fec867e92b2a8a7f443a10f459887a358d3c09d0e794d`. The sole accumulating verifier independently accepted Run 5 as failed with cleanup complete, retained the unsupported judge claims only as limitations, and recorded five ordered nonpassing attempts with passing count 0/2 at accumulation SHA-256 `a374d5afa9346a01c5ad810fccd32c33a66f9871af72c87d4d2a3ee5999619a6`.
- **Execution-method finding:** The engineer reported more than four hours without a completed delivery and challenged the cost of repeated test preparation. The primary accepts the finding: per-run script rebinding, redundant evidence packaging and review, repeated raw-judge tool failures, and ordinary shell permission/cwd mistakes displaced product work. The corrected product is already independently reviewed and passes focused regressions and hosted L5; PR merge and version refresh remain undelivered. This changes the method, not product authority or the required acceptance outcome.
- **Reframe:** Run 5 defeated the generic repository/issues ask: the settled Worker could answer from local Git and roadmap without the required mechanism. The independent direction check returned Reframe and accepted the next sole ask: “Have the workspace agent report the connected GitHub repository’s current description and default branch, together with every currently open issue’s number and title.” Only fresh scene identities and fixture-owned hosted description, issue numbers, and titles change; their requested facts are absent from the tracked checkout. Persona, actor tools and bounds, product bytes, tool schemas and policy, story, and judge count oracle remain unchanged. No UI change or new test facility is justified by Run 5. If this revised objective still produces no tool call, stop repeating L6 and return the assertion to its owner instead of adding guidance.
- **Run 6 observation:** The actor completed in 112,424 ms with 15 provider calls, 14 browser calls, and no guidance or tool errors. The revised goal produced successful `repos.get`, denied `issues.list`, and a granted approval. Resolved controls disappeared correctly, but the actor waited without sending a new Task and received no final answer. Both judges exited 0 with FAIL and one verified count each. Judge 1's automatic-resumption diagnosis conflicts with the accepted separately authorized new-Task owner and remains a raw limitation only. The sealed package is `temp/r058-l6-run6/sealed/package.json`, SHA-256 `ef63390bd87961d66d84d7382bad592b62d901084e6d4b74a4ef3c5fe8af8d80`. Root read the complete verdicts and direct cleanup output at `temp/r058-l6-run6/cleanup-output.log`, SHA-256 `21f591b7946fea55c653f78e2cdd9872e5c4604d73dd02f98aead594785e06ba`: decommission, owned-resource removal, private-log retention, original staging restoration, and backup check passed. The judge export scan found zero source or disposable credential occurrences across 13 files.
- **Run 6 correction:** The MCP-specific approval description now tells the user to send a new task message after approval. The existing parameterized public lifecycle test failed on that missing sentence for both direct Task and selected warm-Worker conversation entry, then the full MCP file passed 8/8 after the one-string correction. NanoCore typecheck and lint passed. The independent Reviewer inspected the exact diff and logs and found no actionable issue; the existing guide remains accurate. No lifecycle, Web, protocol, or test infrastructure changes are needed.
- **Resume reconciliation:** The primary re-read the user objective, current Git, Run 6 evidence, governing next-action lifecycle, and exact correction after ordinary mid-slice compaction; the accepted route remains unchanged and no new direction check ran.
- **Run 7 observation:** The new App passed its standard smoke and UID 1001 read check, and PR CI `33964978916` passed. The actor completed in 129,692 ms with 17 provider calls, 16 browser operations, and no errors or guidance. It read the new approval instruction, granted approval, and explicitly submitted a second Task. Public records retain two Turns and AEPs, but only the first Turn reached MCP. NanoHost exited 1 at `2026-09-05T12:16:05Z` during the second `reference.import` of `package-config/package.json`; its fixed stderr event and terminal import failure are retained, while the underlying stderr body is absent. The second Turn remained running. Root read the full actor action sequence, public state, service exit, and zero-match confidentiality scan across 83 files. Sealed package SHA-256 is `b4912a13a46b36b8fe74c004fc6cda999d7814ad2b4712ac9364add713c89b52`. Judge 1 returned FAIL; Judge 2 timed out after 600,007 ms and two invalid count paths, with no verdict. The sole accumulator records incomplete adjudication and no pass. Cleanup completed: retained evidence names the failed NanoHost exit, candidate resources were removed, original staging and Caddy were restored, and backup checks passed.
- **Run 7 correction:** d2abe7bf passed the existing package-import regression but left Context files colliding at the shared root. Independent completeness review refuted the package-only premise. Core Sandbox and Session Static Workspace already require private AgentSession input namespaces; the fresh Verifier withdrew Ask Human after reconciling that higher authority. The data-boundary global paths are stale projections. Non-authorizing proposal: `/Users/m5pro/Documents/AI/openkit/temp/changes/202609031230162442-r058_worker_mcp/run7-import-scope-decision.md` in the shared checkout, outside this candidate. Preserve slot identities, exact AEP/Turn lineage, no-overwrite atomic imports, byte/digest/link checks, and existing cleanup/fencing; align only this AEP/Context seam.
- **Private-input correction:** The existing shared path projection now binds canonical AEP and Context inputs to `/openkit/sessions/<agent-session-id>/...`. NanoCore admits the exact AgentSession before one-shot imports and `turn.start`; the Harness clears both immutable input slots before reuse and rejects duplicate namespace bindings. The actual file-effect regression covers successor imports, omitted prior files, separate AgentSessions, no-overwrite, and link/integrity rejection. Sandbox and Harness keys exclude only their accepted dynamic inputs while retaining static trust, process, resource, and policy boundaries. Imported historical AEPs remint paths and rebuild the existing session-workspace projection and digest without runtime authority.
- **Current local evidence:** Node 24.18.0 and pnpm 10.33.3: Worker Protocol 20/20, Config Schema 160/160, and Worker Shim 185/185 pass (`temp/r058-private-packages-tests.log`); their typechecks/lints and Worker Shim build pass. NanoCore build/lint/typecheck pass; the final export, lineage, materializer, and factory run passes 109/109 (`temp/r058-private-final-focused.log`), with the final process/resource key run retained at `temp/r058-private-final-keys.log`. The actual helper passes (`temp/r058-private-helper-final2.log`), built public Task Worker MCP smoke passes (`temp/r058-private-mcp-smoke.log`), and repository checks pass (`temp/r058-private-repo-final3.log`). Full NanoCore ran 2,561 tests with 2,557 passing and four failures; its newly exposed incomplete AEP fixture was corrected through the existing producer and passes in the focused run. The three established phase-order, macOS `/proc/self/fd`, and Vault `workspace_registry` baseline failures remain; no full-suite pass is claimed.
- **Independent judgment:** The Reviewer accepted the actual corrected diff, including exact compatibility-key boundaries and imported history. The Verifier returned Continue for the resumed primary context and found no remaining authority/scope falsifier. The Auditor's Context-root grammar and weakened negative-oracle findings are corrected. The amended owners preserve byte/digest/link checks, no-overwrite, no-replay, cleanup failure, and wider fencing. This correction adds no runner, wire field, operation, or dependency.
- **Current artifact evidence:** Exact-commit hosted L5 workflow `33971423582` passed its smoke, stopped-server recovery, installer, and test-image jobs. Canonical A2 App and Worker builds and image smokes passed at `5162a095`; App image is `sha256:650daa38f0792ebbd499c6765bc96e89ddd31ee71484f6b596067161cc9f0217`, Worker OCI descriptor is `sha256:a14b23df933f22fe26e0511f9a789d850fc18659a601ee7d6fe7b886531b2ae1`. Their build log is retained under shared-root `temp/changes/202609031230162442-r058_worker_mcp/a2-prepare/build-images-5162a095.log`.
- **Run 8 observation:** Actor execution completed in 140,063 ms with 18 browser calls, no tool errors, and no guidance. Both clean Luna-low judges returned FAIL after four and seven independent record counts: two directly allowed `repos.get` calls succeeded, but neither `issues.list` call succeeded and no final answer was produced. The successor used a different AgentSession. All stable approval scope fields matched except `argumentsDigest`, so a second approval was lawful and this does not prove rejected reuse of an exact grant. Shared-root `temp/r058-l6-run8/sealed/package.json` has SHA-256 `b388aaa586b9e6e5daaf8c37b116c1925eb05565cdef5ef2c7c67ef6e93b3c15`; the primary verified all 119 manifest entries and inspected both judge completions and the once-only cleanup output. Judge causal speculation is retained but is not accepted as a production diagnosis. Original staging is healthy, baseline routing restored, owned ports free, source authentication unchanged, and prior evidence preserved.
- **Reframe and fresh direction:** The independent Verifier inspected current Intent, Git, Run 8 evidence, fixture, story, and L6 owner after primary compaction and returned Reframe. The scratch MCP schema advertised optional `owner`, `repo`, and `state` arguments while its handler ignored all arguments and returned fixed Workspace data. This artificial variation created distinct approval scopes without a different fixture effect. Define those Workspace-bound fixture tools truthfully as parameterless; retain the unchanged goal-shaped actor ask, product, and story assertions. No production approval relaxation, actor guidance, new runner, or repeated failing setup is authorized.
- **Argument-scope regression:** Direct inspection corrected an overbroad coverage claim: the existing route test covered matching arguments and a consumed/concurrent loser, but not changed arguments before grant claim. The same fixture now has a changed-argument case proving a distinct Gate, the original grant still granted, no upstream call, two denied CapabilityCalls, and zero UsageRecords. The route file passes 9/9 under Node `24.18.0` (`temp/r058-mcp-argument-scope-tests.log`). A temporary constant-digest mutation made this exact assertion fail because the changed call resolved; the production source was restored byte-for-byte (`temp/r058-mcp-argument-scope-mutation.log`). This adds no runner or production behavior.
- **Run 9 preparation and authorization:** The exact `5162a095` App and public scene are ready with the independently accepted parameterless Workspace fixture. Automatic approval review initially rejected NanoHost enrollment and service start because it required explicit informed approval for the shared service transition and persisted temporary credentials. The primary confirmed the expected A2 machine identity, original staging `running:healthy`, and NanoHost inactive/dead with `MainPID=0`. The stage manager paused without a workaround; shared-root `temp/r058-l6-run9/receipts/pre-actor-status.json` retains that prepared state. Epoch 6 now supplies the requested engineer approval, and the stage manager is continuing the same prepared attempt. Source authentication remains unchanged; actor launch and outcome require actual subsequent observations.
- **Run 9 accepted:** The actor completed in 128,096 ms with 18 browser calls and no errors or guidance, returning the requested repository description, default branch, and both issue numbers/titles. Public records contain 22 CapabilityCalls, 70 UsageRecords, two successful `repos.get` calls, one denied then one successful `issues.list`, and two completed Turns with distinct AgentSessions. The exact approval argument digest matches. Both clean judges returned PASS with four and three independent counts. Root verified all 102 entries from the run-relative manifest and the sole accumulator accepted the first countable pass on production `5162a095`: sequence `1/2`. Sealed input is shared-root `temp/r058-l6-run9/sealed/evidence-input.json`, SHA-256 `1054524332e0aafcfb344c7af05919a996df325128daf2e254f953ec83756d36`.
- **Run 9 retained state:** Actor, judges, and browser ended and no Worker-owned MCP process remains. Normal App and NanoHost remain running under Epoch 7, with both original `/home/ubuntu/.openkit` and current `/home/ubuntu/openkit/r058-l6-20260905/run9/data/openkit` preserved. `temp/r058-l6-run9/receipts/final-retained-state.json` records users, configuration, credentials, and subscription retention, not cleanup or restoration. The independent Verifier confirmed the mid-run retention instruction does not change the product verdict or require a rerun. The story's future environment wording now permits retained staging with fresh Workspace state; independent review accepted that no persona, outcome, or deterministic assertion changed.
- **Run 10 observation:** The actor and both judges completed, with raw FAIL/FAIL verdicts. The selected Agent requires Workspace source `r058-run9-candidate`, but new Workspace `ws_3` provided only `r058-run10-candidate`; all three public failure checkpoints report `Workspace data source not found: r058-run9-candidate`. No successful Worker Turn, MCP call, or approval occurred. This is an `environment_failure` under the L6 owner, not a demonstrated production regression. The original submit HTTP response was not retained, so no status or error code is claimed. Root verified all 105 entries of shared-root `temp/r058-l6-run10/MANIFEST.sha256`, whose hash is `91617b3a359e4fd7f8e76488c2f206a42e1a810923048ef4af4bab0311d9a682`; sealed input hash is `ed353f2f0b12f4088b80eed48c15d0741897478ff697ec55d8ffca9de81d8a10`. Raw failure evidence and normal staging data remain retained.
- **Fresh direction:** After primary compaction, the independent Verifier read current Intent, Git, and Run 10's sealed classification and returned Continue: repair the existing Workspace configuration, compare it directly with successful `ws_2`, and accept the source-reference readback before launching another actor. No image rebuild, credential reseed, enrollment, service reset, new wrapper, or product correction follows from this evidence.
- **Run 11 observation:** Direct configuration readback proved fresh Workspace `ws_4` and Thread `th_4` matched successful `ws_2`, resolved the selected Agent's exact data-source reference, and had empty acceptance records. The actor completed without guidance but received no result. OpenShell created a second Workspace-specific Sandbox while Run 9's warm Sandbox remained; NanoHost then exited during `sandbox.create`. The public Turn stayed running, with no Worker records, and interruption returned HTTP 404 `turn_interrupt_failed`. Both clean judges returned FAIL. Shared-root `temp/r058-l6-run11/sealed/evidence-input.json` has SHA-256 `75f7294592b10ba7951dcc35e6f565becc920258bbe0bce0b5c4709c121d4a8a`; the accumulator verified all 89 manifest entries and retained sequence `0/2`. Normal data and credentials remain, App is running, and NanoHost became failed through the product failure rather than intentional teardown.
- **Run 11 reframe:** Independent direction rejected an unnecessary NanoHost multi-Sandbox or wire expansion. Existing one-Sandbox scheduling and idle AgentSession eviction owners permit clean retirement of an unpinned, proved-idle incompatible Sandbox while preserving Core history. Busy, pinned, or unproved capacity stays queued. NanoCore must prove exact session closure, then close the bridge and delete the old Sandbox before replacement; NanoHost must reject occupied creation before any native effect. This correction is tracked as `R058-FND-003` in this bundle's findings. The new NanoHost preflight regression first failed with `Ok(None)` instead of `Err(IdentityMismatch)` and then passed with all 19 epoch tests; Clippy passed and independent review accepted the two-file guard and guide diff. An initial untrusted-mise failure is setup evidence, not RED.
- **Recovery observation:** At `2026-09-05T16:54:49Z`, existing maintenance had changed Run 11's lease to `stale / startup-timeout / needs-evidence`, but backend cleanup and the public Turn remained unsettled while NanoHost was disconnected. The separate shared-root receipt `temp/r058-l6-run11/receipts/recovery-observation-after-startup-deadline.json` has SHA-256 `0cd75d64659a3002fb0d71d0e5f5324b46943284caa3f0eab91c02e898451e90`; sealed run bytes remain unchanged. One normal start of the failed service using its retained configuration is authorized to observe the existing fresh-readiness and recovery owners, without a new Task or actor.
- **Recovery startup result and direction:** The single ordinary start reached OpenShell Health 200 and then failed before any new connection or Sandbox effect. Shared-root `temp/r058-l6-run11/receipts/nanohost-single-recovery-start.json`, SHA-256 `93f6ed587f7df4adef4a7ea1b32071b0a233496c4f451fc762e17ec2aab68f2e`, preserves the expired 300-second fence observation and unchanged pending recovery. After primary compaction, the independent Verifier reconciled current Intent, Git, receipt, startup source, and the accepted rebuild owner and returned Continue: an ordinary failed start establishes a new fence boundary, after which a prompt separate start may prove fresh readiness. Neither failure satisfies the original recovery bound; no marker mutation, timing relaxation, or new recovery mechanism is permitted. Root read the successful exact `0c72afc7` native build log and independently matched artifact SHA-256 `a54dffcf835ba61e35be3da442cdf3522707d27e1d4f0eb2da2117ebc07b679b` on A2.
- **Bounded recovery observation:** A2 now has the accepted NanoHost artifact installed. The first start truthfully failed the expired fence; the second consumed the marker and reached authoritative generation 2 readiness, then the existing result-only expectation rejected polling with HTTP 409 and fenced the connection without a physical CreateSandbox. Ordinary maintenance subsequently marked Run 11's Turn interrupted, lease released, plan completed, and backend cleaned at `2026-09-05T17:15:57Z`. The pending workspace-handoff field remains a projection; no database field was manually terminalized. Shared-root `temp/r058-l6-run11/receipts/nanohost-0c72-bounded-recovery.json`, SHA-256 `ad091ee9567a1d21e3282830ac01ad5e9fac5c86bf5669efbd7b9b7754c47039`, preserves both starts and the actual service-failed state. No third start ran. The inspected `nanohost-session-dispatch.ts` owner removes the unknown result-only group and rejects its waiter before closing that generation; the upcoming ordinary App deployment supplies a fresh process with the now-settled durable history.
- **NanoCore correction evidence:** The final Workspace-only regression keeps the image identical and proves drain before exact private closure, physical deletion before replacement, preserved compatible reuse, and rejection of an overlapping old-key admission or cached materialization during drain. Four focused files passed 168 tests; typecheck, focused Biome, and build passed. The complete NanoCore run emitted 2,567 test records with only the three already classified baseline failures: boot phase-order fixture, Vault admin fixture missing `workspace_registry`, and the macOS `/proc/self/fd` case. Its SHA-256 is `8ed3348a7b91eac5f3e4e209945f5a37fd00f91bd625964dd1de5bf45e0af51d`; raw logs are retained under shared-root `temp/changes/202609031230162442-r058_worker_mcp/idle-sandbox-retirement/`. Documentation model validation covered 215 documents, its 67 tests passed, and lifecycle/index checks passed. Current implementation projections now acknowledge the sole idle-eviction pin reader while retaining the unmet Goal selector/writer criteria; accepted design is unchanged. Independent review traced synchronous Task/Goal cancellation to its existing checkpoint owner and rejected retaining an orphan asynchronous mode admission, so queued behavior remains scoped to the scheduler and ordinary product admission rather than the paused mode lifecycle.
- **Exact deployment observation:** Commit `8305541d` passed canonical A2 App build/smoke and hosted L5 workflow `33981204231`. App image `sha256:2030c0697556f97afb6f2f4123e4b1c6df2e9477bcd790b1e72808d68bce3298` replaced the existing container using unchanged mounts and credentials. The installed NanoHost reached authoritative generation 3 readiness and remained active. Before any actor, direct readback found Run 9's old idle Sandbox/Harness/binding rows still marked reusable despite the fresh physical epoch; Run 11 history remained terminal. Shared-root `temp/r058-l6-run11/receipts/app-830554-deployment-nanohost-recovery.json`, SHA-256 `e0975fb1126b30d555a7027f8ad02259230266a06aac1388c98dda8eddde42b8`, preserves this observation without a database edit.
- **Restart restoration reframe:** The independent Verifier rejected treating connection generation or the repeatedly reported `freshEmpty` readiness flag as Runtime Epoch identity. A durable idle resident missing from the fresh backend's process-local inventory is restart-unproved and cannot be restored solely from durable ready flags. Existing continuity and Sandbox owners permit fencing that idle, unpinned resident and widening cleanup to exact `bridge.close` and `sandbox.delete`, which prove whole-Sandbox absence whether the physical resident survived or is already absent. Only then may private runtime projections be removed and fresh placement begin. Busy, pinned, unsettled, or contradictory state remains non-admitting; exact active-attempt recovery and same-process compatible reuse keep their existing owners. No epoch field, wire, readiness purge, per-run reset, or new recovery framework is admitted.
- **Restart correction acceptance:** The existing continuity seam now treats a durable idle binding absent from the current backend inventory as requiring whole-Sandbox retirement. The successor package and admitted lease authorize bridge closure and physical deletion before predecessor Core retirement; the mixed-Harness case cannot bypass that cleanup through a restored sibling. The focused mixed-Harness check first failed on missing cleanup, then four selected regressions passed; independent review inspected the five final source/test hashes and both guides, reran the deciding mixed-Harness case, and returned ACCEPT / Continue with no actionable finding. The final package run reports 2,565 passed, one skipped, and the same three baseline failures out of 2,569 records. NanoCore build and documentation model, lifecycle, and index checks pass. Exact final output is retained under shared-root `temp/changes/202609031230162442-r058_worker_mcp/idle-sandbox-retirement/restart-final-full-tests.log` and `restart-final-build.log`. No epoch field, wire, or new runner was added.
- **Final candidate deployment:** Commit `ed1036f99f381203057829b9e340205a6459eaea`, tree `537bcdaea7081dab85a0ad407b7558b6e24704eb`, passed PR CI `33983236484`, hosted L5 `33983282037`, and canonical A2 App build/smoke. App image `sha256:0a9b15bc8c8061624e7c7530af22bc6fdc64e8f6339faae2fc5e4e3ad8f36834` replaced the retained container using unchanged data and credentials. NanoHost stayed on PID `502473`, installed SHA-256 `a54dffcf835ba61e35be3da442cdf3522707d27e1d4f0eb2da2117ebc07b679b`, and reconnected normally at generation 4. Hosted log SHA-256 is `0d827acc18ed2d9e04094bc1b85ebe4ce536119924ba8c5209fdfbd5169ef320`; App build log is `1eb2fb93cfdb5fb305aa3dc007aa0839ab06d274011bdbe27d07a7b3472eb535`, both under the existing shared-root evidence directory.
- **Run 12 accepted:** The single actual actor completed in 96,675 ms with 17 provider calls, 16 browser calls, no tool errors, and no guidance. Public records contain 19 CapabilityCalls, 55 UsageRecords, two completed Turns with distinct AgentSessions, two successful `repos.get`, and one denied then successful `issues.list` using the granted approval. The final answer is correct. Initial script-path EACCES and judge self-check flag spelling errors happened before their respective model calls and remain setup history only. The first sealed package and judge binding mistakenly copied the old NanoHost hash; the immutable pre-actor scene and deployment prove actual `a54dffcf`. Those original bytes and raw PASS outputs remain retained. Root corrected only that hash in a separate input and driver, and independent inspection proved that reversing each sole substitution recovers the originals. Two fresh clean judges returned PASS with one and three verified counts on corrected input SHA-256 `9e9910555bc1511d90989868a8791f8b4b5e89c041c6cc6720e59d10216cef8a`; no actor or product work was replayed. Judge 2 overstates schema identity on all MCP calls; the four `mcp.call_tool` records have snapshots while two `mcp.list_tools` records correctly have none, so that wording remains a limitation only. Root and the sole accumulator verified all 99 entries in shared-root `temp/r058-l6-run12/artifact-manifest.sha256`, SHA-256 `edc5cd071b045407dd83bc7a8955a8ba04fca51185a85a9cb5a1a8b1d868970e`. The scan reports zero sensitive-pattern matches; retained-state receipt shows the old Run 9 resident absent, only the current idle clean resident, both leases released, both plans completed, and normal App/NanoHost/data retained. The accumulator accepted sequence `1/2` on exact `ed1036f9`; accumulation SHA-256 is `4b359391e301ca083d23360d64b91631c1aa37dab4d1e52bfea6b18df521896c`.
- **Run 13 verification:** The next fresh Workspace `ws_6` and Thread `th_6` used the same `ed1036f9` App/Worker/NanoHost bytes and retained warm Run 12 state without service restart or credential/configuration reset. The actor completed in 92,075 ms with 13 provider calls, 12 browser calls, no errors, and no guidance. Final public evidence contains 18 CapabilityCalls, 50 UsageRecords, two completed Turns with distinct AgentSessions, two successful `repos.get`, and one denied then successful `issues.list` under the matching grant; the final repository and issue result is correct. Both clean Luna-low judges returned PASS, exit 0, with six and four independent count calls. Root inspected their actual outputs and verified all 73 entries of shared-root `temp/r058-l6-run13/MANIFEST.sha256`, SHA-256 `81dbad8291db882b630ac31ed86d3235d5af18eb4d29934ff0b2804d12ec10e5`; sealed input SHA-256 is `f4d52b126a06907ebcd19361a3c3465a425fae5242b86b805156bc5c74a2010a`. The 72-file sensitive-pattern scan passes. Retained-state receipt SHA-256 `c3dfe4de4283f8d270f8fb6577db7c9f30eb168d93544a6950db48838984e722` shows only the new Workspace resident idle and clean, released leases, completed plans, and unchanged healthy App/NanoHost PID. The sole accumulator independently verified the final files and accepted the exact-product sequence at `2/2`; its updated shared-root `l6-accumulation.md` SHA-256 is `af98c3e5d445ccfcdaa37f6fdd2d16f3c069525b697c3bd74cae25142c54d2b0`. No Run 14 is needed.
- **Next action:** Independently accept the final evidence and documentation closeout, commit it without changing tested executable bytes, and merge PR #4. Continue only the Codex/PI stable version/schema refresh and OKF v0.2; then integrate remaining OpenKit changes into `main` and perform the Epoch 8 all-PR/branch/worktree cleanup. Normal A2 services, data, configuration, and credentials remain retained; later roadmap development remains paused.
- **Predicted observable:** Two consecutive admitted L6 runs demonstrate selected tool discovery, one directly allowed call, the visible approval Gate before the protected effect, a fresh successor Turn claiming the grant, and redacted public lineage and usage. Completed transient test processes exit; normal A2 App, NanoHost, data, configuration, and credentials remain intentionally retained, with both active data-root identity and preserved older data recorded.
- **Reframe evidence:** Any proof that a required path exists only in documentation or unit mocks, source closeout can be bypassed, credentials or upstream topology reach Worker-visible bytes, or owned descendants survive teardown requires a local correction or Reframe before roadmap closure.

## Acceptance And Closeout

- A normal independent Reviewer must inspect the exact final diff and named execution output before feature acceptance.
- Fresh verification or audit is used at owner reconciliation, major direction changes, and final closure where consequence or uncertainty warrants it; producer reports alone never accept their artifacts.
- Focused checks run during each slice; final scope determines package lint, typecheck, tests, build, packaged smoke, and deterministic story evidence.
- Closeout records commits, exact check results, external effects, unresolved findings, and surviving conclusions promoted to their owning documents; the roadmap changes only after the whole accepted R058 plane passes.

## Implementation Summary And Final Verification Evidence

R058 is verified on executable commit `ed1036f99f381203057829b9e340205a6459eaea`. Runs 12 and 13 are consecutive successful real-Codex product attempts with independently passing adjudication; earlier failures and Run 12's original provenance-invalid adjudication remain retained. The final documentation-only closeout does not change those executable, configuration-template, or story bytes. Current NanoCore full-suite baseline limitations remain the three named failures; skipped jobs and tests are not counted as passes. `R058-FND-003` is closed by the occupied-create guard, exact idle retirement and restart correction, focused regressions, and both retained-state runs. The unrelated Agent Catalog finding `R058-FND-001` stays deferred to its named owner. This verified feature is ready for the separately authorized PR merge; the version refresh, OKF update, and repository-wide preservation/cleanup remain outstanding work.
