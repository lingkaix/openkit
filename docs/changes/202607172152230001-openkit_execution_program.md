# OpenKit Execution Program

Type: change-plan
Status: in-progress
Date: 2026-07-17

## Intent

Own the single ordered execution queue for all currently planned OpenKit work: the bounded closeout of the Core/spec/implementation alignment audit, the five planned implementation change plans, and the remaining time-boxed review passes.

This record owns only sequence, entry and exit gates, dispositions, the backlog, and the shared convergence rules. It owns no design contract and no implementation content. Every work package keeps its content in its owning change plan and canonical specifications; this ledger links to them and must stay under roughly 150 lines.

## Why This Record Exists

The [alignment audit](./202607111941330001-core_spec_implementation_alignment_audit.md) produced high-quality slices but converged slowly because of three structural defects recorded in its 2026-07-17 redesign: scope absorption (a review program implementing everything it found, so its finish line receded), uniform rigor (per-command distributed-systems exactness applied to a pre-release single-deployment product), and per-slice protocol overhead. The rules below are binding for every work package so those defects are not reproduced.

## Convergence Rules

1. No absorption. A work package implements only its frozen scope. Any new finding — including findings discovered inside the active package — is classified with the audit's finding codes and appended to the Backlog below, not implemented in place. Only `SECURITY-GAP` findings interrupt the active package.
2. Frozen scope. A package's scope is fixed when it activates. Scope changes require an explicit edit to this ledger with a one-line rationale; silent growth is prohibited.
3. Central idempotency default. All mode and workflow commands use the existing command-idempotency ledger with one shared policy: receipt present means replay projects stored identifiers plus current owner state; changed input returns `409 idempotency_key_conflict`; request-owned effects without a receipt return `409 recovery_required` with no inference, synthesis, settlement, or repair. Byte-exact response reconstruction is not a requirement; callers re-query state. Per-command contracts may narrow what a receipt stores; they may not add reconstruction obligations. This default is normative in C07 Protocol and S62. The only missing-receipt effect-completion exception is G07's exact S60/S61 Knowledge review decision, which may complete its one fixed absent page after the matching accepted review is durable; every other partial state fails `recovery_required`, and no other new exception is authorized.
4. Scoped precision bar. The "two independent implementers" bar applies only to Durable contract families under S62: persisted and portable truth, protocol records, identity, authority, audit. Release-coupled surfaces (App API, Core Client, bundled CLI, unified Skill, Web) require only single-implementation clarity plus test sufficiency, because they carry no cross-release compatibility promise.
5. Lightweight checkpoints. Checkpoints in owning plans are table rows or entries of at most 10 lines: scope, findings by class, commits, verification, remaining. Prose recitals of what was not added are replaced by rule 6.
6. Standing prohibitions. No work package may add: a second workflow engine, a settlement or recovery workflow, reservation or settlement records, compatibility shims or aliases for internal contracts, per-document tracking files, a new runner or acceptance harness, or a speculative framework. Stated once here; never restated per slice.
7. Bounded verification. Focused suites during a slice; affected-package suites plus repository gates at slice close; `verify:full` only at package exit. Per-slice re-reading covers the affected documents, not a full group set.
8. One active package. Work packages run sequentially. Documentation-only preparation for the next package may proceed in parallel; implementation may not.
9. Size caps. New change-plan prose stays under roughly 250 lines before implementation. Detail belongs in the owning specifications.
10. Real-use evidence first. Where a contract's remaining risk is behavioral rather than safety-critical, prefer shipping the surface and collecting dogfooding evidence over further pre-use specification.
11. Audit preamble. A work package that absorbs a dissolved audit group (WP-1, WP-2, WP-5, WP-6) records one review-only preamble checkpoint in its owning plan before implementation starts: the group authority map for the concepts the package touches, findings classified with the audit's finding codes (in-scope findings fold into the frozen scope; the rest go to the Backlog), and confirmation of the inherited group exit criteria. The preamble is bounded to at most one review day and authorizes no implementation.

## Work Package Queue

| # | Work package | Owning record | Entry gate | Exit gate |
| --- | --- | --- | --- | --- |
| WP-0 | Audit G01 bounded closeout | [Alignment audit](./202607111941330001-core_spec_implementation_alignment_audit.md) | Frozen closeout list recorded in the audit | Class A items verified; Class B recorded as accepted posture and the idempotency default promoted to C07/S62; Class C dispatched; G01 closed |
| WP-1 | Unified Agent Skill interface, absorbing audit G02 | [Agent Skill Interface plan](./202607131935040001-openkit_agent_skill_interface.md) | WP-0 exit; G02 audit preamble recorded in the owning plan | MCP package and four legacy Skills deleted; unified Skill and CLI pass the replacement acceptance stories |
| WP-2 | Worker Agent adapter boundary, absorbing audit G03 | [Adapter Boundary plan](./202607160036500001-worker_agent_adapter_boundary.md) | WP-1 exit; G03 audit preamble recorded | Every owning-plan exit and verification criterion passes, including deletion of both security gaps, one control plane, unchanged lease/recovery behavior, stock OpenShell containment, disabled capabilities, three landed adapters, and the fourth-runtime criterion |
| WP-3 | Storage and provider review boxes: audit G05 and G04, review-only | [Alignment audit](./202607111941330001-core_spec_implementation_alignment_audit.md) | WP-2 exit | Combined time box of at most two review days; the Material-writeback owner decision (G05/C09) is recorded; all findings are ticketed; frozen security interruptions are corrected and verified |
| WP-4 | Work and Resource Interaction Phase 1 kernel, absorbing the G01 evictions | [Work Resource Interaction plan](./202607132212000001-work_resource_interaction_model.md) | WP-3 exit; absorbed items recorded in the owning plan | Stages 0-4 complete, including S39 delivery, Goal steering, version-owned Review, conflict-safe apply, and portability; Stage 5 and Web-dependent acceptance assigned to post-program S10 |
| WP-5 | Contract stability and multi-user Workspaces, absorbing audit G06 | [Contract Stability plan](./202607160021540001-contract_stability_multi_user_workspaces.md) | WP-4 exit; G06 audit preamble recorded | Owner-independent root, membership lifecycle, central authorization, bounded attribution/concurrency, current-effect authority, portability, and implemented same-release projection gates pass; Web remains with S10 |
| WP-5A | Provider and external-effect entry corrections | [Provider and External-Effect plan](./202607191752510001-provider_external_effect_entry_corrections.md) | WP-5 exit; G07 docs-only authority correction complete | Provider ownership is coherent; contradictory CapabilityCall attribution and uncertain Git-push retry fail closed; the existing real-provider story proves the bounded path |
| WP-6 | Explicit self-improvement loop, absorbing audit G07 | [Self-Improvement plan](./202607111600390001-self_improvement_loop_foundations.md) | WP-5A exit; one retained real completed work history exists to mine | Exact Knowledge callers, governed selection, accepted S39 delivery, human-reviewed create-only proposal and bounded reversal pass the owning plan |
| WP-7 | Residual reviews (audit G00, G08, G09, and residual G03) and program closeout | [Alignment audit](./202607111941330001-core_spec_implementation_alignment_audit.md) | WP-6 exit | Time-boxed reviews and explicit residual G03 consolidation/deletion dispositions complete; the full real-use scenario matrix re-checked; the audit record reaches `verified`; this program closes |

Current position: WP-0 through WP-5A are complete, including the G07 Stage 0 docs-only exit. The original WP-6 Draft bundle was removed from current authority because it would create a second evaluation and trigger platform. A stock OpenShell/Codex direct Task now supplies the retained completed history, strict S39 bytes, final assistant Item, and meaningful feedback required by the last entry gate; WP-6 Stage 1 is active over only existing-owner Knowledge correctness and one explicit human-reviewed loop.

## Ordering Rationale

- WP-1 before WP-2: deleting the legacy MCP surface and four Skills removes a test and maintenance drag that every later slice currently pays, the public command surface the CLI wraps is freshest immediately after WP-0, and it produces the first dogfooding surface — the real-use evidence the audit classifies as `REAL-USE-GAP`. The adapter boundary cannot change public APIs by its own acceptance criterion, so nothing in WP-1 waits on it.
- WP-2 before WP-4: S39 Context Package delivery and Material handoff ride the worker-runtime boundary that the adapters reshape; building delivery on a moving boundary would repeat rework.
- WP-3 before WP-4: the audit's Material-writeback item is explicitly blocked on one G05/C09 owner decision, which the time-boxed storage review makes. The G04 review supplied the provider and attribution findings now dispatched to WP-5A or their post-program owners.
- WP-5 after WP-4: freeze the stability baseline only after the last planned public-surface churn (Skill interface, Artifact and Material commands) has landed, and migrate storage roots only after the G05 review confirms layout assumptions.
- WP-5A before WP-6: real-use entry must not rely on contradictory provider ownership, fail-open CapabilityCall request attribution, or an external Git effect whose retry cannot prove completion. The slice reuses existing owners and the existing provider story.
- WP-6 last among builds: its explicit reviewing agent needs retained real work history. A persistent Reflector, scheduler, Skill Catalog and Harness are not prerequisites and remain non-authorizing future work.
- The Web UI rebuild (S10) is not part of this program; it follows as product work over settled contracts, with the G09 review in WP-7 as its preamble, and it owns the deferred S16 Material and S63 multi-user Web projections plus their browser-dependent acceptance.

#### Extra Note
WP-1's replacement L6 stories after MCP deletion are a substantial hard exit gate. WP-2 integrates three real runtimes and relies on its stop rules and smallest-first evidence to stay bounded. WP-4 Stages 3 and 4 delivered S39, Review, expected-base apply, and the related portability graph through existing owners without another framework; its Web-only remainder moves to S10. WP-5's migration has completed without a compatibility reader. WP-6 reuses those owners and one existing real-use surface; it does not add an evaluation or acceptance platform.

## G01 Eviction Map

Items evicted from the audit's G01 group by its 2026-07-17 redesign, and their destinations. Each destination plan records the absorbed item when its work package activates.

| Evicted item | Destination |
| --- | --- |
| S16 Material identity, immutable revision, Thread binding, inclusion queue, reads, restart, portable export | WP-4 |
| S39 Context Package trace, Goal steering delivery, `lastWorkerSeenRevisionId` | WP-4 |
| Exact Artifact command surface: introduction, workspace-only import, deterministic refine and redo | WP-4 |
| Material worker proposal and conflict-safe writeback | Decision in WP-3; implementation in WP-4 |
| Final Web, CLI, and Action Center projections plus minimal acceptance for the items above | Action Center and kernel-facing projections in WP-4; rebuilt Web projection and Web-dependent acceptance in post-program S10 |

## Backlog

New findings land here as one-line entries: `classification code — finding — proposed owner`. The active work package never absorbs them (rule 1). An entry leaves the backlog only by being scheduled into a work package through an explicit ledger edit.

- `TEST-GAP` — the A1 restart runner does not load and validate its owning Markdown story — WP-7 / G08.
- `OWNERSHIP-CONFLICT` — S23/S24 and dormant snapshot/resume implementation still disagree about the contracted fresh-session continuity boundary — WP-7 residual G03 deletion review and dispatch.
- `OWNERSHIP-CONFLICT` — S29 remains a separate current deployment/timing owner because S30 has not absorbed those contracts — WP-7 residual G03 consolidation review; retain both until then.
- `IMPLEMENTATION-DEFECT` — the bespoke GitHub credential helper remains outside the generic S32 declaration path — WP-7 residual G03 review and dispatch.
- `IMPLEMENTATION-DEFECT` — shared Agent readiness can report `ready` before provider/model compatibility and the fixed control-binary set are complete, although AEP resolution later fails closed — WP-7 residual G03 review and dispatch.
- `OWNERSHIP-CONFLICT` — pi-ai cost estimates are written as UsageRecord truth despite C19 reserving metering authority for provider facts — WP-7 / G08 evidence and metering review.
- `IMPLEMENTATION-DEFECT` — the native pi Responses path still bridges through Chat and has incomplete content-index, thinking, StopReason, and cancellation-partial-usage handling — post-program S40/S42 provider hardening; not a dependency of the explicit G07 loop.
- `DESIGN-DEFECT` — public Gateway lineage metadata is caller assertion rather than trusted evidence, so current projections can overstate provenance — WP-7 / G08 evidence review; no proof framework in WP-3.
- `REAL-USE-GAP` — Codex OAuth has pre-provisioned-account evidence but no real browser/device login, refresh, cancellation, and logout lifecycle proof — post-program S44 provider real-use work.
- `DOC-DRIFT` — S45's remaining gap is license declaration and validation — post-program snapshot maintenance.
- `REAL-USE-GAP` — one-time server bootstrap can be consumed before local credential storage succeeds, while repeat bootstrap is forbidden and no bounded operator credential-recovery contract exists — WP-7 / G00 residual design review and dispatch; do not add an onboarding or recovery workflow in WP-5.
- `OWNERSHIP-CONFLICT` — S47 claims general contract-evolution authority already owned by C03 — WP-7 / G00 residual documentation review.
- `IMPLEMENTATION-DEFECT` — Chat Mode Task handoff bypasses the existing WorkerCheckpoint/S39 loop used by direct Task Mode, so its exported worker history can lack the required Context Package trace — WP-7 / G00 residual alignment review and dispatch.
- `DEFERRED-ALIGNMENT` — recurring reflection, user Automation execution and event triggers lack real demand and the current Draft overbuilds fire/recovery ownership — post-program S35 only after manual-loop evidence.
- `DEFERRED-ALIGNMENT` — no real materialized worker Skill or repeated runtime promotion demand justifies the Draft Skill Catalog — post-program S34 / Agent Supply work after a concrete consumer exists.
- `DEFERRED-ALIGNMENT` — S19's suite, Judge and Harness platform is not authorized by current Core or proportionality rules — roadmap; a future accepted design must reauthorize current effects, bound retained fixtures and use a fresh explicit attempt after interruption.

## Checkpoints

- 2026-07-17 — Program established. Queue, convergence rules, eviction map, and backlog recorded; the alignment audit redesigned in the same slice.
- 2026-07-17 — Rule 11 (audit preamble definition) added; the four absorbing plans annotated with their inherited audit responsibility, the Work Resource plan annotated with its absorbed G01 evictions, and the in-flight direct-Task fence slice recorded into the G01 Class B landed baseline.
- 2026-07-18 — WP-0 exited. All frozen G01 Class A items passed their owner and package gates; Class B remains the accepted fail-closed posture, Class C remains dispatched, and WP-1 is next after its G02 preamble.
- 2026-07-18 — WP-1 entered after its bounded G02 preamble found no security gap, froze eight finding families, and dispatched two unrelated findings to the Backlog; owning-document corrections precede production work.
- 2026-07-18 — WP-1 owning documents corrected the Core/App catalog split, flat input, proportional acceptance, reachability removal, credential and cancellation compromises, active-turn transport boundary, duplicate repository route contract, and current worker-MCP status; S01 was superseded into named current owners, so Stage 1 test-first implementation may begin.
- 2026-07-18 — WP-1 Stages 1-4 passed through `d41b1dd`: full checked catalog coverage, the standalone Skill/CLI, focused process and redaction coverage, clean-copy L5, one local NanoCore L3, and one real A1 metadata-first Codex L6 all passed without a new runner; Stage 5 clean deletion is now active.
- 2026-07-18 — WP-1 exited. The user-facing MCP package and four legacy Skills are deleted without compatibility paths; the unified Skill/CLI, real A1 L6, release verification, and full repository verification passed. WP-2 is next after its G03 preamble; detailed evidence remains in the owning Agent Skill Interface plan.
- 2026-07-18 — WP-2 entered after its bounded G03 preamble froze the manifest-to-AEP-to-generic-shim authority, retained S25/S29, corrected the fourth-runtime criterion, found two security blockers, and dispatched unrelated continuity, scheduler consolidation, and credential-helper findings to the Backlog.
- 2026-07-18 — WP-2 closed both pre-adapter security blockers through `bc678d1`: OpenShell base egress is exactly AEP-derived, non-transient provider effects fail closed without hidden Providers v2 policy, and the complete arbitrary command/result surface is deleted with `interrupt` as the sole worker command. Focused package, OpenAPI, Skill, shim, rebuild, network, provider, typecheck, lint, build, and remnant checks passed; manifest/AEP and adapter contracts are next.
- 2026-07-18 — WP-2 narrowed the current adapter contract before implementation: adapters return no generated files, each launch consumes exactly one already resolved LLM route, and Pi result acceptance requires the pinned final settlement correlation. This closes the shared-harness config-envelope and route-selection ambiguities without adding a fallback or framework.
- 2026-07-18 — WP-2 pinned provider-route research found no universal native projection: Codex and OpenCode support the trusted relay through adapter-local non-secret configuration, Pi trusted relay is deferred under the no-file contract, direct and relay authority remain mutually exclusive, and AgentManifest `mode`/`deployment`/`transport` ownership is deleted while remote OpenShell Gateway topology remains backend-owned.
- 2026-07-18 — WP-2 exited: one strict manifest-to-AEP path, the generic shim with three static adapters, three pinned arm64 images, the fourth-runtime proof, and stock OpenShell `0.0.80` create/upload/dry-run/`--no-keep` checks passed; the full NanoCore suite passed outside the restricted sandbox with 200 files passed and 1 skipped and 1,973 tests passed and 3 skipped, plus NanoCore typecheck/build, worker-shim 159, config-schema 117, App API schemas 62, Docker contracts 13, `check:repo`, and `git diff --check`. A1 makes no control, recovery, or real-provider claim; G04/WP-3 retains real-provider evidence and is next.
- 2026-07-18 — WP-3 exited after the bounded G04/G05 review: the Material owner is the exact turn-output Artifact version, version-keyed Artifact Review, and one existing Workspace SQLite transaction; all non-security findings are scheduled in the Backlog. Frozen security corrections now fail closed for provider/model authority and public errors, direct secret aliases and OAuth diagnostics, worker credential scope and staged Git blobs, authoritative SQLite corruption, and imported effect authority. Final verification passed NanoCore 200 files with 1 skipped and 1,996 tests with 3 skipped, Web 12 files and 125 tests, package typechecks and focused suites, `check:repo`, and `git diff --check`; WP-4 documentation authority is next.
- 2026-07-18 — WP-4 Stage 0 exited after grouped S16, S39, S23, S46, C07, App API, and owning-plan reconciliation passed two independent contract reviews and one minimality review. The accepted contract reuses the existing generated `context` slot, WIS/WMR, backend handoff, binding mutation proof, command ledger, and bounded steering owners; it adds no lifecycle, recovery workflow, runner, or duplicate authority. Stage 1 test-first app-local schemas are next.
- 2026-07-18 — WP-4 Stage 1 exited with one cohesive runtime-neutral schema module for all fifteen target operation responses and nine mutation bodies. The App API schema package passed 68 tests, typecheck, lint, and build; `check:repo` and `git diff --check` passed; two contract reviews and one deletion-first review returned clean. Stage 2 owns Artifact import, Artifact introduction, and the eleven Material operations; the two version-owned Artifact Review operations remain paired with their legal Stage 4 producer and owner. S51 portability also remains one Stage 4 graph slice after Material, Review, and Context Package owners all exist.
- 2026-07-18 — WP-4 Stage 2 exited with direct Artifact import and idle-Thread introduction, exactly three Workspace Material owner tables, eleven Material operations, checked App API and Core Client projections, and the bounded `recovery_required` outcome for request-owned Artifact authority without a receipt. NanoCore passed 203 files and 2,005 tests with one file and three tests skipped; protocol passed 152 tests, App API schemas passed 68 tests, Core Client passed 26 tests, and OpenAPI passed 13 tests, together with the applicable typecheck, lint, build, repository, and whitespace gates. Stage 3 now owns S39 delivery, worker-seen projection, and active-turn handoff; version-owned Artifact Review, proposal apply, portability, Web, and acceptance remain with their later stages.
- 2026-07-18 — WP-4 Stage 3 exited with one exact S39 Context Package trace before each accepted Task or Goal backend launch, stock OpenShell materialization and verification, trace-derived worker-seen projections, and bounded Goal steering over the existing pending, ledger, Item, Turn, Goal, Task, Material, WIS, WMR, backend-session, and handoff owners. Delivery budget, replay, terminal lineage, restart projection, and contradictory-state failures are closed without a new state machine, settlement or recovery workflow, runner, harness, fork, or patch. Final verification passed NanoCore 207 files and 2,047 tests with one file and three tests skipped, protocol 152, config schema 129, App API schemas 70, Core Client 27, Skill interface 10, and OpenAPI 14, plus the applicable typecheck, lint, build, repository, lifecycle, and whitespace gates. Stage 4 is active.
- 2026-07-18 — WP-4 Stage 4 passed its implementation-entry contract review. Worker Artifact collection stays inside the stock OpenShell retained-session command and download boundary, uses an aggregate 16 MiB plus one sentinel-byte transfer ceiling, and rejects any contiguous exact injected-value byte substring before canonical writes. Restart with declarations but without the original ephemeral comparison set runs existing cleanup and returns `recovery_required`. Imported Review history remains non-authorizing; refinement or redo requires the exact currently enabled target Agent or returns `stale` without remapping. No new control family, durable owner, recovery state, transfer service, runner, harness, fork, or patch is authorized. The minimal failing-test slices are active.
- 2026-07-18 — WP-4 Stage 4 exited with bounded stock OpenShell Artifact collection, canonical Artifact/reference/version-keyed Review authority, one-transaction conflict-safe Material apply, versioned App API and Action Center projections, exact target-Agent refinement or redo through the existing S39 launch path, and one source-validated and target-revalidated S51 Material/Review/Context graph. Imported history remains non-authorizing, missing trace and contradictory lineage fail closed, and a focused re-export and re-import proves only this related subgraph. Final deletion-first review found one swapped trace-path defect, which now fails closed through the existing portable-file validator and one 73-test targeted slice; it found no deletable over-engineered mechanism. NanoCore had already passed 208 files with one skipped and 2,107 tests with three skipped; App API schemas passed 71 tests, Core Client 28, and Worker Shim 161, with the applicable typecheck, lint, build, generated OpenAPI, lifecycle, repository, and whitespace gates. Stage 5 is next.
- 2026-07-18 — WP-4 Stage 5 preflight found a direct sequencing conflict: the owning plan requires the rebuilt Web stack, S10 is accepted but not started, and this program expressly excludes the rebuild. Implementation stopped before adding behavior. The current Solid Web baseline was repaired only for Stage 4 Artifact and Core Client shape alignment and passes 125 tests, typecheck, lint, and build. Program sequencing now requires an explicit decision between deferring Stage 5 and Stage 6 to post-program S10 or authorizing a bounded temporary Solid projection.
- 2026-07-19 — WP-4 exited at the verified Stage 4 kernel boundary. Its accepted Stage 5 Web projection and Web-dependent Stage 6 closure are assigned to the post-program S10 rebuilt-stack work after G09; no temporary Solid projection or duplicate acceptance platform is authorized. WP-5 now enters only through its bounded G06 review preamble.
- 2026-07-19 — WP-5 entered after its bounded G06 preamble confirmed the inherited security and authority criteria, froze five implementation lines, removed the stale unbound-Vault re-export finding as `NO-ACTION`, and narrowed migration, CAS, runtime failure, Web, and L6 obligations to the S62 proportionality bar. Stage 0 documentation authority is active.
- 2026-07-19 — WP-5 Stage 0 exited after two independent contract reviews and one minimality review. It corrected the S46/S49 synchronization owner, S50 Git failure vocabulary, Vault scope/export boundary, S52-S54 identity/bootstrap wording, S56 Artifact operation language, S62 exact CAS and current Skill/CLI status, S63 policy/first-writer/migration/evidence/fallback/acceptance scope, and roadmap/Web sequencing. The two resolved documentation findings left the Backlog; no production behavior changed, and Stage 1 test-first boundary guards plus migration preflight are next.
- 2026-07-19 — WP-5 Stage 1 exited with one mechanical App/Core/Gateway Workspace-operation inventory, one nine-case read-only migration preflight, and exact red assertions for restrictive user foreign keys plus active owner membership. The preflight passes and the remaining failures name only their Stage 3/4 owners; no shared fixture platform, migration workflow, crash matrix, or runtime behavior was added. Stage 2 shared schemas are active.
- 2026-07-19 — WP-5/G06 exited at the accepted non-Web boundary. Owner-independent V2 storage and its stopped-process migration, sharing and one-way user-disable lifecycle, centralized fail-closed authorization, named attribution and current-effect checks, eligible-principal/Vault/portability projections, all fifteen Core Client operations, and the eleven bearer-reachable CLI/Skill operations passed their owning gates. The four session-only operations remain an explicit bounded projection compromise, S10 retains the rebuilt Web, and the deletion pass added no runner or recovery platform.
- 2026-07-19 — The G07 preamble reviewed the four original Drafts, narrowed and accepted S18 as an explicit existing-operation composition, and kept S19, recurring triggers, and Skill Catalog as non-authorizing Drafts. WP-6 now owns exact Knowledge correctness and one explicit human-reviewed loop; speculative trigger, auto-promotion, generic proposal workflow, reconstruction, and Harness machinery moved to named future owners. WP-5A and a retained real completed history are the remaining entry gates.
- 2026-07-19 — G07 Stage 0 exited docs-only after blocker-focused contract and deletion-first reviews plus repository document gates. It froze one proposal producer, one S61 retrieval owner, one S39 delivery owner, bounded `recovery_required`, and deletion of duplicate claim/worker proposal paths; WP-5A entered without starting G07 production work.
- 2026-07-19 — WP-5A exited after coherent non-Codex provider ownership, product-neutral dispatch evidence, reported cache quantities, contradictory-effect rejection, and fail-closed uncertain Git replay passed their package gates. The existing real-provider runner passed on disposable A1 through stock pi-ai and stock Ollama with an empty leak scan; its keyless-provider defect was reduced to a focused regression and a fixed non-secret bounded placeholder, with no new runner, harness, recovery workflow, fork, or patch. One retained real completed work history is now WP-6's sole entry gate.
- 2026-07-19 — WP-6 entered after the existing real Task story passed on disposable A1 through stock OpenShell `0.0.80` and Codex `0.144.1`. The retained completed Turn has one final assistant Item, strict-verifier-confirmed S39 bytes and digest, normal runtime evidence, and meaningful positive feedback; the task repository stayed clean and whole-Cell cleanup returned zero containers and sandboxes. Three bounded entry defects were reduced to focused regressions and direct fixes without a new runner or recovery owner. Stage 1 is active.

## Exit Condition

Every work package has met its exit gate, the Backlog is empty or every entry has an accepted owner and schedule, and the alignment audit record is `verified`. This record then closes with a final summary.
