---
type: change-plan
status: verified
---
# Dashboard And Search Visibility

## Intent Epoch 1

Source: engineer request and GitHub issue #54. Deliver one focused PR for private-thread visibility in `getWorkspaceDashboard`, `getThreadDashboard`, and `searchApp`, durable visibility and fail-closed cutover, followed by the three bundled CLI mappings. Test first, keep fixture credentials fake, honor hooks, push `feat/54-dashboard-search-visibility`, open against main with `Fixes #54.`, and stop without merging. Full sharing/handoff, subscriptions, export/import, unrelated leftovers and reopening #17 are excluded.

## Owners

- `docs/specs/20260909-thread_visibility_and_sharing.md`
- `docs/core/permissions.md`
- `docs/core/storage.md`
- `docs/specs/20260713-openkit_agent_skill_interface.md`
- `docs/specs/20260704-app_api_openapi_projection.md`

## Closeout Summary

The scoped implementation is locally verified. Public creation defaults to actor-owned private visibility; formal work uses shared inception and private Task/Goal admission fails without converting history. Canonical envelopes carry the registered visibility feature. Cutover uses owner-bound Quick Chat identity or formal Task/Goal inception, and ambiguous or damaged records fail closed. Dashboard/search audiences are checked before dependent discovery, including Artifact origins and embedded Artifact-reference Items. Counts are audience-filtered. Exactly the three requested CLI exclusions were removed and their existing client methods mapped.

Tests were committed before implementation, including observed failures for missing visibility, member isolation, feature emission, Goal cutover, private formal-work admission, immutable ownership, embedded private Artifact titles and ambiguous cutover chronology. Implementation commits include `0a3dcb0`, `199995a`, `541179a`, `52bab71`, and the final cutover correction `6430c64`; all used the tracked Lefthook hooks.

## Verification

- `pnpm --filter @openkit/protocol test`: 171 passed.
- `pnpm --filter @openkit/config-schema test`: 190 passed.
- `pnpm --filter @openkit/app-api-schemas test`: 142 passed.
- `pnpm --filter @openkit/core-client test`: 82 passed.
- `node --test tests/openkit-skill-interface.test.mjs`: 38 passed, including bundled transport denials and fake-token redaction.
- `pnpm --filter @openkit/nanocore exec vitest run src/dashboard-search-visibility.test.ts src/workspace-dashboard.test.ts src/thread-dashboard.test.ts src/search.test.ts src/storage/workspace-file-records-thread-envelope.test.ts src/lib/store.test.ts src/lib/store-reload.test.ts src/administration/administration-routes.test.ts src/openapi.test.ts src/thread-goal-summary.test.ts src/quick-chat.test.ts src/quick-chat-workspace.test.ts --reporter=tap-flat`: 168 passed on the final code.
- NanoCore and dependency builds, Core Client dependency typechecks and Web typecheck passed. Protocol JSON Schema, OpenAPI and CLI regeneration produced no diff after the generated artifacts were committed.
- Focused Biome and all commit hooks passed, retaining the pre-existing unused Vault-listing parameter warning. Specification lifecycle, documentation model/index, interface reachability, test governance, OpenAPI validation and diff whitespace checks passed.

## Review And Boundaries

Actual diffs, generated bytes and named execution output were inspected. The review found and corrected embedded private Artifact-reference titles and ambiguous predecessor chronology; the new regressions reproduced both before correction. No registered `.codex/agents/` capabilities exist in this worktree, so no delegated review was claimed. Independent human approval remains the PR gate.

The visibility specification is explicitly Partial. Full sharing/handoff, other publication surfaces, subscriptions, standalone Artifact delivery and export/import remain outside this issue's accepted scope. Ambiguous predecessor project history intentionally blocks startup for explicit classification; no implicit shared fallback or operator conversion workflow is added. Validation used isolated local fixtures, not a deployed A2 instance. No deployment or merge occurred. This checkpoint precedes the authorized branch push and PR creation against main; publication evidence belongs to the resulting PR.
