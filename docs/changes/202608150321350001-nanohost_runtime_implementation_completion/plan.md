---
type: change-plan
status: in-progress
date: 2026-08-15
branch: feat/r001-nanohost-noninterference
---
# NanoHost Runtime Implementation Completion

## Intent

Complete every unsettled NanoHost runtime predicate after the predecessor exhausted its coordination budget. This record starts with fresh matrices and identities; it transfers no predecessor assignment, verdict, lease, denominator, or budget.

## Authority

- [`AGENTS.md`](../../../AGENTS.md)
- [`docs/change-execution.md`](../../change-execution.md)
- [`docs/documentation-model.md`](../../documentation-model.md)
- [`docs/engineering-doctrine.md`](../../engineering-doctrine.md)
- [`docs/toolchain.md`](../../toolchain.md)
- [`docs/verification-instruments.md`](../../verification-instruments.md)
- [`docs/core/communication.md`](../../core/communication.md)
- [`docs/core/runtime-model.md`](../../core/runtime-model.md)
- [`docs/core/agent-session.md`](../../core/agent-session.md)
- [`docs/core/sandbox.md`](../../core/sandbox.md)
- [`docs/specs/20260529-test_strategy.md`](../../specs/20260529-test_strategy.md)
- [`docs/specs/20260802-nanohost_runtime_and_transport.md`](../../specs/20260802-nanohost_runtime_and_transport.md)
- [`docs/specs/20260801-nanohost_workspace_data_boundary.md`](../../specs/20260801-nanohost_workspace_data_boundary.md)
- [`docs/specs/20260703-worker_control_protocol.md`](../../specs/20260703-worker_control_protocol.md)
- [`docs/specs/20260704-agent_session_continuity.md`](../../specs/20260704-agent_session_continuity.md)
- [`docs/specs/20260716-codex_worker_adapter.md`](../../specs/20260716-codex_worker_adapter.md)
- [`docs/specs/20260716-opencode_worker_adapter.md`](../../specs/20260716-opencode_worker_adapter.md)
- [`docs/specs/20260716-pi_worker_adapter.md`](../../specs/20260716-pi_worker_adapter.md)
- [`docs/specs/20260715-openshell_disposable_cell_lifecycle.md`](../../specs/20260715-openshell_disposable_cell_lifecycle.md)
- [`docs/specs/20260629-worker_runtime_communication_model.md`](../../specs/20260629-worker_runtime_communication_model.md)

## Historical Evidence And Immutable Boundaries

- The superseded NanoHost Runtime Implementation program, record identifier 202608132249150001, closed at state revision 90. Its state and findings are evidence, never authority.
- Closed historical packages before WP-6 remain closed. Historical WP6-F1 remains `RETURN`, its `1/1` denominator remains consumed, and no later rule reinterprets it.
- The predecessor's H1-O owner change, strict response schema, route, access classification, OpenAPI source and generated projection, tests, and local guide remain uncommitted candidates. They are accepted only after NHC-1 passes.
- The predecessor's two host-manifest test files remain uncommitted test-author candidates. Superseded NHC-2T proved that their mixed manifest/aggregate test cannot be repartitioned while preserving a count of fifty-eight. NHC-2TP extracts projection ownership with the required `58→59` split; superseded NHC-2TM proves the remaining fixture runner is shared; NHC-2MS establishes that one shared owner and moves manifest/provision/assertion ownership while preserving fifty-nine; NHC-2A accepts manifest provisioning/assertion, superseded NHC-2B/NHC-2H retain finite lifecycle/shared-owner evidence, discarded NHC-2I proves the two payloads require different positive observations, NHC-2J accepts provision streaming, superseded NHC-2K leaves a correct assertion candidate behind an unsound marker, discarded NHC-2L exposes the aggregate-control count, and NHC-2C accepts the exact harness-contract plus command/document/identity remainder.
- All 57 historical WP-6 events and all predecessor events remain immutable. No event is relabeled, suppressed, copied into this state, or counted as a new identity.
- No file under `temp/` is deleted by this plan before NanoHost, Agent Function Model, and Execution Environment plans close.

## Scope

Execution is sequential: NHC-1 configured-target correctness; superseded NHC-2 broad candidate; superseded NHC-2R authority-only candidate; superseded NHC-2O prohibited repair identity; NHC-2G test-local projection authority plus verification owner; superseded NHC-2T one-unit repartition; NHC-2TP projection extraction; superseded NHC-2TM private-helper movement; NHC-2MS shared fixture-runner and manifest-test ownership; NHC-2A manifest provisioning/assertion; superseded NHC-2B credential-safe attempt lifecycle; superseded NHC-2H shared SSH-alias extraction; discarded NHC-2I two-payload matrix; NHC-2J provision-stream preservation; superseded NHC-2K assertion-stream candidate; discarded NHC-2L miscounted harness matrix; NHC-2C exact harness contract, command, preflight, documentation, and aggregate identity; discarded zero-cycle NHC-3 two-cycle wrapper; superseded integrated NHC-4; superseded NHC-4A host-identity/root candidate; NHC-4C finite shared sole-PATH command boundary; NHC-4B current dependency materialization and one real A1 Unit E rehearsal; NHC-5 Unit F F1-F4 and Aggregate; NHC-6 retired-path deletion; NHC-7 current projections; NHC-8 closeout. No later package opens before its dependency passes.

No remote host is contacted before NHC-2G, NHC-2TP, NHC-2MS, NHC-2A, NHC-2J, and NHC-2C pass. After those packages pass, the directly authorized NHC-4B builder supplies the lowercase non-secret alias `a1` to the verification-only remote-host boundary owned by `docs/verification-instruments.md`; host, user, key, host-key trust, token, cookie, and credential values remain outside repository artifacts. Credentials are generated per attempt and removed on every terminal path.

## Non-Goals

- No rerun of a historically closed package or reinterpretation of historical WP6-F1.
- No product-runtime or general-purpose remote-execution mechanism, repository-held credential, TLS-policy relaxation, compatibility path, new harness framework, or completeness claim. NHC-2G is explicitly allowed to establish only the test-local exception for the verification instrument's already-declared projection, align that projection, and establish the verification-only real-use host-access boundary.
- No edit under `docs/specs/superseded/`, `docs/specs/retired/`, or `docs/specs/rejected/`.
- No deletion under `temp/` until the three unfinished product plans close.
- No push, amend, or history rewrite. One local implementation checkpoint commit is authorized after frozen local verification and independent final audit; the final closeout commit remains gated on NHC-8.

## Risk

Tier 4 applies whenever a package touches authorization, credentials, remote effects, sandbox containment, deletion, or real-use acceptance. Such packages require direct human scrutiny and a different-family reviewer or verifier when available. Lower packages are classified by their actual surface; no package may lower a raised tier without engineer disposition.

## Queued-Package Necessity Recheck

| Package | Named present observation | Necessity decision | Current predecessor |
| --- | --- | --- | --- |
| NHC-4B | Running `node -e "const fs=require('fs'),s=require('./temp/state/202608150321350001-nanohost_runtime_implementation_completion.state.json'),e=s.events.filter(x=>x.workPackage==='NHC-4B'),n=(f)=>e.filter(f).length; console.log('revision='+s.revision+' slice_frozen='+n(x=>x.kind==='slice_frozen')+' status='+n(x=>x.kind==='status_changed')+' assignment='+n(x=>x.kind.startsWith('assignment_'))+' gate='+n(x=>x.kind==='gate_recorded')+' yield='+n(x=>x.kind==='yield_observed')+' evidenceRoot='+(fs.existsSync('temp/state/wp6/v3-real-dependency-evidence/attempt-1')?'present':'absent'))"` outputs exactly `revision=397 slice_frozen=4 status=0 assignment=0 gate=0 yield=0 evidenceRoot=absent`. | Still necessary because no real Unit E evidence archive exists; nothing replaces it. | NHC-4C `PASS` |
| NHC-5 | Running `node -e "const fs=require('fs'),p='temp/state/wp6/wp6-f-gate'; console.log('NHC-5 evidence-root='+(fs.existsSync(p)?'present':'absent'))"` outputs exactly `NHC-5 evidence-root=absent`; no current Unit F runner or recoverable F1-F4/Aggregate verdict archive exists, and the accepted NanoHost specification's current implementation projection states that the separate WP6 fault-acceptance matrix is incomplete. | Still necessary because no executable or retained evidence satisfies its F1-F4 and Aggregate predicates. | NHC-4B `PASS`, which produces the real Unit E predecessor evidence |
| NHC-6 | Running `rg -l 'OpenShellCellController' apps/nanocore/src/runtime/openshell-cell.ts` outputs exactly `apps/nanocore/src/runtime/openshell-cell.ts`, confirming the retired `OpenShellCellController` source remains; the accepted NanoHost specification requires that legacy source to be deleted after fault acceptance. | Still necessary because the retired implementation and tests still exist; nothing replaces the deletion package. | NHC-5 `PASS`, plus direct human strict-risk acceptance |
| NHC-7 | Running `sed -n '301p;313p' apps/nanocore/README.md` shows lines 301 and 313 still presenting Cell, SSH lifecycle, Gateway forwarding, and direct worker-control connectivity as current operation; `node scripts/generate-doc-index.mjs --check` separately reports `Documentation index is current.` | Still necessary for semantic projection alignment even though the generated index is currently green; nothing replaces it. | NHC-6 `PASS`, which produces the implementation truth the projections must describe |
| NHC-8 | Running `node -e "const fs=require('fs'),p=fs.readFileSync('docs/changes/202608150321350001-nanohost_runtime_implementation_completion/plan.md','utf8'),s=require('./temp/state/202608150321350001-nanohost_runtime_implementation_completion.state.json'); console.log('plan status='+p.match(/^status: (.+)$/m)[1]+' NHC-8 events='+s.events.filter(x=>x.workPackage==='NHC-8').length)"` outputs exactly `plan status=in-progress NHC-8 events=0`. | Still necessary because no closeout, accumulated-diff verification, or terminal verdict exists; nothing replaces it. | NHC-7 `PASS`, which produces the aligned closeout input |

The only live evidence question previously deferred despite a cheap authorized probe was whether NHC-4C still had its intended RED. Running `bash temp/state/wp6/v3-preseal/self-check.sh --host-identity` locally and offline exits `1` with `wp6-v3-self-check=RED predicate=HOST_IDENTITY detail=observation-log-command-root-path-observation-missing`. No remaining engineer question in this plan can be settled by another local read-only probe; direct human scrutiny remains where Tier-4 authorization, credentials, remote effects, cleanup, deletion, or irreversible effects require it.

## Work-Package Queue

### NHC-1 — Configured RuntimeTarget fail-closed correction

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `apps/nanocore/README.md`; `apps/nanocore/AGENTS.md`; `apps/nanocore/src/auth/README.md`
- **Scope:** add two finite regressions for stored-row identity mismatch and deployment mismatch, then fail closed before response serialization; preserve matching success, absent-target `404`, every existing server/storage/config/actor failure, the exact seven-field response, operation access, OpenAPI, and all redaction boundaries
- **Mode and permitted writes:** `implementation`; test-author lease exactly `apps/nanocore/src/auth/nanohost-transport-routes.test.ts`; builder lease exactly `apps/nanocore/src/auth/nanohost-transport-routes.ts`; all schema, OpenAPI, access, storage, migration, configuration, UI, client, diagnostics, and documentation files are immutable
- **Risk tier:** Tier 4 authorization-bound readiness read; no credential value, authorization rule, public field, route, storage, configuration, lifecycle, or external effect changes
- **Dependencies:** accepted NanoHost owner at `docs/specs/20260802-nanohost_runtime_and_transport.md`; current H1-O candidate; predecessor NH-FND-004; current baseline commit `539eee41a6bc72f7712f16860f1a0b863b0b5fa6`
- **Predicate:** a row stored under the configured lookup key returns the existing `404 nanohost_runtime_target_not_found` whenever either its `identityId` or `deploymentId` differs from current configuration; a fully matching current row alone may return the exact success object; no mismatch value is serialized
- **Oracle:** one table with exactly identity mismatch, deployment mismatch, matching success, and absent-target control; the existing non-server, missing-storage, missing-config, and wrong-actor table remains green; actual-diff exit scope includes the route test plus every currently modified H1-O schema, route, access, and OpenAPI test; the oracle is prior, finite, a `bounded oracle`, reproducible, and re-runnable, and contains no “name one” instruction. The named false-predicate failure is `configured RuntimeTarget mismatch accepted`: either named mismatch row returns anything other than the existing `404` or exposes mismatch data, while setup or collection failure does not count
- **Failure disposition:** one same-predicate correction may continue only on the two exact leases; a new error code, public field, storage/config owner, lifecycle, or authorization relaxation is scope expansion and returns; any credential/security defect blocks
- **Next owner:** independent entry reviewer, fresh test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** seam `GET /api/app/nanohost/runtime-target` configured-row validation; one execution unit; two files; approximately 20–45 changed lines; 24-event ceiling

### NHC-2 — Superseded broad host-manifest package

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/toolchain.md`; `docs/specs/20260529-test_strategy.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `tests/README.md`; `tests/AGENTS.md`; `docs/cookbooks/README.md`; `docs/cookbooks/AGENTS.md`
- **Scope:** independently adopt the two existing test candidates after removing their inadmissible Markdown source-text assertions, then implement the host manifest, idempotent provisioning, fail-closed assertion, teardown, authenticated-readiness bring-up, four root commands, root contribution guidance, cookbook, index, thirteen-file artifact identity, and retained-result convention; no Unit E write or real A1 cycle
- **Mode and permitted writes:** `implementation`; test-author lease `tests/host-manifest.test.mjs` and `tests/host-manifest-runtime.test.mjs`; builder lease `package.json`, `README.md`, `CONTRIBUTING.md`, `tests/support/host/README.md`, `tests/support/host/manifest.json`, `tests/support/host/provision.sh`, `tests/support/host/assert.sh`, `tests/support/host/nanohost-bring-up.sh`, `tests/support/host/teardown.sh`, `docs/cookbooks/README.md`, and new file `nanohost-real-use-host.md` under `docs/cookbooks/`
- **Risk tier:** Tier 4 because the instrument prepares A1 and starts credential-bearing NanoHost readiness; raw credentials, TLS policy, sandbox effects, Unit E/F effects, and destructive replacement are excluded
- **Dependencies:** NHC-1 `PASS`; lowercase `ssh a1`; complete frozen host facts below; `tests/support/host/` absent at baseline; predecessor temp matrix read-only
- **Predicate:** root scripts are exactly `host:provision`, `host:assert`, `host:nanohost:bring-up`, and `host:teardown`; manifest values equal the finite table below; Node projection follows the absent-target/source-identity rule; provision twice is idempotent; every scalar and command mismatch fails; bring-up observes only authenticated configured current-generation readiness through NHC-1 and always tears down attempt-local state; one manifest digest and one sorted path/content digest identify all thirteen tracked files; exactly four workflow documents expose the commands and retained-result path
- **Oracle:** after the test author removes the current Markdown source-text assertion, the two candidate tests decide the exact finite manifest, mismatch, Node, command, identity, and cleanup tables; an independent artifact reviewer decides the exact four-document projection table without an executable source-text assertion; actual-diff focused scope is recomputed at exit; both finite tables are prior `bounded oracle`s, reproducible and re-runnable, with no “name one” instruction. The named false-predicate failure is `host-manifest finite row or four-document projection mismatch`, identifying the exact row or path instead of treating setup or collection failure as RED
- **Failure disposition:** one exact-lease same-predicate correction; a second failure trips the breaker; any new input class, remote owner, credential retention, destructive host behavior, or Unit E reuse returns or blocks as appropriate
- **Next owner:** independent entry reviewer after NHC-1, fresh standing test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Terminal disposition:** the package is superseded. Its candidate passes every admitted finite artifact check, but the package predicate combined manifest identity, provisioning, assertion, bring-up, teardown, four root commands, four workflow projections, and thirteen-file identity. Successive reviews exposed different behavior classes, proving that the package-level predicate was not bounded. The engineer rejected another event-ceiling increase and directed decomposition; no further NHC-2 event, assignment, correction, or verdict is authorized.
- **Retained evidence:** final NHC-2 count `42`; manifest SHA-256 `9d5af17f70ea0df4c543a72935209891fee919e5023af522ec4b199160048aab`; thirteen-file candidate digest `a83fb0f580ed7f50a5e3cbc9e0b40d3cf8007a0caba63e26eb4e4ec7c2cefc82`; focused checks `58/58`; raw candidate diffstat `13 files changed, 871 insertions(+), 0 deletions(-)`. These are candidate evidence only and transfer no assignment, verdict, unit, denominator, or package acceptance.

#### Frozen A1 facts

| Fact | Value |
| --- | --- |
| `schemaVersion` | `1` |
| `architecture` | `aarch64` |
| `kernelRelease` | `6.17.0-1018-oracle` |
| `cgroupMode` | `unified-v2` |
| `initSystem` | `systemd` |
| `containerRuntime` | `docker` |
| `bash` | `/usr/bin/bash`; `GNU bash, version 5.2.21(1)-release (aarch64-unknown-linux-gnu)` |
| `curl` | `/usr/bin/curl`; `curl 8.5.0 (aarch64-unknown-linux-gnu) libcurl/8.5.0 OpenSSL/3.0.13 zlib/1.3 brotli/1.1.0 zstd/1.5.5 libidn2/2.3.7 libpsl/0.21.2 (+libidn2/2.3.7) libssh/0.10.6/openssl/zlib nghttp2/1.59.0 librtmp/2.3 OpenLDAP/2.6.10` |
| `docker` | `/usr/bin/docker`; `Docker version 29.6.1, build 8900f1d` |
| `node` | `/usr/bin/node`; `v24.18.0` after provision |
| `sha256sum` | `/usr/bin/sha256sum`; `sha256sum (GNU coreutils) 9.4` |
| `sudo` | `/usr/bin/sudo`; `Sudo version 1.9.15p5` |
| `systemctl` | `/usr/bin/systemctl`; `systemd 255 (255.4-1ubuntu8.16)` |
| `tar` | `/usr/bin/tar`; `tar (GNU tar) 1.35` |
| `timeout` | `/usr/bin/timeout`; `timeout (GNU coreutils) 9.4` |

Before provision, `/usr/bin/node` is absent. `$HOME/.local/share/mise/installs/node/24.18.0/bin/node` is executable, reports `v24.18.0`, and has SHA-256 `6bf69d0eda41a12030d5f28d958cd09ce323bc0c13f1ab4d8bb426933aa08812`. Provisioning may create only the absent `/usr/bin/node` symbolic projection when all three source facts match. A matching target is an idempotent no-op; any other target or source mismatch fails closed without replacement.

### NHC-2R — Real-use host access and manifest-instrument authority

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/documentation-model.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`
- **Scope:** resolve the activated `Real-Use Remote Execution Has No Owner` debt before another script or A1 attempt: define the one verification-only SSH-target boundary, the asserted-manifest identity output consumed by retained evidence, the shared-comparator obligation, the existing `:preflight` attachment, and the finite strong-risk checklist; replace the remote-execution debt with its accepted owner contract and remove the false “not implemented” manifest debt while retaining the existing rule that the manifest remains unauthoritative until a real bring-up passes; no implementation, test, package command, cookbook, product/API, A1, Docker, credential, or terminal-spec write
- **Mode and permitted writes:** `implementation`; builder lease exactly `docs/verification-instruments.md`; every other file is immutable
- **Risk tier:** Tier 4 because this owner decides remote access, SSH identity location, host-key enforcement, credential-safe use, and evidence identity; product authorization, TLS policy, raw credential values, and external effects are untouched
- **Dependencies:** NHC-1 `PASS`; NHC-2 `superseded`; engineer findings A–F received in `findings.md`; current `docs/toolchain.md` delegation to this owner remains read-only
- **Predicate:** the owner states that real-host commands require one explicit non-secret SSH alias supplied at invocation, resolved only by the operator's local SSH configuration with ordinary host-key verification and no repository-held host, user, key, or trust override; only a directly authorized Tier-4 real-use builder may invoke it; the assertion half emits exactly `manifestDigest=<lowercase 64-hex SHA-256 of manifest.json>` for retained evidence; fixture and remote modes collect facts separately but execute one shared comparison owner; the existing `test:e2e:nano-restart:preflight` slot invokes the assertion half; the strong-risk matrix is closed over the two URL/credential consumers and named terminal paths; `Real-Use Remote Execution Has No Owner` is replaced by this normative boundary, while `The Real-Use Host Manifest Is Decided And Not Implemented` is removed because tracked realization exists and the enduring main-section real-bring-up rule still prevents premature authority
- **Oracle:** independent finite owner review decides exactly eight named decisions: SSH alias source and syntax; identity/key location; host-key behavior; allowed caller; asserted-manifest identity output; shared comparator ownership; existing preflight attachment; and closed strong-risk matrix ownership. It also decides exactly two debt dispositions: remote-execution debt replaced by the normative boundary, and manifest-not-implemented debt removed without removing the real-bring-up prerequisite. No executable source-text assertion or “name one” instruction; the review oracle is prior, a `bounded oracle`, reproducible, and re-runnable. The named false-predicate failure is `verification-only owner decision missing or contradictory`, identifying the exact one of eight decisions or two debt dispositions; an unreadable artifact is not that failure
- **Standing admission:** proof layer `L0 finite authority-row inspection plus independent Tier-4 owner review`; artifact type `governing-authority Markdown amendment`; exact governing rule set `Execution Environment / The Real-Use Host Manifest`, a new normative verification-only remote-host subsection, and both current `Known Debt` entries; test-author standing admission must enumerate all eight decisions and two debt dispositions before any builder opens
- **Failure disposition:** any unresolved remote-access identity, alternate SSH mechanism, trust override, credential location, assertion-output ambiguity, second preflight, or open-ended review instruction blocks; implementation belongs only to later packages after this owner passes
- **Next owner:** independent entry reviewer, fresh standing test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one authority-only logical slice; one execution unit; one builder file; approximately 30–75 changed lines; 24-event ceiling

- **Terminal disposition:** superseded after artifact review proved that the exact one-file lease omitted `tests/verification-instruments-contract.test.mjs`, which `docs/verification-instruments.md` declares as its executable projection. The authority candidate satisfies all eight decisions and two debt dispositions, but the unchanged projection fails `14/15` because it still requires both removed debt headings. No further NHC-2R event, assignment, correction, or verdict is authorized.
- **Retained evidence:** the unaccepted authority candidate changes only `docs/verification-instruments.md` by `15 insertions / 7 deletions`; documentation model, spec lifecycle, state validation, and diff-check pass. The candidate and failing focused projection transfer no assignment, verdict, unit, denominator, or package acceptance.

### NHC-2O — Verification-instrument owner and executable projection

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/documentation-model.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `tests/README.md`; `tests/AGENTS.md`
- **Scope:** adopt the existing unaccepted authority candidate only together with the one executable projection that the owner already declares: define the verification-only SSH-target boundary, asserted-manifest identity output, shared-comparator obligation, existing preflight attachment, finite strong-risk matrix, and both debt dispositions, then align the existing contract test to those accepted semantics; no host script, manifest, package command, cookbook, product/API, A1, Docker, credential, terminal-spec, new test file, new helper, or new framework
- **Mode and permitted writes:** `implementation`; test-author lease exactly existing `tests/verification-instruments-contract.test.mjs`; builder lease exactly `docs/verification-instruments.md`; every other file is immutable. The current authority diff and failing `14/15` projection are unaccepted inputs, not transferred NHC-2R artifacts.
- **Risk tier:** Tier 4 because the owner decides remote access, SSH identity location, host-key enforcement, credential-safe use, and evidence identity; product authorization, TLS policy, raw credential values, and external effects are untouched
- **Dependencies:** NHC-1 `PASS`; NHC-2 and NHC-2R `superseded`; engineer findings A–F plus NHC-FND-008 received; current `docs/toolchain.md` delegation remains read-only
- **Predicate:** the owner satisfies exactly the eight decisions and two debt dispositions frozen in NHC-2R; its existing executable projection rejects the old two-debt contract and accepts the normative verification-only access boundary, exact asserted identity, shared comparator, existing preflight, closed strong-risk matrix, and `Known Debt: None`; focused projection is `15/15`; all owner prose and test expectations have one current meaning; no additional input class or source-text assertion is created
- **Oracle:** the existing failing focused projection plus one finite ten-row owner/projection comparison decide the exact eight decisions and two debt dispositions. The test-author changes only the obsolete debt expectation necessary to project the accepted owner semantics; independent reviewer and verifier decide semantic alignment. No wildcard, open-ended search, new source-text category, or “name one” instruction; the oracle is prior, a `bounded oracle`, reproducible, and re-runnable
- **Failure disposition:** one bounded correction on the exact two-file matrix; any third artifact, new behavior class, remote mechanism, owner ambiguity, trust relaxation, A1 effect, or executable-projection conflict stops the package and records the observation without widening it
- **Next owner:** independent entry reviewer, fresh test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one owner-plus-existing-projection logical slice materially different from the incomplete NHC-2R lease; one execution unit; two files; approximately 20–55 changed lines; 20-event ceiling

- **Current entry disposition:** blocked before any producer opens. Independent entry review found that adding only the omitted projection lease is a same-predicate NHC-2R subdivision, not a fresh logical slice or budget, and that changing the projection conflicts with `tests/AGENTS.md`, which permits source-text assertions only for `agents-root-contract.test.mjs`. NHC-2R fixed denominator `1`; opening NHC-2O for the same predicate made the cumulative numerator `2`, so planning drift declares plan failure. The current NHC-2O freeze authorizes nothing.
- **Escalation recommendation:** correct the current plan and refreeze only unopened remainder: authorize one exact `tests/AGENTS.md` exemption for `tests/verification-instruments-contract.test.mjs`, then freeze one materially different three-file governance matrix containing only that local rule, its named projection, and `docs/verification-instruments.md`. Transfer no NHC-2O identity, unit, denominator, event budget, or verdict.
- **Predicted observable:** after acceptance, the local guide names exactly one additional owner-declared projection exemption; one independently reviewed three-file freeze replaces the invalid NHC-2O boundary; the focused contract projection reaches `15/15`; and no host script, product, A1, Docker, credential, new test file, helper, or behavior changes. Without acceptance, no producer opens and every current candidate remains unaccepted.
- **Evidence that changes the recommendation:** an accepted authority proving the existing projection is already exempt, or a bounded non-source-text correction that preserves rather than weakens or deletes its failing check, removes the need for the local-guide amendment. `frameworkHypothesis: none`: omitted lease inventory and the prohibited repair identity are planning defects exposed by the framework, not framework obstruction.

- **Terminal disposition:** superseded after the independent escalation verifier passed and the standing engineer authorization selected the recommended current-plan correction. NHC-2O remains an invalid same-predicate repair identity; no identity, unit, denominator, event budget, or verdict transfers to NHC-2G, and no further NHC-2O event is authorized.

### NHC-2G — Test-local projection authority and verification owner

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/documentation-model.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `tests/README.md`; `tests/AGENTS.md`; standing engineer authorization after NHC-2O escalation verifier `PASS`
- **Scope:** amend the test-local execution rule with one exact exemption for the already-declared `tests/verification-instruments-contract.test.mjs` projection, align only that existing projection to the accepted verification-instrument semantics, and adopt the existing unaccepted owner candidate; no additional test exemption, test file, helper, input class, host script, manifest, package command, cookbook, product/API, A1, Docker, credential, terminal spec, or framework
- **Mode and permitted writes:** `implementation`; test-author lease exactly existing `tests/verification-instruments-contract.test.mjs`; builder lease exactly `tests/AGENTS.md` and `docs/verification-instruments.md`; every other file is immutable. The current authority diff and focused `14/15` failure are unaccepted inputs, not transferred NHC-2R/O artifacts.
- **Risk tier:** Tier 4 because the owner decides remote access, SSH identity location, host-key enforcement, credential-safe use, and evidence identity, while the local-rule change governs its executable projection; product authorization, TLS policy, raw credential values, external effects, and A1 remain untouched
- **Dependencies:** NHC-1 `PASS`; NHC-2, NHC-2R, and NHC-2O `superseded`; NHC-FND-002 through NHC-FND-009 received; NHC-2O escalation verifier `PASS`; standing engineer authorization selects the exact three-file recommendation
- **Predicate:** `tests/AGENTS.md` permits exactly one additional source-text assertion file, `tests/verification-instruments-contract.test.mjs`, only because `docs/verification-instruments.md` explicitly declares it as its executable projection and the test holds no authority; the owner satisfies exactly the eight remote-host/manifest decisions and two debt dispositions; the existing projection rejects the superseded two-debt contract and projects the normative access boundary, asserted identity, shared comparator, existing preflight, closed strong-risk matrix, and `Known Debt: None`; focused projection is `15/15`; all three files have one meaning
- **Oracle:** the current focused `14/15` failure plus one finite eleven-row comparison decide exactly one local-rule exception, eight owner decisions, and two debt dispositions. The test-author changes only the obsolete projection expectation; independent reviewer and verifier decide three-file semantic alignment. No wildcard, additional source-text category, open-ended search, or “name one” instruction; the oracle is prior, a `bounded oracle`, reproducible, and re-runnable
- **Failure disposition:** one bounded correction on the exact three-file matrix; any fourth artifact, second exemption, new behavior class, remote mechanism, owner ambiguity, trust relaxation, A1 effect, or projection conflict stops without scope expansion
- **Next owner:** independent entry reviewer, fresh test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one governing local-rule/projection/owner logical slice materially different from NHC-2R/O; one execution unit; three files; approximately 22–60 changed lines; 20-event ceiling

- **Terminal disposition:** `PASS` at exactly `20/20` events. Independent artifact review and unconditional verification found exactly two test-local source-text exemptions, the five named dependency seams, the finite `1 + 8 + 2` semantic matrix, focused projection `15/15`, state fidelity, and all eight coherence propositions intact. The observed yield is `3 files changed, 51 insertions(+), 19 deletions(-)`. No later NHC-2G event is authorized.

### NHC-2T — One-time host-test oracle repartition

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md` after NHC-2G; `docs/specs/20260529-test_strategy.md`; `tests/README.md`; `tests/AGENTS.md`
- **Scope:** mechanically repartition the two broad unaccepted host-test candidates into three primary regression owners before any behavior correction: manifest/provision/assertion, attempt lifecycle, and command/document/aggregate identity; preserve every current assertion and test case except changing the aggregate inventory from thirteen to fourteen paths because the projection test becomes a third tracked file. The four workflow projection inputs are exactly `README.md`, `CONTRIBUTING.md`, `tests/support/host/README.md`, and `docs/cookbooks/nanohost-real-use-host.md`, and they are identity inputs only: no Markdown semantics or source text is asserted. `docs/cookbooks/README.md` remains only the fifth Markdown member of the fourteen-path aggregate identity. No production, script, manifest, command, documentation, owner, A1, Docker, credential, or expected behavior change
- **Mode and permitted writes:** `implementation`; producer lease exactly existing `tests/host-manifest.test.mjs`, existing `tests/host-manifest-runtime.test.mjs`, and new `tests/host-manifest-projection.test.mjs`; every other file is immutable; the test-author substitutes for builder because the acceptance artifact itself is the only implementation, recorded with `role_substituted`
- **Risk tier:** Tier 3 cross-package verification-artifact ownership change; Tier-4 credential and remote-effect behavior is not changed or accepted
- **Dependencies:** NHC-2G `PASS`; NHC-2, NHC-2R, and NHC-2O `superseded`; current two broad files remain unaccepted inputs
- **Predicate:** `tests/host-manifest.test.mjs` owns only manifest vocabulary, provisioning, assertion, and asserted identity; `tests/host-manifest-runtime.test.mjs` owns only origin, readiness, cleanup, bring-up, and teardown; `tests/host-manifest-projection.test.mjs` owns only four root commands, the four exact identity-only workflow paths named above, the fourteen-path inventory including `docs/cookbooks/README.md`, manifest digest, and sorted path/content identity; every prior assertion appears exactly once, no primary invariant is duplicated, and the pre-repartition fifty-eight tests remain fifty-eight and green before NHC-2A/B intentionally release new REDs
- **Oracle:** one finite test-name/assertion inventory with exactly three destination groups, direct before/after test-count equality, unique primary-invariant mapping, and scoped diff showing moves plus the fourteen-path identity update only; no semantic source-text test and no “name one” instruction; the oracle is prior, a `bounded oracle`, reproducible, and re-runnable. The named false-predicate failure is `repartition inventory not bijective`, identifying a lost or duplicated assertion, wrong owner, wrong path identity, or changed test count rather than a setup or collection failure
- **Failure disposition:** any changed expectation, lost/duplicated assertion, fourth test owner, production need, or behavior finding stops repartition and routes to NHC-2A/B/C as appropriate
- **Next owner:** independent entry reviewer after NHC-2G, fresh test-author producer under recorded builder substitution, independent reviewer, verifier
- **Slice:** one test-artifact logical slice; one execution unit; three exact test files; approximately 80–180 moved/changed lines; 18-event ceiling

- **Current disposition:** stopped before any write. The current `58/58` candidate contains one top-level test that jointly owns manifest vocabulary and aggregate identity. Distinct manifest and projection owners require that mixed test to become two tests, so the frozen requirements “preserve every current test case,” “three distinct owners,” and “remain exactly fifty-eight tests” cannot all hold.
- **Escalation recommendation:** discard this one-unit decomposition and refreeze only unopened remainder as two materially distinct packages. NHC-2TP extracts command and aggregate-identity projection into the new projection file and splits the one mixed case, producing exactly `59/59`; NHC-2TM then moves only manifest/provision/assertion tests and their private helpers from runtime to manifest while preserving `59/59`. Transfer no NHC-2T identity, unit, denominator, budget, or verdict.
- **Predicted observable:** NHC-2TP changes only the three test files, creates exactly one new top-level test by separating the mixed owner, moves the root-command test, preserves every assertion once, and reaches `59/59`. NHC-2TM subsequently changes only the manifest and runtime test files, moves existing blocks/helpers without changing an assertion, and remains `59/59`.
- **Evidence that changes the recommendation:** a mechanical inventory proving the mixed test can give both owners separate direct entry points while remaining one test case, without module side effects, a cross-file helper, assertion loss, or owner hiding, would remove the split. Current source and the producer's pre-write inventory show the opposite. `frameworkHypothesis: none`: the finite oracle exposed an internally inconsistent package boundary.

- **Terminal disposition:** discarded at `17/18` events after independent escalation verification `PASS`. No leased artifact was written. The engineer's standing direction to split an unbounded or internally inconsistent package selected the exact NHC-2TP then NHC-2TM recommendation; no NHC-2T identity, unit, denominator, budget, or verdict transfers, and no later NHC-2T event is authorized.

### NHC-2TP — Projection-test extraction

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `tests/README.md`; `tests/AGENTS.md`
- **Scope:** extract only command and aggregate-identity projection ownership from the current two unaccepted test files: split the mixed manifest-vocabulary/aggregate-identity case into two direct owners, move the existing root-command case to the new projection file, update the aggregate inventory from thirteen to fourteen paths, and preserve every assertion once; no provision/assertion/runtime test movement, expectation change, source-text assertion, product, script, manifest, package, documentation, A1, Docker, credential, or remote effect
- **Mode and permitted writes:** `implementation`; producer lease exactly existing `tests/host-manifest.test.mjs`, existing `tests/host-manifest-runtime.test.mjs`, and new `tests/host-manifest-projection.test.mjs`; every other file is immutable; the test-author substitutes for builder because the only implementation artifacts are repository tests, recorded with `role_substituted`
- **Risk tier:** Tier 3 cross-package verification-artifact ownership change; every Tier-4 credential and remote-effect behavior remains unaccepted and unchanged
- **Dependencies:** NHC-2G `PASS`; NHC-2T discarded with zero artifact write and escalation verifier `PASS`; current two broad files remain unaccepted inputs
- **Predicate:** `tests/host-manifest.test.mjs` retains the exact manifest-vocabulary assertion as one direct test; `tests/host-manifest-projection.test.mjs` contains the separated aggregate identity test plus the moved root-command test; `tests/host-manifest-runtime.test.mjs` no longer contains the root-command test; the four workflow paths are identity-only, `docs/cookbooks/README.md` remains aggregate-only, the inventory names exactly fourteen paths including the new projection file, every prior assertion appears once, and the mechanical total changes exactly from `58/58` to `59/59`
- **Oracle:** one finite before/after inventory decides exactly one mixed-case split, one root-command move, fourteen named paths, every existing assertion once, no Markdown semantics or source-text assertion, and test count `58→59`; named Markdown files are read only as opaque aggregate-identity bytes. The oracle is prior, a `bounded oracle`, reproducible, re-runnable, and contains no “name one” instruction. The named false-predicate failure is `projection extraction inventory mismatch`, identifying the split, move, assertion, path, or `58→59` count that differs; setup or collection failure does not count
- **Failure disposition:** any other test split, assertion or expectation change, helper shared across files, Markdown semantic or source-text assertion, fourth test file, product need, or behavior finding stops and is not absorbed
- **Next owner:** independent entry reviewer, fresh test-author producer under recorded builder substitution, independent reviewer, verifier
- **Slice:** one projection-extraction logical slice; one execution unit; three exact test files; approximately 70–140 moved or changed lines; 18-event ceiling

- **Terminal disposition:** `PASS` at `16/18` events. Exactly one mixed case split, one root-command test moved, every original assertion remains once except the authorized `13→14` inventory expectation, and focused execution changed `58/58→59/59`. Final physical size is `512→523` lines, net `+11`; no Git diffstat or pre-write hash is claimed because the predecessor candidates were untracked. No later NHC-2TP event is authorized.

### NHC-2TM — Manifest-test ownership movement

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `tests/README.md`; `tests/AGENTS.md`
- **Scope:** after projection extraction passes, move only the existing manifest provisioning/assertion and Node-provisioning test blocks with their private constants, imports, and helpers from the runtime test into the manifest test; no test split, assertion, expectation, projection test, product, script, manifest, package, documentation, A1, Docker, credential, or remote-effect change
- **Mode and permitted writes:** `implementation`; producer lease exactly existing `tests/host-manifest.test.mjs` and `tests/host-manifest-runtime.test.mjs`; `tests/host-manifest-projection.test.mjs` is read-only; every other file is immutable; the test-author substitutes for builder because the only implementation artifacts are repository tests, recorded with `role_substituted`
- **Risk tier:** Tier 3 cross-package verification-artifact ownership change; every Tier-4 credential and remote-effect behavior remains unaccepted and unchanged
- **Dependencies:** NHC-2TP `PASS`; its exact `59/59` three-file artifact; NHC-2T transfers no identity, unit, denominator, budget, or verdict
- **Predicate:** the manifest test owns the unchanged manifest-vocabulary, fake-remote provision-twice and twenty-four assertion-mismatch cases, and four Node-provision mismatch cases; the runtime test owns only readiness, origin, cleanup, bring-up, and teardown; the projection test remains byte-identical; exactly the two existing parent blocks and their private helpers move, every assertion remains once, and the total remains exactly `59/59`
- **Oracle:** one finite before/after inventory decides the two named parent blocks, their twenty-eight nested cases, their private helper/import closure, zero changed assertions, byte-identical projection test, and count `59→59`; the oracle is prior, a `bounded oracle`, reproducible, re-runnable, and contains no “name one” instruction. The named false-predicate failure is `manifest ownership move changed behavior or count`, identifying the block, helper, assertion, projection bytes, or `59→59` count that differs; setup or collection failure does not count
- **Failure disposition:** any test split, changed assertion/expectation, projection edit, cross-file helper, product need, or behavior finding stops and is not absorbed
- **Next owner:** independent entry reviewer after NHC-2TP, fresh test-author producer under recorded builder substitution, independent reviewer, verifier
- **Slice:** one manifest-test ownership logical slice; one execution unit; two exact write files plus one read-only projection file; approximately 180–280 moved or changed lines; 18-event ceiling

- **Current disposition:** stopped before any write. `runHostScript` and `requireSuccess` serve both the two manifest/provision/assertion blocks to be moved and the readiness block that must remain in runtime, so the frozen “private helper closure” does not exist. Continuing would require prohibited duplication, a test-file-to-test-file import, or an unowned helper.
- **Escalation recommendation:** discard NHC-2TM and refreeze one materially different NHC-2MS package that establishes a demonstrated shared fixture-runner owner under `tests/support/host/`, aligns its local README, moves the two manifest/provision/assertion blocks to the manifest test, keeps runtime readiness on the shared runner, and updates the projection aggregate from fourteen to fifteen paths. Transfer no NHC-2TM identity, unit, denominator, budget, or verdict.
- **Predicted observable:** exactly `runHostScript`, `requireSuccess`, and their frozen Node-source fixture constant move to `tests/support/host/fixture-runner.mjs`; both manifest and runtime tests import that existing shared behavior; exactly the two parent blocks and twenty-eight nested cases move to manifest; projection changes only its inventory from fourteen to fifteen; the local README names the fixture runner; all assertions remain once and focused execution stays `59/59`.
- **Evidence that changes the recommendation:** proof that the retained readiness test does not call either helper, or that an existing repository helper already owns the same invocation behavior without a new file or responsibility, would remove the shared-owner package. Current source and entry review show both consumers and no existing owner. `frameworkHypothesis: none`: the finite entry inventory exposed another false private-closure assumption before writes.

- **Terminal disposition:** discarded at `12/18` events after independent escalation verification `PASS`. All three test hashes remain exactly NHC-2TP's terminal hashes and focused execution remains `59/59`; no NHC-2TM artifact was written. The standing engineer split disposition selected NHC-2MS, and no NHC-2TM identity, unit, denominator, budget, or verdict transfers. No later NHC-2TM event is authorized.

### NHC-2MS — Shared fixture runner and manifest-test ownership

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `tests/README.md`; `tests/AGENTS.md`; `tests/support/host/README.md`
- **Scope:** establish one demonstrated shared fixture-runner owner for the invocation behavior already used by both manifest/provision/assertion tests and retained runtime readiness, align its local guide, move the two existing manifest/provision/assertion parent blocks and their remaining private helpers from runtime to manifest, and update aggregate identity from fourteen to fifteen paths; no test split, assertion, expectation, host-script behavior, product, manifest, package, workflow documentation, A1, Docker, credential, or remote effect
- **Mode and permitted writes:** `implementation`; standing test-author admission is read-only over the finite ownership/move matrix; builder lease exactly `tests/support/host/fixture-runner.mjs`, `tests/support/host/README.md`, `tests/host-manifest.test.mjs`, `tests/host-manifest-runtime.test.mjs`, and `tests/host-manifest-projection.test.mjs`; every other file is immutable
- **Risk tier:** Tier 3 shared repository test-support ownership and cross-file test-artifact movement; all Tier-4 credential and remote-effect behavior remains unaccepted and unchanged
- **Dependencies:** NHC-2TP `PASS`; NHC-2TM discarded with zero artifact write and escalation verifier `PASS`; NHC-2T/TM transfer no identity, unit, denominator, budget, or verdict
- **Predicate:** exactly `runHostScript`, `requireSuccess`, and `expectedNodeSource` move from runtime to `tests/support/host/fixture-runner.mjs`; both manifest and runtime tests import that shared owner; exactly the fake-remote provision/assert parent with twenty-four nested cases and the Node-provision parent with four nested cases move unchanged to manifest with only their remaining private imports/helpers; runtime retains readiness/origin/cleanup/bring-up/teardown; the local README names the fixture runner's two demonstrated consumers and non-product scope; projection changes only its inventory from fourteen to fifteen by adding the runner; every assertion remains once and focused execution remains exactly `59/59`
- **Oracle:** one finite before/after inventory decides the three shared exports, two demonstrated imports, two named parent blocks and twenty-eight nested cases, remaining private helper/import closure, one local-guide file-map entry, projection `14→15` only, exact assertion uniqueness, and count `59→59`; the projection file's prior assertions remain byte-identical except the authorized path/count identity update. The oracle is prior, a `bounded oracle`, reproducible, re-runnable, and contains no “name one” instruction. The named false-predicate failure is `shared fixture-runner ownership mismatch`, identifying the export, consumer, block, assertion, guide row, projection, or test count that differs; setup or collection failure does not count
- **Failure disposition:** any fourth shared export, third consumer, changed assertion/expectation, new test case, host-script/product need, additional guide, or behavior finding stops and is not absorbed
- **Next owner:** independent entry reviewer, fresh standing test author, fresh exact-lease builder, independent reviewer, verifier
- **Slice:** one shared-test-support ownership logical slice; one execution unit; five exact files; approximately 210–340 changed lines; 18-event ceiling

- **Terminal disposition:** `PASS` at `16/18` events. The fixture runner has exactly three documented exports and two consumers, the two parent tests plus twenty-eight nested cases moved without count change, runtime retained its exact lifecycle set, the README owns the non-product boundary, projection changed only `14→15`, and focused execution remains `59/59`. Current five-file physical size is 572 lines; no Git baseline diffstat is claimed for the previously untracked candidates. No later NHC-2MS event is authorized.

### NHC-2A — Manifest provisioning, shared assertion, and asserted identity

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md` after NHC-2G; `docs/toolchain.md`; `docs/specs/20260529-test_strategy.md`; `tests/README.md`; `tests/AGENTS.md`
- **Scope:** repair and adopt only manifest provision/assert correctness and identity: both halves consume one explicit invocation-supplied SSH alias, fixture and remote assertion modes collect observations separately but execute the existing shared fact comparator, fixture-root command stubs feed that real path, assertion success emits the computed asserted-manifest identity, and the local guide projects those boundaries. The declarative manifest, absent-target Node projection, and idempotent `provision_node` behavior are accepted controls because the engineer's mutation audit confirms that shared path is already covered; no NanoHost service, NanoCore request, URL, credential, bring-up, teardown, root command/preflight, workflow documentation, aggregate identity, or A1 call
- **Mode and permitted writes:** `implementation`; test-author lease exactly `tests/host-manifest.test.mjs`; builder lease exactly `tests/support/host/provision.sh`, `tests/support/host/assert.sh`, and `tests/support/host/README.md`; read-only controls are exactly `tests/support/host/manifest.json` and `tests/support/host/fixture-runner.mjs`; every other file is immutable
- **Risk tier:** Tier 4 because provisioning may create the admitted absent `/usr/bin/node` symlink, both halves cross the verification-only SSH boundary, and assertion supplies real-use environment identity; credentials, product state, service effects, destructive replacement, and real A1 execution are excluded
- **Dependencies:** NHC-2MS `PASS`; exact frozen A1 fact table above; current four-file candidate is unaccepted; engineer mutation evidence shows `assert_remote` can accept every observed fact while all fifty-eight pre-repartition candidate checks remain green
- **Predicate:** the manifest has exactly six scalar facts and nine command path/version pairs; both provision and assertion require exactly one invocation alias and pass it as one SSH target with no repository default, hardcoded target, second target, option, or shell evaluation. The alias parser is the owner's exact `[a-z][a-z0-9-]{0,62}` grammar; executable checks admit only the fixed boundary rows `a`, `a1`, `a-b`, and sixty-three lowercase characters as accepted, and absent, second argument, `-x`, uppercase, underscore, dot, leading digit, and sixty-four characters as rejected before the SSH stub observes contact. Fixture mode obtains observations from fixture-root stubs and remote mode obtains host observations, but both execute one shared fact-comparison owner instead of fixture bytewise `cmp` or an SSH-stub exit shortcut; with manifest bytes unchanged, each of the six scalar and eighteen command-field observation mismatches produces a deliberate failure through that shared comparator. The existing four Node source/target mismatches and provision-twice control remain green through semantically unchanged `provision_node`; successful assertion itself computes and emits exactly `manifestDigest=<lowercase 64-hex SHA-256 of manifest.json>`, and the local README states the alias, identity-output, and shared-path boundaries
- **Oracle:** test first replaces the false bytewise oracle with one finite table that keeps manifest bytes unchanged and mutates exactly twenty-four fixture-root observations, requiring each to fail through the shared path; it retains four Node mismatch cases and provision-twice control, checks only the four accepted and eight rejected alias boundary rows named above for both provision and assertion, and requires the exact identity emitted by `assert.sh`. Independent reviewer and verifier each run the same one-time temporary accept-all `equal` mutation and require the focused suite to fail, without committing a source-text assertion. The closed tables and one mutation control are prior, `bounded oracle`s, reproducible, re-runnable, and contain no “name one” instruction
- **Failure disposition:** one bounded correction inside the exact three-file builder lease; any new host fact, alias class outside the frozen rows, product/service behavior, URL, credential, alternate SSH mechanism, destructive replacement, A1 effect, root command/preflight, workflow document, or aggregate identity is scope expansion and stops this package
- **Next owner:** independent entry reviewer after NHC-2MS, fresh test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one logical slice materially narrower than NHC-2; one execution unit; one test file, three builder files, and two read-only controls; approximately 90–190 changed lines; 24-event ceiling

- **Terminal disposition:** `PASS` at `23/24` events. Normal focused execution is `56/56`; a scratch-only accept-all comparator mutation makes all twenty-four observation mismatches fail; both scripts pass the exact four-accepted/eight-rejected alias tables with zero rejected-row contact; assertion emits the exact manifest digest; Node controls and provision-twice remain green; hashes and all eight coherence propositions pass. NHC-2A contacted neither A1 nor Docker, and no later NHC-2A event is authorized.

### NHC-2B — Credential-safe NanoHost attempt lifecycle

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md` after NHC-2G; `docs/specs/20260529-test_strategy.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `tests/README.md`; `tests/AGENTS.md`
- **Scope:** repair and adopt only the NanoHost attempt lifecycle: explicit SSH alias consumption, pre-effect input validation, accepted asserted-manifest identity, service start, authenticated configured current-generation readiness, terminal cleanup, direct teardown, and decommission; no manifest fact/comparator, provisioning mutation, root command/preflight, documentation projection, aggregate identity, Unit E/F action, or real A1 call
- **Mode and permitted writes:** `implementation`; test-author lease exactly `tests/host-manifest-runtime.test.mjs`; builder lease exactly `tests/support/host/nanohost-bring-up.sh` and `tests/support/host/teardown.sh`; `tests/support/host/assert.sh` is a read-only dependency; every other file is immutable
- **Risk tier:** Tier 4 credential-bearing remote-effect instrument; raw credential values, TLS relaxation, new URL class, repository-held SSH identity, credential retention, Unit E/F effects, and real A1 execution are excluded
- **Dependencies:** NHC-2A `PASS`; NHC-1 configured RuntimeTarget observation `PASS`; the repartitioned runtime test and current two-script candidate are unaccepted
- **Predicate:** both URL/credential consumers are exactly `nanohost-bring-up.sh` and `teardown.sh`; both consume exactly one explicit SSH alias using the already-admitted `[a-z][a-z0-9-]{0,62}` grammar, with no default, hardcoded target, second argument, option, or shell evaluation. For each consumer the alias boundary reuses exactly the NHC-2A rows: `a`, `a1`, `a-b`, and sixty-three lowercase characters accepted; absent, second argument, `-x`, uppercase, underscore, dot, leading digit, and sixty-four characters rejected before token, SSH, service, or NanoCore effect. Each consumer accepts exactly HTTPS default/explicit port and literal `localhost`, `127.0.0.1`, or `[::1]` HTTP origins and rejects exactly file, other scheme, userinfo, path, query, fragment, and non-loopback plaintext before token, SSH, service, or NanoCore effect; bring-up consumes the asserted manifest identity, starts only after assertion, accepts only exact configured current-generation readiness, and arms cleanup for exactly assertion failure, service-start failure, readiness rejection/timeout, HUP, INT, TERM, and success; direct teardown attempts stop plus authenticated decommission, remains idempotent, reports stop failure, decommission failure, and post-stop-still-active as three distinct failures, removes attempt credentials on every terminal edge, and emits no secret
- **Oracle:** test first expands only the already-admitted class into the finite Cartesian checklist required by NHC-2G: four accepted plus eight rejected alias rows times both named consumers, with rejected rows proving zero contact; five accepted plus seven rejected origins times both named URL/credential consumers; seven named bring-up terminal edges times cleanup invocation and credential removal; direct teardown stop failure, decommission failure, post-stop-still-active, and successful inactive controls; exact call ordering and zero rejected-origin effect; no wildcard, arbitrary alias, arbitrary URL, arbitrary signal, or “name one” instruction; the oracle is prior, a `bounded oracle`, reproducible, and re-runnable
- **Failure disposition:** one bounded correction inside the exact two-file builder lease; any new URL class, terminal edge, product/API owner, credential lifecycle, host fact, provisioning behavior, SSH mechanism, A1 effect, root command/preflight, documentation, or aggregate identity is scope expansion and stops this package
- **Next owner:** independent entry reviewer after NHC-2A, fresh test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one logical slice materially narrower than NHC-2; one execution unit; one test file, one read-only assertion dependency, and two builder files; approximately 80–180 changed lines; 24-event ceiling

- **Terminal disposition:** `FAIL` and superseded at exactly `24/24` events. The complete finite behavior matrix passes `59/59` and the combined host suite passes `117/117`, including bounded readiness timeout cleanup, but unconditional verification finds four independently editable copies of the one authoritative SSH-alias grammar and no honest incremental Git baseline for the previously untracked package artifacts. The third correction would cross the two-script lease and cannot fit a coherent exit; the standing engineer instruction requires a materially different split, not another ceiling increase. No later NHC-2B event is authorized.

### NHC-2H — Shared SSH-alias owner and lifecycle-artifact adoption

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `tests/README.md`; `tests/AGENTS.md`; `tests/support/host/README.md`; standing engineer instruction to split a package whose predicate or remaining capacity cannot close coherently
- **Scope:** establish one demonstrated shell owner for the already-accepted SSH-alias grammar, migrate exactly `provision.sh`, `assert.sh`, `nanohost-bring-up.sh`, and `teardown.sh` to source and call it before effects, align the local guide, and adopt the already-green NHC-2A/NHC-2B behavior under the coherent owner; no accepted/rejected alias, host fact, URL, terminal edge, cleanup behavior, asserted identity, command, preflight, workflow document, aggregate identity, product/API, A1, Docker, credential, or external effect change
- **Mode and permitted writes:** `implementation`; standing test-author admission is read-only over `tests/host-manifest.test.mjs` and `tests/host-manifest-runtime.test.mjs`; builder lease exactly new `tests/support/host/ssh-alias.sh`, existing `tests/support/host/provision.sh`, `tests/support/host/assert.sh`, `tests/support/host/nanohost-bring-up.sh`, `tests/support/host/teardown.sh`, and `tests/support/host/README.md`; `tests/host-manifest-projection.test.mjs` is a read-only aggregate control; every other file is immutable
- **Risk tier:** Tier 4 because the shared owner validates the verification-only SSH target before credential or remote effects; raw credentials, host-key policy, accepted alias grammar, URL/TLS rules, product state, and real A1 contact are unchanged
- **Dependencies:** NHC-2A `PASS`; NHC-2B stopped after its finite lifecycle behavior passed but coherence failed; NHC-FND-012 and NHC-FND-013 received; exact staged pre-write objects are materialized new-helper empty blob `e69de29bb2d1d6434b8b29ae775ad8c2e48c5391`, README `d5e0f4b96032a39e825c10d97edfe84af386df9b`, assert `8d335078b251dc3ccd322788d4429a9e2adf6eb1`, bring-up `30dd3d800c79c448ed6997ba130262b3b3fe5dc5`, provision `18a6f2ac2a3479d5718917ef77090a3d87ab9a9c`, and teardown `14eba297a648ea95936f435f554121eea8479877`; `git cat-file` and `git fsck --no-dangling` prove the helper object exists; staging is evidence only and conveys no acceptance
- **Predicate:** exactly one new shell owner defines exactly one documented `require_ssh_alias` function for `[a-z][a-z0-9-]{0,62}`; exactly four scripts source that owner and call it once on their external invocation path before token, SSH, curl, service, or host effect; the four scripts contain no parallel alias regex or default target; fixture/remote internal modes remain direct and unchanged; the existing four-accepted/eight-rejected rows per all four consumers, NHC-2A's 24 fact mismatches and digest output, NHC-2B's origin/terminal/teardown matrix, and focused counts remain green; the README names the one owner and four consumers
- **Oracle:** a finite L0 inventory decides one helper, one documented function, four source sites, four call sites, zero parallel grammar copies, and one README owner row. Existing tests decide exactly the already-frozen behavior tables and counts. Reviewer and verifier each run one scratch-only mutation of the shared grammar and require the named alias rows in both manifest and runtime suites to fail, proving all four consumers depend on the owner without a committed source-text assertion. The exact six staged objects, including the empty blob for the new helper, supply the pre-write Git baseline, so exit yield is one ordinary six-file worktree diff against the index. The oracle is prior, a `bounded oracle`, reproducible, re-runnable, and contains no “name one” instruction
- **Failure disposition:** one bounded correction inside the exact six-file builder lease; any second helper/function, fifth consumer, changed alias class, script behavior change, test rewrite, new input or terminal class, out-of-lease file, A1/Docker contact, or inability to derive an ordinary diff from the frozen index stops the package
- **Next owner:** independent entry reviewer, fresh standing test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one materially different shared-owner logical slice; one execution unit; six builder files and three read-only tests; approximately 20–60 changed lines; exact 26-event ceiling after two bounded pre-producer baseline corrections

- **Terminal disposition:** `FAIL` and superseded at exactly `26/26` events. The shared owner, four consumer migrations, finite behavior suites, scratch mutation, and ordinary six-file yield pass, but unconditional verification proves that unconditional top-level sourcing makes the two streamed `remote` modes unable to load the sibling helper and therefore unreachable. No later NHC-2H event or same-predicate ceiling increase is authorized; NHC-FND-014 routes the exact two streamed halves into a materially narrower executable-payload package.

### NHC-2I — Streamed remote halves and shared-owner adoption

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `tests/README.md`; `tests/AGENTS.md`; `tests/support/host/README.md`; standing engineer instruction to split rather than raise a package whose remaining capacity cannot close coherently
- **Scope:** make exactly the existing `provision.sh remote` and `assert.sh remote` payloads self-contained when streamed from an isolated directory, while adopting the already-reviewed one-helper/four-consumer candidate and preserving every existing fixture, alias, lifecycle, digest, cleanup, URL, and effect predicate; no third payload, new mode, accepted input, host fact, command, product/API, A1, Docker, credential, or external effect
- **Mode and permitted writes:** `implementation`; test-author writes exactly `tests/host-manifest.test.mjs`; builder writes exactly `tests/support/host/provision.sh` and `tests/support/host/assert.sh`; `tests/support/host/ssh-alias.sh`, `nanohost-bring-up.sh`, `teardown.sh`, `README.md`, `manifest.json`, `fixture-runner.mjs`, `tests/host-manifest-runtime.test.mjs`, and `tests/host-manifest-projection.test.mjs` are read-only adoption/control inputs; every other file is immutable
- **Risk tier:** Tier 4 because the two payloads enforce the real verification-only host boundary; alias grammar, identity, trust, credentials, host facts, URL/TLS, cleanup, service/product state, and real A1 contact are unchanged
- **Dependencies:** NHC-2A `PASS`; NHC-2H `FAIL` at 26 events with NHC-FND-014; its one-owner/four-consumer structure and finite 117-check behavior are unaccepted adoption candidates; exact staged pre-write objects are host-manifest test `045c1016dccdb73b5f65abc220ddea22b5c4ca6c`, provision `4deacae08a674b4649dbc64520c895f8b3caca42`, and assert `f8f6755014c1651025893a6bce6e08512e697a6a`; staging is evidence only
- **Predicate:** one new top-level executable test contains exactly two named streamed-payload rows. From a directory without `ssh-alias.sh`, piping exact `provision.sh` bytes to `bash -s -- remote <target> <version>` reaches its existing `Node source is not executable.` fail-closed boundary rather than a sibling-helper/bootstrap error; piping exact `assert.sh` bytes to `bash -s -- remote e30=` reaches beyond bootstrap without any sibling-helper or `BASH_SOURCE` error. In both scripts helper sourcing and `require_ssh_alias` occur only on external/fixture invocation paths where the repository helper exists; the remote modes remain self-contained. Existing manifest `56/56`, runtime `59/59`, projection `2/2`, combined `117/117`, shared-grammar mutation, and four-consumer structural inventory remain unchanged except the one new two-row test makes manifest `57/57` and combined `118/118`
- **Oracle:** the prior finite L1 oracle has exactly two named streamed inputs and exact failure boundaries; it runs only local isolated Bash with an empty `HOME`, no SSH/network/A1/Docker. The existing finite L0/L1 suites and one scratch shared-grammar mutation decide preservation. No arbitrary shell, path, host, payload, or “name one” instruction; the oracle is prior, a `bounded oracle`, reproducible, and re-runnable
- **Failure disposition:** no correction round or ceiling increase. Any third payload, helper transport mechanism, new script mode/input class, alias/host/URL/credential/lifecycle change, A1/Docker contact, out-of-lease file, or failure of the exact two-row oracle stops this package and triggers decomposition review
- **Next owner:** independent entry reviewer, fresh test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one materially different executable-payload logical slice; one execution unit; one test file, two builder files, and eight exact read-only adoption/control inputs; approximately 15–45 changed lines; exact 16-event ceiling with no correction allowance

- **Terminal disposition:** `FAIL` and discarded before any producer opened. Entry review proved that the assertion row's negative-only oracle could pass after a silent early exit, while provision has an exact positive marker. Per the frozen failure disposition, NHC-2I receives no correction round or ceiling increase; NHC-FND-015 splits the two different observation methods into NHC-2J and NHC-2K with no identity, budget, or verdict transfer.

### NHC-2J — Provision streamed-payload preservation

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `tests/README.md`; `tests/AGENTS.md`; `tests/support/host/README.md`; standing engineer split instruction
- **Scope:** make exactly the existing `provision.sh remote` payload self-contained when streamed from an isolated directory while preserving the shared alias owner candidate and every existing provision/fixture/alias behavior; no assertion payload, second script, new mode/input, host fact, command, product/API, A1, Docker, credential, or external effect
- **Mode and permitted writes:** `implementation`; test-author writes exactly `tests/host-manifest.test.mjs`; builder writes exactly `tests/support/host/provision.sh`; `ssh-alias.sh`, `assert.sh`, `manifest.json`, `fixture-runner.mjs`, runtime/projection tests, bring-up, teardown, and host README are read-only controls; every other file is immutable
- **Risk tier:** Tier 4 because the payload provisions the verification-only host boundary; alias grammar, identity/trust, host facts, credentials, product state, and real A1 contact are unchanged
- **Dependencies:** NHC-2A `PASS`; NHC-2H `FAIL`; NHC-2I discarded before producers; NHC-FND-014 and NHC-FND-015; staged pre-write objects are host-manifest test `045c1016dccdb73b5f65abc220ddea22b5c4ca6c` and provision `4deacae08a674b4649dbc64520c895f8b3caca42`
- **Predicate:** one new top-level executable test pipes exact `provision.sh` bytes from an isolated directory with empty `HOME` to `bash -s -- remote <target> <version>` and observes exact nonzero status plus `Node source is not executable.` with no sibling-helper or `BASH_SOURCE` error. Builder moves helper resolution/alias validation only behind the two existing fixture/remote branches into the external invocation branch; remote and fixture behavior otherwise remains byte-semantically unchanged. Existing manifest `56/56` becomes `57/57`; runtime `59/59`, projection `2/2`, combined `118/118`, shared-owner inventory, and scratch grammar mutation remain green
- **Oracle:** exactly one named local Bash payload and one exact downstream fail-closed marker; prior, finite, bounded, reproducible, re-runnable, no arbitrary path/payload or “name one”; no SSH/network/A1/Docker
- **Failure disposition:** no correction round or ceiling increase; any second payload, new mode/input, helper transport, behavior class, out-of-lease file, or missing exact marker stops the package
- **Next owner:** independent entry reviewer, fresh test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one provision-payload logical slice; one unit; one test and one builder file; approximately 10–25 changed lines; exact 16-event ceiling

- **Terminal disposition:** `PASS` at exactly `16/16` events. The exact two-file incremental diff is `+23/-2`; the isolated stream reaches the existing Node-source fail-closed marker, baseline mutation restores the intended one-row RED, retained suites pass `118/118`, and all eight coherence propositions survive independent falsification. NHC-2K may now establish fresh baselines without reopening NHC-2J.

### NHC-2K — Assertion streamed-payload preservation and shared-owner adoption

- **Authority:** same as NHC-2J, plus NHC-2J accepted provision-payload evidence
- **Scope:** make exactly the existing `assert.sh remote` payload self-contained, then adopt the already-reviewed one-helper/four-consumer and lifecycle candidates after both streamed halves are proven; no provision edit, third payload, new mode/input, host fact, command, product/API, A1, Docker, credential, or external effect
- **Mode and permitted writes:** `implementation`; test-author writes exactly `tests/host-manifest.test.mjs`; builder writes exactly `tests/support/host/assert.sh`; the remaining fifteen aggregate artifacts are read-only adoption/control inputs; every other file is immutable
- **Risk tier:** Tier 4 because assertion supplies the environment-identity gate for real use; all trust, input, credential, host, URL, cleanup, and product boundaries remain unchanged
- **Dependencies:** NHC-2J `PASS`; NHC-2H's structural/lifecycle candidate remains unaccepted; NHC-FND-014 and NHC-FND-015; exact staged pre-write objects are host-manifest test `4ee94f002a874dc3711930e1f980bee5cc71ed0e` and assert `f8f6755014c1651025893a6bce6e08512e697a6a`; accepted provision control is staged object `67812b0971eb78d0825463be8a9198728be1c125`
- **Predicate:** one new top-level executable test runs exact `assert.sh` bytes under `bash -x -s -- remote e30=` from an isolated directory and requires the positive runtime trace of the attempted `/usr/bin/node -e` fact-collection command, plus absence of sibling-helper or `BASH_SOURCE` errors. Bash xtrace emits the command before command lookup, so `/usr/bin/node` need not exist locally and the test never claims local host facts. Builder moves helper resolution/alias validation only into the external invocation branch; remote and fixture behavior otherwise remains byte-semantically unchanged. Existing manifest `57/57` becomes `58/58`; runtime `59/59`, projection `2/2`, combined `119/119`, exact one-helper/four-consumer inventory, scratch grammar mutation, NHC-2A fact/digest matrix, and NHC-2B lifecycle matrix pass
- **Oracle:** exactly one named local Bash-xtrace payload with one exact positive downstream command-attempt marker and two exact forbidden bootstrap errors; prior, finite, bounded, reproducible, re-runnable, no arbitrary trace/payload or “name one”; no SSH/network/A1/Docker
- **Failure disposition:** no correction round or ceiling increase; any second payload, new trace class, new script mode/input, helper transport, behavior change, out-of-lease file, or missing exact marker stops the package
- **Next owner:** independent entry reviewer after NHC-2J, fresh test author, fresh exact-lease builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one assertion-payload/adoption logical slice; one unit; one test and one builder file plus fifteen read-only controls; approximately 10–25 changed lines; exact 16-event ceiling

- **Terminal disposition:** `FAIL` at exactly `16/16` events. The assertion bootstrap correction, all `119/119` retained checks, shared-owner structure, and strict-risk boundaries pass, but the xtrace oracle matches both collector and comparator Node invocations; a scratch collector bypass therefore stays green. NHC-2K receives no correction or ceiling increase. Its stable assertion bytes remain an unaccepted read-only candidate for the host-harness contract package requested by the engineer.

### NHC-2L — Host harness contract and assertion-candidate adoption

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `tests/README.md`; `tests/AGENTS.md`; `tests/support/host/README.md`; engineer review requiring the already-purchased transport, ownership, and positive-observation facts to be visible before the next package
- **Scope:** make the existing assertion-stream trace marker collector-specific, record exactly three host-harness structural facts in the local guide, and independently adopt the read-only assertion/shared-owner/lifecycle candidate; no script/product behavior, helper, input class, host fact, command, preflight, URL, credential, cleanup, A1, Docker, network, or external effect
- **Mode and permitted writes:** `implementation`; test-author writes exactly `tests/host-manifest.test.mjs`; builder writes exactly `tests/support/host/README.md`; `assert.sh` and the remaining fifteen aggregate artifacts are read-only adoption/control inputs; every other file is immutable
- **Risk tier:** Tier 4 because the test and local contract decide a credential-bearing remote verification instrument; no strict-risk implementation or effect changes
- **Dependencies:** NHC-2J `PASS`; NHC-2K `FAIL` solely on NHC-FND-016 while stable assertion bytes and `119/119` behavior remain green; exact staged pre-write objects are test `7ece759608b56de6a664d2526bbd03e379a779e4`, README `0eb4430d9eed0d9852f712676d0f949ab366403e`, and read-only assertion control `d0015cfe94b81df69b0c545dd604b8e5f9412daa`; NHC-FND-014 through NHC-FND-016
- **Predicate:** the existing assertion-stream test uses a function-qualified Bash `PS4` and matches exactly the `collect_remote_facts` invocation of `/usr/bin/node -e`, not the later comparator invocation; current bytes pass, while a scratch early return at collector start makes only that targeted test fail. The local guide records exactly: (1) `provision.sh` and `assert.sh` are streamed whole, so each `remote` branch precedes any sibling source and depends on no sibling file; (2) `tests/host-manifest.test.mjs` owns manifest/provision/assertion and streamed-payload checks, `tests/host-manifest-runtime.test.mjs` owns bring-up/teardown lifecycle checks, and `fixture-runner.mjs` owns shared fixture execution; (3) every named host-harness predicate row needs a positive observation bound to its subject, and absence of an error alone is never PASS, while generic oracle authority remains Verification Instruments/Test Strategy. Existing manifest `58/58`, runtime `59/59`, projection `2/2`, combined `119/119`, exact one-helper/four-consumer inventory, shared-grammar mutation, fact/digest matrix, and lifecycle matrix pass
- **Oracle:** one existing test block, one exact function-qualified trace marker, one exact collector-bypass mutation, and three named README rows; prior, finite, bounded, reproducible, re-runnable, no source-text executable assertion and no “name one” instruction
- **Failure disposition:** no correction round or ceiling increase; any script/product change, fourth guide rule, new test/input/trace class, out-of-lease file, or failure of the exact mutation stops the package
- **Next owner:** independent entry reviewer, fresh test author, fresh exact-lease documentation builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one host-harness contract/adoption logical slice; one unit; one test and one local-guide file plus sixteen read-only controls; approximately 8–20 changed lines; exact 16-event ceiling

- **Terminal disposition:** `FAIL` and discarded before any producer opened. Entry review confirmed the marker/mutation and guide predicate, but the matrix claimed sixteen read-only controls where the future sixteen-artifact aggregate minus two writable files has fourteen. Per its no-correction disposition, NHC-2L receives no retry; NHC-FND-017 moves the exact counted remainder into still-unopened NHC-2C without transferring identity, budget, or verdict.

### NHC-2C — Command, preflight, documentation, and aggregate identity projection

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md` after NHC-2G; `docs/toolchain.md`; `docs/documentation-model.md`; `docs/specs/20260529-test_strategy.md`; `tests/README.md`; `tests/AGENTS.md`; `docs/cookbooks/README.md`; `docs/cookbooks/AGENTS.md`; engineer review requiring transport, ownership, and positive-observation facts before NHC-3
- **Scope:** correct the existing assertion-stream oracle so it names the collector subject; add the shared alias helper to the aggregate projection; adopt the read-only assertion/shared-owner/lifecycle candidate; expose the four root commands; attach host assertion to the existing `test:e2e:nano-restart:preflight`; align four workflow documents, the retained-result convention, and exactly three host-harness structure rows; compute the exact sixteen-path manifest and sorted path/content identities; no script/product behavior, helper, input class, credential, host mutation, service/product effect, Unit E/F action, A1, Docker, network, or external effect
- **Mode and permitted writes:** `implementation` with two sequential units. U1 test-author lease exactly `tests/host-manifest.test.mjs` and `tests/host-manifest-projection.test.mjs`. U2 builder lease exactly `package.json`, root `README.md`, `CONTRIBUTING.md`, `tests/support/host/README.md`, `docs/cookbooks/README.md`, and `docs/cookbooks/nanohost-real-use-host.md`. Eight read-only aggregate controls are exactly `tests/host-manifest-runtime.test.mjs`, `tests/support/host/assert.sh`, `fixture-runner.mjs`, `manifest.json`, `nanohost-bring-up.sh`, `provision.sh`, `ssh-alias.sh`, and `teardown.sh`; every other file is immutable
- **Risk tier:** Tier 4 because one executable oracle and local contract decide a credential-bearing remote verification instrument; no strict-risk implementation or effect changes
- **Dependencies:** NHC-2A and NHC-2J `PASS`; NHC-2B/NHC-2H/NHC-2K are superseded evidence adopted only through this package; NHC-2I/NHC-2L discarded before producers; NHC-2MS test ownership and NHC-2G authority `PASS`; exact sixteen-path staged index inventory digest `3b06b669558be010c0b9edb47fb43013c616d7b0ad614473234af9a7bd855674`; NHC-FND-014 through NHC-FND-017
- **Predicate:** U1 makes the existing assertion-stream test use a function-qualified Bash `PS4` and match exactly the `collect_remote_facts` invocation of `/usr/bin/node -e`; current bytes pass and a scratch early return at collector start makes only that targeted test fail. U1 also adds `tests/support/host/ssh-alias.sh` to the aggregate projection and changes its exact identity from fifteen to sixteen without adding a test case. U2 exposes exactly `host:provision`, `host:assert`, `host:nanohost:bring-up`, and `host:teardown`; the existing `test:e2e:nano-restart:preflight` invokes `host:assert`; exactly root `README.md`, `CONTRIBUTING.md`, host README, cookbook README, and the NanoHost cookbook project their owned workflow facts without a fifth workflow owner; the conventional retained result is `temp/state/nanohost/host-manifest/a1/result.json`. The host README records exactly: streamed `provision.sh`/`assert.sh` remote branches precede sibling source and depend on no sibling; manifest test owns manifest/provision/assertion/stream checks, runtime test owns bring-up/teardown lifecycle, and fixture runner owns shared fixture execution; every named host-harness row needs a positive subject-bound observation and absence of error alone is not PASS, while generic authority stays in Verification Instruments/Test Strategy. The final inventory is exactly sixteen named paths; computed manifest and sorted path/content digests identify it. Manifest `58/58`, runtime `59/59`, projection `2/2`, combined `119/119`, one-helper/four-consumer inventory, grammar mutation, fact/digest and lifecycle matrices pass
- **Oracle:** prior finite checklist: one existing collector-marker block and one exact bypass mutation; one `15→16` path addition; four commands; one preflight; five named document projections with no extra owner; one result path; three host-guide rows; sixteen exact paths and two digests. Documentation is decided by independent artifact review, not an executable source-text assertion. No arbitrary input, checklist-external “name one,” or weak absence-only instruction; the oracle is prior, a `bounded oracle`, reproducible, and re-runnable. The named false-predicate failure is `collector bypass or projection matrix accepted`, meaning the bypass mutation stays green or one exact command, preflight, document, result, guide, path, or digest row differs; setup or collection failure does not count
- **Failure disposition:** no correction round or ceiling increase. Any script/product behavior, third test, seventeenth artifact, fourth host-guide rule, fifth workflow owner, new command/preflight/result, input/trace class, out-of-lease file, A1/Docker/network/effect, or failure of the exact mutation stops this package
- **Next owner:** independent entry reviewer, fresh test author for U1, fresh exact-lease builder for U2, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one harness-contract plus projection package; two sequential units with disjoint leases; eight writable and eight read-only aggregate artifacts; approximately 30–90 changed lines; exact 16-event ceiling
- **Terminal disposition:** `PASS` at exactly `16/16` events. Manifest `58/58`, runtime `59/59`, projection `2/2`, and combined `119/119` pass; the collector-bypass mutation makes only its subject-bound row fail. Manifest SHA-256 is `9d5af17f70ea0df4c543a72935209891fee919e5023af522ec4b199160048aab`; sorted sixteen-path/content SHA-256 is `0b6ec0211bb1fa6f2e2f1e8e46985c1ac6e8b67949b7214decb8643d006e22f7`

### NHC-3 — Two-cycle real A1 proof

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/specs/20260529-test_strategy.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`
- **Scope:** validate the retained-result contract before A1, then run exactly two consumed sequential A1 cycles through the four reviewed root commands and retain one redacted result; no tracked write, Unit E action, sandbox creation, worker Turn, counted marker, or credential retention
- **Mode and permitted writes:** `real-use verification`; test-author lease `temp/state/nanohost/host-manifest/a1/validate-result.mjs`; builder lease `temp/state/nanohost/host-manifest/a1/result.json`
- **Risk tier:** Tier 4 remote host and credential-bearing readiness
- **Dependencies:** NHC-2G, NHC-2TP, NHC-2MS, NHC-2A, NHC-2J, and NHC-2C `PASS`; NHC-2B/NHC-2H/NHC-2K are superseded evidence adopted only through NHC-2C; immutable sixteen-file artifact and manifest digests; explicit invocation-time SSH alias `a1`; exact clean entry; attempt-local credentials; computed asserted-manifest identity
- **Predicate and oracle:** exactly the closed `schemaVersion=1`, target `A1`, digest pair, two-cycle nullable-stage, readiness, teardown, credential-issued/removed grammar frozen in the predecessor's final H1-B entry `PASS`; `provisionExit` is the first observable; pre-A1 self-test accepts one fixed PASS and rejects exactly the twenty named mutations below; missing cleanup or credential removal blocks. The closed top-level and cycle key sets are also inspected directly. The oracle is prior, finite, a `bounded oracle`, reproducible and re-runnable

| ID | Exact mutation from the fixed PASS stand-in |
| --- | --- |
| `I01` | Add top-level key `unexpected` |
| `I02` | Delete top-level key `manifestDigest` |
| `I03` | Set `schemaVersion` to `2` |
| `I04` | Set `targetLabel` to lowercase `a1` |
| `I05` | Replace `artifactDigest` with a different lowercase 64-hex value |
| `I06` | Replace `manifestDigest` with a different lowercase 64-hex value |
| `I07` | Keep only the first cycle |
| `I08` | Append a third cycle |
| `I09` | Add cycle key `unexpected` |
| `I10` | Delete cycle key `credentialRemoved` |
| `I11` | Set `provisionExit` to string `"0"` |
| `I12` | Set `provisionExit=1` while `assertExit=0` |
| `I13` | Set `provisionExit=0` and `assertExit=null` |
| `I14` | Set `assertExit=1` while `bringUpExit=0` |
| `I15` | Set `assertExit=0` and `bringUpExit=null` |
| `I16` | Set `bringUpExit=0` and `readiness=not-observed` |
| `I17` | Set `bringUpExit=1` and `readiness=authenticated` |
| `I18` | Set `teardownExit=null` |
| `I19` | Set `teardownExit=1` |
| `I20` | Set `credentialIssued=true` and `credentialRemoved=false` |
- **Failure disposition:** one retry only after proved teardown and credential removal; any unproved cleanup, retained state, or credential residue blocks immediately; no fourth round or new input class
- **Next owner:** independent entry reviewer after NHC-2C, fresh test author, fresh real-use builder, independent reviewer, different-family verifier, direct human scrutiny
- **Slice:** one execution unit; two retained files; approximately 100–180 lines; 24-event ceiling
- **Execution checkpoint:** the validator self-test passes `1/1` accepted and `20/20` rejected, but the first redacted A1 prerequisite probe found SSH reachable with NanoHost and NanoCore units, NanoHost owner configuration, NanoCore origin, configured identity/deployment, and safe-sink metadata all absent. No cycle is consumed and the builder assignment remains open but held before `host:provision`. The first escalation verification passed an ordering recommendation that later direct authority inspection falsified: Unit E's mandatory cleanup removes the exact units, configuration, and state that recommendation expected to remain for NHC-3. The corrected escalation recommends discarding this zero-cycle dedicated wrapper and making the still-unopened NHC-4 real-dependency run both the host manifest's first real bring-up and Unit E's rehearsal. Its first verifier returned only four bounded packet defects: the run must retain the observed generic `manifestDigest`, Unit E closure/artifact digest, and separately asserted complete Unit E host-prerequisite digest; the manifest-managed `/usr/bin/node` projection must be treated as long-lived machine state that Unit E verifies and never creates or removes; no NHC-3 identity, budget, denominator, verdict, result, or cycle transfers; and the two active host workflow guides must stop promising the discarded two-cycle result. Those four corrections are pending continued escalation verification
- **Terminal disposition:** `STOPPED` and discarded after continued independent escalation verification `PASS`. The package consumed zero cycle, wrote no `result.json`, and transferred no identity, budget, denominator, verdict, result, or cycle. Its validator remains retained non-authorizing evidence. The engineer's standing auto-authorization activates NHC-4 as the single integrated first-real-dependency boundary described below

### NHC-4 — Unit E correction and real-dependency rehearsal

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/toolchain.md`; `docs/specs/20260529-test_strategy.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `tests/support/host/README.md`; `docs/cookbooks/nanohost-real-use-host.md`; `temp/state/wp6/HANDOFF-20260812-unit-e-real-dependency.md` only as recovered implementation evidence; the engineer's verified NHC-3 discard and one-freeze/direct-pipeline disposition
- **Scope:** correct the existing Unit E environment oracle and execute it once: bind the complete Unit E target command vocabulary to the same identities preflight validates; observe architecture, kernel, cgroup v2, the exact attempt-owned absence set, the three legacy-service states, and the preserved unrelated user NanoCore state; make fixture and real execution share that boundary; provision and assert the accepted generic host manifest before opening the Unit E closure; materialize current dependencies; reach authenticated configured NanoHost readiness; then stop first, remove only Unit E product state and credentials, preserve manifest-owned `/usr/bin/node` and unrelated host state, prove byte-identical pre/post baseline, and retain the three required identities. No Unit F scenario, worker, sandbox, transcript, counted marker, F1 verdict, terminal-spec edit, persistent product deployment, second real-use wrapper, or NHC-3 transfer
- **Mode and exact thirteen-file source matrix:** `implementation` with one execution unit and one continuous `test-author → builder → reviewer → verifier` sequence; there is no separate entry-review loop. Test-author writes exactly `temp/state/wp6/v3-preseal/self-check.sh` (baseline SHA-256 `5085857d4f38908c0a25caa32804d006e371df7adcda59097eee3f3930a00435`). Builder writes exactly: `temp/state/wp6/v3-input/host-prerequisites.json` (`5457cf9b5ac721c352e97a4a450ca70d86bbb8fa71d99eddb8cae31c7c5838f2`); new `nanocore.env`, `nanohost.env`, and `driver-base.env` in that directory; `temp/state/wp6/v3-preseal/unit-facts.sh` (`9372ccf133055068598880d4500c2aca17e0c3d41af19c272616e830ffd65d3b`), `real-dependency-rehearsal.sh` (`cda80d1613bce3ca317bf95ea3a71ee9b70bd081a152c67b8c048b5e09c4c51b`), `driver.sh` (`eca54dfc52b4bd39d02cf75c55d66b7df71901da6f1717461d09e86573dc3257`), and `README.md` (`17694f86967d4c4da25e8f69e5844fa102039e41569b6996b4898627b4af35fc`); `tests/support/host/README.md` (`962a9433e6fa056ec6c2d2d4e9c58b17235f48d23015b6a4e0c50d85b0021308`); `docs/cookbooks/nanohost-real-use-host.md` (`c2032278b147ab01ee7781e06058db3b2acc0bb883577b2bd4d59be77394390c`); `temp/state/wp6/HANDOFF-20260812-unit-e-real-dependency.md` (`210beea739597b7b61b70d097ffc474156c4c0646fd24133e7dffaa586178209`); and `temp/state/wp6/unit-e-v3-harness-builder-report.md` (`015d312a0cb25c3e36ff9d1387faa5e8191a388f62a18413ba0cb95daef8337c`). Builder may create only derived content under fresh `temp/state/wp6/v3-dependencies/`; verifier may create only redacted retained evidence under fresh `temp/state/wp6/v3-real-dependency-evidence/`. `temp/state/wp6/v3-input/server.jsonc`, the other forty-five current `v3-preseal` files, the accepted sixteen host-manifest artifacts, product source, and every other repository path are read-only
- **Risk tier:** Tier 4 because the package provisions one long-lived executable projection, creates attempt-local credentials, starts real NanoCore/NanoHost services, and performs bounded remote cleanup; raw credentials, TLS policy, authorization semantics, sandbox/worker effects, data deletion outside the six exact attempt-owned roots/files, and irreversible external effects remain excluded
- **Dependencies:** NHC-1 and NHC-2C `PASS`; NHC-3 `STOPPED`/superseded at zero cycles with no result; manifest SHA-256 `9d5af17f70ea0df4c543a72935209891fee919e5023af522ec4b199160048aab`; accepted sixteen-path/content SHA-256 `0b6ec0211bb1fa6f2e2f1e8e46985c1ac6e8b67949b7214decb8643d006e22f7`; current 46-file Unit E candidate SHA-256 `e608f29abdbd6c73d55e298ce608a36763907153754032bde1419204067f1ce0`; server configuration SHA-256 `5390f250946b86669a1fadc4f95134f391f81880482ea25cfa57f6ee71c0456c`; explicit invocation alias `a1`; the following read-only A1 facts observed before this freeze
- **Frozen A1 machine facts:** architecture `aarch64`; kernel `6.17.0-1018-oracle`; cgroup filesystem `cgroup2fs`, logically `unified-v2`, with controllers `cpuset cpu io memory hugetlb pids rdma misc dmem`; Node source `/home/ubuntu/.local/share/mise/installs/node/24.18.0/bin/node`, version `v24.18.0`, SHA-256 `6bf69d0eda41a12030d5f28d958cd09ce323bc0c13f1ab4d8bb426933aa08812`; target `/usr/bin/node` absent before manifest provision. The three legacy Cell services and all three WP6 services were `not-found/inactive/dead`; the seven exact attempt roots/unit files and attempt-owned jobs, processes, namespaces, containers, networks, sockets, and auth inputs were absent. The user-level `openkit-nanocore.service` was separately `loaded/active/running` with one pre-existing cgroup containing three processes; it is unowned by Unit E, is excluded from the absence set, and must remain baseline-identical

| Exact Unit E command | Observed path → resolved path | Stable SHA-256 identity |
| --- | --- | --- |
| `awk` | `/usr/bin/awk` → `/usr/bin/gawk` | `74a585a10c39d4ddc1b3a21df5dc5137c14bf4c4d2c1a76c25d724dfbb836496` |
| `basename` | `/usr/bin/basename` | `6f59798300ab8020d300226e74f6600e11f3ccc19501e834fdb8c7e494087037` |
| `bash` | `/usr/bin/bash` | `af955ef55333c8fc9c5aa50df91ad1a629d9a79a9afa125cd5e9629585f78015` |
| `cat` | `/usr/bin/cat` | `604ff8110ab37748594c3e61d6ba52140788c7c29500fb0edc314e08db155f2a` |
| `chmod` | `/usr/bin/chmod` | `4fe3b161f39da2cd87d2c55fe8c4f8d61aa3080b9eeaaf21ca7b693c4f4ca473` |
| `cmp` | `/usr/bin/cmp` | `e7a3863a965c20d9b4beaad4736ff4406108bb88ba2d09a122c931ca246a0db4` |
| `cp` | `/usr/bin/cp` | `3ec1dff780dc05085e68c636879751629e2972ad05e4c02a1e57ac67ad16f2fc` |
| `curl` | `/usr/bin/curl` | `67054bcf748d42e1bf4b2a0eb4ba768e37dde8681313a64edd2f343c5d17a0ac` |
| `cut` | `/usr/bin/cut` | `e2cdff490904b029d89ea8e0f39b709d4674e2f828637fad324bd8985381a1d2` |
| `date` | `/usr/bin/date` | `90b4b67c4de251db8c845731670d78ec3fe40ae9f60477a975f83d6dc42efc59` |
| `dirname` | `/usr/bin/dirname` | `0aed6960221e6ec4f6dc82c1009aee99fb26ddbc1dd5634c2be697e2e03782c6` |
| `docker` | `/usr/bin/docker` | `1a0364828b647889b15ea6d363d50478472657c0613cb5eac7099aa6c630c46d` |
| `env` | `/usr/bin/env` | `dde57443115af498aa981159ba6517b156547173342af36487ed58631d9e47c3` |
| `find` | `/usr/bin/find` | `1ee9e96c55e9d3551cebb991882087c5b21d18c4b4ead73203ca84ab54940ce1` |
| `grep` | `/usr/bin/grep` | `fb7efa5f6b958b44258b08ab860b086aaa0446b8b50564b3d43367b2ba4ed407` |
| `ip` | `/usr/sbin/ip` → `/usr/bin/ip` | `4430e2386b96f3614a14f5ef4b07b7547de95ede97d501bad2d0e381defcbb90` |
| `ln` | `/usr/bin/ln` | `925e8bd7b49c7538616c070f2c17bb8b24908c1138c834dcce6c4117485b6118` |
| `mkdir` | `/usr/bin/mkdir` | `e0308b807656f115c844f785865ca009e6df2f5789ebfa3efb49c4b01640344e` |
| `mkfifo` | `/usr/bin/mkfifo` | `008cd305d0d8ad589d11154b206a0ca3c4b829117127822959e451102b570668` |
| `mktemp` | `/usr/bin/mktemp` | `4d082e92bcb9a6a2071fb7247e5785b748f4b5e0f8261ce3fcffeda775f9a05f` |
| `mv` | `/usr/bin/mv` | `8d17f81426c154381c58d5e9d957473b66a828589d4adf20f461b476137ae469` |
| `ps` | `/usr/bin/ps` | `3fa1c04e29647304817b1ffe9a7600dc80c879ea4c8a9302032799830e0c2133` |
| `readlink` | `/usr/bin/readlink` | `068bf5e7b3cfa43ae02ccf7b854f13863adb05d75d011a2de9e89adafd1a524b` |
| `rm` | `/usr/bin/rm` | `acc9c5cb7387a25725bb504bf60fc35fbd487e902dd6b4a725f444d3188e95a6` |
| `sed` | `/usr/bin/sed` | `39ca7d42a112f32abd4f2a016c8a0d1777a5fd75608f8e1a5d80ac161337a0fe` |
| `sh` | `/usr/bin/sh` → `/usr/bin/dash` | `c883d9c27228339580994a1d0ea960645368c7d1b960a712bd0c655860c2d0d7` |
| `sha256sum` | `/usr/bin/sha256sum` | `f3d040161f5c29e4c7cd4e3d6bb513ce9a43b9d1bd06f456a6aab3d34d0f1e33` |
| `sleep` | `/usr/bin/sleep` | `a3202123ba517a3c4f08ca25429e3f48eb9dffae9b888a9d353c21451f807f6a` |
| `sort` | `/usr/bin/sort` | `585d23713156762b18afcebc553fee6d46fe2d5848c27f3f46ab13d877341d79` |
| `ss` | `/usr/bin/ss` | `b80cfa2fefe769074de4099676819f83a0186bc17a0acf198252b4d63d99470f` |
| `stat` | `/usr/bin/stat` | `86b89355842f54a49698069b345eabb94c2f26d2f33b5734ec91c68de8fe6f7a` |
| `strings` | `/usr/bin/strings` → `/usr/bin/aarch64-linux-gnu-strings` | `f6b809537477dfed119b80ad8c0da1653a2b9ed0173ebb72ebf236a1c94ce018` |
| `sudo` | `/usr/bin/sudo` | `ca08025c5f9592863a703b0c023128548a7a5cf13ad1bc90d70dcf395376795e` |
| `systemctl` | `/usr/bin/systemctl` | `f158408c8ff84e0dfd2eda0678b7939a028a7c3c19039ef5792511a92a36267e` |
| `tar` | `/usr/bin/tar` | `241a4105e9efc361cbb54aa314b7449540f85bd1d97637b4db2dae6191033dc5` |
| `timeout` | `/usr/bin/timeout` | `2db30bc57746c940a0643581cd5e2671e505442bea16164143f0ea8ff4e36453` |
| `tr` | `/usr/bin/tr` | `98805355098cc9a3c6efb8c766d8ccbb01226070bdd13f386655d2713d9b1f6c` |
| `uname` | `/usr/bin/uname` | `37b5a64a72058548742fca7fa0c11e30c88b9042a533ab0c8433409ddc61f4f9` |
| `wc` | `/usr/bin/wc` | `4bab599a0dbd6d87e5bcedaa021b727d9306777741850fdb5a4b8a1804e486fe` |
| `xargs` | `/usr/bin/xargs` | `e4d376e8f56a26f8e968566585c6cdbf593e5c77d271b5a16d85d449e57d5993` |

- **Frozen A1 version addendum:** one authorized read-only probe after the dispatch-sufficiency verdict re-observed the same forty names, paths, resolved paths, and SHA-256 values above and completed only their already-required version field. `ip`, `ss`, and `sudo` used `-V`; every other row used `--version`. `sh --version` intentionally returned status `2` with the exact first stderr line below; every other version probe returned status `0`

| Exact Unit E command | Observed version string |
| --- | --- |
| `awk` | `GNU Awk 5.2.1, API 3.2, PMA Avon 8-g1, (GNU MPFR 4.2.1, GNU MP 6.3.0)` |
| `basename` | `basename (GNU coreutils) 9.4` |
| `bash` | `GNU bash, version 5.2.21(1)-release (aarch64-unknown-linux-gnu)` |
| `cat` | `cat (GNU coreutils) 9.4` |
| `chmod` | `chmod (GNU coreutils) 9.4` |
| `cmp` | `cmp (GNU diffutils) 3.10` |
| `cp` | `cp (GNU coreutils) 9.4` |
| `curl` | `curl 8.5.0 (aarch64-unknown-linux-gnu) libcurl/8.5.0 OpenSSL/3.0.13 zlib/1.3 brotli/1.1.0 zstd/1.5.5 libidn2/2.3.7 libpsl/0.21.2 (+libidn2/2.3.7) libssh/0.10.6/openssl/zlib nghttp2/1.59.0 librtmp/2.3 OpenLDAP/2.6.10` |
| `cut` | `cut (GNU coreutils) 9.4` |
| `date` | `date (GNU coreutils) 9.4` |
| `dirname` | `dirname (GNU coreutils) 9.4` |
| `docker` | `Docker version 29.6.1, build 8900f1d` |
| `env` | `env (GNU coreutils) 9.4` |
| `find` | `find (GNU findutils) 4.9.0` |
| `grep` | `grep (GNU grep) 3.11` |
| `ip` | `ip utility, iproute2-6.1.0, libbpf 1.3.0` |
| `ln` | `ln (GNU coreutils) 9.4` |
| `mkdir` | `mkdir (GNU coreutils) 9.4` |
| `mkfifo` | `mkfifo (GNU coreutils) 9.4` |
| `mktemp` | `mktemp (GNU coreutils) 9.4` |
| `mv` | `mv (GNU coreutils) 9.4` |
| `ps` | `ps from procps-ng 4.0.4` |
| `readlink` | `readlink (GNU coreutils) 9.4` |
| `rm` | `rm (GNU coreutils) 9.4` |
| `sed` | `/usr/bin/sed (GNU sed) 4.9` |
| `sh` | `/usr/bin/sh: 0: Illegal option --` |
| `sha256sum` | `sha256sum (GNU coreutils) 9.4` |
| `sleep` | `sleep (GNU coreutils) 9.4` |
| `sort` | `sort (GNU coreutils) 9.4` |
| `ss` | `ss utility, iproute2-6.1.0` |
| `stat` | `stat (GNU coreutils) 9.4` |
| `strings` | `GNU strings (GNU Binutils for Ubuntu) 2.42` |
| `sudo` | `Sudo version 1.9.15p5` |
| `systemctl` | `systemd 255 (255.4-1ubuntu8.16)` |
| `tar` | `tar (GNU tar) 1.35` |
| `timeout` | `timeout (GNU coreutils) 9.4` |
| `tr` | `tr (GNU coreutils) 9.4` |
| `uname` | `uname (GNU coreutils) 9.4` |
| `wc` | `wc (GNU coreutils) 9.4` |
| `xargs` | `xargs (GNU findutils) 4.9.0` |

- **Predicate:** the host record has the exact forty-entry table above plus the separate Node row and no extra tool; each row records `path`, `resolvedPath`, `sha256`, and the observed version string. The shared preflight uses only the accepted absolute Node projection to observe and compare the exact architecture, kernel, cgroup filesystem/controllers, Node source/target/version/digest, forty tool resolutions/digests, three legacy-service states, eleven named attempt-owned absence classes, and the preserved unrelated user service before any Unit E product effect. It creates one attempt-owned command directory whose forty names plus `node` resolve only to those validated paths, exports that as the sole external-command `PATH`, and all subsequent Unit E shell execution uses it; no ambient fallback is permitted. Fixture and real modes call the same preflight and command boundary. The exact external input files contain only the already frozen server mode, `http://127.0.0.1:17891`, identity `integration_wp6_v3`, deployment `deployment_wp6_v3`, slot `B`, and the four absolute slot files under `/home/ubuntu/wp6-v3/runtime/nanohost-state`; no credential value is stored. Generic provision creates `/usr/bin/node`, generic assertion emits exactly `manifestDigest=9d5af17f70ea0df4c543a72935209891fee919e5023af522ec4b199160048aab`, and Unit E only verifies and preserves it. The retained archive records that manifest digest, the materialized closure digest, the final complete host-prerequisite JSON digest, dependency digest, baseline-before/after digests, exact cleanup, and preservation of the unrelated user service; it contains no credential, packet, public-surface, worker, sandbox, transcript, counted verdict, or NHC-3 result
- **Oracle:** prior finite L0/L1 self-check followed by one L4 real-dependency execution. The deciding offline table is exactly: forty tool path/resolution/digest rows plus one Node row; six scalar facts; three legacy services; eleven attempt-owned absence classes; one preserved unrelated service/cgroup row; five exact input files; one shared command-root positive path; path split, omitted tool, path, resolution, digest, architecture, kernel, cgroup, Node, legacy-state, and each of the eleven residue mutations; retained existing closure/readiness/cleanup/evidence-safety rows. The real-use verifier decides exactly one provision exit, assertion digest, dependency/closure identity, authenticated readiness observation, cleanup result, baseline equality, preserved-host observation, and safe archive. This is prior, finite, a `bounded oracle`, reproducible, and re-runnable; it contains no arbitrary target, wildcard falsification, or “name one” instruction
- **Failure disposition:** one frozen pipeline only. Test-author, builder, reviewer, and verifier identities remain continuous through at most one bounded same-class correction; any new executable, input, residue, service, credential, product behavior, evidence type, source file, effect domain, retry after an unproved cleanup, or second correction round stops NHC-4 and records a decomposition finding. A failed real attempt may be retried once only after the same verifier and reviewer prove cleanup, credential removal, unrelated-state preservation, and that the correction remains inside the exact matrix; no ceiling increase or renamed wrapper
- **Next owner and order:** the already authorized direct sequence is fresh test author, fresh builder, independent artifact reviewer, then different-family verifier who alone may run the reviewed A1 real-dependency attempt; direct human scrutiny applies to the final evidence. No entry reviewer, parallel writer, or NHC-3 identity opens
- **Slice:** one integrated Unit E first-real-dependency execution unit; exactly thirteen source files plus two generated temp roots; expected source magnitude approximately 250–650 changed lines plus generated dependencies/evidence; planning denominator `1`; expected clean path approximately fifteen events and hard local ceiling `24`
- **Terminal disposition:** `STOPPED` and superseded at exactly `24/24` events after independent decomposition verification `PASS`. The offline fixture mapped all forty command names to one identity-only stub, then required that root to perform the complete effectful rehearsal as sole `PATH`; the admitted unit was structurally impossible. No dependency root, NHC-4 A1 attempt, Docker effect, product effect, or credential was created. The corrected self-check and six partial builder files remain unaccepted adoption candidates and transfer no identity, budget, denominator, verdict, correction history, or `PASS`. NHC-FND-020 records the accepted split and raised framework hypothesis

### NHC-4A — Finite host identity, preflight, and command-root construction

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/toolchain.md`; `docs/specs/20260529-test_strategy.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `tests/support/host/README.md`; the verified NHC-FND-020 decomposition disposition
- **Scope:** complete only the exact host record and five non-secret inputs, make fixture and real entry paths call one shared preflight, construct one forty-tool-plus-Node command root from validated identities, and run one local positive executable-root smoke through behavior-complete commands. This package proves comparison, rejection, mapping, and the exact `mkdir → cp → tar → rm` round trip only; it does not execute Unit E, simulate any of the forty commands, materialize dependencies, contact A1 or Docker, create credentials, or produce rehearsal evidence
- **Mode and exact lease:** `implementation`, one execution unit. Test author writes only `temp/state/wp6/v3-preseal/self-check.sh`, adding one exact `--host-identity` focused mode that terminates after the finite identity/input/preflight/root oracle. Builder writes only `temp/state/wp6/v3-input/host-prerequisites.json`, `nanocore.env`, `nanohost.env`, `driver-base.env`, and `temp/state/wp6/v3-preseal/unit-facts.sh`. Every rehearsal, driver, dependency, evidence, product, documentation, plan, findings, and other path is read-only. Current partial bytes are adoption candidates, never transferred artifacts
- **Risk tier:** Tier 4 because this instrument decides the executable identity and sole command environment later used at the credential-bearing remote boundary; actual remote access, credentials, product authorization, TLS, Docker, cleanup, data deletion, and irreversible effects remain untouched
- **Dependencies:** NHC-1 and NHC-2C `PASS`; NHC-3 and NHC-4 superseded; accepted manifest and sixteen-path identities; exact forty-command path/resolution/SHA/version addendum and separate Node identity already frozen above; current adoption-candidate hashes are host record `8cd8acf8...f34c9`, three inputs `c3b20870...0c877`, `52ec853f...68922`, `89995c98...8519`, unit facts `74af70e7...5ac8`, and self-check `df125f19...51eea20`
- **Predicate:** the host record contains exactly six scalar/cgroup facts, one Node record, forty tool records with the two frozen version invocation classes and exact path/resolved-path/SHA/version values, three exact legacy-service `not-found/inactive/dead` rows, eleven attempt-owned absence rows, and one preserved unrelated user-service/cgroup row. The three input files contain only the frozen server mode, loopback origin, identity, deployment, slot, and four absolute slot files. One shared preflight rejects every finite mismatch before effects and constructs exactly forty command symlinks plus `node`, each resolving to its validated executable. In the focused fixture, the four named smoke rows resolve through that assembled sole `PATH` to behavior-complete local executables; the check creates a directory, copies one fixed file, archives it, removes the working directory, and verifies the archive contains the exact bytes. The other thirty-six commands are not invoked or simulated
- **Oracle:** L0/L1 `self-check.sh --host-identity` decides the exact 160 tool-field mutations, forty version mutations, six scalar, four Node, three legacy-service, eleven residue, one preserved-service, five-input, omitted/split/root-binding, exact positive inventory rows, and one closed positive `mkdir → cp → tar → rm` round trip with exact archive content through the assembled root. It is prior, finite, a `bounded oracle`, reproducible, re-runnable, and contains no wildcard, arbitrary target, command simulation, or “name one” instruction. The named false-predicate failure is `host identity or command-root defect accepted`, meaning one exact mutation remains green or the admitted four-command round trip does not reach its exact archive-byte observation; setup or collection failure does not count
- **Failure disposition:** one implementation-local correction may continue under the same open identities only when it changes no input class, tool row, service, residue, lease, proof layer, or effect domain. Any apparatus conflict, second correction, dependency/rehearsal need, A1/Docker access, credential, or command-behavior simulation stops and decomposes; no ceiling increase
- **Next owner and order:** one independent reviewer first adjudicates this complete NHC-4A/NHC-4B freeze; after `PASS`, fresh test author, fresh builder, independent artifact reviewer, then different-family verifier. No entry-review loop and no role identity transfer from NHC-4
- **Slice:** one materially distinct host-identity logical slice; six writable files; approximately 180–420 changed source lines; planning denominator `1`; expected clean path fifteen events; hard local ceiling `18`
- **Terminal disposition:** `STOPPED` and superseded at exactly `18/18` events. The 243-case oracle and five adoption candidates remain unaccepted evidence: `WP6_ADMISSION_CALL_LOG` can make the shared preflight return success without binding the assembled root, and this second distinct defect activated the frozen decomposition rule. No A1, Docker, network, credential, product, dependency, cleanup, or Unit E effect occurred; no identity, budget, denominator, verdict, correction history, or `PASS` transfers

### NHC-4C — Shared sole-PATH command boundary

- **Authority:** `AGENTS.md`; `docs/change-execution.md`; `docs/verification-instruments.md`; `docs/toolchain.md`; `docs/specs/20260529-test_strategy.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `tests/support/host/README.md`; NHC-FND-020; the NHC-4A different-family verifier's STOP/decomposition verdict
- **Scope:** settle only the shared command-boundary seam that NHC-4A left open. `preflight_unit_inputs` must unconditionally bind and export exactly the assembled forty-command-plus-Node root on every successful return; `WP6_ADMISSION_CALL_LOG` remains an observation channel only and cannot select, append, bypass, or suppress that command environment. This package preserves the already-finite host/input/root table, local four-command smoke, and caller execution paths; it does not change a host fact, tool row, service, residue, dependency, rehearsal, product behavior, or effect domain
- **Mode and exact lease:** `implementation`, one execution unit. Test author writes only `temp/state/wp6/v3-preseal/self-check.sh`; builder writes only `temp/state/wp6/v3-preseal/unit-facts.sh`. The other forty-four files in the current exact forty-six-file `v3-preseal/` inventory and the exact five `v3-input/` files `server.jsonc`, `host-prerequisites.json`, `nanocore.env`, `nanohost.env`, and `driver-base.env` are read-only controls. The five direct caller files `real-dependency-rehearsal.sh` and `unit-f1.sh` through `unit-f4.sh` are included in that read-only inventory. Every other repository path is immutable
- **Risk tier:** Tier 4 because this two-file boundary selects the sole executable environment later used by a credential-bearing real host attempt; actual A1, Docker, network, credential, product, cleanup, deletion, and irreversible effects remain excluded
- **Dependencies:** NHC-1 and NHC-2C `PASS`; NHC-3, NHC-4, and NHC-4A superseded; self-check candidate SHA-256 `432912646224438bd47b489bf2baf1c049a3014c995e798de27b312ca036a5d7`; unit-facts candidate SHA-256 `74af70e7b14ffdfb5b4c6762b96c11e00c21d7f6795bb394f28065fdc8cd5ac8`; the other four NHC-4A input candidates retain exact hashes `8cd8acf8...f34c9`, `c3b20870...0c877`, `52ec853f...68922`, and `89995c98...8519`; no NHC-4A assignment, budget, denominator, verdict, or `PASS` transfers
- **Predicate:** the normal focused path preserves the exact 243-case host/input/root result, exact root-valued `$PATH`, the real local `mkdir → cp → tar → rm` archive-byte round trip, and nonfunctional-`cp` rejection. One added named case sets `WP6_ADMISSION_CALL_LOG` non-empty before calling the same preflight and still requires success, exact root-only `$PATH`, and the positive smoke marker; no test-control branch may keep ambient `PATH`. A scratch mutation that appends `/usr/bin` to the assembled root remains RED. Independent review verifies the five direct callers use the shared function and none grants the test log variable production command-selection authority; no source-text assertion is added to the executable test
- **Oracle:** L0/L1 `self-check.sh --host-identity` becomes an exact 244-case closed oracle: the retained 243 cases plus one non-empty-test-control success row with exact root and positive observation. It also retains the already-named scratch ambient-fallback mutation and nonfunctional-`cp` deliberate FAIL. Caller inventory is an independent finite five-file artifact review. The deciding oracle is prior, finite, a `bounded oracle`, reproducible, re-runnable, and contains no wildcard, arbitrary target, fake success environment, command simulation, or “name one” instruction. The named false-predicate failure is `observation-log-command-root-path-observation-missing`: the non-empty `WP6_ADMISSION_CALL_LOG` row must produce that failure while the shared function does not preserve the exact root-only `$PATH` and positive observation; a setup, permission, or collection failure is not that failure
- **Failure disposition:** the corrected entry freeze authorizes one direct producer/review/verifier sequence and no post-entry correction. Any producer or exit-gate failure, caller ambiguity, Unit E need, A1/Docker access, credential, or new test-control behavior stops and decomposes; no ceiling increase or wrapper
- **Next owner and order:** one fresh independent entry reviewer, then fresh continuous test-author, builder, independent artifact reviewer, and different-family verifier identities. NHC-4B remains unopened until NHC-4C `PASS`
- **Slice:** one materially different command-boundary logical slice; two writable files and forty-nine exact read-only controls; approximately 4–18 changed lines; planning denominator `1`; expected clean path seventeen events including the entry correction; hard local ceiling `18` with no post-entry correction
- **Historical cutover checkpoint:** the test author released the exact 244-case oracle at live revision 397. Revision-397 state shows NHC-4C `in-progress` since `2026-08-15T11:01:09Z`, one closed test-author assignment, and zero builder, reviewer, or verifier assignments. The retained local offline probe reproduced exit `1` and exact `observation-log-command-root-path-observation-missing` RED. NHC-4C, NHC-4B, NHC-5 through NHC-8, this completion plan, and the predecessor NanoHost plan were incomplete at that cutover

### NHC-4B — Current dependencies and one real A1 Unit E rehearsal

- **Authority:** the NHC-4C authority set after `PASS`; `docs/verification-instruments.md` real-use effect-domain and directly authorized Tier-4 builder rules; `docs/cookbooks/nanohost-real-use-host.md`; `temp/state/wp6/HANDOFF-20260812-unit-e-real-dependency.md` only as recovered implementation evidence
- **Scope:** consume the reviewed NHC-4C inputs and command root, materialize the current NanoCore deploy, NanoHost binary, accepted worker OCI, and store-seed dependency set, then let one directly authorized builder execute exactly one A1 Unit E rehearsal: manifest provision/assert first, authenticated configured readiness, stop first, exact attempt-owned cleanup, baseline restoration, manifest-owned Node and unrelated user-service preservation, and retained environment identities. No fixture substitutes for this success observation. A cleanup-proved named missing fact is a successful manifest-growth observation rather than package failure, but this freeze authorizes no second attempt
- **Mode and exact lease:** `implementation`, one execution unit. Test author writes only `temp/state/wp6/v3-preseal/self-check.sh`, replacing the impossible full-success command-stub fixture with an exact offline validator for the named real evidence archive and its finite mutations. Builder writes only `temp/state/wp6/v3-preseal/real-dependency-rehearsal.sh`, `driver.sh`, `README.md`, `tests/support/host/README.md`, `docs/cookbooks/nanohost-real-use-host.md`, `temp/state/wp6/HANDOFF-20260812-unit-e-real-dependency.md`, `temp/state/wp6/unit-e-v3-harness-builder-report.md`, fresh `temp/state/wp6/v3-dependencies/`, and fresh `temp/state/wp6/v3-real-dependency-evidence/attempt-1/`. NHC-4C command-boundary and input files plus all product source are read-only. Any later missing-fact owner or lease change is outside this freeze and requires a new exact NHC-4B subdivision matrix. Current partial rehearsal bytes are an adoption candidate only
- **Risk tier:** Tier 4 because the builder uses explicit alias `a1`, creates attempt-local credentials, starts real NanoCore/NanoHost services, uses Docker-backed dependencies, and performs bounded cleanup. Raw credential retention, TLS relaxation, authorization changes, worker/Unit F effects, deletion outside exact attempt-owned state, persistent product deployment, and irreversible effects remain excluded
- **Dependencies:** NHC-4C `PASS`; exact current source commit and reviewed candidate identities; existing source owners for a NanoCore deploy containing `dist/index.js`, NanoHost `openkit-nanohost`, accepted `worker-pi-current.oci.tar`, and verifier `wp6-store-seed`; the existing sorted checksum-receiver rule that the builder instantiates as `dependency.sha256`; explicit non-secret alias `a1`; manifest provision/assert owner and long-lived `/usr/bin/node`; no reused credential or failed attempt. The dependency root and `dependency.sha256` are outputs of this package, not prerequisites
- **Predicate:** the derived root has exactly four owned dependency classes and a complete sorted checksum receiver. The real run invokes the accepted manifest provision and assertion before Unit E effects, executes under the NHC-4C command root, reaches authenticated readiness for `integration_wp6_v3` and `deployment_wp6_v3`, then proves stop-first cleanup, byte-identical attempt baseline, three unchanged legacy services, preserved manifest Node and unrelated user NanoCore/cgroup, and no forbidden residue. The immutable archive contains exact milestones, equal baselines, zero cleanup status, host/closure identity PASS, host-preparation before/after, host baseline/restoration, bounds, complete closure checksums, `manifestDigest`, `closure_sha256`, `host_identity_sha256`, host-prerequisite digest, and dependency digest; it contains no secret, packet, public-surface, worker, sandbox, transcript, counted verdict, or NHC-3 result
- **Oracle:** the test author first releases an intended RED because the fresh evidence root is absent and fixes an exact closed archive schema plus finite missing/mismatch/unsafe-evidence mutations; it never executes a fake success rehearsal. The directly authorized builder alone performs the Tier-4 real-use A1 effect-domain attempt outside the L0–L6 test-layer taxonomy and records the subject-owned observations. Independent reviewer and different-family verifier rerun only the offline evidence/checksum/schema gates and reconcile A1 cleanup from the retained redacted facts; they inherit no SSH permission. The deciding PASS oracle is prior, finite, a `bounded oracle`, reproducible, re-runnable, and contains no “name one” instruction. A failed attempt is never a PASS oracle or a completeness search; it may only produce the one structured growth terminal below
- **Failure disposition:** an attempt that does not reach final readiness may be accepted as one successful manifest-growth iteration only when its retained subject-owned evidence proves cleanup status zero, attempt credential removal, baseline restoration, manifest-owned Node preservation, unrelated user-service/cgroup preservation, and one exact missing machine fact with the failing phase and command. The builder then stops, its lease closes, NHC-4B remains `in-progress`, and planning drift is recorded; this freeze authorizes no retry, owner edit, or lease change. The engineer has already selected the correction direction: after the exact fact, owner, lease, test, and budget are known, the orchestrator may refreeze a materially distinct NHC-4B subdivision with a new test-author/builder sequence and independent entry review, without superseding NHC-4B or opening a wrapper. Any unproved cleanup/credential/preservation predicate, product or authority defect, new dependency/evidence/effect class, Unit F effect, or generic “find another” instruction stops and escalates instead
- **Next owner and order:** after NHC-4C `PASS`, the corrected NHC-4B freeze receives a fresh independent entry review before any producer opens, then fresh test author, fresh directly authorized Tier-4 builder, independent artifact reviewer, different-family verifier, and direct human scrutiny. No NHC-4 or NHC-4A identity, budget, or verdict transfers
- **Slice:** one materially distinct real-effect logical slice; one test file, seven source/projection files, and two fresh derived temp roots; approximately 180–450 changed source lines plus dependencies/evidence; planning denominator `1`; expected clean path fifteen events; hard local ceiling `20`. A growth terminal ends this freeze and does not consume a second execution unit under this denominator

### NHC-5 — Unit F F1-F4 and Aggregate

`PASS` on 2026-08-26. The retained A1 execution used the existing runner and current Codex subscription, exited zero after approximately 30 minutes, and recorded F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS` with four scenario rows and no failure record. The recoverable redacted result is `temp/state/wp6/wp6-f-gate/unit-f-aggregate-20260826T065543Z.json`; the directory remains ignored evidence rather than repository authority. The daily real feedback path remains the separate fresh-Workspace `sleep 1` Task plus ordinary NanoHost reset, which completed in 133 seconds without adding a runner mode, fixture, or script.

### NHC-6 — Retired-path deletion

`PASS` on 2026-08-26. After NHC-5 fault acceptance and the engineer's explicit deletion direction, the retired Cell helper and controller, direct OpenShell CLI backend, SSH restart runner, root commands, selector surface, and obsolete tests were deleted without replacement. The current NanoHost backend contract and one canonical-package regression remain; NanoCore typecheck, lint, build, five focused files `95/95`, exact absence search, and `git diff --check` pass.

### NHC-7 — Current projection alignment

`PASS` on 2026-08-26. The obsolete Cell deployment procedure was deleted and current manuals, cookbooks, roadmap, package guides, verification rules, test strategy, index, and accepted implementation projections now describe one configured NanoHost RuntimeTarget, the retained Unit F `PASS`, and deleted legacy execution source. The intentionally Deprecated Cell specification remains at the root pending its governance-required auditor archive. Documentation validation covers 196 documents, documentation field and lifecycle tests pass `104/104`, the verification-instrument contract passes `17/17`, and `git diff --check` passes. NHC-FND-105 records the historical `0.0.80` snapshot's one policy-conformance consumer without expanding this slice into policy architecture.

### NHC-8 — Verification and closeout

`PASS` at its declared gate on 2026-08-27. Exact A1 acceptance at commit `902c5011` completed F1, F2, F3, F4, the ordinary lifecycle, and Aggregate with every verdict `PASS`; the separate fresh-Workspace `sleep 1` Task completed in 18.875 seconds with three product-safe RuntimeEvidence records and did not gate Unit F. NHC-FND-115 records the engineer-authorized credential reset through the existing owner, and NHC-FND-116 records the ignored operator's corrected SSH-forward ownership. The one unconditional independent review returned `PASS` after inspecting the accepted owner, actual diff, retained evidence identities, operator port release, `71/71` focused runner result, and clean Git state. The one final `pnpm verify:full` run exited zero against the accumulated candidate, including repository and documentation checks, lint, typecheck, unit tests, coverage, builds, NanoCore E2E `20/20`, Web E2E `14/14`, and both built-artifact smokes. Later findings reconciliation proved that this closeout omitted NHC-FND-054's still-unmet NHC-IMP-5O real-host noninterference gate, so the recorded NHC-8 package result does not close the plan.

## Verification

- The committed Unit F runner passes `71/71`; exact A1 commit `902c5011` records F1 through F4, the ordinary lifecycle, and Aggregate all `PASS` with no failure record.
- The separate fresh-Workspace fast Task completed in 18.875 seconds and retained exactly three product-safe RuntimeEvidence records; no signup, session preparation, or fast Task gated Unit F.
- One independent final review returned `PASS` with no deciding Safety Kernel, owner, oracle, temporary-artifact, or simplicity finding.
- One final `pnpm verify:full` run exited zero: repository and documentation checks, lint, typecheck, unit tests, coverage, builds, NanoCore E2E `20/20`, Web E2E `14/14`, NanoCore dual-listener smoke, and Web built-artifact smoke all passed.
- The A1 deployment remained healthy after terminal cleanup, and the ignored operator released its preflight SSH forward before the runner acquired the same port.
- NHC-FND-054 remains open because no retained artifact proves every NHC-IMP-5O A1 namespace, slirp, forbidden-host-surface, system-Docker, business-container, and build-network predicate.

## Closeout

This retained closeout candidate is anchored by exact A1 product commit `902c5011` and evidence checkpoint `17ad8102`. It retains the accepted NanoHost runtime, RuntimeEvidence failure projection, retired-path deletion, current documentation projections, separate App HTTP/1.1/SSE and NanoHost native HTTP/2 listeners, and ordinary App clients. No temporary diagnostic, operator script, runner mode, fixture framework, compatibility path, or new evidence owner is committed. It is not terminal while NHC-FND-054 remains open.

The deferred adjacent findings do not block this plan: NHC-FND-045 still requires a separate accepted owner before a locked-out server can recover an operator credential; NHC-FND-052 still requires its own cross-owner Git-write redesign; NHC-FND-105 retains one historical policy-conformance consumer; and the former poisoned A1 data root remains preserved as recovery evidence rather than repaired. NHC-FND-054 is different: it remains open inside this plan because its accepted NHC-IMP-5O real-host noninterference gate has no retained terminal proof. Earlier finding-specific sentences that say Aggregate or real A1 acceptance was pending record the observation at that historical stage; the NHC-8 result supersedes those statements only where their deciding gate is among the exact evidence it records.

## Retained Working Evidence

Historical Unit E/F references remain in this plan and its findings. The current `temp/state/wp6/wp6-f-gate/` archive now contains the recoverable redacted NHC-5 F1-F4/Aggregate `PASS` result. Any retained files under `temp/state/wp6/` are evidence, not authority, and remain undeleted until the three open product plans close.

## Adaptive-loop pilot cutover

### Intent Epoch 1 — 2026-08-20 / 515a7e9cd1154e4b39a24574dc71885f5f32b94a (append-only)

The accepted product, ownership, and strict-risk intent in this record is unchanged. Its earlier package queue, matrices, assignments, gates, events, exact leases, ceilings, denominators, and fixed role sequence remain historical Evidence only and no longer authorize dispatch. Accepted decisions, observed facts, and produced artifacts remain inputs to current work; none is deleted or reconstructed.

### Intent Epoch 2 — 2026-08-21 / current adaptive-loop execution (append-only)

The engineer fixed `e70ad9d9e0d69869dd4765231beb01f61f53fc3f` as this plan's NanoHost product implementation baseline and authorized completion, closeout, and one final commit. `e70ad9d9..HEAD` changes governance and the adaptive Agent loop but does not change `apps/nanohost`, `apps/nanocore/src`, `packages/worker-shim`, or the accepted NanoHost owner set, so those later commits are execution authority rather than NanoHost progress.

### Intent Epoch 3 — 2026-08-21 / engineer-accepted Harness carriage (append-only)

The engineer accepted an Integration-initiated private pull protocol under the existing `/worker-control/*` namespace, bound to the current connection and Harness and explicitly outside both the eight NanoHost effect pairs and the per-Turn worker `interrupt` queue. The owning specifications must define and pass independent Verifier and engineer-commissioned Auditor review before this plan records or begins implementation; after implementation, the plan resumes its current test and acceptance work. The engineer also authorized any reasonable sensitive operation on the dedicated `a1` test server, while credential confidentiality, exact-target checks, cleanup, retained evidence, and every other Safety Kernel obligation remain unchanged.

### Intent Epoch 4 — 2026-08-21 / independent AgentSession instances (append-only)

The engineer clarified that multiple AgentSessions in one Sandbox do not mean one OS Agent process or native runtime instance must be shared among them. The one declared Harness may start and supervise multiple independent Codex instances, one per resident AgentSession as required; later Turns preserve the exact AgentSession conversation through its restricted native handle but need not reuse the same process instance. This keeps independent AgentSession identity and native continuity without making process identity part of the Core contract.

### Intent Epoch 5 — 2026-08-21 / accepted shared-Harness owning specifications (append-only)

The amended AgentSession, continuity, worker-runtime, Codex, OpenCode, Pi, worker-control, and NanoHost owners passed independent Verifier and Auditor review without condition after closing the first-Turn pending-handle lifecycle, immutable adapter-mode interrupt ownership, one shared process-group supervisor, terminal-CAS races, and the private Harness poll's 250–1000 ms start-to-start cadence. The accepted target has exactly two adapter modes, gives only Codex `session-continuity`, keeps OpenCode and Pi `bounded-turn`, adds only `POST /worker-control/harness/poll` and `POST /worker-control/harness/result`, and preserves the eight NanoHost effects, three semantic namespaces, one nested connection, and non-durable raw Turn credentials.

### Accepted shared-Harness implementation program

The adaptive-loop implementation replaces the obsolete NHC-4C package sequence for product work and proceeds through these dependency-ordered slices. Each slice begins with its lowest-sufficient regression, reuses the current NanoHost dispatcher, scheduler lease, route-token hash owner, terminal compare-and-set, and worker-shim process-group supervisor, and adds no abstraction for a second Harness, adapter family, backend, or concurrent Turn.

1. **NHC-IMP-1 — Private Harness authority and sequencing:** add the minimum Core storage projections for `SandboxRuntimeRecord`, `HarnessInstanceRecord`, and `AgentSessionRuntimeBinding`; retain the existing scheduler session lease as the distinct Turn execution lease; implement one queued-or-dispatched Harness operation, deterministic redacted identity, dispatch-time token-hash binding, exact result settlement, non-redelivery, and unknown cleanup widening.
2. **NHC-IMP-2 — Static bridge and private carriage:** change `bridge.open` to the static non-secret Harness descriptor and `harnessBindingRef`; add the two exact private routes to the existing authoritative NanoHost session dispatcher; make NanoHost inject the binding only for those routes; reject inner bearer or binding headers; and require the first exact empty poll `204` before bridge readiness.
3. **NHC-IMP-3 — Shared Integration and Codex continuity:** keep one long-lived worker-shim Integration client and one shared process-group supervisor; implement the six fixed operations, one active Turn, multiple AgentSession-private roots, pending first start, exact Codex thread UUID collection and inspection, exact UUID resume in a fresh process, mode-exclusive interrupt delivery, sibling-preserving close, and 250–1000 ms private poll cadence.
4. **NHC-IMP-4 — NanoCore shared-Sandbox lifecycle:** reuse one compatible Sandbox and Harness across at least two open AgentSessions; compute only `SandboxCompatibilityKey` and `AgentSessionCompatibilityKey`; import fresh per-Turn package and Context data, create distinct route credentials and leases, preserve idle open-session capacity without effect authority, reject incompatible reuse, and widen uncertain cleanup before capacity returns. `HarnessCompatibilityKey`, concurrent active Turns, off-peak rebuild, a second Harness, and a second adapter implementation remain absent.
5. **NHC-IMP-5 — Real acceptance and closeout continuation:** run focused and full gates, obtain one final local implementation audit, create a local implementation checkpoint commit, deploy that exact commit to `a1`, prove authenticated readiness, two Codex AgentSessions with exact resume and sibling isolation, interrupt and cleanup behavior, and the retained Unit E and Unit F scenarios, then resume retired-path deletion, projection alignment, independent final acceptance, plan closeout, and the final closeout commit.

### NHC-IMP-5A — Effect-free restart classification and ordinary cleanup drain

The first real A1 recovery attempt exposed an implementation contradiction inside NHC-IMP-5 rather than a new architecture decision. Phase 8 wrote the exact `cleanup-pending` owner and then awaited NanoHost `bridge.close` or `sandbox.delete` before the only ordinary listener could admit the authoritative NanoHost connection that carries those effects; Node therefore exited with unsettled top-level await and code 13. Independent Verifier review returned `PASS` for the accepted-owner correction: phase 8 must perform only durable classification, compare-and-set fencing, read-only restoration, and capacity preservation, while the existing post-listen scheduler maintenance seam serially resumes `cleanup-pending`, `cleanup-failed`, and effectful accepted-final-status closeout through the existing owners. The implementation adds no state machine, durable queue, timer family, listener, route, connection, or replay path, and A1 product state must not be edited or bypassed before the corrected ordinary path consumes its real durable row.

### NHC-IMP-5B — Fresh-epoch cleanup and unique capacity authority

The corrected post-listener drain exposed the next real A1 boundary. A fresh Runtime Epoch with no current Sandbox, bridge, or Harness monitor received the predecessor cleanup's `bridge.close`; the coordinator took an absent bridge before validating live identity and failed stop. Existing NanoHost authority permits the fresh coordinator to settle exact `bridge.close` and `sandbox.delete` as definite absence without an OpenShell call only when every relevant local object is absent. The focused implementation validates the request and exact live Sandbox before any destructive effect, returns `closed` or `deleted` for the all-absent case, and rejects partial, reordered, mismatched, or auxiliary-bearing state. NanoHost's complete `67 + 1` Rust tests, format check, and warning-denying Clippy check passed, and an independent Verifier accepted the exact implementation bytes without correction.

The same attempt also exposed NHC-FND-027: the durable scheduler owner currently ties one Turn lease and configured slot to Sandbox deletion, while the accepted shared-Sandbox owner retains that Sandbox across sequential Turns and assigns the Turn lease only one active-Turn unit. The dormant `nanohost_runtime_targets.active_lease_id` and `capacity_state` helpers cannot be wired as a second capacity owner. Intent Epoch 6 records the engineer's resolution: preserve shared-Sandbox reuse, keep scheduler lease/capacity rows as the unique active-Turn grant, keep `HarnessInstanceRecord.active_turn_count` as an occupancy projection only, and make `SandboxRuntimeRecord` the physical-Sandbox lifecycle projection.

### Intent Epoch 6 — 2026-08-21 / hidden AgentSession and unique active-Turn authority (append-only)

The engineer accepted AgentSession as Core's hidden runtime-continuity identity rather than a user-visible conversation or physical connection. The user model remains `Workspace -> Thread -> Turn -> Item[]`; one AgentSession binds one Thread for life and spans sequential Turns, while one Thread has zero or more historical AgentSessions and at most one current AgentSession. Exact network reconnect or a fresh native process with the exact restricted handle preserves that AgentSession; unproved continuity retires or fences the predecessor before one internal successor may become current, and an active Turn never moves or replays through that successor. Ordinary product surfaces expose only continue Thread or new Thread and no AgentSession identity, history, picker, restart, or navigation action. One compatible Harness may host multiple AgentSessions only for distinct Threads and still has `maxActiveTurns=1` in this slice.

The engineer also accepted `scheduler_session_leases` plus `scheduler_capacity_records` as the unique active-Turn capacity grant. `HarnessInstanceRecord.active_turn_count` is its runtime occupancy projection, `SandboxRuntimeRecord` owns retained physical-Sandbox lifecycle, and ordinary Turn or AgentSession close releases only its exact local occupancy after terminal and quiescence proof while preserving a compatible shared Sandbox. Destructive uncertainty preserves the existing cleanup owner and fences affected capacity until definite deletion or post-fence fresh-coordinator readiness. The dormant RuntimeTarget mutable lease/capacity fields and helpers are deleted rather than wired into production.

For accepted effects across NanoCore restart, an existing durable cleanup or accepted-final-status owner may register only a result-only expectation whose complete deterministic `requestId` is re-derived from immutable existing lineage. It stores no command, token, queue, journal, settlement state, or replay authority. An exact retained result settles the same owner; a successor that polls first marks the old accepted effect unknown once, preserves cleanup and capacity fences, and fences the connection. A successor with no retained result that loses its first poll response after poll start fail-stops the Runtime Epoch and cannot reconnect the same coordinator. No ninth NanoHost effect, fourth semantic namespace, second connection, second timer family, or new durable owner is introduced.

The amended Core and owning specifications were committed as `a93e023d` after independent Verifier and Auditor `PASS` on the same diff hash `586821873522d01ec7b641029d7df986471e7342b72350f33774612258fbf2c9`. Documentation model, lifecycle, generated index, story schema, Agent Skill reachability, root contract, documentation projections, and diff checks passed under the repository's Mise-managed Node `24.16.0`. This acceptance authorizes implementation but does not pre-accept code or A1 evidence.

### Intent Epoch 7 — 2026-08-22 / disposable A1 reset authorization (append-only)

The engineer clarified that `a1` is a test-only server with no external service obligation and authorized any reasonable safe operation needed to finish NanoHost acceptance, including stopping and deleting the existing OpenKit NanoCore, NanoHost, Gateway, private runtime members, containers, data roots, Image Store, evidence, and credential-slot residue. This removes the prior requirement to preserve the current A1 OpenKit deployment or construct a parallel sibling. The authorization does not extend to actions that could brick the host operating system, SSH access, system Docker installation, boot chain, or unrelated services, and it does not relax credential confidentiality, exact-target resolution, fail-closed interruption handling, cleanup proof, or retained evidence.

### Intent Epoch 8 — 2026-08-22 / external source storage and Sandbox-owned Git (append-only)

The engineer rejected a host-local unpublished Git repository on NanoCore as a remote Agent Runtime source and rejected a special NanoHost Git-bundle ingress for that case. NanoCore may retain bounded product records, agent work data, Artifacts, uploads, evidence, and handoff data through their existing owners, but it is not a general repository, object, or bulk-file storage service. Repository text and code must use a network-addressable external Git service; large static source data beyond accepted bounded NanoCore owners must use external object storage such as S3-compatible storage. Git and hosting clients run inside the Sandbox under current source, permission, approval, Vault, and network policy, while NanoHost owns only Sandbox lifecycle, transport, and network-policy enforcement and no Git, repository, branch, commit, push, or pull-request semantics.

### Intent Epoch 9 — 2026-08-31 / Roadmap R001 completion and pull-request authorization (append-only)

Source: the engineer's current instruction to complete Roadmap outcomes phase by phase through one independently consulted, implemented, reviewed, committed, pushed, and proposed batch at a time.

The outcome for this continuation is Roadmap `R001`: prove that NanoHost runs the existing real Worker Unit F workload while normal start, normal stop, an effect-capable member failure, and NanoHost `SIGKILL` preserve the accepted closed projection of unrelated host networking, system Docker, business containers, services, and user-owned data boundaries. `NHC-IMP-5O`, this plan's current checkpoint, and `NHC-FND-054` remain the unique durable owners; no second change plan, runner, lifecycle, framework, product mechanism, compatibility path, or specification decision is authorized.

Acceptance requires an admitted existing Unit F harness whose retained result directly settles namespace topology, exact slirp identity and fixed arguments, dockerd fixed DNS projection, TAP and default-route readiness, private-namespace inability to reach the host NanoCore listener or an attempt-local host-loopback sentinel, NanoHost service-root inability to open `/run/docker.sock`, real Worker remote Git checkout, the existing system Docker and business-container baselines, bounded build-network smoke, cleanup, credential removal, and residue absence. The closed service projection is only the system Docker daemon, the business containers including NanoCore, and the SSH/systemd execution channel continuously exercised by the gate; user-data acceptance means the attempt writes only accepted product, harness, and temporary evidence paths and does not inspect or claim preservation of arbitrary unknown bytes.

Implementation reuses `tests/nanohost-unit-f-runner.test.mjs` and `tests/support/host/nanohost-unit-f-runner.mjs`. `nsenter` is verification-only, is used only with `-n` after exact member starttime, executable, and network-namespace revalidation, and is never added to the host manifest or invoked with `-m` or `-a`; the service-root boundary is observed through `/proc/<exact-nanohost-pid>/root`. Public Git egress is decided only by the existing policy-governed real Worker checkout, and the loopback sentinel plus negative network probe share one bounded sudo Node process and one cleanup-complete terminal path.

The existing A1 authorization and its exact-target, confidentiality, cleanup, and unrelated-host preservation constraints remain unchanged. Before A1 contact, local mutation-sensitive admission, focused checks, independent review, a fresh-context verifier direction check, and the engineer-directed audit must find no blocker. Any failure keeps NanoHost stopped, leaves `R001` unfinished and `NHC-FND-054` open, and changes product code only when direct evidence proves an owned product defect; a design, authority, credential, effect-boundary, or important residual-risk change returns to the engineer.

For this R001 closeout only, the engineer authorizes the current `feat/r001-nanohost-noninterference` feature branch, one coherent Conventional Commit, a push to `origin`, and one pull request after direct acceptance evidence and final independent review. This supersedes the historical no-push sentence only for that exact closeout and does not authorize merge, deployment beyond the existing A1 test boundary, release publication, history rewrite, amend, or unrelated external effects.

### Intent Epoch 10 — 2026-08-31 / local A1 project-use and provider authorization (append-only)

Source: the engineer's resumed instruction after the prerequisite blocker.

The engineer confirms that `a1` is this machine and is dedicated to testing and development, authorizes the primary to perform any project-related use of it, and rejects additional safety barriers around reversible OpenKit deployment work. This resolves the prior checkpoint's operator-only SSH and container-restoration restriction and reaffirms Intent Epoch 7's disposable OpenKit reset authority. The primary may configure strict local SSH trust, start, stop, replace, or recreate project containers and services, replace disposable OpenKit data and credential state through existing product owners, prepare required images and provider state, and run the accepted A1 gates without another effect-authorization round. Host operating-system damage, loss of SSH access, unrelated services, credential disclosure, and publication beyond the separately authorized branch push and pull request remain excluded by the repository Safety Kernel.

If a real Worker requires an LLM provider, the engineer authorizes either the supplied OpenRouter credential with the free-model route or this machine's existing Codex subscription authorization. Both are protected secret inputs: committed documents, commands, argv, logs, retained evidence, and review transcripts record only the selected public provider identity and presence facts, never credential material. Existing Vault, provider-subscription, NanoCore inference, and Worker bootstrap owners remain the only delivery path; this authorization adds no product API, provider abstraction, fallback framework, compatibility behavior, or durable secret store.

### Intent Epoch 11 — 2026-08-31 / direct A1 terminal-root Reframe (append-only)

Source: the two retained formal A1 failures, one bounded same-driver diagnostic, the accepted decommission owner, one independent Cursor CLI consultation, and two fresh read-only verifier judgments.

Formal attempt `2025cf74-164b-4443-b474-a856f038c443` failed closed with evidence digest `e042e859098e5124cdb821908eb37f02f9c32874ca5ebb5616f61a83a919d693` because the runner captured its pre-attempt baseline before starting NanoHost. The test-first correction starts the service only after all preflight checks and before the baseline, preserves cooperative interruption and the one terminal finalizer, and passes the focused suite. Formal attempt `aefaf995-e26b-4cff-bdec-de1c2579a2af` failed closed with evidence digest `a274de95229e19587c4f8dba815709b607bac5b8ab399950d13a41c06520d2e3`; a bounded same-driver diagnostic then proved service start and baseline success followed by an App API `404`. The corrected existing start owner waits through the existing tunnel for one exact RuntimeTarget whose `predecessorFenced`, `ready`, and `freshEmpty` values are all true before it settles, and the mutation-sensitive focused suite now passes 138 of 138.

Both formal attempts and the diagnostic completed the existing service stop, authenticated identity decommission, credential removal, and independent system-Docker smoke while leaving the current exact state and runtime epoch roots after the service became inactive. This falsifies the terminal finalizer's assumption that those roots disappear without an operator removal step. It does not establish a NanoHost product defect: the accepted specification explicitly says stopping the service alone is not decommission proof, assigns execution-host content removal to the explicit operator or uninstall path, and preserves prior roots for next-start NanoHost-absent recovery. The independent Cursor consultant and the final fresh adjudication verifier therefore select the existing Unit F terminal finalizer as the smallest owner and reject cooperative NanoHost signal handling, systemd `ExecStopPost`, a second teardown script, and any Core or specification change.

The bounded correction adds one explicit operator action inside the existing finalizer after exact terminal-epoch capture, service stop, App API decommission, service-inactive proof, and independent cgroup, network-namespace, and socket absence. It must validate one attempt-observed state root below `/var/lib/openkit/nanohost/epoch-*` and one matching runtime root below `/run/openkit/nanohost/epoch-*`, reject a changed shape or any additional epoch root without broader deletion, remove only that exact pair, and reobserve both absent before Aggregate may pass. Failure remains a retained terminal `FAIL` while later independent baseline and smoke checks continue. This R001 action removes no Image Store, retained invalidation evidence, configuration, or unrelated host content and must not be described as complete uninstall or complete host-decommission conformance.

### Intent Epoch 12 — 2026-09-01 / failed-unit terminal normalization (append-only)

Source: formal A1 attempt `7bc9a695-17b6-4c53-a577-880c039087c5`, retained evidence digest `df70439499a69b900a1c673d172c717015da47b14efc6fa833728077f9a2a6ae`, exact systemd state, NanoHost journal, and one test-first correction in the existing terminal owner.

The attempt failed before its baseline with `pre_attempt_start_failed`. The exact journal proves that the retained rebuild fence exceeded the accepted 300-second hard bound: containerd, dockerd, and the stock OpenShell Gateway reached health, NanoHost then correctly rejected epoch publication and refreshed the fence before exiting one. No service process remained and systemd reported `ActiveState=failed`, but the runner accepted only the literal `inactive` state, emitted `service_still_active`, and consequently skipped the exact terminal-root operator cleanup. The result was retained with mode `0600`, its digest matched stdout, both protected credentials were absent, authenticated decommission and credential removal succeeded, and the failed artifact is preserved under its attempt-specific filename.

This is a runner finalization defect, not a NanoHost product-lifecycle change. The smallest correction reuses one shared runner helper for both scenario and terminal stop: it executes the existing `systemctl stop` and then systemd's native `reset-failed` for the same exact unit, allowing the existing strict `ActiveState=inactive` proof and terminal-root cleanup to proceed after an already-failed start. One mutation-sensitive regression failed against the old single command and passes with the exact two-command sequence; the complete focused suite now passes 148 of 148. No alternate inactive definition, product signal path, retry loop, systemd unit change, compatibility behavior, or broader deletion is added.

### Intent Epoch 13 — 2026-09-01 / no A1 host reboot in R001 acceptance (append-only)

Source: the engineer's instruction after formal attempt `49de4163-e38d-44fe-8eca-5c5301dfdd5d` interrupted the colocated Codex App Server, Herdr, and runner by invoking the existing F3 `/usr/bin/systemctl reboot` path.

The R001 gate must not reboot, shut down, or equivalently reset this A1 server. Its exact deciding scenario set is F1, F2, and F4 plus normal lifecycle, the accepted network-conformance matrix, and terminal cleanup. F1 directly covers NanoCore-only restart during a real Worker checkout, F2 covers NanoHost `SIGKILL`, and F4 covers effect-capable member failure; F2 and F4 also exercise the existing fresh-ready, predecessor-fencing, image-revalidation, interruption, and cleanup owners. No simulated reboot, mass service kill, userspace restart, boot marker, fourth fault, runner option, alternate mode, or external controller is added for R001.

F3's machine-wide restart ordering and truthful post-restart recovery belong to Roadmap R005 rather than R001 noninterference. The current A1 topology cannot retain that evidence because its controller runs on the tested host, and its shared kernel boot identity did not change under the interrupted attempt's systemd reboot. The R001 runner therefore deletes the F3 contract and every reachable reboot-specific port, waiter, branch, and command binding instead of merely skipping it. A deferred finding preserves Scenario 3's unresolved evidence obligation for R005; reactivation requires a fresh engineer-approved effect boundary and either an off-box controller on a suitable execution host or a separately accepted direct non-reboot oracle.

### Intent Epoch 14 — 2026-09-01 / F4 successor-before-cleanup recovery ordering (append-only)

Source: no-reboot formal A1 attempt `894c6394-281a-4039-a3ff-d328524b216a`, its retained credential-free result digest `7e1a61ec9a9e41ea8986eb45652f6b7517f753ce63933dfc1dcfcff7b87c563b`, exact NanoHost journal, exact lease and backend projections, and the succeeding ordinary project-service recovery.

The attempt completed F1, entered F2 and F4 through service- and member-scoped faults only, and retained Aggregate `FAIL` without rebooting A1. F4 reached one accepted `sandbox.create` whose response became unknown after the exact Gateway failure; two successor epochs then fail-stopped on the accepted poll-first `409`, while the runner waited for backend capacity release before starting the fresh successor that owns that cleanup. The retained backend remained `cleanup-failed` and its lease remained acquired until the ordinary operator re-enrolled the same disposable identity and started only `openkit-nanohost.service`; that successor immediately restored fresh-ready generation `93`, changed the lease to `failed` for `scheduler-restart-backend-cleanup`, and recorded the backend physically cleaned. This direct evidence establishes an execution-order defect in the existing gate, not a new product lifecycle or permission to mutate scheduler rows.

The smallest correction stays inside `recoverFreshEpochSequence`: when the post-fence proof successor is nonready, first prove its exact fail-stop and terminal journal row, start the exact fresh successor, and wait for its next-generation fresh-ready target before waiting for the existing durable cleanup projection. One regression withholds cleanup until that third start and failed against the old order. The same attempt also reproduced a systemd race in which `reset-failed` can report the exact static unit as not loaded after `stop` has already made it inactive; the shared stop owner now accepts that reset failure only after a separate native `ActiveState=inactive` proof, and a negative regression requires the original reset error when the exact unit remains active. The three focused regressions pass `3/3`, and the combined host projection suite passes `211/211`. No retry loop, alternate mode, new cleanup owner, direct database mutation, host reboot, or equivalent reset is added.

### Intent Epoch 15 — 2026-09-01 / pre-attempt system-Docker smoke admission (append-only)

Source: retained no-reboot A1 attempt `2e7802af-231d-49c8-9e58-3a8e67b310a5`, exact scenario baselines, source-order inspection, one bounded NanoCore-only restart probe, and one test-first local correction.

The attempt accepted F1 and F4, every F2 fault and recovery proof, normal lifecycle, and every terminal cleanup row, but F2 failed because its pre-baseline changed during the attempt's first system-Docker build-network smoke. F4 and normal lifecycle reused the initialized smoke owner and preserved the resulting baseline. A bounded alternate-cause probe restarted only the NanoCore project container and sampled the exact baseline payload at ready through thirty seconds; every post-ready digest was identical, excluding delayed F1 attachment settlement. The smallest correction reuses the terminal attempt's existing smoke port once before admitting the discarded pre-attempt baseline, while F2, F4, normal lifecycle, and terminal cleanup retain their existing smoke checks. One ordering regression failed against `start -> baseline -> gate` and passes against `start -> smoke -> baseline -> gate`; the combined host projection suite passes `212/212`, targeted formatting, syntax, repository, and diff checks pass, and independent review returned `PASS` with no finding. A fresh-context verifier independently returned `Continue` after confirming that the discarded prewarm cannot mask any scenario-owned comparison, the executable has no F3 or host-reset path, retained failed-attempt cleanup is complete, and the same checks pass from its context. No new phase runner, retry, durable state, failure vocabulary, product behavior, Docker-daemon restart, host reboot, or equivalent reset is added.

### Intent Epoch 16 — 2026-09-01 / F1 exact NanoCore restart delta (append-only)

Source: retained no-reboot A1 attempt `e8b3d86d-988b-4978-97dd-f819c59bf29c`, independent artifact audit, one bounded NanoCore project-container restart diagnostic, and test-first local correction.

The attempt recorded exact F1, F2, F4, network conformance, normal lifecycle, terminal cleanup, and Aggregate `PASS`, but independent audit blocked R001 because F1's raw system-Docker baseline changed while the adjudicator unconditionally admitted every F1 mismatch. The artifact is preserved under its attempt identity with mode `0600` and digest `c1e8a70e0b78d39531e76dd9dd92ee630574c2e0d926b6ad1a9b2bce9cbe4d03`; it closes NHC-FND-054 and NHC-FND-119 but does not close R001. A bounded diagnostic restarted only `openkit-nanocore-a1` and compared the complete existing payload before and after readiness; exactly the configured NanoCore container's `networks.host.endpointId` changed, while bridge, `docker0`, nftables, all other NanoCore fields, and every other container identity and attachment remained equal.

The smallest correction keeps the raw baseline digest and adds one restart-invariant digest whose only normalization is the unique exact configured NanoCore container's unique `host` network 64-hex endpoint ID. F1 requires invariant equality and retains both invariant and raw pre/post digests; F2 and F4 continue to require raw equality. Missing, duplicate, differently networked, malformed, or otherwise changed input fails closed. The deciding tests first produced two expected adjudication failures and one missing-export failure, then pass `4/4`; the combined host projection suite passes `215/215`; targeted Biome, syntax, repository, and diff checks pass; and independent review returned `PASS` after one retained-truthfulness correction required raw F1 digests to remain serialized. A fresh-context verifier independently returned `Continue`, reproduced all deciding checks, confirmed the fixed result path absent and failed-attempt cleanup complete, and found no F3, reboot, shutdown, or equivalent host-reset capability. No component list, retry, alternate runner, durable state, product behavior, host reboot, or equivalent reset is added.

### Intent Epoch 17 — 2026-09-01 / retained no-reboot R001 acceptance (append-only)

Source: retained no-reboot attempts `c6f7de35-925a-4363-baed-f8c80827e366` and `8e5a3c1a-4ec7-4ca7-b150-5f7526b72f70`, bounded phase-isolation diagnostics, exact terminal observations, and independent artifact audit.

The first corrected-instrument attempt retained Aggregate `FAIL` with credential-free digest `6f0e631095e1cff0bf5afc6a61cc803b74ffd64dc8f6b978f4ad795f73f2153b` because F1's restart-invariant digest changed while F2, F4, network conformance, normal lifecycle, and terminal cleanup passed. The result was archived under its attempt identity rather than retried blindly. A complete-payload stability sample remained exact for four minutes, the network-conformance collector changed no baseline leaf, and a separately instrumented real F1 changed only the configured NanoCore container's `networks.host.endpointId`; both pre/post invariant digests were `7602ee63c71975f5251d352b153756bde0302a684a694c62df93d17f22b13ab2`. Each diagnostic used only the existing NanoCore project-container and exact NanoHost service lifecycle; A1, Codex App Server, and Herdr were not rebooted or reset.

The final exact gate retained mode-`0600` artifact digest `faa5f7d417ac643998895fc901a480203dd3e9609b080a4c6119a1c5af949072`. F1 passed with equal restart-invariant digests while retaining distinct raw digests, F2 and F4 passed with equal raw digests, and network conformance, normal lifecycle, terminal cleanup, and Aggregate all passed. The terminal owner stopped the service, decommissioned the identity, removed credentials and epoch residue, preserved the baseline, and passed the system-Docker build-network smoke with no primary or cleanup reason. Independent audit inspected the artifact and current instrument bytes and returned `PASS` for closing NHC-FND-120 and Roadmap R001. Scenario 3 remains explicitly deferred to R005 under NHC-FND-117; no reboot capability, simulation, alternate runner, retry mode, product state, or new lifecycle owner was added.

### NHC-IMP-5C — AgentSession, capacity, and accepted-effect recovery completion

1. **Current AgentSession and product boundary:** add the lowest-sufficient store and admission regressions, enforce one current AgentSession per Thread without a new durable entity, reuse the existing session-workspace compatibility planner for later same-Thread Turns, keep replacement preview read-only until exact scheduler dispatch and lease admission, retire or fence a predecessor only through the post-dispatch commit before successor creation, import only terminal stale AgentSession history, reject a second same-Thread Harness binding, and remove nested as well as top-level ordinary App API, Core Client, Web, CLI, schema, event, and OpenAPI AgentSession identity or actions while retaining protected redacted operator lineage and full Turn validation.
2. **Unique active-Turn capacity and retained Sandbox:** delete RuntimeTarget `active_lease_id`, mutable `capacity_state`, and their unused claim/settle helpers through the next existing migration; gate admission on the existing readiness projection without making it capacity authority; project scheduler claims and releases into Harness occupancy; make definite cleanup remove the exact durable Sandbox projection even when no process-local session exists; and make destructive uncertainty fence Sandbox reuse and affected capacity until the existing recovery boundary proves release.
3. **Effect-free boot and result-only recovery:** keep Phase 8 effect-free, extend the existing post-listener scheduler maintenance service to serially drain cleanup and accepted-final-status closeout, reconstruct only deterministic result-only expectations from existing lineage, settle same-result without replay, treat the successor's first poll of any outer operation as poll-first proof for the one pending result-only group, commit unknown exactly once, fence the successor, and implement the NanoHost first-poll response-loss terminal classifier by reusing its existing reconnect state and epoch supervisor.
4. **Acceptance continuation:** run the focused AgentSession, scheduler, NanoHost dispatcher, worker-shim, migration, App API, OpenAPI, and Rust regressions; run proportionate package and full gates without concurrent build contention; obtain independent implementation review; deploy those exact bytes to `a1`; require the ordinary product path to consume the existing real `cleanup-pending` row without database editing or fixture bypass; then continue NHC-IMP-5 real worker, reconnect, interrupt, cleanup, Unit E/F, retired-path deletion, projection alignment, and final closeout.

### NHC-IMP-5D — Loopback h2c and exact retained-identity re-enrollment

1. **Loopback h2c host harness:** keep the accepted loopback-HTTP exception and add `--http2-prior-knowledge` only to already-validated `http://` readiness and decommission curl calls; preserve HTTPS with zero additional argument, token delivery through curl config stdin, every timeout and failure option, and existing cleanup. The focused image and host projections must prove both loopback calls carry the option and both HTTPS calls exist without it.
2. **Exclusive enrollment slot:** before enrollment or re-enrollment writes a named slot, require both configured files absent and create them without overwrite. A failed attempt may remove only files whose companion still proves the exact attempted Token id; missing, malformed, replaced, differently owned, partial, or otherwise unproved cleanup leaves the identity absent or decommissioned and blocks that slot until explicit operator proof of emptiness. Reuse the existing sink owner and add no lock service, coordinator, retry loop, durable state, or compatibility path.
3. **Exact retained identity transaction:** inside the existing enrollment database transaction, insert only when neither configured identity nor deployment exists; reactivate only the single exact decommissioned identity/deployment row by preserving `created_at`, clearing `decommissioned_at`, and creating one fresh Token; reject active, missing-conflict, duplicate, cross-identity, and cross-deployment state without mutation. Every old Token remains revoked, and no prior connection generation, Runtime Epoch, Sandbox, Harness, AgentSession, capacity, or cleanup result is restored.
4. **Acceptance continuation:** begin with one route-level RED covering enroll, decommission, and same-pair re-enrollment plus concurrent or stale same-slot loss; retain the first identity row and revoked Token history; prove cross-binding and cleanup uncertainty fail closed; run focused auth tests, NanoCore typecheck, Biome, host image tests, and independent internal review. Then rebuild the exact NanoCore image, replace only the current A1 test container while retaining recoverable predecessors, re-enroll only through the authenticated App API, and rerun the official bring-up without manual database, slot, fixture, or RuntimeTarget edits.

The local NHC-IMP-5D implementation is complete and independently reviewed. The host harness selects HTTP/2 prior knowledge only for the validated loopback HTTP readiness and decommission calls, while HTTPS remains unchanged. Enrollment now requires explicit exclusive-create slot acquisition, issue and rotation retain explicit replace semantics, failed enrollment clears only exact attempted-Token-owned files, and the existing transaction either creates a lineage-free first identity or reactivates the one exact retained decommissioned identity after rejecting any identity-only or deployment-only Token history without its identity row. The focused auth suite passes 22/22, the host image projection passes 60/60, NanoCore typecheck and targeted Biome pass, the Grok-produced changes received independent internal reviewer `PASS` after three bounded correction rounds, and the final same-byte `pnpm verify` run exits 0. The implementation was committed as `8e45086a`, its exact `linux/arm64` NanoCore image was loaded onto `a1`, the h2c health probe passed, and repeated authenticated App API re-enrollment delivered only fresh active Tokens through the proved empty slot. Those attempts accepted NHC-IMP-5D itself and exposed the next existing-owner cleanup gap below.

### NHC-IMP-5E — Post-fence fresh-ready cleanup settlement

The exact `8e45086a` A1 deployment established three distinct runtime facts. First, an attempt whose retained rebuild fence was older than the accepted 300-second hard bound reached stock OpenShell health but correctly did not project NanoHost readiness. Second, an attempt inside the new fence window completed authenticated current-generation readiness and then received the intended terminal `409` on its first `sandbox.create` poll, proving the old result-only cleanup outcome unknown and fail-stopping that Runtime Epoch. Third, the next fresh coordinator again completed health and readiness but received the same terminal `409`, while NanoCore logged a second `NanoHost accepted effect outcome is unknown` maintenance failure. The Moby `expected image index descriptor` journal lines are an independently reproduced non-fatal signature-identity-cache warning: both exact platform-child images loaded and inspected successfully, the Supervisor was extracted, and the Gateway returned health `200`, so the owning image pins and importer remain unchanged.

The repeated terminal poll is an implementation contradiction within the already accepted cleanup lifecycle. Poll-first rejection moves the existing backend session to `cleanup-failed` and fences its exact Sandbox projection as `cleanup_state=unknown`, but the next maintenance attempt has no process-local session and reconstructs the same result-only expectation again. It therefore cannot consume the later fresh-ready proof and loops instead of advancing the cleanup owner. The correction must reuse only existing state and owners: when an exact durable Sandbox has the complete unknown-fence shape, no process-local session exists, and the configured RuntimeTarget has a strictly later `observedAt` with `predecessorFenced=true`, `ready=true`, and `freshEmpty=true`, cleanup removes that exact Sandbox/Harness projection without issuing or registering any NanoHost effect. Missing, equal, stale, non-ready, non-fresh, mismatched, or partial proof remains cleanup-owned and fenced without registering the old request again. The existing backend-session owner then advances through `physical-cleaned` and terminal release. No request replay, new request id, durable stage, migration, timer, route, connection, capacity owner, or coordinator is added.

The bounded implementation is complete, independently reviewed, and committed as `008dab11`. The existing durable-lineage lookup finds the one Sandbox owner before validating exact RuntimeTarget and deployment identity; a complete unknown fence transfers effect-free through Phase 8, and only ordinary cleanup consumes a strictly later matching fresh-ready observation before exact projection removal. One sequential regression creates the real poll-first unknown fence, proves wrong or missing RuntimeTarget, wrong deployment, each partial fence shape, stale or equal observation, and each false readiness bit retain both owner and sibling with one original registration and zero effects, then proves the exact later all-true case removes only the owner. The focused file passes 17/17, the cleanup/restart/maintenance set passes 73/73, NanoCore typecheck, targeted Biome, documentation gates, diff checks, and the exact same-byte `pnpm verify` pass, and the independent reviewer returned `PASS`. Its exact `linux/arm64` image passed A1 h2c health and authenticated re-enrollment, but the official bring-up exposed NHC-IMP-5F before the retained A1 row could reach this complete-unknown branch.

### NHC-IMP-5F — Exact backend lineage recovery for the retained Sandbox

The exact `008dab11` A1 deployment again reached stock OpenShell health and then correctly failed the successor's first `sandbox.create` poll with terminal `409`, but read-only durable inspection proved that the expected Sandbox fence was not written. The retained backend session is `cleanup-pending`; its lease-era `sandboxBindingRef` differs from the independently owned Sandbox binding, no AgentSession runtime binding remains, and the sole Sandbox and Harness stay open, ready, accepting, clean, idle, and empty. RuntimeTarget and deployment identity match. The remaining lookup predicate compares `identity.backendSessionId` with `sandbox_binding_ref`, even though `planSession` defines the backend id as `nh-` plus the first sixteen hexadecimal characters of the same Sandbox compatibility key that the Sandbox record stores. The comparison is therefore against the wrong field, the durable owner resolves to null, and the already accepted poll-first rejection cannot call the existing Sandbox fence owner.

The smallest correction reuses that existing deterministic identity rule in the same lookup. An immutable backend lineage match requires the stored Sandbox compatibility key to project exactly to the supplied backend session id; the existing exact Sandbox binding and AgentSession lineage alternatives remain, more than one match still fails closed, and RuntimeTarget and deployment must still match after selection. A malformed or non-projecting backend id, absent owner, target or deployment mismatch, or ambiguous match must register no guessed repair and must leave cleanup and capacity fenced. The correction adds no helper owner, state, migration, fallback identifier, retry stage, effect, request, route, timer, connection, capacity owner, or coordinator.

Implementation begins by adapting the one existing sequential cleanup regression to the real A1 topology: retain a deliberately different lease binding, remove the AgentSession binding, preserve the exact deterministic backend id and Sandbox compatibility key, and require the first production-order poll to produce the complete durable unknown fence instead of leaving the Sandbox clean. The same regression must then retain NHC-IMP-5E's missing, mismatched, partial, stale, equal, and false-bit failure matrix and exact later fresh-ready deletion. After focused and same-byte gates plus independent review, commit the bounded correction, deploy its exact A1 image, re-enroll only through the App API, and require the official bring-up to progress from the single accepted poll-first fail-stop through the next fresh coordinator to stable authenticated readiness without database or fixture edits.

The bounded implementation is complete, independently reviewed, committed as `69adbf82`, and accepted on A1. Production changes one predicate in the existing durable lookup so the stored Sandbox compatibility key must project to the supplied backend session id; all other selection and fail-closed checks remain in place. The existing sequential regression reproduces the retained A1 lease-binding mismatch with no AgentSession binding and proves production-order unknown fencing plus later fresh-ready settlement. One direct characterization additionally proves a last-nibble non-projecting backend id does not match, two matching projections are ambiguous, and the existing exact worker-binding and AgentSession alternatives each still select their sole owner. The focused factory file passes 18/18, the cleanup/restart/maintenance set passes 74/74, and the exact same bytes pass `pnpm verify`, including repository checks, lint, typecheck, unit tests, coverage, and 11/11 builds. Independent review first returned `BLOCK` because four accepted predicates survived mutation, then returned `PASS` after the single added characterization killed all four without changing production or adding a test-only API.

The exact `linux/arm64` image loaded on A1 with config image id `sha256:22d4c1ad6b94e079fd1ecd21e12ba9b6b6026ba7ae8a3ec535f242c9a6e4d2ed` and passed h2c health plus authenticated App API enrollment. The first official bring-up intentionally fail-stopped on terminal `sandbox.create` poll `409`; read-only Core inspection then showed the retained Sandbox at `failed/unknown/draining/unknown`, its Harness at `failed/draining`, the backend at `cleanup-failed`, and generation 9 fenced and non-ready. The next official attempt observed authenticated current-generation readiness. Because the official harness immediately tears down after that observation, one further App-API-authorized start through the same repository-owned systemd service kept generation 11 ready long enough for ordinary maintenance: the exact Sandbox and Harness counts became zero and the backend advanced to `physical-cleaned` with physical-cleanup evidence after fifteen seconds. Repository teardown then stopped the service and decommissioned the identity. No database, fixture, runtime projection, or credential was manually edited.

### NHC-IMP-5G — Current NanoHost real-Turn acceptance instrument

The cleanup acceptance leaves the configured RuntimeTarget empty and ready, but the remaining A1 sequence has no executable current runner. The existing real Task Mode runner owns the nearest honest boundary, yet it assumes that the repository path is local to the runner, speaks through an HTTP/1 client that cannot reach NanoCore's accepted plaintext HTTP/2 endpoint, and requires the retired OpenShell `0.0.80` provenance value. The current NanoHost backend is the sole runtime target and is compiled against the fixed stock OpenShell `0.0.99` boundary, while its capability projection currently omits that version and therefore records `null`. The retired restart runner still selects the rejected Cell and remote-placement path, and the historical Unit E/F files named by the old plan are absent from the current repository; neither may be treated as executable evidence.

The smallest correction reuses the existing real Task Mode runner and current product owners. Its one existing fetch seam uses Node's standard HTTP/2 client only for plaintext `http://` and returns the same buffered WHATWG `Response` the Core Client already consumes; HTTPS retains the installed Undici path and its existing timeout owner. The runner stops inspecting the target repository on its own host and instead requires the real NanoCore `setDefault` response to prove `validation.ok=true` before the quota-bearing Task starts. NanoHost capabilities project the already accepted fixed `0.0.99` value so the existing transcript RuntimeEvidence records truthful backend provenance. The retained evidence requires the exact product commit and host-manifest digest supplied by the same external deployment and host-assertion owners, together with the existing worker image, URL-origin, AEP, Turn, usage, and redaction facts. This adds no product API, protocol field, dependency, durable state, compatibility path, alternate runtime selector, or new deadline.

Implementation begins with one focused real-H2 regression, one remote-repository validation regression, the current version oracle, and exact non-secret evidence-identity validation. After the runner and one-line capability projection pass focused checks and independent internal review, deploy only if product bytes changed, assert the host manifest immediately before the attempt, enroll through the App API, keep the existing systemd NanoHost generation ready, and execute one correlated real Codex Task Turn. A passing first Turn then permits the same runner to grow only the next distinct scenario in order: a second sequential Turn in the same Thread with one current AgentSession, then a bounded long-running Turn interrupted through the ordinary Core Client. Successor reconnect and Unit E/F remain separate later freezes because their only historical runners are retired or absent; they must not be reconstructed from plan prose or merged into this first instrument correction.

The bounded local implementation is complete, independently reviewed, and committed as `855ffa71`. The runner's single fetch seam now uses Node's standard client only for plaintext HTTP/2, preserves the existing HTTPS Undici path, fails cleanly on response-less close and abort, and returns bodyless HTTP responses without inventing another timeout or transport owner. Target-visible repository validation, exact 40-hex product commit and 64-hex host-manifest identity, redacted evidence retention, and exact OpenShell `0.0.99` provenance are enforced before the real Task. The focused runner passes `54/54`, the factory passes `20/20`, NanoCore typecheck and targeted Biome pass, and independent internal review returned `PASS` after its one HTTP/2 boundary finding was corrected. The exact same bytes pass `pnpm verify`, including repository checks, lint, typecheck, unit tests, coverage, and 11/11 builds. Its exact `linux/arm64` image passed the repository smoke check, loaded on A1 with local and remote image identities equal, and replaced the prior test container without deleting the recoverable predecessor or persistent data. Both the candidate and predecessor then failed the same restart-recovery invariant before the real Turn could begin, exposing NHC-IMP-5H below.

### NHC-IMP-5H — Preserve accepted materialization readiness evidence during cleanup

The exact `855ffa71` A1 deployment reached the existing `physical-cleaned` backend session and failed the critical scheduler restart phase on `Workspace backend handle handoff is incomplete`. Restarting the retained predecessor produced the same failure, and its pre-stop log already contained repeated cleanup-maintenance failures for that owner. Read-only Core, Workspace, and AEP inspection proved one complete handoff with one Workspace input, one materialization record, one pending backend handle, exact backend kind and worker-session identity, and no structural mismatch except `canonicalRecord`. The accepted NanoHost materialization had omitted optional `backendStatus`, so its durable diagnostic `readinessEvidence` correctly contained only `sandbox.created`; cleanup reconstructed a synthetic `backend.ready` observation from the backend anchor and rejected the otherwise exact record. No database, fixture, handle, backend session, or AEP record was edited.

The smallest correction remains inside the existing Workspace handoff validator. `readinessEvidence` is the schema-validated diagnostic observation durably recorded from the accepted materialization response; the owning S39 contract explicitly says omitted evidence produces no entry. Cleanup uses the stored array only to determine whether the optional backend observation existed, then re-derives the exact canonical backend and Sandbox evidence with the existing materialization builder while continuing to compare every deterministic field. It must neither invent a later backend observation nor trust stored evidence kind, reference, order, or duplication. The lowest-sufficient regressions create an exact materialization with no `backendStatus`, prove cleanup succeeds without fabricating `backend.ready`, and prove a schema-valid foreign Sandbox reference remains rejected. The implementation adds no compatibility branch, record repair, migration, automatic cross-database write, status, retry, effect, API, or alternate recovery path.

The bounded correction is implemented and independently accepted. The Grok-authored regression first produced one exact RED for the foreign Sandbox reference while the omitted-`backendStatus` positive stayed green; an internal reviewer accepted that oracle before production correction. The final implementation passes the complete focused file `14/14`, the adjacent Workspace synchronization and materialization set `53/53`, NanoCore typecheck, targeted Biome, and scoped diff checks. Independent implementation review additionally falsified six schema-valid evidence mutations and returned `PASS`. The full repository gate exits 0, including repository checks, lint, typecheck, unit tests, coverage, 11/11 builds, and the CLI bundle; the checkpoint commit remains before the next exact-image A1 attempt.

### NHC-IMP-5I — Read the real Codex provider binding selected by the target agent

Checkpoint `dc2d44a8` passes the full repository gate, restored the retained A1 cleanup path, and booted NanoCore. Host assertion passed and NanoHost reached authenticated generation 12 readiness. The first runner attempt then failed only because its operator-local SSH forward had exited during container replacement; after a renewed forward passed authenticated diagnostics, the second attempt still stopped before product admission because the shared real-Codex preflight fixed the historical fixture provider `openai_codex` and account slot `default`. The target's exact agent instead selects provider `a1-openai-codex`; that valid `openai-codex` profile binds the sole logged-in account `a1-codex-slot` and the same accepted `openai-codex/gpt-5.6-sol` model. No Workspace, Thread, Turn, AgentSession, Sandbox, provider call, or runtime-config mutation was created by either failed attempt.

The smallest correction stays in the existing real-Codex test support and runner. It reads the selected agent first, reads the provider file named by that agent, validates the fixed Codex family and model plus the provider's exact subscription account binding, requires that exact account to be logged in, and supplies the derived provider id to the existing AEP assertion. It adds no A1-specific constant, configuration mutation, login action, new environment variable, product surface, or compatibility path. A focused regression must reproduce the current target-shaped provider and account, retain negative checks for missing or mismatched agent/provider/account/model state, and prove the AEP assertion rejects a route that differs from the derived provider identity.

The correction is implemented and independently accepted. The initial target-shaped tests produced two exact REDs: the preflight still queried account slot `default`, and the AEP assertion still compared against the historical provider constant. Internal review then rejected the first green artifact because six provider/account predicates and fixed-provider runner propagation were not mutation-sensitive. The strengthened tests independently reject every required provider family, kind, model, and exact account-slot mismatch, and one successful runner path carries a distinct derived provider id through the real runner into its AEP result. All eight requested mutations are killed; the focused support and runner set passes `73/73`, targeted Biome and diff checks pass, and the unused fixed `modelId` return was deleted rather than retained as speculative output.

### NHC-IMP-5J — Separate deployment-admin preflight from canonical-user product execution

The committed NHC-IMP-5I runner passed the exact A1 provider/account preflight, strict no-op reload, and authenticated readiness diagnostics. Its product sequence still stopped before Workspace creation because the single configured bearer is a `server-admin` Token: the accepted access catalog requires that Token for the deployment-wide reads but rejects it from canonical-user `POST /api/workspaces`. A direct ordinary route probe returned the expected `Workspace access denied.` This is a test-instrument actor-selection defect, not permission to weaken authorization or treat an administrator as a user.

The smallest correction remains inside the existing real Task runner. Its existing client factory creates an admin client from the existing bearer and a product client from one new operator-provided Better Auth session-cookie input. Preflight and `acceptingProductWork` diagnostics use only the admin client; Workspace, Thread, repository, Task, AEP, capability, runtime-evidence, Item, and review-cleanup calls use only the product client. The prerequisite accepts both credentials or neither so the local unauthenticated path remains unchanged and a half-configured server run fails before contact. The redacted config records only credential-presence booleans. The runner does not sign up or sign in a user, retain a cookie, copy credentials between clients, add an auth fallback, or change NanoCore product or deployment authority.

The correction is implemented and independently accepted. The lowest-sufficient regressions first failed because the existing client factory could not be observed directly and whitespace-only credentials were treated as configured. The final factory builds exactly two clients for a server run, with only bearer authorization on the admin client and only the Better Auth cookie on the product client, while the credential-free local path constructs once and shares that client. Table-driven prerequisites reject either missing credential, whitespace-only values, and CR/LF in either secret before creating clients or contacting NanoCore. Actor-routing and swap tests prove that deployment preflight and diagnostics cannot use the product client and product operations cannot use the admin client. Success and failure evidence retain only presence booleans. The focused suite passes `68/68`; targeted Biome and diff checks pass; independent review found no remaining behavior, security, simplicity, or documentation issue.

### NHC-IMP-5K — Preserve existing runtime-config file permissions across atomic updates

The first bounded A1 product-principal attempt recovered an existing operator-held `server-admin` plaintext whose metadata and recent use matched the sole active Token, then proved that bearer through deployment diagnostics without exposing its value. A1 intentionally has `auth.signup.enabled=false`, its existing bootstrap User has no Better Auth account or session, and no accepted bearer-to-session conversion exists. Independent Verifier review therefore accepted one operator-controlled window: prove port `4317` loopback-only, validate and revision-CAS the single signup field through the existing config API, restart, create one ordinary user and cookie, immediately restore the exact original config and restart, prove signup disabled before Task, keep bearer and cookie on separate clients, transfer any retained project Workspace to the existing administrator, then sign out and disable the disposable User. The disabled User and Quick Chat history remain by design; this is not a credential-recovery mechanism or a complete rollback.

That execution stopped before Task because the existing runtime-config atomic writer replaced root-owned `0600` `server.jsonc` with a default-mode `0644` temporary file. The outer exact-byte backup restored the original bytes and mode, restarted NanoCore, and the created User was disabled with zero sessions. The correction stays in the existing `RuntimeConfigFileService.writeFileAtomically` owner: after containment checks, an existing regular target contributes only `mode & 0o777`; the same-directory temporary file is explicitly changed to those bits before rename so umask cannot widen it, while missing targets retain the current default and uid/gid, ACL, xattr, SSH chmod wrappers, and new metadata owners remain absent. The umask-controlled regression produced a genuine `0600` to `0644` RED after a successful PUT and content change, then passed with exact mode preservation while retaining the stale-revision `409`. The complete focused file passes `10/10`, NanoCore typecheck, targeted Biome, and scoped diff checks pass, and independent internal review returned `PASS` on the actual Grok-produced diff.

### NHC-IMP-5L — Reject DATA_ROOT-contained repository sources before durable admission

The exact `c9d8c6c6` NanoCore image preserved `server.jsonc` bytes, owner, and `0600` through both official config writes, closed the loopback-only signup window, re-proved signup disabled, and retained the disposable product session across restart. The first real Task then failed after scheduler dispatch because the attempt configured its repository below NanoCore's data root. `WorkerGovernanceTurnExecutor` persisted that absolute source path in the new AgentSession file before the next global layout validation rejected it; the resulting canonical record now makes every later `ensureLayout` fail, so another Task or Workspace attempt against that data root is prohibited.

The owning portability specification already forbids product records from embedding absolute data-root paths. Its clarified admission rule requires the shared repository owner to reject a linked repository `localPath` whose normalized absolute path or existing canonical real path is equal to or a path-segment descendant of the current data root before changing the repository row or data-source catalog. Turn admission repeats the check before catalog synchronization or a scheduler admission entry, captures one canonical repository path, and uses it for that Turn's `workspaceCwd` and repository Workspace Root. The implementation must reuse one path predicate, redact the failure, preserve the prior repository resource and catalog, and add no path rewrite, migration, quarantine, Workspace deletion, or compatibility behavior. A lowest-sufficient regression must prove exact, descendant, and symlink-alias rejection; sibling-prefix and normalized-external acceptance; configuration pre-write preservation; and stale-or-retargeted pre-Turn rejection with zero new catalog mutation, admission, lease, AgentSession, or backend effect.

The bounded prevention implementation is complete in the shared repository owner. `inspectRepositoryPath` now makes one internal `clear | contained | unresolved` boundary decision, performs lexical segment containment before filesystem validation, captures existing candidate and DATA_ROOT real paths once, and keeps canonical paths out of public diagnostics. Repository upsert rejects non-clear boundary results before `INSERT` or catalog synchronization while preserving existing clear external invalid-path diagnostics. Turn startup consumes the one captured canonical repository path, and Workspace Root materialization keeps that repository root ahead of and instead of any conflicting authored root. The focused App API, filesystem, SQLite, scheduler, Simulator, and materialization suite passes 45/45; NanoCore typecheck, targeted Biome, `git diff --check`, and independent internal review also pass.

The current A1 poison is a separate recovery concern. Read-only inventory found no verified pre-attempt data-root backup, filesystem snapshot, Workspace export, or implemented Workspace deletion owner; retained containers all mount the same live tree. Manual deletion of the AgentSession file or related SQLite rows would bypass canonical Workspace, audit-closure, membership, and cleanup owners and remains prohibited. NHC-IMP-5L may prevent recurrence but cannot itself authorize repair of the affected Workspace or make the current A1 data root acceptable evidence.

### NHC-IMP-5M — Retire revoked legacy human Token scopes through the Core migration owner

The A1 administration preflight independently exposed one bounded current-schema debt: `GET /api/app/auth/tokens` reads all durable human Token rows, and its closed response schema rejects one revoked historical `workspace-readwrite` row. Exact presented-secret authentication, config recovery, diagnostics, and User disable do not enumerate that row, so this did not weaken those operations; nevertheless the list route cannot remain broken, widen its schema, or hide unsupported history behind a permanent query filter.

The owning human remote-auth specification now assigns the smallest no-compatibility correction to the existing Core migration owner. One one-way migration deletes only rows whose exact scope is `workspace-readwrite`, durable status is already `revoked`, and token id is not named by any surviving Token's rotation lineage. In the same transaction it appends one redacted server system audit event per deletion, preserves all pre-existing audit rows and current Token bytes, requires that no unsupported scope remains, and publishes its migration id. Unproved lineage, audit failure, a non-revoked retired row, or any other unknown scope rolls the whole migration back and blocks product startup without revocation, reissue, rewrite, or repair. The App API schema, token read model, list query, and presented-secret verifier remain unchanged.

Implementation starts with one migration regression that seeds a byte-frozen current Token, pre-existing audit history, and unreferenced revoked retired history, then proves exact deletion, one redacted retirement audit, full preservation, ledger publication, idempotent second boot, and the unchanged admin list response. Fail-closed cases cover surviving rotation lineage, non-revoked `workspace-readwrite`, an unknown scope, and audit-publication failure, requiring Token, audit, and migration-ledger rows all to remain unchanged. After focused migration and App API checks, NanoCore typecheck/lint/build, full proportional gates, independent internal review, and a stage commit, the remaining real acceptance route waits for the execution-environment decision below; NHC-IMP-5M does not authorize any manual A1 row edit or repair the poisoned Workspace.

NHC-IMP-5M is implemented through `core_0009_retire_workspace_readwrite` and committed as `b6e073c1`. The focused migration, layout, and real App API suite passes 28/28; NanoCore typecheck, targeted Biome, `git diff --check`, and the complete same-byte `pnpm verify` gate pass. Independent review also exercised a revoked legacy candidate chain and a surviving current-Token lineage reference: the chain retired with one audit per row, while the surviving reference rolled back Token, audit, and ledger changes. No App API schema, list query, Token read model, exact-secret verifier, compatibility branch, or A1 state changed.

### NHC-IMP-5N — Replace host-local runtime repositories with Sandbox-owned Git

Exact commit `7a228369` closes the first real Harness wire mismatch by projecting the accepted four-field `session.open` result instead of leaking the adapter-private `nativeHandle`. Its Worker Shim suite passes `170/170`, the NanoCore Harness consumer suite passes `4/4`, and the exact rebuilt worker image reached `turn.start` on A1. The attempt then returned immediate `dependency_failed` because the AEP declared cwd `/workspace/openkit` and Git input `/workspace/openkit/worktrees/main`, while the selected NanoHost materializer imported only package-config and generated Context files and never created or populated either path. Redacted A1 journal and durable evidence show three successful package/Context imports, zero repository materialization effect, no native process start, and complete terminal cleanup.

This is an authority and implementation mismatch, not permission to weaken the Worker Shim or build an acceptance-only image. Intent Epoch 8 resolves it by rejecting host-local repository ingress altogether. A remote Agent Runtime may consume only a network-addressable Git source with an exact accepted commit; a Sandbox-side materializer performs clone or fetch, checkout, and clean-`HEAD` proof before Agent start. NanoHost permits or denies the resolved network flow but owns no Git operation or Git data interpretation.

The correction therefore replaces the repository resource and real-worker runner's host `localPath` with one credential-free HTTPS Git locator and adds one Sandbox-side pre-Agent Git materialization path using the worker image's existing Git client. This slice accepts no Git credential or Vault grant; private-repository credential injection remains deferred to an owning change. It adds no NanoHost effect, repository carriage, custom Git protocol parser, generic storage service, or compatibility path. The lowest-sufficient regressions prove local paths and unpublished local commits are rejected, query-, fragment-, and user-info-bearing URLs fail before mutation or network contact, exact remote commit checkout succeeds before Agent start, partial targets are removed, and NanoHost observes only the declared network policy. All rebuilt app and worker artifacts will be compiled directly on A1 from an exact detached commit as requested.

### NHC-IMP-5O — Isolate the Docker Runtime Epoch from the host network namespace

Exact A1 cutover evidence disproved the existing `--bridge none` isolation premise. Three NanoHost starts each removed the live system Docker `docker0` projection about one second after the private rootful Docker daemon initialized, while the daemon also attempted to delete global Docker nftables tables. The dedicated roots, sockets, cgroup, and bridge flag therefore isolate container state but not the host network effect domain; another ordinary NanoHost start is prohibited until the owning runtime specification is implemented and real-host noninterference passes.

The accepted smallest correction keeps the NanoHost parent in the host network namespace for its sole NanoCore transport and places only the epoch-private `containerd`, dedicated `dockerd`, and stock Gateway in one fresh Linux network namespace. One exact manifest-owned `/usr/bin/slirp4netns` direct foreground child supplies bounded egress with host-loopback and proxy DNS disabled; NanoHost accepts a bounded non-loopback resolver set only from `/run/systemd/resolve/resolv.conf` and passes it directly to the private Docker daemon. The service uses private mount propagation, and one short-lived fixed connector enters the retained namespace only to create each existing Gateway physical channel. This adds no proxy, listener, durable state, effect, second logical channel, endpoint, configuration input, or member-local restart.

Independent specification acceptance passed, and the local implementation now creates and retains the private namespace, supervises the exact slirp member, applies strict resolver and service-mount boundaries, joins Docker and Gateway members, and routes every Gateway channel through the namespace-scoped connector. Mutation-sensitive focused regressions pass 7/7, the NanoHost suite passes 79 unit plus 1 service test, the host manifest suite passes 61/61, and build, Clippy `-D warnings`, fmt, and diff checks pass; independent implementation review returned `PASS`. Completion still requires A1 proof that the namespace inode differs from the host, all three runtime members share it, slirp ready-fd and fixed DNS/Git egress succeed, forbidden host surfaces are unreachable, and start, normal stop, member failure, and NanoHost `SIGKILL` preserve system Docker bridge metadata, kernel `docker0`, Docker-owned nftables, all business-container attachments, and a bounded system-Docker build-network smoke. Any mismatch keeps NanoHost stopped and blocks product acceptance.

### NHC-IMP-5P — Reuse the RuntimeEvidence owner for real-Task failure evidence

The real-Task runner already had the correct confidentiality boundary for raw exceptions: timeout and owned assertion messages remain stable, while every other untrusted runtime error becomes one constant and no raw stack is persisted. Its missing evidence was already owned elsewhere. NanoCore stores a closed product-safe `RuntimeEvidenceRecord` per Turn and the Core Client already exposes `listWorkspaceRuntimeEvidence`, so the smallest correction is one accessor read for the exact failed Turn and inclusion of those records in the existing exclusive failure file. Temporary A1 diagnostics, data-root paths, Core database handles, direct table reads, a second redaction decision, and a new evidence owner are explicitly excluded.

The correction is implemented and independently accepted. The runner creates at most one evidence-read Promise, reuses it across the success and failure paths, filters only `task.turn.id`, preserves the original thrown error as control-flow authority, and keeps its existing public leak scan. The adjacent checkpoint boundary now reconstructs only its closed recovery fields and redacts only the already-owned `failureSummary`, so product diagnostics cannot weaken Direct Task receipt recovery or smuggle foreign context fields. Runner `86/86`, checkpoint `9/9`, complete server `158/158`, NanoCore typecheck/build, targeted Biome, whitespace checks, and final independent review all pass.

Exact A1 image `openkit/app:a1-88ff4691-r7` passed smoke, deployed, and remained h2c healthy. One real Task returned the expected test-agent failure and wrote exactly three RuntimeEvidence records for one Turn: promoted runtime provenance succeeded, backend teardown succeeded, and the terminal checkpoint failed. NanoHost returned to authenticated generation `97` with predecessor-fenced, ready, and fresh-empty all true. That single product-owned failure artifact replaces the repeated temporary table-scanning diagnostics; it does not reclassify the agent failure as orchestration success or preserve untrusted raw error text.

### Current checkpoint

- **Route status:** blocked after rebase. NHC-FND-120 remains closed for the exact corrected oracle and product baseline `38a83058f900f3cfdc01ade13fd42fbcf11cbf91`, but Roadmap R001 is open under NHC-FND-121 until the rebased product receives fresh no-reboot A1 evidence.
- **Deciding evidence:** retained artifact `faa5f7d417ac643998895fc901a480203dd3e9609b080a4c6119a1c5af949072` records F1, F2, F4, network conformance, normal lifecycle, terminal cleanup, and Aggregate all `PASS` for product commit `38a83058f900f3cfdc01ade13fd42fbcf11cbf91`; its runner digest still matches the rebased candidate, but upstream product changes directly affect runtime paths exercised by those scenarios, so the artifact is historical rather than deciding for the current candidate on `1557792`.
- **Terminal state:** NanoHost is inactive, every configured credential and companion path is absent, NanoCore remains running, signup-disabled configuration is byte-identical at revision `sha256:b55bde1672d73599ad3e9ca2af691cb3c49bfabe0410702e8d3051b5bd56d055`, the disposable User is disabled, and A1 was not rebooted.
- **Residual ownership:** NHC-FND-121 is the sole current R001 blocker. NHC-FND-045, NHC-FND-049, NHC-FND-052, and NHC-FND-105 remain deferred outside this plan. NHC-FND-117 preserves Scenario 3 restart truthfulness for R005 and requires a separately accepted effect boundary.
- **Verification closeout:** after rebase the documentation model validates 208 documents, the generated index is current, the combined host projection and runner suite passes 215/215, runner syntax and `git diff --check` pass, and `pnpm check:repo` passes across 952 files. The rebased full build completes 10/11 tasks and typecheck completes 9/10 tasks before the new upstream NanoCore model-catalog package subpath fails resolution; the pre-rebase candidate had passed 11/11 build and 10/10 typecheck. Earlier broader gates also exposed unrelated existing-tree failures outside this diff: one unchanged Web test exceeds its fixed five-second timeout but passes unchanged with a command-local 15-second timeout, while three unchanged NanoCore filesystem synchronization tests fail identically with and without coverage; the coverage run otherwise passes 2,477/2,480 NanoCore tests. No host or Codex App Server restart was used for any check.
- **Next action:** publish only a draft pull request that discloses NHC-FND-121 and the upstream build/typecheck failure. Do not present R001 as merge-ready until the current product baseline builds, one fresh exact-candidate no-reboot A1 Aggregate passes, and an independent artifact audit accepts it. No merge, release, deployment, host reboot, Codex App Server restart, or host reset is authorized.
- **Remaining human-only decisions:** any change to accepted NanoHost design, Scenario 3's effect boundary, credential disclosure, non-project host scope, merge, release publication, or important residual-risk acceptance remains human-owned; no such decision is being inferred by the draft PR.

### Historical pre-close checkpoint

- **Next action override:** this supersedes the older recovery instruction below. Preserve seven retained formal FAIL artifacts, two interrupted attempts, and the independently audit-blocked PASS artifact, then re-enroll one disposable identity and rerun only the exact no-reboot `pnpm host:nanohost:unit-f` gate. Independent review returned `PASS` after requiring retained raw F1 digests alongside the deciding invariant digests, and the fresh-context verifier returned `Continue` after reproducing all deciding checks. A retained PASS still requires independent artifact audit before R001 closeout; any new failure is archived under its attempt identity and reframed from direct evidence without rebooting A1.
- **Current implementation addendum:** formal attempt `894c6394-281a-4039-a3ff-d328524b216a` exposed two runner defects without changing product design. The candidate now starts the F4 fresh successor before awaiting the cleanup that successor owns, and treats a post-stop `reset-failed` race as success only after native systemd proves the exact unit inactive. The two root-cause tests produced exact REDs; the independent reviewer then found that the reset-race test did not exclude broad error swallowing, and one negative regression now requires the original error while the exact unit remains active. The three focused regressions pass `3/3`; combined host projections pass `211/211`; targeted Biome, syntax, `pnpm check:repo`, and `git diff --check` pass; and the same independent reviewer returned `PASS` with no remaining finding.
- **Newest no-reboot recovery evidence:** retained result `7e1a61ec9a9e41ea8986eb45652f6b7517f753ce63933dfc1dcfcff7b87c563b` is archived with mode `0600` and contains no protected credential pattern. Existing owners decommissioned the formal identity and removed both credential slots; the ordinary project-service recovery settled exact lease `lease_4086993bf4d2f9e8` and its backend without scheduler-row mutation; the service was then stopped and decommissioned again; the sole matching inert state/runtime epoch pair was removed after exact shape, process-reference, and socket-reference checks; and the disposable test User was disabled through the existing App API with zero remaining sessions. A1, Codex App Server, and Herdr were never rebooted.
- **Latest retained no-reboot evidence:** formal attempt `2e7802af-231d-49c8-9e58-3a8e67b310a5` is archived with mode `0600` and credential-free digest `659afab9691929d38a5dabe29cca44128764dc182aed484f809817e36464f671`. It records F1 `PASS`, F4 `PASS`, F2 `FAIL` only for a baseline comparison across the attempt's first system-Docker smoke, normal lifecycle `PASS`, and every terminal cleanup row true with no cleanup reason. The runner left NanoHost inactive, removed both credential slots and exact epoch residue, preserved NanoCore running, disabled the disposable User, and did not reboot A1.
- **Newest audit-block evidence:** formal attempt `e8b3d86d-988b-4978-97dd-f819c59bf29c` is archived with mode `0600` and credential-free digest `c1e8a70e0b78d39531e76dd9dd92ee630574c2e0d926b6ad1a9b2bce9cbe4d03`. It records F1, F2, F4, network conformance, normal lifecycle, terminal cleanup, and Aggregate all `PASS`, while independent audit blocked R001 because F1's raw baseline changed under an unconditional scenario exception. The same audit accepted closure of NHC-FND-054 and NHC-FND-119. A bounded NanoCore-only diagnostic isolated the change to the exact configured NanoCore host-network endpoint ID, and the corrected instrument now retains raw plus invariant F1 digests and rejects every unrelated baseline change. NanoHost is inactive, all four credential and companion slots are absent, NanoCore is running, signup-disabled configuration is byte-identical, the disposable User is disabled, and A1 was not rebooted.
- **Frontier addendum:** seven retained formal FAIL artifacts, two interrupted attempts without retained artifacts, one audit-blocked PASS artifact, the no-host-reboot boundary, F1/F2/F4 admission, the deferred Scenario 3 owner, exact failed-attempt cleanup, deterministic combined checks, accepted Intent Epoch 14 and 15 corrections, closed NHC-FND-054, NHC-FND-118, and NHC-FND-119, independently accepted Intent Epoch 16 implementation, the fresh-context `Continue` direction, and the fixed result path's absence are settled. Only the next retained no-reboot A1 result remains current.
- **Next Action:** create one bounded disposable product session, restore signup-disabled configuration byte-for-byte, re-enroll the configured identity through the existing App API, prove the fixed result filename absent, and run only `pnpm host:nanohost:unit-f`. The expected observable is one retained credential-free result whose exact F1 raw baseline changes only in the normalized configured NanoCore host-network endpoint while its invariant baseline remains equal, whose F2 and F4 raw baselines remain equal, and whose network conformance, normal lifecycle, terminal cleanup, and Aggregate pass without rebooting or equivalently resetting A1. Any new failure is archived under its attempt identity and reframes from direct evidence; a retained PASS still requires independent artifact audit before R001 closeout.
- **Current implementation:** Git is on `feat/r001-nanohost-noninterference` at baseline `38a83058f900f3cfdc01ade13fd42fbcf11cbf91`. The candidate retains the existing Unit F lifecycle owner, network-conformance adjudicator, shared live collectors, and one terminal attempt owner used by the real CLI, but its closed R001 scenario set is now exactly F1, F2, and F4. F3, its execution-server contract, direct-ready exception, reboot target, host-reboot branch, tunnel-disconnect waiter, NanoCore restart-policy reader, SSH-return waiter, and `/usr/bin/systemctl reboot` binding are deleted rather than skipped or parameterized. The terminal owner validates create-only output, exact alias `a1`, and exact `7200000` deadline before effects; starts NanoHost before the pre-attempt baseline; waits for the exact all-true RuntimeTarget before start settles; bounds and cooperatively interrupts the existing coordinator without concurrent cleanup; captures the terminal reference and exact epoch; stops and resets the exact systemd unit's failed state before proving inactivity; decommissions NanoHost; proves live epoch effects absent; validates and removes only the exact matching state/runtime root pair through the explicit operator path; reobserves root absence; rechecks baseline and build-network smoke; retains the credential-free checkout commit plus hashed NanoHost identity and deployment with closed redacted reasons; rejects any gate-result identity mismatch; and writes only after cleanup. The new scenario-set test first produced the expected F1/F2/F3/F4 versus F1/F2/F4 RED; after the deletion, the complete focused suite passes 145 of 145 and the combined host projection plus runner set passes 208 of 208 without host effects. Targeted syntax and diff checks pass; the current no-reboot slice still requires independent review.
- **Observed evidence:** `NHC-FND-054` remains open and Roadmap `R001` remains unfinished. The network collector reached independent reviewer `ACCEPT` after three bounded correction rounds and root observed the focused suite pass 112 of 112, targeted Biome, `node --check`, `git diff --check`, and `pnpm check:repo`. The first pre-A1 fresh-context verifier then returned `Reframe` and the independent auditor returned `BLOCK` because actual CLI bytes do not own terminal stop, authenticated decommission and credential-removal projection, service inactivity, final epoch residue absence, post-cleanup baseline and build smoke, or an exact accepted command and retained filename. A fourth independent Cursor CLI Fable 5 consultation accepted the in-runner terminal-owner design without reservation after requiring a cleanup-time reference baseline instead of the pre-attempt baseline and a repository-relative documented filename resolved to an absolute path only at invocation. A post-compaction fresh-context verifier independently returned `Continue`, confirmed no equivalent existing owner or human/Core/spec decision, and limited the next slice to one test-first runner boundary. The test author froze 14 terminal-owner RED rows and the builder reached 126 of 126 focused green, after which independent review returned `BLOCK` because timeout cleanup raced a live gate, a signal during the last operation could retain PASS, terminal epoch capture was optional, and failed results lost exact identity. Four new mutation-sensitive REDs reproduced those findings at 124 passing and four failing; the bounded runner correction then waited for live gate settlement, rechecked final interruption, required and forwarded the terminal epoch, and retained validated public identity. Root observed the complete focused suite pass 128 of 128, the exact correction subset pass four of four, targeted Biome, `node --check`, and `git diff --check`; the same independent reviewer returned `ACCEPT` after direct bounded re-review. Final local review then blocked stale checkpoint and cookbook-index projections, which were corrected and accepted. The fresh pre-A1 verifier returned `Continue`, but the independent auditor returned `BLOCK` after an injected probe proved the executable admitted non-`a1` aliases and non-`7200000` timeouts and the retained public identity omitted the exact checkout commit plus confidentiality-safe NanoHost identity and deployment bindings. Seven mutation-sensitive REDs reproduced the two executable preflight gaps, three independent gate-result identity mismatches, and missing exact identity on failure and timeout at 126 passing and seven failing. The corrected attempt rejects target or deadline mismatch before effects, normalizes the exact public checkout and hashed NanoHost bindings, rejects any complete identity mismatch after cleanup, and retains the authorized identity on every terminal result; root observed the focused suite pass 133 of 133, the exact audit subset pass seven of seven, targeted Biome, `node --check`, and `git diff --check`. Read-only A1 prerequisite probes resolved `a1` to the current host and proved that the offered ED25519 host key exactly matches the local host public key, while the existing LingkaiX public key exactly matches `authorized_keys`; nevertheless user-owned `known_hosts` has no `a1` entry, the configured agent socket is unavailable, BatchMode SSH stops before authentication, and all 19 documented inputs including both protected credentials are absent from the current process. A fresh Herdr-server shell independently confirmed the same SSH failure and the absence of the six decisive credential, NanoHost, and source inputs. The exact existing `openkit-nanocore-a1` container retains image ref `openkit/app:a1-1124362d` and image ID `sha256:5377674fc3e3263520ba004388208ee648cf69bccb887bc378ef37be7c37c45c`, but it exited zero without OOM when dockerd shut down and its `restart=no` policy left it stopped; NanoHost remains inactive and no required business container is running. Existing public owners yielded the installed NanoHost SHA-256, `nanohost-a1` and `deployment-a1`, ports `4317` and `4318`, the fixed arm64 Supervisor digest, one remaining required Worker digest, and the public Git source with baseline commit, but they do not establish the operator's expected current NanoCore product lineage or supply credentials. The independent prerequisite Verifier returned `Ask Human`, and the independent Auditor returned `BLOCK`, both rejecting primary-owned SSH mutation or pre-credential container recovery. No SSH state, container, service, credential, result, remote command, or A1 mutation changed, the fixed result remains absent, and no retained current-clone A1 result settles the remaining real-host predicates.
- **Latest recovery evidence:** formal attempt `ee32a6db-f275-4413-96e9-75ba9cda9330` retained Aggregate `FAIL` with digest `2ebce888e8730661dd0a23175ec07fc652f80bd7e148802dcca04227c568b494` after a terminal `sandbox.create` `409`; its finalizer stopped and reset the service, decommissioned the identity, removed credentials, passed independent build-network smoke, and removed the exact observed epoch roots, but it could not establish the earlier baseline or epoch fields after the start failure. A post-compaction verifier returned `Reframe` until an exact read-only recovery snapshot was available. The subsequent Core snapshot proves the affected backend session `lease_c856bc9528c14c27` is `cleaned` with a physical-cleaned timestamp, its scheduler lease is `released` for `scheduler-restart-backend-cleanup`, scheduler capacity is `0/1` with queue depth `0`, and no nonterminal lease, unclean backend session, or state/runtime epoch root remains. Formal attempt `49de4163-e38d-44fe-8eca-5c5301dfdd5d` later completed a real Worker Turn and entered F3, whose live reboot capability terminated the colocated runner, Codex App Server, and Herdr before terminal finalization or retained-result creation. Post-interruption cleanup through existing owners decommissioned `nanohost-a1`, revoked every current NanoHost Token, removed both credential slots and all observed state/runtime epoch roots, removed the exact temporary Better Auth session, and left `openkit-nanohost.service` inactive; exact read-only storage still shows `lease_bf68eccc51f253ff` stale with backend cleanup pending, so ordinary NanoHost recovery must settle it before another formal attempt. No scheduler row was manually mutated, and no further host reboot is permitted.
- **Method:** keep the one existing runner and add no second harness, teardown copy, daemon, state machine, generic lifecycle framework, product signal path, systemd cleanup hook, alternate scenario mode, or simulated reboot. The existing finalizer captures the exact terminal epoch, stops and decommissions NanoHost, proves service inactivity plus cgroup, namespace, and socket absence, then the new operator step validates and removes only the matching attempt-observed state/runtime root pair and reobserves both absent before continuing the existing baseline and build-network smoke. The current implementation-projection correction changes no normative specification design. Any changed shape, extra epoch root, live effect, removal failure, or surviving root forces terminal `FAIL` without broader deletion, while independent cleanup checks still run and the create-only `0600` result is written afterward.
- **Frontier:** Intent, durable ownership, the closed R001 acceptance matrix, both collector seams, terminal attempt ownership, exact executable target and deadline, complete public identity, the start-before-baseline and exact fresh-ready start corrections, the mutation-sensitive operator root-removal owner, failed-unit systemd normalization, the single package command, complete guide projections, resumed A1 authority, five retained formal FAIL artifacts, one interrupted attempt without a retained artifact, direct proof that earlier accepted-effect recovery settled without scheduler-row mutation, the no-A1-reboot effect boundary, exact F1/F2/F4 scenario admission, Scenario 3's deferred R005 finding, corrected active-spec projection, independent Cursor consultant `AGREE`, and 145 focused checks are settled. Independent review, fresh-context direction verification, specification audit, exact stale-owner recovery, and the next no-reboot A1 gate remain current.
- **Route status:** NHC-8 retains its declared-gate `PASS`, but this plan remains `in-progress` because `NHC-FND-054` and Roadmap `R001` remain open. NHC-FND-045, NHC-FND-049, NHC-FND-052, NHC-FND-105, and every adjacent Git, storage, provider, or credential architecture remain deferred outside this plan.
- **Remaining human-only decisions:** no current human decision blocks local A1 project work. Any change to accepted NanoHost design, credential disclosure boundary, effect vocabulary, non-project host scope, release publication, or important residual-risk acceptance remains human-owned.

An independent verifier returned `PASS` for the recovery blocker. Session records preserve historical claims and partial patches but no complete executable NHC-4C bytes with a final identity; Git object history and the retained workspace likewise contain no runnable candidate or state controller. Reconstructing that apparatus would invent evidence. The old NHC-4C package, fixed queue, controller state, case count, and lease sequence remain historical only.

Current execution therefore reframes from accepted predicates and the actual `e70ad9d9` product bytes. The first live A1 probe proved the repository host manifest after the manifest-owned `/usr/bin/node` projection was provisioned, then observed that NanoCore, NanoHost, Gateway, required worker OCI, and Image Store admission material were not deployed. The repository command projection also passed a literal `--` to host scripts that accept the SSH alias directly; the affected root documentation, cookbook, host guide, and package preflight now use `pnpm host:<command> a1`.

### Current implementation boundary — Acceptance Predicates 35–45

NHC-IMP-1 through NHC-IMP-4 are implemented in the committed foundation: NanoCore owns distinct private runtime records and one sequenced operation; NanoHost owns the static bootstrap, binding injection, and first-poll readiness latch; the worker shim owns one concrete multi-AgentSession Codex Harness and exact native-handle resume; and NanoCore reuses a Sandbox only across its static compatibility key while retaining the existing session-workspace owner in the AgentSession key. The frozen local NHC-IMP-5C bytes now derive the real SessionCompatibilityKey before lease acquisition, include the Context Package contribution only when the exact Workspace checkpoint proves the future launch will prepare it, gate fresh and continuing work on the sole ready RuntimeTarget, preview replacement without effects and commit it only after exact dispatch and lease admission, use one shared next-dispatchable queue selector with an exact pre-transaction identity recheck, preserve sibling bindings and the shared Sandbox on local close, fence uncertainty, remove ordinary App API AgentSession identity and actions, delete duplicate RuntimeTarget capacity state, the unowned generic snapshot or recovery selector, the stale restart-recovery callback, and the unused Core Client or Agent Skill RuntimeTarget-status projection, make Phase 8 effect-free, restore only deterministic result expectations, and fail-stop the successor first-poll response-loss window. The canonical terminology validator now runs from `scripts/` through `check:repo`, outside the finite source-text test exemption policy. The exact accumulated worktree passes `pnpm verify` with exit 0, including repository checks, lint, typecheck, unit tests, coverage, and builds; the observed package totals include Protocol 166, App API schemas 114, Core Client 75, worker shim 170, Web 587, and NanoHost 71 Rust unit tests plus its service test, while the full NanoCore suite and coverage also pass. The corrected host-manifest fixture pair passes 117/117 in both the repository test image and the Mise-managed host, and the canonical AgentSession terminology gate passes.

The engineer resolved the carriage gap recorded by `NHC-FND-024` through Intent Epoch 3, the process-identity ambiguity recorded by `NHC-FND-025` through Intent Epoch 4, and the AgentSession/capacity authority recorded by `NHC-FND-027` through Intent Epoch 6. Implementation checkpoint `959cdc9d` projects all three decisions without expanding OpenCode or Pi beyond `bounded-turn`, adding another Harness, or adding another active-Turn owner. Independent internal reviews accepted the corrected implementation slices, including the Grok-produced product-surface, Skill, Core Client, and host-fixture changes. The first Claude Code accumulated-diff audit returned `BLOCK` on the four exact integration mismatches recorded by `NHC-FND-035`; lowest-sufficient regressions reproduced them, their shared-owner corrections passed focused and full gates, and the same Claude Code session's bounded re-audit confirmed closure after the final App API guide correction. The implementation checkpoint was then committed locally without pushing as `959cdc9d`. This plan still cannot reach `verified`, delete the remaining retired runtime source, or create the final closeout commit until exact A1 and fault evidence accepts that committed implementation. The engineer separately requested and received the intermediate authority-document commit `a93e023d`; that commit does not satisfy product closeout.

The first post-checkpoint A1 provision completed successfully, but strict host assertion then found one and only one changed manifest fact: `/usr/bin/systemctl` reported `systemd 255 (255.4-1ubuntu8.17)` instead of the historically captured `255.4-1ubuntu8.16`. Read-only `apt-cache policy systemd` and `/var/log/dpkg.log` evidence showed that A1 automatically upgraded the installed and candidate package to `.17` on 2026-08-20 and no longer offered `.16`; every other asserted host fact matched. The correction updates the exact manifest and its exact test projection to `.17` rather than downgrading the test host or weakening strict equality. Independent review accepted the two-file correction, both host projections pass 119/119, and the corrected real `pnpm host:assert a1` invocation passes with manifest digest `cb8092dbe85b7e47caeab28ca10f966cdf633b591dc1d643d8762d13dd6f39db`; NanoHost deployment and product acceptance remain pending.

Implementation checkpoint `959cdc9d` and host-manifest checkpoint `0b073bb6` were then deployed as a Linux arm64 NanoCore image and exact NanoHost binary while retaining the prior stopped container and binary. The h2c health endpoint passed through the operator-local SSH forward, but the official host harness used ordinary curl against the allowed loopback `http://` endpoint and failed before authenticated readiness. The bounded NHC-IMP-5D host correction now selects `--http2-prior-knowledge` only for validated loopback HTTP; its Grok-produced regression passes 60/60 in the repository image and Mise-managed host and received independent internal reviewer `PASS` after the HTTPS negative oracle was made existence-sensitive.

The first official exact-byte bring-up subsequently reached NanoHost Gateway health and then failed stop with `nanohost epoch startup failed`; official teardown stopped and decommissioned the attempt and cleared its credential slots. A new authenticated enrollment through the App API then returned `UNIQUE constraint failed: nanohost_integration_identities.deployment_id` because the prior enrollment transaction always inserted while decommission correctly retained the stable identity row. The NanoHost owning specification now defines exact retained-identity re-enrollment, exclusive no-overwrite slot acquisition, conditional exact-owned cleanup, and cleanup-uncertainty blocking; the final specification diff passed independent Verifier and Auditor review. NHC-IMP-5D now implements that existing-owner lifecycle and its local gates pass, so the next evidence step is the checkpoint commit and exact A1 redeployment; the earlier generic epoch-start failure remains an observation to re-test rather than a basis for manual state repair or an unproved product correction.

After the local implementation checkpoint, completion acceptance still requires exact current artifact identities, authenticated NanoHost readiness, one correlated real worker Turn, four scenario results with terminal cleanup and no surviving effect-capable member, and redacted retained evidence that binds each result to the tested commit and environment. Only after those observations may retired-path deletion, projection alignment, final verification, independent acceptance, plan closeout, and the final closeout commit proceed.

The bounded corrections completed before this blocker remain independently verifiable: `pnpm run check:repo` passed; `node --test tests/host-manifest-runtime.test.mjs` passed all 59 tests after `nanohost-bring-up.sh` began treating curl status 28 as a terminal readiness timeout and invoking its existing cleanup instead of continuing up to 89 more polls; and the successful real `pnpm host:assert a1` invocation emitted the accepted manifest digest. The A1 provision step changed only the manifest-owned `/usr/bin/node` projection. No NanoHost product deployment, credential, sandbox, worker Turn, or acceptance scenario was created.
