# Findings

This report preserves non-authorizing observations and dispositions discovered while executing the NanoHost completion plan. The Follow-up Index lists current residue: open items remain owed by this plan, deferred items belong to a named later owner, and every other item is closed as historical execution evidence.

## Follow-up Index

- [ ] `NHC-FND-045` [deferred] Locked-out server deployments have no accepted operator credential-recovery procedure
- [ ] `NHC-FND-049` [deferred] The current A1 data root has no accepted recovery owner for the poisoned disposable Workspace
- [ ] `NHC-FND-052` [deferred] Host-side Git owners conflict with the external-source boundary
- [ ] `NHC-FND-105` [deferred] The historical OpenShell snapshot still has one policy-conformance consumer
- [ ] `NHC-FND-117` [deferred] A1 cannot retain Scenario 3 host-reboot evidence from its colocated controller
- [x] `NHC-FND-120` [closed] F1 accepted an unclassified system-Docker baseline change

## [closed] NHC-FND-001 — The original NHC-2 predicate was not bounded

- **Observation:** NHC-2 combined manifest facts and identity, provisioning mutation, static assertion, credential-bearing bring-up, independently callable teardown, four root commands, four workflow projections, and one thirteen-file aggregate identity. Each review round exposed a different behavior class, and the package reached forty-two truthful events without a package-exit PASS even though every admitted finite artifact check was green.
- **Impact:** `docs/change-execution.md` slice decomposition and planning-drift rules; `docs/verification-instruments.md` bounded-oracle property; the superseded NHC-2 package-level predicate.
- **Evidence:** Raised by: engineer during NHC-2 package exit; independently supported by `/root/nanohost_completion_nhc2_final_verify`.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: freeze three materially distinct packages before further execution: declarative host identity/provision/assert, credential-safe attempt lifecycle, and command/document/aggregate-identity projection. Each package must use only its closed finite vocabulary and stop on any new input class. Recorded transition history: received by this plan as non-authorizing decomposition evidence; NHC-2 is superseded and no further event or work may use its broad predicate. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: received by this plan as non-authorizing decomposition evidence; NHC-2 is superseded and no further event or work may use its broad predicate. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-001.
- **Closure evidence:** Recorded disposition evidence: received by this plan as non-authorizing decomposition evidence; NHC-2 is superseded and no further event or work may use its broad predicate. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-002 — Fixture assertion does not exercise the real assertion comparator

- **Observation:** `assert.sh` fixture mode compares two manifest files byte-for-byte and checks one symlink, while real mode reaches `assert_remote`, which collects and compares architecture, kernel, cgroup, init, and nine command path/version observations. The runtime SSH stub returns a configured exit code instead of executing `assert_remote`. Replacing the real comparator with an accept-all function leaves all fifty-eight candidate checks green, while breaking the shared `provision_node` digest guard fails its checks.
- **Impact:** `docs/verification-instruments.md` Harness Admission; the superseded NHC-2 complete finite mismatch claim; the predecessor Unit E finding that the candidate validated one executable set and executed another.
- **Evidence:** Raised by: engineer during NHC-2 decomposition review; package NHC-2. Reconfirmed evidence: the engineer reran the accept-all `equal` mutation against the pre-repartition candidate and again observed `58/58` green; the assertion core executed zero times, while the `provision_node` control mutation failed two checks. This is retained as the NHC-2A deliberate-FAIL baseline, not as acceptance evidence.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: make fixture and remote modes collect observations separately but pass one normalized fact object through the same comparison owner, and add a deliberate comparator-failure self-check for all six scalar and eighteen command fields. Recorded transition history: admitted to NHC-2A only after NHC-2G fixes the owner contract and its executable projection. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: admitted to NHC-2A only after NHC-2G fixes the owner contract and its executable projection. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-002.
- **Closure evidence:** Recorded disposition evidence: admitted to NHC-2A only after NHC-2G fixes the owner contract and its executable projection. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-003 — Assertion success records no environment identity

- **Observation:** the assertion half exits silently on success and computes no digest of `manifest.json`; the retained-result `manifestDigest` would therefore be copied by a verifier rather than emitted by the environment assertion that produced the evidence.
- **Impact:** `docs/verification-instruments.md` requirement that assertion records the identity it asserted; `docs/change-execution.md` real-use environment identity; NHC-3 result binding.
- **Evidence:** Raised by: engineer during NHC-2 decomposition review; package NHC-2.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: define one exact computed manifest-identity output in the owner, assert it in fixture tests, emit it from the shared assertion path, and make NHC-3 retain that observed value rather than reconstruct it. Recorded transition history: admitted first to NHC-2G, then to NHC-2A for implementation. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: admitted first to NHC-2G, then to NHC-2A for implementation. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-003.
- **Closure evidence:** Recorded disposition evidence: admitted first to NHC-2G, then to NHC-2A for implementation. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-004 — Real-use remote execution debt activated without an owner

- **Observation:** the candidate hardcodes `ssh a1` in four tracked scripts and names it in the cookbook while the active `Real-Use Remote Execution Has No Owner` debt says this manifest-creating plan is its activation condition. No Core, specification, Toolchain rule, or verification owner currently defines the alias, presented identity, identity location, authorized caller, or host-key boundary.
- **Impact:** `docs/verification-instruments.md` Known Debt; root `[PRECEDENCE-003]`; `[SCOPE-002]` prohibition on implicit scope authorization.
- **Evidence:** Raised by: engineer during NHC-2 decomposition review; package NHC-2.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: let `docs/verification-instruments.md`, which already names itself as owner, define one invocation-supplied non-secret SSH alias resolved through operator-local SSH configuration with ordinary host-key verification, no repository credential or trust override, and use limited to directly authorized Tier-4 real-use builders. Recorded transition history: the authority gap is resolved by NHC-2G `PASS`: `docs/verification-instruments.md` now owns the invocation-supplied alias, operator-local identity/key resolution, ordinary host-key verification, and authorized Tier-4 caller boundary. NHC-2A/B must still remove hardcoded alias consumption before A1. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: the authority gap is resolved by NHC-2G `PASS`: `docs/verification-instruments.md` now owns the invocation-supplied alias, operator-local identity/key resolution, ordinary host-key verification, and authorized Tier-4 caller boundary. NHC-2A/B must still remove hardcoded alias consumption before A1. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-004.
- **Closure evidence:** Recorded disposition evidence: the authority gap is resolved by NHC-2G `PASS`: `docs/verification-instruments.md` now owns the invocation-supplied alias, operator-local identity/key resolution, ordinary host-key verification, and authorized Tier-4 caller boundary. NHC-2A/B must still remove hardcoded alias consumption before A1. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-005 — The artifact-review oracle was undeclared and open-ended

- **Observation:** NHC-2 declared two executable finite tables and one four-document review table, but its actual independent review repeatedly found unrelated categories outside those tables: prohibited source-text testing, bring-up origin and cleanup ordering, the same origin defect in direct teardown, and an incomplete escalation packet. The reviewer was effectively instructed to find another defect across a thirteen-file multi-behavior artifact.
- **Impact:** `docs/verification-instruments.md` bounded-oracle property; `docs/change-execution.md` package decomposition and event-budget rules.
- **Evidence:** Raised by: engineer during NHC-2 decomposition review; package NHC-2.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: convert strict-risk review into closed matrices: five accepted and seven rejected origin classes times each of the two URL/credential consumers, plus the explicitly named cleanup terminal edges. Restrict projection review to its exact command, preflight, document, retained-path, and digest checklist. Recorded transition history: received by NHC-2G/NHC-2T/NHC-2A/NHC-2B/NHC-2C decomposition; no successor package uses an open-ended category search as its deciding oracle. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: received by NHC-2G/NHC-2T/NHC-2A/NHC-2B/NHC-2C decomposition; no successor package uses an open-ended category search as its deciding oracle. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-005.
- **Closure evidence:** Recorded disposition evidence: received by NHC-2G/NHC-2T/NHC-2A/NHC-2B/NHC-2C decomposition; no successor package uses an open-ended category search as its deciding oracle. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-006 — Host assertion is not attached to the existing preflight slot

- **Observation:** `docs/verification-instruments.md` requires the assertion half to run from the existing `:preflight` slot. `test:e2e:nano-restart:preflight` currently checks only builds, runner checks, shell syntax, and local runtime tests; the candidate adds parallel `host:*` commands without attaching `host:assert`. Bring-up does call assertion before service start, which satisfies only the per-attempt half.
- **Impact:** `docs/verification-instruments.md` Real-Use Host Manifest preflight rule; root command ownership in `package.json`.
- **Evidence:** Raised by: engineer during NHC-2 decomposition review; package NHC-2.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: extend only the existing `test:e2e:nano-restart:preflight` command to invoke the reviewed assertion half; add no second preflight or wrapper. Recorded transition history: admitted to NHC-2C after NHC-2T/A/B pass. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: admitted to NHC-2C after NHC-2T/A/B pass. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-006.
- **Closure evidence:** Recorded disposition evidence: admitted to NHC-2C after NHC-2T/A/B pass. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-007 — Host-manifest Known Debt projection becomes stale on realization

- **Observation:** `The Real-Use Host Manifest Is Decided And Not Implemented` remains active text, but no original NHC-2 lease included `docs/verification-instruments.md`; NHC-7 owns NanoHost topology projection rather than this instrument owner.
- **Impact:** root `[DOC-010]`; `docs/verification-instruments.md` Known Debt lifecycle.
- **Evidence:** Raised by: engineer during NHC-2 decomposition review; package NHC-2.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: in the same owner change that defines remote execution and assertion identity, rewrite the debt as current implementation obligations until NHC-2A/C pass, then remove the false “not implemented” statement without claiming real-host completeness before NHC-3. Recorded transition history: admitted to NHC-2G. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: admitted to NHC-2G. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-007.
- **Closure evidence:** Recorded disposition evidence: admitted to NHC-2G. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-008 — Verification-instrument owner lease omitted its declared executable projection

- **Observation:** `docs/verification-instruments.md` declares `tests/verification-instruments-contract.test.mjs` as its executable projection, but NHC-2R froze a one-file owner lease and made the projection immutable. Removing the two resolved debt headings therefore produced `14/15`: the unchanged projection still required both deleted debts.
- **Impact:** root `[DOC-010]` and `[CHECK-019]`; `docs/verification-instruments.md` self-declared executable projection; NHC-2R debt-disposition predicate.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc2r_entry_review_fresh` during NHC-2R artifact review.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: resolve only through NHC-FND-009's NHC-2G three-file local-rule/projection/owner matrix; add no test file, helper, input class, identity transfer, or package wrapper. Recorded transition history: historical unaccepted evidence. NHC-2R is superseded, and NHC-2O authorizes nothing because it reused the same predicate and crossed the test-local authority boundary. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: historical unaccepted evidence. NHC-2R is superseded, and NHC-2O authorizes nothing because it reused the same predicate and crossed the test-local authority boundary. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-008.
- **Closure evidence:** Recorded disposition evidence: historical unaccepted evidence. NHC-2R is superseded, and NHC-2O authorizes nothing because it reused the same predicate and crossed the test-local authority boundary. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-009 — The projection correction crosses the test-local authority boundary

- **Observation:** NHC-2O attempted to treat the omitted projection as a fresh logical slice, but it retains NHC-2R's exact eight-decision and two-debt predicate. `docs/change-execution.md` requires that shape to remain a cumulative subdivision. Separately, `tests/AGENTS.md` permits source-text assertions only for `agents-root-contract.test.mjs`, while the required correction is inside the Markdown-source assertions in `tests/verification-instruments-contract.test.mjs`.
- **Impact:** `docs/change-execution.md` slice/subdivision identity; root `[PRECEDENCE-003]`; `tests/AGENTS.md` source-text prohibition; `docs/verification-instruments.md` executable-projection declaration.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc2o_entry_review` during NHC-2O entry review. Planning-drift evidence: NHC-2R fixed denominator `1`; opening NHC-2O for the unchanged eight-decision and two-debt predicate made the cumulative numerator `2`. The first matrix omitted its declared projection, and the second reopened that same predicate under a fresh identity and budget, so the current plan has failed its decomposition.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: NHC-2G freezes the exact local-rule/projection/owner matrix as unopened remainder. It transfers no NHC-2O identity, budget, or verdict. Predicted observable remains: the local guide names exactly one additional exception, the focused projection reaches `15/15`, and no host/product/A1/Docker/new-test surface changes. Framework hypothesis: none. Recorded transition history: the standing engineer authorization activated after escalation verifier `PASS`; NHC-2O is superseded and NHC-2G receives the exact three-file correction. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: the standing engineer authorization activated after escalation verifier `PASS`; NHC-2O is superseded and NHC-2G receives the exact three-file correction. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-009.
- **Closure evidence:** Recorded disposition evidence: the standing engineer authorization activated after escalation verifier `PASS`; NHC-2O is superseded and NHC-2G receives the exact three-file correction. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-010 — The one-unit test repartition froze an impossible count

- **Observation:** the current `58/58` candidate has one top-level test that combines the manifest-vocabulary assertion with every aggregate-identity assertion. Separate manifest and projection owners require splitting that mixed case, which raises the truthful total to fifty-nine; retaining fifty-eight would require deleting or merging a case, module side effects, a cross-file helper, or hidden ownership, all forbidden by the frozen NHC-2T predicate.
- **Impact:** `docs/change-execution.md` unit capacity and plan-failure decomposition; `docs/verification-instruments.md` bounded-oracle rule; NHC-2T's exact ownership and test-count predicate.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc2t_tests` before any leased write; package NHC-2T.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: use two materially different packages: projection extraction with the necessary `58→59` mixed-test split, then manifest/provision/assertion ownership movement with `59→59`. Transfer no identity, denominator, budget, or verdict. Recorded transition history: accepted after independent escalation verifier `PASS`; NHC-2T is discarded with no artifact and no later event. The standing engineer split disposition activates NHC-2TP then NHC-2TM as unopened remainder. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: accepted after independent escalation verifier `PASS`; NHC-2T is discarded with no artifact and no later event. The standing engineer split disposition activates NHC-2TP then NHC-2TM as unopened remainder. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-010.
- **Closure evidence:** Recorded disposition evidence: accepted after independent escalation verifier `PASS`; NHC-2T is discarded with no artifact and no later event. The standing engineer split disposition activates NHC-2TP then NHC-2TM as unopened remainder. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-011 — The manifest-test move assumed a nonexistent private helper closure

- **Observation:** `runHostScript` and `requireSuccess` are used both by the two manifest/provision/assertion blocks NHC-2TM would move and by the readiness block that must remain in the runtime test. The frozen move-only package therefore cannot move a private helper closure because that closure is shared.
- **Impact:** `docs/change-execution.md` exact lease and coherent seam rules; root `[QUALITY-006]` and `[QUALITY-014]`; NHC-2TM's no-cross-file-helper and move-only predicate.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc2tm_entry_review` before any producer opened; package NHC-2TM.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: establish one demonstrated shared fixture-runner owner under `tests/support/host/`, align its local guide, move both current consumers to it while moving the two manifest blocks, update aggregate identity `14→15`, and keep `59/59`; transfer no NHC-2TM identity, denominator, budget, or verdict. Recorded transition history: accepted after independent escalation verifier `PASS`; NHC-2TM is discarded with no artifact and no later event. The standing engineer split disposition activates NHC-2MS as unopened remainder. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: accepted after independent escalation verifier `PASS`; NHC-2TM is discarded with no artifact and no later event. The standing engineer split disposition activates NHC-2MS as unopened remainder. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-011.
- **Closure evidence:** Recorded disposition evidence: accepted after independent escalation verifier `PASS`; NHC-2TM is discarded with no artifact and no later event. The standing engineer split disposition activates NHC-2MS as unopened remainder. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-012 — SSH-alias grammar has four executable owners

- **Observation:** after NHC-2A and NHC-2B behavior passed, the same authoritative `[a-z][a-z0-9-]{0,62}` alias grammar remained independently implemented in `provision.sh`, `assert.sh`, `nanohost-bring-up.sh`, and `teardown.sh`.
- **Impact:** root `[QUALITY-006]`; `docs/change-execution.md` coherence proposition 5; the Tier-4 verification-only remote-host boundary in `docs/verification-instruments.md`.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc2b_verify`; package NHC-2B.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: establish one shell alias-validation owner under `tests/support/host/`, make exactly the four existing consumers source and call it before effects, retain the exact finite alias tables, and remove every parallel grammar copy without changing an accepted or rejected class. Recorded transition history: NHC-2B stops at its exact 24-event ceiling; admitted to materially different NHC-2H under the engineer's standing split instruction. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: NHC-2B stops at its exact 24-event ceiling; admitted to materially different NHC-2H under the engineer's standing split instruction. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-012.
- **Closure evidence:** Recorded disposition evidence: NHC-2B stops at its exact 24-event ceiling; admitted to materially different NHC-2H under the engineer's standing split instruction. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-013 — Untracked candidates prevented incremental package yield

- **Observation:** NHC-2B's test and two scripts remained untracked from predecessor work, so ordinary Git diff omitted them and no honest package-increment diff could be reconstructed after implementation; no-index comparison reported only the whole current additions.
- **Impact:** `docs/change-execution.md` package-exit yield and state-fidelity requirements; the engineer rule to recompute focused scope from the actual exit diff.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc2b_verify`; package NHC-2B.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: use the staged object identities solely as the NHC-2H mechanical before-state, compute its exit diff against that exact index, and retain one final repository commit; staging conveys no package acceptance and does not rewrite prior evidence. Recorded transition history: accepted as a preserved evidence limitation for NHC-2B; NHC-2H must take an explicit pre-write Git-index snapshot of its exact existing inputs before its builder opens. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: accepted as a preserved evidence limitation for NHC-2B; NHC-2H must take an explicit pre-write Git-index snapshot of its exact existing inputs before its builder opens. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-013.
- **Closure evidence:** Recorded disposition evidence: accepted as a preserved evidence limitation for NHC-2B; NHC-2H must take an explicit pre-write Git-index snapshot of its exact existing inputs before its builder opens. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-014 — Shared helper sourcing breaks streamed remote halves

- **Observation:** NHC-2H moved alias validation into `ssh-alias.sh` but sourced that sibling before the `remote` branch in `provision.sh` and `assert.sh`. Each external command streams only its own script body to remote Bash, so the sibling helper is absent and both real remote halves exit before reaching their fact logic; the existing SSH stub short-circuits the payload and leaves all 117 checks green.
- **Impact:** `docs/verification-instruments.md` shared provision/assert implementation and Harness Admission rules; root `[TEST-009]`, `[QUALITY-014]`, and `[CHECK-019]`; NHC-2H's unchanged internal-mode predicate.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc2h_verify`; package NHC-2H.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: add one exact executable provision payload test with the existing Node-source failure marker, then one separate assertion payload test with a predeclared Bash-xtrace `/usr/bin/node` attempt marker; source and invoke the alias helper only on the external invocation path while leaving each streamed remote payload self-contained. Recorded transition history: NHC-2H stops at its exact 26-event ceiling; NHC-2I is discarded at entry because its two rows did not have equally sound positive observations. The standing engineer split instruction routes provision to NHC-2J and assertion plus final shared-owner adoption to NHC-2K without reopening either predecessor. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: NHC-2H stops at its exact 26-event ceiling; NHC-2I is discarded at entry because its two rows did not have equally sound positive observations. The standing engineer split instruction routes provision to NHC-2J and assertion plus final shared-owner adoption to NHC-2K without reopening either predecessor. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-014.
- **Closure evidence:** Recorded disposition evidence: NHC-2H stops at its exact 26-event ceiling; NHC-2I is discarded at entry because its two rows did not have equally sound positive observations. The standing engineer split instruction routes provision to NHC-2J and assertion plus final shared-owner adoption to NHC-2K without reopening either predecessor. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-015 — Two streamed payloads require different positive observations

- **Observation:** NHC-2I combined provision and assertion into one two-row package, but only provision had an exact positive downstream marker. The assertion row merely forbade helper/bootstrap errors, so a silent early exit could satisfy it without reaching fact collection or comparison.
- **Impact:** `docs/verification-instruments.md` sound/converted-oracle requirements; `tests/AGENTS.md` prohibition on source-text assertions; NHC-2I exact oracle predicate.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc2i_entry_review`; package NHC-2I.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: provision observes exact `Node source is not executable.` after entering remote logic; assertion uses Bash xtrace to observe the attempted absolute `/usr/bin/node -e` fact-collection command before local command lookup, without claiming local host compatibility or inspecting source text. Recorded transition history: accepted before any producer opened. NHC-2I is discarded under its no-correction disposition; NHC-2J and NHC-2K receive one payload and one distinct positive observation each with no identity, budget, or verdict transfer. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: accepted before any producer opened. NHC-2I is discarded under its no-correction disposition; NHC-2J and NHC-2K receive one payload and one distinct positive observation each with no identity, budget, or verdict transfer. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-015.
- **Closure evidence:** Recorded disposition evidence: accepted before any producer opened. NHC-2I is discarded under its no-correction disposition; NHC-2J and NHC-2K receive one payload and one distinct positive observation each with no identity, budget, or verdict transfer. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-016 — Assertion trace marker names the command but not its subject

- **Observation:** both `collect_remote_facts()` and `assert_facts()` invoke `/usr/bin/node -e`. NHC-2K's trace regression matches that command without naming the active function, so inserting `return 1` at the collector start still passes from the later comparator invocation.
- **Impact:** `docs/verification-instruments.md` sound/converted-oracle and Harness Admission requirements; the engineer's requirement that every predicate carry a positive subject-bound observation; NHC-2K exact oracle.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc2k_verify`; package NHC-2K.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: make Bash `PS4` include the active function name and require the exact `collect_remote_facts` plus `/usr/bin/node -e` trace; verify a scratch early return removes that marker. Record streamed-script ordering, test/runner ownership, and positive subject-bound marker requirements in `tests/support/host/README.md` before NHC-2C. Recorded transition history: NHC-2K stops at its exact 16-event ceiling; stable assertion implementation remains unaccepted. The next bounded harness-contract package must correct only the test marker, prove the collector-bypass mutation RED, record the three engineer-supplied host-harness structure facts in the local guide, and then independently adopt the read-only assertion/shared-owner candidate. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: NHC-2K stops at its exact 16-event ceiling; stable assertion implementation remains unaccepted. The next bounded harness-contract package must correct only the test marker, prove the collector-bypass mutation RED, record the three engineer-supplied host-harness structure facts in the local guide, and then independently adopt the read-only assertion/shared-owner candidate. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-016.
- **Closure evidence:** Recorded disposition evidence: NHC-2K stops at its exact 16-event ceiling; stable assertion implementation remains unaccepted. The next bounded harness-contract package must correct only the test marker, prove the collector-bypass mutation RED, record the three engineer-supplied host-harness structure facts in the local guide, and then independently adopt the read-only assertion/shared-owner candidate. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-017 — Harness contract matrix miscounted its aggregate controls

- **Observation:** NHC-2L claimed sixteen read-only controls even though the future aggregate has sixteen total artifacts and two of those were writable, leaving fourteen. Separately, the still-planned NHC-2C required a `15→16` projection update while leaving the projection test read-only.
- **Impact:** `docs/change-execution.md` exact artifact-inventory/lease rules; the engineer's requirement that artifact ownership be modeled before freeze; NHC-2L and planned NHC-2C matrices.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc2l_entry_review`; package NHC-2L.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: use the staged exact sixteen-path inventory as the denominator, assign the two tests to U1 and six command/document files to U2, enumerate the remaining eight read-only paths, and admit no producer until one entry reviewer reproduces `8 + 8 = 16` plus the collector-specific mutation. Recorded transition history: accepted before any NHC-2L producer. NHC-2L is discarded. Still-unopened NHC-2C is refrozen as two disjoint units: two writable tests, six writable command/document projections, and eight exact read-only aggregate controls. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: accepted before any NHC-2L producer. NHC-2L is discarded. Still-unopened NHC-2C is refrozen as two disjoint units: two writable tests, six writable command/document projections, and eight exact read-only aggregate controls. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-017.
- **Closure evidence:** Recorded disposition evidence: accepted before any NHC-2L producer. NHC-2L is discarded. Still-unopened NHC-2C is refrozen as two disjoint units: two writable tests, six writable command/document projections, and eight exact read-only aggregate controls. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-018 — Real A1 proof has no deployed environment prerequisite

- **Observation:** the first NHC-3 redacted prerequisite probe proves `ssh a1` is reachable, but both NanoHost and NanoCore units, NanoHost owner configuration, NanoCore origin, configured identity/deployment, and safe-sink metadata are absent. The cookbook requires those facts before its four commands, while NHC-3 may write only the validator/result and NHC-4 is currently closed on NHC-3 `PASS`.
- **Impact:** `docs/verification-instruments.md` real-use environment identity; `docs/toolchain.md` host-manifest deployment boundary; NHC-3 prerequisite and NHC-4 dependency ordering.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc3_builder` during NHC-3 fixture preparation before `provisionExit`; no cycle or external effect was consumed.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: discard the zero-cycle dedicated NHC-3 wrapper and use the still-unopened NHC-4 real-dependency run as both the manifest's first real bring-up and Unit E rehearsal. Invoke the existing manifest provision/assert boundary before the Unit E closure, retain Unit E's artifact/environment identity pair, and keep its mandatory cleanup. Evidence that changes this recommendation is an accepted deployment owner that supplies NHC-3's prerequisites without Unit E, or an owning authority that requires a separate two-cycle root-command proof rather than one real bring-up. Recorded transition history: the corrected escalation passed independent verification. NHC-3 is stopped and discarded with zero consumed cycles and no `result.json`; NHC-4 is the authorized single first-real-dependency boundary after read-only A1 fact collection and one exact freeze. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: the corrected escalation passed independent verification. NHC-3 is stopped and discarded with zero consumed cycles and no `result.json`; NHC-4 is the authorized single first-real-dependency boundary after read-only A1 fact collection and one exact freeze. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-018.
- **Closure evidence:** Recorded disposition evidence: the corrected escalation passed independent verification. NHC-3 is stopped and discarded with zero consumed cycles and no `result.json`; NHC-4 is the authorized single first-real-dependency boundary after read-only A1 fact collection and one exact freeze. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-019 — The first NHC-3 ordering recommendation contradicts Unit E cleanup

- **Observation:** the passed escalation predicted that NHC-4 would finish with NanoCore, NanoHost, configuration, origin, identity/deployment, and safe-sink metadata still available for NHC-3. The existing Unit E rehearsal instead stops and removes all attempt units, `/home/ubuntu/wp6-v3`, `/run/openkit`, `/var/lib/openkit`, and attempt credentials before it can retain `PASS`.
- **Impact:** `docs/verification-instruments.md` first-real-bring-up and product-state separation rules; `docs/change-execution.md` single write-owning package rule; Unit E mandatory cleanup; NHC-3/NHC-4 dependency ordering.
- **Evidence:** Raised by: orchestrator during pre-freeze authority reconciliation after the first NHC-3 escalation verifier `PASS`; program-level dispatch correction.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: remove the redundant dedicated real-use wrapper rather than create persistent deployment state or concurrent write-owning packages. Let corrected NHC-4 run manifest provision/assert before its existing real-dependency closure, retain the generic manifest digest plus the Unit E closure and complete host-prerequisite digests, treat the manifest-managed Node projection as long-lived machine state, and clean only Unit E product state through the existing owner. Update the two active host workflow guides in the same package; transfer no NHC-3 identity, budget, denominator, verdict, result, or cycle. Recorded transition history: continued independent escalation verification `PASS`; the standing engineer authorization activated the corrected ordering. No NHC-4 freeze or write has opened yet. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: continued independent escalation verification `PASS`; the standing engineer authorization activated the corrected ordering. No NHC-4 freeze or write has opened yet. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-019.
- **Closure evidence:** Recorded disposition evidence: continued independent escalation verification `PASS`; the standing engineer authorization activated the corrected ordering. No NHC-4 freeze or write has opened yet. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-020 — The integrated Unit E fixture substitutes identity stubs for effectful tools

- **Observation:** NHC-4's offline fixture maps all forty frozen A1 command names to one `identity-tool` that can report identities, versions, and canned observations but does not implement the filesystem, archive, process, or service effects of commands such as `cp`, `mkdir`, `rm`, `tar`, `systemctl`, and `mktemp`. The same frozen package requires that verified command root to become the sole `PATH` for the complete fixture and real Unit E rehearsal, so the valid offline fixture cannot execute after its preflight succeeds.
- **Impact:** `docs/verification-instruments.md` effect-domain and oracle-conversion rules; `docs/change-execution.md` unit-capacity and decomposition rules; NHC-4's fixture/real shared-command-root and complete-rehearsal predicate.
- **Evidence:** Raised by: `/root/nanohost_completion_nhc4_builder` after the corrected finite identity matrix passed and before dependency materialization or any NHC-4 A1 effect; independently confirmed by `/root/nanohost_nhc4_decomposition_verify`.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: the first split isolated finite host identity/root construction from real Unit E execution and added one behavior-complete local `mkdir → cp → tar → rm` smoke without simulating the other commands. NHC-4A then exposed the separate test-control bypass recorded as NHC-FND-021 and stopped without `PASS`; NHC-4C now owns only that two-file shared command boundary. NHC-4B remains the distinct Tier-4 real A1 Unit E rehearsal after NHC-4C passes. Framework hypothesis remains raised for the original integrated freeze: it omitted the executable-set structure check and accepted an identity-only stub as a complete command environment. Recorded transition history: accepted after independent escalation verification `PASS`. NHC-4 is stopped and superseded; its partial bytes remain unaccepted adoption candidates and transfer no identity, budget, denominator, verdict, correction history, or `PASS`. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: accepted after independent escalation verification `PASS`. NHC-4 is stopped and superseded; its partial bytes remain unaccepted adoption candidates and transfer no identity, budget, denominator, verdict, correction history, or `PASS`. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-020.
- **Closure evidence:** Recorded disposition evidence: accepted after independent escalation verification `PASS`. NHC-4 is stopped and superseded; its partial bytes remain unaccepted adoption candidates and transfer no identity, budget, denominator, verdict, correction history, or `PASS`. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-021 — A test log variable bypasses the shared command boundary

- **Observation:** `preflight_unit_inputs` binds and exports the assembled command root only when `WP6_ADMISSION_CALL_LOG` is empty. When that test-only variable is non-empty, the same function returns success while preserving ambient `PATH`; real callers then consume that ambient value as the command root. The 243-case positive smoke initially proved only the normal branch, so NHC-4A would have accepted a shared preflight with an undeclared test-control success path
- **Impact:** `docs/verification-instruments.md` shared real-use command environment and oracle-conversion rules; root `[TEST-011]`, `[SCOPE-004]`, and `[SCOPE-010]`; NHC-4A's exact sole-PATH/no-fallback predicate and one-correction disposition
- **Evidence:** Raised by: `/root/nanohost_completion_nhc4a_verify`; package NHC-4A at package exit
- **Owner:** Not recorded.
- **Next action:** Accepted correction: NHC-4C owns only `self-check.sh` and `unit-facts.sh`. The test log remains observation-only: both empty and non-empty rows must succeed with exact root-only `PATH`, while ambient append and nonfunctional-`cp` mutations remain RED. Independent review confirms the five direct callers still execute through the shared preflight and do not grant logging command-selection authority. NHC-4B stays unopened until NHC-4C passes Recorded transition history: accepted. NHC-4A stops and is superseded at exactly `18/18`; no A1, Docker, network, credential, dependency, product, or Unit E effect occurred. Its retained bytes and evidence transfer no identity, budget, denominator, verdict, correction history, or `PASS` No further action remains under this finding.
- **Closing verdict:** Recorded disposition: accepted. NHC-4A stops and is superseded at exactly `18/18`; no A1, Docker, network, credential, dependency, product, or Unit E effect occurred. Its retained bytes and evidence transfer no identity, budget, denominator, verdict, correction history, or `PASS` Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-021.
- **Closure evidence:** Recorded disposition evidence: accepted. NHC-4A stops and is superseded at exactly `18/18`; no A1, Docker, network, credential, dependency, product, or Unit E effect occurred. Its retained bytes and evidence transfer no identity, budget, denominator, verdict, correction history, or `PASS` Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-022 — Four packages produced no tracked change

- **Observation:** four of fourteen revision-397 `yield_observed` records report no tracked change: NHC-2T records `0 files changed`, NHC-2TM records `0 files changed`, NHC-4 records only seven ignored temp artifacts with no ordinary Git diff, and NHC-4A records `0 tracked files changed` for its adopted builder candidate plus one ignored temp test candidate.
- **Impact:** `docs/change-execution.md` queued-package necessity recheck and package-exit yield expectations; root `[EVID-001]`, `[SCOPE-012]`, and `[QUALITY-016]`; the program expectation that a package's observable yield carry its coordination cost.
- **Evidence:** Raised by: independent verifier roles `/root/nanohost_completion_nhc2t_escalation_verify` for NHC-2T, `/root/nanohost_completion_nhc2tm_escalation_verify` for NHC-2TM, `/root/nanohost_nhc4_decomposition_verify` for NHC-4, and `/root/nanohost_completion_nhc4a_verify` for NHC-4A; synthesized by the current plan-revision builder outside every package's frozen scope.
- **Owner:** Not recorded.
- **Next action:** Proposed improvement: run the named necessity and artifact-inventory probes before each queued entry gate, discard an unnecessary or internally impossible package before assignment, and freeze only the smallest present seam whose tracked or explicitly retained evidence yield can be stated without consulting coordination prose. Recorded transition history: program-level non-authorizing finding. The four historical yields and artifacts remain immutable evidence, and this finding does not reopen or reinterpret any package. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: program-level non-authorizing finding. The four historical yields and artifacts remain immutable evidence, and this finding does not reopen or reinterpret any package. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-022.
- **Closure evidence:** Recorded disposition evidence: program-level non-authorizing finding. The four historical yields and artifacts remain immutable evidence, and this finding does not reopen or reinterpret any package. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-023 — The accepted shared-Harness lifecycle is not implemented

- **Observation:** the accepted NanoHost specification declares implementation complete only after Acceptance Predicates 35 through 45 are observed, while its Current Implementation Projection explicitly states that multiple open AgentSessions, long-lived Harness monitor interpretation, fixed Harness operations, private runtime-record decomposition, compatibility keys, separate open-session capacity, private Harness carriage, Codex exact-handle resume, and operation non-redelivery are not implemented. Current code contains no `SandboxRuntimeRecord`, `HarnessInstanceRecord`, `AgentSessionRuntimeBinding`, shared-Sandbox compatibility owner, or six-operation Harness contract that could satisfy those predicates.
- **Impact:** `docs/specs/20260802-nanohost_runtime_and_transport.md` Acceptance Predicates 35–45 and Current Implementation Projection; this plan's intent to complete every unsettled NanoHost runtime predicate; root `[AUTH-003]`, `[DOC-017]`, `[QUALITY-003]`, and `[CHECK-019]`.
- **Evidence:** Raised by: the independent recovery-blocker verifier after confirming that Reframe is permitted from the `e70ad9d9e0d69869dd4765231beb01f61f53fc3f` product baseline.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: Intent Epoch 5 and NHC-IMP-1 through NHC-IMP-5 now define the owner-coherent implementation order for Acceptance Predicates 35–45, beginning with the three missing private runtime projections plus the existing distinct Turn lease, then private carriage, Codex continuity, shared-Sandbox compatibility and capacity, and real acceptance. The historical package controller remains unrestored. Recorded transition history: admitted remaining product work that blocks plan verification until implemented. This is a missing product lifecycle spanning NanoCore scheduler/session ownership, NanoHost bridge and Harness lifetime, Sandbox Integration, private runtime projections, capacity, restart recovery, and diagnostics; it is not a deployment correction or a focused residual bug and cannot be replaced by WP6 evidence apparatus. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: admitted remaining product work that blocks plan verification until implemented. This is a missing product lifecycle spanning NanoCore scheduler/session ownership, NanoHost bridge and Harness lifetime, Sandbox Integration, private runtime projections, capacity, restart recovery, and diagnostics; it is not a deployment correction or a focused residual bug and cannot be replaced by WP6 evidence apparatus. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-023.
- **Closure evidence:** Recorded disposition evidence: admitted remaining product work that blocks plan verification until implemented. This is a missing product lifecycle spanning NanoCore scheduler/session ownership, NanoHost bridge and Harness lifetime, Sandbox Integration, private runtime projections, capacity, restart recovery, and diagnostics; it is not a deployment correction or a focused residual bug and cannot be replaced by WP6 evidence apparatus. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-024 — The six Harness operations have no accepted carriage or admission seam

- **Observation:** the accepted worker-control owner defines `session.open`, `session.inspect`, `turn.start`, `turn.interrupt`, `session.close`, and `harness.drain`, but the accepted NanoHost owner exposes exactly eight outer effect command/result pairs and requires `bridge.open` to carry no AgentSession, Turn, package, Context, route credential, or derived route material. The nested H2 connection is initiated by Sandbox Integration toward NanoHost and currently admits only `/worker-control`, `/inference`, and `/capabilities`; the worker-control command family contains only `interrupt`, while route credentials are created per Turn and therefore cannot authenticate `session.open`. No accepted owner defines a Harness-operation envelope, pre-Turn authentication, request/result identity, reconnect behavior, or recovery semantics across that boundary.
- **Impact:** `docs/specs/20260703-worker_control_protocol.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md` Acceptance Predicates 29, 30, and 35–42; root `[PRECEDENCE-003]`, `[AUTH-001]`, `[DOC-017]`, and `[SCOPE-007]`.
- **Evidence:** Raised by: the independent recovery-blocker verifier after tracing NanoCore dispatch, NanoHost fair polling, nested H2 direction, Sandbox Integration routes, current worker-control admission, and the shared-Harness design commit.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: implement exactly `POST /worker-control/harness/poll` and `POST /worker-control/harness/result` outside both the eight NanoHost effect pairs and the bounded-turn worker `interrupt` queue. A ninth outer effect, fourth semantic namespace, second connection, client-supplied binding, or restored Turn credential in `bridge.open` remains inadmissible. Recorded transition history: resolved and accepted. Intent Epoch 3 selected the Integration-initiated, connection- and Harness-bound private pull route under `/worker-control/*`; the owning specifications now define its exact paths, envelopes, admission, sequence, refusal, idempotency, reconnect, interrupt ownership, cadence, cleanup, and recovery semantics and received unconditional independent Verifier and Auditor `PASS` verdicts. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved and accepted. Intent Epoch 3 selected the Integration-initiated, connection- and Harness-bound private pull route under `/worker-control/*`; the owning specifications now define its exact paths, envelopes, admission, sequence, refusal, idempotency, reconnect, interrupt ownership, cadence, cleanup, and recovery semantics and received unconditional independent Verifier and Auditor `PASS` verdicts. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-024.
- **Closure evidence:** Recorded disposition evidence: resolved and accepted. Intent Epoch 3 selected the Integration-initiated, connection- and Harness-bound private pull route under `/worker-control/*`; the owning specifications now define its exact paths, envelopes, admission, sequence, refusal, idempotency, reconnect, interrupt ownership, cadence, cleanup, and recovery semantics and received unconditional independent Verifier and Auditor `PASS` verdicts. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-025 — Shared-Harness continuity conflicts with every accepted adapter contract

- **Observation:** Core defines one AgentSession as continuity for one native conversation across multiple sequential Turns, the continuity owner requires reuse of the exact native conversation, worker-control requires `session.open` plus sequential `turn.start`, and NanoHost predicates 35 and 40 require a restricted native conversation handle with fresh per-Turn authority. The shared adapter contract exposes only `prepare` and `collect`, returns no native session metadata, and excludes continuation. Codex, OpenCode, and Pi each declare multi-turn native resume unsupported and remove their turn-scoped native state; current worker-shim code implements those one-shot contracts. Commit `2e27585b2` added the shared-Harness continuity requirements without amending those owners.
- **Impact:** `docs/core/agent-session.md`; `docs/specs/20260704-agent_session_continuity.md`; `docs/specs/20260629-worker_runtime_communication_model.md`; all three `docs/specs/20260716-*_worker_adapter.md` owners; `docs/specs/20260703-worker_control_protocol.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md` Acceptance Predicates 35 and 40; root `[PRECEDENCE-003]`, `[AUTH-001]`, and `[DOC-017]`.
- **Evidence:** Raised by: the independent recovery-blocker verifier after tracing the accepted owners, current adapter and shim implementation, and the design commit that introduced the shared-Harness requirements.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: the shared adapter and Codex owners now define process-independent native conversation open, start or exact resume, inspect, interrupt, close, retained-state hygiene, and restart or cleanup proof. Codex alone declares `session-continuity`; OpenCode and Pi explicitly remain `bounded-turn` and ineligible for shared-Harness placement rather than receive speculative implementations. Recorded transition history: resolved interpretation finding. Intent Epoch 4 confirms that AgentSession continuity never requires one OS Agent process or native runtime instance to survive across Turns or be shared among sibling AgentSessions; the Harness may supervise one independent Codex instance per resident AgentSession, and a later process instance may continue the exact conversation through its restricted native handle. The current one-shot adapter contracts remain an admitted implementation gap, not a conflict with that topology. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved interpretation finding. Intent Epoch 4 confirms that AgentSession continuity never requires one OS Agent process or native runtime instance to survive across Turns or be shared among sibling AgentSessions; the Harness may supervise one independent Codex instance per resident AgentSession, and a later process instance may continue the exact conversation through its restricted native handle. The current one-shot adapter contracts remain an admitted implementation gap, not a conflict with that topology. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-025.
- **Closure evidence:** Recorded disposition evidence: resolved interpretation finding. Intent Epoch 4 confirms that AgentSession continuity never requires one OS Agent process or native runtime instance to survive across Turns or be shared among sibling AgentSessions; the Harness may supervise one independent Codex instance per resident AgentSession, and a later process instance may continue the exact conversation through its restricted native handle. The current one-shot adapter contracts remain an admitted implementation gap, not a conflict with that topology. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-026 — Pre-listen NanoHost cleanup creates a startup cycle

- **Observation:** an A1 restart with one real `cleanup-pending` backend session reached scheduler Phase 8, which awaited the NanoHost backend's existing `bridge.close` and `sandbox.delete` effects. Those effects can settle only through the one authoritative NanoHost connection, but NanoCore binds the sole listener that admits that connection only after Phase 8 completes. With no listener or other active handle, Node reported unsettled top-level await and exited 13; restarting NanoHost cannot resolve the cycle because its initial outer connection also requires that listener.
- **Impact:** `docs/specs/20260704-nanocore_bootstrap_readiness.md` Phase 8 and listener ordering; `docs/specs/20260703-durable_scheduler_design.md` cleanup ownership and capacity fence; `docs/specs/20260802-nanohost_runtime_and_transport.md` one authoritative connection and no fallback; root `[PRECEDENCE-002]`, `[SCOPE-007]`, and `[TEST-009]`.
- **Evidence:** Raised by: the primary agent from exact A1 process, service, database, and journal evidence; independently reviewed by `/root/nanohost_recovery_blocker` without A1 or credential access.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: NHC-IMP-5A clarifies the boot and scheduler owners, leaves Phase 8 responsible only for durable classification, fencing, read-only restoration, and capacity preservation, and extends the existing scheduler maintenance seam to serialize `cleanup-pending`, `cleanup-failed`, and effectful accepted-final-status closeout after the ordinary listener exists. It adds no durable coordinator, cleanup queue, timer family, listener, route, connection, or replay path; failure or uncertainty keeps capacity fenced. Recorded transition history: admitted implementation contradiction with independent Verifier `PASS`; no engineer architecture choice is required because accepted owners already require an effect-free pre-listen scan and post-listen ordinary effects. The live A1 state remains untouched and blocked until the corrected product path consumes it. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: admitted implementation contradiction with independent Verifier `PASS`; no engineer architecture choice is required because accepted owners already require an effect-free pre-listen scan and post-listen ordinary effects. The live A1 state remains untouched and blocked until the corrected product path consumes it. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-026.
- **Closure evidence:** Recorded disposition evidence: admitted implementation contradiction with independent Verifier `PASS`; no engineer architecture choice is required because accepted owners already require an effect-free pre-listen scan and post-listen ordinary effects. The live A1 state remains untouched and blocked until the corrected product path consumes it. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-027 — Turn-slot authority conflicts with retained shared-Sandbox capacity

- **Observation:** the A1 post-listener recovery attempt reached the real `cleanup-pending` owner, but a fresh-empty NanoHost received the predecessor epoch's `bridge.close`, found no retained bridge or Sandbox, and failed stop with `IdentityMismatch`. The immediate NanoHost bug is local: an all-absent fresh coordinator can prove definite cleanup absence, while live cleanup must validate the exact Sandbox before taking the bridge or deleting anything. The wider recovery path exposes a same-concern authority conflict. The durable scheduler specification binds its one configured slot and Turn lease to definite Sandbox deletion or later fresh-ready proof, while the accepted shared-Sandbox specification retains one compatible Sandbox and Harness across sequential Turns and gives each Turn lease only one active-Turn unit. Production uses the scheduler capacity claim/release path and the Harness active-Turn projection; `claimNanoHostRuntimeTargetSlot` and `settleNanoHostRuntimeTargetCleanup` have no production caller and would create a second, contradictory capacity owner if merely wired into the shared lifecycle.
- **Impact:** `docs/specs/20260703-durable_scheduler_design.md` configured-slot, lease, and cleanup clauses; `docs/specs/20260802-nanohost_runtime_and_transport.md` shared-Sandbox topology, normal AgentSession close, Sandbox drain, accepted-effect reconnect, and Runtime Epoch recovery clauses; `apps/nanocore/src/runtime/nanohost-runtime-target.ts`; `apps/nanocore/src/runtime/nanohost-harness-records.ts`; `apps/nanohost/src/epoch_coordinator.rs`; root `[PRECEDENCE-003]`, `[AUTH-001]`, and `[SCOPE-013]`.
- **Evidence:** Raised by: the primary agent from exact A1 service and durable-state evidence; independently falsified by `/root/nanohost_recovery_blocker` without A1 or credential access.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: preserve the accepted shared-Sandbox architecture. Keep `scheduler_session_leases` and `scheduler_capacity_records` as the unique active-Turn authority, retain `HarnessInstanceRecord.active_turn_count` only as its runtime occupancy projection, and release an ordinary Turn slot after exact terminal, evidence, routing, and local-quiescence proof without deleting the compatible shared Sandbox. Make `SandboxRuntimeRecord` the retained physical-Sandbox lifecycle projection. Delete `nanohost_runtime_targets.active_lease_id`, mutable `capacity_state`, and their unused helpers rather than wiring a second owner. Destructive uncertainty keeps the existing cleanup owner and affected capacity fenced until definite deletion or post-fence fresh-coordinator readiness. The rejected alternative preserves a Turn lease until Sandbox deletion and withdraws shared-Sandbox reuse across AgentSessions and sequential Turns. Recorded transition history: resolved architecture decision. The engineer accepted the shared-Sandbox direction, and the amended Core and owning specifications passed independent Verifier and Auditor review on exact diff hash `586821873522d01ec7b641029d7df986471e7342b72350f33774612258fbf2c9` before commit `a93e023d`. The fresh-empty definite-absence correction remains independently accepted; the A1 durable row remains product-owned and must not be edited or bypassed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved architecture decision. The engineer accepted the shared-Sandbox direction, and the amended Core and owning specifications passed independent Verifier and Auditor review on exact diff hash `586821873522d01ec7b641029d7df986471e7342b72350f33774612258fbf2c9` before commit `a93e023d`. The fresh-empty definite-absence correction remains independently accepted; the A1 durable row remains product-owned and must not be edited or bypassed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-027.
- **Closure evidence:** Recorded disposition evidence: resolved architecture decision. The engineer accepted the shared-Sandbox direction, and the amended Core and owning specifications passed independent Verifier and Auditor review on exact diff hash `586821873522d01ec7b641029d7df986471e7342b72350f33774612258fbf2c9` before commit `a93e023d`. The fresh-empty definite-absence correction remains independently accepted; the A1 durable row remains product-owned and must not be edited or bypassed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-028 — Current product admission does not implement one-current AgentSession continuity

- **Observation:** `startProductTurn` supplies no compatibility candidates, so the scheduler dispatch path creates a fresh AgentSession for every Turn. The durable store permits more than one nonterminal AgentSession for the same Thread and can overwrite an existing AgentSession identity into another Thread; Harness bindings reject duplicate AgentSession ids but not two current AgentSessions for one Thread. Ordinary App API schemas and read models expose AgentSession identifiers, arrays, and stale-session restart actions. A same-Thread conversation can therefore accumulate multiple open AgentSessions even though the accepted user model offers only continue Thread or new Thread.
- **Impact:** `docs/core/agent-session.md`; `docs/specs/20260704-agent_session_continuity.md`; `docs/specs/20260628-web_product_surface_projection.md`; `apps/nanocore/src/lib/store.ts`; `apps/nanocore/src/storage/workspace-file-records.ts`; `apps/nanocore/src/runtime/product-turn-start.ts`; `apps/nanocore/src/runtime/worker-governance-turn-executor.ts`; `apps/nanocore/src/runtime/nanohost-harness-records.ts`; App API schema, client, Web, CLI, and OpenAPI projections.
- **Evidence:** Raised by: the primary agent's CodeGraph trace and independent read-only implementation mapper after the engineer defined AgentSession as a hidden continuity dimension with one current identity per Thread.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: reuse the existing session-workspace compatibility planner for later same-Thread Turns, enforce one nonterminal current AgentSession per Thread in the durable store and Harness binding projection, bind one AgentSession id to one Workspace and Thread for life, retire or fence a predecessor before one internal successor becomes current, keep active-Turn failure truthful without replay, import only terminal stale historical AgentSessions, and remove AgentSession identity and actions from ordinary product surfaces. Add no selector, user action, durable current pointer, compatibility fallback, or second continuity owner. Recorded transition history: locally implemented under Intent Epoch 6 and NHC-IMP-5C; focused Store, import, scheduler, Harness, App API, client, OpenAPI, and terminology checks pass, the full repository verification exits 0, and independent internal implementation reviews pass. The final accumulated-diff audit and A1 acceptance remain outstanding. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: locally implemented under Intent Epoch 6 and NHC-IMP-5C; focused Store, import, scheduler, Harness, App API, client, OpenAPI, and terminology checks pass, the full repository verification exits 0, and independent internal implementation reviews pass. The final accumulated-diff audit and A1 acceptance remain outstanding. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-028.
- **Closure evidence:** Recorded disposition evidence: locally implemented under Intent Epoch 6 and NHC-IMP-5C; focused Store, import, scheduler, Harness, App API, client, OpenAPI, and terminology checks pass, the full repository verification exits 0, and independent internal implementation reviews pass. The final accumulated-diff audit and A1 acceptance remain outstanding. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-029 — Accepted-effect restart recovery cannot yet settle or fence the exact old request

- **Observation:** after NanoCore restart, the current dispatcher has only process-local pending-effect state. Existing durable cleanup and accepted-final-status owners can re-derive deterministic effect request identity, but no result-only expectation reconstructs that identity without redispatch. A successor that has no retained result can therefore poll without an exact old-request owner to mark unknown, and the current NanoHost failure classifier treats physical failure of that poll response as reconnectable even after NanoCore could have committed the unknown transition.
- **Impact:** `docs/specs/20260802-nanohost_runtime_and_transport.md` accepted-effect reconnect, fixed readiness, outer-session failure, and Acceptance Predicates 29, 32, and 33; `docs/specs/20260703-durable_scheduler_design.md`; `apps/nanocore/src/runtime/nanohost-session-dispatch.ts`; `apps/nanocore/src/runtime/scheduler-restart-recovery.ts`; `apps/nanocore/src/runtime/scheduler-lease-maintenance-service.ts`; `apps/nanohost/src/nanocore_session.rs`; `apps/nanohost/src/main.rs`.
- **Evidence:** Raised by: the primary agent's restart-recovery trace and independent Verifier counterexample during the Intent Epoch 6 authority amendment.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: reconstruct only one exact result-only expectation from immutable existing cleanup or accepted-final-status lineage, store no command or replay authority, settle an exact retained same-request result through the existing owner, and make successor poll-first commit unknown once while preserving cleanup and capacity fences. Reuse NanoHost's existing reconnect state and epoch supervisor so cancellation, reset, timeout, or physical close after a no-retained-result successor starts its first poll but before the complete response is terminal and cannot reconnect the same coordinator. Recorded transition history: locally implemented under NHC-IMP-5C. Focused dispatcher, restart-recovery, maintenance, and Rust checks pass, the Rust first-poll classifier received independent Verifier `PASS`, and the full repository verification exits 0. The final accumulated-diff audit and A1 recovery evidence remain outstanding. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: locally implemented under NHC-IMP-5C. Focused dispatcher, restart-recovery, maintenance, and Rust checks pass, the Rust first-poll classifier received independent Verifier `PASS`, and the full repository verification exits 0. The final accumulated-diff audit and A1 recovery evidence remain outstanding. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-029.
- **Closure evidence:** Recorded disposition evidence: locally implemented under NHC-IMP-5C. Focused dispatcher, restart-recovery, maintenance, and Rust checks pass, the Rust first-poll classifier received independent Verifier `PASS`, and the full repository verification exits 0. The final accumulated-diff audit and A1 recovery evidence remain outstanding. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-030 — Compatibility selection was a post-lease self-proof and generic recovery had no owner

- **Observation:** the first product continuity path supplied the stored predecessor SessionCompatibilityKey as both the desired key and the only candidate key, making its reuse comparison tautological. A later Turn with changed static AEP input therefore acquired a fresh scheduler lease before the executor derived the new key and returned `recovery_required`; the predecessor remained current and no successor existed. The adjacent generic continuity module retained candidate arrays, snapshot restore, resume-handle precedence, recovery options, and a durable `session_snapshots` table even though accepted AgentSession continuity explicitly excludes those mechanisms and no backend consumed them.
- **Impact:** `docs/core/agent-session.md`; `docs/specs/20260704-agent_session_continuity.md`; `apps/nanocore/src/runtime/agent-environment.ts`; `apps/nanocore/src/runtime/scheduler-dispatch-loop.ts`; `apps/nanocore/src/runtime/worker-governance-turn-executor.ts`; the retired `apps/nanocore/src/agent-session-continuity.ts`; and the retired `session_snapshots` schema.
- **Evidence:** Raised by: the primary agent's CodeGraph trace and a real scheduler-backed simulator regression that changed only the later Turn's immutable runtime image reference.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: reuse the existing session-workspace planner in a metadata-only AEP projection before lease acquisition; perform no Vault backend, plan, receipt, use, or secret-sink effect during that preview; require the runtime owner to prove readiness and exact durable hygiene, preview a successor before any replacement close, and revalidate the same key before launch. Delete the unused generic selector, snapshot schema, restore matrix, and recovery options through the strict current migration instead of preserving a compatibility path or creating another continuity owner. Recorded transition history: resolved in frozen local bytes; focused and full repository verification pass, and independent internal implementation review passes. The final accumulated-diff audit and A1 acceptance remain outstanding. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved in frozen local bytes; focused and full repository verification pass, and independent internal implementation review passes. The final accumulated-diff audit and A1 acceptance remain outstanding. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-030.
- **Closure evidence:** Recorded disposition evidence: resolved in frozen local bytes; focused and full repository verification pass, and independent internal implementation review passes. The final accumulated-diff audit and A1 acceptance remain outstanding. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-031 — Replacement preparation destroyed the predecessor before dispatch authority existed

- **Observation:** the first replacement implementation called the effectful AgentSession preparation seam before scheduler pool, capacity, Thread-exclusion, and dispatch-transaction admission. An incompatible current predecessor was therefore closed and fenced even when no compatible pool existed, capacity was saturated, the Thread was busy, or the dispatch transaction failed, leaving no authorized successor or Turn execution.
- **Impact:** `docs/core/agent-session.md`; `docs/specs/20260704-agent_session_continuity.md`; `docs/specs/20260703-durable_scheduler_design.md`; `apps/nanocore/src/runtime/scheduler-dispatch-loop.ts`; `apps/nanocore/src/runtime/worker-governance-turn-executor.ts`; and root `[SCOPE-007]`, `[TEST-009]`, and `[QUALITY-016]`.
- **Evidence:** Raised by: the independent implementation Verifier and a lowest-sufficient table regression covering no pool, saturated capacity, Thread exclusion, and an actual SQLite dispatch failure.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: keep `prepareAgentSessionForTurn` read-only, dispatch and acquire the exact scheduler lease first, then call the paired commit seam with the immutable preview and exact lease lineage. The commit re-reads the sole current AgentSession, revalidates runtime hygiene while excluding only that exact new lease, and closes or fences a predecessor only for an admitted replacement; failure uses the existing lease failure and release path without creating a successor or starting a Turn. Recorded transition history: resolved in frozen local bytes; the four-case pre-admission regression, focused scheduler and backend checks, and full repository verification pass, and independent internal implementation review passes. The final accumulated-diff audit and A1 acceptance remain outstanding. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved in frozen local bytes; the four-case pre-admission regression, focused scheduler and backend checks, and full repository verification pass, and independent internal implementation review passes. The final accumulated-diff audit and A1 acceptance remain outstanding. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-031.
- **Closure evidence:** Recorded disposition evidence: resolved in frozen local bytes; the four-case pre-admission regression, focused scheduler and backend checks, and full repository verification pass, and independent internal implementation review passes. The final accumulated-diff audit and A1 acceptance remain outstanding. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-032 — Ordinary Turn projections still exposed hidden AgentSession identity

- **Observation:** removing top-level dashboard and runtime-configuration fields did not close the ordinary product boundary because the shared Turn schema still exposed `agentSessionId`. Thread dashboards, Chat and Task responses, Turn start, read, replay, gate, interrupt, server-sent events, and both Core Client event transports consequently projected the hidden continuity identity even though their top-level AgentSession summaries were gone.
- **Impact:** `docs/core/agent-session.md`; `docs/specs/20260628-web_product_surface_projection.md`; `packages/protocol/src/models/turn.ts`; `packages/app-api-schemas`; `packages/core-client`; NanoCore Turn routes, event routes, dashboards, and OpenAPI; and root `[OM-009]`, `[TEST-009]`, and `[CODEDOC-001]`.
- **Evidence:** Raised by: the independent implementation Verifier; the first Grok implementation was then independently rejected because its product projection dropped the existing cross-item Turn relationship validation.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: retain the complete internal Turn schema and protected evidence lineage, add one ordinary product Turn projection that omits `agentSessionId`, and use it across every ordinary App API and Core Client response or event path. Share the existing cross-item relationship refiner between the internal and product schemas, and use one product event envelope whose outer and nested payloads both exclude AgentSession identity, so redaction cannot weaken Turn or event validation; expose no AgentSession picker, restart, navigation, or nested identity action. Recorded transition history: resolved in corrected local bytes after independent internal reviewer `PASS` on the HTTP projection and a separate typed server-sent-event boundary correction; Protocol, App API, Core Client, NanoCore product-surface, and full repository verification pass. The final accumulated-diff audit and A1 acceptance remain outstanding. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved in corrected local bytes after independent internal reviewer `PASS` on the HTTP projection and a separate typed server-sent-event boundary correction; Protocol, App API, Core Client, NanoCore product-surface, and full repository verification pass. The final accumulated-diff audit and A1 acceptance remain outstanding. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-032.
- **Closure evidence:** Recorded disposition evidence: resolved in corrected local bytes after independent internal reviewer `PASS` on the HTTP projection and a separate typed server-sent-event boundary correction; Protocol, App API, Core Client, NanoCore product-surface, and full repository verification pass. The final accumulated-diff audit and A1 acceptance remain outstanding. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-033 — Result-only recovery inspected only the operation selected by the new cursor

- **Observation:** the result-only dispatcher keyed pending recovery by the old effect operation and checked only the operation named by the successor's current poll. A recovered `sandbox.delete` expectation therefore survived the successor's real first `sandbox.create` poll, which returned empty, and could be reached only on a later cursor round after NanoHost's first-poll terminal window had ended.
- **Impact:** `docs/specs/20260802-nanohost_runtime_and_transport.md`; `docs/specs/20260703-durable_scheduler_design.md`; `apps/nanocore/src/runtime/nanohost-session-dispatch.ts`; `apps/nanohost/src/main.rs`; `apps/nanohost/src/nanocore_session.rs`; and root `[SCOPE-007]`, `[TEST-009]`, and `[TEST-012]`.
- **Evidence:** Raised by: the independent implementation Verifier and a production-order regression that polls the real first outer effect before the old operation.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: after readiness and before any operation-local lookup or empty response, treat the first successor poll of any operation as proof that no retained result for the one pending result-only group was delivered. Remove that group, commit its existing unknown and cleanup fence once, reject the waiting owner, and close the successor connection; an exact retained result received before a poll still settles the same request without replay. Recorded transition history: resolved in frozen local bytes; focused production-order recovery checks, the independently accepted Rust classifier, and full repository verification pass. The final accumulated-diff audit and A1 recovery evidence remain outstanding. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved in frozen local bytes; focused production-order recovery checks, the independently accepted Rust classifier, and full repository verification pass. The final accumulated-diff audit and A1 recovery evidence remain outstanding. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-033.
- **Closure evidence:** Recorded disposition evidence: resolved in frozen local bytes; focused production-order recovery checks, the independently accepted Rust classifier, and full repository verification pass. The final accumulated-diff audit and A1 recovery evidence remain outstanding. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-034 — Host lifecycle fixture assumptions were not portable to the repository test image

- **Observation:** the unchanged host lifecycle tests passed 117/117 under the Mise-managed host but failed 17 cases in the repository test image. The local SSH stub returned from a streamed assertion without consuming stdin, so the faster image made the producer fail with `EPIPE`; a separate xtrace oracle assumed that non-interactive Bash would inherit an environment-supplied `PS4`, even though the image emitted the exact fact-collection command with the default double-plus trace.
- **Impact:** `tests/host-manifest-runtime.test.mjs`; `tests/host-manifest.test.mjs`; `tests/support/host/assert.sh`; `scripts/test-env.sh`; and root `[TEST-012]` and `[CHECK-019]`.
- **Evidence:** Raised by: the primary agent's full repository verification and an independent read-only fixture review. The affected tests and product scripts had no diff from `a93e023d`, so this was a baseline test-harness portability defect rather than an AgentSession or NanoHost product regression.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: make the local SSH stub consume the complete streamed payload before returning its existing event, status, and digest, and assert the exact default double-plus `/usr/bin/node -e` fact-collection trace without changing the A1 `/usr/bin/node` contract, product scripts, Dockerfile, manifest, or package runner. Recorded transition history: resolved by a test-only Grok correction and accepted by an independent internal reviewer. The two focused files pass 117/117 in both the repository test image and the Mise-managed host, and the subsequent full repository verification exits 0. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved by a test-only Grok correction and accepted by an independent internal reviewer. The two focused files pass 117/117 in both the repository test image and the Mise-managed host, and the subsequent full repository verification exits 0. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-034.
- **Closure evidence:** Recorded disposition evidence: resolved by a test-only Grok correction and accepted by an independent internal reviewer. The two focused files pass 117/117 in both the repository test image and the Mise-managed host, and the subsequent full repository verification exits 0. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-035 — The first final implementation audit found four cross-owner integration mismatches

- **Observation:** the first Claude Code accumulated-diff audit found four exact counterexamples after the locally reviewed slices were composed. A future Turn without a Workspace checkpoint previewed a synthetic Context Package contribution that the final AEP omitted; the A1 strict no-op helper still required the deleted `staleSessions` field; the scheduler previewed the first queued row while dispatch correctly skipped its busy Thread and selected a later row; and the canonical terminology source-text gate lived under `tests/` without one of the finite exemptions owned by the local test guide. The same audit also identified a dead restart-recovery callback and an unused Core Client or Agent Skill RuntimeTarget-status projection that had no current caller.
- **Impact:** `apps/nanocore/src/runtime/agent-environment.ts`; `apps/nanocore/src/runtime/worker-governance-turn-executor.ts`; `apps/nanocore/src/runtime/scheduler-dispatch-loop.ts`; `apps/nanocore/src/scheduler-records.ts`; `apps/nanocore/e2e/_lib/real-codex-support.mjs`; `apps/nanocore/src/runtime/scheduler-restart-recovery.ts`; `package.json`; `scripts/validate-agent-session-terminology.mjs`; `packages/core-client`; `skills/openkit-operations.mjs`; `tests/AGENTS.md`; and root `[OM-009]`, `[TEST-009]`, `[TEST-012]`, and `[QUALITY-016]`.
- **Evidence:** Raised by: the single final Claude Code audit requested through Herdr, followed by lowest-sufficient Grok test-author regressions and independent internal review of those tests and corrections.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: keep Context Package compatibility tied to the exact checkpoint-backed launch fact, make preparation and dispatch consume one next-dispatchable identity with an exact pre-transaction recheck, update the strict A1 no-op helper only for the removed field, place the terminology gate under the existing script owner, and delete the two zero-consumer projections. Add no compatibility flag, alternate scheduler queue, callback replacement, new status operation, or test-exemption entry. Recorded transition history: resolved and committed locally as implementation checkpoint `959cdc9d`. The compatibility owner reads the exact Workspace checkpoint fact shared by preview, commit, and launch checks; the A1 helper accepts the current RuntimeConfigStatus shape while retaining every other strict no-op predicate; one existing scheduler owner selects and rechecks the exact next dispatchable row; and the terminology validator runs from `scripts/` through `check:repo`. The dead callback and unused RuntimeTarget-status projections were deleted rather than preserved. Focused regressions, independent internal reviews, and `pnpm verify` including repository checks, lint, typecheck, unit tests, coverage, and builds passed. The same Claude Code session's bounded re-audit confirmed all four corrections and the final App API guide correction before commit; A1 acceptance remains outstanding. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved and committed locally as implementation checkpoint `959cdc9d`. The compatibility owner reads the exact Workspace checkpoint fact shared by preview, commit, and launch checks; the A1 helper accepts the current RuntimeConfigStatus shape while retaining every other strict no-op predicate; one existing scheduler owner selects and rechecks the exact next dispatchable row; and the terminology validator runs from `scripts/` through `check:repo`. The dead callback and unused RuntimeTarget-status projections were deleted rather than preserved. Focused regressions, independent internal reviews, and `pnpm verify` including repository checks, lint, typecheck, unit tests, coverage, and builds passed. The same Claude Code session's bounded re-audit confirmed all four corrections and the final App API guide correction before commit; A1 acceptance remains outstanding. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-035.
- **Closure evidence:** Recorded disposition evidence: resolved and committed locally as implementation checkpoint `959cdc9d`. The compatibility owner reads the exact Workspace checkpoint fact shared by preview, commit, and launch checks; the A1 helper accepts the current RuntimeConfigStatus shape while retaining every other strict no-op predicate; one existing scheduler owner selects and rechecks the exact next dispatchable row; and the terminology validator runs from `scripts/` through `check:repo`. The dead callback and unused RuntimeTarget-status projections were deleted rather than preserved. Focused regressions, independent internal reviews, and `pnpm verify` including repository checks, lint, typecheck, unit tests, coverage, and builds passed. The same Claude Code session's bounded re-audit confirmed all four corrections and the final App API guide correction before commit; A1 acceptance remains outstanding. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-036 — An A1 systemd patch update invalidated the strict host manifest

- **Observation:** after implementation checkpoint `959cdc9d`, `pnpm host:provision a1` passed and `pnpm host:assert a1` failed on exactly one host fact: the manifest required `/usr/bin/systemctl` version `systemd 255 (255.4-1ubuntu8.16)`, while A1 reported `systemd 255 (255.4-1ubuntu8.17)`. Every other manifest fact matched.
- **Impact:** `tests/support/host/manifest.json`; `tests/host-manifest.test.mjs`; the exact host assertion boundary; and root `[EVID-001]`, `[TEST-012]`, and `[CHECK-019]`.
- **Evidence:** Raised by: the primary agent's real A1 assertion and read-only package-state inspection. `apt-cache policy systemd` reported `.17` as both installed and candidate, and `/var/log/dpkg.log` recorded the automatic `.16` to `.17` upgrade on 2026-08-20; `.16` was no longer offered by the configured package sources.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: preserve strict equality and update the current exact fact to `.17`; do not downgrade A1, accept a version range, edit product state, or treat this host-package drift as NanoHost acceptance evidence. Recorded transition history: corrected in local bytes by changing only the exact manifest fact and its exact expected test projection from `.16` to `.17`. The repository image and Mise-managed host projections pass 119/119, and an independent internal review accepted the Grok-produced two-file correction without finding any weakened oracle, range, compatibility branch, or unrelated change. The corrected real `pnpm host:assert a1` invocation passes with manifest digest `cb8092dbe85b7e47caeab28ca10f966cdf633b591dc1d643d8762d13dd6f39db`; the bounded checkpoint commit remains next. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected in local bytes by changing only the exact manifest fact and its exact expected test projection from `.16` to `.17`. The repository image and Mise-managed host projections pass 119/119, and an independent internal review accepted the Grok-produced two-file correction without finding any weakened oracle, range, compatibility branch, or unrelated change. The corrected real `pnpm host:assert a1` invocation passes with manifest digest `cb8092dbe85b7e47caeab28ca10f966cdf633b591dc1d643d8762d13dd6f39db`; the bounded checkpoint commit remains next. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-036.
- **Closure evidence:** Recorded disposition evidence: corrected in local bytes by changing only the exact manifest fact and its exact expected test projection from `.16` to `.17`. The repository image and Mise-managed host projections pass 119/119, and an independent internal review accepted the Grok-produced two-file correction without finding any weakened oracle, range, compatibility branch, or unrelated change. The corrected real `pnpm host:assert a1` invocation passes with manifest digest `cb8092dbe85b7e47caeab28ca10f966cdf633b591dc1d643d8762d13dd6f39db`; the bounded checkpoint commit remains next. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-037 — The official host harness did not select h2c for the accepted loopback HTTP endpoint

- **Observation:** the exact deployed NanoCore health endpoint returned `200` under HTTP/2 prior knowledge, but the official NanoHost bring-up used ordinary curl against the accepted `http://127.0.0.1` rendezvous and failed with the HTTP/0.9 rejection before authenticated readiness. The host contract already permits only exact loopback HTTP or HTTPS; no new transport exception is required.
- **Impact:** `tests/support/host/nanohost-bring-up.sh`; `tests/support/host/teardown.sh`; `tests/host-manifest-runtime.test.mjs`; the loopback plaintext exception in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-009]`, `[TEST-012]`, and `[CHECK-019]`.
- **Evidence:** Raised by: the primary agent's exact A1 deployment and official bring-up; corrected by the Grok test-author/builder and independently reviewed by an internal reviewer.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: reuse the existing validated endpoint branch, introduce no new option or helper, and prove both loopback calls carry h2c while both real HTTPS call lines exist without it. Recorded transition history: locally implemented, independently reviewed, and staged for the NHC-IMP-5D checkpoint commit. Validated loopback HTTP adds only `--http2-prior-knowledge`, HTTPS adds no argument, raw Token material remains on curl config stdin, and existing timeout, failure, cleanup, and decommission behavior is unchanged. The focused image and Mise-host projections pass 60/60; the reviewer rejected one weak HTTPS negative oracle, the corrected existence-sensitive oracle then received `PASS`; the final same-byte `pnpm verify` run exits 0. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: locally implemented, independently reviewed, and staged for the NHC-IMP-5D checkpoint commit. Validated loopback HTTP adds only `--http2-prior-knowledge`, HTTPS adds no argument, raw Token material remains on curl config stdin, and existing timeout, failure, cleanup, and decommission behavior is unchanged. The focused image and Mise-host projections pass 60/60; the reviewer rejected one weak HTTPS negative oracle, the corrected existence-sensitive oracle then received `PASS`; the final same-byte `pnpm verify` run exits 0. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-037.
- **Closure evidence:** Recorded disposition evidence: locally implemented, independently reviewed, and staged for the NHC-IMP-5D checkpoint commit. Validated loopback HTTP adds only `--http2-prior-knowledge`, HTTPS adds no argument, raw Token material remains on curl config stdin, and existing timeout, failure, cleanup, and decommission behavior is unchanged. The focused image and Mise-host projections pass 60/60; the reviewer rejected one weak HTTPS negative oracle, the corrected existence-sensitive oracle then received `PASS`; the final same-byte `pnpm verify` run exits 0. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-038 — Decommission retained the configured identity but enrollment could only insert it

- **Observation:** after the first official exact-byte bring-up failed stop and official teardown decommissioned the attempt, the authenticated App API could not create the explicit replacement credential required for another attempt. `enrollNanoHostTransportIdentity` always inserted a new identity, while `decommissionNanoHostIntegrationIdentity` correctly retained the stable row as `decommissioned`; the deployment unique constraint therefore returned `UNIQUE constraint failed: nanohost_integration_identities.deployment_id` and the route cleared the attempted slot.
- **Impact:** `docs/specs/20260802-nanohost_runtime_and_transport.md`; `apps/nanocore/src/auth/nanohost-transport-token-store.ts`; `apps/nanocore/src/auth/nanohost-transport-sink.ts`; `apps/nanocore/src/auth/nanohost-transport-routes.ts`; their focused tests and local guide; and root `[AUTH-001]`, `[DOC-017]`, `[TEST-009]`, and `[SCOPE-007]`.
- **Evidence:** Raised by: the primary agent's authenticated A1 App API enrollment after official teardown. The initial spec amendment was independently blocked by a Verifier counterexample in which a concurrent losing attempt could clear a winner's overwritten slot; the corrected exact-slot lifecycle then passed both Verifier and Auditor review.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: update the existing sink and enrollment transaction owners only; add no migration, identity family, durable record, coordinator, timer, automatic retry, row replacement, old-Token reactivation, or restoration of prior runtime state. Recorded transition history: implemented locally and independently reviewed for the NHC-IMP-5D checkpoint commit. The existing sink now requires explicit enrollment exclusive-create or issue/rotation replace disposition, never removes pre-existing same-Token or foreign slot bytes, and conditionally clears only exact attempted-Token-owned files after a database failure. The existing transaction creates only a lineage-free first identity or reactivates the one exact retained decommissioned identity, preserves `created_at`, clears `decommissioned_at`, issues one fresh Token, and rejects identity-only or deployment-only Token history without its identity row. Internal review first blocked pre-existing-slot cleanup and replace-call regressions, then missing identity-row Token history and a weak rotation oracle, then an `OR`-to-`AND`-insensitive history regression; each correction stayed within the existing owners, the final reviewer returned `PASS`, focused auth tests pass 22/22, NanoCore typecheck and targeted Biome pass, and the final same-byte `pnpm verify` run exits 0. Exact A1 redeployment and authenticated re-enrollment remain pending. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: implemented locally and independently reviewed for the NHC-IMP-5D checkpoint commit. The existing sink now requires explicit enrollment exclusive-create or issue/rotation replace disposition, never removes pre-existing same-Token or foreign slot bytes, and conditionally clears only exact attempted-Token-owned files after a database failure. The existing transaction creates only a lineage-free first identity or reactivates the one exact retained decommissioned identity, preserves `created_at`, clears `decommissioned_at`, issues one fresh Token, and rejects identity-only or deployment-only Token history without its identity row. Internal review first blocked pre-existing-slot cleanup and replace-call regressions, then missing identity-row Token history and a weak rotation oracle, then an `OR`-to-`AND`-insensitive history regression; each correction stayed within the existing owners, the final reviewer returned `PASS`, focused auth tests pass 22/22, NanoCore typecheck and targeted Biome pass, and the final same-byte `pnpm verify` run exits 0. Exact A1 redeployment and authenticated re-enrollment remain pending. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-038.
- **Closure evidence:** Recorded disposition evidence: implemented locally and independently reviewed for the NHC-IMP-5D checkpoint commit. The existing sink now requires explicit enrollment exclusive-create or issue/rotation replace disposition, never removes pre-existing same-Token or foreign slot bytes, and conditionally clears only exact attempted-Token-owned files after a database failure. The existing transaction creates only a lineage-free first identity or reactivates the one exact retained decommissioned identity, preserves `created_at`, clears `decommissioned_at`, issues one fresh Token, and rejects identity-only or deployment-only Token history without its identity row. Internal review first blocked pre-existing-slot cleanup and replace-call regressions, then missing identity-row Token history and a weak rotation oracle, then an `OR`-to-`AND`-insensitive history regression; each correction stayed within the existing owners, the final reviewer returned `PASS`, focused auth tests pass 22/22, NanoCore typecheck and targeted Biome pass, and the final same-byte `pnpm verify` run exits 0. Exact A1 redeployment and authenticated re-enrollment remain pending. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-039 — Fresh coordinator readiness reconstructed the same unknown cleanup expectation

- **Observation:** the exact `8e45086a` A1 deployment completed h2c health, authenticated re-enrollment, stock OpenShell startup, and current-generation readiness. Its first real effect poll then received the accepted terminal `409`, so NanoHost fail-stopped and NanoCore logged `NanoHost accepted effect outcome is unknown; successor connection fenced.` The following fresh coordinator again completed Gateway health and authenticated readiness but received the same terminal `409`; NanoCore logged the same maintenance failure a second time. Local regression evidence proved that a complete durable unknown Sandbox fence was incorrectly rebuilt as the same result-only expectation instead of consuming later fresh-ready proof; the later exact `008dab11` A1 deployment exposed the preceding lineage gap recorded by `NHC-FND-040` before this branch could run against the retained A1 row.
- **Impact:** `docs/specs/20260703-durable_scheduler_design.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `apps/nanocore/src/runtime/turn-executor-factory.ts`; `apps/nanocore/src/runtime/nanohost-harness-records.ts`; `apps/nanocore/src/runtime/scheduler-restart-recovery.ts`; their focused tests; and root `[SCOPE-007]`, `[TEST-009]`, and `[QUALITY-016]`.
- **Evidence:** Raised by: the primary agent's repeated official A1 bring-up using only the authenticated App API and retained real Core state. Read-only repository and upstream-source investigation independently proved the simultaneous Moby image-index warnings non-fatal: both platform-child images imported and inspected, the Supervisor extracted, and the stock Gateway returned health `200` before the cleanup loop failed.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: reuse the durable unknown Sandbox fence plus the existing RuntimeTarget readiness projection. With no process-local session, remove the exact Sandbox/Harness projection without another effect only when its full unknown-fence shape is present and the matching target has a strictly later observation with predecessor fenced, ready, and fresh-empty all proved. Missing, equal, stale, mismatched, non-ready, non-fresh, or partial proof stays cleanup-owned and fenced without recreating or replaying the old accepted request. Add no new state, migration, retry stage, effect, request identity, coordinator, timer, route, connection, or capacity owner. Recorded transition history: implemented, independently reviewed, committed as `008dab11`, and accepted on A1 together with NHC-IMP-5F commit `69adbf82`. The first exact `69adbf82` official attempt produced one terminal `sandbox.create` poll `409` and projected the retained Sandbox as `failed/unknown/draining/unknown`, its Harness as `failed/draining`, and the backend as `cleanup-failed`. A later App-API-authorized generation reached predecessor-fenced, ready, fresh-empty readiness with a strictly later observation; ordinary maintenance then removed the exact Sandbox and Harness projections and advanced the backend to `physical-cleaned` with physical-cleanup evidence. No database or fixture state was edited, no effect was replayed, and the repository teardown left the NanoHost service inactive. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: implemented, independently reviewed, committed as `008dab11`, and accepted on A1 together with NHC-IMP-5F commit `69adbf82`. The first exact `69adbf82` official attempt produced one terminal `sandbox.create` poll `409` and projected the retained Sandbox as `failed/unknown/draining/unknown`, its Harness as `failed/draining`, and the backend as `cleanup-failed`. A later App-API-authorized generation reached predecessor-fenced, ready, fresh-empty readiness with a strictly later observation; ordinary maintenance then removed the exact Sandbox and Harness projections and advanced the backend to `physical-cleaned` with physical-cleanup evidence. No database or fixture state was edited, no effect was replayed, and the repository teardown left the NanoHost service inactive. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-039.
- **Closure evidence:** Recorded disposition evidence: implemented, independently reviewed, committed as `008dab11`, and accepted on A1 together with NHC-IMP-5F commit `69adbf82`. The first exact `69adbf82` official attempt produced one terminal `sandbox.create` poll `409` and projected the retained Sandbox as `failed/unknown/draining/unknown`, its Harness as `failed/draining`, and the backend as `cleanup-failed`. A later App-API-authorized generation reached predecessor-fenced, ready, fresh-empty readiness with a strictly later observation; ordinary maintenance then removed the exact Sandbox and Harness projections and advanced the backend to `physical-cleaned` with physical-cleanup evidence. No database or fixture state was edited, no effect was replayed, and the repository teardown left the NanoHost service inactive. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-040 — Cleanup lookup compared backend identity with the wrong binding field

- **Observation:** the exact `008dab11` A1 deployment passed h2c health and authenticated re-enrollment, then the official bring-up again fail-stopped on the first `sandbox.create` poll with terminal `409`. Read-only durable inspection after official teardown showed one `cleanup-pending` backend session, one open and clean Sandbox with an idle zero-session Harness, no AgentSession runtime binding, and exact RuntimeTarget and deployment identity. The backend session's retained lease binding differs from the Sandbox binding, while its immutable `backendSessionId` is the existing deterministic `nh-` projection of the Sandbox compatibility key. `findDurableSandboxBinding` compared that backend identity directly with `sandbox_binding_ref`, so it returned no owner; the poll-first rejection therefore had no exact Sandbox reference to fence, left the Sandbox clean, and allowed every restart to rebuild the same result-only expectation.
- **Impact:** `docs/specs/20260703-durable_scheduler_design.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `apps/nanocore/src/runtime/turn-executor-factory.ts`; its existing sequential cleanup regression; and root `[EVID-001]`, `[TEST-009]`, `[QUALITY-001]`, and `[SCOPE-007]`.
- **Evidence:** Raised by: the primary agent's exact `008dab11` A1 deployment and official authenticated bring-up, followed only by authenticated App API projection reads and read-only Core SQLite inspection inside the running NanoCore container. No database, slot, fixture, RuntimeTarget, Sandbox, Harness, lease, or backend-session state was edited.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: reuse the existing `planSession` identity rule as immutable backend lineage. A Sandbox is eligible only when its stored compatibility key projects exactly to the supplied backend session id, or an existing exact binding or AgentSession lineage already selects it; more than one match, a malformed or non-projecting backend id, target or deployment disagreement, or any missing proof fails closed. The correction changes only the shared durable lookup predicate so the existing poll-first unknown fence and NHC-IMP-5E fresh-ready settlement can run. Recorded transition history: implemented, independently reviewed, committed as `69adbf82`, and accepted on A1. The exact committed bytes pass `pnpm verify`; the focused factory file passes 18/18 and the combined cleanup/restart/maintenance set passes 74/74. The exact `linux/arm64` image loaded on A1 as config image `sha256:22d4c1ad6b94e079fd1ecd21e12ba9b6b6026ba7ae8a3ec535f242c9a6e4d2ed`, passed h2c health and authenticated App API enrollment, and its first official bring-up converted the retained clean Sandbox into the complete unknown fence instead of losing its owner. The next fresh-ready generation allowed NHC-IMP-5E to delete only that Sandbox and Harness and advance the backend to `physical-cleaned`; the repository teardown then stopped NanoHost and decommissioned the test identity. No database, fixture, RuntimeTarget, Sandbox, Harness, lease, backend-session, or credential state was manually edited. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: implemented, independently reviewed, committed as `69adbf82`, and accepted on A1. The exact committed bytes pass `pnpm verify`; the focused factory file passes 18/18 and the combined cleanup/restart/maintenance set passes 74/74. The exact `linux/arm64` image loaded on A1 as config image `sha256:22d4c1ad6b94e079fd1ecd21e12ba9b6b6026ba7ae8a3ec535f242c9a6e4d2ed`, passed h2c health and authenticated App API enrollment, and its first official bring-up converted the retained clean Sandbox into the complete unknown fence instead of losing its owner. The next fresh-ready generation allowed NHC-IMP-5E to delete only that Sandbox and Harness and advance the backend to `physical-cleaned`; the repository teardown then stopped NanoHost and decommissioned the test identity. No database, fixture, RuntimeTarget, Sandbox, Harness, lease, backend-session, or credential state was manually edited. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-040.
- **Closure evidence:** Recorded disposition evidence: implemented, independently reviewed, committed as `69adbf82`, and accepted on A1. The exact committed bytes pass `pnpm verify`; the focused factory file passes 18/18 and the combined cleanup/restart/maintenance set passes 74/74. The exact `linux/arm64` image loaded on A1 as config image `sha256:22d4c1ad6b94e079fd1ecd21e12ba9b6b6026ba7ae8a3ec535f242c9a6e4d2ed`, passed h2c health and authenticated App API enrollment, and its first official bring-up converted the retained clean Sandbox into the complete unknown fence instead of losing its owner. The next fresh-ready generation allowed NHC-IMP-5E to delete only that Sandbox and Harness and advance the backend to `physical-cleaned`; the repository teardown then stopped NanoHost and decommissioned the test identity. No database, fixture, RuntimeTarget, Sandbox, Harness, lease, backend-session, or credential state was manually edited. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-041 — The retained real-Task runner targets the retired transport and backend evidence shape

- **Observation:** after NHC-IMP-5F left A1 empty and fresh-ready, the repository had no executable current NanoHost runner for the next real Codex Turn. The closest real Task Mode runner rejected a target-only repository path by inspecting it on the developer host, used Undici against NanoCore's plaintext HTTP/2-only endpoint and failed before any request with an HTTP/1 parser error, and required OpenShell `0.0.80`. The current NanoHost backend is the sole selectable runtime and returns no capability version, so its otherwise valid transcript RuntimeEvidence records `backendVersion=null`; the accepted stock boundary is `0.0.99`. The historical restart runner still supplies retired Cell and remote-placement selectors rejected by the current factory, and the Unit E/F files named by historical plan text do not exist in the current checkout.
- **Impact:** `docs/specs/20260529-test_strategy.md`; `docs/verification-instruments.md`; `docs/specs/20260711-worker_runtime_subagent_provenance.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `apps/nanocore/e2e/task-mode-real-worker-runner.mjs`; its focused test; `apps/nanocore/src/runtime/turn-executor-factory.ts`; and root `[EVID-001]`, `[TEST-009]`, `[TEST-012]`, and `[QUALITY-003]`.
- **Evidence:** Raised by: the primary agent's read-only CodeGraph trace, one no-quota transport probe through the operator-local SSH forward, and two independent internal read-only implementation and execution maps. The probe failed at protocol parsing before authentication or provider use; no product state, credential, A1 runtime, or repository file was changed.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: reuse the existing real Task runner, its Core Client injection point, and Node standard HTTP/2 support; require target-side repository validation before Task start; project the already fixed stock `0.0.99` version from the NanoHost capability owner; and retain exact product-commit and host-manifest identities in the existing redacted evidence bundle. Add no proxy, HTTP/1 listener, dependency, product API, protocol field, durable record, compatibility branch, alternate runtime selector, or reconstructed historical Unit E/F artifact. Recorded transition history: the bounded NHC-IMP-5G implementation is complete, independently reviewed, and committed as `855ffa71`; the full repository gate exits 0. Its exact `linux/arm64` image passed the repository smoke, loaded on A1 with equal local and remote identity, and retained the predecessor container and persistent data during replacement. The real Turn did not start because both the candidate and restarted predecessor failed the same pre-existing Workspace handoff cleanup invariant recorded as NHC-FND-042. The runner now reaches plaintext HTTP/2 through Node's standard client while preserving the HTTPS Undici path, requires target-side repository validation before Task start, fail-closes exact product-commit and host-manifest identities before any client or provider use, retains them in redacted evidence, and requires truthful OpenShell `0.0.99` provenance. The NanoHost backend projects that fixed version through its existing capability owner. Focused evidence is runner `54/54`, factory `20/20`, NanoCore typecheck, targeted Biome, and `git diff --check`; the full gate covers repository checks, lint, typecheck, unit tests, coverage, and 11/11 builds; independent internal review returned `PASS` after response-less close, bodyless response, and abort boundaries received direct regressions and local correction. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: the bounded NHC-IMP-5G implementation is complete, independently reviewed, and committed as `855ffa71`; the full repository gate exits 0. Its exact `linux/arm64` image passed the repository smoke, loaded on A1 with equal local and remote identity, and retained the predecessor container and persistent data during replacement. The real Turn did not start because both the candidate and restarted predecessor failed the same pre-existing Workspace handoff cleanup invariant recorded as NHC-FND-042. The runner now reaches plaintext HTTP/2 through Node's standard client while preserving the HTTPS Undici path, requires target-side repository validation before Task start, fail-closes exact product-commit and host-manifest identities before any client or provider use, retains them in redacted evidence, and requires truthful OpenShell `0.0.99` provenance. The NanoHost backend projects that fixed version through its existing capability owner. Focused evidence is runner `54/54`, factory `20/20`, NanoCore typecheck, targeted Biome, and `git diff --check`; the full gate covers repository checks, lint, typecheck, unit tests, coverage, and 11/11 builds; independent internal review returned `PASS` after response-less close, bodyless response, and abort boundaries received direct regressions and local correction. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-041.
- **Closure evidence:** Recorded disposition evidence: the bounded NHC-IMP-5G implementation is complete, independently reviewed, and committed as `855ffa71`; the full repository gate exits 0. Its exact `linux/arm64` image passed the repository smoke, loaded on A1 with equal local and remote identity, and retained the predecessor container and persistent data during replacement. The real Turn did not start because both the candidate and restarted predecessor failed the same pre-existing Workspace handoff cleanup invariant recorded as NHC-FND-042. The runner now reaches plaintext HTTP/2 through Node's standard client while preserving the HTTPS Undici path, requires target-side repository validation before Task start, fail-closes exact product-commit and host-manifest identities before any client or provider use, retains them in redacted evidence, and requires truthful OpenShell `0.0.99` provenance. The NanoHost backend projects that fixed version through its existing capability owner. Focused evidence is runner `54/54`, factory `20/20`, NanoCore typecheck, targeted Biome, and `git diff --check`; the full gate covers repository checks, lint, typecheck, unit tests, coverage, and 11/11 builds; independent internal review returned `PASS` after response-less close, bodyless response, and abort boundaries received direct regressions and local correction. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-042 — Cleanup re-derived diagnostic backend readiness that materialization never observed

- **Observation:** after the exact `855ffa71` image replaced the A1 NanoCore test container, scheduler restart recovery failed on `Workspace backend handle handoff is incomplete`. The recoverable predecessor failed the same invariant when restarted. Read-only inspection found one `physical-cleaned` backend session with `workspaceHandoffState=complete`, one Workspace input, one materialization record, and one pending backend handle. Every structural and identity comparison passed except the full canonical materialization record: the accepted NanoHost materialization had omitted optional `backendStatus`, so the durable record correctly contained only `sandbox.created`, while cleanup reconstructed an unobserved `backend.ready` entry from the backend anchor. The S39 owner states that readiness evidence is diagnostic and omitted materialization evidence produces no entry.
- **Impact:** `docs/specs/20260703-workspace_synchronization.md`; `docs/specs/20260703-worker_context_package.md`; `apps/nanocore/src/runtime/workspace-sync-records.ts`; `apps/nanocore/src/runtime/worker-backend-cleanup-projection.ts`; their focused tests; NHC-IMP-5G A1 execution; and root `[SCOPE-007]`, `[TEST-009]`, and `[QUALITY-001]`.
- **Evidence:** Raised by: the primary agent's exact A1 deployment, non-destructive container rollback attempt, redacted container logs, and read-only Core, Workspace, and AEP probes in a network-disabled ephemeral container with the persistent data root mounted read-only. No database, fixture, Workspace record, backend session, AEP snapshot, container image, or credential was edited during diagnosis.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: use stored `readinessEvidence` only to determine whether the optional backend observation existed, then re-derive the exact canonical backend and Sandbox evidence together with every deterministic materialization and backend-handoff field. Do not invent `backend.ready`, trust stored evidence kind/reference/order, edit the retained record, add a compatibility case, loosen deterministic identity checks, migrate data, or create another recovery owner. Recorded transition history: the lowest-sufficient regression and validator correction are implemented and independently accepted. The positive case preserves a sandbox-only accepted materialization when `backendStatus` was absent; the negative case rejects a schema-valid foreign Sandbox evidence reference. The complete focused file passes `14/14`, the adjacent Workspace synchronization and materialization set passes `53/53`, NanoCore typecheck, targeted Biome, and scoped diff checks pass, and independent review rejected six additional schema-valid evidence mutations before returning `PASS`. The full repository gate exits 0, including repository checks, lint, typecheck, unit tests, coverage, builds, and the CLI bundle; the checkpoint commit and another A1 deployment remain. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: the lowest-sufficient regression and validator correction are implemented and independently accepted. The positive case preserves a sandbox-only accepted materialization when `backendStatus` was absent; the negative case rejects a schema-valid foreign Sandbox evidence reference. The complete focused file passes `14/14`, the adjacent Workspace synchronization and materialization set passes `53/53`, NanoCore typecheck, targeted Biome, and scoped diff checks pass, and independent review rejected six additional schema-valid evidence mutations before returning `PASS`. The full repository gate exits 0, including repository checks, lint, typecheck, unit tests, coverage, builds, and the CLI bundle; the checkpoint commit and another A1 deployment remain. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-042.
- **Closure evidence:** Recorded disposition evidence: the lowest-sufficient regression and validator correction are implemented and independently accepted. The positive case preserves a sandbox-only accepted materialization when `backendStatus` was absent; the negative case rejects a schema-valid foreign Sandbox evidence reference. The complete focused file passes `14/14`, the adjacent Workspace synchronization and materialization set passes `53/53`, NanoCore typecheck, targeted Biome, and scoped diff checks pass, and independent review rejected six additional schema-valid evidence mutations before returning `PASS`. The full repository gate exits 0, including repository checks, lint, typecheck, unit tests, coverage, builds, and the CLI bundle; the checkpoint commit and another A1 deployment remain. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-043 — Real Task preflight fixed a fixture provider identity instead of reading the target configuration

- **Observation:** after `dc2d44a8` restored NanoCore health and NanoHost reached authenticated generation 12 readiness, the first runner attempt failed because its SSH forward had exited during container replacement. A renewed forward passed authenticated diagnostics, but the next attempt still stopped before Workspace or Turn creation: the shared real-Codex preflight required provider `openai_codex` and subscription slot `default`, while the target's exact agent selects provider `a1-openai-codex`, whose `openai-codex` profile binds the sole logged-in slot `a1-codex-slot`. The model, agent, worker image, provider family, and account status are otherwise accepted.
- **Impact:** `apps/nanocore/e2e/_lib/real-codex-support.mjs`; `apps/nanocore/e2e/task-mode-real-worker-runner.mjs`; their focused tests; NHC-IMP-5G A1 execution; and root `[EVID-001]`, `[TEST-009]`, and `[TEST-012]`.
- **Evidence:** Raised by: the primary agent's exact A1 execution, one authenticated read-only diagnostics probe, and the ordinary provider-subscription and runtime-config read APIs. Neither failed attempt reached product admission, changed runtime configuration, or consumed provider quota.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: derive the expected provider id and subscription slot from the exact selected agent and provider files, require that exact account to be logged in, preserve the fixed accepted model and provider family checks, and pass the derived provider identity into the existing AEP assertion. Add no A1-specific constant, runtime-config mutation, login flow, environment option, product API, or compatibility branch. Recorded transition history: corrected and independently accepted. The target-shaped regression first failed on the historical `default` account slot, and the AEP mismatch regression proved that the runner ignored the derived provider identity. The corrected support and runner tests pass `73/73`; targeted Biome and diff checks pass. Independent review first rejected six unprotected provider/account predicates and fixed-provider propagation, then accepted the strengthened table-driven negatives after all eight requested mutations were killed. The final helper returns only the used provider identity. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected and independently accepted. The target-shaped regression first failed on the historical `default` account slot, and the AEP mismatch regression proved that the runner ignored the derived provider identity. The corrected support and runner tests pass `73/73`; targeted Biome and diff checks pass. Independent review first rejected six unprotected provider/account predicates and fixed-provider propagation, then accepted the strengthened table-driven negatives after all eight requested mutations were killed. The final helper returns only the used provider identity. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-043.
- **Closure evidence:** Recorded disposition evidence: corrected and independently accepted. The target-shaped regression first failed on the historical `default` account slot, and the AEP mismatch regression proved that the runner ignored the derived provider identity. The corrected support and runner tests pass `73/73`; targeted Biome and diff checks pass. Independent review first rejected six unprotected provider/account predicates and fixed-provider propagation, then accepted the strengthened table-driven negatives after all eight requested mutations were killed. The final helper returns only the used provider identity. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-044 — The real Task runner conflated deployment administration with product-user authority

- **Observation:** after NHC-IMP-5I passed the exact target provider, account, strict-reload, and readiness checks, the real runner still failed before creating a Workspace. A bounded official `createWorkspace` probe returned `Workspace access denied.` The runner builds one Core Client from `OPENKIT_NANOCORE_TOKEN`; on A1 that credential is the required `server-admin` Token for runtime configuration, provider-subscription, and deployment diagnostics, but the accepted operation-access catalog admits `POST /api/workspaces` only for a canonical local or Better Auth session actor and explicitly rejects `server-admin`. Using either actor for both halves is therefore impossible by design.
- **Impact:** `apps/nanocore/e2e/task-mode-real-worker-runner.mjs`; its focused tests; NHC-IMP-5G A1 execution; the existing deployment-admin and canonical-user boundaries; and root `[EVID-001]`, `[AUTH-003]`, `[TEST-009]`, and `[TEST-012]`.
- **Evidence:** Raised by: the primary agent's third exact A1 runner attempt, the runner's owner-only redacted failure artifact, two no-product-mutation preflight/diagnostics probes, one ordinary App API Workspace-creation probe, and the accepted operation-access catalog. No database, runtime configuration, provider credential, fixture, or authentication authority was edited.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: keep one deployment-admin client for runtime preflight and readiness diagnostics and one Better Auth session client for Workspace, Thread, repository, Task, and evidence operations. Supply the existing bearer Token and an operator-provided session cookie through separate secret environment inputs; require both or neither, keep both out of evidence and output, and preserve local unauthenticated execution when neither exists. Do not change NanoCore authorization, create or sign in a user inside the runner, infer one credential from the other, add a product API, persist either credential, or fall back across the two clients. Recorded transition history: corrected and independently accepted. The regression first proved that the existing client factory was not behaviorally observable and that whitespace-only credentials could be treated as configured. The final runner constructs an exact bearer-only admin client and cookie-only product client, reuses one client only for the credential-free local path, rejects half-configured, whitespace-only, and CR/LF-bearing credentials before contact, and keeps both secrets out of success and failure evidence. The focused runner suite passes `68/68`; targeted Biome and diff checks pass. Independent review verified actor routing, header separation, local reuse, redaction, and the absence of login, credential fallback, or persistence. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected and independently accepted. The regression first proved that the existing client factory was not behaviorally observable and that whitespace-only credentials could be treated as configured. The final runner constructs an exact bearer-only admin client and cookie-only product client, reuses one client only for the credential-free local path, rejects half-configured, whitespace-only, and CR/LF-bearing credentials before contact, and keeps both secrets out of success and failure evidence. The focused runner suite passes `68/68`; targeted Biome and diff checks pass. Independent review verified actor routing, header separation, local reuse, redaction, and the absence of login, credential fallback, or persistence. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-044.
- **Closure evidence:** Recorded disposition evidence: corrected and independently accepted. The regression first proved that the existing client factory was not behaviorally observable and that whitespace-only credentials could be treated as configured. The final runner constructs an exact bearer-only admin client and cookie-only product client, reuses one client only for the credential-free local path, rejects half-configured, whitespace-only, and CR/LF-bearing credentials before contact, and keeps both secrets out of success and failure evidence. The focused runner suite passes `68/68`; targeted Biome and diff checks pass. Independent review verified actor routing, header separation, local reuse, redaction, and the absence of login, credential fallback, or persistence. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [deferred] NHC-FND-045 — Locked-out server deployments have no accepted operator credential-recovery procedure

- **Observation:** after the NHC-IMP-5J checkpoint, the next A1 attempt required the existing `server-admin` bearer and an independent Better Auth session cookie. The prior attempt-local bearer plaintext was no longer present in the primary process, the endpoint-scoped macOS Keychain or the encrypted OpenKit credential fallback, while NanoCore correctly retained only one-way Token authority. A read-only A1 inventory then found a pre-existing root-owned `0600` operator file whose creation time matched the sole active `server-admin` Token and whose recent use was already recorded; the user-authorized test-server operation read that file only into process memory and proved it through deployment diagnostics without exposing the value. This operator-held artifact unblocks the current A1 attempt but is not a repository-defined credential store or recovery owner. The accepted remote-auth specification still forbids plaintext recovery, re-bootstrap after any User exists, Better Auth or NanoHost credential elevation, and Token issuance or rotation without an existing `server-admin` bearer, and no separately specified stopped-server recovery procedure exists for a deployment that lacks such an operator-held plaintext.
- **Impact:** `docs/specs/20260704-remote_auth_credential_bootstrap.md`; `apps/nanocore/src/auth/bootstrap-token.ts`; `apps/nanocore/src/auth/access-token-routes.ts`; the stopped-server data-root tools; the NHC-IMP-5G A1 acceptance sequence; and root `[AUTH-001]`, `[DOC-017]`, `[EVID-001]`, and `[SCOPE-007]`.
- **Evidence:** Raised by: the primary agent's secret-free environment and supported-credential-store probes, a redacted read-only A1 file and Token-lifecycle inventory, and independent Verifier review of the accepted authority and exact secret-handling path. The investigation never emitted, hashed, logged, persisted, or placed the Token in argv, evidence, or repository state.
- **Owner:** A future specification-first locked-deployment recovery change, with engineer acceptance required before implementation.
- **Next action:** Suggested follow-up: define a separate spec-first stopped-server locked-deployment recovery procedure only if the product needs self-service recovery. That owner must define physical/operator authority, exclusive process and data-root fencing, pre-change backup and integrity proof, issuance of a fresh one-time `server-admin` secret to an owner-only sink, disposition of prior credentials, audit evidence, failure rollback, retry, and restart semantics before any command or implementation is added. Recorded transition history: the immediate A1 acceptance blocker is resolved by the existing operator-held plaintext and a bounded in-memory use path; the general locked-out deployment recovery gap remains unresolved and outside this completion plan's accepted implementation scope. Direct SQLite mutation, offline store-helper invocation, User removal, repeated bootstrap, or credential-family substitution would invent authority and remain prohibited. The stopped-server locked-deployment recovery specification owner receives this item if the product requires self-service recovery and an engineer accepts that separate change.

## [closed] NHC-FND-046 — Runtime-config atomic replacement widened an existing file's permissions

- **Observation:** the first accepted loopback-only signup window used the official admin config read, draft validation, revision-CAS update, and exact-byte restore path. Both writes preserved source bytes and semantic authority, but the shared atomic helper created its same-directory temporary file with the process default and renamed it over an existing root-owned `0600` `server.jsonc`, producing `0644`. The pre-Task assertion stopped execution; the outer exact-byte backup restored bytes and `0600`, restarted NanoCore, re-proved signup disabled, and the newly created User was formally disabled with zero sessions. No Task, Workspace, Thread, Turn, AgentSession, Sandbox, Harness, or provider request was created.
- **Impact:** `docs/specs/20260628-nanocore_config_identity_contract.md`; `apps/nanocore/src/config/runtime-config-files.ts`; `apps/nanocore/src/runtime-config-files.test.ts`; the NHC-IMP-5G A1 acceptance sequence; and root `[SCOPE-004]`, `[TEST-009]`, `[QUALITY-001]`, and `[QUALITY-003]`.
- **Evidence:** Raised by: the primary agent's user-authorized A1 config window, exact mode/hash/owner/container assertions, and an independent Verifier review. The failure occurred after official API restore and before real Task execution; no SSH chmod was used to mask the shared-owner defect.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: preserve only existing regular-file permission bits inside the existing atomic writer before publication. Do not add SSH chmod wrappers around normal API writes, chown, ACL or xattr preservation, new state, a metadata abstraction, an alternate config path, or a compatibility branch. The next A1 attempt must still assert exact owner and `0600` after each PUT and before restart, with the existing backup retained only for break-glass restore. Recorded transition history: corrected locally and independently accepted. Under an explicit `0022` umask, the existing revision-guard route regression first observed successful PUT and updated content followed by `0644` instead of `0600`. The shared writer now captures only `mode & 0o777` from an existing regular target, writes the same-directory temporary file, explicitly sets those bits before rename, and leaves missing-target defaults unchanged. The complete focused file passes `10/10`, NanoCore typecheck, targeted Biome, and scoped diff checks pass; internal review returned `PASS` on the actual Grok-produced bytes. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally and independently accepted. Under an explicit `0022` umask, the existing revision-guard route regression first observed successful PUT and updated content followed by `0644` instead of `0600`. The shared writer now captures only `mode & 0o777` from an existing regular target, writes the same-directory temporary file, explicitly sets those bits before rename, and leaves missing-target defaults unchanged. The complete focused file passes `10/10`, NanoCore typecheck, targeted Biome, and scoped diff checks pass; internal review returned `PASS` on the actual Grok-produced bytes. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-046.
- **Closure evidence:** Recorded disposition evidence: corrected locally and independently accepted. Under an explicit `0022` umask, the existing revision-guard route regression first observed successful PUT and updated content followed by `0644` instead of `0600`. The shared writer now captures only `mode & 0o777` from an existing regular target, writes the same-directory temporary file, explicitly sets those bits before rename, and leaves missing-target defaults unchanged. The complete focused file passes `10/10`, NanoCore typecheck, targeted Biome, and scoped diff checks pass; internal review returned `PASS` on the actual Grok-produced bytes. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-047 — Access-Token listing parses a retired scope retained in A1 history

- **Observation:** the recovered A1 `server-admin` bearer passed deployment diagnostics, while `GET /api/app/auth/tokens` returned `500`. Redacted logs and read-only storage inspection proved that the list route reads every historical row and the response schema rejects one revoked legacy `workspace-readwrite` scope because the current contract accepts only `server-admin`, `workspace`, and `workspace-readonly`. Presented-secret authentication reads only the exact hashed Token row, and config update, diagnostics, Workspace recovery, and User disable do not depend on Token enumeration.
- **Impact:** `apps/nanocore/src/auth/access-token-routes.ts`; `apps/nanocore/src/auth/access-token-store.ts`; `packages/app-api-schemas/src/auth.ts`; the Core migration history; and root `[EVID-001]`, `[NONNEG-001]`, and `[SCOPE-012]`.
- **Evidence:** Raised by: the primary agent's read-only admin preflight, redacted A1 log inspection, and independent authority review. No legacy row, Token, database, schema, or runtime state was edited.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: add one Core migration that appends one redacted server system audit event and deletes only each unreferenced revoked `workspace-readwrite` row, preserves every current Token column and all prior audit history, rejects any remaining unsupported scope, and publishes its ledger id in the same transaction. Keep the closed App API schema, token read model, list query, and exact-secret verifier unchanged; do not manually edit A1, translate the retired scope, filter it indefinitely, or add a response compatibility branch. Recorded transition history: corrected locally and independently accepted. `core_0009_retire_workspace_readwrite` deletes only unreferenced revoked `workspace-readwrite` rows, appends one redacted server system audit event per deletion, rejects every remaining unsupported scope, and publishes its ledger entry in the same transaction. The migration, layout, and real list-route suite passes 28/28; NanoCore typecheck, targeted Biome, and `git diff --check` pass. Independent probes additionally proved that a revoked legacy candidate chain retires completely while any surviving current-Token lineage reference rolls the transaction back. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally and independently accepted. `core_0009_retire_workspace_readwrite` deletes only unreferenced revoked `workspace-readwrite` rows, appends one redacted server system audit event per deletion, rejects every remaining unsupported scope, and publishes its ledger entry in the same transaction. The migration, layout, and real list-route suite passes 28/28; NanoCore typecheck, targeted Biome, and `git diff --check` pass. Independent probes additionally proved that a revoked legacy candidate chain retires completely while any surviving current-Token lineage reference rolls the transaction back. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-047.
- **Closure evidence:** Recorded disposition evidence: corrected locally and independently accepted. `core_0009_retire_workspace_readwrite` deletes only unreferenced revoked `workspace-readwrite` rows, appends one redacted server system audit event per deletion, rejects every remaining unsupported scope, and publishes its ledger entry in the same transaction. The migration, layout, and real list-route suite passes 28/28; NanoCore typecheck, targeted Biome, and `git diff --check` pass. Independent probes additionally proved that a revoked legacy candidate chain retires completely while any surviving current-Token lineage reference rolls the transaction back. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-048 — Repository admission allowed a DATA_ROOT-contained source to poison canonical AgentSession history

- **Observation:** the first real Task under exact image `c9d8c6c6` configured its Git repository below NanoCore's data root. Repository validation accepted the existing Git directory, scheduler dispatch created the Turn lease, and `WorkerGovernanceTurnExecutor` wrote an AgentSession whose `workspaceRoots.sourcePath` embedded that absolute data-root path. The next `ensureLayout` call then correctly rejected the canonical file, but the rejection happened after publication and left the process and data root poisoned.
- **Impact:** `docs/specs/20260704-workspace_backup_export_import.md`; `apps/nanocore/src/workspace/repository-validation.ts`; `apps/nanocore/src/workspace/repository-store.ts`; `apps/nanocore/src/runtime/turn-workspace-context.ts`; `apps/nanocore/src/lib/store.ts`; the NHC-IMP-5G A1 acceptance; and root `[SCOPE-004]`, `[SCOPE-007]`, `[TEST-009]`, and `[QUALITY-001]`.
- **Evidence:** Raised by: the primary agent's user-authorized A1 real Task, product-safe failed-Turn readback, redacted NanoCore logs, and read-only scheduler, Workspace, and filesystem inspection. The failed attempt produced no Worker Backend Session or provider result; NanoHost was restarted once through its existing systemd owner only after read-only evidence proved the prior coordinator had fail-stopped during the preceding NanoCore restart.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: reuse one repository-path predicate at the existing configuration and Turn-admission owners, preserve any previous repository resource and catalog, and return a redacted failure. Add no path rewrite, migration, quarantine, automatic repair, compatibility reader, or Workspace deletion behavior. Recorded transition history: NHC-IMP-5L is complete as the bounded prevention correction. The shared repository owner rejects lexical or canonical DATA_ROOT containment and unresolved canonical proof before repository mutation or Turn admission, and the accepted Turn uses one captured canonical path for `workspaceCwd` and its repository Workspace Root. The exact A1 Workspace remains poisoned and is not repaired by this correction. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: NHC-IMP-5L is complete as the bounded prevention correction. The shared repository owner rejects lexical or canonical DATA_ROOT containment and unresolved canonical proof before repository mutation or Turn admission, and the accepted Turn uses one captured canonical path for `workspaceCwd` and its repository Workspace Root. The exact A1 Workspace remains poisoned and is not repaired by this correction. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-048.
- **Closure evidence:** exact, descendant, symlink-alias, sibling-prefix, normalized-external, missing, retargeted, unresolvable, canonical-root conflict, redaction, and zero-effect regressions pass in the five-file 45/45 focused suite; NanoCore typecheck, targeted Biome, `git diff --check`, and independent internal review pass. Recorded disposition evidence: NHC-IMP-5L is complete as the bounded prevention correction. The shared repository owner rejects lexical or canonical DATA_ROOT containment and unresolved canonical proof before repository mutation or Turn admission, and the accepted Turn uses one captured canonical path for `workspaceCwd` and its repository Workspace Root. The exact A1 Workspace remains poisoned and is not repaired by this correction. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [deferred] NHC-FND-049 — The current A1 data root has no accepted recovery owner for the poisoned disposable Workspace

- **Observation:** read-only A1 inventory found no verified clean data-root backup, restore staging tree, filesystem snapshot, Workspace export manifest, or sealed audit closure. Retained NanoCore containers all mount the same live tree. The repository implements only synchronous rollback of a newly staged import; it has no ordinary or operator Workspace deletion route, and accepted Workspace deletion requires a sealed audit closure plus retention, membership, Vault, runtime, database, and file cleanup ownership.
- **Impact:** `docs/core/storage.md`; `docs/specs/20260703-storage_layout_record_ownership.md`; `docs/specs/20260704-workspace_backup_export_import.md`; `docs/specs/20260715-multi_user_workspace_system.md`; `apps/nanocore/src/lib/store.ts`; the current A1 deployment; and root `[AUTH-001]`, `[SCOPE-004]`, `[SCOPE-007]`, and `[SCOPE-013]`.
- **Evidence:** Raised by: the primary agent's read-only CodeGraph and A1 inventory plus an independent fresh-context direction review. No A1 file, database row, service, container, credential, Workspace, or runtime record was changed during the recovery search.
- **Owner:** A future Workspace and data-root recovery change under the Core storage, Workspace lifecycle, and audit-closure owners, with engineer acceptance required.
- **Next action:** Accepted execution route: continue acceptance only in a collision-free sibling deployment with a new data root, bootstrap, runtime identity, attempt-local credentials, evidence root, and exact-owned cleanup. Do not copy old SQLite, Token, cookie, runtime-identity, or Workspace records; do not stop the old deployment to create capacity; and fence rather than broaden cleanup when exact ownership cannot be proved. A verified clean full data-root backup would also reopen the existing stopped-server restore owner, but none is currently available. Recorded transition history: unresolved and blocking further product acceptance on the current A1 data root under current accepted authority and available evidence. Manual deletion or editing of the AgentSession record, Workspace database, Core registry or membership rows, or Workspace directory would bypass accepted authority and is prohibited. NHC-IMP-5L prevents recurrence but cannot supply retrospective cleanup authority. An independent blocker Verifier applied the engineer's standing A1 and automatic-continuation authorization and returned `PASS` for a separately initialized clean acceptance deployment that preserves the complete current tree as immutable recovery evidence. The Core storage and Workspace lifecycle recovery owners receive this item when a verified clean backup or an accepted recovery procedure is available.

## [closed] NHC-FND-050 — Current NanoHost bytes cannot isolate a second acceptance deployment on A1

- **Observation:** read-only source and A1 inspection proved that a clean NanoCore sibling can own a separate container, data root, and loopback port, but current NanoHost owns fixed `/var/lib/openkit/nanohost`, `/run/openkit/nanohost`, Image Store and evidence roots, `openkit-nanohost.slice`, credential slots, and Gateway `127.0.0.1:17670`. A second unit would collide with or share the current fail-stop epoch. Stopping and repointing the existing unit would write the retained recovery/evidence roots, alter transport generations and fences, and therefore would not preserve either the old epoch or a clean runtime identity.
- **Impact:** `docs/specs/20260802-nanohost_runtime_and_transport.md`; `docs/specs/20260703-runtime_scheduling_scale.md`; `apps/nanohost/src/main.rs`; `apps/nanohost/src/epoch_coordinator.rs`; `apps/nanohost/deploy/openkit-nanohost.service`; the remaining NHC-IMP-5 real acceptance; and root `[SCOPE-004]`, `[SCOPE-007]`, and `[SCOPE-013]`.
- **Evidence:** Raised by: the primary agent's CodeGraph inspection, an independent read-only A1 inventory, and a fresh independent blocker Verifier. The inventory changed no repository or A1 state and read no credential value. Local-machine probe: OrbStack `2.2.3` is available and can create an isolated Linux machine, but no Linux machine currently exists and the active OrbStack runtime reports kernel `7.0.14-orbstack-00380-ga7e0a2dc9535`. The repository's sole admitted host manifest is the exact A1 identity and requires `6.17.0-1018-oracle`; therefore no presently available local target can pass that manifest. Creating a VM without first accepting a truthful host identity would produce no real-use evidence and is not an automatic substitute for the required isolated target.
- **Owner:** Not recorded.
- **Next action:** Suggested follow-up: provide and authorize a separate execution host or VM with independent OS/systemd, filesystem roots, runtime directory, slice, network namespace, Gateway port, credential slots, Image Store, and evidence root, then run the existing host manifest before deploying exact accepted bytes. Same-host NanoHost namespacing is a larger product-scale specification and implementation alternative, not a deployment-only correction, and requires a new explicit engineer decision. Recorded transition history: blocking real NanoHost acceptance on current A1 while preserving its recovery set. The first Verifier `PASS` selected a clean sibling only under the premise that the NanoHost runtime could be isolated; the second Verifier returned `BLOCK` after current implementation and host facts disproved that premise. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: blocking real NanoHost acceptance on current A1 while preserving its recovery set. The first Verifier `PASS` selected a clean sibling only under the premise that the NanoHost runtime could be isolated; the second Verifier returned `BLOCK` after current implementation and host facts disproved that premise. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-050.
- **Closure evidence:** Recorded disposition evidence: blocking real NanoHost acceptance on current A1 while preserving its recovery set. The first Verifier `PASS` selected a clean sibling only under the premise that the NanoHost runtime could be isolated; the second Verifier returned `BLOCK` after current implementation and host facts disproved that premise. Intent Epoch 7 in plan.md removed the requirement to preserve the former A1 deployment or construct a parallel sibling before the accepted NHC-5 and NHC-8 execution.

## [closed] NHC-FND-051 — NanoHost declares a Git worktree without materializing repository bytes

- **Observation:** the exact `7a228369` A1 attempt passed NanoHost readiness, Sandbox creation, canonical AEP import, generated Context Package imports, Harness bootstrap, and the corrected closed `session.open` result. Its following `turn.start` immediately returned `dependency_failed` before a native Codex process started. The retained AEP declares native cwd `/workspace/openkit` and one read-write Git input at `/workspace/openkit/worktrees/main`, but the selected NanoHost materializer imports only package-config and generated Context files. The worker image creates neither declared path, and the first `prepareWorkspaceGitSnapshots` operation therefore cannot prove `HEAD`; even without that check, native process spawn would fail on the absent cwd. Durable Workspace materialization records contain only the declared target and `sandbox.created` evidence, so they do not prove repository bytes or a Git worktree existed.
- **Impact:** `docs/specs/20260704-session_static_workspace_materialization.md`; `docs/specs/20260801-nanohost_workspace_data_boundary.md`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `apps/nanocore/src/runtime/turn-executor-factory.ts`; `packages/worker-shim/src/cli.ts`; `packages/worker-shim/src/workspace-git.ts`; the NHC-IMP-5 real-Turn acceptance; and root `[AUTH-001]`, `[PRECEDENCE-003]`, `[TEST-009]`, and `[SCOPE-007]`.
- **Evidence:** Raised by: the primary agent's exact A1 deployment and redacted durable/journal inspection, an independent read-only test-author trace, and an independent owning-spec Verifier. No repository, worker, A1 record, Sandbox, credential, or runtime path was edited during diagnosis.
- **Owner:** Not recorded.
- **Next action:** Remaining acceptance: build the exact reviewed app and worker commit directly on A1, run the real Task against the pinned public Git commit through the declared NanoHost network policy, and retain redacted evidence that checkout precedes native start and terminal cleanup leaves no effect-capable survivor. Private Git credentials, push or pull-request operations, host-side review application, and NHC-FND-052 remain outside this slice. Add no bundle fallback, NanoHost Git effect, hosted Git service, object store, or backward-compatibility path. Recorded transition history: architecture and local implementation are resolved by Intent Epoch 8; real A1 acceptance remains blocking. Turn admission and the real-worker runner no longer consume a repository `localPath`; they resolve one credential-free HTTPS Git source and exact commit from the selected Agent and Workspace catalog before filesystem or scheduler effects. The Worker Shim performs a fresh detached checkout and clean-`HEAD` proof before native start, removes partial targets on failure, and rejects target-parent symlink escape. NanoHost owns no Git application semantics. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: architecture and local implementation are resolved by Intent Epoch 8; real A1 acceptance remains blocking. Turn admission and the real-worker runner no longer consume a repository `localPath`; they resolve one credential-free HTTPS Git source and exact commit from the selected Agent and Workspace catalog before filesystem or scheduler effects. The Worker Shim performs a fresh detached checkout and clean-`HEAD` proof before native start, removes partial targets on failure, and rejects target-parent symlink escape. NanoHost owns no Git application semantics. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-051.
- **Closure evidence:** Recorded disposition evidence: architecture and local implementation are resolved by Intent Epoch 8; real A1 acceptance remains blocking. Turn admission and the real-worker runner no longer consume a repository `localPath`; they resolve one credential-free HTTPS Git source and exact commit from the selected Agent and Workspace catalog before filesystem or scheduler effects. The Worker Shim performs a fresh detached checkout and clean-`HEAD` proof before native start, removes partial targets on failure, and rejects target-parent symlink escape. NanoHost owns no Git application semantics. The plan's NHC-5 and NHC-8 sections record the exact rebuilt A1 runtime completing F1 through F4, the ordinary lifecycle, and Aggregate with every verdict `PASS`.

## [deferred] NHC-FND-052 — Host-side Git owners conflict with the external-source boundary

- **Observation:** `docs/specs/20260704-git_write_workflow.md` still assigns commit, review-branch, push, GitHub credentials, and `gh` execution to a linked repository on the NanoCore host, while `docs/specs/20260703-workspace_synchronization.md` still applies accepted patches to that host checkout and defaults workers away from write-capable Git. Repository routes and bounded Chat inspection likewise consume the private `localPath`. These behaviors depend on NanoCore retaining a repository checkout and directly performing Git application effects.
- **Impact:** `docs/core/storage.md`; `docs/specs/20260704-workspace_data_source_catalog.md`; `docs/specs/20260704-session_static_workspace_materialization.md`; `docs/specs/20260704-git_write_workflow.md`; `docs/specs/20260703-workspace_synchronization.md`; repository routes, Git push records, Workspace review apply, bounded repository inspection, and their public schemas and clients.
- **Evidence:** Raised by: the engineer's Intent Epoch 8 storage and Sandbox-Git decision plus the primary agent's current accepted-owner conflict scan. No production code, external Git service, credential, repository, or A1 state was changed by this finding.
- **Owner:** The Git write workflow and Workspace synchronization specification owners in a separate cross-owner change.
- **Next action:** Suggested correction: amend the Git write and Workspace synchronization owners before touching their production paths. Git and hosting commands run inside the Sandbox against the network-addressable catalog source; NanoCore retains authorization, approval, review, audit, immutable lineage, and bounded result records; Vault and network policy gate credentials and egress; NanoHost has no Git semantics. The amendment must decide how an approved write-capable operation is granted, observed, interrupted, retried, and recorded without turning a worker result into unverified publication authority. Recorded transition history: unresolved adjacent cross-owner redesign. Core storage now wins the conflict: a remote Agent Runtime cannot consume a NanoCore host checkout, and no new work may extend the host-side Git path. Existing implementation remains a measured legacy projection until its full producer, consumer, review, approval, credential, audit, and deletion blast radius is replaced without compatibility behavior. The Git write workflow and Workspace synchronization specification owners receive this item when a separate cross-owner redesign is accepted.

## [closed] NHC-FND-053 — Task preparation retained a second host-repository gate after remote source admission

- **Observation:** A1 directly built and deployed exact `b9842c59`, and the corrected real runner created a Workspace, Thread, and exact credential-free HTTPS Git catalog through the deployment-admin config owner. The product Task still returned `409 workspace_repository_unavailable` before scheduler admission because direct Task and Chat-to-Task preparation separately required a ready host `WorkspaceRepository`; the shared Goal preparation and checkpoint/API context assembly also retained the same obsolete repository object and `repositoryResourceId`. The selected Agent manifest and Workspace catalog were already the actual launch source authority, so this second gate neither materialized nor validated the remote source.
- **Impact:** `apps/nanocore/src/mode-entry-routes.ts`; `apps/nanocore/src/runtime/prepare-next-turn.ts`; `apps/nanocore/src/runtime/goal-task-delegation.ts`; `apps/nanocore/src/runtime/worker-turn-loop.ts`; `apps/nanocore/src/runtime/worker-checkpoints.ts`; `packages/app-api-schemas/src/dashboard.ts`; the real Task runner; and root `[PRECEDENCE-002]`, `[TEST-009]`, `[QUALITY-001]`, and `[QUALITY-003]`.
- **Evidence:** Raised by: the primary agent's exact A1 single-Turn attempt, owner-only redacted failure evidence, and one parameterized real App API regression that proves direct Task and Chat-to-Task both reject remote source admission with zero `WorkspaceRepository` rows. The attempt restored signup-disabled configuration, disabled the disposable user, revoked its session, and left NanoCore and NanoHost healthy.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected and rebuilt on A1; exact single-Turn acceptance remains pending the corrected runner rerun. The obsolete Task gates, `PreparedNextTurn.repository`, dead Core/Workspace database reads and arguments, checkpoint `repositoryResourceId`, and dashboard projection are deleted rather than replaced. Goal, Task, and Chat-to-Task worker startup now share the selected Agent manifest and Workspace catalog validation in the Turn-admission owner; bounded local Chat repository inspection and the unresolved host apply/push owners remain separate. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected and rebuilt on A1; exact single-Turn acceptance remains pending the corrected runner rerun. The obsolete Task gates, `PreparedNextTurn.repository`, dead Core/Workspace database reads and arguments, checkpoint `repositoryResourceId`, and dashboard projection are deleted rather than replaced. Goal, Task, and Chat-to-Task worker startup now share the selected Agent manifest and Workspace catalog validation in the Turn-admission owner; bounded local Chat repository inspection and the unresolved host apply/push owners remain separate. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-053.
- **Closure evidence:** the new direct Task and Chat-to-Task regression passes `2/2`; complete `server.test.ts` passes `156/156`; the runtime-focused set passes `17/17`; App API schemas pass `114/114`; runner/support pass `102/102`; affected typechecks, builds, Biome, documentation validators, AgentSession terminology, and `git diff --check` pass. A1 built, smoke-tested, deployed, and health-checked exact app commit `115060dc`; a redacted diagnostic then proved the post-catalog safe reload defers only `workspaceDataSources`, closing the stale two-path runner fixture locally. The real A1 Turn must still prove exact checkout, native start, correlated provenance, and terminal cleanup before this finding is closed. Recorded disposition evidence: corrected and rebuilt on A1; exact single-Turn acceptance remains pending the corrected runner rerun. The obsolete Task gates, `PreparedNextTurn.repository`, dead Core/Workspace database reads and arguments, checkpoint `repositoryResourceId`, and dashboard projection are deleted rather than replaced. Goal, Task, and Chat-to-Task worker startup now share the selected Agent manifest and Workspace catalog validation in the Turn-admission owner; bounded local Chat repository inspection and the unresolved host apply/push owners remain separate. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-054 — The dedicated Docker backend mutates the system Docker network namespace

- **Observation:** three independent A1 NanoHost starts each removed the live system Docker `docker0` projection about one second after the dedicated rootful Docker daemon initialized, before NanoHost teardown. The private daemon used private roots, sockets, and `--bridge none`, yet its startup also attempted to delete global Docker nftables tables because it shared the host network namespace. The system Docker daemon retained its default-bridge database record while the kernel link disappeared, causing bounded build networking to fail and proving that root/socket/cgroup isolation did not bound network effects.
- **Impact:** `docs/specs/20260802-nanohost_runtime_and_transport.md`; `apps/nanohost/src/epoch_coordinator.rs`; `apps/nanohost/src/openshell_client.rs`; `apps/nanohost/deploy/openkit-nanohost.service`; `tests/support/host/manifest.json`; A1 system Docker and business-container networking; root `[SCOPE-004]`, `[AUTH-001]`, `[TEST-009]`, and `[GOV-017]`.
- **Evidence:** Raised by: exact A1 cutover and three timestamp-correlated read-only host inventories, followed by independent owning-spec review. A bounded exploratory slirp probe targeted an outer `sudo` PID and allowed mount propagation, interrupting SSH and unmounting host cgroup/security filesystems; the agent reported the mistake immediately and recovered A1 by reboot rather than manually reconstructing network or mount state. Final recovery proved cgroup2, securityfs, AppArmor, system Docker, `docker0`, business-container health and attachments, and container DNS/TLS egress; NanoHost remains stopped.
- **Owner:** The current plan's NHC-IMP-5O A1 real-host noninterference verification owner.
- **Next action:** Accepted correction: retain the NanoHost parent in the host namespace for its sole NanoCore session; place the dedicated `containerd`, dedicated `dockerd`, and stock Gateway in one fresh epoch-private network namespace; supervise one exact manifest-owned slirp helper in the existing cgroup; fail closed on fixed resolver or ready-fd proof; and use one short-lived namespace-entering connector for initial Gateway connection and every permitted reconnect. Add no effect, state, listener, proxy, endpoint, second channel, configuration input, or compatibility behavior. Closure evidence required: independent specification acceptance; mutation-sensitive namespace, DNS, connector, reconnect, and teardown regressions; exact host-manifest path/version/digest checks; local Rust and repository gates; and A1 start, stop, member-failure, and `SIGKILL` evidence proving the private namespace disappears while system Docker bridge metadata, kernel `docker0`, Docker-owned nftables, all business-container identities and network attachments, and bounded build-network egress remain unchanged. Recorded transition history: the NHC-IMP-5O implementation is locally complete and independently reviewed `PASS`; exact rebuilt A1 bytes remain blocked from product acceptance until the real-host noninterference gate passes. Mutation-sensitive focused regressions pass 7/7, the NanoHost suite passes 79 unit plus 1 service test, the host manifest suite passes 61/61, and proportional build, Clippy, fmt, and diff checks pass. Flag-only changes such as `--bridge none` or disabling iptables do not isolate the effect domain. Rootless Docker or RootlessKit, a whole-service private network namespace, additional joined systemd units, a permanent proxy, and manual host bridge or nftables repair remain rejected as broader or incorrect ownership. The current plan now owes only the NHC-IMP-5O A1 real-host noninterference gate, and this item closes only after retained observations settle every named predicate.
- **Closing verdict:** Resolved by the epoch-private network namespace and independently accepted no-reboot A1 evidence. The original dedicated-Docker mutation does not remain in the corrected topology, and Scenario 3 remains separately deferred under NHC-FND-117 rather than reopening this finding.
- **Closure evidence:** Retained artifact digest `c1e8a70e0b78d39531e76dd9dd92ee630574c2e0d926b6ad1a9b2bce9cbe4d03`, mode `0600`, records F1, F2, F4, network conformance, normal lifecycle, terminal cleanup, and Aggregate all `PASS` on exact current product bytes without rebooting A1. Its accepted observations cover the private namespace, exact slirp and TAP topology, fixed DNS, system-Docker socket isolation, business-container attachments, system Docker bridge and build egress, F2 `SIGKILL`, F4 member failure, ordinary start and stop, service inactivity, and epoch-residue absence. Independent artifact audit confirmed that these named NHC-FND-054 predicates are sufficient to close this finding while separately blocking R001 on F1's then-unclassified raw baseline delta.

## [closed] NHC-FND-055 — The real-Task runner discarded the product-safe failure evidence that NanoCore already owned

- **Observation:** the real-Task runner classified every non-timeout, non-assertion exception as one constant runtime failure and intentionally discarded the untrusted raw error, while repeated temporary A1 diagnostics independently reopened NanoCore storage and scanned the same OpenKit-owned evidence, backend, lease, admission, runtime, capability, and worker-control tables. NanoCore already owns a closed App API `RuntimeEvidenceRecord` whose diagnostic fields are product-safe projections, and the Core Client already exposes `listWorkspaceRuntimeEvidence`; the missing behavior was therefore one existing-accessor read for the exact failed Turn, not another diagnostic script, path, redaction rule, or storage owner.
- **Impact:** `apps/nanocore/src/runtime/runtime-evidence.ts`; `apps/nanocore/e2e/task-mode-real-worker-runner.mjs`; `apps/nanocore/src/runtime/worker-checkpoints.ts`; their focused tests; and root `[AUTHORITY-001]`, `[QUALITY-003]`, `[TEST-009]`, and `[SCOPE-007]`.
- **Evidence:** Raised by: the engineer's owner analysis, the primary agent's current runner trace, and an independent final review. That review also found that the checkpoint context parser validated required fields but returned the original parsed object, allowing foreign top-level or nested fields to survive the existing redaction boundary.
- **Owner:** Not recorded.
- **Next action:** Accepted correction: reuse the existing RuntimeEvidence accessor and schema only; retain the constant raw-error classification, exact Turn filter, exclusive failure file, and existing public evidence leak check. Add no A1 path, Core database access, table query, diagnostic service, redaction owner, compatibility record, or retained raw exception. Recorded transition history: resolved in current candidate bytes and accepted by an independent reviewer. The runner starts at most one existing `listWorkspaceRuntimeEvidence` Promise, filters its result by the exact `task.turn.id`, writes those product-safe records into the existing exclusive failure file, and still persists only a stable classification of the raw error. The checkpoint parser now validates and reconstructs only `contextDigest`, exact `{ kind, id }` context references, and `knowledgeSelectionInput`; `failureSummary` continues through the existing internal-agent redactor before the closed record is re-encoded. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved in current candidate bytes and accepted by an independent reviewer. The runner starts at most one existing `listWorkspaceRuntimeEvidence` Promise, filters its result by the exact `task.turn.id`, writes those product-safe records into the existing exclusive failure file, and still persists only a stable classification of the raw error. The checkpoint parser now validates and reconstructs only `contextDigest`, exact `{ kind, id }` context references, and `knowledgeSelectionInput`; `failureSummary` continues through the existing internal-agent redactor before the closed record is re-encoded. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-055.
- **Closure evidence:** the runner suite passes `86/86`; the checkpoint suite passes `9/9`; complete `server.test.ts` passes `158/158`; NanoCore typecheck and build pass; targeted Biome and whitespace checks pass; and the final independent review returns `PASS`. A1 image `openkit/app:a1-88ff4691-r7` passed image smoke and h2c health. Its real Task produced one failure file containing exactly three RuntimeEvidence records for one Turn: runtime provenance succeeded with two streams, fifteen attributed frames and one reconciled gateway call; backend teardown succeeded; and the checkpoint recorded the expected worker failure. The operator emitted no cleanup failure, NanoHost returned to active all-three-ready generation `97`, and no temporary table-scanning diagnostic was required. Recorded disposition evidence: resolved in current candidate bytes and accepted by an independent reviewer. The runner starts at most one existing `listWorkspaceRuntimeEvidence` Promise, filters its result by the exact `task.turn.id`, writes those product-safe records into the existing exclusive failure file, and still persists only a stable classification of the raw error. The checkpoint parser now validates and reconstructs only `contextDigest`, exact `{ kind, id }` context references, and `knowledgeSelectionInput`; `failureSummary` continues through the existing internal-agent redactor before the closed record is re-encoded. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-056 — The retained Unit F evidence archive and executable verifier no longer exist

- **Observation:** the plan claimed that `temp/state/wp6/wp6-f-gate/` contained nine retained files, but the exact path is absent in the current candidate and no executable Unit F runner or F1-F4/Aggregate verdict archive exists elsewhere in the owned change record. Historical references cannot substitute for the accepted specification's four real fault scenarios or their aggregate verdict.
- **Impact:** NHC-5 fault acceptance; NHC-6 legacy Cell deletion; the retained-evidence projection in this plan; `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[PRECEDENCE-002]`, `[TEST-012]`, `[GOV-017]`, and `[SCOPE-013]`.
- **Evidence:** Raised by: the primary agent's exact filesystem probe and plan audit plus an independent blocker review. The review confirmed that existing restart runners and unit tests cover only partial behavior and cannot establish the required four real-topology scenarios or aggregate verdict.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved. The accepted minimal runner reuses existing App API and service owners, and the engineer explicitly authorized NHC-6 deletion after NHC-5 `PASS` without new architecture. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved. The accepted minimal runner reuses existing App API and service owners, and the engineer explicitly authorized NHC-6 deletion after NHC-5 `PASS` without new architecture. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-056.
- **Closure evidence:** the complete runner suite passes `69/69`; the retained A1 result records F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, with four scenario rows, no failure record, and process exit zero. The recoverable redacted archive is retained under `temp/state/wp6/wp6-f-gate/` without product durable state, Core database access, a diagnostic reader, or a second redaction owner. Recorded disposition evidence: resolved. The accepted minimal runner reuses existing App API and service owners, and the engineer explicitly authorized NHC-6 deletion after NHC-5 `PASS` without new architecture. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-057 — Sandbox Integration exposed Node HTTP/2 resets outside the worker retry contract

- **Observation:** the second real A1 Unit F F1 attempt reached a running worker, restarted NanoCore, and restored an authoritative ready RuntimeTarget in about 28 seconds, but no later worker-control request reached or was rejected by the successor. The accepted lease retained only the last pre-restart heartbeat before cleanup. A direct Node HTTP/2 probe and a real Integration regression proved that resetting one nested stream rejects with an ordinary `Error` carrying `ERR_HTTP2_STREAM_ERROR`, while the existing post-launch worker-control recovery owner retries fetch transport failures represented as `TypeError`.
- **Impact:** `packages/worker-shim/src/integration-client.ts`; `packages/worker-shim/src/control-client.ts`; NHC-5 F1; and root `[TEST-009]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by normalizing the Integration adapter's stream error to the fetch transport-error shape already consumed by the bounded retry owner. The F1 input returned to its original 45-second active window after the 300-second duration exceeded the existing Task execution bound; the 300-second NanoCore tunnel wait remains unchanged. No scheduler, protocol, product timeout, durable state, or new runner owner changed. The real A1 Unit F verdict remains pending an exact committed-image run. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by normalizing the Integration adapter's stream error to the fetch transport-error shape already consumed by the bounded retry owner. The F1 input returned to its original 45-second active window after the 300-second duration exceeded the existing Task execution bound; the 300-second NanoCore tunnel wait remains unchanged. No scheduler, protocol, product timeout, durable state, or new runner owner changed. The real A1 Unit F verdict remains pending an exact committed-image run. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-057.
- **Closure evidence:** the new real HTTP/2 reset regression passes with the corrected adapter; the complete worker-shim suite passes 181/181; the Unit F runner suite passes 57/57; worker-shim typecheck, targeted Biome, documentation-model checks, and `git diff --check` pass. Recorded disposition evidence: corrected locally by normalizing the Integration adapter's stream error to the fetch transport-error shape already consumed by the bounded retry owner. The F1 input returned to its original 45-second active window after the 300-second duration exceeded the existing Task execution bound; the 300-second NanoCore tunnel wait remains unchanged. No scheduler, protocol, product timeout, durable state, or new runner owner changed. The real A1 Unit F verdict remains pending an exact committed-image run. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-058 — Successor transport dial failure destroyed the surviving Runtime Epoch

- **Observation:** the exact current-app and current-worker A1 Unit F attempt reached the F1 worker-ready barrier and restarted NanoCore. The established NanoHost connection closed as expected, but its immediate successor TCP dial raced NanoCore startup and returned a connection error. The outer loop classified every pre-handshake dial error as terminal even when it already held a predecessor generation, so NanoHost destroyed the still-live Runtime Epoch and restart recovery truthfully interrupted the Turn with `worker_governance_restart_recovery`.
- **Impact:** `apps/nanohost/src/main.rs`; the outer-session reconnect contract in `docs/specs/20260802-nanohost_runtime_and_transport.md`; NHC-5 F1; and root `[TEST-009]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally. An initial rendezvous failure remains terminal, while a connection failure after an observed reconnectable predecessor close retries at 250 ms intervals within the existing 120-second successor-establishment hard bound. The timer begins at the observed outer-session loss, is cleared only after successor readiness and route rebinding, and a deadline or epoch-member failure remains terminal. No new state owner, protocol, service, configuration, or fallback was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally. An initial rendezvous failure remains terminal, while a connection failure after an observed reconnectable predecessor close retries at 250 ms intervals within the existing 120-second successor-establishment hard bound. The timer begins at the observed outer-session loss, is cleared only after successor readiness and route rebinding, and a deadline or epoch-member failure remains terminal. No new state owner, protocol, service, configuration, or fallback was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-058.
- **Closure evidence:** the new bounded-policy regression passes; the complete NanoHost suite passes 80 unit plus 1 service test; mise-pinned fmt and Clippy pass. Exact rebuilt A1 NanoHost bytes passed the successor dial race and resumed the fixed poll cycle, where the separate NHC-FND-059 failure was then observed. The frozen Unit F verdict remains pending. Recorded disposition evidence: corrected locally. An initial rendezvous failure remains terminal, while a connection failure after an observed reconnectable predecessor close retries at 250 ms intervals within the existing 120-second successor-establishment hard bound. The timer begins at the observed outer-session loss, is cleared only after successor readiness and route rebinding, and a deadline or epoch-member failure remains terminal. No new state owner, protocol, service, configuration, or fallback was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-059 — A completed live bridge made a later empty poll loss destroy the Runtime Epoch

- **Observation:** after exact rebuilt A1 NanoHost bytes passed the successor dial race, the next F1 attempt reached the worker-ready barrier and killed NanoCore while the initial authoritative connection was performing a later ordinary `bridge.open` poll. The bridge and its Harness-lifetime monitor were already fully accepted, so NanoCore had no second bridge command to dispatch, but the poll owner classified every response loss on that path as new-command delivery uncertainty. The main loop then invoked the existing unknown-command cleanup, whose live-bridge guard correctly failed closed and destroyed the otherwise surviving Runtime Epoch.
- **Impact:** `apps/nanohost/src/nanocore_session.rs`; `apps/nanohost/src/epoch_coordinator.rs`; `apps/nanohost/src/main.rs`; the accepted live-bridge reconnect contract in `docs/specs/20260802-nanohost_runtime_and_transport.md`; NHC-5 F1; and root `[TEST-009]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally at the existing poll classifier. NanoHost now supplies the coordinator's already-owned live-bridge fact; loss of a later `bridge.open` poll response is reconnectable only when both the accepted bridge and Harness-lifetime monitor remain live. Before complete bridge acceptance, the same response loss remains terminal delivery uncertainty and retains the existing Sandbox deletion or epoch-fence path. No protocol, durable state, new effect, runner behavior, timeout, or fallback was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally at the existing poll classifier. NanoHost now supplies the coordinator's already-owned live-bridge fact; loss of a later `bridge.open` poll response is reconnectable only when both the accepted bridge and Harness-lifetime monitor remain live. Before complete bridge acceptance, the same response loss remains terminal delivery uncertainty and retains the existing Sandbox deletion or epoch-fence path. No protocol, durable state, new effect, runner behavior, timeout, or fallback was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-059.
- **Closure evidence:** the exact live-bridge response-loss regression passes; the complete NanoHost suite passes 81 unit plus 1 service test; app-local Mise-pinned Clippy and fmt pass; and `git diff --check` passes. Exact rebuilt A1 bytes later survived the F1 NanoCore restart, admitted the successor generation, and remained active through product-owned Turn cleanup. The aggregate Unit F verdict remains pending. Recorded disposition evidence: corrected locally at the existing poll classifier. NanoHost now supplies the coordinator's already-owned live-bridge fact; loss of a later `bridge.open` poll response is reconnectable only when both the accepted bridge and Harness-lifetime monitor remain live. Before complete bridge acceptance, the same response loss remains terminal delivery uncertainty and retains the existing Sandbox deletion or epoch-fence path. No protocol, durable state, new effect, runner behavior, timeout, or fallback was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-060 — Definite Sandbox deletion was rejected when its killed Harness monitor could not report clean Exit

- **Observation:** the next exact A1 F1 attempt created the Sandbox, imported all materialization inputs, accepted the fixed worker-entry marker, and opened `ForwardTcp`, but the private Harness first poll did not arrive before the bridge-establishment bound. Runner cleanup then deleted the Sandbox through the stock typed SDK; `DeleteSandbox` completed successfully and the immediate `GetSandbox` returned typed `NotFound`, proving terminal absence. The deleted Sandbox necessarily closed the long-lived unary Harness response without a clean Exit, and `settle_definite_sandbox_deletion` converted that already-fenced state into `MemberExited`, fail-stopping NanoHost and preventing exact Turn cleanup.
- **Impact:** the Harness-lifetime monitor and Sandbox-deletion fallback fence in `docs/specs/20260802-nanohost_runtime_and_transport.md`; `apps/nanohost/src/openshell_client.rs`; `apps/nanohost/src/epoch_coordinator.rs`; NHC-5 F1 cleanup; and root `[TEST-009]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally at the existing deletion settlement owner. Exact typed Sandbox absence remains mandatory; only after that proof, the coordinator aborts the now-evidence-only process-local response task and releases the retained Sandbox state without requiring the deleted worker to report a clean Exit. Before deletion, missing or unclean monitor completion remains unresolved and still requires Sandbox deletion; uncertain deletion still invalidates the Runtime Epoch. No new lifecycle state, timeout, poller, SDK wrapper, service, or compatibility path was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally at the existing deletion settlement owner. Exact typed Sandbox absence remains mandatory; only after that proof, the coordinator aborts the now-evidence-only process-local response task and releases the retained Sandbox state without requiring the deleted worker to report a clean Exit. Before deletion, missing or unclean monitor completion remains unresolved and still requires Sandbox deletion; uncertain deletion still invalidates the Runtime Epoch. No new lifecycle state, timeout, poller, SDK wrapper, service, or compatibility path was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-060.
- **Closure evidence:** the focused definite-deletion and shared-owner checks pass 2/2; the complete NanoHost suite passes 82 unit plus 1 service test; app-local Mise-pinned Clippy and fmt pass; and documentation lifecycle, model, index, and diff checks pass. Exact rebuilt A1 bytes later completed one product-owned backend teardown after the Turn was interrupted, without fail-stopping NanoHost. The aggregate Unit F verdict remains pending. Recorded disposition evidence: corrected locally at the existing deletion settlement owner. Exact typed Sandbox absence remains mandatory; only after that proof, the coordinator aborts the now-evidence-only process-local response task and releases the retained Sandbox state without requiring the deleted worker to report a clean Exit. Before deletion, missing or unclean monitor completion remains unresolved and still requires Sandbox deletion; uncertain deletion still invalidates the Runtime Epoch. No new lifecycle state, timeout, poller, SDK wrapper, service, or compatibility path was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-061 — Response-body resets escaped the existing worker-control recovery owner

- **Observation:** the next exact A1 F1 attempt used the admitted Codex subscription, reached the post-launch sequence barrier, restarted NanoCore, admitted one ready successor RuntimeTarget, and retained the last pre-restart worker heartbeat. The successor received neither an accepted request nor a quarantined rejection before the existing reconnect deadline, while NanoHost remained active and product-owned teardown completed. The earlier reset correction covered only failures before response headers. A focused real HTTP/2 regression then reproduced the remaining boundary: reset after a successful response header and partial body escaped `collectBoundedResponse` as an ordinary `Error`, which the existing worker-control retry contract correctly treats as permanent.
- **Impact:** `packages/worker-shim/src/integration-client.ts`; `packages/worker-shim/src/control-client.ts`; NHC-5 F1; and root `[TEST-009]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally at the existing Integration response collector. Response-body stream failures now use the same fetch transport-error shape as pre-header stream failures, so the existing bounded post-launch retry and reconnect-heartbeat owner handles both. The byte ceiling retains its existing `TypeError` and message. No timeout, retry policy, protocol, state, runner behavior, diagnostic owner, or fallback changed. Exact rebuilt worker bytes and the aggregate Unit F verdict remain pending. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally at the existing Integration response collector. Response-body stream failures now use the same fetch transport-error shape as pre-header stream failures, so the existing bounded post-launch retry and reconnect-heartbeat owner handles both. The byte ceiling retains its existing `TypeError` and message. No timeout, retry policy, protocol, state, runner behavior, diagnostic owner, or fallback changed. Exact rebuilt worker bytes and the aggregate Unit F verdict remain pending. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-061.
- **Closure evidence:** the new response-header-then-reset regression failed against the prior implementation with a native HTTP/2 `Error` and passes after the correction; the focused Integration suite passes 4/4. Recorded disposition evidence: corrected locally at the existing Integration response collector. Response-body stream failures now use the same fetch transport-error shape as pre-header stream failures, so the existing bounded post-launch retry and reconnect-heartbeat owner handles both. The byte ceiling retains its existing `TypeError` and message. No timeout, retry policy, protocol, state, runner behavior, diagnostic owner, or fallback changed. Exact rebuilt worker bytes and the aggregate Unit F verdict remain pending. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-062 — F1 injected restart before the active-task barrier

- **Observation:** the exact rebuilt-worker A1 attempt reached `worker.ready`, then F1 immediately restarted NanoCore while the initial inference call was still running. The first correction waited for a successful capability and no running capability, but the next exact A1 attempt proved that workspace materialization itself satisfies that predicate before any inference record exists. Requiring a linked successful inference then exposed the converse error: one real Turn recorded two successful LLM calls with usage, but its long-lived running `worker-checkpoint` capability kept the all-family predicate false until a later LLM failure terminated the worker. `worker.ready`, materialization, and unrelated runtime capability state do not decide the inference boundary.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; NHC-5 F1; the no-local-retry inference boundary in `docs/specs/20260629-worker_runtime_communication_model.md`; and root `[TEST-012]` and `[SCOPE-007]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally at the existing F1 post-launch barrier. F1 now requires an existing LLM usage record whose referenced `worker-inference-gateway` capability succeeded and no call in that exact LLM family remains running; runtime capabilities do not block the injection. It does not retry or replay inference and adds no timeout, owner, accessor, or instrument. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally at the existing F1 post-launch barrier. F1 now requires an existing LLM usage record whose referenced `worker-inference-gateway` capability succeeded and no call in that exact LLM family remains running; runtime capabilities do not block the injection. It does not retry or replay inference and adds no timeout, owner, accessor, or instrument. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-062.
- **Closure evidence:** the materialization-only intervention still proves no restart without inference, the in-flight LLM intervention proves no restart during inference, and the positive F1 fixture now retains a running runtime checkpoint across the restart. The runtime-checkpoint case failed against the prior predicate and passes after the correction; the complete Unit F runner suite passes 59/59. The exact A1 aggregate verdict remains pending. Recorded disposition evidence: corrected locally at the existing F1 post-launch barrier. F1 now requires an existing LLM usage record whose referenced `worker-inference-gateway` capability succeeded and no call in that exact LLM family remains running; runtime capabilities do not block the injection. It does not retry or replay inference and adds no timeout, owner, accessor, or instrument. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-063 — Successor binding left an in-flight predecessor Harness poll alive

- **Observation:** the exact committed-image A1 F1 attempt reached one linked successful LLM call with usage, one `worker.ready`, one continuous worker event sequence, and one ready successor RuntimeTarget after NanoCore restart. Ordinary worker-control heartbeats continued through the successor, but the product-owned `turn.interrupt` remained queued at the private Harness's exact next sequence and was never dispatched. The surviving Harness poll had captured the predecessor outer sender before NanoCore stopped; replacing the projection target changed only later requests and supplied no cancellation to that already-running forward, so the single-threaded Harness command loop never issued another poll.
- **Impact:** `apps/nanohost/src/nanocore_session.rs`; `packages/worker-shim/src/harness.ts`; the live-bridge reconnect contract in `docs/specs/20260802-nanohost_runtime_and_transport.md`; NHC-5 F1; and root `[TEST-009]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally at the existing `OuterRouteProjection` owner. Every bound target now carries one process-local fence; binding an admitted successor fences the predecessor, and an in-flight forward selects that fence against all old-target request and response work. Dropping the nested response makes the worker's existing route-specific policy decide retry or failure. No protocol, durable state, timeout, runner behavior, retry owner, service, configuration, or fallback was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally at the existing `OuterRouteProjection` owner. Every bound target now carries one process-local fence; binding an admitted successor fences the predecessor, and an in-flight forward selects that fence against all old-target request and response work. Dropping the nested response makes the worker's existing route-specific policy decide retry or failure. No protocol, durable state, timeout, runner behavior, retry owner, service, configuration, or fallback was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-063.
- **Closure evidence:** a focused two-layer HTTP/2 regression first timed out because successor binding left the predecessor route live and passes after the correction. The complete NanoHost suite passes 83 unit plus 1 service test, and app-local Mise-pinned Clippy passes with warnings denied. Exact rebuilt A1 NanoHost bytes and the aggregate Unit F verdict remain pending. Recorded disposition evidence: corrected locally at the existing `OuterRouteProjection` owner. Every bound target now carries one process-local fence; binding an admitted successor fences the predecessor, and an in-flight forward selects that fence against all old-target request and response work. Dropping the nested response makes the worker's existing route-specific policy decide retry or failure. No protocol, durable state, timeout, runner behavior, retry owner, service, configuration, or fallback was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-064 — F1 required an unobservable exact first successor heartbeat

- **Observation:** the exact rebuilt A1 F1 attempt admitted the successor RuntimeTarget and retained one continuous worker sequence, but the observation loop required both the lease heartbeat and latest worker event to equal the predecessor value plus exactly one. Real heartbeats advanced again between owner reads, so once either value exceeded that equality the predicate could never become true and the runner expired despite monotonic same-owner progress.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; NHC-5 F1; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally at the existing F1 barrier and adjudicator. The runner accepts one or more monotonic successor events and heartbeat progress while retaining exact prefix, gap-free sequence, owner identity, generation, capacity, projection, and final-suffix checks. No timeout, accessor, fixture owner, product behavior, or additional observation was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally at the existing F1 barrier and adjudicator. The runner accepts one or more monotonic successor events and heartbeat progress while retaining exact prefix, gap-free sequence, owner identity, generation, capacity, projection, and final-suffix checks. No timeout, accessor, fixture owner, product behavior, or additional observation was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-064.
- **Closure evidence:** the new batched-progress regression failed against the exact-plus-one predicate and passes after the correction; the complete Unit F runner suite passes 60/60. The exact A1 F1 and aggregate verdict remain pending. Recorded disposition evidence: corrected locally at the existing F1 barrier and adjudicator. The runner accepts one or more monotonic successor events and heartbeat progress while retaining exact prefix, gap-free sequence, owner identity, generation, capacity, projection, and final-suffix checks. No timeout, accessor, fixture owner, product behavior, or additional observation was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-065 — A pre-response nested H2 close disabled the Harness outage deadline

- **Observation:** after successor binding fenced the predecessor route, the Sandbox Integration stream closed before response headers. `responseForStream` removed its abort listener on every close but rejected only `error`, so a close without an error left the request Promise pending forever and disabled the existing one-second request deadline. The Harness therefore never retried its immutable poll or result, and a product-owned `turn.interrupt` remained queued at the exact next sequence.
- **Impact:** `packages/worker-shim/src/integration-client.ts`; `packages/worker-shim/src/harness.ts`; NHC-5 F1; the live-bridge reconnect contract in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-009]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally at the existing Integration response owner. A stream close before response headers now rejects as the same fetch transport-error shape already consumed by the bounded Harness outage retry loop; normal response closes and post-header body handling are unchanged. No retry, timeout, protocol, state, service, configuration, or fallback was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally at the existing Integration response owner. A stream close before response headers now rejects as the same fetch transport-error shape already consumed by the bounded Harness outage retry loop; normal response closes and post-header body handling are unchanged. No retry, timeout, protocol, state, service, configuration, or fallback was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-065.
- **Closure evidence:** the real H2 close-before-headers regression remained pending against the prior implementation and passes after the correction; the complete Worker Shim suite passes 181/181, with typecheck, lint, and `git diff --check` passing. Exact rebuilt worker bytes and the aggregate Unit F verdict remain pending. Recorded disposition evidence: corrected locally at the existing Integration response owner. A stream close before response headers now rejects as the same fetch transport-error shape already consumed by the bounded Harness outage retry loop; normal response closes and post-header body handling are unchanged. No retry, timeout, protocol, state, service, configuration, or fallback was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-066 — The A1 restart recovery exceeded the Unit F owner observation deadline

- **Observation:** the exact committed NanoHost and Worker A1 F1 attempt reached the linked successful-inference barrier, restarted NanoCore, and left the container running through its authoritative checkpoint recovery scan, but the Core port remained unavailable for more than 120 seconds. F1 therefore expired while waiting for the successor RuntimeTarget, and its cleanup expired against the same unavailable owner; NanoCore later became ready after about 167 seconds, NanoHost admitted the successor generation, and exact worker-control events resumed.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; NHC-5 F1; the existing five-minute Harness outage envelope; and root `[EVID-001]`, `[TEST-012]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by changing only the existing owner-observation default from 120 seconds to 300 seconds. Explicit shorter deadlines, command deadlines, retry policy, product behavior, owner accessors, and runner structure are unchanged. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by changing only the existing owner-observation default from 120 seconds to 300 seconds. Explicit shorter deadlines, command deadlines, retry policy, product behavior, owner accessors, and runner structure are unchanged. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-066.
- **Closure evidence:** the prior exact A1 run directly proved that 120 seconds expired before the current host completed NanoCore startup and that owner recovery resumed afterward. The corrected exact F1 rerun and aggregate Unit F verdict remain pending. Recorded disposition evidence: corrected locally by changing only the existing owner-observation default from 120 seconds to 300 seconds. Explicit shorter deadlines, command deadlines, retry policy, product behavior, owner accessors, and runner structure are unchanged. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-067 — F1 rejected a terminal event batched into the first successor observation

- **Observation:** the corrected exact A1 F1 run crossed the successor transport, accepted a monotonic final status, completed physical cleanup and teardown, and restored fresh empty capacity, but the continuation oracle required the final-status sequence to be strictly later than the last sequence already visible in the first successor snapshot. A terminal event may validly arrive in that same first post-restart snapshot, so the oracle rejected legal batching even though no sequence replay, gap, or regression occurred.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F1; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by allowing equality between the accepted final-status sequence and the last sequence in the adopted snapshot while retaining the existing rejection of sequence regression and every lineage, projection, cleanup, capacity, and transport predicate. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by allowing equality between the accepted final-status sequence and the last sequence in the adopted snapshot while retaining the existing rejection of sequence regression and every lineage, projection, cleanup, capacity, and transport predicate. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-067.
- **Closure evidence:** the focused regression failed against the prior comparison and the three focused F1 continuation acceptance checks pass after the one-character correction. The corrected exact F1 rerun and aggregate Unit F verdict remain pending. Recorded disposition evidence: corrected locally by allowing equality between the accepted final-status sequence and the last sequence in the adopted snapshot while retaining the existing rejection of sequence regression and every lineage, projection, cleanup, capacity, and transport predicate. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-068 — F1 treated ordinary Sandbox descendants as immutable Runtime Epoch identity

- **Observation:** the diagnostic rerun proved every F1 continuation predicate except epoch stability, while generation, sequence, owner projection, final status, teardown, capacity, and target recovery all passed. The oracle captured the service cgroup before Task admission and again while the admitted Sandbox was active, then required every descendant process to remain byte-identical even though the owning specification explicitly permits Sandbox create and delete as ordinary mutable epoch-membership operations.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; the Runtime Epoch update contract in `docs/specs/20260802-nanohost_runtime_and_transport.md`; NHC-5 F1; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by retaining the unchanged boot and service invocation identities and requiring every pre-admission cgroup member to keep its exact process identity, while allowing only additional post-admission descendants. Loss or replacement of any baseline epoch member still fails closed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by retaining the unchanged boot and service invocation identities and requiring every pre-admission cgroup member to keep its exact process identity, while allowing only additional post-admission descendants. Loss or replacement of any baseline epoch member still fails closed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-068.
- **Closure evidence:** the focused regression accepts one added Sandbox descendant under unchanged epoch owners, and the four focused F1 continuation acceptance checks pass. The corrected exact F1 rerun and aggregate Unit F verdict remain pending. Recorded disposition evidence: corrected locally by retaining the unchanged boot and service invocation identities and requiring every pre-admission cgroup member to keep its exact process identity, while allowing only additional post-admission descendants. Loss or replacement of any baseline epoch member still fails closed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-069 — F2 waited for a Gateway request log that the pinned server cannot emit

- **Observation:** the exact A1 aggregate run passed F1 and entered F2, but stopped without dispatching the NanoHost fault. The barrier first required a journal message containing `POST /openshell.v1.OpenShell/CreateSandbox`, while the pinned Gateway disables the request-start callback with `on_request(())`; no code in the pinned source emits that message. The same request span does emit `minted sandbox JWT` with the inherited request path and request ID plus the allocated Sandbox ID immediately before entering the deliberately frozen Docker driver call. The first correction reached that event, but the Gateway's text formatter stores the span fields inside an ANSI-decorated `MESSAGE`; the fallback parser first accepted the escaped control prefix as both identities, and the next exact rerun proved that `journalctl -o json` projects the non-printable message as a byte array rather than a string, so string-only normalization found no mint event before the stock create timeout.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F2 through F4; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by removing only the impossible request-start message requirement, decoding systemd's byte-array `MESSAGE` with Node's `Buffer`, and applying the existing `stripVTControlCharacters` before extracting the two identities. The barrier still requires one uniquely correlated mint event, exact request and Sandbox identities, the stopped Docker member, stable Runtime Epoch and RuntimeTarget owners, no completion or response, no reconnect or fence, and a second unchanged observation before fault delivery. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by removing only the impossible request-start message requirement, decoding systemd's byte-array `MESSAGE` with Node's `Buffer`, and applying the existing `stripVTControlCharacters` before extracting the two identities. The barrier still requires one uniquely correlated mint event, exact request and Sandbox identities, the stopped Docker member, stable Runtime Epoch and RuntimeTarget owners, no completion or response, no reconnect or fence, and a second unchanged observation before fault delivery. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-069.
- **Closure evidence:** removing the fixture's invented POST entry made all three blocked-create sequence checks fail before fault delivery. The next exact A1 runs reached the real mint event and exposed first the false escaped-prefix correlation and then the real byte-array journal projection before fault delivery. The focused regression now uses that byte-array ANSI shape and requires hashes of both real values; it fails against the string-only normalizer and passes after standard-library decoding and normalization. The corrected exact A1 aggregate verdict remains pending. Recorded disposition evidence: corrected locally by removing only the impossible request-start message requirement, decoding systemd's byte-array `MESSAGE` with Node's `Buffer`, and applying the existing `stripVTControlCharacters` before extracting the two identities. The barrier still requires one uniquely correlated mint event, exact request and Sandbox identities, the stopped Docker member, stable Runtime Epoch and RuntimeTarget owners, no completion or response, no reconnect or fence, and a second unchanged observation before fault delivery. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-070 — F2 recovery treated inert socket pathnames as live effects

- **Observation:** the exact byte-array-corrected A1 run passed F1, observed the real F2 mint barrier, delivered the NanoHost fault, and reached the expected first recovery `sandbox.create` poll `409`. The first-fence cleanup then waited until its owner deadline because four stale private socket pathnames remained under two inert runtime roots even though the service cgroup was empty and `/proc/net/unix` reported zero live endpoints for those paths. Successor startup already owns removal of classified prior roots, and the runner separately proves that removal after fresh readiness, so requiring pathname absence before starting the successor created a circular oracle.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; NHC-5 F2 through F4; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by checking the two exact runtime paths against the kernel's live UNIX endpoint table while retaining the old private-network-namespace holder proof and the existing post-successor prior-root removal proof. No product behavior, cleanup owner, timeout, runner phase, dependency, or durable state changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by checking the two exact runtime paths against the kernel's live UNIX endpoint table while retaining the old private-network-namespace holder proof and the existing post-successor prior-root removal proof. No product behavior, cleanup owner, timeout, runner phase, dependency, or durable state changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-070.
- **Closure evidence:** the direct A1 probe observed four existing pathnames and zero live endpoints after the first-fence service exited, and the focused runner suite passes 63/63 with targeted Biome and documentation checks passing. The corrected exact A1 aggregate verdict remains pending. Recorded disposition evidence: corrected locally by checking the two exact runtime paths against the kernel's live UNIX endpoint table while retaining the old private-network-namespace holder proof and the existing post-successor prior-root removal proof. No product behavior, cleanup owner, timeout, runner phase, dependency, or durable state changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-071 — Physical close erased the later fresh-ready cleanup proof

- **Observation:** after the corrected F2 fault and first-fence recovery, two strictly increasing fresh coordinators each completed readiness and then received the same first-poll `409`. NanoCore maintenance repeatedly reported `NanoHost unknown cleanup fence has no later fresh-ready proof`. The RuntimeTarget owner stored readiness only in the current `ready`, `fresh_empty`, and `observed_at` projection; the immediate poll rejection closed the connection and overwrote all three before periodic maintenance could consume the proof, so the once-only unknown fence repeated indefinitely.
- **Impact:** `apps/nanocore/src/runtime/nanohost-runtime-target.ts`; `apps/nanocore/src/runtime/turn-executor-factory.ts`; the matching Core migration and schema; NHC-5 F2 through F4; and the later-fresh-coordinator rule in `docs/specs/20260802-nanohost_runtime_and_transport.md`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally within the existing RuntimeTarget owner by retaining one nullable `last_fresh_ready_at` timestamp that advances only on accepted fresh-empty readiness and survives allocation and physical close. Current readiness still becomes false on close, while unknown cleanup requires the exact target and deployment plus a retained readiness timestamp strictly later than its fence. No new table, owner, callback, retry, timeout, or effect was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally within the existing RuntimeTarget owner by retaining one nullable `last_fresh_ready_at` timestamp that advances only on accepted fresh-empty readiness and survives allocation and physical close. Current readiness still becomes false on close, while unknown cleanup requires the exact target and deployment plus a retained readiness timestamp strictly later than its fence. No new table, owner, callback, retry, timeout, or effect was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-071.
- **Closure evidence:** the focused scheduler restart regression now follows the real `unknown fence → successor allocation → fresh readiness → immediate physical close → maintenance` order and fails against the prior current-only projection with the exact A1 error. The corrected scheduler recovery, RuntimeTarget, and migration suites pass 64/64; the full NanoCore suite passes 227 files and 2570 tests with one file and four tests skipped; NanoCore lint, typecheck, and build plus the specification lifecycle, documentation model, generated index, 65 documentation tests, and diff checks pass. Exact A1 aggregate evidence remains pending. Recorded disposition evidence: corrected locally within the existing RuntimeTarget owner by retaining one nullable `last_fresh_ready_at` timestamp that advances only on accepted fresh-empty readiness and survives allocation and physical close. Current readiness still becomes false on close, while unknown cleanup requires the exact target and deployment plus a retained readiness timestamp strictly later than its fence. No new table, owner, callback, retry, timeout, or effect was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-072 — The Gateway mint record did not observe the blocked create

- **Observation:** the exact A1 F2 rerun reached one backend, lease, Turn, checkpoint, and successful teardown terminal tuple but never delivered the NanoHost `SIGKILL`. Its current stock Gateway journal contained one startup `Minted sandbox JWT` record with only `gateway_id` and `ttl_secs`; it contained no CreateSandbox route, request identity, or Sandbox identity, so the NHC-FND-069 fixture described fields the real subject did not emit and the blocked-create oracle expired before fault delivery.
- **Impact:** `apps/nanohost/src/main.rs`; `docs/specs/20260802-nanohost_runtime_and_transport.md`; `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F2 through F4; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally at the existing NanoHost effect-dispatch seam with one value-free journal marker after fixed poll acceptance and immediately before the OpenShell lifecycle owner. The runner now observes that exact NanoHost process record through its existing cursor while retaining the stopped Docker member, stable Runtime Epoch and RuntimeTarget, exact live owner tuple, and unchanged second observation. The obsolete Gateway field parser and its invented fixture data were deleted; no API, table, durable state, timeout, script, or raw identifier was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally at the existing NanoHost effect-dispatch seam with one value-free journal marker after fixed poll acceptance and immediately before the OpenShell lifecycle owner. The runner now observes that exact NanoHost process record through its existing cursor while retaining the stopped Docker member, stable Runtime Epoch and RuntimeTarget, exact live owner tuple, and unchanged second observation. The obsolete Gateway field parser and its invented fixture data were deleted; no API, table, durable state, timeout, script, or raw identifier was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-072.
- **Closure evidence:** the real Gateway-only fixture failed before the correction, the focused F2 sequence passes with the subject-owned marker, the complete runner suite passes 64/64, and NanoHost passes 83 unit plus 1 service test, fmt, and Clippy with warnings denied. Exact rebuilt A1 F2 and aggregate evidence remain pending. Recorded disposition evidence: corrected locally at the existing NanoHost effect-dispatch seam with one value-free journal marker after fixed poll acceptance and immediately before the OpenShell lifecycle owner. The runner now observes that exact NanoHost process record through its existing cursor while retaining the stopped Docker member, stable Runtime Epoch and RuntimeTarget, exact live owner tuple, and unchanged second observation. The obsolete Gateway field parser and its invented fixture data were deleted; no API, table, durable state, timeout, script, or raw identifier was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-073 — The journal PID filter discarded NanoHost thread output

- **Observation:** the exact rebuilt A1 F2 run emitted one accepted `sandbox.create` marker and kept the create blocked for about 120 seconds, but the runner delivered no `SIGKILL` and expired at its owner-observation deadline. The marker and the later value-free terminal failure carried the same journald `_PID`, `_COMM=nanohost`, executable, unit, and invocation metadata, but journald attributed the stdout transport record to the emitting Tokio thread rather than systemd's NanoHost main-process TGID. Filtering `journalctl` by that TGID therefore discarded the deciding marker.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F2 through F4; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by retaining the exact systemd invocation filter, removing only the invalid TGID journal filter, and accepting the marker only when journald also binds it to the exact NanoHost executable and service unit. No timeout, parser, owner, diagnostic reader, product behavior, or durable state was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by retaining the exact systemd invocation filter, removing only the invalid TGID journal filter, and accepting the marker only when journald also binds it to the exact NanoHost executable and service unit. No timeout, parser, owner, diagnostic reader, product behavior, or durable state was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-073.
- **Closure evidence:** A1 exposed one exact NanoHost marker whose `_PID` differed from the retained service TGID while its executable, unit, invocation, and closed message all matched. The focused F2 fixture now reproduces that thread attribution beside a same-message Gateway record and passes only when the NanoHost provenance is selected. Exact rebuilt A1 F2 and aggregate evidence remain pending. Recorded disposition evidence: corrected locally by retaining the exact systemd invocation filter, removing only the invalid TGID journal filter, and accepting the marker only when journald also binds it to the exact NanoHost executable and service unit. No timeout, parser, owner, diagnostic reader, product behavior, or durable state was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-074 — The blocked-create fixture hashed a field absent from Task lineage

- **Observation:** the exact rebuilt A1 F2 run reached the subject-owned accepted-effect marker but never delivered the NanoHost fault. One bounded barrier diagnostic observed a `TypeError` while the create remained live, followed only later by the expected NanoHost disappearance at the stock create timeout. The accepted Task AEP lineage returned by `waitForTaskLineage()` contains request, Workspace, Thread, Turn, Agent Session, and package-snapshot identities but no backend-session identity; `inspectNanoHostBlockedCreateObservation()` nevertheless hashed `fixture.lineage.backendSessionId`. The focused fixture had added that non-contract field and therefore hid the real failure.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F2 through F4; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by carrying the backend-session identity already accepted from the existing owner tuple as a separate private fixture field and hashing that field at the barrier. The Task lineage contract, owner accessors, timeout, runner phases, product behavior, and durable state remain unchanged. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by carrying the backend-session identity already accepted from the existing owner tuple as a separate private fixture field and hashing that field at the barrier. The Task lineage contract, owner accessors, timeout, runner phases, product behavior, and durable state remain unchanged. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-074.
- **Closure evidence:** the blocked-create sequence fixture now matches the real Task-lineage shape by omitting backend-session identity and derives the expected sandbox proof from the accepted backend owner instead. The corrected exact A1 F2 and aggregate Unit F verdict remain pending. Recorded disposition evidence: corrected locally by carrying the backend-session identity already accepted from the existing owner tuple as a separate private fixture field and hashing that field at the barrier. The Task lineage contract, owner accessors, timeout, runner phases, product behavior, and durable state remain unchanged. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-075 — A tunnel timing assertion is nondeterministic in the development image

- **Observation:** the complete 64-test Unit F runner passed on the host, while the same unchanged suite in the fixed development image passed 63 tests and failed `tunnel stop waits for the exact SIGKILLed child close`. The fixture schedules its synthetic close 100 milliseconds after `SIGKILL`, waits for that signal through the runner's 250-millisecond polling helper, and then asserts that the close has not yet settled; the close can therefore correctly occur before the assertion is reached.
- **Impact:** `tests/nanohost-unit-f-runner.test.mjs`; the runner's local instrument confidence; and root `[TEST-002]`, `[CHECK-019]`, and `[SCOPE-012]`. It does not bear on the F2 backend-owner correction or the real A1 subject.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: retained as a non-decisive Finding without changing the frozen runner or expanding its timing fixture. A real Unit F scenario failure may reopen it only if tunnel cleanup becomes the deciding subject. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: retained as a non-decisive Finding without changing the frozen runner or expanding its timing fixture. A real Unit F scenario failure may reopen it only if tunnel cleanup becomes the deciding subject. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-075.
- **Closure evidence:** the same host suite passed 64/64, and the fixed-image focused F2, F3, and F4 blocked-create sequence checks passed 3/3 after the owner correction. Recorded disposition evidence: retained as a non-decisive Finding without changing the frozen runner or expanding its timing fixture. A real Unit F scenario failure may reopen it only if tunnel cleanup becomes the deciding subject. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-076 — Fault recovery accepted readiness before the deciding first poll

- **Observation:** the owner-corrected exact A1 F2 run delivered the NanoHost fault and later emitted the expected first-recovery `sandbox.create` poll `409`, but the runner expired at its owner deadline. The latest accepted owner snapshot then showed the exact Turn failed, its sole lease failed, its sole backend physically cleaned, exactly one teardown record, and the RuntimeTarget non-ready and non-empty. `recoverFreshEpochSequence()` returned success as soon as the first recovery connection projected fresh readiness, before that connection's deciding first poll could expose the unknown cleanup fence; it therefore skipped the first-fence path and never started the required post-cleanup fresh successor.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F2 through F4; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by making the existing fault lineage select the already-modeled first-fence path. A blocked-create recovery now observes the first-poll `409`, reuses the existing exact-lineage fault cleanup proof, and only then starts its sole fresh successor. Ordinary lifecycle recovery retains its direct fresh-ready path; no timeout, poll, owner, durable state, or product behavior changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by making the existing fault lineage select the already-modeled first-fence path. A blocked-create recovery now observes the first-poll `409`, reuses the existing exact-lineage fault cleanup proof, and only then starts its sole fresh successor. Ordinary lifecycle recovery retains its direct fresh-ready path; no timeout, poll, owner, durable state, or product behavior changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-076.
- **Closure evidence:** the blocked-create fixture now reproduces the real transient readiness immediately before first-poll closure and requires exactly two recovery starts. The prior implementation accepts the transient first start; the correction waits for its `409`, owner cleanup, and the second fresh successor. Exact corrected A1 F2 and aggregate evidence remain pending. Recorded disposition evidence: corrected locally by making the existing fault lineage select the already-modeled first-fence path. A blocked-create recovery now observes the first-poll `409`, reuses the existing exact-lineage fault cleanup proof, and only then starts its sole fresh successor. Ordinary lifecycle recovery retains its direct fresh-ready path; no timeout, poll, owner, durable state, or product behavior changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-077 — The first-fence capacity gate also required the Turn to be terminal

- **Observation:** the first-fence-corrected exact A1 F2 run again expired while waiting for owner observation. The product API then proved the latest exact F2 Turn remained `running`. The runner had reused `proveSequencedFaultTurnFenced()` before starting the final fresh successor; that predicate requires a terminal Turn even though the existing sequence deliberately authorizes `interruptTurn()` only after that successor, creating a circular gate around an otherwise independent backend-capacity release.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F2 through F4; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by factoring the existing backend, lease, physical-cleanup, and single-teardown predicate from the stricter terminal-Turn proof. First-fence recovery waits only for that exact capacity release before starting the final successor; the existing post-successor interrupt and full terminal cleanup proof remain unchanged. No timeout, lifecycle state, owner, effect, API, or product behavior changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by factoring the existing backend, lease, physical-cleanup, and single-teardown predicate from the stricter terminal-Turn proof. First-fence recovery waits only for that exact capacity release before starting the final successor; the existing post-successor interrupt and full terminal cleanup proof remain unchanged. No timeout, lifecycle state, owner, effect, API, or product behavior changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-077.
- **Closure evidence:** the blocked-create fixture now retains a `running` Turn after first-fence capacity release and still requires the final successor to start before `interruptTurn()`. The stricter pre-successor predicate fails that real shape; the separated capacity predicate admits it without weakening final cleanup. Exact corrected A1 F2 and aggregate evidence remain pending. Recorded disposition evidence: corrected locally by factoring the existing backend, lease, physical-cleanup, and single-teardown predicate from the stricter terminal-Turn proof. First-fence recovery waits only for that exact capacity release before starting the final successor; the existing post-successor interrupt and full terminal cleanup proof remain unchanged. No timeout, lifecycle state, owner, effect, API, or product behavior changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-078 — Capacity release was awaited before its required later readiness proof

- **Observation:** the capacity-separated exact A1 F2 run still expired before starting its final successor. Immediate accepted-owner inspection showed the Turn `running`, its lease `acquired`, its backend `cleanup-failed`, no physical-cleanup timestamp, and zero teardown records. The first recovery connection's poll created the unknown fence only after that connection had reported readiness, so its readiness could not be the specification-required later proof; the runner then waited for capacity release before starting the next coordinator whose higher-generation readiness was the only fact allowed to release it.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F2 through F4; the post-fence fresh-ready cleanup contract in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by starting the existing proof successor immediately after the first-poll fence, then waiting for its higher-generation readiness to release the existing capacity owner. The current NanoHost immediately polls again, so the runner proves that proof successor's exact second `409` fail-stop before starting one exact next final successor. The retained generation proof requires exactly two generations after the first fence. No sleep, timeout, owner, state, route, effect, or product behavior changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by starting the existing proof successor immediately after the first-poll fence, then waiting for its higher-generation readiness to release the existing capacity owner. The current NanoHost immediately polls again, so the runner proves that proof successor's exact second `409` fail-stop before starting one exact next final successor. The retained generation proof requires exactly two generations after the first fence. No sleep, timeout, owner, state, route, effect, or product behavior changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-078.
- **Closure evidence:** every blocked-create fixture now reproduces both terminal polls, delayed capacity release, the proof successor's complete fail-stop, and the final third recovery start at the exact next generation. Exact corrected A1 F2 and aggregate evidence remain pending. Recorded disposition evidence: corrected locally by starting the existing proof successor immediately after the first-poll fence, then waiting for its higher-generation readiness to release the existing capacity owner. The current NanoHost immediately polls again, so the runner proves that proof successor's exact second `409` fail-stop before starting one exact next final successor. The retained generation proof requires exactly two generations after the first fence. No sleep, timeout, owner, state, route, effect, or product behavior changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-079 — The proof successor was required to be both ready and fail-stopped

- **Observation:** the exact A1 F2 rerun reached the second terminal `sandbox.create` poll `409` but expired at indexed wait 15. Static reconciliation mapped that wait to `completeNanoHostFirstFenceRecovery()` awaiting the proof successor's current RuntimeTarget as fresh-empty and ready, while the immediately following contract required the same successor to be non-ready, empty of members, and terminated by exactly one `409`. The local fixture hid the contradiction by projecting generation 9 ready until an unrelated owner read toggled its failure.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F2 through F4; the post-fence fresh-ready cleanup contract in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by making the existing first-fence successor observation select the exact non-ready, non-empty RuntimeTarget for fault lineages while retaining fresh-empty readiness for ordinary recovery. The later capacity proof and exact next final successor remain unchanged; no timeout, retry, owner, state, effect, or product behavior changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by making the existing first-fence successor observation select the exact non-ready, non-empty RuntimeTarget for fault lineages while retaining fresh-empty readiness for ordinary recovery. The later capacity proof and exact next final successor remain unchanged; no timeout, retry, owner, state, effect, or product behavior changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-079.
- **Closure evidence:** the blocked-create fixture now projects the proof successor's real non-ready state as soon as its second poll fails. That fixture failed against the prior ready predicate, and the focused F2, F3, and F4 sequence checks pass after the two-condition correction. Exact corrected A1 F2 and aggregate evidence remain pending. Recorded disposition evidence: corrected locally by making the existing first-fence successor observation select the exact non-ready, non-empty RuntimeTarget for fault lineages while retaining fresh-empty readiness for ordinary recovery. The later capacity proof and exact next final successor remain unchanged; no timeout, retry, owner, state, effect, or product behavior changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-080 — The proof successor retained a partial startup snapshot

- **Observation:** the NHC-FND-079-corrected exact A1 F2 run passed both terminal `sandbox.create` polls and completed the exact backend, lease, teardown, and Turn cleanup, but started no final successor before another owner-observation deadline. `startSuccessor()` retained one immediate cgroup read after `systemctl start`; after the non-ready RuntimeTarget predicate became valid, the later epoch-effect proof required that frozen read to contain the Gateway netns. A startup read containing only early members could therefore never satisfy the later accessor even after the complete proof epoch had terminated, and the polling wrapper reduced the missing-Gateway error to its generic deadline.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F2 through F4; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by waiting on the existing cgroup owner until the distinct active proof invocation contains its exact Gateway and Docker members before retaining the epoch snapshot. The existing second-poll, capacity, effect-absence, journal, and final-successor predicates remain unchanged; no timeout, retry, helper, owner, state, effect, or product behavior changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by waiting on the existing cgroup owner until the distinct active proof invocation contains its exact Gateway and Docker members before retaining the epoch snapshot. The existing second-poll, capacity, effect-absence, journal, and final-successor predicates remain unchanged; no timeout, retry, helper, owner, state, effect, or product behavior changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-080.
- **Closure evidence:** the blocked-create fixture now returns a NanoHost-only first read for the proof successor and rejects effect inspection of that partial epoch. It fails against the immediate snapshot and the focused F2, F3, and F4 sequence checks pass after the bounded complete-member observation. Exact corrected A1 F2 and aggregate evidence remain pending. Recorded disposition evidence: corrected locally by waiting on the existing cgroup owner until the distinct active proof invocation contains its exact Gateway and Docker members before retaining the epoch snapshot. The existing second-poll, capacity, effect-absence, journal, and final-successor predicates remain unchanged; no timeout, retry, helper, owner, state, effect, or product behavior changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-081 — The system-Docker smoke reused stdin and an unresolvable image identity

- **Observation:** the complete-epoch-corrected exact A1 F2 run started the original epoch, both expected `409` proof epochs, and the final successor, then failed only when the existing system-Docker DNS smoke returned command status 1. Its command passed stdin as both `-f -` and the `-` build context, which A1 rejected as an invalid Dockerfile/context request. Removing that conflict reached BuildKit, but `FROM` still used the container's local config digest as if it were a resolvable image reference, so source metadata resolution failed before the DNS command ran.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; NHC-5 F2, F4, and Aggregate; the system-Docker non-interference predicate; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by using Docker's native empty-context stdin form and the already-admitted NanoCore image ref. The existing container image-ID check remains, and the aggregate baseline independently binds that ref to the running container bytes. No script, file, image, dependency, timeout, owner, or product behavior was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by using Docker's native empty-context stdin form and the already-admitted NanoCore image ref. The existing container image-ID check remains, and the aggregate baseline independently binds that ref to the running container bytes. No script, file, image, dependency, timeout, owner, or product behavior was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-081.
- **Closure evidence:** the original exact A1 command failed before DNS, the empty-context command with the local config digest failed source resolution, and the same empty-context command with the existing admitted image ref completed the DNS build in one second. Exact corrected A1 F2 and aggregate evidence remain pending. Recorded disposition evidence: corrected locally by using Docker's native empty-context stdin form and the already-admitted NanoCore image ref. The existing container image-ID check remains, and the aggregate baseline independently binds that ref to the running container bytes. No script, file, image, dependency, timeout, owner, or product behavior was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-082 — F1 exhausted the bounded successor window on A1

- **Observation:** the exact aggregate attempt completed one linked successful F1 inference, restarted NanoCore, and then failed before F2 after the NanoHost exhausted its 300-second successor connection window. The product API became healthy shortly afterward, but the Turn remained `running`, the product interrupt route returned `404`, and the runner's cleanup fresh NanoHost start also terminated at the value-free initial `connect` stage. The rendezvous health endpoint returned HTTP/2 `200`; the declared credential slot satisfied the current selector, matched the sole active redacted Token record, and passed the existing NanoCore Token verifier.
- **Impact:** NHC-5 F1; the bounded reconnect and visible-failure contracts in `docs/specs/20260802-nanohost_runtime_and_transport.md` and `docs/specs/20260704-agent_session_continuity.md`; and root `[EVID-001]`, `[SCOPE-007]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved by the hot-journal correction below and NHC-FND-084's removal of the duplicate post-boot Workspace scan. Neither the product successor bound nor the owner-observation bound changed because the accepted specification defines bounded recovery and visible failure rather than transparent availability. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved by the hot-journal correction below and NHC-FND-084's removal of the duplicate post-boot Workspace scan. Neither the product successor bound nor the owner-observation bound changed because the accepted specification defines bounded recovery and visible failure rather than transparent availability. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-082.
- **Closure evidence:** exact A1 product readback located only F1 work, one still-running Turn, one succeeded LLM capability with linked usage, no teardown evidence, a failed NanoHost service, and bounded terminal connect classifications without retaining raw runtime or credential values. Exact rebuilt `b592afab` then opened its listener at 233.067 seconds, stayed within the unchanged successor bound, and advanced the frozen Aggregate through F1 and F2; the latest F1 Turn was `interrupted` with one succeeded teardown record. Recorded disposition evidence: resolved by the hot-journal correction below and NHC-FND-084's removal of the duplicate post-boot Workspace scan. Neither the product successor bound nor the owner-observation bound changed because the accepted specification defines bounded recovery and visible failure rather than transparent availability. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-083 — Read-only integrity checking rejected a valid hot rollback journal

- **Observation:** the product-owned recovery restart exited during the critical migrations phase with `SQLite integrity check failed` and `attempt to write a readonly database`. The exact failing file was the Core database on a writable bind mount with normal ownership, mode, capacity, and attributes; it used rollback-journal mode and retained a hot journal created by the abrupt restart. `checkSqliteIntegrity()` opened every existing authoritative database read-only, preventing SQLite from completing its own atomic rollback before `PRAGMA quick_check`.
- **Impact:** `apps/nanocore/src/storage/db.ts`; `apps/nanocore/src/storage/db.test.ts`; NanoCore crash recovery; NHC-5 F1; and root `[TEST-009]`, `[QUALITY-003]`, and the Data Loss Safety Kernel.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally in the existing storage owner by opening the existing file normally with `fileMustExist` before the unchanged quick check. This permits only SQLite's native crash recovery; the existing corrupt-database checks still require failure without byte replacement, quarantine, or inferred repair. No schema, migration, database owner, fallback, compatibility path, or runner behavior changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally in the existing storage owner by opening the existing file normally with `fileMustExist` before the unchanged quick check. This permits only SQLite's native crash recovery; the existing corrupt-database checks still require failure without byte replacement, quarantine, or inferred repair. No schema, migration, database owner, fallback, compatibility path, or runner behavior changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-083.
- **Closure evidence:** a lowest-sufficient DELETE-journal regression forces dirty-page spill in one uncommitted transaction and then `SIGKILL`s its writer. It fails against the prior read-only check with the exact A1 error and passes after the one-line correction while the complete focused storage file passes 11/11. Rebuilt A1 images recovered the retained data root across repeated abrupt NanoCore and whole-host restart paths without repeating the read-only migration failure. Recorded disposition evidence: corrected locally in the existing storage owner by opening the existing file normally with `fileMustExist` before the unchanged quick check. This permits only SQLite's native crash recovery; the existing corrupt-database checks still require failure without byte replacement, quarantine, or inferred repair. No schema, migration, database owner, fallback, compatibility path, or runner behavior changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-084 — App construction repeated the scoped-database migration scan before listening

- **Observation:** the exact `e4f4cfd6` F1 rerun restarted NanoCore, but the original NanoHost successor exhausted its bounded window at 297.425 seconds and the cleanup fresh start failed at 326.237 seconds. NanoCore recorded `boot.outcome` at 221.442 seconds yet did not log its HTTP listener until 328.217 seconds. The only synchronous all-Workspace path between those events was `createApp()` calling `backfillRepositoryDataSourceCatalogs()`, which reopened every Workspace database and reapplied scoped migrations immediately after the migrations phase had already verified and migrated the same database set.
- **Impact:** `apps/nanocore/src/app.ts`; `apps/nanocore/src/workspace/repository-data-source-catalog.ts`; `apps/nanocore/src/repository-routes.test.ts`; NanoCore crash-recovery availability; NHC-5 F1; and root `[NONNEG-001]`, `[TEST-009]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by deleting the startup-only compatibility scan and its dead traversal helpers. Existing repository mutations still synchronize their exact data-source catalog through `syncRepositoryDataSourceCatalog()`; App construction no longer discovers or rewrites pre-existing Workspace repository state. No timeout, migration, durable owner, listener, compatibility path, or runner behavior was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by deleting the startup-only compatibility scan and its dead traversal helpers. Existing repository mutations still synchronize their exact data-source catalog through `syncRepositoryDataSourceCatalog()`; App construction no longer discovers or rewrites pre-existing Workspace repository state. No timeout, migration, durable owner, listener, compatibility path, or runner behavior was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-084.
- **Closure evidence:** the inverted startup-backfill regression passes inside the complete repository route file `10/10`; NanoCore typecheck, build, targeted Biome, documentation lifecycle, model, index, diff checks, and Git hooks pass. Exact rebuilt `b592afab` reduced `boot.outcome` to listener from 106.775 seconds to 0.104 seconds and total restart to listener from 328.217 seconds to 233.067 seconds. The next frozen Aggregate passed F1 and F2 before exposing the separate F3 oracle ordering defect below. Recorded disposition evidence: corrected locally by deleting the startup-only compatibility scan and its dead traversal helpers. Existing repository mutations still synchronize their exact data-source catalog through `syncRepositoryDataSourceCatalog()`; App construction no longer discovers or rewrites pre-existing Workspace repository state. No timeout, migration, durable owner, listener, compatibility path, or runner behavior was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-085 — F3 required a projection that its blocked-create fault never produced

- **Observation:** exact rebuilt `b592afab` advanced the frozen Aggregate through F1 and F2, rebooted A1 during F3's blocked `sandbox.create`, restarted only NanoCore, and proved RuntimeTarget generation 300 as predecessor-fenced, nonready, and nonempty with NanoHost inactive. The F3 Turn remained `running` with no RuntimeEvidence or Action Center item because the anchored backend correctly retained `cleanup-pending` until a NanoHost successor could prove external cleanup. After the exact recovery sequence completed, the Turn was `interrupted`, the backend was physically cleaned, its lease was released, teardown evidence existed, and RuntimeTarget was ready and fresh-empty, but `/api/app/recovery/interrupted-workers` correctly remained empty because the pre-worker blocked create had produced no recoverable checkpoint.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F3; the separate Core and external Runtime effect domains in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[SCOPE-007]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by deleting the non-owner interrupted-worker read and its fixture projection. Existing cleanup proof remains decisive: it requires the same lineage's terminal Turn, physically cleaned backend, terminal lease, one teardown evidence record, and ready fresh-empty RuntimeTarget after the recovery successor. No product behavior, timeout, wait, owner, state, effect, or additional instrument changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by deleting the non-owner interrupted-worker read and its fixture projection. Existing cleanup proof remains decisive: it requires the same lineage's terminal Turn, physically cleaned backend, terminal lease, one teardown evidence record, and ready fresh-empty RuntimeTarget after the recovery successor. No product behavior, timeout, wait, owner, state, effect, or additional instrument changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-085.
- **Closure evidence:** exact A1 recovery proved the owner facts above, and the corrected complete Unit F runner suite passes `64/64`. The next exact Aggregate advanced F3 through the same owner cleanup before exposing the separate direct-recovery oracle defect below. Recorded disposition evidence: corrected locally by deleting the non-owner interrupted-worker read and its fixture projection. Existing cleanup proof remains decisive: it requires the same lineage's terminal Turn, physically cleaned backend, terminal lease, one teardown evidence record, and ready fresh-empty RuntimeTarget after the recovery successor. No product behavior, timeout, wait, owner, state, effect, or additional instrument changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-086 — F3 rejected a valid direct-ready recovery successor

- **Observation:** exact `4d331ce5` completed F1, F2, and F3 owner cleanup but created no F4 Workspace. The F3 Turn was `interrupted`; its sole lease was released for `scheduler-restart-backend-cleanup`; its sole backend was physically cleaned; and it had exactly one succeeded teardown record. On the rebooted host, NanoHost started once, remained active, and emitted neither a terminal poll `409` nor a connection failure. The runner nevertheless excluded every ready target whenever fault lineage existed and waited only for a first-fence `409`; its 300-second observation bound expired 316 seconds after teardown, then fail-stop correctly stopped NanoHost and projected RuntimeTarget nonready.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F3; the Execution Server Restart and Required Scenario 3 contracts in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by allowing only F3 to accept the existing exact direct-ready successor predicate: the strictly next generation must be predecessor-fenced, ready, and fresh-empty. F2 and F4 retain the existing first-fence path, and F3 still proves a distinct fresh epoch, prior-root removal, terminal owner cleanup, and ready fresh-empty RuntimeTarget before returning. No product behavior, timeout, wait, owner, state, effect, port, or additional instrument changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by allowing only F3 to accept the existing exact direct-ready successor predicate: the strictly next generation must be predecessor-fenced, ready, and fresh-empty. F2 and F4 retain the existing first-fence path, and F3 still proves a distinct fresh epoch, prior-root removal, terminal owner cleanup, and ready fresh-empty RuntimeTarget before returning. No product behavior, timeout, wait, owner, state, effect, port, or additional instrument changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-086.
- **Closure evidence:** the corrected complete Unit F runner suite passes `64/64`. Exact `5e73aede` then accepted the direct-ready successor before exposing the separate F3 cleanup-owner race below. Recorded disposition evidence: corrected locally by allowing only F3 to accept the existing exact direct-ready successor predicate: the strictly next generation must be predecessor-fenced, ready, and fresh-empty. F2 and F4 retain the existing first-fence path, and F3 still proves a distinct fresh epoch, prior-root removal, terminal owner cleanup, and ready fresh-empty RuntimeTarget before returning. No product behavior, timeout, wait, owner, state, effect, port, or additional instrument changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-087 — F3 direct readiness raced its existing cleanup owner

- **Observation:** exact `5e73aede` again completed F1 and F2, then reached F3 direct fresh-empty readiness and proved the reboot-observed prior state and runtime roots absent, but created no F4 Workspace. At fail-stop the F3 Turn remained `running`, its lease remained `acquired`, its backend remained `cleanup-pending`, and no RuntimeEvidence existed. The direct-ready branch returned immediately, so the runner called the product interrupt route before the existing post-listener cleanup owner completed; a `404` followed by a still-running Turn caused the runner to fail closed. Starting NanoHost again through the same product path let that existing owner transition the Turn to `interrupted`, release the lease, physically clean the backend, and write one succeeded teardown record; the concurrent interrupt then returned `404` with the Turn already terminal and was correctly idempotent.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F3; the effect-free boot and post-listener cleanup owner in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by reusing the existing `proveSequencedFaultTurnFenced()` owner predicate before a direct-ready recovery returns. The predicate requires the exact lineage's physically cleaned backend, terminal lease, one teardown record, and terminal Turn; the later interrupt remains an idempotent no-op. F2, F4, product behavior, timeouts, waits, owners, states, effects, ports, and instruments are unchanged. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by reusing the existing `proveSequencedFaultTurnFenced()` owner predicate before a direct-ready recovery returns. The predicate requires the exact lineage's physically cleaned backend, terminal lease, one teardown record, and terminal Turn; the later interrupt remains an idempotent no-op. F2, F4, product behavior, timeouts, waits, owners, states, effects, ports, and instruments are unchanged. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-087.
- **Closure evidence:** the corrected focused F3 sequence, complete Unit F runner suite, and exact Aggregate remain pending. Recorded disposition evidence: corrected locally by reusing the existing `proveSequencedFaultTurnFenced()` owner predicate before a direct-ready recovery returns. The predicate requires the exact lineage's physically cleaned backend, terminal lease, one teardown record, and terminal Turn; the later interrupt remains an idempotent no-op. F2, F4, product behavior, timeouts, waits, owners, states, effects, ports, and instruments are unchanged. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-088 — Completed F1 owner state did not prove F1 sequence acceptance

- **Observation:** exact `cae9b40d` and `72bdce1c` each created and terminally cleaned only F1 before Aggregate failed, and no F2 Workspace was created. That durable end state originally localized the failure too broadly to the post-F1 or next pre-F2 baseline boundary. A same-driver exact F1 probe then reported `effectCompleted=false` and proved its post-baseline was never invoked, falsifying the baseline premise; the next unchanged exact F1 completed successfully.
- **Impact:** NHC-5 F1; `tests/support/host/nanohost-unit-f-runner.mjs`; owner evidence interpretation; and root `[EVID-001]`, `[SCOPE-012]`, and `[CHECK-019]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: retained as a non-blocking, non-Safety finding. The unsupported baseline retry correction was withdrawn; no code, timeout, retry, oracle, owner, state, effect, command, or instrument remains changed for this finding. Intermittent F1 failure without a stable owner contradiction permits another exact Aggregate but no implementation inference. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: retained as a non-blocking, non-Safety finding. The unsupported baseline retry correction was withdrawn; no code, timeout, retry, oracle, owner, state, effect, command, or instrument remains changed for this finding. Intermittent F1 failure without a stable owner contradiction permits another exact Aggregate but no implementation inference. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-088.
- **Closure evidence:** the same-driver probe proved the earlier failure occurred inside F1 effect sequencing, while the next exact F1 passed unchanged. The next exact Aggregate remains pending. Recorded disposition evidence: retained as a non-blocking, non-Safety finding. The unsupported baseline retry correction was withdrawn; no code, timeout, retry, oracle, owner, state, effect, command, or instrument remains changed for this finding. Intermittent F1 failure without a stable owner contradiction permits another exact Aggregate but no implementation inference. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-089 — The Unit F fail-closed result discarded completed scenario evidence

- **Observation:** the exact `ac485778` Aggregate using the existing logged-in A1 Codex subscription ran for approximately 27 minutes, created F1, F2, and F3 Workspaces, created no F4 Workspace, and then returned a pre-adjudication `FAIL` with zero scenario rows. The existing product accessor showed F2's expected unknown-fence checkpoint plus successful teardown and showed F3's Turn as `interrupted` with successful teardown. The RuntimeTarget was nonready and nonfresh only after the scenario catch performed its deliberate fail-stop cleanup, so that later projection cannot identify the original failure.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; NHC-5 F3 and F4; owner evidence interpretation; and root `[TEST-012]`, `[SCOPE-012]`, and `[CHECK-019]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected after two later exact Aggregates failed inside F1 before creating F2 while the fail-closed result again retained no deciding failure evidence. The Unit F runner now reuses the existing product App API operation backed by `listWorkspaceRuntimeEvidence()` and retains only the failing scenario id plus records for the exact Turn. It retains no raw error, adds no diagnostic script or table reader, and changes no retry, timeout, owner, state, effect, or product behavior. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected after two later exact Aggregates failed inside F1 before creating F2 while the fail-closed result again retained no deciding failure evidence. The Unit F runner now reuses the existing product App API operation backed by `listWorkspaceRuntimeEvidence()` and retains only the failing scenario id plus records for the exact Turn. It retains no raw error, adds no diagnostic script or table reader, and changes no retry, timeout, owner, state, effect, or product behavior. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-089.
- **Closure evidence:** the focused regression proves the read uses the product App API and excludes records from other Turns. The complete Unit F runner suite and the next exact Aggregate remain pending. Recorded disposition evidence: corrected after two later exact Aggregates failed inside F1 before creating F2 while the fail-closed result again retained no deciding failure evidence. The Unit F runner now reuses the existing product App API operation backed by `listWorkspaceRuntimeEvidence()` and retains only the failing scenario id plus records for the exact Turn. It retains no raw error, adds no diagnostic script or table reader, and changes no retry, timeout, owner, state, effect, or product behavior. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-090 — F3 accepted fresh readiness before the successor's first poll

- **Observation:** the unchanged `ab275104` Aggregate again ran for approximately 27 minutes, created F1 through F3 but no F4, and returned the same pre-adjudication `FAIL`. This time the exact F3 Turn remained `running` with no RuntimeEvidence after fail-stop. The F3 direct-recovery branch accepted the fresh successor as soon as RuntimeTarget became ready and fresh-empty, before that successor's first outer poll could either settle the retained cleanup result or receive the required terminal `409`. The branch then stopped monitoring the first-fence outcome and waited for terminal cleanup against the same 300-second bound used by the product result-only expectation.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F3; the result-only recovery and post-fence fresh-ready contracts in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by accepting F3 direct readiness only when the existing exact-lineage cleanup owner is already terminal. Otherwise the same recovery loop continues observing the epoch and takes its existing first-fence path when the successor's first poll fails. The later duplicate owner wait was deleted. No timeout, retry, state, owner, effect, API, script, or product behavior changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by accepting F3 direct readiness only when the existing exact-lineage cleanup owner is already terminal. Otherwise the same recovery loop continues observing the epoch and takes its existing first-fence path when the successor's first poll fails. The later duplicate owner wait was deleted. No timeout, retry, state, owner, effect, API, script, or product behavior changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-090.
- **Closure evidence:** the new regression presents one transient ready/fresh projection with a nonterminal cleanup owner, then the terminal first poll and existing proof-successor sequence. It failed against the prior branch at the cleanup-owner wait, passes after the predicate correction, and the complete Unit F runner suite passes `65/65`. Recorded disposition evidence: corrected locally by accepting F3 direct readiness only when the existing exact-lineage cleanup owner is already terminal. Otherwise the same recovery loop continues observing the epoch and takes its existing first-fence path when the successor's first poll fails. The later duplicate owner wait was deleted. No timeout, retry, state, owner, effect, API, script, or product behavior changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-091 — F3 required a healthy proof successor to fail-stop again

- **Observation:** the exact `47efab0d` Aggregate passed the earlier F1 failure boundary and reached F3. Its first recovery epoch failed closed exactly once on the expected `sandbox.create` poll `409`. The next generation started, remained healthy for more than five minutes, and supplied the later fresh-ready proof that let the existing cleanup owner interrupt the Turn, release the lease, clean the backend, and record one succeeded teardown. The runner nevertheless waited for that healthy proof successor to become nonready and emit a second `409`, then timed out and deliberately stopped it.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F3; the post-fence fresh-ready and result-only cleanup paths in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by accepting the exact next generation when it is ready and fresh-empty and the existing exact-lineage cleanup owner is terminal. A genuinely nonready proof successor still takes the existing second-fence and final-successor path. No timeout, retry, state, owner, effect, API, or product behavior changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by accepting the exact next generation when it is ready and fresh-empty and the existing exact-lineage cleanup owner is terminal. A genuinely nonready proof successor still takes the existing second-fence and final-successor path. No timeout, retry, state, owner, effect, API, or product behavior changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-091.
- **Closure evidence:** the existing F3 transient-readiness regression presents a nonterminal owner before the first fence and a healthy proof successor only after terminal cleanup. The complete Unit F runner suite passes `67/67`, and the exact corrected F3 execution passed with consecutive prior, first-fence, and healthy-successor generations. Recorded disposition evidence: corrected locally by accepting the exact next generation when it is ready and fresh-empty and the existing exact-lineage cleanup owner is terminal. A genuinely nonready proof successor still takes the existing second-fence and final-successor path. No timeout, retry, state, owner, effect, API, or product behavior changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-092 — NanoCore restart retained stale RuntimeTarget readiness

- **Observation:** a clean exact F3 reboot stopped NanoCore before its physical-close callback could run. After explicit NanoCore restart with NanoHost still stopped, the latest F3 Turn remained `running`, its exact lease remained `acquired`, its backend was `cleanup-pending`, and no RuntimeEvidence existed, but the durable RuntimeTarget still projected the prior generation as ready and fresh-empty. The runner correctly rejected that stale pre-successor projection and expired before starting recovery NanoHost.
- **Impact:** `apps/nanocore/src/index.ts`; `apps/nanocore/src/runtime/nanohost-runtime-target.ts`; NHC-5 F3; the process-local transport authority and effect-free restart-fencing contracts in `docs/specs/20260704-nanocore_bootstrap_readiness.md` and `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-009]`, `[TEST-012]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by reusing the existing RuntimeTarget physical-close owner during phase 8 before product admission. Restart fences the sole durable generation nonready and nonfresh without allocating a successor, adding state, changing a timeout, or changing the runner. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by reusing the existing RuntimeTarget physical-close owner during phase 8 before product admission. Restart fences the sole durable generation nonready and nonfresh without allocating a successor, adding state, changing a timeout, or changing the runner. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-092.
- **Closure evidence:** the focused owner regression proves restart removes stale readiness while preserving the generation high-water mark. The rebuilt A1 F3 execution passed with one restart fence followed by one fresh healthy successor. Recorded disposition evidence: corrected locally by reusing the existing RuntimeTarget physical-close owner during phase 8 before product admission. Restart fences the sole durable generation nonready and nonfresh without allocating a successor, adding state, changing a timeout, or changing the runner. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-093 — Phase 8 repeated full Workspace layout validation

- **Observation:** exact rebuilt `c4e6999f` removed stale RuntimeTarget readiness, but the next clean F3 still expired at the unchanged five-minute owner bound. Container start to listener took 305.7 seconds. The boot checkpoint scan emitted 127 recovery-required classifications over 105.1 seconds and then continued opening empty Workspace scopes; each open called the ordinary Workspace opener, which revalidated the complete directory layout even though the same boot's critical migrations phase had already verified and migrated every scoped database.
- **Impact:** `apps/nanocore/src/index.ts`; `apps/nanocore/src/storage/db.ts`; NHC-5 F3; the bounded local phase-8 scan in `docs/specs/20260704-nanocore_bootstrap_readiness.md`; and root `[TEST-009]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by reusing the storage owner through a boot-verified existing-Workspace opener during the post-migration phase-8 checkpoint scan. The scan still visits and classifies every checkpoint; no timeout, skipped recovery candidate, persisted index, concurrency, listener, or runner behavior changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by reusing the storage owner through a boot-verified existing-Workspace opener during the post-migration phase-8 checkpoint scan. The scan still visits and classifies every checkpoint; no timeout, skipped recovery candidate, persisted index, concurrency, listener, or runner behavior changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-093.
- **Closure evidence:** the focused storage regression proves the boot-only opener neither creates a missing Workspace layout nor bypasses the ordinary opener for creation. Exact rebuilt `ace85304` reached its listener in 152.763 seconds during deployment and in 184.257 seconds after a controlled reboot, both below the unchanged 300-second owner bound. A correctly invoked exact F3 completed its effect and recovery sequence before exposing the separate baseline oracle defect below. Recorded disposition evidence: corrected locally by reusing the storage owner through a boot-verified existing-Workspace opener during the post-migration phase-8 checkpoint scan. The scan still visits and classifies every checkpoint; no timeout, skipped recovery candidate, persisted index, concurrency, listener, or runner behavior changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-094 — F3 required byte-equal host Docker state across an execution-server reboot

- **Observation:** a correctly invoked exact `ace85304` F3 accepted the blocked create, rebooted A1, fenced generation 368 with successor generation 370, proved prior roots absent, terminally cleaned the exact Turn, and restored ready fresh-empty capacity. Its effect sequence succeeded, but the generic scenario adjudicator rejected the run because the pre/post system-Docker baseline digests differed. A controlled reboot reproduced the cause without a worker: Docker recreated the default bridge `Created`, `Id`, and `Containers` projection, the `docker0` address projection changed, unrelated host containers changed from 14 to 17, and the canonical nftables row count remained 122 while Docker regenerated rule values. Required Scenario 3 owns execution-server restart recovery, not byte identity for the host Docker daemon that the fault itself restarts; the owning specification applies system-Docker equality to normal stop, member failure, and NanoHost `SIGKILL` instead.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F3; Required Scenario 3 and the explicit system-Docker noninterference checks in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[SCOPE-007]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by requiring both F3 baseline observations to remain valid digests without requiring equality across the host reboot. F1, F2, F4, and the Aggregate-owned ordinary lifecycle retain exact baseline equality; no product behavior, timeout, retry, state, owner, effect, port, or additional instrument changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by requiring both F3 baseline observations to remain valid digests without requiring equality across the host reboot. F1, F2, F4, and the Aggregate-owned ordinary lifecycle retain exact baseline equality; no product behavior, timeout, retry, state, owner, effect, port, or additional instrument changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-094.
- **Closure evidence:** the focused regression passes only when F3 has two valid but distinct baseline digests while the other scenario baseline rejection remains unchanged, and the complete Unit F runner suite passes `67/67`. The exact corrected F3 execution then passed with distinct pre/post baseline digests. Recorded disposition evidence: corrected locally by requiring both F3 baseline observations to remain valid digests without requiring equality across the host reboot. F1, F2, F4, and the Aggregate-owned ordinary lifecycle retain exact baseline equality; no product behavior, timeout, retry, state, owner, effect, port, or additional instrument changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-095 — F3 adjudication retained the deleted second-fence generation shape

- **Observation:** exact corrected F3 completed Required Scenario 3 with prior generation 371, first-fence generation 372, and one healthy proof successor at generation 373. The existing NHC-FND-091 correction intentionally accepts that healthy proof successor after exact-lineage cleanup instead of requiring it to fail-stop and allocate another generation, but the independent scenario adjudicator still required every non-null fence to advance the successor by two. The effect and cleanup were therefore correct while the retained verdict was `FAIL`.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F3; NHC-FND-091; the post-fence fresh-ready and result-only cleanup paths in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by accepting F3's healthy proof successor at exactly first-fence generation plus one. F2 and F4 retain the existing second-fence and final-successor requirement at first-fence generation plus two, while the no-fence direct-ready shape remains prior generation plus one. No product behavior, timeout, retry, state, owner, effect, port, or additional instrument changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by accepting F3's healthy proof successor at exactly first-fence generation plus one. F2 and F4 retain the existing second-fence and final-successor requirement at first-fence generation plus two, while the no-fence direct-ready shape remains prior generation plus one. No product behavior, timeout, retry, state, owner, effect, port, or additional instrument changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-095.
- **Closure evidence:** the shared complete-evidence fixture now projects F3's accepted first-fence shape while retaining F2 and F4's existing generation shape, and the complete Unit F runner suite passes `67/67`. The exact corrected F3 execution passed with generations 373, 374, and 375 for the prior target, first fence, and healthy successor respectively. Recorded disposition evidence: corrected locally by accepting F3's healthy proof successor at exactly first-fence generation plus one. F2 and F4 retain the existing second-fence and final-successor requirement at first-fence generation plus two, while the no-fence direct-ready shape remains prior generation plus one. No product behavior, timeout, retry, state, owner, effect, port, or additional instrument changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-096 — Empty NanoHost effect polling starved NanoCore App API work

- **Observation:** the exact A1 F1 input was already the bounded `sleep 45` task, but the authoritative NanoHost connection immediately repeated every fair eight-path cycle after all paths returned empty `204`. A short syscall sample observed about 1,600 HTTP/2 writes per second on the sole NanoHost transport connection while NanoCore consumed about one full CPU and the App API exceeded its 30-second request deadline. Stopping NanoHost through the existing F1 cleanup primitive immediately reduced NanoCore CPU to `0.00%`, proving the delay came from empty effect-poll carriage rather than the model or Task payload.
- **Impact:** `apps/nanohost/src/main.rs`; `apps/nanohost/README.md`; NHC-5 F1; the fair fixed-effect polling contract in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-009]`, `[QUALITY-001]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by waiting 100 milliseconds only after one complete fair cycle returns eight empty `204` responses. An accepted command resets the empty count and enters its existing local owner without that idle delay. No protocol, path, effect owner, timeout, retry, cursor order, durable state, configuration input, or runner structure changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by waiting 100 milliseconds only after one complete fair cycle returns eight empty `204` responses. An accepted command resets the empty count and enters its existing local owner without that idle delay. No protocol, path, effect owner, timeout, retry, cursor order, durable state, configuration input, or runner structure changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-096.
- **Closure evidence:** the focused regression failed without idle-cycle pacing and passes after the correction; NanoHost formatting and its complete `83 + 1` test set pass. Exact rebuilt A1 CPU and F1 evidence remain pending. Recorded disposition evidence: corrected locally by waiting 100 milliseconds only after one complete fair cycle returns eight empty `204` responses. An accepted command resets the empty count and enters its existing local owner without that idle delay. No protocol, path, effect owner, timeout, retry, cursor order, durable state, configuration input, or runner structure changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-097 — Full fault scenarios were being used as the per-change feedback loop

- **Observation:** the complete Unit F runner pays for baseline collection, fault injection, recovery, and final adjudication even when the immediate question is only whether a fresh Task can traverse NanoCore, NanoHost, OpenShell, the worker image, transcript import, teardown, and RuntimeEvidence. A fresh-Workspace A1 probe using a credential-free public fixture, its matching commit, the Task `Run sleep 1 once. After it returns, reply exactly DONE.`, the existing App API, and a 240-second request bound completed that ordinary path in 40.411, 38.110, and 39.286 seconds. The first two successful probes retained their distinct shared Sandboxes without a Gateway `DeleteSandbox`, and both immediately following F1 attempts then failed during the next Sandbox create. An ordinary NanoHost stop followed by start removed the probe epoch and restored a later ready, predecessor-fenced, fresh-empty RuntimeTarget; direct service restart was not a valid substitute because the unit's stop lifecycle cancelled that combined job. Earlier probes also correctly failed when the public repository URL and source commit did not name the same Git object, while a private product repository was unsuitable because this fixture contract carries no repository credential.
- **Impact:** the NHC-5 execution method; the existing Unit F runner; the Workspace source contract in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[EVID-001]`, `[TEST-002]`, `[CHECK-019]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected as an execution method without changing or extending the accepted runner. Per-change feedback uses the bounded fresh-Workspace probe, then the runner's existing NanoHost stop and start primitives establish a later ready fresh-empty epoch before another probe or exact scenario. Exact F1 through F4 plus Aggregate run only after that closed feedback loop passes. A five-minute expiry remains setup or collection failure rather than product evidence. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected as an execution method without changing or extending the accepted runner. Per-change feedback uses the bounded fresh-Workspace probe, then the runner's existing NanoHost stop and start primitives establish a later ready fresh-empty epoch before another probe or exact scenario. Exact F1 through F4 plus Aggregate run only after that closed feedback loop passes. A five-minute expiry remains setup or collection failure rather than product evidence. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-097.
- **Closure evidence:** all three exact rebuilt A1 probes completed the Turn and recorded succeeded checkpoint, transcript-collection, and teardown evidence in less than 41 seconds. The first complete probe-plus-fresh-reset loop restored the next ready fresh-empty generation in 133 seconds. No product code, durable owner, runner option, fixture, timeout, retry, or committed diagnostic script was added. Recorded disposition evidence: corrected as an execution method without changing or extending the accepted runner. Per-change feedback uses the bounded fresh-Workspace probe, then the runner's existing NanoHost stop and start primitives establish a later ready fresh-empty epoch before another probe or exact scenario. Exact F1 through F4 plus Aggregate run only after that closed feedback loop passes. A five-minute expiry remains setup or collection failure rather than product evidence. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-098 — F1 lost the exact create failure after the Gateway acknowledged the sandbox

- **Observation:** after each of two fast probes passed without a subsequent fresh reset, the exact rebuilt A1 F1 reached a new `sandbox.create`. Each Gateway request completed successfully and its sandbox supervisor started, but NanoHost failed while obtaining the SDK ready or identity proof, retained only the terminal outer create classification, fail-stopped, and left the Task running. The second run was stopped at the five-minute feedback bound without treating the expiry as product evidence. In both pairs, the successful probe's prior shared Sandbox had no observed Gateway `DeleteSandbox`; three earlier probes that did issue `DeleteSandbox` did not poison their successor create.
- **Impact:** `apps/nanohost/src/openshell_client.rs`; NHC-5 F1; the create-outcome and fail-closed contracts in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[EVID-001]`, `[TEST-012]`, and `[SCOPE-012]`.
- **Evidence:** Recovery evidence: NanoCore restart recovery interrupted both retained Turns, succeeded their teardown, and later NanoHost generations proved ready fresh-empty capacity. The most recent recovery completed its expected first-poll `409` fence and restored the clean generation used by the 133-second feedback loop and the later successful F1 effect sequence.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved as pre-test state rather than a product or runner correction. The next exact F1 started from the proved fresh-empty generation after the closed probe reset, remained healthy through its first create, and completed the required F1 effect sequence without another create failure. The feedback loop therefore retains the existing stop/start reset before a different Workspace or exact scenario. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved as pre-test state rather than a product or runner correction. The next exact F1 started from the proved fresh-empty generation after the closed probe reset, remained healthy through its first create, and completed the required F1 effect sequence without another create failure. The feedback loop therefore retains the existing stop/start reset before a different Workspace or exact scenario. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-098.
- **Closure evidence:** Recorded disposition evidence: resolved as pre-test state rather than a product or runner correction. The next exact F1 started from the proved fresh-empty generation after the closed probe reset, remained healthy through its first create, and completed the required F1 effect sequence without another create failure. The feedback loop therefore retains the existing stop/start reset before a different Workspace or exact scenario. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-099 — F1 effect acceptance was masked by an unattributed whole-host baseline change

- **Observation:** the exact F1 started from ready fresh-empty generation 386, completed create and worker launch, restarted only NanoCore, reconnected through ready fresh-empty generation 387 without an additional fence or create, reached terminal restart recovery, and recorded succeeded teardown. The scenario returned after 595 seconds with complete lineage and generation proof, but its retained verdict was `FAIL` only because the pre/post normalized system-Docker digests were unequal. Two immediate baseline captures five seconds apart were equal after the run, while the A1 Docker daemon concurrently served unrelated containers and health checks during the scenario; the retained digest does not identify which projected field changed or attribute the change to F1.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; NHC-5 F1; the system-Docker noninterference predicate in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[EVID-001]`, `[TEST-012]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by requiring two valid baseline digests without requiring equality for F1, matching the existing F3 treatment of a scenario whose owned fault restarts the host system's container substrate. F2, F4, and Aggregate-owned ordinary lifecycle retain exact baseline equality. No product behavior, timeout, retry, snapshot field, effect, owner, or additional instrument changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by requiring two valid baseline digests without requiring equality for F1, matching the existing F3 treatment of a scenario whose owned fault restarts the host system's container substrate. F2, F4, and Aggregate-owned ordinary lifecycle retain exact baseline equality. No product behavior, timeout, retry, snapshot field, effect, owner, or additional instrument changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-099.
- **Closure evidence:** the exact runner returned `baselineEqual=false`, prior generation 386, successor generation 387, no fence generation, and scenario `FAIL`; the Product App API independently returned an interrupted restart-recovery Turn with one succeeded teardown record and no raw runtime output. The independent blocker review rejected whole-host equality as an F1 deciding oracle and required the local correction without another A1 rerun; the focused regression accepts distinct valid F1 digests while retaining the existing strict scenarios. Recorded disposition evidence: corrected locally by requiring two valid baseline digests without requiring equality for F1, matching the existing F3 treatment of a scenario whose owned fault restarts the host system's container substrate. F2, F4, and Aggregate-owned ordinary lifecycle retain exact baseline equality. No product behavior, timeout, retry, snapshot field, effect, owner, or additional instrument changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-100 — F3 treated a network namespace inode as stable across host reboot

- **Observation:** the exact F3 rebooted A1 and proved a different kernel boot id, stopped NanoHost, explicitly restarted NanoCore, and restored the authenticated App API with RuntimeTarget predecessor-fenced, nonready, and nonfresh. The runner then spent two consecutive 300-second bounds proving the old epoch effects absent, including its fail-stop cleanup, and returned after 940 seconds with both failures at `proveSequencedEpochAbsent()`. The rebooted host had zero old runtime-epoch directories and zero NanoHost processes, so no old pathname socket or process namespace could survive; the oracle nevertheless compared the pre-reboot `net:[inode]` string with post-reboot processes even though Linux namespace inode numbers are scoped to one boot and may be reused.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F3; the Execution Server Restart contract in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected by retaining exact pathname-socket absence across every boot while considering a matching network namespace inode live only when the current kernel boot id equals the observed epoch boot id. No product behavior, timeout, retry, effect, owner, state, port, fixture, or runner mode changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected by retaining exact pathname-socket absence across every boot while considering a matching network namespace inode live only when the current kernel boot id equals the observed epoch boot id. No product behavior, timeout, retry, effect, owner, state, port, fixture, or runner mode changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-100.
- **Closure evidence:** the focused regression proves that a same-boot namespace match remains residue, a cross-boot numeric match does not, and a live socket still blocks across a boot boundary. The complete Unit F runner suite passes `69/69`. The exact corrected A1 F3 passed in 561 seconds with consecutive prior, first-fence, and healthy-successor generations 393, 394, and 395; the expected cross-reboot baseline difference remained nondeciding. Recorded disposition evidence: corrected by retaining exact pathname-socket absence across every boot while considering a matching network namespace inode live only when the current kernel boot id equals the observed epoch boot id. No product behavior, timeout, retry, effect, owner, state, port, fixture, or runner mode changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-101 — F4 waited for Turn cleanup before the successor that releases it

- **Observation:** the exact F4 killed the Gateway during one accepted create, proved the complete old epoch absent, and fenced RuntimeTarget generation 395 nonready and nonfresh. The Product App API then showed the exact Turn still `running` with no RuntimeEvidence, while the runner waited 300 seconds for that Turn to become terminal before it allowed any recovery successor and failed after 389 seconds. The existing unknown-effect owner releases the accepted create only when a successor performs its first poll, so the F4-only pre-recovery Turn wait blocked the owner event required to satisfy itself; the immediately following F4-only full-cleanup gate repeated the same ordering defect, while F2 already uses the correct successor-first sequence.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; NHC-5 F4; Required Scenario 4 in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected by deleting only the two adjacent F4-specific pre-recovery terminal cleanup gates and reusing the shared fenced-epoch recovery sequence. The runner still proves the old epoch and capacity unusable before recovery, requires the successor poll and exact cleanup owner, interrupts any surviving Turn through its existing product route, and accepts only final ready fresh-empty capacity. No product behavior, timeout, retry, state, owner, effect, port, fixture, or runner mode changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected by deleting only the two adjacent F4-specific pre-recovery terminal cleanup gates and reusing the shared fenced-epoch recovery sequence. The runner still proves the old epoch and capacity unusable before recovery, requires the successor poll and exact cleanup owner, interrupts any surviving Turn through its existing product route, and accepts only final ready fresh-empty capacity. No product behavior, timeout, retry, state, owner, effect, port, fixture, or runner mode changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-101.
- **Closure evidence:** the focused regression keeps the pre-recovery F4 Turn running and requires fresh recovery to begin before the existing interrupt and terminal cleanup proof. The complete Unit F runner suite passes `69/69`. The exact corrected A1 F4 passed in 257 seconds with equal system-Docker baselines, prior generation 398, first-fence generation 399, and final ready fresh-empty successor generation 401. Recorded disposition evidence: corrected by deleting only the two adjacent F4-specific pre-recovery terminal cleanup gates and reusing the shared fenced-epoch recovery sequence. The runner still proves the old epoch and capacity unusable before recovery, requires the successor poll and exact cleanup owner, interrupts any surviving Turn through its existing product route, and accepts only final ready fresh-empty capacity. No product behavior, timeout, retry, state, owner, effect, port, fixture, or runner mode changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-102 — F1 completed its bounded task before NanoCore cold recovery

- **Observation:** the retained full Aggregate failed in F1 after 584 seconds. The exact product owners showed one successful model capability call, one worker-ready event, thirteen heartbeats, a final worker status of `failed` with stop reason `error`, and succeeded teardown. NanoCore needed approximately 195 seconds from container start to listener readiness, while F1's `sleep 45` command completed during that outage. The workload therefore tested retention of a completed result across the remaining outage instead of the accepted restart-during-active-work continuation.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; NHC-5 F1 and Aggregate; Required Scenario 1 in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected locally by changing only the existing F1 task from `sleep 45` to `sleep 240`, which remains below the worker control channel's existing 300-second outage budget and above the observed NanoCore cold-start interval. No product behavior, timeout, retry, owner, state, runner mode, fixture, script, or diagnostic path changed. The daily fresh-Workspace feedback loop remains the separate `sleep 1` task and completed its full reset in 133 seconds. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected locally by changing only the existing F1 task from `sleep 45` to `sleep 240`, which remains below the worker control channel's existing 300-second outage budget and above the observed NanoCore cold-start interval. No product behavior, timeout, retry, owner, state, runner mode, fixture, script, or diagnostic path changed. The daily fresh-Workspace feedback loop remains the separate `sleep 1` task and completed its full reset in 133 seconds. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-102.
- **Closure evidence:** the complete Unit F runner suite passes `69/69`. The corrected retained A1 Aggregate then exited zero after approximately 30 minutes with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, four scenario rows, and no failure record. Recorded disposition evidence: corrected locally by changing only the existing F1 task from `sleep 45` to `sleep 240`, which remains below the worker control channel's existing 300-second outage budget and above the observed NanoCore cold-start interval. No product behavior, timeout, retry, owner, state, runner mode, fixture, script, or diagnostic path changed. The daily fresh-Workspace feedback loop remains the separate `sleep 1` task and completed its full reset in 133 seconds. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-103 — The retired Cell execution path remained after NanoHost fault acceptance

- **Observation:** after NHC-5 `PASS`, NanoCore still retained the privileged Cell helper and controller, direct OpenShell CLI adapter, NanoCore-owned Gateway and Sandbox operations, SSH restart runner, their public root commands, and thousands of lines of tests for that non-selectable path. Their only callers were the obsolete tests and restart runner; production execution already selected only the NanoHost RuntimeTarget.
- **Impact:** NHC-6; `apps/nanocore`; the root test commands; the deletion gate in `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[QUALITY-016]`, `[SCOPE-012]`, and `[CHECK-019]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved by deleting the nine path-owned source, helper, runner, and test files, removing the legacy backend implementation and selector surface from their mixed owners, and retaining only the current NanoHost backend contract and its smallest canonical-package regression. No replacement runtime, compatibility path, config option, state, migration, or architecture was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved by deleting the nine path-owned source, helper, runner, and test files, removing the legacy backend implementation and selector surface from their mixed owners, and retaining only the current NanoHost backend contract and its smallest canonical-package regression. No replacement runtime, compatibility path, config option, state, migration, or architecture was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-103.
- **Closure evidence:** exact current-source search finds none of the retired controller, helper, CLI, runner, selector, SSH lifecycle, Gateway-forward, or sandbox-direct names under `apps/nanocore` or root `package.json`. NanoCore typecheck, lint, and build pass; five focused files pass `95/95` in the fixed test image; and `git diff --check` passes. Recorded disposition evidence: resolved by deleting the nine path-owned source, helper, runner, and test files, removing the legacy backend implementation and selector surface from their mixed owners, and retaining only the current NanoHost backend contract and its smallest canonical-package regression. No replacement runtime, compatibility path, config option, state, migration, or architecture was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-104 — Current projections still instructed operators and readers to use the deleted Cell topology

- **Observation:** after NHC-6, the deployment manual, cookbooks, roadmap, test strategy, verification instrument, and several accepted specifications still described Cell placement, SSH lifecycle, Gateway forwarding, direct endpoints, the deleted restart runner, incomplete NanoHost fault evidence, or pending legacy-source deletion as current facts.
- **Impact:** NHC-7; the current documentation and local-guide surfaces; `docs/specs/20260802-nanohost_runtime_and_transport.md`; and root `[PRECEDENCE-002]`, `[OM-009]`, `[QUALITY-016]`, and `[CHECK-019]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved by deleting the obsolete operator procedure, replacing it with the existing NanoHost commands and one configured RuntimeTarget, and aligning only current implementation projections to the retained Unit F `PASS` and NHC-6 deletion. Historical and deferred evidence remains labeled as such; no runtime, configuration, script, fixture, compatibility path, or architecture was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved by deleting the obsolete operator procedure, replacing it with the existing NanoHost commands and one configured RuntimeTarget, and aligning only current implementation projections to the retained Unit F `PASS` and NHC-6 deletion. Historical and deferred evidence remains labeled as such; no runtime, configuration, script, fixture, compatibility path, or architecture was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-104.
- **Closure evidence:** the documentation model validates all 196 documents, the index is current, documentation field and lifecycle tests pass `104/104`, the verification-instrument contract passes `17/17`, and exact current-support searches find no deleted runner, helper, controller, or pending Cell cutover claim outside the intentionally Deprecated Cell specification and historical evidence. Recorded disposition evidence: resolved by deleting the obsolete operator procedure, replacing it with the existing NanoHost commands and one configured RuntimeTarget, and aligning only current implementation projections to the retained Unit F `PASS` and NHC-6 deletion. Historical and deferred evidence remains labeled as such; no runtime, configuration, script, fixture, compatibility path, or architecture was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [deferred] NHC-FND-105 — The historical OpenShell snapshot still has one policy-conformance consumer

- **Observation:** `@openkit/openshell-schema-snapshot` still pins historical stock OpenShell `0.0.80` and remains a dependency of `apps/nanocore/src/runtime/openshell-policy.ts` for YAML conformance even though the Cell runtime consumer is deleted and NanoHost owns the current app-local `0.0.99` pin.
- **Impact:** the policy renderer and vendor-snapshot owners; `packages/openshell-schema-snapshot`; and root `[SCOPE-012]`, `[QUALITY-003]`, and `[QUALITY-016]`.
- **Evidence:** Prior recorded closure evidence: exact import search finds the one product consumer in `apps/nanocore/src/runtime/openshell-policy.ts`; NanoHost production selection and the NHC-6 absence checks do not depend on this package as a runtime selector.
- **Owner:** The policy renderer and vendor-snapshot owners in a separate safety-relevant change.
- **Next action:** Recorded transition history: recorded as a non-blocking adjacent finding. NHC-7 corrects the package and specification projections so the snapshot grants no current runtime authority. Deleting or replacing its remaining policy validation would change a separate safety-relevant owner and is not authorized by this completion slice. The policy renderer and vendor-snapshot owners receive this item when a separate safety-relevant deletion or replacement change is accepted.

## [closed] NHC-FND-106 — The completion gate exposed two stale test projections

- **Observation:** the first NHC-8 full gate stopped because `apps/nanocore/src/storage/db.test.ts` created a schema table outside the migration owner, and the next build check found the tracked bundled OpenKit Skill CLI stale against its source.
- **Impact:** NHC-8; migration discipline; the generated CLI projection; and root `[PRECEDENCE-002]`, `[TEST-009]`, and `[CHECK-019]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved in checkpoint commit `2cae3503`. The database crash regression now mutates one committed migration record inside the existing transaction and proves rollback restores its original timestamp, while the bundled CLI is regenerated from its existing owner. No migration, schema, product behavior, dependency, or generation path changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved in checkpoint commit `2cae3503`. The database crash regression now mutates one committed migration record inside the existing transaction and proves rollback restores its original timestamp, while the bundled CLI is regenerated from its existing owner. No migration, schema, product behavior, dependency, or generation path changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-106.
- **Closure evidence:** the focused database suite passes `29/29`; the subsequent full gate passes all `2468/2468` NanoCore unit tests and the tracked bundle equality check. Recorded disposition evidence: resolved in checkpoint commit `2cae3503`. The database crash regression now mutates one committed migration record inside the existing transaction and proves rollback restores its original timestamp, while the bundled CLI is regenerated from its existing owner. No migration, schema, product behavior, dependency, or generation path changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-107 — A polling oracle could not observe the interval it claimed to test

- **Observation:** the full root test suite failed `tunnel stop waits for the exact SIGKILLed child close` because its 250-millisecond observation poll could not observe a fake child's 100-millisecond interval between `SIGKILL` and `close`, so loaded execution could settle before the assertion even when the implementation correctly waited for `close`.
- **Impact:** `tests/nanohost-unit-f-runner.test.mjs`; NHC-8; and root `[TEST-012]`, `[QUALITY-003]`, and `[CHECK-019]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: corrected only in the fake child and its existing regression by synchronizing on the exact synthetic `SIGKILL` event. The production tunnel, timeout, signal order, cleanup owner, runner, and fixture remain unchanged. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: corrected only in the fake child and its existing regression by synchronizing on the exact synthetic `SIGKILL` event. The production tunnel, timeout, signal order, cleanup owner, runner, and fixture remain unchanged. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-107.
- **Closure evidence:** the focused test passes in the fixed execution image, and the subsequent full root suite passes `469/469` under load. Recorded disposition evidence: corrected only in the fake child and its existing regression by synchronizing on the exact synthetic `SIGKILL` event. The production tunnel, timeout, signal order, cleanup owner, runner, and fixture remain unchanged. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-108 — The accepted h2c listener is unreachable to ordinary local product clients

- **Observation:** the NHC-8 full gate passes repository checks, lint, typecheck, unit tests, coverage, and all builds, then fails every live NanoCore stage at the listener boundary. The test image's inherited non-loopback bind correctly fails closed without TLS. A one-line diagnostic override to loopback removes that rejection but leaves `19/20` NanoCore E2E cases timing out because their ordinary `fetch` clients cannot speak NanoCore's accepted plaintext HTTP/2 listener; the override was withdrawn. The Web E2E and smoke gates fail at the same listener boundary, and the bundled CLI defaults to that loopback URL while also using ordinary `fetch`.
- **Impact:** NHC-8; `apps/nanocore/src/index.ts`; `apps/nanocore/e2e`; `apps/web/e2e`; `tests/smoke`; `skills/openkit-cli.mjs`; the browser and CLI integration claim in `apps/nanocore/README.md`; `docs/core/communication.md`; and root `[PRECEDENCE-003]`, `[AUTH-001]`, `[TEST-012]`, and `[CHECK-019]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved architecture decision with local implementation evidence. The engineer selected a dedicated App HTTP/1.1 and SSE listener plus a separate native NanoHost HTTP/2 listener. `server.bind` and the existing App environment overrides own the first; required `nanohost.bind` owns the second; the two use distinct local TCP ports; and `nanohost.rendezvousUrl` remains the advertised endpoint after any deployment mapping. App and NanoHost routes fail closed on the wrong listener, Caddy never proxies the private prefix, and no HTTP/1 fallback or proxy substitutes for NanoHost's physical HTTP/2 session authority. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved architecture decision with local implementation evidence. The engineer selected a dedicated App HTTP/1.1 and SSE listener plus a separate native NanoHost HTTP/2 listener. `server.bind` and the existing App environment overrides own the first; required `nanohost.bind` owns the second; the two use distinct local TCP ports; and `nanohost.rendezvousUrl` remains the advertised endpoint after any deployment mapping. App and NanoHost routes fail closed on the wrong listener, Caddy never proxies the private prefix, and no HTTP/1 fallback or proxy substitutes for NanoHost's physical HTTP/2 session authority. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-108.
- **Closure evidence:** the dual-listener built-artifact smoke passes App health and metadata through ordinary `fetch`, observes App-side private-route `404`, observes H2-side App-route `404`, reaches private admission over native H2 with the expected unauthenticated `401`, and completes orderly shutdown. Focused config, transport, phase-order, typecheck, and packaged-Caddy checks pass; exact A1 Unit F and Aggregate acceptance passes at commit `902c5011`, and the final NHC-8 full gate exits zero. Recorded disposition evidence: resolved architecture decision with local implementation evidence. The engineer selected a dedicated App HTTP/1.1 and SSE listener plus a separate native NanoHost HTTP/2 listener. `server.bind` and the existing App environment overrides own the first; required `nanohost.bind` owns the second; the two use distinct local TCP ports; and `nanohost.rendezvousUrl` remains the advertised endpoint after any deployment mapping. App and NanoHost routes fail closed on the wrong listener, Caddy never proxies the private prefix, and no HTTP/1 fallback or proxy substitutes for NanoHost's physical HTTP/2 session authority. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-109 — The packaged Caddy projection used HTTP/1 for its h2c NanoCore upstream

- **Observation:** under the superseded single-listener topology, the app entrypoint proved NanoCore readiness with `curl --http2-prior-knowledge`, but all three Caddy reverse proxies used the default HTTP/1 upstream transport. The broad public `/api/*` proxy also included the private `/api/nanohost/transport/*` carriage whose authority depends on NanoHost's original native HTTP/2 physical session.
- **Impact:** `Caddyfile`; the packaged app's sole public HTTP boundary; `containers/app`; NHC-8; and root `[SCOPE-007]`, `[TEST-009]`, and `[CHECK-019]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: superseded and resolved by the accepted dual-listener boundary in NHC-FND-108. Caddy correctly speaks ordinary HTTP/1.1 to the App listener, the entrypoint probes that same listener with ordinary curl, and the public entry returns `404` for the private NanoHost prefix. Caddy neither connects to nor publishes the dedicated native HTTP/2 listener. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: superseded and resolved by the accepted dual-listener boundary in NHC-FND-108. Caddy correctly speaks ordinary HTTP/1.1 to the App listener, the entrypoint probes that same listener with ordinary curl, and the public entry returns `404` for the private NanoHost prefix. Caddy neither connects to nor publishes the dedicated native HTTP/2 listener. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-109.
- **Closure evidence:** the focused app-image suite passes `5/5`, including the public-route exclusion and ordinary upstream assertions; dual-listener built-artifact smoke passes; and the final NHC-8 full gate, including the packaged App and Web smoke paths, exits zero. Recorded disposition evidence: superseded and resolved by the accepted dual-listener boundary in NHC-FND-108. Caddy correctly speaks ordinary HTTP/1.1 to the App listener, the entrypoint probes that same listener with ordinary curl, and the public entry returns `404` for the private NanoHost prefix. Caddy neither connects to nor publishes the dedicated native HTTP/2 listener. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-110 — One NanoCore E2E Agent fixture retained the obsolete image-reference shape

- **Observation:** after the dual-listener correction passed the full gate through all builds, the NanoCore E2E gate loaded the custom provider but correctly rejected its paired Agent fixture because `runtime.image` omitted the schema-required `kind: 'reference'` discriminator.
- **Impact:** `apps/nanocore/e2e/config-load.spec.ts`; NHC-8; and root `[PRECEDENCE-002]`, `[TEST-009]`, and `[CHECK-019]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved by adding only the missing discriminator to the existing fixture. Product schema, loader, runtime behavior, and harness remain unchanged. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved by adding only the missing discriminator to the existing fixture. Product schema, loader, runtime behavior, and harness remain unchanged. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-110.
- **Closure evidence:** the focused config-load file passes `3/3`, and the complete NanoCore E2E gate passes `20/20`. Recorded disposition evidence: resolved by adding only the missing discriminator to the existing fixture. Product schema, loader, runtime behavior, and harness remain unchanged. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-111 — One Web E2E Agent fixture retained the same obsolete image-reference shape

- **Observation:** after the NanoCore E2E correction, the final full gate reached Web E2E and found the simulator Agent fixture missing the same schema-required `kind: 'reference'` discriminator.
- **Impact:** `apps/web/e2e/openkit-local-self-check.spec.ts`; NHC-8; and root `[PRECEDENCE-002]`, `[TEST-009]`, and `[CHECK-019]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved by adding only the missing discriminator to the existing fixture. No schema, product behavior, harness, or coverage changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved by adding only the missing discriminator to the existing fixture. No schema, product behavior, harness, or coverage changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-111.
- **Closure evidence:** the complete Web E2E gate passes `14/14`. Recorded disposition evidence: resolved by adding only the missing discriminator to the existing fixture. No schema, product behavior, harness, or coverage changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-112 — Historical Context Package verification compared a mutable current pointer

- **Observation:** the corrected Web fixture reached its restart path and failed because historical Context Package verification compared the package's immutable Agent Environment Package snapshot to the current AgentSession `environmentPackageSnapshotId`. The accepted implementation reuses one compatible AgentSession across sequential Turns and updates that current pointer, while each historical trace already binds the exact Turn and immutable package snapshot.
- **Impact:** `apps/nanocore/src/runtime/context-package-trace.ts`; sequential AgentSession reuse; the Material handoff Web E2E path; and root `[PRECEDENCE-002]`, `[QUALITY-003]`, and `[TEST-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved by deleting only the redundant mutable-pointer equality. Historical trace, Turn, and snapshot identities remain required; no repair, compatibility path, new state, or new oracle was added. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved by deleting only the redundant mutable-pointer equality. Historical trace, Turn, and snapshot identities remain required; no repair, compatibility path, new state, or new oracle was added. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-112.
- **Closure evidence:** the exact Material handoff/proposal-conflict E2E passes, the complete Web E2E gate passes `14/14`, and the final `pnpm verify:full` run exits zero. Recorded disposition evidence: resolved by deleting only the redundant mutable-pointer equality. Historical trace, Turn, and snapshot identities remain required; no repair, compatibility path, new state, or new oracle was added. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-113 — A1 retained the old single-listener App port

- **Observation:** exact image `openkit/app:a1-fc44769e` passed image smoke, but its first A1 start correctly failed config because inherited `OPENKIT_HTTP_PORT=4317` collided with the newly required NanoHost bind on `4317`. Port `4000` was already owned by LiteLLM rather than NanoCore.
- **Impact:** the dedicated A1 deployment; `/etc/openkit/nanocore.env`; NHC-FND-108; and root Safety Kernel deployment-state and rollback obligations.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved only in A1 operator state. The previous container and config were restored after the failed start; the current deployment uses App loopback port `4318` and NanoHost loopback port `4317`, and the persistent container environment now carries the same split. No repository code, NanoHost binary, host network mode, or public route changed. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved only in A1 operator state. The previous container and config were restored after the failed start; the current deployment uses App loopback port `4318` and NanoHost loopback port `4317`, and the persistent container environment now carries the same split. No repository code, NanoHost binary, host network mode, or public route changed. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-113.
- **Closure evidence:** A1 observes App health as HTTP/1.1 `200`, the App-side private route as `404`, the NanoHost listener as native HTTP/2 with unauthenticated `401`, and HTTP/1 rejection on the NanoHost port. Stock NanoHost then restored predecessor-fenced, ready, fresh-empty state in 22 seconds. The prior stopped container and exact config backups remain available. Recorded disposition evidence: resolved only in A1 operator state. The previous container and config were restored after the failed start; the current deployment uses App loopback port `4318` and NanoHost loopback port `4317`, and the persistent container environment now carries the same split. No repository code, NanoHost binary, host network mode, or public route changed. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-114 — The real runners still treated the App listener as h2c

- **Observation:** after the A1 dual-listener deployment became healthy, the first exact acceptance attempt failed before any Task or Unit F effect because both the Unit F shared App client and the real Task fetch adapter still opened plaintext HTTP/2 to the ordinary App listener.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `apps/nanocore/e2e/task-mode-real-worker-runner.mjs`; their existing protocol regressions; NHC-8; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved in checkpoint `3f5048db`. The real Task adapter now uses Node's native `fetch` with its existing Undici deadline dispatcher, and the Unit F App client uses native `fetch` with its existing explicit bound. The deleted custom h2c implementations and three tests that only exercised the deleted transport code were not replaced; NanoHost native HTTP/2 carriage is unchanged. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved in checkpoint `3f5048db`. The real Task adapter now uses Node's native `fetch` with its existing Undici deadline dispatcher, and the Unit F App client uses native `fetch` with its existing explicit bound. The deleted custom h2c implementations and three tests that only exercised the deleted transport code were not replaced; NanoHost native HTTP/2 carriage is unchanged. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-114.
- **Closure evidence:** the Unit F runner suite passes `69/69`, the real Task runner suite passes `83/83`, Biome and `git diff --check` pass, the correction deletes 300 lines while adding 66, GitHub contains the checkpoint, and A1 fast-forwarded to it. Recorded disposition evidence: resolved in checkpoint `3f5048db`. The real Task adapter now uses Node's native `fetch` with its existing Undici deadline dispatcher, and the Unit F App client uses native `fetch` with its existing explicit bound. The deleted custom h2c implementations and three tests that only exercised the deleted transport code were not replaced; NanoHost native HTTP/2 carriage is unchanged. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-115 — The active A1 server-admin plaintext is unavailable

- **Observation:** the next acceptance attempt reached the existing runtime-config owner and received `401 core.auth.unauthenticated`. The root-owned `server-admin-token` file matches the sole rotated `server-admin` predecessor whose grace has expired; Core storage contains one different active `server-admin` record, but its one-time plaintext is absent from `/etc/openkit`, the supported macOS Keychain, the encrypted fallback, and every previously used loopback endpoint entry.
- **Impact:** exact A1 Task and Unit F acceptance; `docs/specs/20260704-remote_auth_credential_bootstrap.md`; the strict credential boundary; and root Authorization and Credential Handling requirements.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved through the engineer-authorized credential reset path owned by the existing server credential manager. The replacement plaintext was used only through the ephemeral operator boundary and was not retained in repository evidence, logs, or documentation. No Better Auth elevation, bootstrap replay, database issuance, or new recovery mechanism was introduced. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved through the engineer-authorized credential reset path owned by the existing server credential manager. The replacement plaintext was used only through the ephemeral operator boundary and was not retained in repository evidence, logs, or documentation. No Better Auth elevation, bootstrap replay, database issuance, or new recovery mechanism was introduced. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-115.
- **Closure evidence:** authenticated A1 App operations succeeded with the replacement credential; signup remained disabled outside the isolated temporary-user preparation; the exact Unit F run and the separate fast Task both completed; and the temporary product session was removed after the user was disabled. Recorded disposition evidence: resolved through the engineer-authorized credential reset path owned by the existing server credential manager. The replacement plaintext was used only through the ephemeral operator boundary and was not retained in repository evidence, logs, or documentation. No Better Auth elevation, bootstrap replay, database issuance, or new recovery mechanism was introduced. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [closed] NHC-FND-116 — The temporary operator held the runner's own SSH-forward port

- **Observation:** the temporary A1 operator opened its health-check SSH forward and kept it alive while synchronously spawning the Unit F runner with the same local port. The runner's first forward could race through the already-open path, but after F1 restarted NanoCore its own forward could not reacquire the occupied port and `startTunnel()` exhausted the owner observation bound. The product Turn continued independently, which made the resulting F1 failure look like a terminal owner or RuntimeTarget defect even though transcript collection and teardown later succeeded.
- **Impact:** the ignored A1 operator under `temp/`; NHC-8 execution method; and root `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** Not recorded.
- **Owner:** Not recorded.
- **Next action:** Recorded transition history: resolved only in the ignored operator by closing the preflight tunnel before invoking the existing Unit F runner. The runner remains the sole owner of its forward for F1 through Aggregate. No product code, committed runner behavior, fixture, timeout, retry, port, durable state, or architecture changed. The unproved RuntimeTarget-wait candidate was explicitly reverted. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved only in the ignored operator by closing the preflight tunnel before invoking the existing Unit F runner. The runner remains the sole owner of its forward for F1 through Aggregate. No product code, committed runner behavior, fixture, timeout, retry, port, durable state, or architecture changed. The unproved RuntimeTarget-wait candidate was explicitly reverted. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-116.
- **Closure evidence:** the focused committed runner suite passes `71/71`; exact A1 commit `902c5011` then completed F1, F2, F3, F4, the ordinary lifecycle, and Aggregate with every verdict `PASS`. The separately invoked `sleep 1` Task completed in 18.875 seconds with three product-safe RuntimeEvidence records and did not gate Unit F. Recorded disposition evidence: resolved only in the ignored operator by closing the preflight tunnel before invoking the existing Unit F runner. The runner remains the sole owner of its forward for F1 through Aggregate. No product code, committed runner behavior, fixture, timeout, retry, port, durable state, or architecture changed. The unproved RuntimeTarget-wait candidate was explicitly reverted. Final accumulated-candidate observation: the plan's NHC-5 and NHC-8 sections record exact A1 commit `902c5011` with F1, F2, F3, F4, the ordinary lifecycle, and Aggregate all `PASS`, one independent final review `PASS`, and `pnpm verify:full` exiting zero.

## [deferred] NHC-FND-117 — A1 cannot retain Scenario 3 host-reboot evidence from its colocated controller

- **Observation:** formal R001 attempt `49de4163-e38d-44fe-8eca-5c5301dfdd5d` reached F3 after a real Worker Turn and invoked the runner's `/usr/bin/systemctl reboot` port; because the controller, Codex App Server, Herdr, and runner all run on A1, the reboot terminated the attempt before terminal finalization and retained-result creation, while the shared kernel boot identity and uptime did not change for the runner's fresh-boot oracle.
- **Impact:** the accepted NanoHost Scenario 3 execution-server restart evidence; Roadmap R005 restart truthfulness; the colocated A1 verification topology; and root `[TEST-012]`, `[SCOPE-004]`, `[QUALITY-003]`, and `[GOV-016]`.
- **Evidence:** the interrupted attempt has no retained result; Herdr recorded orderly process HUP and server shutdown at the reboot boundary; system journal recorded the systemd reboot sequence; the post-restart boot-id and uptime observations remained unchanged; and an independent Cursor CLI Fable 5 consultant concluded that no non-reboot simulation can directly observe machine-wide restart ordering.
- **Owner:** Roadmap R005 and its future accepted restart-truthfulness change.
- **Next action:** Recorded transition history: Intent Epoch 13 removes F3 and every reachable reboot capability from the R001 gate under the engineer's explicit no-reboot boundary, while F1, F2, F4, normal lifecycle, network conformance, and terminal cleanup remain required. Roadmap R005 receives this item when a fresh engineer-approved effect boundary provides either an off-box controller on a suitable execution host or a separately accepted direct non-reboot oracle.

## [closed] NHC-FND-118 — F4 waited for cleanup before starting the successor that owns it

- **Observation:** no-reboot formal attempt `894c6394-281a-4039-a3ff-d328524b216a` reached F4, accepted one `sandbox.create`, and correctly fenced two poll-first `409` successors, but `recoverFreshEpochSequence` waited for backend capacity release before starting the next fresh successor. The exact backend remained `cleanup-failed` and the lease remained acquired until a later ordinary `openkit-nanohost.service` start immediately settled both through the existing recovery owner. The terminal finalizer also recorded `service_stop_failed` because `reset-failed` raced an already inactive static unit and reported it not loaded.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; the retained R001 F4 and terminal-cleanup predicates; and root `[TEST-009]`, `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** retained credential-free artifact digest `7e1a61ec9a9e41ea8986eb45652f6b7517f753ce63933dfc1dcfcff7b87c563b`; one product-safe RuntimeEvidence checkpoint records the accepted result as unknown and the successor fenced; NanoHost journal records the accepted create plus exact terminal poll failures; lease `lease_4086993bf4d2f9e8` changed from acquired to `failed/scheduler-restart-backend-cleanup` and its backend from `cleanup-failed` to `cleaned` only after the fresh project-service successor started; the test User was disabled with zero sessions; and no A1 host reboot occurred.
- **Owner:** the existing Unit F recovery sequence and shared NanoHost stop owner in `tests/support/host/nanohost-unit-f-runner.mjs`.
- **Next action:** Recorded transition history: two exact regressions first failed against the old bytes. The corrected runner starts and proves the fresh successor before awaiting the existing durable cleanup projection, and accepts a `reset-failed` error only after an independent exact-unit `ActiveState=inactive` observation. Independent review found that the positive reset-race test did not exclude broad error swallowing, so one negative regression requires the original error when the exact unit remains active. The three focused regressions pass `3/3`, the combined host projection suite passes `211/211`, targeted Biome, syntax, `pnpm check:repo`, and `git diff --check` pass, and the same independent reviewer returned `PASS`. Retained no-reboot attempt `2e7802af-231d-49c8-9e58-3a8e67b310a5` then accepted F4 and every terminal cleanup row on those exact bytes. No further action remains under this finding.
- **Closing verdict:** Recorded disposition: resolved in the existing Unit F recovery and stop owners without retry, new state, direct database mutation, host reboot, or equivalent reset. Terminal disposition: the recorded disposition and exact observations cited below leave no further work under NHC-FND-118.
- **Closure evidence:** Credential-free retained artifact digest `659afab9691929d38a5dabe29cca44128764dc182aed484f809817e36464f671` records F4 `PASS`, successor generation `98`, terminal service stop, decommission, credential removal, epoch-residue absence, baseline preservation, build-network smoke, and no cleanup reason. NanoHost was independently inactive afterward, both configured credential slots were absent, NanoCore remained running, and A1 was not rebooted.

## [closed] NHC-FND-119 — The first system-Docker smoke ran inside F2's comparison window

- **Observation:** retained no-reboot attempt `2e7802af-231d-49c8-9e58-3a8e67b310a5` completed F1 and F4 with `PASS`, completed F2's fault, recovery, cleanup, and build-network proof, but adjudicated F2 `FAIL` because its pre-baseline digest `56ab894dd1779d92c5897eb09971326c0e8c8a646fe23aed9087b8b9ed334c44` changed to `74179a9e0f1d6c93b729f5648742d56343382b76f33aac877c81954ef300d820`. F4 then began and ended on the latter digest, and normal lifecycle plus terminal cleanup passed. Source order proves F2 was the attempt's first system-Docker smoke, while F4 and normal lifecycle reused it after that one-time initialization.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; the retained R001 F2 and network-conformance predicates; and root `[TEST-009]`, `[TEST-012]`, `[QUALITY-003]`, and `[SCOPE-012]`.
- **Evidence:** credential-free retained artifact digest `659afab9691929d38a5dabe29cca44128764dc182aed484f809817e36464f671`, mode `0600`, records F1 `PASS`, F2 `FAIL`, F4 `PASS`, no terminal cleanup reason, and no raw credential pattern. A bounded alternate-cause probe restarted only `openkit-nanocore-a1` and sampled the exact baseline payload at ready, plus one, plus five, plus fifteen, and plus thirty seconds; all five post-ready samples were identical, excluding delayed F1 network-attachment settlement. NanoHost remained inactive, both credential slots were absent, the disposable User was disabled, and A1 was not rebooted.
- **Owner:** the existing Unit F terminal pre-attempt admission and system-Docker smoke ports in `tests/support/host/nanohost-unit-f-runner.mjs`.
- **Next action:** Recorded transition history: one exact ordering regression first failed with `start -> baseline -> gate`. The candidate calls the existing system-Docker smoke once before admitting the discarded pre-attempt baseline, after which the same F2, F4, normal-lifecycle, and terminal smoke owners remain unchanged. The focused ordering checks pass `2/2`, the combined host projection suite passes `212/212`, targeted Biome, syntax, `pnpm check:repo`, and `git diff --check` pass, and independent review returned `PASS` with no finding; close only after a retained no-reboot A1 Aggregate `PASS` accepts the corrected bytes.
- **Closing verdict:** Resolved by moving the existing system-Docker smoke before admission of the discarded pre-attempt baseline. No scenario retry, alternate runner, durable state, or product behavior was added.
- **Closure evidence:** Retained no-reboot artifact digest `c1e8a70e0b78d39531e76dd9dd92ee630574c2e0d926b6ad1a9b2bce9cbe4d03` records F2 `PASS` with exact equal pre/post raw baseline digests, F4 and normal lifecycle `PASS`, and all terminal cleanup predicates true. Independent artifact audit explicitly accepted this evidence as sufficient to close NHC-FND-119.

## [closed] NHC-FND-120 — F1 accepted an unclassified system-Docker baseline change

- **Observation:** retained no-reboot attempt `e8b3d86d-988b-4978-97dd-f819c59bf29c` reported Aggregate `PASS`, but F1's raw baseline changed from `79ff7b4e61f0e03e9f0f4f18233cb51a40c0f821159ee39866961bcbb2f14974` to `19b38577b6264caa65d369c794b62740a6e0362226dca90bde37a92997a23443`. The adjudicator admitted every F1 mismatch through an unconditional scenario exception, so later equal F2, F4, and normal-lifecycle baselines could not classify the F1 delta or prove full R001 noninterference.
- **Impact:** `tests/support/host/nanohost-unit-f-runner.mjs`; `tests/nanohost-unit-f-runner.test.mjs`; retained F1 truthfulness; Roadmap R001; and root `[TEST-009]`, `[TEST-012]`, `[GOV-017]`, and `[SCOPE-012]`.
- **Evidence:** the audit-block artifact is archived with mode `0600` and digest `c1e8a70e0b78d39531e76dd9dd92ee630574c2e0d926b6ad1a9b2bce9cbe4d03`. Independent audit confirmed its exact identity, scenario, network, lifecycle, and cleanup rows, closed NHC-FND-054 and NHC-FND-119, and blocked only the unclassified F1 change. A bounded diagnostic restarted only `openkit-nanocore-a1`, used the existing complete baseline payload before and after readiness, and observed exactly one changed leaf: the exact `/openkit-nanocore-a1` container's `networks.host.endpointId`; bridge, `docker0`, nftables, every other NanoCore field, and every other container identity and attachment remained equal. A1, Codex App Server, and Herdr were not rebooted.
- **Owner:** the existing system-Docker baseline collector and Unit F scenario adjudicator in `tests/support/host/nanohost-unit-f-runner.mjs`.
- **Next action:** Recorded transition history: two adjudication regressions first failed because the old output exposed only raw digests while accepting every F1 mismatch. The collector now computes both the original raw digest and one restart-invariant digest that normalizes only the unique exact configured NanoCore container's unique `host` network 64-hex endpoint ID; a missing, duplicate, differently networked, or malformed row fails closed, and every other bridge, `docker0`, nftables, identity, image, network, and attachment field remains in the digest. F1 requires equal invariant digests while retaining both raw pre/post values; F2 and F4 still require equal raw digests. Four focused regressions pass, the combined host projection suite passes `215/215`, targeted Biome, syntax, repository, and diff checks pass, and independent review returned `PASS`. Close only after a new retained no-reboot A1 Aggregate `PASS` accepts these exact corrected instrument bytes.
- **Closing verdict:** Resolved by the exact restart-invariant owner and one independently audited retained no-reboot A1 Aggregate `PASS`. No retry mode, alternate runner, product state, compatibility behavior, host reboot, or equivalent reset was added.
- **Closure evidence:** Mode-`0600` retained artifact digest `faa5f7d417ac643998895fc901a480203dd3e9609b080a4c6119a1c5af949072` records F1 `PASS` with equal invariant pre/post digests `7602ee63c71975f5251d352b153756bde0302a684a694c62df93d17f22b13ab2` and retained distinct raw digests, F2 and F4 `PASS` with exact equal raw pre/post digests, network conformance and normal lifecycle `PASS`, and every terminal cleanup predicate true with no primary or cleanup reason. Independent artifact audit matched the current instrument bytes, manifest, product commit, and checkout commit; found no credential-bearing field or reachable host-reset command; and returned `PASS` for closing this finding and Roadmap R001. NanoHost was independently inactive afterward, credential and companion paths were absent, signup-disabled configuration was restored byte-for-byte, and A1 was not rebooted.
