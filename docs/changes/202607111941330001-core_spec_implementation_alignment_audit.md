# Core, Specification, Implementation, And Test Alignment Audit

Type: change-plan
Status: in-progress

## Intent

Audit OpenKit's complete current design hierarchy against its real implementation, tests, and practical product use, then reconcile every confirmed design defect, documentation drift, implementation defect, ownership conflict, test gap, and real-use gap in a controlled sequence.

This is a review-led alignment program rather than a documentation rewrite. Related Core documents, specifications, supporting projections, implementation paths, durable records, and tests are audited as one contract group; no individual document is declared aligned in isolation from the rest of its group.

Redefined on 2026-07-17: this record no longer reconciles every confirmed finding itself. It closes G01 against a frozen list, runs time-boxed review-only passes for the groups without an owning implementation plan, and dispatches everything else to the owning change plans through the [OpenKit Execution Program](./202607172152230001-openkit_execution_program.md). The Convergence Redesign section below is binding for all remaining work under this record.

## Scope

Redefined on 2026-07-17 by the Convergence Redesign. This record now owns exactly three things:

- The bounded G01 closeout defined by the frozen closeout list below. G01 closes when that list is satisfied, not when every recorded defect is resolved.
- Time-boxed, review-only audit passes for the groups without an owning implementation plan: the G00 residual, G04, G05, G08, and G09. These passes classify and ticket findings to the Execution Program backlog; they implement nothing except in-scope `SECURITY-GAP` corrections.
- Program-level bookkeeping: the finding classification, the coverage inventories, the decision and checkpoint history, and the final reconciliation summary.

Audit responsibility for G02, G03, G06, and G07 is dissolved into their owning change plans as a required audit preamble; each of those plans reviews and implements its surfaces exactly once. Earlier scope directives (doctrine ownership, Metering retention, future-concept retention, archived-spec handling, layer placement) remain accepted decisions recorded under Accepted Directional Decisions and the checkpoints; they are no longer open audit work in this record.

## Non-Goals

- Do not remove `docs/core/metering.md` merely because its complete implementation is future work.
- Do not remove deliberate future Core boundaries merely because their specifications or implementations are still being completed.
- Do not assume that the document is correct and the code is wrong; the audit must be willing to classify the design itself as defective or impractical.
- Do not assume that passing unit tests proves the design works in realistic use, under failure, or across public projections.
- Do not create one change record, framework, repository abstraction, or tracking file per audited document.
- Do not rewrite archived specifications unless current authority, historical evidence, or broken links require a targeted correction.
- Do not preserve repository-owned backward compatibility for internal contracts, routes, schemas, layouts, names, or runtime behavior when a clean correction is approved.
- Do not combine unrelated worktree changes or silently override an active overlapping change plan.
- Do not use line count, file count, test count, or document count as a quality target.
- Do not implement any finding outside the frozen G01 closeout list inside this record; classify it and ticket it to the Execution Program backlog.
- Do not extend a frozen closeout list or a review time box after activation; scope changes require an explicit Execution Program ledger edit.
- Do not require byte-exact replay reconstruction or per-command bespoke receipt semantics beyond the central idempotency default.
- Do not apply the "two independent implementers" precision bar to release-coupled surfaces; it is reserved for Durable contract families under S62.

## 2026-07-17 Convergence Redesign

Five days of execution evidence showed that the plan itself, not the audited design, was the main obstacle to convergence. Three structural defects are recorded so they are not repeated:

1. **Absorption.** The audit, chartered as review-led alignment, had become the de facto implementation program for every surface it touched. Its finish line receded as findings accumulated, and its groups duplicated the scope of five already-planned change records, so every surface was set to be worked twice.
2. **Uniform rigor.** Per-command exactness — bespoke exact replay, receipt-body reconstruction, crash-before-receipt reconstruction — was specified and implemented uniformly for every command of a pre-release, single-deployment product whose public surfaces carry no cross-release compatibility promise (S62). The safety-critical subset of that work was valuable and is retained; its uniform application was the primary source of slow convergence.
3. **Ledger overhead.** The per-slice protocol (full group-set re-reads, repeated full-suite runs, long prose checkpoints, and negative-space recitals of what was not added) consumed a large fixed share of every slice.

The binding corrections are the convergence rules in the [OpenKit Execution Program](./202607172152230001-openkit_execution_program.md): no absorption, frozen scope, the central idempotency default, the scoped precision bar, lightweight checkpoints, standing prohibitions, bounded verification, and sequential work packages. Historical checkpoints below remain unchanged evidence; this redesign supersedes only the forward-looking plan sections.

## Related Context

- [OpenKit Execution Program](./202607172152230001-openkit_execution_program.md) — owns work-package order, gates, convergence rules, and the finding backlog.
- [Product Vision](../product-vision.md)
- [Design Roadmap](../roadmap.md)
- [Core Documentation Guide](../core/README.md)
- [Core Agent Rules](../core/AGENTS.md)
- [Specification Guide](../specs/README.md)
- [Specification Agent Rules](../specs/AGENTS.md)
- [Architecture](../core/architecture.md)
- [Work Model](../core/work-model.md)
- [Agent Workflow](../core/agent-workflow.md)
- [Protocol](../core/protocol.md)
- [Change Tracking](../change-tracking.md)
- [Application API Boundary](../app-api.md)
- [Product Design](../../DESIGN.md)
- [Test Strategy](../specs/20260529-test_strategy.md)
- [L6 Story Acceptance](../specs/20260529-l6_story_acceptance.md)
- [NanoCore Maintainability Recovery](./202607111531450001-nanocore_maintainability_recovery.md)
- Concurrent self-improvement implementation plan: `docs/changes/202607111600390001-self_improvement_loop_foundations.md`
- [Spec Lifecycle Governance](./202607111650190001-spec_lifecycle_governance.md)
- [Evidence Surface Simplification](./202607111848520001-evidence_surface_simplification.md)
- Completed worker runtime sub-agent provenance implementation plan: `docs/changes/202607111937290001-worker_runtime_subagent_provenance.md`
- Planned contract stability and multi-user Workspace implementation: `docs/changes/202607160021540001-contract_stability_multi_user_workspaces.md`
- Planned Worker Agent Adapter Boundary implementation: `docs/changes/202607160036500001-worker_agent_adapter_boundary.md`

## Accepted Directional Decisions

1. `docs/core/foundation.md` is the accepted cross-aspect doctrine owner, and `docs/core/metering.md` remains a separate Core aspect.
2. Future concepts intentionally retained in Core must also be represented in the roadmap or an active specification, but they do not need to be removed while implementation is catching up.
3. Foundation owns human final authority, agent execution accountability, Core ownership of durable product truth, observability and reviewability, projection boundaries, source-traceable learning, and explicit security boundaries.
4. Stable semantics to promote into existing Core owners include the complete `StopReason` vocabulary, the four Human Attention categories, generated-knowledge hypothesis semantics, Audit/Usage/Evidence separation, scheduler placement concepts, sandbox freedom principles, Quick Chat workspace semantics, workspace change governance, workspace data source identity, portability invariants, and worker-control authority.
5. Concrete fields, TypeScript and Zod shapes, API routes, transport choices, storage schemas, backend lists, algorithms, error encodings, rollout procedures, and current implementation snapshots belong in specifications, generated contracts, package guides, or change records rather than Core doctrine.
6. Every canonical concept must have one Core owner. Other Core documents may describe relationships but must not redefine the concept.
7. Existing consolidation candidates remain review targets rather than automatic deletions. A specification may be superseded or retired only after the audit proves that another current authority and implementation fully cover its contract.
8. Behavior corrections use tests first, preserve trust-boundary validation and data safety, and follow the repository's no-backward-compatibility rule.
9. Governed workers use one authenticated direct NanoCore `/api/worker-control` connection. The worker capability and MCP contracts remain accepted future design, but current AEPs advertise the capability plane as disabled with no routes; reimplementation must not restore a sidecar or create another control path.

## Current Baseline

The baseline was refreshed from the current worktree on 2026-07-19 after WP-5 exit and the G07 documentation authority correction.

- Core now contains 20 accepted aspect documents. Foundation is the cross-aspect doctrine owner, while Metering remains the separate owner for system-wide measurement principles and non-gateway resource consumption.
- The active specification root contains 65 tracked specifications; 61 are Accepted and 4 are Draft.
- Current implementation alignment is 20 Implemented, 40 Partial, and 5 Not Started. Every Partial or Not Started contract remains explicit owning-plan or audit work.
- Evidence Surface Simplification reconciled Audit/Usage/Evidence records and Workspace Synchronization divergence before this audit begins. The former AI Interface has since been superseded by the accepted Agent Skill Interface, and all current implementation claims still require the evidence-backed review assigned below.
- The active Not Started set contains the accepted Web rebuild plus four Draft specifications: the future Evaluation Harness, recurring scheduler triggers, Skill Catalog, and separately owned verification calibration. The former self-improvement Draft was narrowed to the accepted, partially implemented explicit Knowledge-improvement composition rather than mechanically accepted as an evaluation platform.
- The archive contains 59 lifecycle-classified specifications. They are evidence, not active authority.
- NanoCore Maintainability Recovery is complete and has handed off its settled workspace-review, route, storage, and capability boundaries for independent audit in this ledger.
- Evidence Surface Simplification is verified and has handed off its evidence boundary. Self-Improvement Foundations, Contract Stability and Multi-User Workspaces, and Worker Agent Adapters retain separate planned implementation records whose accepted design scope remains authoritative while this ledger audits coherence and eventual alignment.
- Worker Runtime Sub-Agent Provenance is implemented and handed off; its current claims remain unverified until G03 reviews them with the rest of the runtime and adapter contract group.

## Finding Classification

| Code | Meaning | Required response |
| --- | --- | --- |
| `DESIGN-DEFECT` | The documented model is contradictory, unsafe, needlessly complex, operationally unrealistic, or infeasible in the owning implementation boundary. | Update the owning Core or specification decision first, record the rationale, then implement the corrected contract test-first. |
| `DOC-DRIFT` | The intended design and implementation are sound, but current documentation is stale, incomplete, duplicated, or in the wrong layer. | Update or move the documentation without changing behavior; verify all current links and projections. |
| `IMPLEMENTATION-DEFECT` | The accepted design is sound, but code behavior, persistence, API projection, or runtime handling diverges. | Add a failing regression test, fix the shared root cause, and update current implementation projections. |
| `TEST-GAP` | The design and apparent implementation exist, but the required invariant, failure path, integration, or acceptance behavior is not proved. | Add the smallest test at the lowest reliable L0-L6 layer and prove it fails before any behavior correction. |
| `REAL-USE-GAP` | Deterministic tests pass, but a realistic workflow, operator path, external runtime, or human review scenario is missing or impractical. | Add or update an L3-L6 scenario and fix the product or operational contract that causes the gap. |
| `OWNERSHIP-CONFLICT` | Two documents, packages, modules, records, or routes claim the same authority or can produce conflicting truth. | Choose one owner, remove the duplicate path, and update every projection and test in the same slice. |
| `SECURITY-GAP` | Authority, credential, redaction, policy, sandbox, tenant, or untrusted-input handling is incomplete or fail-open. | Stop the affected slice, add adversarial tests, and correct the trust boundary before continuing. |
| `DEFERRED-ALIGNMENT` | Core or roadmap declares a future boundary that is not yet fully specified or implemented, without contradicting current behavior. | Keep the boundary, ensure roadmap/spec ownership and prerequisites are explicit, and do not fabricate current implementation. |
| `NO-ACTION` | Design, implementation, tests, projections, and actual-use evidence align. | Record the evidence in the owning group and close that group finding. |

## Audit Pass Protocol

Redefined on 2026-07-17. The unit of execution is a bounded pass with a frozen scope — never one document, and never an unbounded group. The reference indexes below prevent omissions and provide stable C, S, and D labels; their rows are coverage inventory, not tasks.

1. Read the documents and code paths named by the pass scope. Do not re-read a full group set per slice; per-slice re-reading covers only the affected documents.
2. Build or update the authority map only for the concepts the pass touches, naming the canonical owner, durable record owners, projections, and forbidden duplicates.
3. Classify findings with the table above. Implement only findings inside the frozen pass scope; ticket everything else to the Execution Program backlog. Only `SECURITY-GAP` findings interrupt the pass.
4. For in-scope behavior corrections: failing test first, smallest cohesive fix at the canonical owner, delete obsolete or duplicate behavior, no compatibility shims. Apply the central idempotency default instead of authoring per-command replay semantics; apply the "two independent implementers" bar only to Durable contract families.
5. Verify with focused suites during the pass, and the affected-package suites plus repository gates at pass close. `verify:full` runs at work-package exit, not per slice.
6. Record one checkpoint per pass close, at most 10 lines or a table row: scope, findings by class, commits, verification, remaining. The Execution Program's standing prohibitions replace per-slice recitals of what was not added.

## Contract Group Audit Ledger

Each document appears in exactly one primary document set below. Cross-group interfaces are reviewed by both affected groups, but authority and completion remain with the named primary group.

### Group Dispositions (2026-07-17 Redesign)

This disposition table is the current execution truth and supersedes the `State` column of the reference table below. Dissolved groups are audited and implemented exactly once inside their owning change plan, which inherits the group's document set and exit criteria as its audit preamble. The preamble's required content, bound, and timing are defined by Execution Program rule 11, and each owning plan carries an Inherited Audit Responsibility section recording that duty.

| Group | Disposition | Where |
| --- | --- | --- |
| G00 | Residual review-only time box; preflight, Foundation, C01, C02, and C03 work already completed | WP-7 |
| G01 | Closed against the frozen list; WP-0 complete | WP-0 (complete) |
| G02 | Closed after the Agent Skill Interface replacement and clean removal passed | WP-1 (complete) |
| G03 | Closed after the Worker Agent Adapter Boundary implementation, bounded A1 stock-OpenShell evidence, and local exit gates passed; explicitly dispatched consolidation and deletion reviews remain review-only | WP-2 (complete), then WP-7 residual review |
| G04 | Closed after the bounded provider review, finding dispatch, and frozen security corrections passed | WP-3 (complete) |
| G05 | Closed after the bounded storage review, Material-writeback owner decision, finding dispatch, and frozen security corrections passed | WP-3 (complete) |
| G06 | Closed through the verified Contract Stability and Multi-User Workspaces plan | WP-5 (complete) |
| G07 | Dissolved into the Self-Improvement Loop Foundations plan | WP-6 |
| G08 | Review-only time box | WP-7 |
| G09 | Review-only time box; preamble to the Web rebuild | WP-7 |

### Group Scope And Exit-Criteria Reference

The table below is retained as the criteria reference that owning work packages inherit; its `State` column is historical.

| Group | Contract scope | Primary document set | Group-level review and exit condition | State |
| --- | --- | --- | --- | --- |
| G00 | Doctrine, architecture, hierarchy, and contract governance | [G00 document set](#g00-document-set) | Establish one canonical owner per concept, keep Core short and hard, keep Specs precise and narrow, preserve decision-grade criteria, and ensure roadmap and lifecycle governance expose deliberate future boundaries without authorizing implementation. | Queued: the selective-rehydration and group-execution preflight is complete, but the full group is not active or closed |
| G01 | Work semantics, workflow coordination, human attention, and mode behavior | [G01 document set](#g01-document-set) | Audit and update the complete group together across protocol work records, App API schemas, NanoCore mode, Coordinator, scheduler, worker-turn, checkpoint, receipt, Action Center, Artifact and Material paths, generated contracts, Core Client, Skill and CLI, Web, removal-only MCP consumers, and L1-L6 tests; close only when authority, lifecycle, failure and recovery, and observable acceptance agree without a second workflow engine. | Closed through WP-0 and the WP-4 Stage 0-4 kernel package; the accepted S16 rebuilt-Web projection and Web-dependent acceptance are scheduled with post-program S10 after G09 rather than keeping this audit group open |
| G02 | Protocol, communication, public App API, Core Client, and end-user Agent Skill surface | [G02 document set](#g02-document-set) | Prove one schema and command authority, transport-neutral operations, authentication, idempotency, errors, replay, cancellation, generated-contract parity, and removal of legacy user-facing MCP and split Skills. | Pending |
| G03 | Agent supply, runtime, session, scheduler, sandbox, worker control, worker capability, and adapters | [G03 document set](#g03-document-set) | Prove declared-to-resolved-to-materialized execution, one control plane, exact leases and recovery, bounded scheduler ownership, stock OpenShell, sandbox enforcement, adapter isolation, disabled-until-proven capabilities, and no replacement runtime or test platform. | Closed with WP-2; residual consolidation and deletion review remains scheduled in WP-7 |
| G04 | Provider routing, model backends, credentials, capability accounting, and metering | [G04 document set](#g04-document-set) | Prove provider authorization is owned outside adapters, one non-Codex backend, isolated credentials, streaming and cancellation semantics, usage/cache attribution, vendor pinning, real-provider evidence, and no duplicate client path. | Closed with WP-3; scheduled non-security findings remain with their Backlog owners |
| G05 | Storage, Workspace data, synchronization, Git, backup, export, import, and portability | [G05 document set](#g05-document-set) | Prove one writer and source of truth per record family, owner-independent Workspace layout, atomic and rebuildable persistence, isolated review/apply, unrelated-Git preservation, recovery, corruption handling, and portable secret-free transfer. | Closed with WP-3; the Material owner is decided and scheduled non-security findings remain with their Backlog owners |
| G06 | Identity, bootstrap, policy, authorization, Vault, and multi-user Workspace access | [G06 document set](#g06-document-set) | Prove one-deployment trust-domain semantics, fail-closed actor and membership resolution, exact policy enforcement, secure bootstrap and token lifecycle, Vault isolation and cleanup, redaction, and no second tenancy or RBAC engine. | Closed through verified WP-5; the rebuilt multi-user Web remains an accepted S10 projection rather than an open G06 kernel criterion |
| G07 | Knowledge, Knowledge Manager, and governed self-improvement | [G07 document set](#g07-document-set) | Prove source-traceable Knowledge authority, exact operation callers, proposal-only generated change, deterministic retrieval into the G01-owned Context Package trace, explicit human evaluation authority, bounded reversal, and no passive agent framework. | Stage 0, WP-5A, and retained-history entry gates complete; WP-6 Stage 1 active |
| G08 | Audit, Usage, Evidence, capability accounting, and L0-L6 acceptance | [G08 document set](#g08-document-set) | Prove distinct record meanings and owners, complete trusted producers, idempotent lineage, redaction and retention, realistic failure evidence, honest skip semantics, and the smallest sufficient acceptance harness. | Pending |
| G09 | Web and remaining repository projections | [G09 document set](#g09-document-set) | Project settled contracts without owning workflow state, verify accessibility and error states, keep package and cookbook guidance executable, and delete stale stack or setup paths rather than preserving compatibility guidance. | Pending |

Sequencing is owned by the Execution Program work-package queue. G01/WP-0, G02/WP-1, G03/WP-2, the bounded G04/G05 WP-3 review boxes, the WP-4 Stage 0-4 kernel package, WP-5/G06, G07 Stage 0, and WP-5A are complete. The retained-history entry gate passed and WP-6 Stage 1 is active. The accepted S16 and rebuilt multi-user Web projections remain assigned to post-program S10 after G09.

## Contract Group Document Sets

The sets below are the complete primary document assignment for this audit. The linked Core and specification documents retain their decision-grade criteria; this change plan records only the combined review scope, findings, implementation work, and evidence for the owning group. A group may inspect a neighboring document as an interface, but it must not create a second completion stream or update that document outside a combined group finding. The sets of dissolved groups (G02, G03, G06, G07) are consumed by their owning work packages' audit preambles and are retained here only as the assignment inventory.

### G00 Document Set

- Core: C00 [Foundation](../core/foundation.md), C01 [Core Concepts](../core/core-concepts.md), C03 [Architecture](../core/architecture.md), and C18 [Contract Evolution](../core/contract-evolution.md).
- Specifications: S62 [Contract Stability Baseline](../specs/20260715-contract_stability_baseline.md).
- Supporting documents: [repository README](../../README.md), [Product Vision](../product-vision.md), [Roadmap](../roadmap.md), [root agent rules](../../AGENTS.md), [Contributing](../../CONTRIBUTING.md), [Template Overview](../template-overview.md), [Change Tracking](../change-tracking.md), [change records](./), and archived `docs/working_logs/` entries when present.

### G01 Document Set

- Core: C02 [Work Model](../core/work-model.md) and C06 [Agent Workflow](../core/agent-workflow.md).
- Specifications: S05 [Worker Turn Reliability Envelope](../specs/20260531-worker_turn_reliability_envelope.md), S06 [Human Attention Intervention Model](../specs/20260531-human_attention_intervention_model.md), S11 [Chat Mode Assistant](../specs/20260704-chat_mode_assistant.md), S12 [Task Mode Worker Delegation](../specs/20260704-task_mode_worker_delegation.md), S13 [Goal Mode Coordination](../specs/20260704-goal_mode_coordination.md), S14 [Quick Chat Workspace](../specs/20260709-quick_chat_workspace.md), S15 [Workflow Coordinator Internal Core Role](../specs/20260704-workflow_coordinator_internal_agent.md), S16 [Work And Resource Interaction Model](../specs/20260713-work_resource_interaction_model.md), and S39 [Worker Context Package](../specs/20260703-worker_context_package.md).
- Required interface documents in the same grouped review are the [App API boundary](../app-api.md), S37 [Worker Control Protocol](../specs/20260703-worker_control_protocol.md), S46 [Storage Layout And Record Ownership](../specs/20260703-storage_layout_record_ownership.md), S49 [Workspace Synchronization](../specs/20260703-workspace_synchronization.md), and S51 [Workspace Backup, Export, And Import](../specs/20260704-workspace_backup_export_import.md). They remain primary members of G02, G03, or G05, so G01 updates only the shared boundary and does not close those groups.
- Other supporting projections are reviewed through the implementation surfaces named in the G01 ledger row; their primary documentation ownership remains with G02, G03, G08, or G09.

#### G01 Frozen Closeout List (2026-07-17)

This list is frozen by the Convergence Redesign. New findings go to the Execution Program backlog. G01 closes when every Class A item is verified and Classes B and C are recorded exactly as below — not when every recorded defect is resolved. Per-slice re-reading covers the affected documents only.

Class A — safety, authority, and data-loss items; finish inside G01:

1. Done 2026-07-18: Goal worker user-input and approval, policy-backed, and exact AEP-backed Gate families close through structured answers, exact Item and `humanGate` lineage, Core-owned Goal, Task, checkpoint, Turn, Session, scheduler, and receipt owners, winner-only replay, and fail-closed unsupported states. A runtime that reports raw `blocked/ask_user` without a named Core Gate uses the bounded interruption and `recovery_required` fallback instead of synthesizing one.
2. Done 2026-07-18: boot fences scheduler and worker-control ownership first, then classifies every uncleared Task or Goal checkpoint through the same mode-owned closeout contract; live work is preserved and contradictory tuples remain discoverable as `recovery_required`.
3. Done 2026-07-18: all remaining read, action, and retry paths were checked against the existing interrupted-worker retry predicate; no bypass remains.
4. Done 2026-07-18: the generic Artifact metadata `PATCH`, unversioned review route, Workspace Review identifier-prefix inference, and generic-to-Workspace Review verdict translation are deleted. The replacement command surface remains Class C.
5. Done 2026-07-18: the central idempotency default is normative in C07 Protocol (receipt-and-current-owner replay, `idempotency_key_conflict`, missing-receipt `recovery_required`, and the named `chat.start` closed-outcome exception) and S62 (Precision And Reliability Scope plus the shared-write family). NanoCore enforces exact receipt response fields and bounded `chat.start` metadata at the shared in-memory and SQLite storage boundary.

Class B — downgraded to the central idempotency default; recorded as the accepted v1 posture, not implemented per command:

6. Per-command exact-replay refinement beyond the central default, including further receipt-body narrowing and response reconstruction. Fail-closed `recovery_required` is the accepted posture.
7. Crash-before-receipt reconstruction via request-derived deterministic identities and deterministic missing-receipt publication beyond what has already landed. Fail-closed `recovery_required` is the accepted posture; revisit only on real-use evidence of operational pain, through a Backlog entry. The direct-Task launch-fence and missing-receipt slice that was in flight on 2026-07-17 (request id and canonical input hash written into the existing checkpoint, command-identity-derived Turn lineage, owner-tuple replay validation) finished at its natural commit boundary and counts as part of the already-landed baseline; it is not a precedent for further per-command work.

Class C — evicted to owning work packages per the Execution Program eviction map:

8. S16 Material identity, immutable revision, Thread binding, inclusion queue, reads, typed expected-base rejection, restart behavior, and portable row export (WP-4).
9. The S39 Context Package trace, Goal steering delivery, and `lastWorkerSeenRevisionId`; until then Goal steering remains the zero-write `goal_steering_delivery_unavailable` rejection (WP-4).
10. The exact Artifact command surface: introduction, workspace-only import, and deterministic refinement or redo through existing Turn and Review owners (WP-4).
11. Material worker proposal and conflict-safe writeback: WP-3/G05 selected the exact immutable turn-output Artifact version plus its existing version-keyed `ArtifactReview` and one `workspace.sqlite` Material transaction as the proposal, decision, apply, replay, and recovery authority; S16 records the closed contract. Implementation remains in WP-4, with no Workspace Sync alias, merge/rebase, or new proposal, apply-result, settlement, or recovery record.
12. Final generated-contract, Core Client, unified Skill and CLI, Web, Action Center, and portability projections plus minimal acceptance for items 8-11, delivered with their owning features.

### G02 Document Set

- Core: C07 [Protocol](../core/protocol.md), C08 [Communication](../core/communication.md), and C18 [Contract Evolution](../core/contract-evolution.md).
- Specifications: S02 [Core Client Boundary](../specs/20260528-core_client_boundary.md), S03 [App API OpenAPI Projection](../specs/20260704-app_api_openapi_projection.md), and S04 [OpenKit Agent Skill Interface](../specs/20260713-openkit_agent_skill_interface.md). S01 [Protocol Contract Consolidation](../specs/superseded/20260628-protocol_contract_consolidation.md) was superseded during G02 after its continuing contracts were routed to C07, C08, C18, `@openkit/protocol`, S02-S04, App API, and NanoCore owners.
- Supporting documents: [Application API Boundary](../app-api.md), generated OpenAPI and API-facing guides, the legacy MCP guidance deleted with WP-1, and [Skill guidance](../../skills/README.md).

### G03 Document Set

- Core: C04 [Runtime Model](../core/runtime-model.md), C05 [Agent Session](../core/agent-session.md), C13 [Agent Supply](../core/agent-supply.md), and C16 [Sandbox](../core/sandbox.md).
- Specifications: S20 [Agent Environment Package](../specs/20260616-agent_environment_package.md), S22 [Agent Manifest And AEP Resolution](../specs/20260703-agent_manifest_aep_resolution.md), S23 [Session Static Workspace Materialization](../specs/20260704-session_static_workspace_materialization.md), S24 [Agent Session Continuity](../specs/20260704-agent_session_continuity.md), S25 [Worker Runtime Communication Model](../specs/20260629-worker_runtime_communication_model.md), S26 [OpenShell Disposable Cell Lifecycle](../specs/20260715-openshell_disposable_cell_lifecycle.md), S27 [OpenShell Mechanism Internalization](../specs/20260703-openshell_mechanism_internalization.md), S28 [Container Image Packaging](../specs/20260708-container_image_packaging.md), S29 [Runtime Scheduling Scale](../specs/20260703-runtime_scheduling_scale.md), S30 [Durable Scheduler Design](../specs/20260703-durable_scheduler_design.md), S31 [Worker Sandbox Freedom Policy](../specs/20260709-worker_sandbox_freedom_policy.md), S32 [Worker Credential Access Declarations](../specs/20260709-worker_credential_access_declarations.md), S33 [Worker Runtime Sub-Agent Provenance And Inference Identity](../specs/20260711-worker_runtime_subagent_provenance.md), S34 [Skill Catalog Versioning And Pinning](../specs/20260711-skill_catalog_versioning_pinning.md), S35 [Scheduler Recurring Event Triggers](../specs/20260711-scheduler_recurring_event_triggers.md), S36 [Worker Agent Capability](../specs/20260703-worker_agent_capability.md), S37 [Worker Control Protocol](../specs/20260703-worker_control_protocol.md), S38 [Worker MCP Tool Supply](../specs/20260704-worker_mcp_tool_supply.md), S64 [Codex Worker Adapter](../specs/20260716-codex_worker_adapter.md), S65 [OpenCode Worker Adapter](../specs/20260716-opencode_worker_adapter.md), and S66 [Pi Worker Adapter](../specs/20260716-pi_worker_adapter.md). S21 [Agent Setup Runtime Supply Contract](../specs/superseded/20260628-agent_setup_runtime_supply_contract.md) was superseded during the WP-2 preamble after its continuing contracts moved into C13, S20, S22, S23, and S25.
- Supporting documents: [Deployment](../deployment.md), NanoCore deployment-mode and release cookbooks, and [NanoCore guidance](../../apps/nanocore/README.md).

### G04 Document Set

- Core: C14 [Agent Capability](../core/agent-capability.md) and C19 [Metering](../core/metering.md).
- Specifications: S40 [LLM Gateway Responses API](../specs/20260526-llm_gateway_responses_api.md), S41 [Pi AI Provider Gateway Adoption](../specs/20260703-pi_ai_provider_gateway_adoption.md), S42 [Unified Pi AI LLM Backend](../specs/20260708-pi_ai_unified_llm_backend.md), S44 [Codex ChatGPT Subscription Login](../specs/20260526-codex_chatgpt_subscription_login.md), and S45 [Vendor Snapshot Packages](../specs/20260522-vendor_snapshot_packages.md).
- Supporting projections are reviewed with their owning provider, protocol, package, deployment, and product surfaces without creating another provider contract.

### G05 Document Set

- Core: C09 [Storage](../core/storage.md).
- Specifications: S46 [Storage Layout And Record Ownership](../specs/20260703-storage_layout_record_ownership.md), S47 [Schema Evolution Record Envelope](../specs/20260703-schema_evolution_record_envelope.md), S48 [Workspace Data Source Catalog](../specs/20260704-workspace_data_source_catalog.md), S49 [Workspace Synchronization](../specs/20260703-workspace_synchronization.md), S50 [Git Write Workflow](../specs/20260704-git_write_workflow.md), and S51 [Workspace Backup, Export, And Import](../specs/20260704-workspace_backup_export_import.md).
- Supporting projections are reviewed across storage maps, repositories, backup and portability guides, public operations, and realistic cross-machine evidence.

### G06 Document Set

- Core: C10 [Identity](../core/identity.md), C11 [Vault](../core/vault.md), and C15 [Permissions](../core/permissions.md).
- Specifications: S52 [NanoCore Config And Identity Contract](../specs/20260628-nanocore_config_identity_contract.md), S53 [Remote Auth Credential Bootstrap](../specs/20260704-remote_auth_credential_bootstrap.md), S54 [NanoCore Bootstrap Readiness](../specs/20260704-nanocore_bootstrap_readiness.md), S55 [OpenKit Policy Model](../specs/20260629-openkit_policy_model.md), S56 [Policy Enforcement Mapping](../specs/20260703-policy_enforcement_mapping.md), S57 [Vault Secret Injection](../specs/20260703-vault_secret_injection.md), S58 [Vault Backend Implementation](../specs/20260704-vault_backend_implementation.md), and S63 [Single-Deployment Multi-User Workspace System](../specs/20260715-multi_user_workspace_system.md).
- Supporting projections are reviewed across bootstrap, auth, membership, policy, Vault, CLI and Skill, Web, deployment, redaction, backup, and audit surfaces.

### G07 Document Set

- Core: C12 [Knowledge](../core/knowledge.md).
- Specifications: S17 [Knowledge Manager Service](../specs/20260704-knowledge_manager_internal_agent_runtime.md), S18 [Explicit Work Reflection And Knowledge Improvement](../specs/20260710-self_improvement_evaluation_loop.md), S19 [Evaluation Harness Design](../specs/20260711-evaluation_harness_design.md), S60 [Knowledge Store Governance Rules](../specs/20260702-knowledge_store_governance_rules.md), and S61 [Knowledge Store Implementation](../specs/20260703-knowledge_store_implementation.md).
- Supporting projections are reviewed across Knowledge storage, retrieval and selection into the accepted G01 Context Package interface, evaluation, public inspection, self-improvement change records, and rollback evidence.

### G08 Document Set

- Core: C17 [Audit](../core/audit.md).
- Specifications: S07 [Test Strategy](../specs/20260529-test_strategy.md), S08 [L6 Story Acceptance](../specs/20260529-l6_story_acceptance.md), S43 [Capability Usage Gateway Foundation](../specs/20260704-capability_usage_gateway_foundation.md), and S59 [Audit, Usage, And Evidence Records](../specs/20260703-audit_usage_evidence_records.md).
- Supporting documents: story suites and runner guidance under `tests/stories/`, acceptance documentation, repository verification gates, and real-runtime evidence handling.

### G09 Document Set

- Core authority is consumed from the settled neighboring groups; Web and repository guidance remain projections and must not create another Core owner.
- Specifications: S09 [Web Product Surface Projection](../specs/20260628-web_product_surface_projection.md) and S10 [Web UI Rebuild Stack](../specs/20260710-web_ui_rebuild_stack.md).
- Supporting documents: [Product Design](../../DESIGN.md), Web design and cookbooks, [Web guidance](../../apps/web/README.md), [package guidance](../../packages/README.md) and all package-local guides, and [cookbooks](../cookbooks/).

## Coverage Appendix: Core Documents

The following rows preserve the decision-grade criteria and pre-group evidence that must not be lost during compression. They are a coverage appendix, not an ordered audit list: never traverse, schedule, update, close, or report them as independent document tasks.

| ID | Document | Primary implementation and test surfaces | Required review focus | Pre-group snapshot, not an execution state |
| --- | --- | --- | --- | --- |
| C00 | [Foundation](../core/foundation.md) | `README.md`, `docs/product-vision.md`, root and local `AGENTS.md`, Core index and direct doctrine projections | Verify Foundation's own authority and non-duplication boundary; keep product positioning, aspect definitions, package architecture, concrete fallback mechanics, and implementation detail with their owning documents. | Completed 2026-07-16: direct authority aligned; four implementation findings routed to owning rows |
| C01 | [Core Concepts](../core/core-concepts.md) | `packages/protocol/src/models/*`, `packages/protocol/src/common/ids.ts`, App API schemas, NanoCore storage and public records | Reduce duplicate definitions to owner links; verify the backbone and ID semantics; move manifest parsing, concrete ID registry mechanics, and schema-evolution implementation detail downward while preserving intentional reserved future concepts. | Completed 2026-07-16: authority narrowed and exact Turn assignment aligned; item-backed Artifact lineage routed to C02 and S16 |
| C02 | [Work Model](../core/work-model.md) | Protocol work records, NanoCore stores and route flows, Action Center, Chat/Task/Goal/Quick Chat, Agent Skill CLI and Web projections | Promote the four Human Attention categories and Quick Chat workspace semantics; preserve human acceptance; move exact item and Action Center shapes downward; test whether real workflows remain expressible without hidden product state. Resolve C01-F02 by reconciling Artifact creation with the accepted item-backed lineage invariant. | Completed 2026-07-16: authority and Artifact lineage aligned; unsupported steering fails closed; remaining workspace-only introduction and invalid-lineage behavior stay owned by S16 |
| C03 | [Architecture](../core/architecture.md) | Package import graph, NanoCore composition, protocol, Core Client, Agent Skill CLI, Web, worker and provider adapters | Verify module authority and internal roles; retain and roadmap the Generative Kernel and active Task Evaluator direction; move deployment, backend, record-layout, and role-procedure details downward; identify implementation boundary violations. | Pre-group evidence refreshed: generic internal-role runtime and diagnostics projection deleted; full G00 architecture group audit remains queued |
| C04 | [Runtime Model](../core/runtime-model.md) | `apps/nanocore/src/runtime/*`, scheduler records and services, worker protocol, worker shim, runtime tests and e2e | Promote stable placement concepts; verify Turn, AgentSession, Worker, runtime and scheduler relationships; remove private backend topology from stable product projections; move exact trigger and lifecycle encodings downward; test concurrency, leases, restart and failure semantics. | Pending |
| C05 | [Agent Session](../core/agent-session.md) | Protocol agent models, `agent-session-continuity.ts`, scheduler leases, checkpoints, recovery, session snapshots | Preserve continuity and compatibility invariants; keep backend-native session identity private; move exact state and compatibility-key shapes downward; verify resume, replace, fork, crash, rollback, static supply and dynamic turn behavior against real runtimes. | Pending |
| C06 | [Agent Workflow](../core/agent-workflow.md) | Internal Core Roles, Chat/Task/Goal mode modules, goal runtime modules, Action Center, workspace review and evidence | Own coordination, acceptance and workspace-change governance; retain roadmap-backed graph and recipe direction; prove accepted Goal delegation boundaries reach the real worker turn; test bounded steps, handoffs, gates, refinement, stop and evidence behavior. | Pending |
| C07 | [Protocol](../core/protocol.md) | `@openkit/protocol`, generated JSON Schema, NanoCore events and replay, Core Client, App API schemas, Agent Skill CLI and Web consumers | Promote complete StopReason semantics; keep authority, ordering, replay, terminal, idempotency and error principles; move exact payloads, routes, HTTP/SSE behavior, code types and authoring stack downward; verify conformance and unknown-feature handling. | Pending |
| C08 | [Communication](../core/communication.md) | Core Client transport/SSE, Agent Skill CLI process contract, worker protocol, worker control gateway, worker shim, OpenShell bridge | Promote worker-control authority; preserve plane boundaries; move concrete transports, commands, endpoint mappings, deployment topology and retry algorithms downward; test disconnect, replay, duplication, partial delivery and redaction. | Pending |
| C09 | [Storage](../core/storage.md) | NanoCore DB, migrations, filesystem layout, store, workspace export/import, backup/restore, indexes | Promote WorkspaceDataSource and portability invariants; preserve ownership and rebuildability; move SQLite, table, compaction, FTS/vector and backend choices downward; test crash consistency, atomicity, corruption, export and restore. | Pending |
| C10 | [Identity](../core/identity.md) | Better Auth, local/server identity, bootstrap and access tokens, workspace membership, auth middleware | Preserve credential-family and isolation semantics; keep team tenancy roadmap-backed; move exact membership records and status shapes downward; test local/server actors, token lifecycle, cross-workspace access and future membership boundaries. | Pending |
| C11 | [Vault](../core/vault.md) | Vault backends, references, grants, use records, injection plans and receipts, provider credential resolver | Preserve prohibited secret surfaces and explicit injection authority; move backend and target mappings downward; test locking, revocation, expiry, rebind, redaction, failed injection and secret absence from all public/evidence surfaces. | Pending |
| C12 | [Knowledge](../core/knowledge.md) | OKF storage, Knowledge Manager, search, retrieval, context packages, proposal and conflict ledgers | Promote generated-knowledge hypothesis semantics; preserve source traceability, human override, recoverable proposal application, and exact reversal lineage; move pipeline, page, ingest, retrieval and UI detail downward; test stale, conflicting, sensitive, invalid and adversarial knowledge. | Pending |
| C13 | [Agent Supply](../core/agent-supply.md) | Config schemas, agent manifest and setup resolver, readiness, AEP resolution, supply catalogs and snapshots | Own AgentProfile, AgentCatalog, entry, setup and readiness concepts; move exact catalogs, enums and diagnostics downward; verify declared versus resolved versus materialized supply and future Skill pinning alignment. | Pending |
| C14 | [Agent Capability](../core/agent-capability.md) | Protocol capability/usage models, NanoCore capability ledger, current LLM gateway, and future worker capability/MCP gateway policy | Own CapabilityCall and UsageRecord semantics; preserve authorization, attribution and audit links; move fields, routing and deployment downward; test denied, failed, retried, partial and billable calls plus future budgets. | Pending |
| C15 | [Permissions](../core/permissions.md) | `@openkit/policy-kernel`, config policy schemas, permission decisions, approval gates, auth and App API enforcement | Preserve NGAC direction, default-deny and fail-closed authority; move exact decision fields and enforcement mappings downward; test policy conflict, stale decisions, revoked authority, cross-scope access and audit linkage. | Pending |
| C16 | [Sandbox](../core/sandbox.md) | OpenShell policy and backend, AEP materialization, worker governance, worker shim, container packaging | Promote broad process freedom with strict filesystem, network, credential and review boundaries; retain future summary direction; move backend lists and exact policy shapes downward; test escape, exfiltration, unavailable controls and degraded backends. | Pending |
| C17 | [Audit](../core/audit.md) | Protocol audit models, NanoCore audit writers, EvidenceBundle, RuntimeEvidence, usage ledger, export/import | Own Audit/Usage/Evidence separation and evidence authority; move exact record and retention schemas downward; verify complete producer coverage, idempotence, redaction, scope homing, quarantine and proof claims. | Pending |
| C18 | [Contract Evolution](../core/contract-evolution.md) | Protocol conformance, config schema evolution, import/export readers, worker protocol, generated schemas, lifecycle validator | Preserve additive and fail-closed evolution rules; move current removed fields, fixtures, loader errors and release snapshots downward; test unknown optional and authority-bearing features at every trust boundary. | Pending |
| C19 | [Metering](../core/metering.md) | Protocol usage records, capability usage ledger, LLM and future MCP usage producers, terminal runtime, workspace transfer, Git publication and scheduler evidence | Preserve the current runtime, storage, and network measurement foundation without collapsing Usage into billing; audit aggregation, duration, compute, retained storage, traffic, budgets, and cost projection only when real producers exist. | Initial producer boundary aligned; expansion audit pending |

## Coverage Appendix: Active Specifications

Every active specification is listed exactly once so group review cannot omit a contract. Status and implementation values are baseline evidence, not per-document work state; change them only inside the owning group review.

### A. Protocol, Public API, Product Projection, And Test Contracts

| ID | Specification and current alignment | Primary implementation and test surfaces | Required review focus | Pre-group snapshot, not an execution state |
| --- | --- | --- | --- | --- |
| S02 | [Core Client Boundary](../specs/20260528-core_client_boundary.md), Accepted / Implemented | `packages/core-client/src/*`, package tests, Web, bundled CLI, and NanoCore black-box fixtures | Confirm the client is a thin typed public boundary, transport and errors are coherent, no NanoCore internals leak, and every public method has schema, failure and cancellation coverage. | Pending |
| S03 | [App API OpenAPI Projection](../specs/20260704-app_api_openapi_projection.md), Accepted / Implemented | App API schemas, NanoCore route registration, `openapi.ts`, generated artifact, Core Client and route/OpenAPI tests | Prove bidirectional route/OpenAPI parity, one schema owner, redaction and auth metadata, current generation reproducibility, and absence of hand-maintained competing inventories. | Pending |
| S04 | [OpenKit Agent Skill Interface](../specs/20260713-openkit_agent_skill_interface.md), Accepted / Implemented | Unified end-user Skill, bundled CLI, App API OpenAPI catalog, Core Client, shared schemas, NanoCore public routes and replacement stories | Verify progressive disclosure, complete public user/operator capability coverage, single ownership of route and payload contracts, secret and mutation boundaries, worker-MCP separation, and clean deletion of the user-facing MCP and four legacy Skills. | Completed 2026-07-18 with WP-1/G02 |
| S05 | [Worker Turn Reliability Envelope](../specs/20260531-worker_turn_reliability_envelope.md), Accepted / Partial | Protocol turn/events, worker turn loop, checkpoints, transcript import, host adapters, SSE replay and recovery tests | Verify one terminal outcome, complete StopReason mapping, interruption, continuation, checkpoint ordering, duplicate completion, partial streams, restart, adapter errors and evidence closeout. | Pending |
| S06 | [Human Attention Intervention Model](../specs/20260531-human_attention_intervention_model.md), Accepted / Partial | Protocol gates/items, Action Center, pending user turns, approval policy, Chat/Task/Goal, Agent Skill CLI and Web projections | Reconcile four intervention categories, blocking semantics, steering, review acceptance, row-kind ownership and active Draft dependencies; cover stale gates, repeated decisions, concurrent human action and recovery. | Pending |
| S07 | [Test Strategy](../specs/20260529-test_strategy.md), Accepted / Implemented | Root scripts, package tests, NanoCore e2e, Web e2e, smoke tests, story runner and CI | Audit whether L0-L6 definitions match actual scripts and failure coverage; remove redundant gates, close missing trust-boundary and real-runtime layers, and ensure deterministic tests do not impersonate L6 proof. | Pending |
| S08 | [L6 Story Acceptance](../specs/20260529-l6_story_acceptance.md), Accepted / Partial | `tests/stories/*`, `tests/story-runner/*`, unified Skill/CLI stories, real Codex/provider/task runners, and Web stories | Verify story metadata, agent-first acceptance, environment preflight, skip semantics, evidence capture, real failure reporting and regression reduction into L1-L5. | Pending |
| S09 | [Web Product Surface Projection](../specs/20260628-web_product_surface_projection.md), Accepted / Partial | Current Web app, Core Client, App API schemas, NanoCore APIs, Web tests and README | Verify the now-durable Goal Reject and Revise path and all remaining projection rules; move valid guidance to Web README, and supersede only after the rebuild contract lands and no independent design authority remains. | C03 prerequisite evidence recorded for G09 |
| S10 | [Web UI Rebuild Stack](../specs/20260710-web_ui_rebuild_stack.md), Accepted / Not Started | `apps/web`, Core Client, app schemas, UI cookbooks, component and Playwright tests | Pressure-test framework, design-system, migration and accessibility choices against product needs and stable APIs; verify the rebuild can proceed without making Web a second workflow owner. | Pending |
| S62 | [Contract Stability Baseline](../specs/20260715-contract_stability_baseline.md), Accepted / Partial | Core contract owners, protocol and App API schemas, generated contracts, Core Client, bundled CLI, unified Skill, Web, migrations and conformance gates | Include C00-F10 when verifying that private Gateway and sandbox topology stays outside stable product projections; verify each Durable and release-coupled family against one exact current owner and readiness gate without inventing compatibility windows. | Pending |

### B. User Workflows, Internal Core Roles, And Evaluation

| ID | Specification and current alignment | Primary implementation and test surfaces | Required review focus | Pre-group snapshot, not an execution state |
| --- | --- | --- | --- | --- |
| S11 | [Chat Mode Assistant](../specs/20260704-chat_mode_assistant.md), Accepted / Partial | Chat Mode App API schemas/routes, direct Quick Chat provider call, mode services, Core Client, MCP, focused and story tests | Verify lightweight assistant behavior, worker prohibition, context selection, persistence, stop and error behavior, and distinction from Quick Chat workspace kind and Task Mode. | Direct provider boundary verified; failure persistence and command half-state findings remain in G01 |
| S12 | [Task Mode Worker Delegation](../specs/20260704-task_mode_worker_delegation.md), Accepted / Partial | Task Mode schema/routes, worker coordinator, goal-task delegation, scheduler/runtime, MCP and real/deterministic Task stories | Verify bounded scope, target selection, escalation, context refs, stop conditions, evidence, failures, explicit retries, human gates and no hidden Goal lifecycle. | C03 delivery and command-replay findings carried into G01 |
| S13 | [Goal Mode Coordination](../specs/20260704-goal_mode_coordination.md), Accepted / Partial | `goal-*` runtime modules, Action Center, schemas/routes, Core Client, MCP, Web and Goal stories | Resolve C00-F08 by proving the complete accepted delegation reaches the real worker turn; trace objective, planning, approval, bounded steps, steering, verification, refinement and closeout; test concurrent actions, crashes, stale plans, worker failure and incomplete evidence. | Pending |
| S14 | [Quick Chat Workspace](../specs/20260709-quick_chat_workspace.md), Accepted / Partial | Quick Chat workspace and route services, app schemas, storage, MCP/Web projections and tests | Verify it remains a real durable workspace while prohibiting workers, repositories and external side effects; repair the missing change link and test guard bypasses. | Pending |
| S15 | [Workflow Coordinator Internal Core Role](../specs/20260704-workflow_coordinator_internal_agent.md), Accepted / Partial | `internal-agents/worker-coordinator*`, delegation, mode selection, context and workflow tests | Verify coordinator authority, deterministic boundaries, worker selection, semantic worker-context composition, plan drafting, stop/refine decisions and evidence use without duplicating scheduler, Knowledge Manager or runtime ownership. | Generic runtime boundary verified; full-plan, immutable delivery, and Goal stop-authority findings remain in G01 |
| S16 | [Work And Resource Interaction Model](../specs/20260713-work_resource_interaction_model.md), Accepted / Partial | Protocol work/resource records, NanoCore App API and mode services, unified Agent Skill CLI, Web projections and L6 stories | Verify the accepted judgment, delegation, interaction, artifact and resource model against current product paths; keep durable product authority in NanoCore and prevent presentation surfaces from inventing parallel workflow state. Resolve C01-F02 by defining and implementing exact Artifact-to-Item lineage without a second work-record authority. | Pending; exact Artifact lineage prerequisite implemented under C02 |
| S17 | [Knowledge Manager Service](../specs/20260704-knowledge_manager_internal_agent_runtime.md), Accepted / Partial | `knowledge-manager.ts`, governed retrieval and proposal routes, Core Client, unified Skill/CLI and focused tests | Verify exact route-assigned callers, one S61 retrieval owner, safe typed errors, proposal-only mutation, source and conflict handling, and separation of selection evidence from S39 delivery. | Rebaselined by the G07 preamble; implementation corrections remain |
| S18 | [Explicit Work Reflection And Knowledge Improvement](../specs/20260710-self_improvement_evaluation_loop.md), Accepted / Partial | Existing work-history reads, Knowledge Proposal and Review owners, S39 delivery, unified Skill/CLI and one existing L3/L6 path | Verify one explicit existing-operation composition, exact source-linked create-only proposals, human-only activation, S39-only later-use proof, bounded reversal and no new endpoint, role, lifecycle, runner or Harness. | Rebaselined and accepted by the G07 preamble; implementation gate remains |
| S19 | [Evaluation Harness Design](../specs/20260711-evaluation_harness_design.md), Draft / Not Started | No current implementation surface; future retained evidence, Context Package, sandbox and human-review owners | Preserve non-authorizing activation gates, held-back-check isolation, current authority, bounded retention and fresh-attempt interruption semantics without a second workflow or test platform. | Deferred by the G07 preamble pending repeated real evaluation demand |

### C. Agent Supply, Runtime, Session, Scheduler, And Sandbox

| ID | Specification and current alignment | Primary implementation and test surfaces | Required review focus | Pre-group snapshot, not an execution state |
| --- | --- | --- | --- | --- |
| S20 | [Agent Environment Package](../specs/20260616-agent_environment_package.md), Accepted / Partial | Config schema AEP, worker protocol, NanoCore agent environment/materialization, worker governance, worker shim and real OpenShell e2e | Audit the full declared-resolved-materialized-executed-evidence chain, fail-closed features, portability, redaction, lifecycle, backend mapping and whether the large contract still has cohesive ownership. | Pending |
| S22 | [Agent Manifest And AEP Resolution](../specs/20260703-agent_manifest_aep_resolution.md), Accepted / Partial | `@openkit/config-schema`, NanoCore agent manifest/setup modules, runtime agent environment, setup ledgers and tests | Verify single-manifest selection, exact adapter/image/binary/network declarations, required features, readiness blockers, redacted snapshots, strict unknown-field rejection and no hidden launch authority. | Active in WP-2 |
| S23 | [Session Static Workspace Materialization](../specs/20260704-session_static_workspace_materialization.md), Accepted / Partial | Config session-workspace schemas, runtime workspace materializer, AEP snapshots, sandbox roots and materialization tests | Verify slot layout, static-versus-turn materialization, catalog source identity, digest keys, mount safety and partial-materialization failure without treating runtime reuse or snapshots as current authority. | Broad reuse/snapshot drift remains dispatched from WP-2; WP-4 owns only the exact unimplemented S39 generated input-to-`context` slot mapping |
| S24 | [Agent Session Continuity](../specs/20260704-agent_session_continuity.md), Accepted / Partial | Agent session continuity module, scheduler leases, checkpoints, snapshots, recovery and App API read models | Preserve exact same-worker restart reconnect and truthful interruption or fresh authorized retry; do not test or build snapshot/resume/replace/fork/rollback matrices outside an accepted current contract. | Broad dormant machinery dispatched from WP-2 rather than absorbed |
| S25 | [Worker Runtime Communication Model](../specs/20260629-worker_runtime_communication_model.md), Accepted / Partial | Worker protocol, direct worker-control shim and transcript paths, AEP, disabled capability projection and workspace-sync integrations | Retain as the unique shared worker contract; verify the generic shim, static adapter registry, candidate-record boundary, direct-control failure behavior and disabled capability sequencing. | Retained by the WP-2 preamble |
| S26 | [OpenShell Disposable Cell Lifecycle](../specs/20260715-openshell_disposable_cell_lifecycle.md), Accepted / Implemented | OpenShell Cell helper/controller, worker backend, deployment config, scheduler capacity, restart cleanup and real A1 e2e | Verified official artifact identity, single-slot ownership, prepare/recycle ordering, full-epoch termination, fresh stable-empty readiness, same-boot fail-closed retention, cross-boot and fenced retry, local-only diagnostics and absence of resource-delete success claims. | Completed 2026-07-15 |
| S27 | [OpenShell Mechanism Internalization](../specs/20260703-openshell_mechanism_internalization.md), Accepted / Partial | OpenShell schema snapshot, NanoCore policy/vault/audit/runtime adapters and conformance tests | Verify borrowed mechanisms are translated into OpenKit-owned contracts, external version drift is bounded, native ids never become product authority, and upgrade/failure behavior is explicit. | Pending |
| S28 | [Container Image Packaging](../specs/20260708-container_image_packaging.md), Accepted / Implemented | Dockerfiles, image manifests, build/run scripts, worker shim, NanoCore Docker tests and smoke | Verify reproducible contents, pinned tools, non-root behavior, architecture support, credential absence, health/readiness, upgrade path and real runtime compatibility. | Pending |
| S29 | [Runtime Scheduling Scale](../specs/20260703-runtime_scheduling_scale.md), Accepted / Partial | Scheduler records, placement, capacity and health modules, schemas and tests | Retain until S30 or Core actually absorbs the configured deployment profile, exact timing defaults and remote-target fallback boundary; no consolidation occurs inside WP-2. | Retained by the WP-2 preamble |
| S30 | [Durable Scheduler Design](../specs/20260703-durable_scheduler_design.md), Accepted / Partial | Scheduler admission, dispatch, capacity, health, lease, renewal, watch, recovery and service tests | Verify the current one-target, one-slot admission, exact reconnect, cleanup fencing and truthful interruption contract only; fairness, starvation policy, multi-target placement, quarantine automation and distributed takeover are excluded. | Unchanged in WP-2; later consolidation review dispatched |
| S31 | [Worker Sandbox Freedom Policy](../specs/20260709-worker_sandbox_freedom_policy.md), Accepted / Implemented | OpenShell policy/backend, AEP, worker governance, worker shim, container tests and runtime diagnostics | Promote durable freedom/boundary principles to Core; verify process freedom, filesystem/network/credential limits, policy compilation, unsupported controls, bypass resistance and review gates. | Pending |
| S32 | [Worker Credential Access Declarations](../specs/20260709-worker_credential_access_declarations.md), Accepted / Implemented | Config AEP schemas, setup resolver, vault references/grants, injection plans/receipts and runtime materialization | Verify least privilege, declaration-to-grant binding, target visibility, revocation/expiry, provider mapping, redaction and absence of undeclared fallback credentials. | Pending |
| S33 | [Worker Runtime Sub-Agent Provenance And Inference Identity](../specs/20260711-worker_runtime_subagent_provenance.md), Accepted / Implemented | Worker shim raw capture, worker protocol, transcript import, runtime evidence, capability ledger, LLM worker inference, prompt-cache keys and vendored runtime schemas | Audit the implemented raw-stream bounds, origin indexing, authority separation, trusted inference binding, cache lineage, quarantine, privacy contracts, and completed real root-plus-two-child A1 acceptance evidence. | Pending |
| S34 | [Skill Catalog Versioning And Pinning](../specs/20260711-skill_catalog_versioning_pinning.md), Draft / Not Started | Existing static supply metadata and the first future real worker Skill consumer | Confirm the Draft remains non-authorizing until a real materialized worker Skill and repeated version-selection or rollback demand exist; at activation, review only content identity, one current pointer, one Workspace pin, exact AEP materialization proof and pointer rollback. | Deferred activation gate |
| S35 | [Scheduler Recurring Event Triggers](../specs/20260711-scheduler_recurring_event_triggers.md), Draft / Not Started | Existing admission and Automation owners plus the first future repeated scheduling demand | Confirm the Draft remains non-authorizing until repeated real demand proves a current trigger owner; at activation, define only one schedule authority, existing admission reuse, bounded overlap and dependency failure without a fire ledger or recovery platform. | Deferred activation gate |

### D. Worker Control, Capability, Context, Provider, And Vendor Contracts

| ID | Specification and current alignment | Primary implementation and test surfaces | Required review focus | Pre-group snapshot, not an execution state |
| --- | --- | --- | --- | --- |
| S36 | [Worker Agent Capability](../specs/20260703-worker_agent_capability.md), Accepted / Partial | Protocol capability/usage schemas, shared ledger, disabled AEP projection and future worker gateway/client | Verify the accepted target against the current disabled plane; rebuild authentication, catalog and grants, request identity, idempotence, denial/failure accounting, source lineage, redaction and backend-independent routing before advertisement. | Pending |
| S37 | [Worker Control Protocol](../specs/20260703-worker_control_protocol.md), Accepted / Partial | Worker protocol, control routes/gateway/commands/records/sequences/rebuild, worker shim and tests | Verify narrow control authority, sequence ordering, authentication, replay, duplicate commands, stale sessions, rejected evidence, rebuild and the prohibition on direct product-state mutation. Same-snapshot renewal is active without a refresh acknowledgement; source-to-target refresh remains unimplemented until its request, negotiation, rebinding, rollback, and evidence contract exists. | Renewal and refresh boundary aligned; full audit pending |
| S38 | [Worker MCP Tool Supply](../specs/20260704-worker_mcp_tool_supply.md), Accepted / Partial | Static runtime supply substrate and the future worker MCP gateway, tool schemas, vault/policy linkage, capability ledger and tests | Rebuild the executable plane before claiming availability; verify allowlists, schema validation, approval gates, credential grants, health, lifecycle, usage/audit, denied calls and separation from the end-user Agent Skill Interface. | Pending |
| S39 | [Worker Context Package](../specs/20260703-worker_context_package.md), Accepted / Partial | Context projection policy, LLM projection, Knowledge Manager context, AEP materialization, source catalog and tests | Close current scope and producer gaps; verify minimality, sensitivity, freshness, token bounds, source traceability, deterministic exclusions, runtime materialization and stale context behavior. | Pending |
| S40 | [LLM Gateway Responses API](../specs/20260526-llm_gateway_responses_api.md), Accepted / Implemented | NanoCore LLM clients/converters/policy/usage/routes, OpenAPI, provider config and gateway tests | Verify streaming, Responses and Chat parity, tool and image inputs where supported, cancellation, errors, usage, cache, credential isolation, policy, provider feature negotiation and public contract accuracy. | Pending |
| S41 | [Pi AI Provider Gateway Adoption](../specs/20260703-pi_ai_provider_gateway_adoption.md), Accepted / Implemented | Pi dependency pin, public-vocabulary boundary, models catalog reconciliation and tests | Preserve only the unique stock-dependency, exact-pin, no-fork/no-patch, public-vocabulary, and catalog-reconciliation obligations; routing and mapping remain solely with S42. | WP-5A complete |
| S42 | [Unified Pi AI LLM Backend](../specs/20260708-pi_ai_unified_llm_backend.md), Accepted / Partial | Provider dispatcher/registry/config, Pi client/converters/usage, Codex-native exception path and tests | Verify one non-Codex owner, provider capability normalization, credentials, cancellation, streaming, usage, errors, health, real-provider behavior and no lingering native OpenAI duplicate path; native pi-ai Responses remains a bounded post-program partial. | WP-5A complete; the named post-program partial remains |
| S43 | [Capability Usage Gateway Foundation](../specs/20260704-capability_usage_gateway_foundation.md), Accepted / Partial | Capability usage ledger, current LLM gateway usage, future worker MCP usage, protocol schemas and audit/evidence links | Determine whether this one-time foundation still owns a distinct contract; keep current LLM evidence separate from future MCP conformance and supersede only after all attribution and idempotence tests remain. | Pending |
| S44 | [Codex ChatGPT Subscription Login](../specs/20260526-codex_chatgpt_subscription_login.md), Accepted / Implemented | Codex OAuth account slots, provider profiles, server-owned account metadata, isolated Codex homes, secure pre-provisioned account files, App API, Core Client, Web diagnostics and tests | Verify multi-account isolation, browser/device flows, cancellation/logout, refresh, redaction, storage ownership, worker materialization and failure recovery without leaking auth state. | Secure real-L6 preflight aligned; full audit pending |
| S45 | [Vendor Snapshot Packages](../specs/20260522-vendor_snapshot_packages.md), Accepted / Implemented | Codex/OpenShell/models.dev snapshot packages, refresh scripts, conformance tests and update change records | Verify provenance, pinning, reproducible refresh, reviewable diffs, license/source metadata, consumer compatibility and fail-closed behavior when native contracts drift. | Pending |
| S64 | [Codex Worker Adapter](../specs/20260716-codex_worker_adapter.md), Accepted / Implemented | Shared worker harness contract, Codex adapter and image profile, native-output/provenance fixtures and image smoke | Verify that all Codex command, output and native-provenance knowledge is removable with one adapter and image; reuse shared supervision and control, advertise only proven capabilities, and add no NanoCore runtime branch. | Verified with WP-2 |
| S65 | [OpenCode Worker Adapter](../specs/20260716-opencode_worker_adapter.md), Accepted / Implemented | Shared worker harness contract, OpenCode adapter and image profile, pinned native JSON fixtures and image smoke | Verify one-shot native translation, bounded output parsing, safe resource and credential supply, and clean adapter deletion without an OpenCode server becoming a second control plane. | Verified with WP-2 |
| S66 | [Pi Worker Adapter](../specs/20260716-pi_worker_adapter.md), Accepted / Implemented | Shared worker harness contract, Pi adapter and image profile, pinned native JSON fixtures and image smoke | Use Pi only to falsify accidental Codex/OpenCode coupling; verify one adapter/profile/image, bounded resource discovery, no RPC control-plane expansion, and no native schema leakage into NanoCore or worker protocol. | Verified with WP-2 |

### E. Storage, Workspace Data, Synchronization, Git, And Portability

| ID | Specification and current alignment | Primary implementation and test surfaces | Required review focus | Pre-group snapshot, not an execution state |
| --- | --- | --- | --- | --- |
| S46 | [Storage Layout And Record Ownership](../specs/20260703-storage_layout_record_ownership.md), Accepted / Partial | NanoCore DB/migrations/schema, filesystem layout, store, backups, Workspace export/import and tests | Retain the implemented owner-independent V2 Workspace root, one-way migration, and no-compatibility-reader boundary while later owners finish the broader record-family, retention, deletion, and conformance criteria. | Pending |
| S47 | [Schema Evolution Record Envelope](../specs/20260703-schema_evolution_record_envelope.md), Accepted / Implemented | Config schema evolution, protocol/worker schemas, JSONL readers and writers, workspace import/export and conformance tests | Remove its false claim to own general evolution; keep concrete record-envelope, naming, digest, registry and reader/writer contracts; test unknown optional and authority-bearing data. | Pending |
| S48 | [Workspace Data Source Catalog](../specs/20260704-workspace_data_source_catalog.md), Accepted / Implemented | Config source catalog, repository data-source catalog, AEP/source resolution, storage, capability and usage lineage tests | Verify declare-once/reference-by-ID behavior, path and kind validation, policy/vault linkage, portability, stale/missing sources, remote path mapping and lineage propagation. | Pending |
| S49 | [Workspace Synchronization](../specs/20260703-workspace_synchronization.md), Accepted / Partial | Workspace sync/review/apply/filesystem/Git/reconciliation/quarantine modules, storage schemas, routes, MCP and tests | Coordinate with maintainability recovery; prove reads are non-mutating, worker output never becomes truth directly, staging/apply are isolated and atomic, unrelated Git state is safe, recovery is deterministic and every transition is audited. | Pending |
| S50 | [Git Write Workflow](../specs/20260704-git_write_workflow.md), Accepted / Partial | Repository store/routes, Git push policy/records/command/executor, workspace review Git, MCP and tests | Verify commit-on-apply, isolated index/worktree behavior, protected branch policy, approval-gated push, request idempotence, uncertain-effect fail-closed behavior, remote errors, stale approvals and no inclusion of unrelated user changes. | WP-5A completed the bounded uncertain-effect guard |
| S51 | [Workspace Backup, Export, And Import](../specs/20260704-workspace_backup_export_import.md), Accepted / Partial | Data-root backup/restore, workspace export/import, config export schema, MCP portability runner and tests | Verify offline validation, atomic import, corrupt/truncated archives, identity rebinding, repository/vault rebind, no secrets, unsupported features, rollback and realistic cross-machine portability. | Pending |

### F. Identity, Bootstrap, Policy, Vault, Audit, And Knowledge

| ID | Specification and current alignment | Primary implementation and test surfaces | Required review focus | Pre-group snapshot, not an execution state |
| --- | --- | --- | --- | --- |
| S52 | [NanoCore Config And Identity Contract](../specs/20260628-nanocore_config_identity_contract.md), Accepted / Partial | Config loaders/data root/mode, auth identity, workspace membership, provider and agent config, diagnostics and tests | Verify local/server identity, config precedence and ownership, path safety, stale-session behavior, actor attribution and removal of obsolete passthrough or duplicate configuration owners. | Pending |
| S53 | [Remote Auth Credential Bootstrap](../specs/20260704-remote_auth_credential_bootstrap.md), Accepted / Partial | Bootstrap/access-token stores, auth middleware, Better Auth, bundled CLI credential store, App API schemas and server-flow tests | Verify one-time bootstrap, token scopes/rotation/revocation, secure storage, redaction, stable `openkit-cli` and `agent-skill` last-use labels, local versus server behavior, replay and expired or compromised credentials. | Pending |
| S54 | [NanoCore Bootstrap Readiness](../specs/20260704-nanocore_bootstrap_readiness.md), Accepted / Partial | Bootstrap phases/readiness/lock/shutdown/vault/policy/audit, index startup, diagnostics and tests | Verify deterministic phase order, lock exclusivity, partial initialization, restart recovery, degraded dependencies, readiness truth, safe shutdown and operator-actionable diagnostics. | Pending |
| S55 | [OpenKit Policy Model](../specs/20260629-openkit_policy_model.md), Accepted / Implemented | `@openkit/policy-kernel`, config policy schema, conformance fixtures, permission decisions and approval gates | Verify the strict NGAC subset is coherent and sufficient, relation updates are deterministic, unsupported semantics fail closed and Core identity/resource/action mappings are unambiguous. | Pending |
| S56 | [Policy Enforcement Mapping](../specs/20260703-policy_enforcement_mapping.md), Accepted / Partial | Permission decision writers, approval gates, auth, capability, vault, workspace mutation and public route enforcement tests | Close missing enforcement points; prove every authority-bearing operation checks the correct subject/object/action context, decisions are immutable and stale or denied paths cannot mutate state. | Pending |
| S57 | [Vault Secret Injection](../specs/20260703-vault_secret_injection.md), Accepted / Partial | Vault references/grants, injection plans/receipts, provider resolver, AEP materialization, OpenShell backend and tests | Verify grant lifecycle, visibility target, backend capability, expiry/revocation, cleanup, redaction, retry and that secrets never enter persistent product records or command arguments. | WP-5A removes the two consumer-free provider fields; remaining Vault scope stays pending |
| S58 | [Vault Backend Implementation](../specs/20260704-vault_backend_implementation.md), Accepted / Partial | OS keychain and encrypted-file backends, key files, unlock state, admin routes/audit and tests | Verify key-source security, lock/unlock, rotation, corruption, permissions, platform failure, fallback warnings, backup/export exclusions and no plaintext recovery path. | Pending |
| S59 | [Audit, Usage, And Evidence Records](../specs/20260703-audit_usage_evidence_records.md), Accepted / Partial | Protocol records, audit writers, usage ledger, EvidenceBundle, RuntimeEvidence, workspace sync producers, export/import and tests | Coordinate with evidence simplification; prove distinct ownership, automatic trusted producers, scope homing, idempotence, redaction, retention, quarantine, import/export and complete required producer coverage. | Pending |
| S60 | [Knowledge Store Governance Rules](../specs/20260702-knowledge_store_governance_rules.md), Accepted / Partial | OKF validator/store, Knowledge Manager, observations/claims/conflicts/proposals, source refs and tests | Verify create-only generated proposals, exact candidate bytes and digest, human-only activation, the bounded accepted-review missing-page completion exception, stale-safe unchanged-page reversal, direct human governance, sensitivity, path validation, source lineage and no automatic promotion. | Pending |
| S61 | [Knowledge Store Implementation](../specs/20260703-knowledge_store_implementation.md), Accepted / Partial | OKF files, one governed retrieval owner, proposal/review/page owners, S39 bridge, import/export and App API | Delete the duplicate substring selector and standalone context materialization; verify safe file-first page identity, deterministic retrieval without secret-bearing traces, create-only digest/source equality, bounded application and reversal, one S39 delivery proof, portability and realistic small-store behavior. Worker `knowledge.*` routes and generalized revision history remain unauthorized. | Pending |
| S63 | [Single-Deployment Multi-User Workspace System](../specs/20260715-multi_user_workspace_system.md), Accepted / Implemented | Better Auth identity, Core Workspace registry and membership, owner-independent storage, policy mapping, auth resolver, audit, concurrency, App API, Core Client, CLI/Skill, and same-release Web alignment | Verify one deployment as one trust domain, Workspace as neither tenant nor organization, exactly one owner, fixed owner/editor/viewer access, fail-closed request resolution, actor attribution, narrow shared-write CAS, one-way storage migration and no second RBAC engine. | Verified with WP-5; rebuilt Web remains with S10 |

## Coverage Appendix: Supporting Documentation Projections

Supporting documents do not override Core or active specifications. Their D labels preserve coverage inside the primary sets above, and they are corrected only as part of the owning group rather than as independent audit items or new design authorities.

| ID | Document group | Required review focus | Pre-group snapshot, not an execution state |
| --- | --- | --- | --- |
| D01 | `README.md`, `docs/product-vision.md`, `docs/roadmap.md` | Keep mission, current posture and future sequencing accurate; remove duplicated Core definitions, stale storage/provider claims and non-English repository text; preserve the newly aligned future-direction entries. | Pending |
| D02 | `AGENTS.md`, `CONTRIBUTING.md`, `docs/template-overview.md`, `docs/change-tracking.md` | Reconcile README-first governance, TDD, documentation ownership, no-backward-compatibility and commit sequencing without duplicate or contradictory rulebooks. | Pending |
| D03 | `docs/app-api.md`, generated OpenAPI and API-facing guides | Keep stable boundary and projection rules while making generated contracts own operation inventory; remove future provider, transport and implementation detail from the conceptual API guide. | Pending |
| D04 | `docs/deployment.md`, NanoCore deployment-mode guides and release/deployment cookbooks | Verify local/server/container/remote topology, auth, data roots, backup, health, rollout and operator commands against current runtime behavior and real smoke tests. | Pending |
| D05 | `DESIGN.md`, Web design docs and Web cookbook | Keep product IA, interaction, accessibility and visual rules; remove old Memory vocabulary, backend semantics and obsolete Solid-stack authority as the rebuild contract progresses. | Pending |
| D06 | `apps/nanocore/README.md` and local rules | Correct UI-first, remote-runtime, session, route, data-root, provider, auth and operational claims against the settled NanoCore implementation; move host-specific topology to runbooks. | Pending |
| D07 | `apps/web/README.md` and local rules | Align current versus target stack, public client-only boundary, available UI behavior, tests and rebuild sequencing; remove the deprecated cookbook reference when the accepted stack is ready. | Pending |
| D08 | `packages/README.md` plus every package README and local rule | Verify the complete package inventory, single ownership boundaries, generated artifacts, public exports, commands and consumer relationships, including `worker-shim`. | Pending |
| D09 | Deleted `mcp/README.md` and local rules | Verify the README and local rules were deleted with the package and no active guide routes users to MCP setup. | Completed 2026-07-18 with WP-1/G02 |
| D10 | `skills/README.md` and all OpenKit Skill packages | Verify the single end-user `openkit` Skill target, progressive references, bundled CLI, human gates and credential rules, then verify the four legacy setup/loop variants are deleted without aliases. | Pending |
| D11 | `docs/cookbooks/*` | Verify each recipe against current CLIs, packages, stack, deployment and security behavior; delete obsolete recipes after inbound links are removed rather than preserving parallel setup paths. | Pending |
| D12 | `tests/stories/*`, story-runner guides and acceptance documentation | Align narrative acceptance criteria with runnable deterministic and real-agent paths; remove stale host-mode assumptions and ensure skips cannot be reported as proof. | Pending |
| D13 | `docs/changes/*` and any archived working logs | Verify active overlapping plans have distinct ownership, current statuses and final evidence; do not rewrite historical checkpoints, but prevent completed plans from remaining apparent current authority. | Pending |

## Design And Feasibility Review Questions

Every contract-group audit must answer the questions that apply to its combined authority and interfaces.

- Does the design solve a present OpenKit requirement, or has a hypothetical variant become mandatory complexity?
- Can one module and one durable record family own the behavior without a second writer, mirror ledger, or competing route?
- Does the design preserve `Workspace -> Thread -> Turn -> Item[]` and introduce a new Core entity only when it owns a distinct lifecycle or authority boundary?
- Can the complete path be traced from one public or runtime entry point through authorization, validation, persistence, effects, evidence, projection and recovery?
- Are current field, state and API choices required by stable semantics, or are they accidental projections that should remain replaceable?
- Can the design survive interruption between each persistent or external side effect without ambiguous truth, duplicate work, lost review state or unbounded repair?
- Are idempotence, concurrency, ordering, retries, cancellation, timeouts, stale state, clock behavior and process restart defined where they matter?
- Does every secret, identity, permission and sandbox claim have a real enforcement point and an adversarial test, or is it only descriptive documentation?
- Can a user understand what happened, what requires attention, what evidence supports the result and which action is safe without reading adapter-private state?
- Can an operator diagnose configuration, dependency, storage, provider, runtime and network failure without exposing credentials or host-private detail?
- Are derived indexes, projections, summaries and generated contracts rebuildable from one authoritative source?
- Does a deterministic test prove the invariant it claims, and is a real-runtime or story-level check required to expose integration behavior?
- Is a dependency, abstraction, schema, route, record, helper, configuration option or compatibility layer removable without losing required behavior?
- If implementation difficulty is high, is the difficulty intrinsic to the product requirement or caused by a poor ownership boundary in the design?

## Remaining Execution (2026-07-17 Redesign)

Phases 0 and 1 of the original plan completed; their evidence lives in the checkpoints below. The former Phase 2-7 program is superseded with these dispositions:

- Group audit and vertical correction (former Phase 2): the bounded G01 closeout runs here under the audit pass protocol; dissolved groups are audited inside their owning change plans; orphan groups get review-only time boxes scheduled by the Execution Program.
- Cross-group interface review (former Phase 3): performed once at each work-package exit against already-settled neighbors; interface findings are ticketed to the Execution Program backlog against the owning group, never reconciled by a bridge record, compatibility layer, or third owner.
- Promotion, consolidation, and layer repair (former Phase 4): S01 completed consolidation during G02 and S21 during the WP-2 preamble after every continuing contract was assigned to named current owners. The remaining approved consolidation candidates — Runtime Scheduling Scale (S29), Worker Runtime Communication Model (S25), Capability Usage Gateway Foundation (S43), Pi AI Provider Gateway Adoption (S41), and Web Product Surface Projection (S09) after the rebuild — are reviewed inside their owning work packages. A specification is superseded only when a named current owner absorbs every continuing contract and implementation and test evidence survives the move.
- Real-use and failure-oriented acceptance (former Phase 5): the scenario matrix below dispatches to owning work packages as acceptance criteria. Deterministic adapters prove lower-layer regression; real runtimes and providers prove only what fixtures cannot; a skip is never acceptance evidence. The full matrix is re-checked once at program closeout in WP-7.
- Projection completion and repository-wide closeout (former Phases 6 and 7): retained as the WP-7 closeout pass — inventory and lifecycle validation, link validation, stale-term and duplicate-owner sweeps, dead-code and stale-projection review, and closing every group with `NO-ACTION` evidence or resolved finding IDs, followed by final human approval.

## Real-Use Scenario Matrix

Each scenario is owned as acceptance criteria by the work package whose surfaces it exercises; the full matrix is re-checked once at program closeout (WP-7). Scenarios are not an audit task list.

| ID | Scenario | Evidence required |
| --- | --- | --- |
| U01 | Fresh local-mode bootstrap and first Workspace creation | Deterministic startup, stable local actor, valid data root, readiness, no server-auth requirement, clean restart and inspectable initial records. |
| U02 | Fresh server-mode owner bootstrap, sign-up/sign-in and scoped token lifecycle | One-time token consumption, secure credential storage, correct scope, revocation/rotation, unauthenticated rejection, redacted audit and restart persistence. |
| U03 | Workspace onboarding with repository, data-source catalog, provider and agent setup | Path validation, source identity, readiness explanation, no secret-bearing config, remote-path clarity and deterministic resolved setup. |
| U04 | Chat Mode lightweight request and follow-up | Correct Assistant selection, durable thread/turn/items, no worker or external side effect, streaming/terminal behavior and recoverable provider failure. |
| U05 | Task Mode bounded worker delegation | Correct worker selection, context, lease, sandbox, stop condition, evidence, failure and Goal escalation without hidden workflow state. |
| U06 | Goal Mode planning through verified closeout | Human plan approval, bounded steps, steering, worker retry, artifact/evidence review, risk reporting, verification and terminal acceptance. |
| U07 | Approval, elicitation, steering and review occurring around active work | Correct blocking versus non-blocking semantics, stale-decision rejection, idempotence, Action Center truth and no action before authority exists. |
| U08 | Worker interruption, crash, NanoCore restart and session recovery | Durable checkpoint, no duplicate completion, valid resume/replace choices, lease recovery, pending input handling and preserved lineage. |
| U09 | Concurrent scheduling under limited capacity | Fair admission, stable queue positions, cancellation/retry, lease renewal, stale worker quarantine, no double dispatch and actionable attention records. |
| U10 | Local and remote OpenShell worker execution | Equivalent Core semantics, secure connectivity, version/capability negotiation, bounded data movement, cleanup and clear degraded diagnostics. |
| U11 | Sandbox attempts disallowed filesystem, network and credential access | Fail-closed enforcement, useful error, audit/evidence, no secret or host-path leak and no bypass through alternate tool or process paths. |
| U12 | Native Codex and unified pi-ai provider calls, including streaming, cancellation and error | Correct provider routing, credential isolation, StopReason, usage/cache accounting, policy, capability and real-provider evidence where configured. |
| U13 | Worker MCP tool allow, deny, approval and failure | Tool schema validation, grant and policy binding, approval gate, usage/audit, network and secret boundaries, timeout and clear separation from the end-user Agent Skill Interface. |
| U14 | Vault lock, unlock, injection, revocation, expiry and imported-reference rebind | No plaintext persistence, deterministic failure, cleanup, receipts, redacted diagnostics, cross-workspace isolation and safe operator recovery. |
| U15 | Worker workspace changes against clean, dirty, conflicting and externally modified Git state | Read-only inspection, isolated staging, review before apply, unrelated-change preservation, atomic commit, rollback/reconciliation and approval-gated push. |
| U16 | Workspace export/import and data-root backup/restore with valid, corrupt and unsupported input | Offline verification, no secrets, atomicity, quarantine/fail-closed behavior, identity/repository/vault rebind and no partial target state. |
| U17 | Knowledge ingest, generated proposal, conflict, stale source, retrieval and context materialization | Source traceability, hypothesis status, human override, deterministic validation, sensitivity filtering, stale/conflict visibility and reproducible selection trace. |
| U18 | Audit, Usage, Evidence and RuntimeEvidence inspection after success and failure | Complete producer coverage, distinct record meaning, common lineage, idempotence, redaction, retention, quarantine and no unsupported proof claim. |
| U19 | Unified end-user Skill and bundled CLI loops against local and server NanoCore | Progressive capability discovery, public-contract-only operation, safe credentials, correct mutation gates, recoverable failures, usable diagnostics and evidence-backed completion. |
| U20 | Web projection of the same current workflows | No second source of truth, client-only API use, accessible human attention, correct reconnect/replay, error states and parity with CLI-visible records. |
| U21 | Evaluation, recurring schedules, Skill pinning and runtime sub-agent provenance before implementation acceptance | Feasible authority and storage boundaries, adversarial design cases, bounded cost/data, explicit prerequisites and no false current product claim. |

## Test-First And Commit Discipline

- Choose the smallest cohesive implementation owner before writing a test; do not encode an already-fragmented shape into new fixtures.
- Commit behavior tests before implementation where the repository can keep the test commit reviewable and intentionally failing in sequence.
- Apply package changes in dependency order: Core/protocol documentation, `@openkit/protocol`, shared schemas and policy packages, NanoCore, Core Client, the transport-neutral operation catalog, bundled CLI and unified Skill, Web and supporting docs.
- Commit each changed package separately when behavior crosses package boundaries, as required by repository guidance.
- Keep generated artifacts with the owning package implementation commit unless a generator contract requires a separate mechanical commit.
- Update this record at completed audit groups, material design decisions, implementation handoffs, blockers and verification milestones, not after every command or file edit.
- Never stage or commit unrelated dirty files from another active or planned change record.
- Prefer deletion and direct ownership over compatibility shims, parallel routes, duplicated records, passthrough services or speculative configuration.

## Verification Plan

### Per-Group Verification

- Relevant package unit and conformance tests.
- Targeted NanoCore black-box or runtime tests for public and failure behavior.
- Generated schema and OpenAPI drift checks when public contracts change.
- Core Client and bundled CLI operation mapping tests for every public projection change.
- Web component or browser tests only when the Web projection changes.
- One applicable L6 story or explicit reason why the group is fully proved below L6.
- `git diff --check` for every completed slice.

### Documentation And Repository Gates

- `node --test tests/spec-lifecycle.test.mjs`
- `node scripts/validate-spec-lifecycle.mjs`
- Exact active Core/spec/archive inventory and active-spec index comparison.
- Repository-relative Markdown link validation, excluding documented literal syntax examples.
- Searches for invalid lifecycle values, stale archived authority, old paths, duplicate canonical definitions and obsolete terminology.
- `CI=true pnpm run check:repo`

### Package And Product Gates

- `CI=true pnpm run verify:l0-l2`
- `CI=true pnpm run test:e2e:nano`
- `CI=true pnpm run test:e2e:web` when Web is in scope.
- `CI=true pnpm run test:smoke`
- `CI=true pnpm run test:stories`
- `CI=true pnpm run verify:release`
- `CI=true pnpm run verify:full` at final convergence.
- Opt-in real Codex, real provider and real Task Mode stories for applicable runtime/provider slices, with explicit preflight and non-secret evidence.

## Expected Handoffs And Review Gates

- Human review is required before changing Foundation doctrine, replacing a canonical Core owner, accepting a Draft specification, or changing a security or data-loss boundary.
- Overlapping implementation must be handed off from the owning active change plan before this plan edits the same code path.
- Ambiguous documentation-versus-code mismatches must be classified from product intent, current Core authority and actual use rather than resolved by whichever artifact is newest.
- Specification consolidation requires explicit review of replacement coverage and lifecycle evidence before any move to `superseded/` or `retired/`.
- Final closeout requires human review of the canonical owner map, remaining deferred work, real-use evidence and every intentionally retained Partial or Not Started implementation status.

## Risks And Mitigations

- Risk: The audit becomes a permanent program with no completed slices. Mitigation: one contract group at a time, explicit group exit criteria, cohesive checkpoints and no parallel tracking system.
- Risk: Core cleanup deletes near-term product direction. Mitigation: retain deliberate future boundaries, mirror them in roadmap or active specs and distinguish `DEFERRED-ALIGNMENT` from stale doctrine.
- Risk: The document hierarchy is made internally elegant while implementation remains wrong. Mitigation: no document closes without call-path, persistence, test and actual-use evidence.
- Risk: Current implementation is treated as proof that an awkward design is feasible. Mitigation: require explicit design-feasibility review and classify intrinsic complexity separately from ownership-induced complexity.
- Risk: Test suites preserve accidental behavior. Mitigation: trace product intent and Core authority first, then rewrite tests when a documented or implementation behavior is itself defective.
- Risk: Deterministic adapters conceal provider, runtime, network, filesystem or timing failures. Mitigation: use real integration stories for the slices where those external properties are the subject of the contract.
- Risk: Consolidating specifications loses a unique invariant. Mitigation: maintain a replacement checklist and move a spec only after every continuing obligation has a named owner and passing evidence.
- Risk: Concurrent active changes invalidate audit conclusions or cause lost work. Mitigation: re-read status and diffs at every slice boundary, preserve unrelated work and require explicit ownership handoff for overlapping files.
- Risk: Security and secret-handling gaps are treated as ordinary follow-ups. Mitigation: stop the affected slice, add adversarial coverage and resolve the trust boundary before other cleanup continues.
- Risk: The plan adds validators, abstractions and process instead of fixing the system. Mitigation: reuse CodeGraph, existing tests, lifecycle validation and change tracking; add only the smallest mechanical check for proven recurring drift.
- Risk (observed 2026-07-17): the audit absorbs implementation and its finish line recedes. Mitigation: frozen closeout lists, the no-absorption rule, Backlog ticketing, and scope changes only through an explicit Execution Program ledger edit.
- Risk (observed 2026-07-17): uniform per-command exactness consumes the program. Mitigation: the central idempotency default and the scoped precision bar; exceed them only for Durable contract families or on recorded real-use evidence.
- Risk (observed 2026-07-17): checkpoint and protocol overhead dominates slices. Mitigation: the lightweight audit pass protocol, checkpoints of at most 10 lines, and standing prohibitions replacing per-slice recitals.

## Checkpoints

### 2026-07-11: Plan Established And Roadmap Preflight Completed

- Preserved all 19 current Core aspect documents, explicitly retained Metering and established Foundation as the twentieth planned Core aspect.
- Compared the previously identified future Core directions with the current roadmap.
- Corrected the pi-ai current-state description, added an explicit Workflow Graph and reusable Recipe roadmap entry, and recorded active Task evaluation, self-improvement, recurring scheduler, Skill pinning, sandbox freedom and worker runtime sub-agent provenance design work.
- Recounted the current hierarchy as 19 Core documents, 61 active specifications and 55 archived specifications, and recorded current status and implementation-alignment distributions.
- Used CodeGraph to identify the principal runtime, protocol, public API, storage, governance, provider and test call-path clusters before defining the implementation audit ledger.
- Identified active overlap with NanoCore maintainability recovery, evidence simplification, self-improvement foundations and concurrent worker runtime provenance work; this plan does not claim their in-flight files.
- Created the complete C00-C19, S01-S61 and D01-D13 review ledger plus the U01-U21 real-use scenario matrix before beginning implementation remediation.
- Verified exact Core and active-spec ledger coverage, all local Markdown links in the roadmap and this plan, spec lifecycle metadata and tests, whitespace, and the full `CI=true mise exec -- pnpm run check:repo` gate.

### 2026-07-13: Agent Skill Interface Lifecycle Alignment

- Replaced the superseded AI Interface entry with the accepted Agent Skill Interface, removed the retired Development Loop entry, and intentionally retained the stable ledger-id gap rather than renumbering later audit items.
- Refreshed the current hierarchy to 60 active specifications and 57 archived specifications, with implementation alignment of 41 Implemented, 13 Partial, 0 Diverged, and 6 Not Started.
- Replaced the MCP-first end-user audit path with the unified Skill and bundled CLI path while preserving worker-side MCP as a separate capability plane.

### 2026-07-13: Direct Worker Control And Capability Truth Reset

- Classified the mismatch as `DOC-DRIFT`: the implementation had already removed the worker control sidecar, worker capability client, NanoCore worker-capability routes, worker MCP gateway, and worker MCP smoke, while multiple canonical documents still described those surfaces as current.
- Recorded direct authenticated NanoCore `/api/worker-control` as the only governed worker-control path. Transcript files remain evidence and import input, not a fallback control mode; capability mediation must not become another control path.
- Preserved `capability.local`, worker Knowledge operations, and worker MCP tool supply as accepted future contracts, while aligning current AEP truth to `capabilities.mode: disabled` with no routes.
- Corrected the affected Core, roadmap, AEP, runtime communication, control, capability, MCP, usage-ledger, Knowledge, OpenShell, packaging, supply, synchronization, and audit-ledger documents without editing the dedicated worker runtime provenance specification or its change plan.
- Updated the active-spec baseline to 37 Implemented, 17 Partial, 0 Diverged, and 6 Not Started. The four corrected Partial classifications are explicit review work, not implementation regressions introduced by this documentation slice.
- Real L6 setup found that initial provider and agent writes require an explicit NanoCore restart. The runner must restart safely and prove that its idempotent rerun neither duplicates nor widens the configured authority.
- Authenticated repository linking materializes workspace data-source configuration. A rerun may consume only the exact safe session-scoped restart deferral and must then prove that the repository-link operation is a no-op.
- Real OpenShell preflight failed closed before worker launch when the local CLI was `0.0.63` and the gateway required `0.0.80`. The test environment now uses the officially validated `0.0.80` CLI; the failure remains evidence that exact-version negotiation is an enforced launch gate.
- The first Goal in another thread or workspace exposed a collision between user-store-global item IDs and thread-local Goal IDs. The regression test and objective-item identity correction are complete, so distinct Goal lineages no longer reuse one store-global item ID.

### 2026-07-14: Real Codex L6 Hardening And Initial Core Alignment

- Added the accepted Foundation Core document as the twentieth aspect, kept Metering as a separate Core owner, and aligned the Core inventory, term index, reading order, roadmap, and measurement doctrine with current runtime, workspace-transfer, and Git network UsageRecord producers.
- Aligned scheduler documentation and implementation around strict same-snapshot renewal, bounded renewal lifetime, terminal and stale lease recovery, and the rule that cross-snapshot supply refresh requires a future NanoCore-issued source-to-target request and atomic rebinding contract rather than an acknowledgement shortcut.
- Corrected Goal dispatch, lifecycle, identity, provider selection, cleanup, and lease paths found by repeated real-story execution, including scheduler recovery records being reopened under the originating admission owner instead of `user_local`.
- Preserved typed Codex provider failures, current `openai-codex/gpt-5.6-sol` selection, safe provider-message redaction, and actionable workspace-review rejection reasons without retaining the retired model or adding fallback compatibility.
- Replaced order-sensitive workspace-base comparison with structural comparison, then fixed the deeper real-use defect by capturing the host repository's immutable Git `HEAD` in the AEP before worker materialization. NanoCore and the worker now independently bind the same review base instead of weakening the review gate.
- Hardened the A1 topology used for the real story: the worker image is built natively on A1, the local runner and NanoCore connect to the A1 OpenShell gateway through explicit tunnels, the worker-control endpoint is reverse-forwarded, and the A1 Codex auth file is streamed only into the local server-owned account slot with strict file preflight and no worker exposure.
- Removed the remaining host-path Codex auth upload option from NanoCore and the OpenShell backend. Non-relay sandbox auth now has one owner, the vault-backed runtime-file path, while trusted-relay workers receive no Codex auth file.
- Corrected the Knowledge delivery projection: current Task Mode can retain selected Knowledge references in delegation metadata, but automatic binding into an AEP, worker input, or materialized Context Package remains unimplemented. Captured the missing durable deadline and restart-recovery path for stuck `releasing` leases in the scheduler spec and roadmap instead of calling the 5-minute release grace active.
- Extended the real L6 oracle to require the completed terminal `runtime.worker_turn` CapabilityCall and its single `sandbox_sessions` UsageRecord in addition to LLM inference, audit, evidence, RuntimeEvidence, review, Goal, and Git assertions.
- Reduced every deterministic defect in this hardening sequence to focused L1-L5 regression coverage before its implementation correction. The remaining contract groups remain open and will continue from the group ledger rather than being declared complete by one successful story or one edited document.
- Passed the real A1 acceptance run with the A1-built ARM64 Linux worker image, OpenShell `0.0.80`, `openai-codex/gpt-5.6-sol`, exact proof content, unchanged Git `HEAD`, one applied workspace review, one completed Goal task, exactly one terminal runtime CapabilityCall, exactly one linked `sandbox_sessions` UsageRecord, `0600` redacted evidence files, and zero residual A1 sandboxes.

### 2026-07-15: Stock OpenShell Causal Teardown Boundary

- Classified the late-create cleanup defect as a backend lifecycle design gap: stock OpenShell `0.0.80` resource deletion cannot prove that every previously accepted create has terminated before scheduler capacity is released.
- Removed the active OpenShell source patch dependency and prohibited any external dependency fork, patch queue, replacement Gateway, or private release artifact.
- Reviewed current NemoClaw prior art and retained its official artifact pinning, ownership-aware process control, fail-closed probes, and staged recovery lessons without claiming that its shared-Gateway sandbox delete path closes the late-create race.
- Proved on A1 that killing and discarding a complete stock OpenShell epoch containing the Gateway, containerd, dockerd, state roots, sockets, and JWT material fences a held Docker create without modifying OpenShell.
- Promoted the stable capacity-release invariant into the Core Sandbox document and accepted `docs/specs/20260715-openshell_disposable_cell_lifecycle.md` as the concrete single-slot, cold-Cell implementation contract.
- Local and A1 fork checkouts, patched build outputs, and test-epoch artifacts were removed; the GitHub fork was deleted and verified absent with `404 Not Found`; the retained A1 CLI and Gateway binaries remain stock `0.0.80`.
- The remote OpenShell Gateway specification was retired without a compatibility path; active-spec alignment is now 36 Implemented, 18 Partial, 0 Diverged, and 6 Not Started across 60 active specifications, with 58 archived specifications.
- The local controller, privileged helper, backend lifecycle, one-slot scheduler baseline, and helper-level A1 acceptance are implemented under C04, C16, S26, and S27. One real NanoCore Worker turn through the replacement Cell remains before the specification can become Implemented.
- Built `openkit/worker-codex:dev` natively on A1 with host-network package resolution, passed the isolated image smoke with Node `24.18.0` and Codex `0.144.1`, and saved the 899 MB arm64 worker archive beside the exact 14 MB stock supervisor archive in the root-owned Cell image cache.
- Exercised normal prepare/recycle, intentional fresh-start failure, same-owner recovery, idempotent recycle retry, wrong-owner recycle, and second-owner prepare against the installed helper. The final stock replacement is healthy and empty at epoch `7`; the retry retained `7 -> 7`, and ownership mismatches did not signal, delete, or change the epoch.
- Landed the official-artifact disposable Cell command surface in `eca6df8`, the NanoCore local/remote lifecycle, product scheduler projection, durable target binding, bounded CLI execution, retry-safe helper, and remote backend path in `5bcbf68`, and the A1-discovered worker-shim E2E fixture correction in `f156187`.
- Rebuilt and smoked the worker image natively on A1, ran the local worker-shim sandbox E2E, and ran the Mac-to-A1 remote backend E2E through an operator SSH local-forward. The remote E2E materialized a sandbox, executed a command, downloaded its result, and recycled the complete Cell into a fresh empty epoch.
- Proved same-boot Docker control-plane loss fails closed with the owner and roots retained, same-owner cross-boot recovery removes the stale epoch, and a post-fence root-removal failure persists the `fenced` marker and bridge identity for a Docker-independent retry. Wrong-owner recycle remained inert and same-owner idempotent retry preserved epoch `20 -> 20`.
- Verified the former `lingkaix/OpenShell` GitHub fork remains absent with HTTP 404, no active patch or fork artifact exists in the repository or A1 home, and A1's package-owned CLI and Gateway both report stock `0.0.80`.

### 2026-07-16: Phase 0 Refresh And Foundation Audit

- Refreshed the Current Baseline and active-spec index, appended S62-S66 without renumbering, reconciled every ledger metadata value, and marked the confirmed Web projection and Goal delegation gaps Partial.
- Closed C00-F01 through C00-F06 by narrowing C00, promoting one compact scope/fallback/compromise invariant, and removing Product Vision, Storage, Vault, and Metering ownership conflicts. Routed C00-F07 through C00-F10 into existing owning rows rather than adding another tracker or implementing out of sequence.
- Passed the spec lifecycle tests, lifecycle validator, repository check, exact active-ledger/index comparison, and whitespace validation. No C00 runtime code, generic scope engine, fallback framework, validator, package test, or L6 harness was added.

### 2026-07-16: C01 Core Concepts Audit

- Reduced Core Concepts to its backbone and relationship authority, removed duplicated aspect definitions, the static identifier registry, universal UUIDv7 encoding, and implementation-specific manifest and evolution mechanics, and aligned the Core ownership index and direct projections with their owning documents.
- Closed C01-F01 as an implementation defect: the orchestrator now persists the selected Agent on the Turn, Worker and Simulator executors consume only that exact assignment, AgentProfile and AgentSession lineage is persisted after Session creation, and dashboard, feedback, review, and interrupt paths no longer infer ownership from defaults or recent Sessions.
- Kept the correction deletion-first: removed the attribution resolver, fallback selection, duplicate assignment ownership, Thread-scoped Simulator Session reuse, and single-use helpers; no compatibility path, workflow state, runtime abstraction, or test platform was added.
- Routed C01-F02 to C02 and S16 because Artifact records can still be created without the accepted item-backed lineage. C01 did not invent a new schema or implementation before the owning Work Model and interaction specification are audited.
- Landed the Core authority correction in `84dee53`, the failing regression coverage in `7f6893e` and `54c4896`, and the implementation correction in `2e7a92c`. The complete NanoCore suite passed with 1,965 tests and 7 skips, together with NanoCore lint, typecheck, build, focused lineage tests, Core/spec lifecycle validation, and repository checks.

### 2026-07-16: C02 Work Model Audit Completed

- Narrowed Work Model to durable product-work authority, promoted Quick Chat and the four Human Attention modes, and moved exact Item, Action Center, input-delivery, and resource-interaction mechanics into their owning specifications.
- Closed the C01 Artifact finding: every turn-bound Artifact creation or revision maintains one exact versioned `artifact-reference` Item; handled write failures restore prior state; refinement and redo reuse the source Turn's exact Agent and reject an unrelated active Turn before claiming review.
- Made both unsupported input paths truthful. The generic direct-Turn adapter returns typed `thread_busy` before recording input, while Goal steering returns `goal_steering_delivery_unavailable` with no Item, pending row, command, Turn, scheduler admission, or Web submission control.
- At this checkpoint, real-worker tracing proved that Goal launch passed only its objective and lacked a generic immutable worker Context Package trace, so C02 removed speculative acceptance and recovery paths instead of adding another workflow. WP-4 later completed Goal launch and steering through the existing S39 Turn-owned trace, and that later checkpoint is the current implementation fact.
- C09 retains cross-file process-crash atomicity. S16 remains Partial for workspace-only Artifact provenance, explicit Thread introduction, Workspace Material identity, revision history, binding, worker-availability proof, conflict-safe apply, and complete Plane 1 acceptance.
- Selective rehydration fixed the last ambiguous Artifact-review error: a reviewed durable Artifact whose source Turn or assigned Agent is missing must return `409 recovery_required` before a review claim. Current NanoCore still maps that case to generic `artifact_review_failed`, and workspace-only Artifact introduction remains unimplemented; both stay explicit S16 implementation findings rather than being hidden by the completed Core audit.

### 2026-07-16: C03 Architecture Boundary And Web Truth

- Narrowed Architecture to App/Core/Agent authority, Core-owned product truth, adapter translation, Internal Core Roles, and governed workspace services; removed backend, storage, provider, deployment, and role-procedure detail already owned elsewhere.
- Closed the Web-local Goal plan truth defect through existing Core Client and NanoCore revision authority: Reject and Request changes persist durable revision input, use the returned Goal, retain the plan on failure, and never create a Web-only terminal state.
- Classified the generic NanoCore internal-agent runner, registry, event loop, hooks, private stream, tool allowlists, and diagnostics ledger as a `DESIGN-DEFECT`: two aggregate Quick Chat calls do not authorize a second execution runtime, while deterministic Workflow Coordinator never consumed it. The owning specifications now require one direct Quick Chat executor; deletion remains the active C03 implementation slice.
- Narrowed Workflow Coordinator to three exact request-scoped deterministic V1 output shapes while preserving its Core responsibility for semantic routing, worker-request composition, planning, and stopping. Mode services perform authorized Knowledge reads, persist and materialize accepted context, request scheduler and runtime effects through their existing owners, apply durable workflow mutations, and read Action Center and evidence owners. The current Goal route still mutates from the lower-level stop result before creating the Coordinator summary, so S15 remains Partial until the Coordinator decision is consumed before mutation. Narrowed Knowledge Manager to explicit deterministic operations and removed passive event hooks, scheduled calls, model execution, private lifecycle authorization, and the stale rollout step that still implied those loops.
- Marked Task Mode Partial because its launch does not deliver the complete Coordinator-composed request or Knowledge material into worker input, its command identity is optional, and Task-to-Goal escalation ignores exact request replay. Replaced the unimplemented and underspecified automatic retry with an explicit new-invocation compromise, removed the stale rollout checklist, and added direct delivery plus exact replay, conflict, and half-state predicates.
- Replaced the speculative worker-checkpoint state and field examples with the exact V1 stage set and durable `WorkerCheckpointRecord`, including the no-row-is-idle and terminal-stage predicates.
- The direct-executor audit also found that standalone Quick Chat exposes generic caught messages with HTTP 400 while Chat Mode uses a different fallback, and the Chat outcome schema still admits a success-shaped `failed` result that no route owns. S05 and S11 now fix the shared redacted timeout, abort, invalid-response, provider, and internal error mapping plus the explicit new-invocation retry compromise; C03 implementation must align the two routes and remove the unused outcome without adding replay or settlement state.
- Routed Core runtime-family inference and closed `codex | opencode` product schemas to the already accepted Worker Agent Adapter Boundary plan instead of creating a second adapter framework inside C03.

### 2026-07-16: G01 Contract Group Authority Checkpoint

- Audited Work Model, Agent Workflow, the Worker Turn and Human Attention contracts, Chat, Task, Goal, Quick Chat, Workflow Coordinator, the Work and Resource Interaction Model, and their App API projection as one contract group. No document is closed independently, and preliminary edits outside G01 remain non-completion evidence.
- Fixed the authority map: Coordinator owns request-scoped routing, complete Goal Plan, semantic worker-request, and Goal-stop decisions; Chat, Task, and Goal services apply those decisions through their named Turn, Item, checkpoint, command, Goal, Plan, Goal Task, and Review tuples. Scheduler, worker control, context, Knowledge, review, evidence, and workspace services retain their existing records and effects. Task Mode has no durable Task record, and no mode service copies another owner's lifecycle.
- The group contract reuses the existing command-idempotency ledger for Chat, Task, Goal, Review, steering closeout, and interrupted-worker retry. Lookup precedes mutable projections and effect selection; resolved defaults and decisions stay outside caller input hashes. Chat-to-Task, Chat-to-Goal, and Task-to-Goal use the outer command as the sole receipt owner while deriving one downstream business tuple, so nested mode calls do not publish a second command. Chat provider execution uses the ordinary Turn and user Item as its pre-dispatch attempt owner; a crash may require a new request rather than a settlement workflow.
- The group contract reduces the Worker Checkpoint stage set to `preparing`, `running_worker`, `waiting_for_user`, and the three terminal stages. Review, verification, saving, and reconnect remain with their existing owners. Core gate responses attach to the same active Turn, resume it only to close the stopped envelope, and never imply worker Session resume; any further worker execution uses a new command and Turn. Every uncleared terminal checkpoint stays discoverable until mode-conditional durable closeout, projections are not clear authority, retry is fenced by scheduler reconnect authority, and caller-selected terminal clear is removal-only.
- Closed the StopReason and gate boundary without adding state: every StopReason now maps to one durable Turn status and Worker Checkpoint stage, Goal worker user-input and approval gates share one exact closeout tuple, and planning and worker gate receipts publish only after their named Items, Turns, checkpoints, Goal Tasks, and Goal state are durable. Exact replay may finish only deterministic writes fixed by the original gate identity; contradiction fails closed as `recovery_required`, with no settlement workflow.
- Added one immutable app-local `GoalPlanRecord` because the lightweight protocol plan Item cannot own the executable payload. Coordinator returns the exact full plan, `goal.plan` persists it with its visible plan Item, approval accepts only `planItemId`, verifies digest and acyclic dependencies server-side, and creates complete immutable Goal Task execution snapshots. The current client-resubmitted plan, missing dependency-cycle check, split planner authority, and dropped task execution fields are defects; no second planning workflow or generic Item payload was added.
- Every V1 Goal worker step now requires an approved immutable Plan, while low-risk work uses one lightweight Task rather than a planless branch. Approval assigns stable array order, starts dependency-free Tasks as ready and the rest as pending, and leaves no current Task until `goal.step` wins the launch fence.
- Made Goal, Goal Task, and Goal Review records executable contracts with exact fields, transitions, terminal predicates, pre-launch Task/checkpoint CAS fencing, pre-mutation Coordinator input, Workspace transaction boundaries, truthful failure mapping, and bounded step replay. Removed unsupported general Goal abort, planless execution, unowned `verifying`, `needs_revision`, and `skipped` states, and Goal Review `ask_user`, `decompose`, and false `block -> ask_user` semantics. An unresolved Goal Review now owns fixed evidence with a null decision; the human resolution command supplies exactly `accept`, `refine`, `retry`, or `abort` and stores the decision plus immutable snapshot atomically. At this contract checkpoint, the preselected verdict and accept-only implementation remained defects; the later Goal Review checkpoint below records their correction.
- Tightened first-writer and product-boundary outcomes: Goal Review resolution uses one unresolved compare-and-set and maps `abort` to a failed Task plus aborted Goal; Quick Chat Task or Goal intent becomes one durable Chat-owned `project_workspace_required` refusal with no downstream tuple; provider failures retain their exact typed codes, while `chat_mode_persistence_failed` is limited to post-provider answer persistence.
- Defined Goal Steering closeout without another workflow: application, follow-up, and cancellation first win a three-field CAS claim on the one Thread-unique pending row, then write the exact Context Package, deterministic completed Core-local follow-up history Turn plus copied Item and terminal command record, or cancel-command record. Send and terminal request identities remain distinct, and each terminal proof preserves the original Item and exact Material tuple when applicable. Only the winning claimant may clean a crash-residual row; competing claims return `conflict`, incomplete proof returns `recovery_required`, cleanup makes the singular current `activeDelivery` null, and no path redelivers or retargets input.
- Narrowed S16 instead of filling conceptual gaps with a framework: `applied` means delivery to one proven worker Turn under the same Goal, not plan mutation; Workspace-only Artifact introduction is the exact idle-Thread `artifact.introduce` command and deliberately returns unrecorded `thread_busy` during any active Turn; grounded annotation, locator, and text-range patching are deferred until a separate accepted specification. The current Material slice names its mutation commands and keeps existing review/apply owners.
- Selectively updated Core Protocol and Runtime Model only where G01 exposed shared Turn, active-input, idempotency, session, Artifact-lineage, and audit-authority invariants. These edits repair G01 interfaces but do not close G02 or G03; those groups still require their own grouped code, persistence, failure, and verification audits.
- Corrected Quick Chat current truth by recognizing existing Workspace registry ownership, owner membership, project-operation guards, and tests while retaining the missing owner-independent root and complete sharing-lifecycle rejection as Partial work. Narrowed Core Steering language so unsupported interruption is not re-authorized, and restored accepted design authority over conflicting executable projections in the App API guide.
- Recorded the implementation blockers rather than hiding them behind document acceptance: generic internal-agent deletion; exact outer-command replay; direct Chat attempt, clarification continuation, and Quick Chat refusal ownership; complete Coordinator plan and worker-request delivery; immutable Goal Plan persistence and validation; truthful Goal Review input and first-writer CAS; Goal command identity and gate destinations; Goal-step launch and closeout transactions; Task stop mapping; route-wide failure remapping; checkpoint schema, StopReason mapping, discovery, and mode-conditional cleanup; reconnect-safe request-bound retry; unsafe terminal-clear deletion; state and verdict schema removal; first-writer steering delivery proof; exact Artifact and Material command surfaces; removal of the unversioned generic Artifact metadata PATCH; and atomic Material apply publication. G01 remains active until those defects receive test-first corrections and group-level L0-L6 verification.

### 2026-07-17: G01 Goal Graph And Checkpoint Authority Correction

- Added the failing Goal Plan dependency-cycle regression in `9ad365b` and the bounded existing-validator correction in `90b6bd2`. The validator now rejects missing references and cyclic task graphs before Goal Task writes without a graph framework or dependency, but the route still trusts a caller-resubmitted plan and changes Goal state before task validation. Immutable `GoalPlanRecord` authority, digest verification, self-dependency error mapping, and one atomic approval transaction remain open; this slice is not Goal Plan completion evidence.
- Added the failing public-route regression in `9ad8252` proving that a caller-supplied `terminalStage` cleared a live checkpoint. Removed that authority from the legacy MCP surface in `690cdd2`, Core Client in `3247a9a`, NanoCore route and Action Center projections in `1bb73e6`, App API schemas in `0267b30`, and the regenerated OpenAPI projection in `5e3fc53`. The existing internal cleanup function remains only for mode-owned closeout calls; no replacement command, settlement record, compatibility alias, or harness was added.
- Passed the focused Goal task-persistence test, the five affected NanoCore suites with 188 tests, the complete NanoCore suite with 1,965 passed and 7 skipped tests, App API schema tests with 62 tests, Core Client tests with 23 tests, legacy MCP tests with 139 tests, all four affected package typechecks and builds, package lint checks, and OpenAPI generation and validation. The Core Client run also corrected its stale Goal steering fixture to the already accepted immutable queued-receipt shape instead of retaining the obsolete mutable Goal projection.
- Kept G01 active. The next shared command-boundary slice is Chat and Task outer-command replay before Coordinator or downstream effects; reconnect-fenced worker retry, exact checkpoint schema and discovery, immutable Goal Plan approval, generic internal-agent deletion, and the remaining recorded G01 defects are still open and must be handled without widening the runtime or acceptance harness.

### 2026-07-17: G01 Chat Outer-Command Replay Correction

- Added the failing Chat-to-Task replay and conflict regression in `6ea68f4` and the missing-request schema regressions in `2af0ea1`. The test fixes one accepted outer Chat request id, proves replay returns the same Chat Turn, Item, and handoff lineage, proves changed input returns `idempotency_key_conflict`, and proves neither replay nor conflict adds a Turn or worker launch.
- Required Chat and Task request ids in the App API schemas in `e8a3a95`, retained caller convenience by generating omitted ids in Core Client in `7db81f0`, and removed the hidden NanoCore worker-start UUID fallback. This is a clean contract change with no compatibility alias or optional server path.
- Reused the existing command-idempotency ledger and in-flight duplicate map in `39c1bb5`. `chat.start` lookup now precedes Workspace-state reads, Coordinator, provider selection, and downstream Task or Goal effects. The receipt points to the original Turn and stores a prompt-free accepted-response snapshot containing the result Item and original Turn state; replay reuses the existing initiating user Item instead of copying its prompt or creating another business lifecycle.
- Added the mutable-clarification and missing-owner regressions in `48ce4d4`, then corrected replay in `6b053e9`. Later Turn and gate updates no longer combine with a frozen old outcome, and a missing, malformed, wrong-kind, or contradictory receipt owner now returns `409 recovery_required` rather than a generic 404.
- Kept the compromise explicit: this slice proves normal replay and process-local concurrent duplicate collapse once a receipt exists, but it does not add deterministic owner ids or repair a crash between a downstream effect and receipt publication. That half-state remains `recovery_required` design work under the existing Turn, Item, Goal, checkpoint, admission, and command owners; no reservation row, settlement workflow, mode runtime, runner, or harness was added.
- Refreshed the generated OpenAPI contract in `393ef9c`. Passed 62 App API schema tests, 23 Core Client tests, 139 legacy MCP tests, 32 focused Chat and Task tests, all affected package lint, typecheck, build, and OpenAPI validation gates, and the complete NanoCore suite. The first sandboxed full run failed only because the real HTTP disconnect test could not bind `127.0.0.1`; the focused test and complete suite passed outside that network sandbox.
- Kept G01 active. Direct `task.start` replay, deterministic Chat and Task owner identities, crash half-state reconstruction, direct Chat attempt and typed failure persistence, clarification continuation, Task launch fencing and Context Package delivery, generic internal-agent deletion, and the other recorded G01 defects remain open.

### 2026-07-17: G01 Receipt Authority Design Correction

- Re-audited Chat and direct Task replay against Core Protocol, App API, Chat, Task, and Workflow Coordinator as one G01 document-and-implementation group. The prompt-free Chat snapshot committed in the preceding slice and the uncommitted direct Task draft both copied most of a public response. Removing the prompt did not make those bodies valid command evidence: they duplicated Turn, Item, completion, and Coordinator authority and exceeded the Core rule that a bounded snapshot may contain only stable identifiers and closed outcomes.
- Rejected the uncommitted direct Task whole-response snapshot before implementation commit. The accepted Task contract now uses no payload snapshot: the receipt names the original response Turn, and replay projects current Task state, completion, evidence, or Goal escalation from the existing Turn, Item, Artifact, Review, and Goal owners. The unowned public Coordinator decision projection is removal-only; Coordinator output belongs in the immutable worker request and Context Package delivery path, not in the command ledger.
- Narrowed the sole Chat exception to `resultKind`, result and user Item identifiers, HTTP success status, and downstream Task Turn or Goal and Goal Turn identifiers. Replay must use durable Item revisions and downstream owners to reconstruct the accepted response. Prompts, explanations, Turn or Item bodies, assistant content, provider output, Coordinator output, and full response bodies are forbidden.
- Kept the compromise explicit: completed-receipt replay is corrected without a Task row, reservation, settlement, recovery workflow, runner, or harness. Deterministic request-derived identities, crash-before-receipt half-state handling, scheduler-admission proof, and immutable worker-request delivery remain later G01 work. Implementation and regression evidence for this correction are pending; G01 remains active.

### 2026-07-17: G01 Owner-Derived Mode Replay Implementation

- Added the grouped replay regressions in `dfd0460`, `478d763`, and `ac84d2f`. They prove direct Task worker and Goal replay, exact Chat provider, Knowledge, refusal, clarification, and Goal-handoff replay, input conflict, no repeated downstream effect, and fail-closed behavior for missing or contradictory durable owners. The tests extend existing NanoCore suites and add no runner, fixture framework, or acceptance surface.
- Removed the unowned public Task Coordinator decision from the App API schema in `5b3e9ac`, deleted its invalid NanoCore and story-runner assertions in `4b3be7c` and `abf6bf6`, deleted the remaining Core Client fixture in `ae940db`, and regenerated OpenAPI in `5da890d`. The internal decision remains launch input pending its accepted immutable worker-request and Context Package owner; it is no longer a public replay payload.
- Implemented both completed-receipt paths in `d524290` by reusing the existing command ledger and in-flight duplicate map. Direct `task.start` performs replay lookup before Coordinator and effects, stores no payload snapshot, and projects from the existing Turn, Item, Artifact, Review, and Goal owners. `chat.start` stores only `resultKind`, HTTP success status, and required downstream owner identifiers; its normal receipt already names the response Turn, and deterministic Chat Item identifiers are derived rather than stored.
- Reconstructing exact Chat acceptance now uses the first durable user and result Item revisions, their timestamps and lifecycle, fixed closed-result semantics, and validated Task or Goal lineage. Terminal replay additionally requires the current Turn to remain completed with the same completion timestamp, while a clarification may replay its original accepted gate after legitimate current-owner progress. Contradiction returns `409 recovery_required` and performs no repair mutation.
- Kept the implementation inside the existing mode route and command ledger. No table, Task record, durable state, module, dependency, recovery coordinator, settlement workflow, runner, or harness was added. The accepted availability compromise remains: this slice handles replay only after a receipt exists; crash-before-receipt reconstruction and scheduler-admission fencing must use request-derived identities and existing owners in a later G01 slice.
- Passed 62 App API schema tests, 23 Core Client tests, 139 legacy MCP tests, 142 NanoCore server tests, 15 Chat tests, 9 OpenAPI tests, and 16 existing real-Task-runner unit tests. The complete NanoCore suite, NanoCore lint, typecheck, and build, all affected package lint, typecheck, and build gates, and OpenAPI generation and validation also passed. G01 remains active; this evidence closes only the grouped completed-receipt correction, not any individual document or the full workflow group.

### 2026-07-17: G01 Task State Authority Cleanup

- Audited the Task state schema, NanoCore projection, real-worker story, and downstream consumers as one G01 owner boundary. `needs-review` had no Task lifecycle owner and duplicated independent Workspace Review and Action Center authority, while a cancelled Turn was mislabeled `blocked`. The accepted S12 state table already rejected both choices, so no new design surface was needed.
- Added the failing schema, cancelled-replay, and real-story regressions in `2d14695`. Updated the clean App API contract in `113ea76`, preserved cancelled Turn truth in `72eeed4`, deleted the real runner's `needs-review` acceptance branch in `0186fd9`, and refreshed generated OpenAPI in `5bcda3e`. The slice is a net deletion and adds no record, state machine, compatibility alias, helper, runner, or dependency.
- Passed 62 App API schema tests, 23 Core Client tests, 139 legacy MCP tests, all 16 existing real-Task-runner tests, the complete NanoCore suite, all affected package lint, typecheck, and build gates, and OpenAPI validation. G01 remains active because ordinary Task states still need their required checkpoint and terminal StopReason tuple, and crash-before-receipt launch fencing remains unresolved.

### 2026-07-17: G01 Direct Quick Chat Runtime Boundary Correction

- Audited S05 Worker Reliability, S11 Chat, S15 Workflow Coordinator, the App API diagnostics contract, NanoCore composition, Core Client, and Web diagnostics as one G01 contract group. Core Architecture and Agent Workflow already contained the required stable boundary, so they were verified without duplicating implementation details or declaring either document complete.
- Added the failing public-contract guards in `e1df7a2`, `ceb7929`, and `2e46197`, the direct-call reliability tests in `3f32c83`, and the malformed and schema-invalid request guards in `7be705b`. The tests require strict rejection of the removed diagnostics fields, no generic runner in NanoCore composition, no Web internal-agent panel, rejection before provider dispatch for invalid input, the exact fixed Quick Chat system-and-user projection, bounded timeout even when a provider ignores cancellation, caller abort, invalid content, redacted typed and unexpected failures, and preservation of usage, cache, credential, and replay behavior.
- Removed the public generic diagnostics schema and its `internalTasks` default in `5bb1661`, removed the Core Client fixture in `8c4087c`, deleted the NanoCore runner, registry, loop, hooks, tool allowlists, provider-output schema, private event and streaming vocabulary, diagnostics ledger, and their dedicated tests in `de8a25e`, and deleted the Web projection in `717c3ed`. Workflow Coordinator remains a pure deterministic function over caller-owned input, while Quick Chat now uses one concrete role function over the existing provider resolver and Gateway dispatcher.
- The direct role function preserves the fixed instruction, caller and cache lineage, durable usage attribution, credential resolution, a 30-second rejection race, downstream cancellation, non-empty assistant-content validation, typed provider mapping, exact `504` timeout, `499` abort, `502` invalid-response, and `500` unexpected-error mapping, with every public message and provider detail passing the existing redaction boundary. The owning route now rejects malformed or schema-invalid input as `400 invalid_request` before provider selection or dispatch through `bc96170`. It creates no table, state, record, registry, lifecycle, event protocol, hook chain, workflow, dependency, runner, or harness.
- Across App API schemas, Core Client, NanoCore, and Web, the correction changed 29 files with 397 additions and 4,937 deletions, a net deletion of 4,540 lines. Excluding generated OpenAPI, production and guide code is net minus 2,468 lines and tests are net minus 1,814 lines; this is the deletion-first correction required by the accepted boundary rather than a smaller replacement framework.
- Verification passed all 62 App API schema tests, 23 Core Client tests, 123 Web tests, the affected NanoCore Quick Chat, diagnostics, delegation, and Coordinator suites, the typed-provider server regression, affected package lint, typecheck, and build gates, and OpenAPI generation and validation. The updated Quick Chat suite passes all 19 tests, including the two new pre-dispatch validation cases. The full NanoCore run recorded 1,938 passes and 7 skips; its only sandboxed failure was the existing real HTTP disconnect test being denied a loopback bind with `EPERM`, and that exact test passed when rerun with the required loopback permission.
- A deletion-only follow-up proved that no Chat producer or consumer used `ChatModeOutcomeSchema.failed`, rejected that value in `2ba91fb`, removed it from the App API schema in `23bfd19`, and refreshed generated OpenAPI in `3115866`. It adds no route branch, fallback, record, or failure workflow.
- The checkpoint-stage follow-up proved that `reviewing`, `verifying`, `saving`, and `recovering` had no production writer and conflicted with the accepted S05 owner map. Tests `e479582` and `916807f` reject them, `2b18b5c` and `4005408` remove them from App API, runtime, export, and import schemas, and `dd09b3d` refreshes generated OpenAPI; the slice is a net deletion and adds no transition or recovery mechanism.
- The Goal lifecycle follow-up proved that `verifying` had no transition owner and duplicated the separate verification-evidence record. `0d06685` rejects it publicly, `8ea17f0` and `014774a` remove it from App API, NanoCore, export, and import contracts, `b4fb4cb` refreshes OpenAPI, and `6d4c640` proves the workspace-import trust boundary rejects the stale value; no migration or alias was added.

### 2026-07-17: G01 Goal Task Skipped-State Contract Correction

- The Goal Task follow-up audited the accepted Goal Mode specification, App API projection, NanoCore lifecycle and workspace-portability boundary, generated OpenAPI, downstream first-party fixtures, and the superseded historical repair record as one contract group. No normal lifecycle path wrote `skipped`; portable import was the only remaining ingress, and the state had no successor, recovery rule, or independent completion predicate.
- `0e6367e` first made the public schema rejection executable. `44884e6` removed the state, count, and terminal-summary projections from App API schemas; `b7c9811` removed NanoCore storage, import, completion, audit, and read-model handling while extending the existing import trust-boundary regression; `49d9fd9`, `76cfe6c`, and `2d6cfbb` deleted stale Core Client, Web, and Story fixtures; and `c0be07a` refreshed generated OpenAPI. The slice adds no fallback state, transition, migration, compatibility alias, helper, runner, or workflow.
- Verification passed all 62 App API schema tests, 70 focused NanoCore Goal and workspace-portability tests, 23 Core Client tests, 123 Web tests, all 20 existing real-Goal-runner tests, affected package lint, typecheck, and build gates, and OpenAPI generation and validation. Goal verification status, Workspace materializer and sync skip results, test-runner skip results, and Review-gate skipping remain separate owned concepts and were not changed.
- G01 remains active. This checkpoint closes only the generic runtime, direct provider-call boundary, success-shaped failure-enum, checkpoint-stage authority, Goal-verification-state, and unowned Goal-Task-skipping defects. Chat provider failures still lack their durable terminal Turn, Item, and command receipt; crash-before-receipt reconstruction, clarification continuation, Task launch fencing and immutable worker-request delivery, Goal plan and stop authority, gates, reviews, steering, Artifact and Material commands, checkpoint request lineage and terminal discovery and closeout tuples, reconnect-fenced retry, and the other recorded G01 defects remain open.

### 2026-07-17: G01 Goal Review Authority Contract Rehydration

- Audited Goal Mode Coordination, Human Attention and Intervention, the App API projection, Goal Review storage and portability, decision routes, Action Center, Core Client, Web, legacy MCP, generated OpenAPI, and existing L1-L3 tests as one G01 contract group. The accepted verdict and transition semantics were already clear, but the durable Review and immutable replay snapshot fields were not exact enough to authorize one implementation.
- Selectively rehydrated only that missing contract before implementation. The record now has one fixed creation-and-evidence tuple and one all-null or coherently resolved decision tuple; the bounded snapshot contains only Task identity and status, Goal identity and terminal fields, one closed outcome, and a non-authoritative next-ready Task id. Action Center reuses four existing action kinds, and clients must collect required text rather than adding a generic action-payload framework or preselecting a verdict.
- Implemented the contract deletion-first. App API commits `c5e364a`, `90b1658`, `a523f56`, `6424f46`, and `abcc3ac` narrowed the verdict, request, response, Task-state, and bounded-snapshot schemas and removed the unreachable Review `continue` result. NanoCore tests `d8fc025`, `8aa0d2b`, and `77183b9` preceded implementation `3fa1420`, which removed `needs_revision`, unsupported verdicts, the preselected tuple, the 182-line duplicate decision interpreter, full-record snapshots, verdict-bearing Action Center sources, and nullable worker Turn lineage while adding exact first-writer, replay, conflict, stale, recovery, owner-transaction, and portability enforcement. The NanoCore production diff is net minus 24 lines.
- Aligned existing first-party paths without adding a payload framework. Removal-parity MCP tests and mapping landed in `2cf8fa7` and `fb6ee8e`; Core Client tests and the required decision-input signature landed in `307e5af` and `166bffa`; Web tests, exact fixtures, and the four-action handler landed in `7e8dd17`, `1a4a1aa`, and `2ae189a`. Web uses the accepted native-prompt compromise for required text and leaves the Review unresolved on cancellation or empty input. Generated OpenAPI was refreshed and validated in `124a94d`.
- Verification passed 214 focused existing NanoCore Goal Review, Action Center, server, migration, and portability tests; all 62 App API, 23 Core Client, 127 Web, and 139 removal-only MCP tests; and the complete NanoCore suite with 1,943 passes and 7 skips. Its sole sandbox failure was the existing real HTTP disconnect test being denied a loopback bind with `EPERM`, and that exact test passed when rerun with loopback permission. All five affected packages passed lint, typecheck, and build; OpenAPI generation and validation and `CI=true pnpm run check:repo` also passed. No table, workflow, recovery coordinator, runner, harness, dependency, compatibility alias, generic Action schema, external fork, or patch was added.
- G01 remains active after this Review boundary closes. Refine and retry still require the next Coordinator worker request to carry the resolved Review and prior-attempt evidence, while Goal launch fencing, immutable worker-request delivery, Goal Plan authority, gate closeout, checkpoint discovery, reconnect-fenced retry, steering, Artifact and Material commands, and the other recorded G01 defects remain open as grouped work.

### 2026-07-17: G01 Coordinator Worker Request And Review Continuation Contract Rehydration

- Audited Core Agent Workflow, Task Mode, Goal Mode, Workflow Coordinator, structured delegation, Goal Review authority, scheduler Turn input, AEP input, Turn-owned Items, Task and Goal routes, and their existing route tests as one G01 contract group. Both real mode paths currently discard the accepted structured request and launch only a caller or Task objective string; Goal Mode also creates a separate generic Coordinator request before preparing a second Task-specific request and never reads a resolved Review.
- Selected one request and one existing proof path. The complete schema-parsed `StructuredWorkerDelegationRequest` is the accepted semantic request, compact JSON is the V1 text-adapter compromise, and the existing Turn-owned `user-message` Item retains the exact delivered bytes. Scheduler, AEP, worker, and Item delivery reuse the same input; no payload table, delivery receipt, settlement record, consumed marker, workflow, runner, or harness is authorized.
- Added the exact nullable `reviewContext` contract for Goal continuation and kept it null for Task and initial Goal attempts. The Goal service inspects only the deterministic latest Review for the selected ready Task, requires the exact resolved `refine` or `retry` snapshot, and otherwise fails contradictory state as `recovery_required` before mutation. The context carries only Review id, verdict, nullable reason and instruction under their verdict rules, prior Turn id, and Item and Artifact evidence ids; Review verification evidence is not copied.
- Accepted a bounded fallback: eligible Review context is sticky across later attempts of the same Task until a newer Review supersedes it or the Task terminates. Repeating the correction after interruption is preferable to inventing one-shot consumption lineage. At this contract checkpoint, tests and implementation were pending; the grouped implementation evidence below supersedes that current-state note without rewriting the decision history.
- Rechecked the implementation boundary before accepting code and found that the exact Coordinator input omitted the Goal-owned facts required to produce its specified output. The contract now permits one bounded `workerRequestDetails` value containing acceptance, artifact, constraint, verification, review-policy, and Review-context facts but forbids objective, refs, schema, request identity, adapter payload, or durable owner fields. This is an explicit scope compromise: the mode service derives authorized facts from existing records, Coordinator remains the only final request composer, and no second request, record, or lifecycle is introduced.
- Added the existing-route regressions in `0bf9719`. Direct Task must persist the exact regenerated Coordinator request as the Turn-owned user Item, while one real Goal route sequence proves initial null context, refine continuation, retry continuation, new Turn lineage, and unchanged prior Turn, Review, Item, and Artifact owners. No test file, helper framework, runner, L3 platform, or A1 harness was added.
- Clarified the previously missing exact Coordinator input in `11f2e4e`, then implemented the grouped correction in `0ce4fd0`. Mode preparation now returns only authorized objective, source refs, and bounded request facts; the private Coordinator selector remains behind the single decision function, Coordinator-owned objective and refs cannot be overridden by details, and only Coordinator creates the final schema-valid request. Task, Chat-to-Task, and Goal serialize that one request as compact JSON for the existing scheduler, AEP, worker, Turn, and Item input path. Goal continuation reads only the deterministic latest Review, fails contradictory state as `recovery_required` before mutation, and applies the accepted sticky fallback without a consumed marker.
- The NanoCore implementation commit changes six production files by 289 additions and 106 deletions, net plus 183 lines; updates to five existing internal test files are net plus one line. It adds no table, record, durable state, state machine, workflow, delivery receipt, settlement, runner, harness, package, dependency, external fork, patch, compatibility alias, or generated public schema.
- The final group audit prevented two false closeout claims. Current Goal Task storage omits the accepted Plan's `planItemId`, resources, expected Artifacts, review policy, and escalation conditions, so Goal request delivery is schema-complete and exact for its current persisted facts and bounded defaults but not yet semantically complete for the approved Task snapshot. The grouped current projections now say so explicitly, and full request-fact persistence remains with the existing immutable Goal Plan and Goal Task correction rather than being patched with another payload owner.
- The same audit found that Coordinator emitted `confirm_worker_turn` for a selected worker while Task Mode silently projected no approval. Tests `cf6fed2` first require `requiredUserAction=none`; fix `0e4e5f5` makes selected worker decisions use that value, rejects any other value before mode mutation, and deletes the laundering branches from Task and Goal projections. The production follow-up is net minus five lines and adds no gate, approval, record, or compatibility path.
- Verification passed 188 focused Coordinator, delegation, preparation, Task-route, and Goal-route tests; NanoCore lint, typecheck, and build; and the complete NanoCore suite with 1,943 passed and 7 skipped tests. The sandboxed full run's sole failure was the existing real HTTP disconnect test being denied a loopback bind with `EPERM`; that exact test passed outside the network sandbox. `CI=true pnpm run check:repo` and `git diff --check` also passed. G01 remains active for Context Package materialization, request-bound launch fencing, Goal Plan and stop authority, gates, steering, terminal closeout, recovery, Artifact and Material commands, and the other recorded grouped defects.
- Opened the next G01 slice as one immutable Goal Plan and Goal Task authority group across Core Work Model, S12, S13, S15, App API, NanoCore guidance, schemas, storage, approval, Coordinator request composition, portability, and their existing tests. The audit confirmed that the server currently returns the full Plan without persisting it, accepts a caller-resubmitted Plan at approval, writes only a partial Goal Task snapshot, mutates the Goal before graph validation, and replaces omitted approved facts with request defaults. It also found a contradictory ready-task response for dependency-bound Tasks and an unauthorized mutable `orderIndex` update path.
- Stopped implementation because the accepted documents did not uniquely map the full Goal Task snapshot into one worker request. The grouped contract now makes the immutable Goal Task the only task-specific request-fact authority and defines a lossless mapping for objective, criteria, resources, expected Artifacts, context budget, verification, human review policy, escalation conditions, and latest eligible Review context. Resource declarations remain exact semantic input while `contextRefs` retain separately authorized delivery ownership; incomplete or mismatched Task facts fail as `recovery_required` without defaults or reconstruction.
- Removed three conflicting concepts from the target before code: `requiresUserConfirmation` and generic stop conditions leave the Coordinator request, escalation conditions retain their own exact field, and `goal.step.reviewPolicyOverride` leaves the App API because post-step review is immutable approved Task state. V1 Plan review policy permits only the existing human reviewer; `required=true` creates the existing Goal Review and `required=false` skips it. Task Mode keeps one explicit fixed default request in S12 and gains no Task record or parallel payload owner.
- The smallest accepted storage correction is one immutable insert/read `goal_plan_records` owner required by S13 plus additional immutable columns on the existing `goal_tasks` owner. The Plan record has no status, update path, settlement, or workflow. Approval must load and verify that record, validate the graph before mutation, create the complete ordered Task set and transition the Goal in one existing Workspace transaction, and delete the caller Plan payload, false all-ready projection, and mutable Task order update. Portability carries the same records and recomputes only identity-dependent Plan digests after import reminting.
- Planned proof stays inside existing schema, store, route, Core Client, MCP, Web, export/import, and Goal worker-request suites. It must reject non-human reviewers and removal-only request fields, prove caller Plan mutation cannot affect approval, detect digest and graph corruption before effects, persist every Task fact exactly once, prove post-delivery replay reads the existing Turn-owned bytes, require `recovery_required` for pre-delivery replay while the S39 Worker Context Package trace is absent, and fail malformed Task facts before checkpoint, Turn, Item, or scheduler mutation. No resolver, payload extension on Plan Items, command snapshot, compatibility path, second request owner, state machine, runner, harness, L6 platform, external fork, or patch is authorized.
- A final grouped cross-reference sweep found that the Human Attention current projection and one completed repair record still treated caller-selected Review skipping as current authority. Corrected both before downstream implementation: immutable `GoalTask.reviewPolicy` alone decides whether a Goal Review exists, while the still-running override path is recorded only as a defect to remove.
- The storage and portability sweep closed one remaining implementation-choice gap before persistence work: export reads Goal, historical and active Plan, and Task rows from one SQLite snapshot; import validates source digests before reminting, includes Plan-only Task ids in the canonical map, rejects duplicate or Plan/Task-incoherent rows, and recomputes each digest only after the exact reminted payload is complete. This uses the existing export/import transaction and id-map owners rather than adding a migration workflow or repair layer.
- Accepted the smallest cross-store creation compromise instead of adding compensation state: the visible plan Item completes first, then one Workspace transaction inserts the immutable Plan and Goal pointer. A failed Workspace write leaves the Goal in planning and the orphan Item permanently non-authoritative; approval and recovery read only the Plan record and active pointer.

### 2026-07-17: G01 Immutable Goal Plan, Goal Task, And Worker Request Group

- Audited and updated `docs/specs/20260704-goal_mode_coordination.md`, `docs/specs/20260704-workflow_coordinator_internal_agent.md`, `docs/specs/20260531-human_attention_intervention_model.md`, `docs/app-api.md`, `apps/nanocore/README.md`, and this Change Plan as one G01 document group. These documents jointly own the contract and its projection; none is independent completion evidence or a separate audit stream.
- Current truth is one complete Coordinator Plan decision, one immutable Workspace-scoped Goal Plan authority plus lightweight lineage Item, one complete immutable Goal Task authority per approved task, and one exact Coordinator worker request derived losslessly from that Task. Approval accepts only `requestId` and the active `planItemId`, validates digest, Goal and Plan Item lineage, and graph before its Workspace transaction, maps non-active authority to `stale`, missing or contradictory authority to `recovery_required`, and graph invalidity to `goal_plan_invalid`, and never trusts or reconstructs caller Plan content. Export and import preserve Goal, Plan, Task, dependency, Item, Artifact, and digest coherence through the existing snapshot, canonical remint, and transaction owners.
- The bounded approval compromise persists the existing app-local command receipt immediately after the Workspace approval transaction. Exact replay and changed-input conflict use that receipt; a crash that leaves a complete approved Goal and Task tuple without it returns `recovery_required` instead of inference, rollback, receipt synthesis, or a settlement workflow. At this checkpoint, plan creation and revision required request ids but still lacked the complete command-ledger replay and crash-half-state contract; the later Goal Plan revision checkpoint below supersedes only that revision current-state note. Goal start, pause, resume, launch fencing, Context Package delivery proof, and stop-decision commit remained open G01 work. No status machine, second payload owner, compatibility path, runner, harness, dependency, external fork, or patch was added.
- Group verification passed all 62 App API schema tests, all 23 Core Client tests, 80 focused Web tests plus Web typecheck and build, all 20 existing Story runner tests, NanoCore OpenAPI validation with 9 tests, the focused NanoCore L3 Goal-planning e2e with 1 pass, and the complete NanoCore suite with 1,964 passes and 7 skips. The L3 run also corrected one stale test-only `nextTaskId` field to the canonical `nextReadyTaskId`; the production projection was already correct. The complete sandboxed run's only failure was the existing localhost disconnect test being denied a loopback bind with `EPERM`; that exact test and the L3 test passed when rerun with loopback permission.

### 2026-07-17: G01 Stored Worker Checkpoint Terminality Correction

- Audited Core Work Model, Agent Workflow, Worker Turn Reliability, Human Attention, Chat, Task, Goal, Quick Chat, Workflow Coordinator, Work and Resource Interaction, and the existing recovery implementation as one G01 group. The accepted documents already make the stored Worker Checkpoint and its terminal owner tuple authoritative, so no Core or specification rewrite was needed.
- The audit found that `clearWorkerCheckpointAfterTerminalState` trusted a caller-supplied `terminalStage`. A checkpoint durably stored as `running_worker` could therefore be presented as `completed` and deleted before terminal closeout. Test-first commit `274f896` reverses that invalid assumption; fix `f49f486` deletes the parameter from the cleanup contract and all callers and gates deletion on the checkpoint's stored stage. The implementation commit is net minus eleven lines and adds no record, state, module, fallback path, workflow, runner, harness, dependency, fork, or patch.
- The five focused worker-recovery tests, the existing interrupted-Goal retry route regression, NanoCore typecheck, and `git diff --check` pass. G01 remains active: reconnect-fenced request-bound retry, checkpoint request lineage and terminal discovery, Task and Goal launch fencing, Goal command snapshot cleanup and remaining lifecycle replay, Goal stop authority, gate closeout, steering, Artifact and Material commands, and the other recorded grouped defects remain open.

### 2026-07-17: G01 Goal Plan Approval Receipt Authority Correction

- Audited Core Protocol, Work Model, Agent Workflow, Goal Mode, immutable Goal Plan and Goal Task storage, command receipts, route projection, and existing Goal tests as one G01 owner group. The accepted documents already require `goal.plan.approve` replay from the original Goal, immutable Plan, Plan Item, and exact Task set and do not authorize a response snapshot, so no new design document or specification expansion was needed.
- Test-first commit `3983060` proves the completed receipt has no snapshot while exact replay still returns the approved result. Fix `b952b7d` deletes the full public-response snapshot and parser, retains only the existing non-secret `goal_plan` response id, verifies the original Plan digest, Plan Item, Goal lineage and lifecycle, and complete Task id, order, and Plan lineage, then projects the response from those owners. Missing, legacy-snapshot, or contradictory owner tuples fail closed as `recovery_required`; replay does not resolve the latest Goal or mutate state.
- The production diff is 71 additions and 9 deletions because owner validation and original-Goal projection replace one trusted copied response; it adds no table, record, status, workflow, settlement, compatibility path, module, dependency, runner, harness, fork, or patch. All 24 focused Goal Plan, approval, Task persistence, and route tests, NanoCore lint and typecheck, the complete NanoCore suite, and `git diff --check` pass. At this checkpoint G01 remained active for `goal.start`, plan, revise, pause, and resume identity and replay; the later revision checkpoint below supersedes only the `revise` item. Request-bound launch fencing, Goal stop authority, terminal closeout, reconnect-fenced retry, gates, steering, Artifact and Material commands, and the other recorded grouped defects remained open.

### 2026-07-17: G01 Goal Stop Decision And Owner Commit Correction

- Audited Work Model, Agent Workflow, Worker Turn Reliability, Human Attention, Goal Mode, Workflow Coordinator, checkpoint cleanup, Goal and Review storage, scheduler failure behavior, and existing L1-L3 Goal tests as one G01 stop boundary. The accepted S13 and S15 rules were already exact, so the specification edits update only current implementation truth.
- Test-first commit `2ace8c4` covers Coordinator completion versus continuation, invalid lower-level continuation, post-worker cleanup failure, Review transaction rollback, non-terminal `ask_user`, and startup or deferred-admission failure without inferred Goal terminal state. Fix `c31fe8a` fixes lower-level iterations at zero, consumes the pre-mutation Coordinator decision, commits the Goal, Task, Review, current-task, terminal-reason, checkpoint-evidence, and dependency-unlock tuple in the existing Workspace transaction, and deletes the route-wide failed/error rewrite. Post-worker owner, projection, response-shape, or cleanup failure now returns `recovery_required`; non-terminal worker gates retain their checkpoint.
- The production diff is 146 additions and 161 deletions, net minus 15 lines. It adds no record, state, error type, workflow, settlement, compatibility path, helper module, dependency, runner, harness, fork, or patch. All 40 focused Coordinator and Goal route tests, the complete NanoCore suite, NanoCore lint and typecheck, repository validation, and `git diff --check` pass. G01 remains active for request-bound launch fencing, Goal step replay, lifecycle command identity, Context Package delivery proof, terminal-checkpoint discovery, reconnect-fenced retry, gates, steering, Artifact and Material commands, and the other recorded grouped defects.

### 2026-07-17: G01 Generic Pending-Input Mutation Removal

- Audited Human Attention, Work and Resource Interaction, App API schemas, NanoCore recovery storage and routes, Action Center, generated OpenAPI, Core Client, Web fixtures, the removal-only MCP facade, and the deterministic recovery story as one G01 contract boundary. S06 and S16 already forbid generic pending-input editing and interrupt promotion, so this slice changes current implementation projections without inventing another design.
- Test-first commit `43d5cb6` requires both old App API routes to return `404` without changing the Item, pending row, active Turn, or audit records; rejects both Action Center action kinds and all three public schemas; removes both actions from the Action Center and both operations from OpenAPI; and requires Core Client and MCP to expose neither capability. It also removes the edit step from the existing recovery story instead of adding a new runner.
- Fix `974ed5a` deletes both route implementations, runtime mutations and audit writers, App API schemas, Action Center actions, Core Client methods, OpenAPI paths and components, legacy MCP tools and facade methods, and stale Web fixtures. It does not add a tombstone, `410` route, compatibility alias, replacement queue, new owner, state, receipt, settlement workflow, module, dependency, runner, harness, fork, or patch. The independent Turn interrupt command remains unchanged.
- Hand-written source and fixture code is two additions and 514 deletions, net minus 512 lines. Generated OpenAPI also removes the two paths and three schemas; most of its textual reduction comes from deleting the 13,015-line inline Turn schema previously duplicated under the promotion response. A semantic comparison found no other changed path or schema except the two removed Action Center enum values and the generated source digest.
- Verification passed all 63 App API schema tests, 23 Core Client tests, 136 MCP tests, 127 Web tests, 173 focused NanoCore recovery, Action Center, OpenAPI, and server tests, the three existing recovery-runner tests, all affected lint, typecheck, and build gates, and OpenAPI validation. The complete NanoCore suite passed outside the loopback-restricted sandbox with 1,966 passes and 7 skips.
- G01 remains active. At this intermediate checkpoint, the remaining generic follow-up and cancellation recovery projections still lacked S16's original-Goal lineage, terminal claim, command receipt, and deterministic downstream proof; the later full-authority deletion checkpoint below supersedes that current-state observation while preserving this historical decision record.

### 2026-07-17: G01 Goal Plan Revision Replay Correction

- Audited Work Model, Agent Workflow, Goal Mode, App API command identity, Goal and Plan authority, visible Turn and Item lineage, and the existing command ledger as one G01 boundary. The review found that deterministic revision owners alone cannot prove which concurrent request won the cross-store Goal transition, so S13 now records the bounded compromise instead of authorizing inferred receipt repair.
- Test-first commit `cb79bfd` extends the existing real route scenario rather than adding a fixture, runner, or harness. It proves normal exact replay without another Turn or Item, changed-input conflict, immutable replaced-Plan and request lineage, receipt publication only after a schema-valid Goal projection, a missing-receipt `recovery_required`, and a Turn-only half-state that never launches a replacement or gains a synthesized receipt. The obsolete helper test with a nonexistent Plan parent was deleted.
- Fix `4a6a3a2` reuses the existing command ledger and in-flight duplicate map, derives the revision Turn and Item from actor, Workspace, Thread, command, and request identity, records `parentItemId` and `causationId`, verifies the exact active immutable Plan before writes, and rechecks the same active Plan inside the existing Workspace transaction before returning the Goal to `planning`. Receipt replay validates the deterministic owners and original Goal id; receipt absence always fails closed once any request-owned effect exists.
- Refactor `5fc6ea1` deletes the duplicate owner lookup from the fresh-write helper. The final production delta is 341 additions and 54 deletions, net 287 lines, in three existing files; the test delta is net 152 lines in one expanded route scenario after deleting the redundant helper case. No schema migration, state, winning-revision field, settlement or recovery workflow, compatibility path, dependency, module, runner, harness, fork, or external patch was added.
- Follow-up test-first commits `cc09510` and `f82248c` make a handled receipt-publication failure or partial revision owner tuple return `recovery_required` immediately instead of first exposing a generic route error. Refactor `d1f193b` keeps the owner-reader input private rather than publishing another module contract, and deletion pass `1bc50cc` removes 29 redundant response-shape, intermediate-count, and duplicate receipt assertions without removing a replay, conflict, lineage, or fail-closed criterion.
- Verification passed 20 focused Goal Plan tests, NanoCore lint and typecheck, and the complete NanoCore suite with 1,965 passes and 7 skips outside the loopback-restricted sandbox. G01 remains active for `goal.start`, `goal.plan`, pause and resume identity and replay, request-bound launch fencing, Context Package delivery proof, terminal-checkpoint discovery, reconnect-fenced retry, gates, exact steering, Artifact and Material commands, and the other recorded grouped defects.

### 2026-07-17: G01 Goal Plan Creation Replay Correction

- Audited Core Work Model, Agent Workflow, Human Attention, Goal Mode, Workflow Coordinator, App API schemas and routes, immutable Goal and Plan storage, visible Turn and Item lineage, command receipts, and existing L1-L3 tests as one G01 contract group. Core already states the required stable invariants, while S13 needed one selective rehydration: the public command acknowledges only an approvable Plan, complete Plan authority can prove its winning request, and question, error, or partial tuples cannot.
- Design commit `4f7bb4d` records that boundary before implementation. Test-first commits `846d92d`, `f323a99`, and `9be1413` cover complete receipt-gap recovery, ordinary receipt replay, planning transition fencing, question and planner-error fail-closed behavior, and projection from the stored immutable Plan. Refactor `03473dc` deletes the duplicate partial-route scenario and redundant private-id and intermediate-count assertions while retaining every decision-grade criterion.
- Fix `2365dc7` reuses the existing app-local command ledger, derives the planning Turn and all outcome Item identities from actor, Workspace, Thread, command, and request identity, and rechecks the original `planning` Goal with no active Plan inside the existing Workspace transaction before inserting the immutable Plan and active pointer. The complete Plan record's `createdByRequestId` proves a receipt-gap winner, so retry rebuilds the schema-valid response and publishes the receipt without another Turn, Item, Plan, current-Goal lookup, or Plan generation. Planning-question, planner-error, and partial tuples return `recovery_required` and never create replacement owners.
- The production delta is 340 additions and 60 deletions, net 280 lines, across four existing files. The final test delta is 122 additions and 9 deletions, net 113 lines, across three existing test files after the deletion pass. The slice adds no schema, migration, record, state, workflow, settlement, compatibility path, module, dependency, runner, harness, fork, or external patch; the only new function projects an already-authoritative Plan through the existing Coordinator response owner.
- Verification passed 47 focused Coordinator and Goal Plan tests, NanoCore lint, typecheck, build, the complete NanoCore suite outside the loopback-restricted sandbox, spec lifecycle validation, repository validation, and `git diff --check`. G01 remains active for `goal.start`, pause and resume identity and replay, request-bound launch fencing, Context Package delivery proof, terminal-checkpoint discovery, reconnect-fenced retry, gates, exact steering, Artifact and Material commands, and the other grouped defects.

### 2026-07-17: G01 Goal Start Identity And Historical Projection Correction

- Audited Core Work Model, Agent Workflow, Human Attention, Goal Mode, Chat and Task handoff, App API schemas and generated projection, Core Client, NanoCore, Web, the removal-only MCP facade, and existing L1-L3 tests as one G01 contract group. Core retained its stable invariants; S13 received only the exact deterministic owner, transaction fence, receipt replay, missing-receipt failure, optional-title ambiguity, and outer-handoff criteria needed to prevent two conforming implementations from choosing incompatible authority.
- Design commit `4a619d9` records the bounded compromise before implementation: direct `goal.start` uses the existing command receipt for ordinary replay and conflict detection, while any deterministic owner without that receipt returns `recovery_required` because the stored Goal tuple cannot prove whether the caller supplied an optional title equal to the derived default. Chat and Task derive the same downstream tuple from their outer command identity but retain only `chat.start` or `task.start` receipts; no nested Goal receipt is authorized.
- Test-first commits `e38f2c1`, `2cbeda8`, `8cdc7e4`, `7606736`, and `0925e43` cover required raw App API identity, Core Client generation, exact replay and changed-input conflict, deterministic Goal, Turn, Item, actor and scope lineage, Item causation, receipt publication order, receipt-gap and Turn-only fail-closed behavior, the optional-title compromise, historical Goal isolation, outer-only handoff receipts, and the existing MCP request-id pass-through. Fixes `906ca1d`, `748d616`, `6be88d0`, and `c899e3c` implement those tests through the existing schema, client helper, command ledger, Goal, Turn, Item, Workspace transaction, and one-line removal-parity adapter path.
- The NanoCore production commit is 293 additions and 170 deletions, net 123 lines including the generated OpenAPI update; the historical-projection review deletes the thread-wide pending-attention fallback and its parameter chain because Goal status is the sole current attention authority. No schema migration, record, status, response snapshot, receipt synthesis, recovery workflow, settlement engine, compatibility path, module, dependency, runner, harness, fork, or external patch was added, and Web production remains unchanged because Core Client owns missing request-id generation.
- Verification passed 63 App API schema tests, 23 Core Client tests, 136 MCP tests, 127 Web tests, OpenAPI generation and validation, all affected lint, typecheck, and build gates, and the complete NanoCore suite outside the loopback-restricted sandbox with 1,967 passes and 7 skips. G01 remains active for pause and resume identity and replay, request-bound launch fencing, Context Package delivery proof, terminal-checkpoint discovery, reconnect-fenced retry, gates, exact steering, Artifact and Material commands, and the other grouped defects; no individual document is closed by this checkpoint.

### 2026-07-17: G01 Goal Pause And Resume Command Boundary

- Audited Core Protocol, Work Model, Agent Workflow, Human Attention, Goal Mode, the App API projection, Goal and Turn storage, Core Client, NanoCore routes, generated OpenAPI, first-party consumers, and existing tests as one G01 lifecycle group. Core already owns the single-resource replay rule; S13 and the App API projection needed only the exact response, transition fence, and bounded receipt-gap criteria that prevent incompatible implementations.
- Rejected a full Goal response snapshot because it would copy mutable Goal, Task, attention, and terminal projections into the command ledger and violate Core's bounded-snapshot rule. The accepted clean contract stores only the original `goalId` and returns a literal historical command outcome plus the current projection of that exact Goal. Replay never resolves the latest Goal, never repeats a status transition, and remains truthful after a later opposite transition.
- Storage verification found that Goal state and workspace-scoped command receipts already share `workspace.sqlite`, so the accepted design needs no receipt-gap fallback. Design commits `882b84a` and `2af9e64` record the exact metadata-only replay, transaction, safe-boundary, and explicit-resume scope before behavior changes. Test-first commits `a1b5e53`, `6b7ddf0`, `1436d22`, and `a8a8928` require raw request identity, strict bodies, generated client identity, transaction rollback, non-terminal Turn and contradictory-owner rejection, metadata-only receipts, historical outcomes with current exact-Goal projections, OpenAPI request bodies, paused-step rejection, and the narrow pause/step lifecycle mutex. Schema and client implementations `2fd9803` and `b5a4fa8` plus NanoCore fix `ec670b3` reuse the existing request-id helper, command ledger, Workspace database, Goal and Task records, route, and OpenAPI generator.
- The NanoCore slice co-transacts the two pre-existing Goal and Task reservation writes and rechecks `running` only to serialize step admission against an accepted pause. It does not claim S13's complete `goal.step` launch tuple: reserved Turn identity, preparing checkpoint, request id, canonical input hash, exact replay, and crash recovery remain open G01 work. Explicit `goal.resume` changes only Goal lifecycle state and does not resume a Turn, Session, sandbox, lease, worker, or backend session. No table, migration, transition-history row, request-lineage field, response payload snapshot, settlement or recovery workflow, compatibility path, MCP or Web operation, module, dependency, runner, harness, fork, or external patch was added.
- Verification passed all 63 App API schema tests, all 23 Core Client tests, 36 focused NanoCore tests, NanoCore OpenAPI generation and validation, lint, typecheck, build, and the complete NanoCore suite outside the loopback-restricted sandbox with 1,968 passes and 7 skips. Independent deletion-first review found no blocking pause/resume defect or unnecessary mechanism after the incomplete step launch fence remained explicit. G01 stays active for that complete launch fence, Context Package delivery proof, terminal-checkpoint discovery, reconnect-fenced retry, gates, exact steering, Artifact and Material commands, and the other grouped defects; no individual document closes independently.

### 2026-07-17: G01 Goal Step Launch Fence And Replay Boundary

- Audited Core Work Model, Agent Workflow, Worker Turn Reliability, Human Attention, Goal Mode, Workflow Coordinator, Work and Resource Interaction, the App API projection, Worker Checkpoint and Goal storage, scheduler admission, Core Client, Web, the removal-only MCP facade, real Goal acceptance, and existing L1-L3 tests as one G01 launch-and-replay group. The stable Core invariants remain sufficient; S05, S13, and the App API projection required one selective correction before implementation.
- The review found a design contradiction: S13 authorizes only the bounded `goalId`, Task, Turn, outcome, stop, evidence, and Review snapshot, while the current public response additionally requires Coordinator, Context Assembly, Session, checkpoint-stage, and duplicate pending-attention projections that cannot be reconstructed after terminal checkpoint cleanup. The accepted deletion-first response is `{ goal, result }`; current attention comes from the Goal and Action Center owners, and runtime diagnostics remain on their existing records. The receipt will not expand into a copy of the current HTTP response.
- The existing Worker Checkpoint remains the only launch fence. The exact reserved Turn derives from command identity, and one Workspace transaction must reselect the first ready Task, recheck the runnable Goal, change Goal and Task, and insert the request-bound `preparing` checkpoint. No reservation row, pending command, settlement, recovery workflow, replacement Turn, replacement admission, new module, dependency, runner, harness, fork, or external patch is authorized.
- The bounded fallback is fail-closed: until S16 supplies an immutable worker-request and Context Package trace, a failure or restart after the `preparing` fence but before the exact Turn-owned request is durable returns `recovery_required` and never reruns Coordinator or reselects context. Terminal owner tuples can still rebuild the small result snapshot, publish a missing receipt, and clear the checkpoint.
- Test-first commits `ca4f3e7`, `63f1f2a`, and `ad10d99` cover request identity, stable first-ready reservation, transaction rollback, exact replay, receipt-before-cleanup, post-fence failure, and competing-request rejection; `94a4b99` implements the fence by reusing the existing command ledger, PermissionDecision, Goal, Task, Turn, checkpoint, and terminal owners. Web commits `8e8bb50` and `dc624ff`, real-story commits `2b18a9b` and `8abb5c9`, and removal-only MCP smoke commit `1b31544` delete the obsolete private response projections and verify the durable Review owner instead. The production source increment is 369 lines with no new table, state, module, workflow, dependency, runner, harness, fork, patch, or compatibility path. Verification passed all 1,968 NanoCore tests with 7 existing skips, all 127 Web tests, all 136 MCP tests, all 20 real Goal runner tests, repository-wide lint and typecheck, OpenAPI projection, and focused import, migration, transaction, and replay checks. G01 remains active only for the grouped gates, S16 delivery and steering, Artifact and Material commands, reconnect-fenced retry, and broader recovery work; no individual document closes independently.

### 2026-07-17: G01 Generic Pending-Input Authority Deletion

- Audited Worker Turn Reliability, Human Attention, Goal Mode, Workflow Coordinator, Work and Resource Interaction, Scheduler, Audit/Usage/Evidence, App API, L6 acceptance, NanoCore storage and runtime, Action Center, workspace portability, generated OpenAPI, Core Client, Web fixtures, the removal-only MCP facade, and existing recovery tests as one G01 contract group. Core Work Model and Agent Workflow already own the stable invariants and required no expansion; this slice updates only the precise specification and implementation projections that could otherwise authorize incompatible behavior.
- The review proved that the generic `pending_user_turns` row was not S16 authority: it lacked `goalId`, `activeTurnId`, a terminal claim, and immutable Context Package delivery proof; its only production writer was a private deterministic recovery seed, and the worker loop selected rows by Thread without exact Goal lineage. The accepted correction deletes that owner completely. The reserved Goal steering route remains fail-closed with `503 goal_steering_delivery_unavailable` and zero Item, pending-row, command, Turn, scheduler, audit, or Action Center writes until the exact S16 owner exists.
- Lifecycle and recovery stay narrow. Interrupted-worker inspection, authoritative checkpoint retry, mode-owned terminal cleanup, scheduler restart/reconnect adoption, and the real restart/reconnect L3 remain intact. The generic table, module, queue drain, worker-context injection, recovery routes and actions, Action Center rows, import/export family, public deterministic seed, fixed-zero Goal steering summary, first-party clients, removal-only MCP tools, and dedicated recovery story and runner are deleted. The baseline schema is corrected directly with no drop migration or compatibility shim; old internal-development data roots must be rebuilt.
- Test-first commit `e2320fb` established cross-layer absence and fail-closed contracts. App API commits `081d558` and `e32c9fe`, Core Client commits `2d06ee4` and `ea630ca`, MCP and story deletion `060ea53`, Web fixture deletion `9e7622d`, and NanoCore deletion `e83e66d` implement the correction. Across 51 committed files the slice has 273 additions and 4,956 deletions, net minus 4,683 lines; it adds no table, state, status, module, workflow, settlement, dependency, runner, harness, fork, external patch, or compatibility path. OpenAPI semantic comparison adds no path or schema and changes only the expected reduced Goal, Action Center, and interrupted-worker projections.
- Verification passed all 63 App API schema tests, 23 Core Client tests, 130 MCP tests, 127 Web tests, 235 focused NanoCore tests, the complete NanoCore suite outside the loopback-restricted sandbox with 1,950 passes and 7 skips, all affected lint, typecheck, and build gates, OpenAPI generation and validation, production-remnant scans, `git diff --check`, and an independent deletion-first review. The observable predicates are: every deleted route returns `404`; no generic table, writer, import/export row, public method, MCP tool, Action Center source, Goal counter, or story remains; Goal steering returns the exact `503` with zero writes; interrupted-worker retry and restart/reconnect tests still pass. G01 remains active for the exact S16 owner and immutable delivery proof, gates, Artifact and Material commands, reconnect-fenced retry, and broader checkpoint recovery; no individual document closes independently.

### 2026-07-17: G01 Interrupted-Worker Retry Authority

- Audited Core Agent Workflow, Worker Turn Reliability, Human Attention, Goal Mode, Scheduler, Agent Session continuity, worker-control, restart/reconnect contraction, App API, Core Client, NanoCore checkpoints, Action Center, scheduler and backend records, and existing L1-L3 tests as one G01 recovery contract group. Core and the automatic exact-worker reconnect mechanism remain structurally unchanged; the missing contract was the single boundary between reconnect, cleanup ownership, anchored interruption projection, visible recovery, and a later user-authorized retry.
- The accepted predicate reuses only existing owners. Checkpoint presence or a timeout claim is insufficient: the original request-bound checkpoint must still be `preparing` or `running_worker` without a terminal StopReason, the exact Turn and recorded Session must already be interrupted, and the matching lease must be terminal `released` through `scheduler-restart-backend-cleanup` with no recovery state; that existing terminal transition proves backend cleanup and capacity release. Goal retry additionally requires the exact running Goal Task to be current under a running Goal. No eligibility row, recovery phase, settlement, timer, service, workflow, dependency, runner, harness, fork, patch, or compatibility path is authorized.
- `worker.recovery.retry` uses a strict `{ requestId }` request and stable `{ outcome: "released_for_retry", turnId }` result. One Workspace transaction terminalizes the checkpoint, releases the exact Goal Task when present, clears `currentTaskId`, and records the existing command receipt; existing terminal-checkpoint cleanup runs after the receipt and can be completed by exact replay. The command never rewrites the interrupted Turn, changes scheduler or backend owners, creates an admission, or launches a worker. Public interrupted-worker rows appear only after the base interruption predicate, and retry appears only after the stricter mode predicate.
- Design commit `5a8c39b` fixed the shared S03/S05/S06/S13 predicate before behavior changed. Test-first commits `664bd20`, `9ded6a7`, and `e79b82d` established strict App API identity, generated Core Client identity, exact terminal authority, pending-reconnect rejection, stable replay, unchanged Turn and lease owners, Goal release, Action Center gating, and anchored restart interruption. Implementations `ee76144`, `467aafb`, and `48727b4` reuse the existing schemas, request-id helper, scheduler lease, Turn, Session, checkpoint, Goal, Task, Workspace transaction, command receipt, and terminal cleanup owners.
- The NanoCore implementation is 499 additions and 13,265 deletions, net minus 12,766 lines; most deletion removes the old mutable Turn response recursively expanded into generated OpenAPI. The complete NanoCore suite passes with 1,949 tests and 7 skips, alongside focused App API and Core Client suites, OpenAPI generation and validation, NanoCore lint, typecheck and build, `git diff --check`, and an independent deletion-first review. No table, migration, state, retry phase, workflow, settlement, service, timer, module, dependency, runner, harness, fork, external patch, compatibility path, or remote-Gateway behavior was added. This recovery slice is implemented inside G01; the group remains active for its other listed contracts, and no individual document closes independently.

### 2026-07-17: G01 Remaining Authority Group Audit

- Re-audited the complete G01 document set, App API projection, Worker Control and Context Package interfaces, Workspace Sync boundary, NanoCore gates, Action Center, terminal checkpoint recovery, Artifact and Review paths, Material target contract, and existing tests as one group. Core Work Model and Agent Workflow remain short and sufficient; the selective rehydration belongs only in the implementation-facing contracts that could otherwise permit incompatible authority, lifecycle, retry, or recovery choices.
- The accepted Gate contract has two strict request shapes, one exact non-empty answer per question, Core-owned Goal/Task/checkpoint closure, winner-only replay, and Action Center projection only from a consistent Gate tuple. A single nullable Goal Task pointer retains the latest worker-Gate response or decision, and the next Goal step must include its unique request/response Item pair in the Coordinator request and S39 trace; this bounded sticky compromise replaces a new continuation record or workflow. Unsupported Approval statuses fail closed, and runtime callbacks cannot own business state. The accepted terminal recovery contract keeps no recovery state: scheduler fencing runs first, then the same Task or Goal owner classifies every uncleared checkpoint and either preserves live or reconnectable work, resumes generic runtime closeout, replays the original mode transaction when zero mode writes exist, clears an already complete tuple, or leaves a contradictory tuple discoverable as `recovery_required`. Only direct Task or Goal commands may publish a deterministic missing receipt; an outer Chat handoff without its sole receipt fails closed rather than gaining a nested receipt.
- Corrected the S16 current-state and scope contract. Exact Artifact origin, digest, request proof, per-Turn communication, workspace-only import, and introduction remain unimplemented; `artifact.register` is unauthorized because its proposed sources own no unique canonical bytes; strict JSON UTF-8 content and digest rules replace an ambiguous byte transport; a complete Artifact authority tuple without its cross-store receipt fails closed instead of synthesizing a receipt or settlement; and the generic Artifact metadata `PATCH`, unversioned review route, identifier-prefix inference, and review translation are deletion work. Artifact Review is version-owned, while the exact `artifactId` relation to a durable Workspace Sync Review excludes its presentation Artifact from generic review; neither owner can translate or fall back to the other.
- Material implementation is limited to identity, immutable inline-content revision, singular Thread binding, queue, closed public reads, typed expected-base rejection, restart, and portable row export. The three Material record families and their command receipt share one `workspace.sqlite` transaction, so no Material file authority, cross-store half-state, durable conflict record, Action Center conflict row, or settlement owner exists. Portable import carries the owner rows without source receipts and rewrites request proof to reserved non-command lineage, deliberately giving up pre-export command replay rather than rebuilding idempotency authority. `lastWorkerSeenRevisionId` remains null until the G01-owned S39 trace proves the accepted Turn, exact selected revision and digest, and ready materialization; restricted Material is excluded rather than overridden. The App API now fixes one target method, path, operation id, closed read shape, and stable response identity per authorized Artifact and Material operation, while implementation remains pending.
- Material proposal and writeback remain blocked until G05/C09 and S16 select one existing or narrowed owner. The conflict-safe criteria remain documented because they constrain that future choice, but they authorize no proposal route, state, workflow, UI acceptance, or recovery path now.
- Active Goal steering is serialized to at most one pending row per Thread, with the original Goal retained as immutable lineage rather than a uniqueness partition. That row stores the exact message-or-Material input lineage; the product Item remains visible content; the S39 trace preserves the exact Item and Material tuple for application; and the existing follow-up and cancellation command records preserve them for those terminal outcomes until bounded cleanup. Follow-up is a directly completed Core-local history Turn with no mode, Agent, provider, scheduler, worker, or S39 effect; later execution requires an explicit command. A second send returns conflict rather than creating queue ordering, coalescing, prioritization, or another delivery engine. The singular Material `activeDelivery` projects only a matching residual Material row and becomes null after cleanup instead of selecting historical proofs. Active pending steering blocks portable export instead of inventing cross-deployment replay.
- The ordered group review list above supersedes older per-finding order notes without erasing their evidence. Current G01 execution truth is maintained in the ledger row; historical checkpoints remain evidence and are not extended per document or commit. G01 remains active.

### 2026-07-17: Program Redesign For Convergence

- Diagnosed three structural defects from five days of execution evidence — scope absorption, uniformly applied per-command exactness, and per-slice protocol overhead — and recorded them in the Convergence Redesign section. Historical checkpoints remain unchanged evidence; only forward-looking plan sections were rewritten.
- Rebounded this record to the frozen G01 closeout list (Class A finish, Class B downgraded to the central idempotency default, Class C evicted), the review-only time boxes for G00 residual, G04, G05, G08, and G09, and program bookkeeping. Dissolved G02, G03, G06, and G07 into their owning change plans as audit preambles.
- Established the [OpenKit Execution Program](./202607172152230001-openkit_execution_program.md) as the single ordered work-package queue and the owner of the binding convergence rules; the central idempotency default is promoted into C07 Protocol and S62 during WP-0 (Class A item 5).
- Replaced the ten-step contract-group protocol with the bounded audit pass protocol, affected-document re-reading, and checkpoints of at most 10 lines.

### 2026-07-17: Proportionality Doctrine And Idempotency Promotion

- Scope: risk-proportional reliability and testing doctrine (AGENTS.md, change tracking, deployment, C00, C02, C03, C05, C07) and small-deployment contractions (S05, S07, S08, S24, S29, S30, S37, S62); frozen-list alignment only.
- Class A item 5 done: C07 owns receipt-and-current-owner replay, `idempotency_key_conflict`, missing-receipt `recovery_required`, and the named `chat.start` closed-outcome exception; S62 owns the scoped precision bar and adds the idempotency default to the shared-write Durable family.
- S08 contracted from 655 to about 230 lines, deleting the generic executor, evidence platform, and future runner designs; explicit `skipped` semantics restored — a skip is never acceptance evidence, and a release gate requires its applicable real-path stories to have executed.
- change-tracking.md scoped the two-independent-implementers bar to Durable families, matching AGENTS.md and S62. Findings: none outside the frozen list; Backlog unchanged.
- Verification: spec lifecycle validation passed; package and repository gates run with the owning slice commits.

### 2026-07-18: WP-0 / G01 Closeout

- Scope: the frozen G01 Class A list only; Class B posture and Class C dispatch were rechecked without absorbing their implementation.
- Class A: exact Gate families, boot checkpoint classification, retry-predicate audit, deletion-only Artifact authority, and the central idempotency storage boundary are complete; the unavailable named-Gate case uses the documented bounded interruption fallback.
- Commits: Artifact deletion `5ce60c8` through `898a7e6`; structured Gates and boot `537b4c7` through `7819bca`; exact and policy/AEP closeout `8e350f1`, `1e58ff4`, `b41866e`, and `a692433`; receipt boundary `7f6123b`, `335d224`, `c95dba7`, and `2c43387`; contract alignment `27961ef`.
- Verification: NanoCore 200 test files passed and 1 skipped, with 1,973 tests passed and 7 skipped; lint, typecheck, build, OpenAPI generation and validation, spec lifecycle validation, repository checks, and `git diff --check` passed.
- Class B: current-owner replay, changed-input conflict, and fail-closed `recovery_required` remain the accepted V1 default; only already-landed named missing-receipt exceptions remain.
- Class C: Artifact and Material commands, S39 delivery trace and steering, writeback, and their final projections remain dispatched to WP-3/WP-4 exactly as recorded.
- Remaining: none in G01. WP-1 begins only after its bounded G02 audit preamble is recorded.

### 2026-07-18: WP-1 / G02 Audit Preamble

- Scope: C07, C08, S01-S04, App API/OpenAPI, Core Client, removal-only MCP and Skills, credentials, release wiring, and interface stories; review-only and no implementation.
- Authority: Core and App operations retain separate existing contract sources; S04 owns only the agent-facing catalog, Skill, CLI, local credential resolution, redaction, and envelopes.
- Findings: four design defects, one ownership-conflict family, one implementation defect, current documentation drift, and the planned test/real-use gap; no `SECURITY-GAP`.
- Disposition: all in-scope findings are frozen in the Agent Skill Interface plan; S16 Artifact wording and A1 story validation are ticketed to the Execution Program Backlog.
- Exit criteria: unchanged G02 authority, auth, idempotency, error, replay, cancellation, generated-contract, and clean-removal requirements now belong entirely to WP-1.
- Remaining: owning-document corrections are complete and S01 is superseded into named current owners; begin test-first WP-1 implementation without a second route catalog, SDK, workflow engine, or runner platform.

### 2026-07-18: WP-1 / G02 Replacement Acceptance

- Scope: Stages 1-4 only; no MCP or legacy-Skill deletion occurred before replacement evidence passed.
- Implementation: commits through `d41b1dd` added the checked transport-neutral catalog, standalone CLI, unified Skill, credential mediation, request/error contract, and proportional L1-L5 coverage without a second route catalog, SDK, workflow engine, or runner.
- Deterministic evidence: catalog and handler coverage, process envelopes, redaction, clean-copy L5, and the single local NanoCore `doctor -> workspace.create -> workspace.list` L3 passed.
- Real-use evidence: official Codex 0.144.5 on A1 started from Skill metadata, loaded only `SKILL.md` and `loop.md`, discovered and described `workspace.create`, failed closed on one empty-stdin call, executed exactly one successful mutation, and confirmed the same record through `workspace.list`.
- Security evidence: the A1 secret-marker scan and independent public readback passed; the temporary NanoCore, data root, Skill copy, auth copy, and raw event files were deleted after the verdict was recorded.
- Remaining: Stage 5 deletes the complete reachable user-facing MCP and four legacy Skills; Stage 6 aligns current documentation and release wiring before the WP-1/G02 exit gates.

### 2026-07-18: WP-1 / G02 Closeout

- Scope: Stages 5-6 only, followed by the owning package exit gate; unrelated G01 evictions, worker adapters, S39 delivery, Material, and steering remained with their scheduled owners.
- Implementation: deletion tests `42da171`, `8e7e9ba`, and `7c0cc67`; clean removal `7e12e74`; bounded test and projection cleanup `93f4363` through `d50451e`; final documentation is recorded in the owning Agent Skill Interface plan.
- Result: one checked transport-neutral catalog, bundled CLI, and unified Skill remain; the user-facing MCP package and four legacy Skills have no tracked reachable surface or compatibility path; worker-side MCP remains separate.
- Verification: release and full gates passed, including lifecycle and reachability checks, lint, typecheck, unit and coverage suites, builds, NanoCore L3, Web L4, smoke, deterministic stories, the stock Skill validator, and the earlier real A1 progressive-discovery L6.
- Remaining: none in G02. WP-2/G03 is next after its bounded audit preamble.

### 2026-07-18: WP-2 / G03 Audit Preamble

- Scope: C04, C05, C13, C16, S20-S38, S64-S66, and their current runtime, schema, shim, OpenShell, image and test projections; review-only and bounded to the adapter/AEP/image package.
- Authority: one authored AgentManifest resolves through `ResolvedAgentSetup` into one AEP; S25 owns the shared shim and static registry, S64-S66 own native translation, S37 owns control, stock OpenShell owns no product truth, and NanoCore alone commits candidate worker records.
- Findings: two `SECURITY-GAP`s cover hidden backend network allowlists and a public arbitrary terminal-command issuer; the remaining in-scope ownership, design, implementation, documentation, test and real-use findings are frozen in the Adapter Boundary plan.
- Consolidation: S21 is superseded into C13/S20/S22/S23/S25; S25 and S29 remain active; S30 is not modified by WP-2.
- Dispatch: session snapshot/reuse drift, later S29/S30 consolidation, and the bespoke GitHub credential helper moved to the Execution Program Backlog; deletion of the complete arbitrary command/result surface extinguished the terminal-result replay finding.
- Exit criteria: unchanged declared-to-materialized execution, one control plane, exact existing lease/recovery behavior, stock OpenShell containment, adapter isolation, disabled capabilities, three adapters and the corrected fourth-runtime criterion.

### 2026-07-18: WP-2 / G03 Security Blocker Closeout

- Scope and commits: the two frozen pre-adapter `SECURITY-GAP`s only, `5e3e234` through `bc678d1`.
- Result: OpenShell base egress is exactly AEP-derived, non-transient provider effects fail closed before effects, and the complete arbitrary command/result surface is deleted with `interrupt` as the sole command.
- Verification: focused schema, Core Client, Skill, shim, NanoCore, OpenAPI, rebuild, network, provider, typecheck, lint, build, and remnant checks passed; the final provider slice passed 131 focused tests.
- Remaining: manifest/AEP ownership, generic shim, three adapters, fourth-runtime proof, three images, stock OpenShell, and A1 evidence remain with WP-2.

### 2026-07-18: WP-2 / G03 Adapter Contract Precision

- Boundary: current adapters return argv, safe environment, capture requirements, and normalized collection only; they return no generated files, and the shared harness gains no config-artifact envelope.
- Routing: each current launch consumes exactly one already resolved LLM route; route selection remains with NanoCore, while zero or multiple routes fail before the child and no adapter fallback exists.
- Pi terminal proof: the pinned JSON stream must reach its final `agent_settled`, and the last successful assistant message must correlate across `message_end`, `turn_end`, and a non-retrying `agent_end` before it is accepted.
- Remaining: begin the smallest test-first manifest/AEP and generic-shim implementation without a runtime selector, plugin framework, or new harness.

### 2026-07-18: WP-2 / G03 Provider Route And Topology Precision

- Routing: native route projection is adapter-specific; Codex and OpenCode support the trusted relay at their current pins through non-secret adapter-local projection, while Pi trusted relay is unsupported until an owning design accepts a native config artifact or a future pin supplies a safe custom-base binding.
- Authority: trusted-relay and direct-provider credentials and egress are mutually exclusive; unsupported runtime-route pairs fail before spawn without provider, model, protocol, or route fallback.
- Topology: AgentManifest owns runtime supply but no `mode`, `deployment`, or `transport`; scheduler and backend records continue to own local or remote placement, including the stock remote OpenShell Gateway and fixed disposable-Cell lifecycle.
- Remaining: update the already-authored failing tests to this boundary, then implement the smallest manifest/AEP and adapter changes without a route framework or generated-file envelope.

### 2026-07-18: WP-2 / G03 Adapter And A1 Evidence

- Result: strict AgentManifest input now resolves through `ResolvedAgentSetup` into one immutable AEP; closed runtime compatibility, inferred selectors, and duplicate catalog launch branches are deleted.
- Worker and image boundary: one generic shim uses the static Codex, OpenCode, and Pi adapters; focused contract and fourth-runtime tests pass, and all three pinned non-root arm64 images built and passed their image smokes on A1.
- Stock OpenShell: unmodified `0.0.80` created a disposable sandbox, uploaded the package, ran the generic shim dry-run, and removed the sandbox with `--no-keep` for each image.
- Evidence boundary: A1 proves image contents, adapter preparation, containment, upload, and cleanup only; worker-control readiness, heartbeat, interrupt, reconnect, recovery, and real-provider behavior are not claimed by that check.
- Remaining: the final affected-package and repository exit gates must confirm the existing control, reconnect, recovery, recycle, workspace-capture, and provenance suites remain green; G04/WP-3 owns real-provider evidence.

### 2026-07-18: WP-2 / G03 Closeout

- Verification: the full NanoCore suite passed outside the restricted sandbox with 200 files passed and 1 skipped and 1,973 tests passed and 3 skipped; NanoCore typecheck/build, worker-shim 159, config-schema 117, App API schemas 62, Docker contracts 13, `check:repo`, and `git diff --check` also passed.
- Result: existing control, reconnect, recovery, recycle, workspace-capture, and provenance coverage remained green; every frozen WP-2/G03 exit criterion and S64-S66 acceptance boundary is satisfied.
- Evidence boundary: A1 remains limited to image, adapter preparation, stock OpenShell containment, upload, and cleanup; it does not establish control lifecycle, recovery, or real-provider acceptance.
- Remaining: none in WP-2/G03. Residual consolidation and deletion reviews stay in WP-7, and G04/WP-3 owns real-provider evidence.

### 2026-07-18: WP-3 / G04 And G05 Closeout

- Scope: one bounded provider review and one bounded storage review; every non-security finding is scheduled in the Execution Program Backlog rather than absorbed.
- Owner decision: Material writeback uses the exact turn-output Artifact version, version-keyed Artifact Review, existing Material owners, and one Workspace SQLite transaction; Workspace Sync is not an alias and no proposal, settlement, or recovery workflow is added.
- Security result: provider/model authorization and fixed public errors, provider secret aliases and OAuth diagnostics, worker credential scope and staged Git blobs, authoritative SQLite corruption, and imported effect authority now fail closed at their existing owners.
- Verification: NanoCore passed 200 files with 1 skipped and 1,996 tests with 3 skipped; Web passed 12 files and 125 tests; focused package suites, typechecks, `check:repo`, and `git diff --check` passed.
- Remaining: none in G04/G05. Their scheduled Backlog findings retain their named later owners; WP-4 begins with documentation authority only.

### 2026-07-18: WP-4 Documentation Authority Exit

- Scope: grouped reconciliation of S16, S39, S23, S46, C07, App API, and the Work Resource owning plan before any new production behavior.
- Result: exact Material, Artifact Review, steering, Context Package, generated `context` slot, WIS/WMR, queue proof, failure, restart, and read-projection contracts are deterministic under the two-independent-implementers bar.
- Minimality: two independent contract reviews and one minimality review returned clean; the target reuses existing owners and adds no lifecycle, recovery workflow, runner, harness, slot, source adapter, or duplicate authority.
- Current truth: S23 is Partial because its planner still routes the dedicated generated Context Package input to `turn-inputs`; WP-4 Stage 3 owns only the accepted exact input-to-`context` mapping. The next checkpoint records the completed Stage 1 schema slice.

### 2026-07-18: WP-4 Shared Contract Exit

- Scope: one runtime-neutral App API schema module for the closed Material, Revision, Thread Binding, Thread material projection, and version-owned Artifact Review target contracts.
- Result: all fifteen bounded success responses and nine mutation bodies are strict; owner-only fields, reserved lineage tokens, invalid digests, invalid JSON Artifact content, incoherent Review tuples, and caller path identifiers are rejected without promoting these app-local shapes into stable Core protocol.
- Verification: the package passed 68 tests, typecheck, lint, and build; `check:repo` and `git diff --check` passed; two independent contract reviews and one deletion-first review returned clean.
- Sequencing: Stage 2 adds checked contracts and runtime owners for Artifact import, Artifact introduction, and the eleven Material operations. The two version-owned Artifact Review operations stay at Stage 4 with their valid worker-output producer and single SQLite owner; the current unversioned file Review is not reused, migrated, or dual-written. S51 portability also waits for that stage so Material, Review, and Context Package references enter one complete graph rewrite rather than a temporary partial transfer contract.

### 2026-07-18: WP-4 Material Authority Exit

- Scope: checked Artifact import and introduction contracts, exactly three Material owner families, eleven Material operations, Core Client projection, and generated OpenAPI parity.
- Result: Artifact authority now carries exact digest, request, origin, and deterministic reference proof; request-owned authority without a receipt fails closed as `recovery_required`. Material mutations and receipts share one Workspace SQLite transaction, while the read projection does not infer worker-seen, current-Turn, or active-delivery state.
- Verification: NanoCore passed 203 files and 2,005 tests with one file and three tests skipped; protocol passed 152 tests, App API schemas passed 68 tests, Core Client passed 26 tests, and OpenAPI passed 13 tests, together with the applicable typecheck, lint, build, repository, and whitespace gates.
- Remaining: S39 delivery and steering remain Stage 3; version-owned Artifact Review, proposal apply, and portability remain Stage 4; Web and acceptance remain later stages.

### 2026-07-18: WP-4 Worker Context And Steering Exit

- Scope: the accepted S39 Context Package trace, stock OpenShell materialization, exact worker-seen projections, and bounded Goal steering over existing owners only.
- Result: every accepted Task and Goal backend launch now has one verified immutable trace; Material selection, exclusion, queue mutation, steering application, backend session, WIS, WMR, and generated `context` handoff share that exact lineage. Goal steering reports its durable queued, applied, follow-up, cancelled, conflict, stale, or recovery result without mutating a live worker filesystem or creating another workflow owner.
- Minimality: the slice adds no scheduler state, recovery workflow, settlement engine, runner, harness, OpenShell fork or patch, or compatibility path. Delivery-capability and Material-budget failures remain truthful bounded fallbacks over the existing pending owner.
- Verification: NanoCore passed 207 files and 2,047 tests with one file and three tests skipped; protocol passed 152 tests, config schema passed 129 tests, App API schemas passed 70 tests, Core Client passed 27 tests, the Skill interface passed 10 tests, and OpenAPI passed 14 tests, together with the applicable typecheck, lint, build, repository, lifecycle, and whitespace gates.
- Remaining: none in WP-4 Stage 3. Version-owned Artifact Review, conflict-safe Material apply, and the single complete portability graph update remain Stage 4; Web and final acceptance remain later stages.

### 2026-07-18: WP-4 Worker Proposal Contract Entry

- Scope: implementation-entry reconciliation for the existing transcript declaration and retained-session Artifact data plane, exact injected-value guard, version-owned Review producer, imported Review history, and fresh target-Agent requirement.
- Result: stock OpenShell collection is hard-bounded to 16 MiB plus one sentinel payload byte through the existing command and download operations; exact credential matching is contiguous UTF-8 byte-substring containment over the deduplicated complete injected values; restored sessions with Artifact declarations clean up and return `recovery_required` without Vault re-resolution; and imported refinement or redo requires the exact currently enabled target Agent or returns `stale` with zero writes and no substitution.
- Review: an independent contract review returned clean after the three implementation-changing ambiguities were resolved. No new protocol phase, transfer service, durable state, recovery workflow, runner, harness, fork, or patch is authorized.
- Current position: Stage 4 minimal failing tests are active over only the existing Artifact, Item, Workspace SQLite, Material, Context Package, command ledger, Turn, scheduler, and S51 transfer owners.

### 2026-07-18: WP-4 Worker Proposal And Portability Exit

- Scope: the Stage 4 worker-output Artifact producer, version-keyed Artifact Review and conflict-safe Material apply, exact refinement or redo launch lineage, App API and Action Center projections, and one complete S51 Material, Review, and Context Package graph update.
- Result: stock OpenShell retained-session collection creates the exact Artifact, reference Item, and Review only after bounded bytes and exact-secret checks; Review and Material effects reuse one Workspace SQLite transaction or the documented fail-closed cross-store compromise; S51 validates source authority, remints every affected owner and nested reference, revalidates target authority, and keeps imported history non-authorizing.
- Minimality: the obsolete Artifact-id file Review is deleted, the existing command ledger and S39 executor path are reused, and independent deletion-first review found no deletable over-engineered mechanism; receipt replay is not expanded into a second S39 verifier.
- Verification: final review found and closed one swapped trace-path export defect through the existing portable-file validator and a 73-test targeted slice. NanoCore had already passed 208 files with one skipped and 2,107 tests with three skipped; App API schemas passed 71 tests, Core Client 28, and Worker Shim 161, together with affected typecheck, lint, build, generated OpenAPI, lifecycle, repository, and whitespace gates.
- Remaining: none in the WP-4 kernel package. The accepted Web Material projection and Web-dependent Phase 1 acceptance move together into post-program S10 after G09; two unrelated import/runtime findings remain dispatched in the Execution Program Backlog.

### 2026-07-19: WP-4 Kernel Package Exit

- Scope: resolve the Stage 5 rebuilt-stack sequencing conflict without temporary implementation or program-scope absorption.
- Result: WP-4 closes at its verified Stage 4 kernel boundary; S16 Stage 5 and Web-dependent Stage 6 remain accepted and move together into post-program S10 after G09.
- Verification: no production behavior changed; the current Solid baseline remains only aligned with Stage 4 contracts and no duplicate runner or acceptance platform was added.
- Remaining: WP-5 begins only after its bounded G06 review preamble freezes scope.

### 2026-07-19: WP-5 G06 Preamble

- Scope: C10, C11, C15, S52-S58, S63, storage and portability interfaces, and current implementation paths touched by the owning plan; review-only and bounded to one day.
- Findings: the existing administrator, Workspace Vault, stale-effect, cascading-delete, and pre-opener authorization gaps are frozen as in-scope security work; scheduled storage defects remain in scope; the unbound Vault re-export item is `NO-ACTION`.
- Minimality: migration journal/special boot, open-ended CAS, gate takeover, quarantine recovery, test matrices, temporary Web, and a second L6 are removed; no new engine, workflow, runner, or harness is authorized.
- Exit criteria: the inherited G06 trust-domain, fail-closed policy, token/bootstrap interface, Vault, attribution, redaction, and no-second-RBAC requirements remain unchanged and now belong entirely to WP-5.

### 2026-07-19: WP-5 Stage 0 Exit

- Scope: reconcile the G06 owning documents and freeze the smallest implementation boundary before any WP-5 behavior change.
- Result: identity, policy, Workspace authority, Vault, storage, portability, CAS, terminal-decision, stale-effect, and release-surface owners now agree; the existing Web receives only necessary same-release schema alignment while its multi-user UX and browser acceptance remain with S10.
- Minimality: open-ended CAS and first-writer families, migration lifecycle machinery, automation pause state, WP-5 quarantine/recovery ownership, browser verification, temporary Web work, and duplicate L6 proof are explicitly excluded.
- Remaining: Stage 1 owns only the mechanical operation-coverage guard, one table-driven migration preflight fixture, and the minimum failing Core DB constraints; production behavior remains unchanged.

### 2026-07-19: WP-5 Stage 1 Exit

- Scope: establish the smallest failing proofs before shared schema, storage, lifecycle, or authorization behavior changes.
- Result: one existing OpenAPI test enumerates direct and derived Workspace operations plus Core/Gateway boundaries, one file-local migration table proves the read-only preflight across nine representative layouts, and the two restrictive user foreign keys plus active owner membership remain exact red assertions.
- Minimality: no shared fixture package, migration journal, crash matrix, policy matrix, runner, or acceptance harness was added; the preflight is a pure scan and has no publication or repair responsibility.
- Remaining: Stage 2 owns shared durable and release-coupled schemas; Stages 3 and 4 must close only their named red storage and operation-metadata assertions.

### 2026-07-19: WP-5 / G06 Exit

- Scope: close the inherited G06 identity, policy, authorization, Vault, multi-user Workspace, storage, attribution, effect-authority, portability, and exact-release non-Web criteria through the single WP-5 stream.
- Result: S63 is Implemented and WP-5 is verified through owner-independent V2 storage and migration, the complete bounded sharing/user lifecycle, centralized authorization, named attribution and current-effect checks, authorized projections, portability, and the Core Client plus bearer-reachable CLI/Skill surface. S62 remains Partial across other plans, and S10 retains the rebuilt Web.
- Verification: affected package tests, typechecks and builds, generated contracts, migration and backup evidence, the composed release bundle, repository gates, built smoke, and independent deletion-first review pass. The restricted listener `EPERM` is recorded as an environment condition rather than answered with another runner or harness.
- Minimality: no compatibility reader, second credential system, policy engine, durable access ledger, recovery workflow, runner, acceptance harness, OpenShell fork, or patch entered WP-5; the four session-only Skill/CLI operations remain the accepted bounded compromise.
- Remaining: the G07 preamble and WP-5A are complete; retained real history solely gates WP-6/G07 implementation, and the overall audit remains in progress through WP-7.

### 2026-07-19: G07 Review-Only Preamble

- Scope: C12, S17-S19, S60-S61, S39's accepted delivery interface and their current Knowledge projections; review-only and bounded to one day.
- Findings: one raw-error `SECURITY-GAP`, duplicate selection/delivery ownership, stale callers, delayed validation, proposal half-state, missing worker-visible Knowledge provenance, speculative Draft mechanisms, and an unproved real-history entry gate.
- Frozen scope: one governed selector, exact internal callers, product-safe errors, save-time validation, create-only proposal application and reversal, and exact Knowledge bytes plus lineage in the existing S39 trace.
- Minimality: persistent reflection, provisional self-confirmation, triggers, Skill Catalog, generic ImprovementProposal, reconstruction, Harness, Judge, suites, long-horizon evaluation and new runners are removed from WP-6 implementation authority.
- Stage 0 exit: blocker-focused contract and deletion-first reviews closed proposal replay, live S39 projection, source/review identity, index coherency, output-isolation, and duplicate-producer ambiguities; documentation and repository gates pass with zero production or test change.
- Remaining: WP-5A and its provider entry corrections are complete; one retained real completed work history now solely gates the explicit human-reviewed loop, and the inherited G07 exit criteria remain unchanged.

### 2026-07-19: WP-5A Provider And External-Effect Exit

- Scope: close only the G04 findings that could make G07 dogfooding rely on contradictory provider ownership, misleading cache evidence, repeated external effects, or uncertain Git replay.
- Result: one S42-owned non-Codex path, product-neutral public dispatch evidence, provider-reported cache quantities, contradictory CapabilityCall rejection, and fail-closed Git uncertainty now agree across specifications, schemas, NanoCore, generated contracts, and the existing real-provider story.
- Real proof: disposable A1 passed the existing story through stock pi-ai, stock Ollama, `local-ollama`, and `qwen2.5:0.5b`, with two distinct successful calls, non-empty output, Usage evidence, reported zero cache quantities, and an empty leak scan.
- Compromise: an explicitly keyless provider receives one fixed non-secret pi-ai placeholder that is neither authority nor persisted evidence; hosted profiles retain explicit credential requirements.
- Verification: the complete NanoCore package and affected App API, Web, schema, typecheck, build, generated-contract, repository, and whitespace gates passed.
- Minimality: no fork, patch, transport, durable owner, recovery workflow, runner, harness, or compatibility path was added; retained real completed work history is the only remaining WP-6 entry gate.

### 2026-07-19: WP-6 Retained-History Entry

- Scope: satisfy the existing real-use gate without adding a G07 runner, evaluation lifecycle, or recovery owner.
- Result: one disposable-A1 direct Task through stock OpenShell `0.0.80` and Codex `0.144.1` retained a completed Turn, one completed assistant Item, normal runtime evidence, strict-verifier-confirmed S39 bytes and digest, and meaningful positive feedback; the task repository remained clean.
- Corrections: focused tests and direct fixes made OpenShell exec arguments newline-free, left the existing story supervisor as sole request deadline, and created the isolated `0700` Codex home before launch.
- Verification: the real story passed with one root and two child origins, the S39 strict verifier passed, evidence files remained `0600`, and whole-Cell cleanup returned zero containers and sandboxes.
- Remaining: WP-6 Stage 1 is active over the frozen existing-owner Knowledge corrections; the inherited G07 exit criteria remain unchanged.

## Implementation Summary

Active under the 2026-07-17 Convergence Redesign. The roadmap preflight, durable group ledger, Phase 0 baseline refresh, Foundation and Core Concepts authority work, Work Model authority repair, Metering current-state alignment, worker-capability truth reset, scheduler hardening, Codex provider hardening, workspace-review base correction, real Goal L6 slice, and disposable OpenShell Cell audit are recorded as historical inputs to their owning groups. G01/WP-0, G02/WP-1, G03/WP-2, the bounded G04/G05 WP-3 reviews, the WP-4 Stage 0-4 kernel package, WP-5/G06, G07 Stage 0, and WP-5A are closed, and S01 is superseded into named current owners. G07's speculative platform scope remains removed, its retained-history entry gate has passed, and WP-6 Stage 1 is active. Explicitly dispatched findings remain scheduled with their Execution Program Backlog owners; the accepted S16 and rebuilt multi-user Web projections are scheduled with post-program S10 after G09, while G00 residual, G08, and G09 remain scheduled for WP-7 and the overall audit remains in progress.

## Final Verification

Program-level closeout remains pending until every contract group satisfies its exit condition. The 2026-07-14 hardening slice passed focused regression tests, the complete NanoCore suite, NanoCore lint, typecheck and build, the repository-wide check, the real L6 runner suite, real A1 OpenShell/Codex Goal acceptance, exact proof-file and unchanged-`HEAD` checks, redacted evidence checks, and the zero-residual-sandbox check; that evidence remains input to its owning groups rather than program closeout.
