# Findings

This report retains execution findings from the stable Worker refresh and combined staging acceptance; it does not authorize product behavior or operator exceptions.

## Follow-up Index

- [ ] `CPR-FND-001` [deferred] Expired rebuild interval has no explicit operator recovery
- [ ] `CPR-FND-002` [deferred] Failed idle Sandbox has no automatic cleanup selector

## [deferred] CPR-FND-001 — Expired rebuild interval has no explicit operator recovery

- **Observation:** After the failed Unit F F2 fixture caused an uncertain Sandbox deletion, the retained NanoHost fence marker recorded `2026-09-05T23:28:13.775847980Z`. One ordinary service start reached generation 12 and failed before sending readiness at `2026-09-05T23:43:04.244756Z`: the original marker was approximately 890 seconds old, beyond the 300-second hard bound. The current create-once marker survives every later failure, while only an in-bound readiness acknowledgement may consume it; ordinary delayed manual starts therefore cannot recover this interval.
- **Impact:** Persistent staging remains non-ready after a diagnostic pause exceeds the hard bound. Repeated starts cannot change that condition. The failed interval remains FAIL; changing the marker to manufacture a passing recovery measurement would violate its owner.
- **Evidence:** Runtime source `0a528186432db2089ee90e59f5772421a7fe23b6`; `apps/nanohost/src/main.rs` readiness gate and `apps/nanohost/src/epoch_evidence.rs` first-fence lifecycle; `temp/changes/202609051702580059-codex_pi_stable_refresh/a2-runtime/unit-f-0a528186-attempt1-recovery.md`, SHA-256 `054eb4485a6c12e034409343b2b2303d3b34ce6814210d387b8602c6f1f1ce5e`.
- **Owner:** `docs/specs/20260802-nanohost_runtime_and_transport.md`, epoch rebuild budgets and first-fence lifecycle. No standing operator recovery action is specified; Intent Epoch 9 records the engineer's explicit one-time operator exception.
- **Next action:** The independent direction check returned Ask Human because the accepted owner permits only an in-bound acknowledgement to consume the marker. Await the engineer's decision on one explicit outside-test recovery that preserves the failed interval and marker evidence, proves whole-epoch absence, archives the exact expired marker, and starts a new interval while retaining all users, data, configuration, credentials, and identity. No marker mutation or new recovery feature is authorized by this report. On 2026-09-06 the engineer explicitly authorized reasonable A2 operations after reviewing the concrete recovery proposal; Intent Epoch 9 records that authority, and the fresh independent check returned Continue. Execute the one-time operator exception, retain its exact result, and preserve the expired interval as FAIL; general automatic or operator recovery remains a separate product concern. The guarded archive and later normal start completed, preserving the failed interval; the exact receipt is `temp/changes/202609051702580059-codex_pi_stable_refresh/a2-runtime/unit-f-0a528186-attempt1-operator-recovery-result.md`, SHA-256 `a8e212209b2d4b08d4a39d4f7dc2cd46666dc3254c86a571d2ce3dcb55f7f383`. Deferred under the current upgrade-only scope and explicit operator exception: the NanoHost runtime specification receives any general recovery design when separately authorized.

## [deferred] CPR-FND-002 — Failed idle Sandbox has no automatic cleanup selector

- **Observation:** The discarded F2 fixture stopped Docker during retirement of the preceding idle Sandbox, before its required accepted-create barrier. The failed/unknown/draining/cleanup-unknown Sandbox remains after a later fresh coordinator, while its original backend is already cleaned and its lease terminal. Restart recovery selects neither record, and read-only admission reports capacity-saturated without reaching the existing cleanup owner.
- **Impact:** New Tasks remain deferred until an operator invokes the existing exact cleanup callback. This is a missing automatic selector, not evidence of a provider credential failure or a passing F2 recovery.
- **Evidence:** `apps/nanocore/src/runtime/turn-executor-factory.ts` capacity inspection and `cleanupSession`; `apps/nanocore/src/runtime/scheduler-restart-recovery.ts` terminal lease and cleaned backend exclusions; `temp/changes/202609051702580059-codex_pi_stable_refresh/a2-runtime/unit-f-0a528186-attempt3-f1-diagnosis.md`, SHA-256 `ad1e8941b2d75bdedf8fca8fad555f92d37af9b8dfb6fbb38e349be0ada5b202`. Generation 15 has strictly later fresh-ready proof, with no open binding, active lease/backend, or pinned Goal.
- **Owner:** `docs/specs/20260802-nanohost_runtime_and_transport.md`, unknown-effect fencing and later-fresh cleanup. The existing exported `cleanupBackendSession` callback is the exact cleanup owner; no automatic selector is claimed.
- **Next action:** The fresh independent direction check initially proposed automatic retirement, then re-read current scope and accepted `Continue`: no current upgrade gate requires that automatic selector, and the discarded fixture residue is outside the F2 acceptance barrier. Under Intent Epoch 9, invoke the existing cleanup callback once with the exact retained backend identity and immediate empty-occupancy, unpinned, later-fresh checks; retain the mutation and prove only the exact runtime projection is removed. Run the complete corrected matrix separately without counting maintenance as a PASS. The existing compiled cleanup owner completed exactly once after the guards; readback proved zero target Sandbox rows and preserved the cleaned backend, released lease, original Turn lineage, and generation 15 readiness. The retained diagnosis now includes the exact command and result. Deferred after the independent scope check against accepted owners: the NanoHost runtime specification receives an automatic cleanup selector only when that additional recovery behavior is separately authorized; current upgrade acceptance continues on the corrected full matrix.
