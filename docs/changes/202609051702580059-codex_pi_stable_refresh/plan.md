---
type: change-plan
status: verified
started: 2026-09-05
completed: 2026-09-06
branch: codex/codex-pi-stable-refresh
---
# Codex And PI Agent Stable Refresh

## Intent Epoch 1

- **Source:** Delegated builder assignment in the Cursor CLI session on 2026-09-05, after explicit engineer steering that Codex and PI Agent version/schema compatibility work may proceed in parallel while the primary finishes PR #4 tests.
- **Requested outcome:** Refresh only Codex and PI Agent Workers to the latest formal non-prerelease stable releases, including exact pins, lockfile, freshly generated Codex app-server schema and metadata, PI contract alignment, affected adapter/protocol consumers, tests, and local guides.
- **Non-negotiables:** Conform to the new versions rather than retain backward compatibility; add no migration layer, wrapper, runner, compatibility shim, or broader feature; do not change OpenCode, OpenShell, models.dev, Open Knowledge Format, roadmap feature state, or the R058 change record; do not deploy, SSH, access credentials, run real-provider/host tests, publish, push, or open or merge PRs; do not commit until requested; leave the result reviewable for the primary so PR #4 can merge first.
- **Acceptance observations:** Official registry and release identities for the selected Codex and PI pins are recorded under this worktree `temp/research`; Codex schema metadata records the exact immutable tag and peeled commit from a complete non-shallow checkout; image, manifest, adapter, schema, provenance, smoke, and focused tests name the same pins; focused schema, adapter, PI, lint, typecheck, and build checks report exact observed results.
- **Exclusions:** OpenCode, OpenShell, models.dev catalog, Open Knowledge Format, pi-ai Gateway pin, NanoHost/OpenShell pins, R058 feature work, real-provider or host tests, image publication, and any new Worker architecture.
- **Effect boundary:** Edits and local installs stay inside `/Users/m5pro/Documents/AI/openkit/temp/worktrees/codex-pi-stable-refresh` on branch `codex/codex-pi-stable-refresh`; network is used only to read official npm and GitHub sources and to clone exact tags into this worktree `temp/research`.

## Intent Epoch 2

- **Source:** Delegated builder follow-up in the same Cursor CLI session on 2026-09-05, authorizing local Docker image proof while the completed source diff stays under independent read-only review.
- **Requested outcome:** Build and smoke only `worker-codex` and `worker-pi` with unique task tags `openkit/worker-codex:codex-pi-refresh-20260905` and `openkit/worker-pi:codex-pi-refresh-20260905`; capture source input digests, image IDs, logs, and exit statuses under this worktree `temp/research`; update this plan's evidence.
- **Non-negotiables:** Do not change shared `:dev` tags; keep production source bytes frozen during reviewer inspection and report any genuine build/smoke failure before editing; no A2, host credential, provider request, push, commit, deploy, or unrelated cleanup; do not broaden scope or create test frameworks.
- **Acceptance observations:** Local Docker Engine 29.5.2 on linux/aarch64 produces named task-tag images; smoke scripts exit 0 and print the current Codex `0.153.4` and PI `0.85.0` proofs; evidence files under `temp/research/image-proof-20260905/` record digests, inspect output, logs, and statuses.
- **Exclusions:** OpenCode image, shared `:dev` tags, publication, host/provider tests, and any source change unless a named build/smoke bug is first reported.
- **Effect boundary:** Same isolated worktree; local Docker build and `docker run --rm` smoke for the two unique tags; network only as those image builds require for pinned package installs.

## Intent Epoch 3

- **Source:** Independent reviewer findings returned to this isolated worktree on 2026-09-05 after local unique-tag image proof.
- **Requested outcome:** Reconcile the compaction spec's current Pi pin and source identity; restore truthful historical July/August image-evidence attribution and add only actual 2026-09-05 results; add per-generated-file checksum evidence and explicit compatible/adapted/blocking dispositions plus recorded feasibility/realization results under the existing snapshot metadata owner.
- **Non-negotiables:** Do not invent a validator or framework; do not widen repin scope to unrelated baseline gaps; keep provenance fixtures synthetic; no commit, push, deploy, or A2.
- **Acceptance observations:** Compaction spec current Pi pin is `0.85.0` at `107d79f11072bbc8a3a757ed7fd69596bee7d68c`; owning image specs attribute 2026-07-21/2026-08-01 evidence to previous pins and record 2026-09-05 unique-tag results separately; `packages/codex-app-server-schema/metadata.json` checksums cover every generated JSON file; consumed-surface dispositions and gate results are recorded.
- **Exclusions:** OpenCode, models.dev, OpenShell snapshot repair, native provenance recapture, A2, and new architecture.
- **Effect boundary:** Same isolated worktree; production edits limited to the named spec, snapshot metadata, existing schema validator, and this plan.

## Intent Epoch 4

- **Source:** Root direction in the same Cursor CLI session on 2026-09-05 after the resume `--cd` correction: CI verification is a separate isolated checkout, and remaining selectable-pin checks for this refresh are cut to image smokes plus stock OpenShell packaging.
- **Requested outcome:** Keep this worktree on version, schema, adapter compatibility, metadata, documentation, and the concrete resume `--cd` fix. Record that required remaining re-pin checks are eight catalog worker/platform image smokes with `push:false` and A2 stock OpenShell create, AEP upload, generic shim dry-run, and delete. Do not treat general real-provider, reconnect, recovery, or provenance L6 work as new refresh blockers.
- **Non-negotiables:** Do not edit CI or workflow files in this checkout. Tag-only image-matrix cannot verify without publication; the dedicated CI builder in `temp/worktrees/codex-pi-refresh-ci` (base `edc9e31f1366897f69152328e26aaf0fe24bf737`) owns a minimal existing manual release-gate extension that runs those eight smokes with `push:false`. Root integrates the reviewed CI diff later. No commit, push, deploy, or A2 from this delegated session.
- **Acceptance observations:** This plan and the owning adapter and image evidence distinguish refresh-required remaining checks from separately owned baseline gates. This checkout still contains no CI/workflow edits.
- **Exclusions:** `.github/` and other CI/workflow files; the CI checkout; real-provider turns; worker-control reconnect and recovery; provenance L5/L6 and production advertisement; inventing a new image-matrix or publication path.
- **Effect boundary:** Same isolated worktree; documentation and metadata only for this remaining-gate cut.

## Intent Epoch 5

- **Source:** Root direction on 2026-09-05 after independent acceptance of the CI slice: integrate the exact reviewed patch into this refresh worktree as the sole destination writer.
- **Requested outcome:** Apply unchanged `temp/changes/202609031230162442-r058_worker_mcp/candidate-review/codex-pi-ci-reviewed.patch` (SHA-256 `55a4ef614943818ab61a72115b93360156eb8f2c69d5d761eee15ef60b1493c0`) after hash verify and `git apply --check`. It may touch only `.github/workflows/ci.yml`, `.github/workflows/README.md`, and `tests/release-workflow.test.mjs`. Run focused eight workflow tests and `git diff --check`. Freeze the whole candidate including the resume `--cd` fix and metadata. Remaining required effect-domain checks are the actual eight GitHub-hosted worker/platform smoke jobs and A2 stock OpenShell packaging.
- **Non-negotiables:** The previous no-CI-edit instruction is superseded only for this accepted patch. Do not redesign the workflow. Do not call the producer `red.log` RED; it was a missing-yaml Node 22 setup failure. Independent Node 24 evidence is 7 pass / 1 fail then 8/8 plus the read-only/`push: false` boundary, recorded in `temp/changes/202609031230162442-r058_worker_mcp/candidate-review/codex-pi-refresh-ci-review.md`. No commit, push, workflow dispatch, or A2 from this delegated session. Root owns next execution.
- **Acceptance observations:** Patch hash matched; `git apply --check` and apply were clean; this tree's `node --test tests/release-workflow.test.mjs` is 8 pass / 0 fail under Node `v24.18.0`; `git diff --check` is clean.
- **Exclusions:** Redesign of image-matrix or publication; commit; push; dispatch; A2; real-provider, reconnect, recovery, and provenance L6 as new refresh blockers.
- **Effect boundary:** Same isolated worktree; production CI/workflow edits limited to the three reviewed files.

## Intent Epoch 6

- **Source:** Independent Auditor finding CPR-AUD-001 in `/Users/m5pro/Documents/AI/openkit/temp/changes/202609031230162442-r058_worker_mcp/final-audit/20260905-r058-worker-mcp-repository-integrity-audit.md`, returned to this worktree on 2026-09-05.
- **Requested outcome:** Restore the standing vendor re-pin MUST and the consuming Codex, Pi, and image owners' existing required-gate meaning. Record every applicable unrun feasibility or realization gate as pending. Remove the unsupported claim that eight CI worker smokes plus stock OpenShell packaging alone close selectability or integration. Clarify that `push: false` nonpublication applies to worker release images; the existing `test-image` job may publish a content-addressed CI execution image.
- **Non-negotiables:** Do not invent optional, disabled, or release-only exceptions. Preserve only exceptions already explicit in an owning specification. No code, CI, metadata, schema, or test edits. No commit, push, dispatch, or A2. Earlier Intent Epochs stay immutable historical record.
- **Acceptance observations:** Affected Codex, Pi, and image evidence paragraphs again state that real-provider, readiness, heartbeat, interruption, reconnect, recovery, stock OpenShell, and architecture evidence remain unobserved acceptance obligations. The plan pending list matches those consuming-spec gates.
- **Exclusions:** New checks, new architecture, vendor-owner exception drafting, implementation, and remaining execution.
- **Effect boundary:** Same isolated worktree; documentation restoration only.

## Intent Epoch 7

- **Source:** Root direction on 2026-09-06 after official npm `@earendil-works/pi-coding-agent` latest moved to `0.85.1` while Codex GitHub latest remained non-prerelease `rust-v0.153.4`. Authorized parallel Pi patch-release preparation in `/Users/m5pro/Documents/AI/openkit-codex-pi-stable-refresh` while PR #4 completes.
- **Requested outcome:** Independently verify Pi `0.85.1` official npm/release/tag/source identity, inspect the actual upstream delta, and update only existing Pi Worker version pins and affected adapter/smoke/test/doc consumers. If consumed contract is unchanged, make only the pin/provenance update plus focused Pi checks and one unique-tag local Pi image build/smoke.
- **Non-negotiables:** Do not integrate PR #4 or merge main. Preserve every pre-existing root edit including this dirty plan; never rewrite Intent Epochs 1–6. Codex version/schema bytes, OpenCode/OpenShell/models.dev, NanoCore pi-ai Gateway dependency, CI, R058, and OKF are excluded. No backward-compatibility path, new dependency, wrapper, runner, framework, or speculative feature. No commit, push, SSH, credentials, provider tests, deploys, Git history rewrites, or shared `:dev` tags. Do not repeat all eight images.
- **Acceptance observations:** Official npm `latest` is `@earendil-works/pi-coding-agent@0.85.1` published 2026-09-05T12:17:19Z; GitHub non-prerelease `v0.85.1` published 2026-09-05T12:29:01Z peels to commit `d981de1229ef899957bbe968bc8dcda02a21f477`, matching npm `gitHead`. A complete non-shallow checkout of that exact tag is at `temp/research/earendil-works-pi-v0.85.1`. JSON-mode docs, print-mode, agent-session events, compaction index, `dist/bundle/cli.js`, fail-closed flags, and `anthropic/claude-sonnet-4-5` are unchanged from `0.85.0`. Focused Pi adapter TAP `1..23` all ok; unique-tag local image `openkit/worker-pi:pi-0.85.1-refresh-20260906` id `sha256:6cd46bcc208092417082152cf022a1818674f21bdcc921b02d6424c8e60662de` built and smoked exit 0 with native `0.85.1`.
- **Exclusions:** PR #4 integration; Codex schema; CI; A2; eight-image campaign; OpenCode pin; NanoCore `@earendil-works/pi-ai` Gateway pin.
- **Effect boundary:** This worktree; existing Pi pin consumers, owning Pi/image/compaction docs, local Pi guides, and current plan sections only.

## Intent Epoch 8

- **Source:** Engineer steering on 2026-09-06 designating A2 as continuously used staging and then requesting complete OpenKit PR, branch, and worktree reconciliation after the two current major tasks.
- **Requested outcome:** Retain normal A2 data, users, configuration, credentials, subscriptions, and services across tests and version upgrades. After PR #4 and the Codex/PI plus OKF v0.2 work complete, integrate all OpenKit changes into `main`, resolve all repository PRs, and remove remaining development branches and worktrees locally, on A2, and on GitHub after preserving their changes.
- **Effect and sequencing boundary:** This supersedes earlier per-run restoration/decommission/data-deletion assumptions. Existing tests may settle their own Worker lifecycle and transient processes, while normal product data and credentials remain. Preserve both the original `~/.openkit` and currently exercised data until safely reconciled. No A1 effect is authorized; later roadmap feature development remains paused.

## Intent Epoch 9

- **Source:** Engineer steering on 2026-09-06 after the expired-marker recovery proposal and remaining-test explanation: authorize any reasonable operation and use of A2, with rapid completion of final tests and PR integration because time is limited.
- **Requested outcome:** Execute the prepared one-time failed-interval recovery, finish the existing remaining runtime acceptance, merge PRs 5, 6, and 7, and complete the already authorized branch and worktree cleanup after preservation.
- **Effect boundary:** This explicitly authorizes archiving the exact expired marker after proving old-epoch absence and preserving its failure evidence, then starting a new recovery interval. It is an engineer-authorized operator exception, not proof that the failed interval met its deadline and not a new automatic recovery feature. Retain all users, data, configuration, credentials, subscriptions, and identity on A2 machine `34b95b09f8c1475ea242800759411fea`.
- **Execution boundary:** Reuse the existing accepted App, Worker, and NanoHost artifacts, corrected Unit F runner, normal staging configuration, and test commands. Do not repeat completed PR #4 L6, OKF migration and normal-use, stock-image, or unchanged full CI acceptance. The existing F1-F4 Aggregate and separate public Codex and OpenCode observations remain required before ordered integration and cleanup.
- **Independent direction:** The fresh resume check returned `Continue` after inspecting the exact proposal and new engineer instruction; the previous approval blocker is resolved. The operator revalidated the exact machine, failed service, absent process, unchanged marker, absent archive destination, and current runner identity before effect.
- **Integration method:** Use the repository-permitted GitHub merge-commit method in PR5, PR6, PR7 order. No repository rule requires squash or linear history. Keeping the reviewed ancestry preserves the source and stash receivers directly and avoids replaying stacked changes; live-main tree equality remains the final content proof.

## Intent Epoch 10

- **Source:** Reconciliation of the engineer's original request to finish PR #4, refresh only Codex and Pi plus OKF v0.2, pause subsequent features, and then integrate preserved work and clean development refs; the later instruction authorizes reasonable A2 operations and asks for fast final testing and merge. The PR #4 handoff and those engineer messages name no F2-F4 Aggregate or separate OpenCode runtime campaign. Independent source-intent inspection compared those exact requests with the consuming owners and the four-gate classification already recorded above the Working Checkpoint.
- **Attribution correction:** Intent Epoch 9 over-recorded the agent's selected F1-F4/OpenCode execution method as an engineer requirement. That historical sentence remains visible but is not an additional source of authority. This epoch corrects that attribution; it does not waive an engineer instruction or consuming-owner gate. The four existing refresh gates remain eight Worker/platform smokes, three-leaf stock OpenShell packaging, Codex F1 readiness/provider/restart/reconnect/completion, and public Codex interruption/cleanup.
- **Outcome and boundaries:** Complete those four gates and the accepted OKF migration/use checks, integrate PR5, PR6, and PR7 with exact content preservation, and perform the authorized finite Git cleanup. Retain all failed attempts as failed and all normal A2 state. Generic NanoHost release qualification, separately disabled Pi eligibility, optional provenance advertisement, and subsequent roadmap work remain unclaimed and deferred; no new feature or compatibility path is authorized.
- **Acceptance:** The original engineer outcome and every applicable current refresh-owner predicate are satisfied before final integration. Source-intent correction is independently reviewed against the messages and owners, rather than justified solely by a later agent's preferred method.

## Owning Documents

- `docs/specs/20260522-vendor_snapshot_packages.md` owns snapshot packaging, exact-tag non-shallow evidence, and the standing re-pin obligation.
- `docs/specs/20260716-codex_worker_adapter.md` owns the Codex adapter, command surface, and native version pin.
- `docs/specs/20260716-pi_worker_adapter.md` owns the PI adapter, JSON-mode command surface, and native version pin.
- `docs/specs/20260721-worker_execution_environment_images.md` owns worker image runtime pins.
- `docs/specs/20260711-worker_runtime_subagent_provenance.md` owns Codex provenance fixture and parser version alignment.
- `docs/specs/20260802-nanohost_runtime_and_transport.md` remains the NanoHost boundary and is not changed by this pin refresh.
- `packages/codex-app-server-schema/` owns generated Codex app-server schema artifacts.

## Accepted Decisions Already In Force

- Compatibility means the current accepted adapter contracts against the new pinned runtimes, not a retained old-version path.
- PI remains a `bounded-turn` adapter on the historical direct Anthropic pair until a separately accepted relay-capable contract exists; this refresh does not invent that contract.
- Codex remains the sole `session-continuity` adapter using `codex exec` / `codex exec resume` and the trusted NanoCore Responses relay.
- Schema generation uses the pinned Codex CLI command recorded in package metadata, with source identity taken from the exact GitHub tag checkout.
- CPR-AUD-001 restoration remains: the vendor Standing Re-Pin Obligation and consuming-spec required-gate meaning are not waived. After owner reconciliation, this refresh's current integration gates are the four listed below. Pi target-NanoHost ineligibility and template `readiness.status: disabled` keep Pi provider, lifecycle, session-continuity, and `inference.local` obligations pending before future Pi selectability; they are not waived and are not required to land a still-disabled pin. Optional provenance stays pending while explicitly unadvertised. This refresh does not create a new real-resume gate, full release qualification, generic RelayStream campaign, or separate OpenCode acceptance.

## Re-Pin Dispositions And Gates

Codex consumed-surface dispositions are recorded in `packages/codex-app-server-schema/metadata.json`. The Responses Lite local-tool and client-search surface is additionally `adapted` by the unified Gateway backend correction, with real-provider F1 and interruption acceptance still tracked below. Summary: exec/resume positional argv and flags except resume `--cd` are `compatible`; first-turn `--cd` versus resume Harness cwd is `adapted`; `-c` TOML override keys, `CODEX_HOME`/`sessions`/`rollout-*.jsonl`, native version output `codex-cli 0.153.4`, exec JSONL provenance fields, rollout JSONL fields, and NanoCore worker-inference runtime-hint fields are `compatible`; app-server JSON-RPC schema growth is `compatible` because the worker adapter does not parse that protocol; `ThreadReadResponse.json` parent-thread fields remain present (`compatible`); `ItemStartedNotification.json` no longer carries parent-thread fields and is not consumed by the rollout parser (`compatible`). `metadata.implementationValues` is the single frozen map of actual consumed upstream literals from `packages/worker-shim/src/adapters/codex.ts`, `packages/worker-shim/src/codex-runtime-provenance.ts`, `apps/nanocore/src/llm/worker-inference-runtime-hint.ts`, and `containers/worker-codex/smoke.sh`. The OpenKit image path `/usr/local/lib/codex/bin/codex` is excluded. That metadata completion and the resume `--cd` adaptation do not close the current integration gates.

Pi consumed-surface dispositions are recorded in `docs/specs/20260716-pi_worker_adapter.md`. For `0.85.1` versus `0.85.0`: JSON-mode flags, `claude-sonnet-4-5`, `agent_settled`, and published bin `dist/bundle/cli.js` are `compatible`; Gateway/`inference.local` remains `blocking` for the target route, unchanged from `0.80.7`. The 0.85.1 upstream delta does not touch the consumed Worker contract.

`docs/specs/20260522-vendor_snapshot_packages.md` Standing Re-Pin Obligation requires consuming-spec feasibility or realization gates to re-run before a new pin becomes selectable. Unique-tag local linux/aarch64 image smoke, focused workflow tests, and the integrated manual worker-image job graph do not close that obligation. Spec meaning restored by CPR-AUD-001 is unchanged. The lists below classify current integration gates versus future disabled or unadvertised obligations after owner reconciliation. Independent Verifier and Auditor inspection confirmed this classification against Codex readiness, the explicitly disabled Pi manifest and owner, and the unadvertised optional provenance owner; no gate was waived or reported passed.

Observed 2026-09-05 as evidence only, not as selectable-pin closure: unique-tag local linux/aarch64 Docker build and smoke for `worker-codex` and `worker-pi`; focused adapter, provenance, schema, image-contract, lint, typecheck, and build checks; isolated native `exec`/`exec resume` parse proof; accepted CI patch applied unchanged; focused eight workflow tests 8/8 under Node `v24.18.0`.

Independent CI review path: `/Users/m5pro/Documents/AI/openkit/temp/changes/202609031230162442-r058_worker_mcp/candidate-review/codex-pi-refresh-ci-review.md`. Reviewed patch: `/Users/m5pro/Documents/AI/openkit/temp/changes/202609031230162442-r058_worker_mcp/candidate-review/codex-pi-ci-reviewed.patch` SHA-256 `55a4ef614943818ab61a72115b93360156eb8f2c69d5d761eee15ef60b1493c0`. Independent Node 24 evidence is RED 7 pass / 1 fail (`codex-pi-ci-independent-red.log`) then GREEN 8/8 (`codex-pi-ci-independent-green.log`). The producer `temp/research/codex-pi-ci/red.log` is a missing-yaml Node 22 setup failure and is not RED. Manual worker-image jobs use `contents: read`, `load: true`, and `push: false` and do not publish release worker images. The existing `test-image` job has `packages: write` and may publish a content-addressed CI execution image.

Current integration gates and observed status:

- **Passed:** Eight catalog worker/platform image builds and smokes: `worker-common`, `worker-codex`, `worker-opencode`, and `worker-pi` on `linux/amd64` and `linux/arm64`. The `worker-common` smoke already includes the throwaway derived-image proof. Owners: `docs/specs/20260721-worker_execution_environment_images.md`, `docs/specs/20260708-container_image_packaging.md`.
- **Passed:** A2 stock OpenShell create, AEP upload, generic shim dry-run, and delete for all three final leaves (`worker-codex`, `worker-opencode`, `worker-pi`) — `docs/specs/20260721-worker_execution_environment_images.md`, `docs/specs/20260708-container_image_packaging.md`.
- **Passed:** Codex Unit F F1 attempt 9 on the exact new Codex worker image: real-provider success, ready, heartbeat progress, NanoCore restart, next-generation reconnect, same-lineage completion, and teardown/fresh-empty — `docs/specs/20260716-codex_worker_adapter.md`, `docs/specs/20260703-worker_control_protocol.md`, `docs/specs/20260802-nanohost_runtime_and_transport.md`.
- **Passed:** Public `interruptTurn` on that exact Codex image after ready and successful inference, with native `interrupted` / `aborted`, Core Turn `interrupted`, Task `cancelled`, exact lease release, backend cleanup, and ready capacity — `docs/specs/20260716-codex_worker_adapter.md`, `docs/specs/20260703-worker_control_protocol.md`.

Pending before future Pi selectability, not waived, and not required to land the still-disabled Pi pin (`docs/specs/20260716-pi_worker_adapter.md` target-NanoHost ineligibility; template `readiness.status: disabled`):

- Pi real-provider turn, worker-control readiness, heartbeat, interruption, reconnect, and recovery
- Pi NanoHost RuntimeTarget, session-continuity, and Gateway `inference.local`

Pending while optional provenance remains explicitly unadvertised (`docs/specs/20260711-worker_runtime_subagent_provenance.md`):

- Provenance L5 and L6 packaged NanoCore Codex task that spawns at least two sub-agents
- Provenance skip-aware real Codex story opt-in acceptance gate
- `worker.runtime-provenance.v1` production advertisement

This refresh does not create a new real-resume gate, a full release-qualification campaign, a generic RelayStream campaign, or a separate OpenCode pin-acceptance obligation. OpenCode appears in the eight smokes and three-leaf A2 check only because the shared worker catalog and Dockerfile already require that leaf.

## Working Checkpoint

- **Source:** PR #4 is merged as `f7cab19136f30fe928c84995ca303eace7377c4a`. PR #5 is `80e23a15`; PR #6 is `240e4e2c`; the combined accepted runtime is `ec9490e6a6d53f2b9d19e968754f754938b98c9e`, tree `5d83080438512bc0cf290633e6b3e5215eec0981`. Codex is `0.153.4`, Pi is `0.85.1` and remains disabled, and OpenCode remains `1.18.1`.
- **Accepted artifacts:** A2 Run9 App index is `sha256:5cd666ef358a138f404d4b3abf4da0041e27493faf818ee06780258654040203`, native leaf `sha256:1f14441fe1a9279c945bf32090fea189ed5780083c2ae1c2feb397f408f63d79`, and Web SHA-256 `f06f58101c88fb50467f0e8c6e2bc04775b0bd3a8bf34d78a8161173cad23327`. Worker images and NanoHost bytes remain unchanged from the accepted `6bf9bfbc` builds.
- **Runtime acceptance:** All four current refresh gates pass: eight Worker/platform smokes; three-leaf stock OpenShell packaging; Codex F1 attempt 9; and final public interruption `ws_24` / `th_27`, receipt SHA-256 `40ed7f8be04812ababededfa23102136426f4602a418419ec3b92808c94f5691`. OKF migration and public use remain accepted. Earlier failed attempts retain their actual FAIL dispositions.
- **Checks:** Complete Linux L0-L2 run `34001207497` passes. Final runtime corrections pass 289 focused cases, exact abort regressions, typecheck, lint, and build; full NanoCore reports 2645 passes and only the unchanged macOS `/proc/self/fd` failure already proved on Linux. Exact runtime-head CI `34014415157` passes.
- **Next action:** None for this refresh. Runtime acceptance, ordered integration, and preservation-checked Git cleanup are complete; later roadmap work remains paused.
- **Retention and exclusions:** Retain normal A2 data, failed records, users, credentials, subscriptions, services, and evidence. Generic NanoHost F2-F4, separate OpenCode acceptance, optional provenance advertisement, future Pi selectability, and later roadmap features are outside this refresh closure and remain unclaimed. No next feature starts.

## Verification

Toolchain: `node -v` = `v24.18.0`; `pnpm -v` = `10.33.3`; PATH used `/Users/m5pro/.local/share/mise/shims`.

- Final independent review observed Worker Shim 185/185, schema checks and all 304 hashes, workflow 8/8, documentation checks, and catalog validation passing. The required four-file release suite reported 36/42, with six unchanged package-release cases failing because macOS BSD tar rejects the existing GNU `--mtime=@0` argument. Repository validators passed before Biome processed zero files because this isolated checkout is beneath excluded `temp/`; package lint passed separately. The primary moved the unchanged Git worktree to `/Users/m5pro/Documents/AI/openkit-codex-pi-stable-refresh`, outside the excluded `temp/` tree, trusted the unchanged pinned mise file at its new location, and observed `pnpm -w check:repo` exit 0 with all validators, 976 Biome files, and catalog checks passing; raw output is `temp/research/check-repo-root-owned-final.log`. Linux CI must still settle the host-specific GNU tar cases. Evidence is retained in `temp/changes/202609031230162442-r058_worker_mcp/candidate-review/codex-pi-refresh-review.md` and its named raw logs.

- `pnpm --filter @openkit/codex-app-server-schema test` exit 0 after resume `--cd` `adapted` disposition; `lint` exit 0, Biome checked 3 files, no fixes. The frozen `implementationValues` map remains 59 unique consumed literals; dispositions are now 11.
- `pnpm --filter @openkit/worker-protocol build` then `test` TAP `1..20` all ok, exit 0; `typecheck` exit 0; `lint` checked 4 files, no fixes.
- `pnpm --filter @openkit/worker-shim test` TAP `1..186` all ok, including Codex `0.153.4` provenance parse and PI adapter JSON command tests, exit 0; `typecheck` exit 0; `lint` checked 28 files, no fixes. That whole-package TAP predates collapsing the resume `--cd` regression into the session-continuity test.
- Focused `pnpm --filter @openkit/worker-shim exec vitest run --reporter=tap-flat src/adapters/codex.test.ts` TAP `1..16` all ok, exit 0 after first-turn `--cd` retains `workingDirectory` and resume omits `--cd` in the existing session-continuity test; the separate full-argv resume case is removed.
- `pnpm --filter @openkit/protocol build` then `pnpm --filter @openkit/config-schema test` TAP `1..159` all ok, exit 0; `typecheck` exit 0; `lint` checked 32 files, no fixes.
- `pnpm exec turbo run build --filter=@openkit/nanocore^...` 7 successful / 7 total, exit 0.
- NanoCore focused Vitest: `openshell-worker-dockerfile.test.ts`, `worker-inference-runtime-hint.test.ts`, `worker-runtime-provenance.test.ts`, `nanohost-harness-records.test.ts`, `worker-recovery.test.ts`, `turn-executor-factory.test.ts` TAP `1..85` all ok, exit 0; `worker-governance-turn-executor.test.ts` TAP `1..106` all ok, exit 0.
- `pnpm --filter @openkit/nanocore typecheck` exit 0; `lint` checked 564 files, no fixes; `build` exit 0.
- `node scripts/validate-spec-lifecycle.mjs` exit 0; `node scripts/validate-doc-model.mjs` validated 216 documents, exit 0; `node scripts/generate-doc-index.mjs --check` index current, exit 0.

Full-suite green is not claimed. Generated schema tree matches `temp/research/codex-schema-0.153.4` (304 JSON files; no extra/missing names). Snapshot SHA-256 values match `packages/worker-shim/snapshots/codex-0.153.4/metadata.json`.

Local unique-tag image proof on Docker Engine 29.5.2 / linux/aarch64, Node `v24.18.0`, PATH `/Users/m5pro/.local/share/mise/shims`:

- Source-input aggregate SHA-256 `9f9786dd057bd2951e131738d6cca5908505e6909f19fb2e0c05b3b3505b6ac4` (`temp/research/image-proof-20260905/source-input-digests.json`).
- First `scripts/docker/build-image.sh worker-codex openkit/worker-codex:codex-pi-refresh-20260905` docker log completed naming/unpack of smoked id `sha256:2fdd17abc6d39b87227ee0a6dbbf1890d63949b909a3e9758f576f932fca161d` and recorded `codex-cli 0.153.4`. The zsh wrapper then used read-only `status` and aborted before capturing a process exit; that wrapper failure is not a captured docker build exit.
- Cached rerun of the same unique-tag command captured `codex_unique_tag_build_exit=0` in a task-specific variable, not zsh `status` (`startedAt=2026-09-05T07:50:20Z`, `finishedAt=2026-09-05T07:50:23Z`; `temp/research/image-proof-20260905/worker-codex-build-cached-rerun.exit.txt`). All layers were CACHED. BuildKit re-exported current tag id `sha256:bdcff050a506859966ef95373573444867dbe9d501a0a885f3ecb781f5119ef2`; RootFS layers, Created, and Size matched the smoked `2fdd17` image; the original `2fdd17` index digest is no longer named.
- `scripts/docker/smoke-image.sh worker-codex openkit/worker-codex:codex-pi-refresh-20260905` exit 0 against smoked id `sha256:2fdd17abc6d39b87227ee0a6dbbf1890d63949b909a3e9758f576f932fca161d`; log ends `OpenKit Codex worker image smoke OK` (`startedAt=2026-09-05T07:39:07Z`, `finishedAt=2026-09-05T07:39:09Z`). Smoke was not rerun after the cached re-export.
- `scripts/docker/build-image.sh worker-pi openkit/worker-pi:codex-pi-refresh-20260905` exit 0; image id `sha256:ba074c6f0caa0a52b9f3fd9ca0c87e6703f842f98966e0e506e1a8ad86a7b745`; build log recorded `0.85.0`.
- `scripts/docker/smoke-image.sh worker-pi openkit/worker-pi:codex-pi-refresh-20260905` exit 0; log ends `OpenKit Pi worker image smoke OK` (`startedAt=2026-09-05T07:39:55Z`, `finishedAt=2026-09-05T07:39:57Z`).
- Isolated native parse proof on `openkit/worker-codex:codex-pi-refresh-20260905`, `--network=none`, no provider credentials (`temp/research/codex-0.153.4-exec-parse-proof/`): `codex exec --help` documents `-C, --cd`; `codex exec resume --help` does not. First-turn full emitted argv with `--cd` parsed (`thread.started`, then network reconnect wait, timeout exit 124). Resume full emitted argv without `--cd` parsed (`no rollout found for thread id 00000000-0000-0000-0000-000000000000`, exit 1). Neither form reported `unexpected argument '--cd'`.
- Shared `:dev` tags remained absent. Smoke logs also printed `mise WARN  mise version 2026.9.1 available`; that is an upstream update notice, not a smoke failure. Unique-tag linux/aarch64 image smoke does not close the vendor re-pin obligation. Current integration gates remain unobserved.
- Accepted CI patch SHA-256 `55a4ef614943818ab61a72115b93360156eb8f2c69d5d761eee15ef60b1493c0` matched; `git apply --check` exit 0; apply clean to `.github/workflows/ci.yml`, `.github/workflows/README.md`, and `tests/release-workflow.test.mjs` only.
- Independent review `temp/changes/202609031230162442-r058_worker_mcp/candidate-review/codex-pi-refresh-ci-review.md`: Node 24 RED 7 pass / 1 fail then GREEN 8/8 plus read-only/`push: false`. Producer `red.log` is a missing-yaml Node 22 setup failure and is not RED.
- This integrated tree: `node --test tests/release-workflow.test.mjs` under Node `v24.18.0` / pnpm `10.33.3`: tests 8, pass 8, fail 0, exit 0. `git diff --check` exit 0. `pnpm exec biome check tests/release-workflow.test.mjs .github/workflows/README.md` checked 1 file, no fixes, exit 0.

Pi `0.85.1` patch-release verification on 2026-09-06, Node `v24.18.0`, pnpm `10.33.3`, PATH `/Users/m5pro/.local/share/mise/shims`, Docker Engine 29.5.2 linux/arm64:

- Official identities: npm `@earendil-works/pi-coding-agent@0.85.1` `latest`, tarball shasum `4cd00f653c3dabeb193b46f511044e7fbfe0f947`, `gitHead` `d981de1229ef899957bbe968bc8dcda02a21f477`; GitHub non-prerelease `v0.85.1` peels to the same commit. Complete non-shallow checkout `temp/research/earendil-works-pi-v0.85.1` `describe --tags --exact-match` = `v0.85.1`, shallow = `false`. Codex GitHub latest non-prerelease remains `rust-v0.153.4`.
- Consumed-contract inspection versus `0.85.0` commit `107d79f11072bbc8a3a757ed7fd69596bee7d68c`: `packages/coding-agent/docs/json.md`, `src/modes/print-mode.ts`, `src/core/agent-session.ts`, and `src/core/compaction/index.ts` are identical; `bin.pi` remains `dist/bundle/cli.js`; JSON-mode flags including `--no-approve` remain parsed; `anthropic` / `claude-sonnet-4-5` remains in the catalog. Upstream delta is GPT-6 Astra, TUI/mouse/keybinding fixes, unpublished experimental client/plugin dist, and GPT-5.6+ prompt-cache option, none consumed by this adapter.
- Focused `pnpm --filter @openkit/worker-shim exec vitest run --reporter=tap-flat src/adapters/pi.test.ts` TAP `1..23` all ok, exit 0.
- Initial focused `pnpm --filter @openkit/nanocore exec vitest run --reporter=tap-flat src/docker/openshell-worker-dockerfile.test.ts` TAP `1..18`: 17 ok, 1 not ok (`passes the manifest target through the release candidate build` expected 1 `matrix.target` match, observed 2). The primary inspected both real build jobs, worker smoke at `.github/workflows/ci.yml:651` and release candidate at line 777, and aligned this existing assertion and title with those two accepted consumers; no workflow behavior changed. The full existing `src/docker` slice then passed 35/35 across five files under pinned Node `v24.18.0` and pnpm `10.33.3`, retained in `temp/research/pi-0.85.1-docker-tests-node24.log`; focused Biome checked one file, no fixes, exit 0. An earlier 35/35 observation under unintended Node `v22.22.3` remains in `temp/research/pi-0.85.1-docker-tests.log` and is not the pinned-toolchain proof.
- `pnpm exec biome check packages/worker-shim/src/adapters/pi.ts apps/nanocore/src/docker/openshell-worker-dockerfile.test.ts` checked 2 files, no fixes, exit 0.
- `node scripts/validate-spec-lifecycle.mjs` exit 0; `node scripts/validate-doc-model.mjs` validated 216 documents, exit 0; `node scripts/generate-doc-index.mjs --check` index current, exit 0; `git diff --check` on the Pi slice files exit 0.
- `scripts/docker/build-image.sh worker-pi openkit/worker-pi:pi-0.85.1-refresh-20260906` `pi_unique_tag_build_exit=0` (`startedAt=2026-09-05T14:38:01Z`, `finishedAt=2026-09-05T14:38:38Z`); image id `sha256:6cd46bcc208092417082152cf022a1818674f21bdcc921b02d6424c8e60662de`; build log recorded native `0.85.1`.
- `scripts/docker/smoke-image.sh worker-pi openkit/worker-pi:pi-0.85.1-refresh-20260906` `pi_unique_tag_smoke_exit=0` (`startedAt=2026-09-05T14:38:48Z`, `finishedAt=2026-09-05T14:38:50Z`); log ends `OpenKit Pi worker image smoke OK`.
- Shared `:dev` tags remained absent. Smoke printed `mise WARN  mise version 2026.9.1 available`; that is an upstream update notice, not a smoke failure. Unique-tag local smoke does not close A2, Codex F1, public `interruptTurn`, or future Pi selectability obligations.

Post-PR integration checks on 2026-09-06:

- PR #4 main `f7cab191` merged without source conflicts as `095345bb006e67194dd3994cd694570c7f61e2de`. The initial automatic merge commit hook stopped under shell-default Node 22; the merge remained intact and completed under pinned Node `24.18.0` with an explicit conventional commit message. This setup failure is not test evidence.
- Dependency builds passed 7/7; Codex schema validation passed; existing workflow tests passed 9/9; Worker Protocol, Config Schema, and Worker Shim passed 365/365 combined. Worker package typechecks, NanoCore and Worker Shim builds, and all five affected package lints passed.
- The combined NanoCore slice initially passed 213/214. A newly merged continuity fixture requested old adapter version `0.144.1` against a persisted `0.153.4` row, so the production exact-version guard correctly rejected it. Aligning that fixture and the only other stale active capacity-fixture literal leaves every assertion unchanged; the same 214-test slice then passed. No runtime behavior or compatibility fallback changed.
- Logs are retained under shared-root `temp/changes/202609051702580059-codex_pi_stable_refresh/` as `post-merge-*.log`. These focused checks do not close the eight-image, three-leaf OpenShell, F1, or public-interrupt gates.

- Final candidate `545b3f48` independent integration review accepted the two fixture literals and conflict-free merge; an independent factory rerun passed 28/28, and the generated schema tree remained exact to `f51849a5`. PR CI `33986603146` passed. Four local amd64 canonical image builds/smokes, including the common derived image, passed; their inspect outputs confirm `linux/amd64`, retained under `temp/changes/202609051702580059-codex_pi_stable_refresh/local-amd64/`. A Verifier independently confirmed the release-CI placement correction; run `33986626729` completed with the image-specific result below.

- Final hosted run `33986626729` on `545b3f48` passed all eight required `smoke-worker-images` jobs; root inspected the job JSON and raw logs, including both-architecture native smoke markers. App recovery, L5 smoke, L3 NanoCore e2e, Workspace portability source/target, and installer jobs also passed. The overall workflow failed its unrelated L0–L2 job: four NanoHost source-structure tests could not locate the end-of-run marker; `apps/nanohost` is byte-identical to merged main `f7cab191`. No full-workflow green is claimed. Retained final result SHA is `b45b60215843f8ce2467f2cfc04ce15c0d82ab0318bf31ba6672af651487e4c8`; raw log SHA is `46b48cd597078567c781c7c5608446933a7d7b50f8aebd0b685cee6b10e30633`.
- Canonical A2 App and all four arm64 Worker builds/smokes passed, including the common derived image. Exact build log SHA is `0d84e9123d189ff795aa74696c647df553331dbb9166272381dc273ff582bd33`. Selected Codex native descriptor is `sha256:c7af94b11168f0bf5f911124d2654b902d0bce3960ae3bdae1dad89c84d5599d`; its canonical-member OCI archive SHA is `802c30e2690d357c264167b59c9ee14ec5db7a9adb7db10fe90ba36e6e02b3d1`. Intermediate OCI packaging attempts remain nonselected setup evidence; no runtime image or import adapter was changed to accommodate them. Stock OpenShell and F1/interruption observations remain pending.

- Final stock OpenShell three-leaf check passed on A2 using CLI/Gateway `0.0.99`: each exact Codex, OpenCode, and Pi image completed create, AEP upload, private package import, generic shim dry run, delete, and final exact-name absence. Root inspected the actual command and output; manifest SHA `860d705b392393c237250eb3657eac4dab771dbf6151d849c551bf5d1c34e696`, deciding output SHA `eaced9c919fdf3bfb6133cea2721e6415917ea07e96276848175f61053c8e39e`, retained under shared-root `temp/changes/202609051702580059-codex_pi_stable_refresh/stock-openshell/`. The CLI, public artifacts, and exact image cache remain as normal staging data.
- The first product-reaching F1 on `ws_9`/`th_8` failed before provider execution: OpenShell accepted `CreateSandbox`, then NanoHost exited with terminal `stage=execute operation=sandbox.create status=none`; the existing runner cleanup started another epoch that stopped on poll `409`, while NanoCore reported a result-only expectation conflict. The App remains healthy and the failed attempt's normal records are retained. F1 recovery and public interruption remain unproven; the exact current failure is being diagnosed before another run. Earlier connection and selected-Agent catalog failures are retained as setup failures, not model or Worker evidence.

- After the required-image correction, F1 on `ws_10`/`th_9` created the Worker Sandbox successfully, then its first Gateway call failed in 21 ms with `unsupported_gateway_feature` / `pi-ai Responses additional tools`; the Turn failed in 10.971 seconds and teardown succeeded. The canonical raw response proves the error but does not retain the native request body. The request prefix shape is a source-derived fact from the exact 0.153.4 checkout, not a fabricated runtime observation. Redacted receipt SHA is `54b0c6ebc52ae0400264dd10f5ae0947c32a9c72a5110a1ebbd788d844b2cca6`. The accepted unified-backend owner now defines the optional canonical item id and nested local custom representation; a focused stock-pi-ai integration regression fails on the old guard before provider access.

- **Gateway compatibility evidence:** The exact stock Pi serializer/SSE parser lifecycle test now proves reserved client-search call reconstruction, next-request discovered-tool activation, namespaced function/custom output, same-bare-name cross-namespace history, and native nullable reasoning preservation. The initial post-implementation test failure was an overly narrow test oracle and is not claimed as a product RED. Review found and corrected misplaced declaration admission and undeclared provider output; the independent Reviewer accepted the frozen 54/54 adapter result. Root additionally observed final 101/101 combined adapter, provider-dispatcher, and Worker-inference-route tests plus NanoCore build and schema metadata validation. Final independent acceptance includes the pinned optional message-phase follow-up; the adapter reports 56/56 and the stock lifecycle case 1/1, with typecheck and focused lint passing. Only consumed-surface metadata changed; the 304 generated schemas remain byte-identical. The final client SHA-256 is `cab09c69aab996a0829cf750b770ac09cc7146c154cf2599a433d8e92d4ecabb`; test SHA-256 is `8d644ee4c19d2aa6eb8be3309ecec3c98c5d2359c9f98f5f00797377cc7c24b0`. Raw output is retained under `temp/changes/202609051702580059-codex_pi_stable_refresh/gateway-compatibility/`; A2 provider acceptance is not inferred from mocked HTTP.

- **Exact Gateway candidate deployment:** PR CI `33992386291` passed for `a14a7b58`. Canonical A2 App build and smoke passed; build log SHA-256 is `f8e407965ddf0ed51d2867a5433db85e2b58918c3e48282c6f9d36d07ec1f894`. A following registry-only `imagetools` inspection failed for the unpublished local tag; local image inspection retained the successful build without rebuilding. App image/index is `c48bb53a5be454ad41a5ad8c122ac3a91164442991dc36f79f5d14637ed2b4a2`, native leaf `78d3c830eeed517bd2a716c76286da180bdff233534dc554ff71a27d5db5a009`, Web index `016acc02670db335291c38580f020f88400e89a9d3c511a1538a6fb4ba8b63a1`. A silent replacement wrapper first exited in an unidentified pre-mutation guard: Docker events prove no Run9 transition or rollback, so this is setup failure, not candidate App failure. The subsequent direct locked stop/remove/run passed, retaining the same data, credentials, configuration, ports, and original staging. Root independently verified machine `34b95b09f8c1475ea242800759411fea`, exact new Run9 image running/healthy, exact original `ba559...` image running/healthy, and active NanoHost. The deployment operator observed generation 8 ready/fenced/fresh-empty. F1 effect ownership then passed to the existing Unit F operator.
- **Test-only closeout delta:** After executable `a14a7b58` was frozen, the builder added one 18-line stock-test assertion for streamed native search reconstruction alongside the existing nonstream case. Root inspected the exact test-only diff; it reports 56/56 and 1/1, and executable client bytes remain `cab09c69...`. Retain this uncommitted test delta for the final evidence closeout; it does not require an App rebuild.

- **Resume direction:** After primary compaction, the independent Verifier read the current intent, Git state, and exact F1 failure receipt and returned `Continue`: source diagnosis is the smallest belief-changing action; only a bounded redacted diagnostic at the existing seam is justified if those bytes cannot identify the cause. Public interruption and ordered PR5, OKF, then final preservation/cleanup remain pending.

- **Namespace reframe and valid regression:** Exact Codex source equates absent, empty, and `functions` namespaces; stock Pi emits an added tool item before its optional namespace arrives at completion. The existing adapter rejected that valid start before the parser could finish it. Root inspected `/tmp/codex-01534-default-namespace-red.log`: the stock-parser case reaches `responsesToolCallItem` and fails with `pi-ai Responses undeclared provider tool output` (1 failed, 55 filtered), so this is a valid regression rather than setup failure. The independent Reviewer accepted the narrow owning-spec and local-guide clarification. Correct canonical lookup/collision/history and defer incomplete stream identity at the existing projection; no new transport, parser, diagnostic framework, or live retry is needed to establish this source defect. Runtime attribution of the earlier ws11 failure remains unproven until a corrected candidate is exercised.

- **Combined-candidate method:** The independent Verifier found no accepted owner requiring isolated PR5-only F1: the vendor repin rule binds consuming gates and exact Worker identity, and the NanoHost scenario binds observed lineage and recovery. One reviewed final candidate therefore supplies the required evidence while avoiding repeated intermediate builds and deployments. Local composition does not change the required PR5-before-PR6 GitHub merge order. Receipts must bind final commit/tree and actual App, Worker, and NanoHost identities; final main must match the accepted candidate, with any intervening resolution or closeout delta checked proportionately. Preserve both normal A2 data roots and original staging; apply the already-reviewed schema maintenance only to Run9.

- **Oracle correction:** Temporarily disabling only missing-namespace deferral while keeping canonical lookup left the initial whole-body stock fixture green (`/tmp/codex-01534-missing-namespace-mutation.log`). Pi had time to mutate its shared event block before consumption, so that fixture did not independently prove the late-namespace branch. Keep the existing case and separate the added and done HTTP chunks with a native event-loop yield, then require this mutation to fail before accepting deferral coverage.

- **Final namespace proof:** The corrected existing stock HTTP fixture separates added/delta from completed namespace with one native event-loop yield and handles cancellation locally. Disabling only deferral now produces the intended undeclared-output failure with no unhandled fixture error; restoring production passes the same case. Root observed the final adapter, Worker-inference-route, and dispatcher slice pass 102/102, NanoCore typecheck exit 0, focused Biome 3 files with no fixes, and diff check exit 0. Client SHA-256 is `7efa748b81b949d579f85191f27d968867ec875e85b78f66eabe2a6f6acafad8`; test is `e5da6d5d3b8ad3e3ab60ca4670cc1a47ef844bd441e06615bf29355bffe13719`; policy is `1f5ec0e3f92f630f49b2cca54352459f1d59212958d209910ce4dd45b7389d03`. Root retained the deciding RED, refuted whole-body mutation, clean split mutation RED, focused GREEN, final 102-test log, and typecheck under shared-root `temp/changes/202609051702580059-codex_pi_stable_refresh/gateway-compatibility/`.

- **Independent namespace acceptance:** Final review accepted the six-file slice and returned `Continue` for the combined-candidate method. Its remaining oracle finding was corrected in the existing test with exact deferred delta and done arguments plus call/item lineage assertions; the final adapter run passes 57/57. No source, owner, guide, test, or checkpoint finding remains. Real A2/provider acceptance is still pending and the earlier ws11 EOF is not attributed from mocked HTTP.

- **Combined deployment direction:** After primary compaction, the fresh Verifier inspected current intent, owners, Git, migration preconditions, and final build receipts and returned `Continue`. The remaining Web test-concurrency failure blocks final merge but not the authorized stopped-App Run9 migration and staging deployment. Final combined acceptance includes Codex Unit F F1-F4 plus Aggregate from the retained NanoHost/R005 surface, the separately required public Codex interrupt, OpenCode real-provider success and restart/reconnect interrupt, OKF migration and Knowledge normal use, and stock OpenShell probes for all three rebuilt leaf images. Earlier leaf results do not transfer across changed Worker inputs. A2 effect ownership passes exclusively to the existing deployment operator and then to the existing real-case operator; original staging and provider credentials stay retained.

- **Web gate correction:** The hosted L0-L2 result contains 12 Web failures: ten reach the unchanged five-second limit, while two subsequent tests observe input operations left running by timed-out predecessors. Isolated failing cases complete in under 600 ms. The existing Vitest configuration now runs Web files sequentially inside the already parallel monorepo gate; no timeout or assertion changed. Root inspected the one-line test-only diff and retained output: all 857 Web tests, Web production build, focused Biome, repository check, and diff check pass. The hosted L0-L2 run remains the deciding complete correction observation. Logs are retained under shared-root `temp/changes/202609031230162442-r058_worker_mcp/final-preservation/combined-ci-web-*.log`; the original failed CI log remains `combined-ci-l0-l2.log`. This Vitest-only setting does not change the deployed App or Worker runtime.

- **Complete Linux unit-gate findings:** Run `33996895863` has no Web failure and reveals 37 NanoCore failures. Thirty-three fixtures use the differently owned CI checkout as writable Git input; the production child environment deliberately omits the workflow-only `safe.directory` variables, so Git rejects that checkout. Reuse the existing process-owned Git fixture seeder without weakening production trust. Two public Worker MCP cases still send old outer control/Harness version 1 and one Vault auth fixture omits Core migrations; their existing fixture corrections pass 26/26. The remaining phase-order failure is a production mismatch: the entrypoint adds a ninth phase while the bootstrap owner requires eight. Independent owner review accepted folding the unchanged deletion-fence reconstruction into critical phase 8 before NanoHost fencing; the unchanged eight-name oracle plus ordering assertion and deletion routes pass 21/21. This source correction requires one App-only build before the real campaign; Worker and NanoHost inputs are unchanged. Root inspected actual diffs and logs, and NanoCore typecheck passes. Raw baseline and corrected results remain under shared-root final-preservation `combined-ci-*.log`.

- **Final unit-gate correction evidence:** Root inspected the three-file Git fixture diff and retained 187/187 cohort output, plus NanoCore lint across 564 files and typecheck success. The independent Reviewer accepted the actual two-file boot correction after inspecting its exact failure ordering and 21/21 result. Together with the 26/26 MCP/Vault correction, all 37 named failures have a local correction and direct focused evidence; the next hosted L0-L2 run must establish the complete gate. Build only the changed App entrypoint from the next pushed candidate, reuse the byte-identical 6bf Worker and NanoHost artifacts, and replace Run9 through its existing retained-data deployment operation before any real Worker attempt. No migration rerun or credential/config reset is required.

- **Web execution-budget reframe:** Run `33997777836` refuted the sufficiency of file serialization: four aggregate five-second timeouts and two subsequent userEvent cascades remain, while NanoCore and 17 of 18 package tasks pass. Direct owner inspection confirms that five seconds is Vitest's implicit execution budget, not an accepted Web behavior or latency predicate. The existing Vitest configuration now sets an explicit finite ten-second aggregate budget; all semantic assertions, `waitFor` and `findBy` deadlines, and user interaction paths remain unchanged. Root inspected the one-line diff, the retained causal diagnosis, all 857 passing Web tests, focused Biome, and repository-check output. The exact production remains the deployed `0a528186` artifacts; this test-only correction requires no App or Worker rebuild. The next complete hosted L0-L2 result remains pending.

- **Second blocked-create fixture correction:** Attempt 4 retained F1 success and Aggregate FAIL because F2 never reached its accepted-create fault predicate. Restarting the App before the existing fresh NanoHost preparation drops only process-local continuity; the existing forced whole-Sandbox retirement path then handles the physically absent predecessor without queuing `session.close`. The expected connection generation is re-read after that normal App restart, and F3 static-unit rejection still precedes every effect. The deciding RED and pinned Node 24.18.0 86/86 GREEN are retained in `temp/changes/202609051702580059-codex_pi_stable_refresh/a2-runtime/unit-f-core-continuity-red.log` and `unit-f-core-continuity-green-node24.log`; the source-backed rationale is appended to the existing instrument readout. No runtime image or product behavior changed.

- **F1 terminal oracle correction:** Attempt 5 exposed that a non-null failed terminal status could pass the original F1 adjudicator. The existing condition now requires `completed`; the existing mutation table rejects a failed terminal while preserving every lineage, sequence, projection, and cleanup predicate. The deciding RED and Node 24.18.0 87/87 GREEN are retained as `a2-runtime/unit-f-terminal-status-red.log` (SHA-256 `35eb5f047e1d7d3144c6c483dabc83639353c2c409051b55de6d5f68897fcc8a`) and `a2-runtime/unit-f-terminal-status-green-node24.log` (SHA-256 `d595691ab3d378d0d013413bd52fe1234704369dcdbd2a8e1f2cc0f311a0f173`). Independent review accepted the actual two-file diff and named output; focused Biome and diff check pass. This instrument-only correction requires no image rebuild.

- **Isolated F1 and differential limits:** The unchanged runtime on runner `54cbd8e171012af8fc0765b507853781ad053224110f439f5b7e7f95eab13be7` failed isolated F1 attempt 6; result `a2-runtime/unit-f-0a528186-f1-isolated-attempt6.json` has SHA-256 `c45847ef0896edb4c744523a5c34b9cb842df2093971b89cd1f21ba3839bd1c6`. A preparing checkpoint with no worker session also occurs in successful F1 and its boot diagnostic has no terminal side effect, so it is not the SSE cause. Source review found a possible completed-item ordering mismatch, but the reasoning probe observed matching arrival and terminal order. Its replay rejection of `input[2].status` is a probe fidelity failure: native Codex drops that field when serializing reasoning, while the probe copied raw completed items. Neither observation authorizes a production fix or explains the actual failure. No production source changed. A2 recovered normally to generation 26 ready/fenced/fresh-empty with the failed backend cleaned; normal services and data remain retained.

- **Capture admission and instrument reframe:** Diagnostic attempt 7 loaded the patched module before Task creation, then the real Task completed with all nine inference calls succeeded, native exit 0, accepted final completion, and cleaned backend/released lease. The Gateway diagnostic was correctly empty. Its wrapper returned FAIL only after final owner reads but discarded the deciding Unit F error and snapshots, so this is product success with an unclassified proof failure, not another observed SSE failure. Original Gateway bytes were restored and the App restarted healthy at generation 29. Root now owns A2 effects. The existing invoker is locally corrected to retain repository-owned error constants and the existing bounded owner observations on failure. The next single diagnostic invocation changes that missing evidence: either the Gateway structural error is captured or the complete owner snapshots identify the precise final assertion. No new runner or effect domain is introduced; original compiled bytes and normal services are restored afterward.

- **F1 sampling correction:** Attempt 8 completed the real Task at sequence 213 with exact lineage, projections, teardown, and capacity. Independent recomputation found only the pre-Task epoch-member preservation clause failed. The journal proves the prior sandbox shim was deleted at 04:11:24 UTC and the current F1 shim started at 04:11:25, before the durable barrier at 04:11:34. The runner now refreshes its epoch baseline after that barrier and before the NanoCore fault, retaining the original capture for unresolved-lineage cleanup and retaining every exact membership assertion. The existing regression is RED with `F1 continuation proof failed` and GREEN with 88/88 tests; focused Biome and diff checks pass. Runner SHA-256 is `554a9f6a89c830e82bd3c3ecc8fb0fe78045b312be275141da6b2f597071b56e`. Diagnostic Gateway bytes were restored to SHA-256 `3b6fe46a124c3ed42642a611903a3c75ca06424e7a57b86561df2741d3fd6470`, the App restarted healthy, and the diagnostic log is empty. The next real F1 uses the unchanged product image and corrected sampling; its result must preserve the full deciding observations. No production patch or image rebuild is justified.

- **Refresh-gate reframe after F1 acceptance:** Independent exact-result review accepts F1 attempt 9, SHA-256 `0e02678943b2b70e526bdf217b32c0fa4e3dcafcc3c01697cc8c58c3cf54b97b`: generation 32 to 33, identical post-barrier epoch members and exact session lineage, completed sequence 217, singular teardown, released lease, cleaned backend, and fresh-empty readiness. Fresh direction inspection reconciled the affected Codex and image owners with the accepted four-gate classification above. Later F2-F4 Aggregate, separate normal-lifecycle, and OpenCode additions were agent method expansion, not an engineer requirement or affected-owner refresh gate. They are removed from this refresh method; historical failures remain FAIL and generic NanoHost release obligations remain unclaimed. The sole remaining runtime check is one public Codex interrupt after readiness and active inference on the same image, proving interrupted state and cleanup through existing App and storage owners. After that observation, resolve the stacked squash integration conflicts to preserve the reviewed combined tree, then perform the already authorized finite Git cleanup.

- **Public interruption correction:** A2 `ws_24` / `th_25` reached Worker readiness and successful inference, then accepted native `interrupted` / `aborted` at 2026-09-06T05:00:53.759Z while Core failed with `NanoHost Harness producer already has an unsettled operation.` The receipt remains FAIL, SHA-256 `729d7634ef8e667073bb85e6ff4f006812205e1aa7b45d473ef1d248555b4980`. Source tracing found terminal inspection could overtake the public interrupt result; the human-Gate path already joined that settlement. Test commit `df28b18a` records terminal acceptance while the interrupt result is pending and is RED only for public interruption. Correction `69fcd571b0ec9612249d43b1022fa480daf0e804` reuses that process-local settlement for both purposes and introduces no durable state, queue, retry, operation, or cross-domain owner. Independent review accepts the exact diff and F1 transfer because ordinary completion still awaits a null field. The complete factory file passes 30/30; NanoCore typecheck, 564-file lint, and build pass. Full NanoCore execution reports 2645 passes and the one known macOS `/proc/self/fd` failure; that storage test and implementation are byte-identical to the accepted complete Linux run `34001207497`, so its Linux evidence transfers without weakening the check. Only the corrected App build and real public interrupt remain.

- **Canonical interruption mapping correction:** Corrected App `69fcd571` delivered the public interrupt on `ws_24` / `th_26` and recorded native `interrupted` / `aborted`, released the exact lease, cleaned the backend, removed all runtime bindings, and retained generation 35 ready/fenced/fresh-empty. The original command receipt remains FAIL because its immediate-HTTP-state assertion ran before asynchronous closeout. Subsequent exact-lineage reads exposed a separate real defect: Core persisted Turn `cancelled`. Core protocol interruption semantics, reliability-envelope terminal mapping, and the Task Mode state table require Turn `interrupted`; only the Task outcome is `cancelled`. Independent owner inspection rejected that product result. The existing two restart table cases are RED with expected `interrupted`, received `cancelled`; the local correction changes the existing shared mapping and online closeout, explicitly preserves Task `aborted` to `cancelled`, and removes an unreachable recovery comparison. Independent review accepts correction `ec9490e6a6d53f2b9d19e968754f754938b98c9e`; F1 transfers because its `completed` path is unchanged. Four focused suites pass 289/289 and both amended terminal/AgentSession assertions pass; typecheck, lint, build, and full NanoCore verification pass apart from the unchanged macOS `/proc/self/fd` check already proved on Linux. No new state or status migration is introduced; prior failed records remain normal staging history.

### Historical Pre-Closeout Checkpoint

- **Source:** PR #4 is merged as `f7cab19136f30fe928c84995ca303eace7377c4a`. PR #5 is `80e23a155aa69f670bf831adc50027724474339a`; PR #6 is `240e4e2c`. Combined PR #7 runtime source is `0a528186432db2089ee90e59f5772421a7fe23b6`, tree `2ff50acb1b1de5c25c27cc7d34871ec146032a9a`. Codex is `0.153.4`, Pi is `0.85.1` and disabled, and OpenCode remains `1.18.1`.
- **Accepted artifacts:** Run9 uses the canonical `0a528186` App with index `sha256:278d292b77f9a0e2dd6d9665670c697a0af4abf265b03c62b2229d728f0f27ba` and native leaf `sha256:6faedba86b4e110df50d633c18347732b05feb13185152e266af1ce1d9a8e185`. Its Web hash remains `f06f58101c88fb50467f0e8c6e2bc04775b0bd3a8bf34d78a8161173cad23327`. Byte-identical Worker and NanoHost inputs reuse the `6bf9bfbc` builds: Codex leaf `8bc5033350edebe85293e8528452c09ac78637ea55915bed7878cdc3b72387a9`, OpenCode leaf `08c773d10421edbbbbc65c28c13cab05cb5841fac75819f71172f6f34c75b823`, and NanoHost executable `d6447b87be0ea7ab7f0c0224249c928d5b1ad8091e8e6fa4098bd6ff747667b1`. Root inspected actual build, smoke, replacement, readiness, and three-leaf stock OpenShell logs. The original staging App remains healthy.
- **Current runtime evidence:** Run9 OKF migration and normal use, final Worker/platform and stock OpenShell checks, and complete Linux L0-L2 remain accepted. Attempt 4 F1 completed the same real-provider lineage after App restart and cleaned up; its Aggregate remains FAIL because F2 never reached accepted-create. Attempt 5 reached F2 accepted-create after the fixture correction, but its preceding F1 actually failed: six post-restart inference streams ended before `response.completed`, and the old non-null terminal oracle incorrectly accepted a failed Turn. Attempt 5 remains FAIL. Its result and diagnosis are retained under `temp/changes/202609051702580059-codex_pi_stable_refresh/a2-runtime/` as `unit-f-0a528186-attempt5.json` (SHA-256 `dc6dbab82890921f1cf75a0d99f3caba4f0610a2d4e70141c9518a509b8db4a7`) and `unit-f-0a528186-attempt5-diagnosis.md`. The underlying provider error and exact failed request were not retained, so neither invalid credentials nor a transient upstream failure is established. Two bounded existing-client probes using the deployed Pi client and current subscription pass both baseline and continuation; the second replays full returned tool history and the captured opaque Codex turn state without persisting the state. F2 reached the expected generation-23 first-poll `sandbox.create` 409 fence but did not prove the absence needed for the next coordinator. Classify its actual live effects and observer before correcting or recovering it. Earlier one-time stale-marker and projection recoveries retain their evidence; their general automation gaps remain deferred.
- **Current CI evidence:** All eight final Worker/platform jobs, final three-leaf stock OpenShell probes, and `0a528186` hosted smoke run `33997779252` pass. Hosted L0-L2 run `33998689855` on `ae07e3bb` passes all 18 Turbo test tasks, including all 857 Web cases, plus lint and typecheck. The explicit ten-second aggregate Web budget preserves all assertions and actual wait deadlines. The gate then detects a stale generated Skill CLI, before root tests run. Canonical `pnpm -w bundle:openkit` regenerates exactly the hosted expected Git blob `21aa1e39dbbf60208470b497cdcb625f4fdde7d7`; independent regeneration is byte-identical and the existing CLI interface suite passes 17/17. CLI SHA-256 is `a3aa45c1dde2aa6723852b81dc87e1f1b43b4d64ce96bd6d2d1c47490bd18247`. This generated release artifact is outside the App runtime copy surface and needs no App or Worker rebuild. The complete Linux L0-L2 gate `34001207497` on `b2d0fb562ae37920f98579e9864e4c3d6d31ec14` passed on 2026-09-06: all 12 lint tasks, 15 typecheck tasks, 18 Turbo test tasks, all 502 root tests, all nine configured coverage suites, and all 10 build tasks pass; the installer gate also passes. Root inspected the named full log, retained at `temp/changes/202609031230162442-r058_worker_mcp/final-preservation/combined-ci-l0-l2-b2d0fb56.log`, SHA-256 `d39d31b55ee53853d44d300357cbdd12dac43c3507a06d7869f4b9bb33dbd59b`. The full job took 22 minutes 34 seconds; coverage and build ran after the unit boundary that had stopped previous attempts. This closes the current complete Linux gate without transferring PASS to earlier failed runs.
- **Next action:** Complete the single remaining public Codex interrupt on a normally ready A2 deployment, retain exact readiness, interrupted Turn, same-lease/backend cleanup and capacity observations, then merge PR5, PR6, and PR7 with final-tree preservation proof and perform the finite approved Git cleanup. F1 attempt 9 and complete Linux CI are accepted; generic NanoHost fault scenarios are outside the refresh gate and remain unclaimed.
- **Runtime method reframe:** Fresh independent owner inspection confirms that Required Scenarios 1-4 and their acceptance predicates require the named observations, not one invocation, fixed ordering, common runner digest, or a named Aggregate PASS. The earlier Aggregate wording records the selected method, not an additional engineer requirement. Use existing per-scenario driver exports with exact candidate identities, baselines, complete raw observations, and independent acceptance. Keep every failed Aggregate as FAIL; never splice prior evidence into a synthetic Aggregate or weaken its assertions. Prior observations transfer only while their deciding product path remains unchanged and no unresolved contradictory failure affects the same predicate. After primary compaction the fresh direction check returned Continue for bounded causal probes, local correction, and this per-scenario route.
- **Integration method correction:** The earlier Intent Epoch 9 method bullet incorrectly claimed that no repository merge-method rule exists. Direct re-reading of `CONTRIBUTING.md` found the explicit squash-or-rebase instruction. Use ordered GitHub squash merges for PR5, PR6, and PR7, retargeting each successor to `main`; prove each prospective merge tree and final-main content against the accepted candidate. Preserve source/stash receiver content by the existing exact diff and tree evidence rather than claiming receiver commit ancestry after squash. This corrects execution method without changing engineer intent.
- **Retained staging and ordering:** Keep both data roots, users, configuration, credentials, subscriptions, and services. Only Run9 is the upgraded target; original `~/.openkit` and its v1 App remain unchanged. The existing Worker Task route exposes no reasoning-effort override: host actors/judges use Luna-low where supported, while native Codex keeps its previously disclosed default medium. Finish required runtime and CI observations before PR5, PR6, then PR7 integrations, preserve intended work and ignored evidence, then remove development refs and linked worktrees while keeping canonical main checkouts.


### Final Runtime Acceptance

The final public interruption on `ws_24` / `th_27` passed using normal existing credentials and configuration. Root inspected the complete same-lineage receipt with successful inference and readiness before interruption, accepted native `interrupted` / `aborted`, eventual Core Turn `interrupted`, Task state `cancelled`, lease `released` with reason `turn-interrupted`, same backend `cleaned`, and generation 36 ready/fenced/fresh-empty. Receipt SHA-256 is `40ed7f8be04812ababededfa23102136426f4602a418419ec3b92808c94f5691`. App build and canonical smoke passed; replacement receipt SHA-256 is `b7041bbcaf8872e96dab96759a72400c322d5c2efc64a5cfb58bf372e6eabad3`. No further runtime test or product correction is required for this refresh.

## Closeout

PR #5 merged as `68f096eab48b405d098fbb89d9a127e0b6c75ac9`, PR #6 as `adef9dccd79d6ca89744b7c049d4c06ee2c51dd7`, and PR #7 as `6acf1c5a2d720e8ac6c4a8a82ae25cdae9ce8dca`. Every expected squash parent/tree was verified; final merged tree `a2cc06e45368ee4b29ef147a5b228d23a006e16d` is byte-identical to independently accepted candidate `aa3d5abcc817af1c0479400014976c7adc422934`. The frozen receiver map proves preservation of the shared-root, Phase1, old-CI, and 43-path stash content without relying on post-squash ancestry.

All OpenKit PRs are resolved. Six local auxiliary worktrees, eight local development branches, five GitHub development branches, and 29 auxiliary A2 Git checkouts were removed after preservation and dependency checks. Local and GitHub retain only `main`; A2 retains only canonical `/home/ubuntu/openkit/source` on `main`. The exact already-integrated stash was dropped last. Four local evidence mirrors, all A2 evidence-bearing parents, normal staging data, schema history, users, credentials, subscriptions, CLI homes, images, and running services remain. Exact receipts and the preserved receiver map are under canonical `temp/changes/202609031230162442-r058_worker_mcp/final-preservation/` and `final-git-inventory.md`. This final documentation update records completed effects and changes no runtime bytes. Later roadmap features remain paused.
