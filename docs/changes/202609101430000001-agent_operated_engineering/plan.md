---
type: change-plan
status: in-progress
date: "2026-09-10"
---
# Agent-Operated Engineering

## Intent Epoch 1

On 2026-09-10 the engineer clarifies the operating goal: use OpenKit itself for its ongoing development and maintenance, principally through Web-directed Agents, with repository engineering principles, independent verification, tests and acceptance usable through that platform. Desktop Agents use the OpenKit Skill and separately authorized SSH for exceptional work. NanoCore and Web are the routine release-or-exact-commit build/update targets; NanoHost maintenance is explicitly separate and must not become part of ordinary App updates. Agent-usable installation, configuration, upgrade, diagnosis and recovery guidance should be independently distributable as a Skill rather than parallel user manuals. Provider model parameters must be optionally authorable using the models.dev declaration format, including limits, modalities and pricing. The engineer authorizes implementing settled feasible parts now through Cursor Agents and permits the primary and independent Claude Consultant to settle implementation details; unresolved user choices remain discussion topics. Primary personally writes canonical Markdown, retains Product Vision unchanged and uses independent review/audit for consequential artifacts.

## Intent Epoch 2

The engineer confirms that dual-instance operation is optional, not a prerequisite. Existing A2 Web and Desktop Agent plus Skill paths should carry most scenarios. SSH alias `a1` is available only if a task genuinely needs an additional machine; no mandatory staging deployment or NanoHost maintenance is implied. Preserve separately owned services and data, and verify actual host identities before treating aliases as isolated environments.

## Intent Epoch 3

The engineer requires one model parameter: a maximum context length so context compression can use a known bound. Prices, reasoning-intensity options, modalities and other metadata remain optional. A positive context limit inherited from the pinned catalog satisfies that requirement; an uncatalogued model without one must declare it explicitly. This supersedes the earlier admission of a model with no known context limit, not exact native-ID support. Adapter operating defaults are not sourced physical limits.

## Intent Epoch 4

The engineer offers another server for optional staging in response to the earlier placement question. Its SSH identity is not yet supplied or equated with the previously authorized `a1`; confirm the named destination before any effect. This offer does not make dual-instance operation a prerequisite or replace the A2 Web and Desktop Skill paths.

## Intent Epoch 5

The engineer identifies the offered optional staging server as SSH alias `a1`. Read-only qualification may now use that alias; compare its destination with A2 before treating it as an independent machine. The existing two main paths remain the immediate delivery target.

## Intent Epoch 6

The engineer states that SSH a1 was a test/development server, all of its data may be deleted or overwritten, and requests complete cleanup of its accumulated temporary files. This authorizes identifying and removing A1 legacy test deployments, temporary artifacts and build caches after exact host/path identification. It does not authorize changing A2 or destroying the operating system and SSH access. Cursor `live-deployment` owns the bounded host cleanup; source/document writers remain unchanged.

## Intent Epoch 7

The engineer declares `400k` as the maximum context for OrcaRouter `deepseek/deepseek-v4-flash-free`: configure `modelMetadata["deepseek/deepseek-v4-flash-free"].limit.context` as 400000 tokens. This is the operator-authored ceiling, not a Provider-discovery claim. Apply it with the new parser during coordinated App activation; other optional metadata remains omitted unless sourced or requested.

## Intent Epoch 8

The engineer changes the maximum context for OrcaRouter `deepseek/deepseek-v4-flash-free` to `111k`: the current operator-authored `limit.context` is 111000 tokens, superseding Epoch 7's 400000. Update and revalidate the staged configuration before activation; preserve earlier execution evidence as historical observations.

## Owners

Existing [persistent-deployment acceptance](../../specs/20260909-persistent_deployment_acceptance.md), [test strategy](../../specs/20260529-test_strategy.md)/L6, workflow, policy, Artifact/Evidence/Audit/Usage and [repository execution](../../change-execution.md) contracts retain authority. Gateway, Provider configuration and pi-ai backend owners define the model-parameter extension. The accepted [App-update delivery](../../specs/20260910-app_update_delivery.md) and [operations Skill](../../specs/20260910-agent_operator_skill.md) specifications own the two new delivery seams. Release management retains publication and distribution identity; it does not own deployment effects.

## Checkpoint

Current A2 App/Web is exact source `0620e228ae148ca0fd7779f1094c1729ffe569ed`, image `sha256:dc31459e32bf610054f3fcadff230c403b18ac89d452fb344cebe81ea54e100c`, boot `boot_55bff192-4a39-43e0-a046-ccd22c3bb1b3`, after the successful 09:34 UTC bootstrap. The live OrcaRouter configuration matches the validated 111000-context stage and remains the default; earlier probes authenticated Codex and completed inference on all three requested logical models. NanoHost remains PID 1263840 with no service restarts, ready on authoritative connection generation 10. Retained Workspace reads succeed. These are setup and host-continuity facts, not a completed Web engineering or public-update acceptance. The older persistent plan remains open at that frontier. Telemetry exports explicit HTTP response-handoff spans and process diagnostics; product records provide additional evidence but do not automatically create a self-maintenance loop.

Primary owns all Markdown. Cursor `live-builder` owns model metadata and runtime-configuration corrections; the operations package slice is committed as `e36c1e95`. Cursor `app-update-builder` owns App API/client/Skill operations and the Web update surface; `helper-correction` exclusively owns the Python host helper and its tests after `app-host-helper` explicitly released both paths. Their exact writable paths are assigned independently; App composition belongs to `app-update-builder`, and runtime configuration belongs to `live-builder`. Cursor `live-deployment` owns authorized host work, while `live-tester` owns the dedicated A2 engineering fixture and the separate function-tool bridge regression. Independent Claude `live-resume-direction` examines the scheme and direction; Cursor `live-review` inspects implementation. The fresh operations Skill Actor's completed read-only task remains bounded evidence. No second instance, NanoHost maintenance, new daemon or evaluation database is implied by this checkpoint.

The three terminal conversation-owned Task checkpoints still produce a nonblocking diagnostic on `0620e228`; their exact owner-dispatch correction is independently accepted and committed as `90397b26ea38ebe830745b37b9fef6a98349f102`, the next public-update source. No manual checkpoint deletion or retry is authorized by this correction. The App-update readiness and NanoHost successor amendment is accepted and committed as `35c5dea3`. Helper implementation and its three fail-closed corrections are independently accepted and committed as `e34cf49c`; its two-file installer is independently accepted but not yet executed. The isolated A1 lifetime/concurrency/failed-candidate restoration proof passes on frozen helper `cb736216`; the current helper passes 51 focused tests, and its corrected live fixture passes six independent local checks. The packaged Desktop Actor reads real authenticated diagnostics and NanoHost state; the Web member session is ready for the bounded engineering Task, with its optional administrator grant unsent. Neither real Web engineering output nor public App replacement is yet proved.

The engineering delivery frontier is a repeatable Web-started change with independent evidence and a bounded NanoCore/Web update that survives target process replacement, plus Desktop/Skill diagnosis and recovery. Reuse existing records and host supervision. Determine the smallest missing public/tool integration from actual attempts rather than coding this repository's whole governance procedure into Core.

## Acceptance

- Provider declarations preserve models.dev naming/units and exact native-ID admission with a known context limit, drive existing capability/adapter consumers, validate invalid inputs and retain explicit zero/false values without changing auth or public identity redaction.
- Independently distributable operations guidance and executable support cover actual supported install, configuration, update and recovery paths without a running NanoCore prerequisite or a second independently maintained manual corpus.
- A user can direct a real bounded engineering task through the deployed Web, inspect exact output and independent verification, and give any required human decision through existing product records.
- Normal tests run in the owning Worker environment; full real-use acceptance uses the existing persistent paths. Container-owning or destructive work uses a separately authorized effect domain without weakening sandbox containment.
- A release/exact-commit App/Web update has reviewable target identity, bounded authorization, preserved data/credentials, a process-independent execution path, observed post-update health/identity and inspectable failure. No automatic NanoHost restart or assumed database rollback is introduced.
- Desktop Agent plus packaged Skill can operate the same product and use separate authorized host tools for recovery. Optional second-host support is not a prerequisite or an HA/state-replication claim.

## Evidence

Independent review accepts the final live fixture `5755ea554eebf004fbd1e7d96f7af83b6338efe05f49a56296268db14ba801fd`: its HTTP responses implement the public seven-subsystem boot shape and typed no-target response. Six local checks pass in 3.295 seconds, including a parser-valid blocking candidate and invalid empty-subsystem control (`temp/ops-packager/fixture-boot-shape-review/review.md`). No new A1 execution is claimed or needed for this fixture correction. Independent installer review accepts `48f715d218e3c76c0e55fae6adaaf855d00d2194ea0c11927edfbd44380c4533`, helper payload `ebea447b` and configuration `ac91e5d9`, with syntax, execution guard and actual loader checks (`temp/live-review/install-e34cf49c/review.md`); host installation remains the next effect.

Independent review closes helper findings F1–F3 and accepts helper digest `ebea447bd97dabee9cb0db041f0a9a385cf21cf583648f81c90c3c4e8e7899fc` with 51 passing tests in 22.078 seconds (`temp/ops-packager/helper-readiness-review/review.md`). Only the actual typed no-target 404 can establish NanoHost absence; unavailable responses and malformed Boolean readiness refuse before stop. Degraded states require named reasons. The existing failed-candidate restoration lifecycle remains covered by the preserved A1 run. The fixture's remaining incomplete boot projection is being aligned separately before any future host run; it does not justify weakening the production parser. Selected-host installation and public update acceptance remain pending.

Bootstrap `0ae4527e` executes once at 09:34 UTC and reports `APP_REPLACE_OK`, exit 0, with actual image `dc31459e`, new boot `55bff192`, matching Web assets, retained `ws_3` ownership, NanoHost PID 1263840 and successor generation 10. Post-observation confirms the live OrcaRouter file digest equals the validated 111000 stage; the helper remains absent. The stopped previous App, old backups/logs and failed candidate remain retained. Evidence is `temp/live-deployment/activate-0620e228/replace-app-0620e228ae148ca0fd7779f1094c1729ffe569ed-resume-0ae4527e-20260910T093403Z.execute.out.txt` and its `.post-observe.txt` companion. This is a successful bootstrap, not a public helper update.

Independent review accepts resumed bootstrap `0ae4527eab6a8ec9e76567529c0fd621533d05058574e293bf529a9e5fab4ae6` after refusing its predecessor's leftover consumed-Web-path reference before any host effect (`temp/live-review/resume-0620e228-0ae4527e/review.md`). Controls execute the script's exact extracted readiness code: empty blocking sets pass; malformed, critical-failed, blocking and same-boot observations fail. The next authorized A2 action runs these frozen bytes once after current-host and no-active-work checks, using the already proved image/Web/configuration identities and unique backup paths. Expected evidence is the actual `0620e228` boot with effective context 111000, retained Workspace identity and unchanged NanoHost process on a greater ready connection generation. A preflight failure is retained; no blind retry or public-helper completion is implied.

Independent review accepts the existing Task checkpoint classifier's conversation-owner branch (`temp/ops-packager/checkpoint-root-review/review.md`). The exact `conversation.submit` receipt is matched on actor, request, input hash, receiving Thread and Turn before the direct `task.start` identity check. Five focused tests cover completed and failed conversation checkpoints, missing/misbound receipts and the existing direct Task case; NanoCore typecheck and focused Biome checks pass. The three live rows each have one matching conversation receipt and no direct Task receipt, as observed in `conversation-submit-receipt-metadata-ident-20260910T091115Z.txt`. Their actual next-boot cleanup remains to be observed on the fixed App, rather than inferred from tests.

A1 run `a1-dispatch-20260910T091013Z-199f85ad` completes seven tests in 119.375 seconds, including the actual host-domain job-survival, concurrent-request rejection and failed-candidate restoration test. It executes the frozen reviewed fixture `4afa5deb` with helper `cb736216`; earlier failing runs remain preserved. This establishes the isolated host mechanism under that helper revision, not A2 product or public-update acceptance.

Fresh Claude context `37109e64-3ff5-4176-a690-c39733f0b05e` reads this primary context's Intent, checkpoint and evidence and returns Continue. Reconcile both readiness tolerance and NanoHost successor-generation validation in one App-update owner amendment, with a single read-only sweep of the other seven predicates before implementation. The existing NanoHost transport owner requires a greater connection generation after App restart; unchanged process identity remains separately observed. The reviewed A1 fixture can run independently because it has no connected NanoHost. No new acceptance rule may admit unknown diagnostics or weaken retained-data proof; such a decision would return to the engineer. The terminal conversation-checkpoint dispatch correction remains a separate root-cause fix, with no manual record deletion.

Read-only capture `temp/live-deployment/activate-0620e228/checkpoint-three-records-20260910T085154Z.txt` finds three checkpoints in `ws_3`, two completed and one failed, all with null Goal/Task IDs. Boot rejects each with `The boot Task checkpoint contradicts its command identity`; retained leases already record the terminal outcomes. The public interrupted-worker list returns zero items. This defeats the hypothesis that an ordinary user retry can resolve these records. No deletion, retry or new activation occurs during the probe.

A1 run `a1-dispatch-20260910T084442Z-00f414d8` passes the new HTTP/auth precondition and reaches the build, which fails because the fixture's `docker build .` uses the host job's foreign working directory. App replacement never begins, so `previousAppRestored=false` is truthful and the restoration acceptance remains unproved. The fixture correction derives its source root from `BASH_SOURCE`; a local execution from a foreign directory observes the expected source tree and existing Dockerfile. Six local checks pass; successor wrapper `20260910T085000Z-dispatch-a1.sh` and fixture `4afa5debc2ec1b178c0d6e7d933b3a00e6daf10dc2618b7de280c2d739fc3f53` remain held for independent review and a fresh direction observation before another A1 effect.

The `1d37d011` bootstrap performs the real App/Web/configuration replacement with one Data Root writer. Packaged Desktop diagnostics observe candidate boot `boot_6be5596f-8536-4787-8b1a-1dd3e8e5f8d4`, `acceptingProductWork=true`, and nonblocking `scheduler.checkpoint_recovery_required` for three attempts, alongside `storage.index-rebuilt`; both have empty `blocks`. The script's stricter admitted-code set rejects the scheduler warning and restores the previous image/configuration/Web while retaining the stopped failed candidate. The current old App is accepting work at new boot `boot_9d0de35d-4ac0-4c24-b8a0-7139b090a647` and exposes the same scheduler warning. Inspect its exact recovery records and accepted resolution path before a new activation; neither a public update pass nor effective 111000 is claimed.

Independent review admits fixture `d01e3cde1f0404b65a4db8d74cf581d0bae1d04c3b1fb4785d5fd327c9b30f0c` and wrapper `20260910T083200Z-dispatch-a1.sh` (`d357ec5ebc2e07100765dbd11d588887588a977aa19ad115f0ccb5727866c1b2`). Five local controls include execution of the exact generated fixture entrypoint and distinguish ready HTTP 200 from transport status 0 and HTTP 401. The prior A1 outcome remains ambiguous between transport and non-200 response; it is not relabeled as a proven startup race. The next A1 run first retains this exact HTTP observation before any helper job and captures container logs on failure (`temp/ops-packager/fixture-http-review/review.md`). This is a distinct diagnostic hypothesis with frozen bytes, not an unchanged retry.

The `265ae0ee` bootstrap passes every corrected digest check, then refuses before App stop because Docker's observed lowercase `error: no such object` does not match its uppercase-only absence classifier. Post-attempt inspection confirms no rename/configuration change, the retained old App and NanoHost, and a preserved protected log snapshot (`temp/live-deployment/activate-0620e228/replace-265ae0ee-state-confirm-20260910T0829Z.txt`). Independent review accepts successor `1d37d011db3d16af52a5e847a1e2c8aabe71b3a874f7b12d987819ba354ac476`: both known-absence classifiers admit the observed spelling, other failures remain unknown, and the next log snapshot has a unique UTC suffix with a privileged collision check. Syntax and local known/unknown controls pass (`temp/live-review/inspect-final/review.md`). Dispatch once after fresh idle verification; neither the old snapshot nor previous dispatched bytes are replaced.

The all-digest probe attributes live and staged configuration pins to their original no-final-LF listing form, preserving both pins and all 14-file comparisons (`temp/live-deployment/activate-0620e228/digest-repr-20260910T0812Z/matrix.json`). The historical Web pin cannot be reproduced, so the operator compares all four ready-tree files with `/srv/web` from immutable image `sha256:dc31459e32bf610054f3fcadff230c403b18ac89d452fb344cebe81ea54e100c`; every path and byte digest agrees. Their observed `tree_digest_hex` is `6bc743b3a74d9115a9a16dec714bbd925dfaf6db609b8a551d19d67d3345fdae` (`temp/live-deployment/activate-0620e228/web-image-provenance-20260910T0818Z.out.txt`). The prepared successor `265ae0ee9ab170bed52367bf59552014b668ac4326194b742913154f84cadd19` uses that one image-derived Web identity before and after replacement; independent review accepts its exact bytes (`temp/live-review/digest-final/review.md`), and it is not a restaging operation. Dispatch once after a fresh no-active-work read; the expected actual boot, 111000 context, retained Workspace and unchanged NanoHost observations remain unchanged.

The third A1 execution `a1-dispatch-20260910T081649Z-eaa2c71d` passes runtime admission, then terminates before apply/build with `Retained auth-store identity could not be read`. The corrected fixture preserves the actual receipt and journal before cleanup; the deciding live test fails while four local controls pass. No concurrency or replacement pass is claimed. Investigate the exact fixture HTTP/record path with a local probe before another host run; do not weaken production auth-store validation.

The corrected A1 fixture is independently admitted at `6a7a19f1aff699ae26f21bd670ddab71bc9a8b42a68ed09eeb8016f049ed118d`, with unchanged helper `cb736216dfd248a68a4f99b566947e55fbb994c84bac25c6186896268536c8c9` and wrapper `20260910T081000Z-dispatch-a1.sh` hash `2b02d1da0ad9c7859217194e6459f4edba2b8c87ffbbab5c008421f4cff25cb1`. Controlled runtime omission and skipped evidence preservation fail at their intended assertions; restored bytes and four focused local checks pass (`temp/ops-packager/fixture-admission-review/review.md`). The next independent A1 execution observes actual job survival, busy exclusion, failed-candidate restoration and bounded cleanup. It can run while A2 remains at a read-only digest probe: the helper is frozen and neither lane blocks the bootstrap. No A1 result is inferred in advance.

The `d490f9c4` A2 attempt again refuses before stop: live file contents equal the saved manifest, but the pinned digest hashes the listing without its final LF while the replacement script hashes the file including that LF. Read-only byte attribution proves this distinction (`temp/live-deployment/activate-0620e228/live-config-manifest-bytes-20260910T0810Z.txt`). Correct the existing digest representations across live, staged configuration and Web before another dispatch; do not restage data or accept an unexplained hash. Live A2 and its default Docker runtime are observed as standard `runc`; NanoHost PID remains 1263840.

Fresh Claude session `2d61bfe3-3857-46ba-81d4-226fb0e0116f` returns Continue after this primary compaction (`temp/live-resume-direction/next-investment-verdict-20260910T0803Z.md`). Use the existing Desktop administrator credential for public update acceptance; keep the optional Web admin grant unsent and use the existing member session for Web engineering. The App target remains `76281460`, with the separately installed protocol-identical `4a5f1ef2` helper. The stale `3ae46` installation artifact is retired and must not execute. Next effect is the independently accepted bootstrap `d490f9c41ee9500e42583552b182e670a6a642f816627dde1873718d4f5cb292` after a fresh no-active-work observation. It corrects privileged stage/backup reads without changing permissions or frozen stage identity. Expect actual `0620e228`, a new ready boot, effective context 111000, retained Workspace identity, standard `runc` and unchanged NanoHost; preserve any preflight failure and do not repeat unchanged bytes to overcome it.

Independent intent audit confirms that the selected Web patch, exact-byte independent review and observed reject decision can establish the bounded engineering workflow without source merge/push. Closure still requires that actual Web result, public App replacement with its owner predicates, real helper lifetime/concurrency and failed-candidate recovery observations, live effective Orca context 111000 and post-activation packaged Desktop access. Existing authenticated Desktop diagnostics already count as a bounded operation; no additional destructive recovery or mandatory second instance is introduced.

The `47edcfba` bootstrap execution at 07:46 UTC observes zero active scheduler leases, then aborts at the protected-stage existence check with `recovery_skipped_before_stop_attempt=1`. Independent post-attempt inspection retains the old running image, old Web pointer and NanoHost PID 1263840; no helper is installed (`temp/live-deployment/activate-0620e228/replace-app-0620e228ae148ca0fd7779f1094c1729ffe569ed-20260910T0745Z.execute-20260910T0746Z.out.txt`). A new frozen script must correct that check without changing stage permissions. Separately, the `76281460` staging digest mismatch is attributable to exactly one import-created `scripts/docker/__pycache__/app-update-helper.cpython-312.pyc`; reconstruct from the verified archive with bytecode generation disabled, rather than accepting the polluted digest. Independent helper review accepts explicit standard `runc` preservation and 32 tests; the live fixture still requires explicit runtime selection and unconditional pre-cleanup evidence capture (`temp/live-review/runtime-fixture-recheck/review.md`).

The second A1 run's journals establish that both jobs terminated immediately with `App runtime is unsupported`; the first was already finished when the second start was admitted. This does not establish a concurrency-lock defect. Both exact units are inactive/not-found and the fixture root is absent (`temp/live-tester/app-update-host-probe/inspect-073606-busy-failure/raw.txt`). Correct the helper's rejection of Docker's populated standard runtime and the fixture's failure capture before another run. The separate A2 bootstrap is independently accepted at `47edcfbab240717701b0e1f31c69aca89a97e969f7ff0a9936d8bbc5dd29d42c` (`temp/bootstrap-exec/replace.sh`, `temp/live-review/app-swap-final/review.md`); dispatch follows an immediate no-active-work observation. Expected evidence is actual `0620e228` App/Web, a new ready boot, effective Orca context 111000, retained Workspace identity and unchanged NanoHost. A preflight mismatch prevents the stop; failure after the stop keeps recovery subject to proved writer exclusion. The public helper remains uninstalled.

The repository projection correction is committed as `76281460`; independent review runs all 11 route tests successfully and verifies that no production startup backfill or mutation-time projector remains (`temp/app-update-builder/repository-seam-review/REVIEW.md`). The fresh packaged Desktop Actor authenticates and reads live diagnostics through the extracted public Skill; its read-only status probe receives HTTP 404 from the old App. Local CLI search/describe establishes packaged operation availability only, not server implementation (`/tmp/openkit-packaged-desktop-20260910/report/readonly-diagnosis.md`). The first A1 fixture execution fails during setup because the generated Docker repository name includes uppercase timestamp characters; no helper job or replacement runs. Preserve its original wrapper and `a1-dispatch-20260910T072550Z-09476438` output, correct only the fixture name, and rerun with new frozen bytes after observing cleanup. This is an instrument failure, not product acceptance evidence.

The optional host helper and operations projection are committed as `94f63017`. Independent review observes 31 helper tests passing, including retained token metadata and exact protected SSH-bind preservation (`temp/ops-packager/helper-live-correction-review/review.md`). Independent owner review accepts the operations guidance; documentation-model validation covers 253 documents, lifecycle and index checks pass. The A2 staged configuration now resolves Orca at exactly 111000, with seven profiles and six logical routes valid; its new stage hash is `d1078770044964461207499a744f510c553373c2dd002a8aad432896a910d730` (`temp/live-deployment/activate-0620e228/apply-orcarouter-context-111000-20260910T0718Z.out.txt`). Live configuration and App remain unchanged. The separately reviewed A1 fixture wrapper `20260910T072500Z-dispatch-a1.sh`, hash `b02a4a7f17ee9315f12ceb0104db054e37c8892ce2a028106ef65dbbf22075c6`, is dispatched once to observe real systemd lifetime, lock exclusion and failed-candidate restoration. It retains its small transferred source/evidence directory; only the fixture owns its bounded container/image cleanup. No result is inferred before the retained execution output arrives.

Fresh Claude session `fc5bfbcb-4306-4654-9177-5be6f3fac923` checks this resumed primary context and returns Continue (`temp/live-resume-direction/current-context-verdict-20260910T0715Z.md`). The primary selects a staged Git diff with independent review and a reject decision as the bounded engineering probe; this is an execution method, not a new engineer requirement or a claim of tested source merging. It needs the existing repository resource record after removing the condemned host-path catalog projection, but no new Core Git clone or container bind. The next A2 effect updates only the private staged Orca context to 111000 and revalidates resolved metadata before freezing the replacement script. Its expected observable is the exact resolved limit, with live configuration and App unchanged; a different value or changed live state stops the apply route.

Prior Provider deployment and independent configuration evidence remain in `temp/live-deployment/deploy-26abdc12-evidence.json` and `temp/live-review/a2-config-26abdc12.md`. Model research is in `temp/live-builder-model-metadata/`; its corrections supersede the first proposal's pi-ai cache-field spelling and unverified pricing assumptions. New raw evidence stays under task-owned `temp/` paths. No acceptance is inferred from documentation, a tool inventory, a submitted Task or an Agent's self-report.

The independent audit rejected making optional incomplete prices an inference prerequisite. The narrowed backend contract preserves inference and token accounting while withholding an unsupported USD estimate. Complete known rates still use stock pi-ai calculation. After primary compaction, Claude session `2027a8ec-5859-46fc-981c-e1b8acbf9be9` is inspecting source Intent, current Git and retained deployment evidence before new update-delivery commitment.

Fresh Claude direction check (`2027a8ec-5859-46fc-981c-e1b8acbf9be9`) returns Continue for the required-context model slice, real Worker proof and operations Skill. It recommends `openkit-ops` to distinguish the Skill from the existing stopped-server `openkit-operator` executable. New host authority remains held for the concrete restricted design; there is no mandatory second instance or measured desktop-hop prerequisite. Independent model owner review reports no material objections after mandatory context and optional-price corrections; `git diff --check` passes.

Model owner review, document-model validation (250 documents), lifecycle validation and generated-index check pass. The operations Skill remains a committed Draft proposal for independent review; it grants no host effect.

Next host action is A1 cleanup under Intent Epoch 6: enumerate exact test/temp paths and services against the observed A1 identity, inspect the proposed deletions, then remove only that selected scope. Expected observable is reclaimed storage and absence of those legacy artifacts with SSH usable; a host-identity or mount-scope mismatch stops deletion. A2 operation and pending implementation continue independently.

The exact Orca free-model context is absent from both public and authenticated Provider model discovery (HTTP 200, selected row present; `temp/live-review/model-context/authenticated-orca-v1-models.json`). The engineer has been asked for its required declared ceiling; A2 model activation waits for that number rather than inventing one. OpenRouter publishes 200000 for its exact free-router ID and the pinned catalog agrees. Code, Skill migration, Worker preparation and A1 cleanup proceed independently.

Independent `ops_delivery_owner_review` reports no owner-level blocker for the operations Skill and manual migration. Claude agrees with its boundary and naming. The owner is Accepted; package/projection code will follow that committed contract. App-update remains Draft while correcting released-artifact identity and receipt-loss admission. These do not gate the separate Skill packaging or model slice.

Independent owner re-review accepts the corrected App-update contract: published releases retain their published digest; exact commits remain source builds; inert prepare assigns a host UUID and pins the review object, and start never recreates missing/corrupt/expired receipt state. Claude confirms the bounded scheme. The delivery owner is Accepted, ready for a concrete Cursor implementation with one writer per seam. No key or host job has yet been installed.

The required Orca context input is now supplied by Intent Epoch 7; model activation no longer waits on user information. Its parser/configuration swap still belongs to the coordinated deployment step.

The Web exploratory Worker checked out the exact public OpenKit revision `1bc77878b4607f6b1b4fc7f175536ae1a8ee8de2` through workspace `ws_3` and the Agent's `task-mode-repository` binding. Safe reload activated that catalog without a Core restart. The requested README Artifact was not produced; inspect the actual transcript before another run, and do not admit this source-activation probe as an L6 pass. Model review also identified initial-startup admission, nested object isolation and incomplete-pricing defects; their focused corrections remain in progress. The operations archive exists with resolvable local references, but its independent review and fresh Actor task still decide package acceptance.

Fresh Claude session `c130c924-9abe-4431-bfcf-0f3497405236` re-read Intent Epochs 1–7, the accepted owners, Git and A1 cleanup artifacts after primary compaction and returned Continue. The App-update helper remains justified by Web invocation and surviving App replacement; a second instance remains optional. A1 dispatch incorporates raw mount enumeration, all-container bind inspection, an early non-interactive sudo prerequisite and final evidence emission despite a service-check failure. The model's 400000 context declaration supplies metadata for existing adapter consumers; it does not implement the separately owned context-compaction lifecycle. Focused model failures observed during correction must pass before model deployment. The package review also found that release CI must verify actual extracted operations references, not only archive membership; that local correction is in progress.

The fresh operations Actor completed a bounded read-only A2 diagnosis using the extracted Skill and ordinary host tools, with retained evidence at `/tmp/openkit-ops-actor-20260910151441/REPORT.md`. Independent intent audit corroborated the container/HTTP observations and package bytes. This satisfies the bounded operator-task predicate only: authenticated product-work readiness and offline recovery execution were not demonstrated. The audit's release-versus-source-build wording ambiguity was corrected in the operations reference. Neither that task nor successful archive extraction closes the overall engineering plan.

Independent package recheck (`temp/live-review/ops-packager/ci-recheck.md`) confirms that both CI stages now extract and verify the operations archive; its four focused archive/helper tests pass. Producer checks report release workflow 9/9 and release preflight 23/23 passing; the unchanged NanoHost archive-creation test cannot run with Darwin's BSD tar because it requires GNU `--mtime`. No GitHub release was published. Documentation-model validation covers 252 documents and lifecycle validation passes; the index was regenerated for the operations specification's Partial implementation status.

Before model activation, retain two further concrete corrections: stock tier pricing must preserve explicitly authored rate precedence, and the generated custom Provider placeholder must not make a fresh required-context boot invalid. These are not waived by the earlier 102 focused passing model tests. A1's first cleanup reclaimed about 27 GiB and left SSH and retained services active; the remaining confirmed test temporary directories are being removed in one bounded batch. Host helper implementation now belongs to Cursor `app-host-helper`; the separate App-update writer retains API, client, Web and image wiring.

Independent owner review accepts the explicit authored-rate precedence across stock tiers and the inactive custom Provider example. Implementation evidence remains pending final review. A separate real Worker diagnosis found empty bridged function-tool supply; its lowest regression must distinguish lossless function tools from unsupported native/custom grammars under the existing pi-ai backend owner. Native Codex selection for one Task uses the existing logical-model request field and must preserve OrcaRouter as the deployment default.

A1 cleanup has an unresolved incident: direct host inspection found `/tmp` itself absent after the temporary-prefix batch. Its root-owned mode-1777 directory has been restored, with SSH and Docker active; exact deletion cause and content loss remain unknown from surviving evidence. Newline-delimited `xargs` is unsafe for general pathnames but is not the established cause. Further recursive deletion is stopped. Claude session `473eea88-9c15-4943-b51e-b54feaef25d5` completed this primary context's direction check: Continue independent A2 model activation and the bounded native-Codex Web task, while retaining the A1 incident; Reframe helper implementation around observed fixed-target Docker behavior. Every new effect uses a preserved script hash and a distinct output file, without editing the dispatched script or recursive A2 deletion. The helper remains undeployed because its draft checks inferred identity from their own inputs instead of observing the running system.

Final model verification records 112 passing NanoCore tests across six focused files, including one separate App-update restart-classification test, and 15 passing Provider-schema tests. Independent F6/tier review ran four targeted checks plus the template-order check, all passing; its report is `temp/ops-packager/model-review/review.md`. Earlier independent F1–F5 dispositions remain in `temp/live-review/model-metadata/recheck.md`. The model-only commit excludes the App-update restart hunk and its test, preserving that worktree scope for the later delivery commit. Focused Biome checks pass; document-model validation covers 253 documents, lifecycle validation passes, and the index is current. These establish the model slice, not the still-failing separate Worker bridge or deployed 400000-token activation.

The exact-source `9b85d003` image build failed before any A2 App stop (`temp/live-deployment/activate-9b85d003/preflight-facts-20260910T0601Z.txt`). The compiler rejected duplicated optional cost-property types under `exactOptionalPropertyTypes`; the focused correction uses `ProviderProfile` metadata directly. Previously passing tests did not establish image-build readiness.

The native Codex Web attempt dispatched through `new-task-worker` and logical model `reasoning` in workspace `ws_3` (`temp/live-tester/self-hosted-engineering/runs/2026-09-10T055847294Z-ws3-reasoning-artifact.json`). It ended with `worker_governance_turn_failed` and no Artifact. Eight of nine Responses calls failed after stream handoff; existing usage and Docker records discard the inner failure identity (`temp/live-tester/self-hosted-engineering/native-stream-error-origin.md`). Investigate a bounded correction in the existing Gateway terminal-record path before repeating the task; neither HTTP 200 handoff nor one successful inference proves the requested engineering result.

The type correction is committed as `71fae907`; independent review accepts its reuse of the exact Provider schema type (`temp/ops-packager/model-review/authored-cost-type.md`). The separate function-only Worker Responses bridge is committed as `07caa28b`; independent recheck observes all 67 focused tests passing and retained native/custom rejection (`temp/ops-packager/bridge-review/recheck.md`). Neither commit is active on A2 yet.

Fresh Claude session `ae78580f-f151-4e8f-9353-5dea2f86de62` checked this resumed primary context and returned Continue (`temp/live-resume-direction/current-context-verdict-20260910T0616Z.md`). Before another image build, Cursor verifies NanoCore and Web types from the exact clean build revision; its previous dirty-tree probe is insufficient. The A2 replacement retains actual image, bindings, current Task terminality and post-update NanoHost/service observations. Gateway normalized terminal-code reuse restores an existing owner requirement but does not by itself distinguish every generic codec/truncation failure; a new real Task needs an informative hypothesis or diagnostic observation. A1 cause reconstruction is exhausted; the remaining bounded obligation is observed service state and honest incident disposition.

Independent App API/Web review accepts the prepared-source consent binding, real administrator denials, truthful handoff Audit, bounded SSH output and public nullable predicates (`temp/live-review/app-update/recheck.md`). Independent F5 review accepts configured-but-unusable identity handling without Core startup failure (`temp/ops-packager/app-update-f5/review.md`). NanoCore, Web, config-schema, app-api-schemas and core-client typechecks pass on this API worktree; the exact deployed-source build remains separate.

The helper remains rejected despite 20 passing producer tests: independent review found unverified source acquisition, pre-verification predicate/schema disagreement, unsafe concurrent writable Data Root cases and a retained-data check reduced to HTTP 200 (`temp/live-review/app-helper/review.md`). The previous writer released its files; fresh Cursor `helper-correction` must correct those specific contracts and their deciding checks before another helper review or host effect. No new diagnostics field is required: the accepted owner permits verified source acquisition linked to the observed built and running image.

A1 selected cleanup is complete: independent residual inspection found the selected old trees, Docker objects and temporary prefixes absent; exact nonrecursive removal then cleared the disabled user service and its two drop-in files. The unit is not-found/inactive with no later restarts, `/tmp` and `/var/tmp` remain root-owned mode 1777, and SSH/Docker remain active. `AOE-FND-001` is closed as an independently audited historical incident disposition, retaining unknown cause and unquantified content loss; no original-deletion correctness, deleted-content restoration or full-host health is claimed. Evidence is linked from the finding.

The exact `0620e228ae148ca0fd7779f1094c1729ffe569ed` source archive passes clean NanoCore/Web typechecks and the A2 Docker build and App smoke. Its observed immutable image is `sha256:dc31459e32bf610054f3fcadff230c403b18ac89d452fb344cebe81ea54e100c`; evidence is under `temp/live-deployment/activate-0620e228/`. This activation target includes the committed App interface, Worker bridge and normalized Gateway stream-code correction. The successful earlier `71fae907` image is retained unused, avoiding an extra live interruption. A2 still runs `26abdc12`; private candidate configuration and Web staging precede review of the exact replacement script, and 400000 remains inactive until post-boot observation.

The arbitrary Markdown Artifact probe also assumed a publication capability that is not wired: Worker adapters do not call the transcript Artifact writer, while the current linked-Git path publishes `workspace-changes.json` and imports a reviewable diff Artifact. The read-only trace is `temp/app-update-builder/artifact-path/findings.md`. Reframe the next Web task around a bounded `CONTRIBUTING.md` clarification of the already accepted App/NanoHost maintenance boundary, the resulting workspace-change Artifact and independent content review; `git diff --check` supplies hygiene evidence only. Leave the unattributed local README edit untouched and uncommitted so it is not this probe's deliverable. Do not add an unrelated Markdown-publication feature or treat `expectedArtifacts` as an activation switch. The known public source revision remains explicit and no push is authorized.

Fresh Claude session `c61655b9-1127-4681-a2d5-1354b4842eae` checked this resumed primary context and returned Continue (`temp/live-resume-direction/next-investment-verdict-20260910T0650Z.md`). The four remaining lanes address separate live acceptance observations and proceed independently where possible. Before the Web task, inspect the retained effective AEP's writable Git input and change-set manifest path rather than inferring them from catalog intent. The corrected public Skill bundle is committed as `ebac80eb`; its new archive-level regression and independent old-bundle negative establish the packaging correction, with a fresh exact-commit extraction still required for the Desktop Actor.

Live integration exposed two helper prerequisites missed by mock-only checks: replacement must preserve its own two protected read-only SSH credential binds, and a server-admin token cannot read private Workspace catalogs. Independent owner review accepts the existing administrator-only `GET /api/app/auth/tokens` as the helper's `retainedAuthRead` observation: compare a nonempty stable metadata projection before/after, omitting last-use observations. This proves Core auth-store continuity, not Workspace content. A2 activation separately reads retained `ws_3` data with its existing Workspace token. Both corrections stay within existing transport, authorization and retained-data owners; no new privilege or helper credential is added. The real AEP read confirms writable Git and its change-set manifest, but the Workspace repository catalog is empty, so the receiving repository resource must be configured before a useful diff-import probe.
