---
type: change-plan
status: verified
date: "2026-09-12"
completed: "2026-09-12"
---
# Roadmap Execution Refresh

## Intent Epoch 1

On 2026-09-12 the engineer asks to reorganize the Roadmap for completed work and changed plans, then provide a pathway on which Agents can autonomously develop, test, submit PRs and merge until a new blocker. The evaluation framework goes last in a separate section, to be designed and implemented together after OpenKit operates: internal-Agent performance, task completion and performance, Worker evaluation and Skill evaluation including A/B tests. The engineer's suggestion that evaluation may be the only blocker is conditional, not permission to ignore other unaccepted contracts. A follow-up explicitly authorizes an independent Claude Code Consultant to help choose the pathway.

Preserve the existing two operating paths, Web-directed work and Desktop Agent plus Skill, optional second-instance use, separate NanoHost maintenance and current-user administrator authority. Preserve all Roadmap IDs and complete outcome obligations, identify verified slices without claiming broader closure, and leave Product Vision unchanged. This turn reorganizes documents; it does not start the entire implementation program, deploy, or publish a PR or release.

## Owners And Write Boundary

The primary exclusively writes `docs/roadmap.md`, a scheduling clarification in `docs/specs/20260711-evaluation_harness_design.md`, this record and a historical pointer in `temp/pathway/20260903-execution-pathway-blockers.md`. Regenerate `docs/INDEX.md` only if its projection changes. `docs/documentation-model.md`, `docs/change-execution.md`, root `AGENTS.md`, `CONTRIBUTING.md`, current Core and accepted feature specifications retain their authority. No runtime, evaluation architecture, approval bypass, new controller or Product Vision amendment is introduced.

## Working Checkpoint

The Roadmap now follows Intent Epoch 2: three substantial development batches, necessary development regressions/review, then one frozen-candidate acceptance campaign with evidence mapped to all included plans. The batch table distinguishes candidate slices from full parent-outcome obligations; it does not promise the entire Roadmap within the remaining week. First-release installation audience is requested from the engineer and remains explicitly conditional in the document until answered. No new remote or production effect occurs in this turn.

Base `e94d6916` had five completed and 104 unchecked outcomes. All 109 original IDs remain; R041 alone is newly complete after independent Auditor inspection of actual A1 candidate, activation, completed successor, native executable, absent old writers and recomputed replay equality. New R111 records product-native PR/merge, R112 internal-Agent evaluation and R113 Worker evaluation. All other changed outcome text aligns existing accepted Skill, personal Assistant, privacy and scoped learning decisions or explicitly labels the Git conflict. The completed-slice ledger remains non-closing and preserves broad R005/R011/R035/R104 and MVP limitations.

Claude `roadmap-consultant` in Herdr `wF:p4P`, session `993f33e7-0873-49aa-a196-494c57d8797d`, independently examined the sources and successive proposal bytes. It identified the accepted host-versus-Sandbox Git conflict, missing PR/merge contract and overbroad implied first-release coverage. The primary retains D1 without picking an owner, uses the existing authorized Desktop/repository route during release development, and narrows the batch table's claim while preserving every backlog obligation. The user correction, rather than the original acceptance-first proposal, owns cadence. The Consultant re-read the scope-corrected candidate and returned route agreement with no material objection; it explicitly confirmed the large-batch/single-campaign cadence, conditional installation audience, preserved obligations and absence of a new review quota. Its final response is retained at `temp/roadmap-20260912/claude-final.json`. Independent Reviewer found no actionable final-document finding after the R049 membership-lifecycle correction. Final Auditor requested one authority clarification: this roadmap request does not itself authorize publication across the whole backlog. The primary corrected the autonomy paragraph to apply only within an engineer-authorized implementation scope; ordinary work does not require repeated permission, but no new publication authority is created.

Registered Auditor `roadmap_status_audit` accepted the final authority correction and returned Close with no remaining intent/status finding. Registered Reviewer `roadmap_refresh_review` re-read the actual corrected bytes and found no actionable issue. The Roadmap, Draft scheduling note and historical blocker pointer now agree. This documentation task is complete; no implementation batch, Git publication or deployment was started. The requested first-release installation-audience answer remains optional context for this conditional roadmap, but its applicable gate must be resolved before that deployment.

## Acceptance

- The Roadmap has one clear ordered pathway for ordinary autonomous development, product acceptance, independent review and authorized PR/merge, using existing governance and operating paths.
- Every original Roadmap ID and full outcome remains accounted for; completion changes have direct independent evidence, and already delivered slices do not become new implementation work.
- B1–B6 are accurately classified; known later design or use-case gates remain explicit and affect only dependent work.
- Integrated evaluation is a distinct final stage covering all four requested targets and A/B comparison, without postponing ordinary regression, L6, independent verification or evidence capture.
- Documentation checks, an independent review and an intent/status audit inspect the final bytes before closeout.

## Intent Epoch 2

The engineer corrects the proposed cadence on 2026-09-12: fewer than one week remains before the first release; use large development batches with larger task boundaries, then one consolidated testing and acceptance campaign at the end. This supersedes serial feature-by-feature live acceptance and making the missing product-native PR/merge loop the prerequisite for developing the release. Existing authorized repository/Desktop engineering can continue while product design gates remain explicit. Keep necessary development regressions and independent review, but consolidate full L6/real-host acceptance against one final release candidate and map that evidence back to every included plan. Do not claim those plans verified before the final observations.

## Closeout

The primary wrote all documentation. Independent Claude consultation, registered review and intent/status audit accepted the resulting scheme. All 109 existing Roadmap IDs remain, R111–R113 add only the requested missing publication/evaluation outcomes, and R041 is the sole new completion. Integrated evaluation is the last separate phase. Old B1–B6 labels no longer masquerade as current global stops; concrete later contract gates remain visible without delaying independent release work.

## Verification Evidence

On Node 24.18.0, documentation model validation passed for 256 documents, specification lifecycle validation passed, the generated index was current, and whitespace checks passed. A direct inventory/link check confirmed unique IDs, preservation of all original IDs, only R041 changing completion, all ten evaluation outcomes in the final phase, and existing local link targets. The initial identical documentation checks also passed on ambient Node 22.22.3; they were repeated with the pinned runtime and are not the canonical toolchain evidence. No product code or runtime behavior changed, so no product suite or live deployment was rerun. Product Vision and the prior implementation plans remain unchanged. The documents were initially left uncommitted after acceptance. The engineer subsequently requested a local commit; no remote publication, deployment or release-program execution is included.
