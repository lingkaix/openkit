---
type: change-plan
status: implemented
started: 2026-09-22
branch: feat/operator-failure-truth
---
# Operator-Visible Failure Truth: First Delivery

## Intent Epochs

### Intent Epoch 1 — 2026-09-22

Source: Simon's implementer kickoff, the consultation proposal `/workspace/ok-err-vis-artifacts/CONSULT_PLAN.md`, binding decisions `/workspace/ok-err-vis-artifacts/SIMON_DECISIONS.md`, and incident RCA `/workspace/ok-git-fetch-rca.md`. These external artifacts preserve decision evidence, not repository design authority.

Deliver one PR containing Slice 0 foundations needed by Slice 1 and durable typed failure projection. Preserve an observed Git HTTP refusal without claiming sandbox enforcement attribution. Include a Draft delegated-maintenance contract, focused regressions, shared projections, and recovery guidance. Simon accepts bounded delegated worker policy maintenance under current administrator authority, excluding self-expansion, Vault grants and containment/TLS changes. Slice 5 implementation is later. Whole-service recovery is deferred; no Slice 7, SSH operations, full enforcement correlation, or full product-journey redesign belongs here.

Acceptance requires exact safe codes and evidence completeness through the Harness result and durable Turn after restart, without raw secret canaries, automatic replay, retained-file cleanup, or invented denial attribution. Open a PR against `lingkaix/openkit` from latest main, obtain direct Herdr Consultant plan and PR acceptance, and retain both verdicts with green required CI in `/workspace/ok-err-vis-artifacts/PEER_ACCEPT.md`. Publishing the branch and PR is authorized; merging is optional only after both ACCEPT and green checks.

### Intent Epoch 2 — 2026-09-22 — Separate The Superseded Incident Follow-Up

Simon directed disposition of existing PR #105 separately from this program and clarified that live #104 was already repaired through DATA_ROOT path grants, with th_46 PASS and issue closure. Both implementer and `review-err` accepted closing #105 without merge after inspecting its helper-grant diff and obsolete incident-fix claim. Its branch is unrelated to this local program branch. This delivery opens a distinct PR without `Fixes #104`; `/workspace/ok-err-vis-artifacts/PR105_DISPOSITION.md` records the separate disposition. No live smoke was rerun by these peers.

## Owners

- [Worker Turn Reliability Envelope](../../specs/20260531-worker_turn_reliability_envelope.md): durable normalized explanation and terminal/recovery truth.
- [Audit, Usage, And Evidence Records](../../specs/20260703-audit_usage_evidence_records.md): evidence provenance, audience and completeness.
- [NanoHost Runtime And Transport](../../specs/20260802-nanohost_runtime_and_transport.md): existing authenticated operation-result carriage and receipt.
- [Session-Static Workspace Materialization](../../specs/20260704-session_static_workspace_materialization.md): bounded Git observation and retained-slot behavior.
- [Delegated Worker Policy Maintenance](../../specs/20260922-delegated_worker_policy_maintenance.md): Draft future contract only.

## Working Checkpoint

The implementer owns all repository writes. Fresh Herdr Codex `review-err` owns read-only consultation and independent review; root's referenced `.codex/agents/` catalog is absent, and Simon explicitly commissioned this Consultant. Baseline is `b1e94f855d90e9a32c992cf726b7b154c4c9ebac`, fetched from origin/main. No `.codegraph/` index exists.

Method: extend the existing closed startup-failure result and Turn error schemas; validate authenticated lineage at existing result acceptance rather than introducing a failure store or exposing hidden AgentSession identity. Worker observation is not enforcement proof. The maintenance Draft does not change the active propose-only Tool boundary.

Consultant `review-err` returned PLAN_ACCEPT, then found an unbounded ISO fractional timestamp and a forbidden spec-to-change backlink; both were corrected. Its receipt inspection established a crash window: the latest Harness slot may be replaced by cleanup before Turn persistence. Owners now state this unavailable-cause limit instead of claiming historical receipt retention.

Next Action: publish the distinct program PR, request independent review of its exact head, and wait for required CI. Publication is expected to produce a reviewable diff containing the Draft contract and typed durable failure seam; a scope mismatch, failing contract check or unresolved review finding prevents acceptance. No incident repair or real sandbox enforcement PASS is claimed.

## Verification And Closeout

Implemented closed Git fetch explanation across the shared protocol, startup refusal, Harness validation and existing Core Turn terminalization. HTTP 401/403 stays unattributed with partial evidence; primary facts survive cleanup aggregation. No policy grants, live configuration, delegated write Tool, service recovery, new storage owner or retry path changed.

Observed local verification on Node 24.18.0 and pnpm 10.33.3: protocol 195 tests, worker-protocol 27 tests, worker-shim 263 tests, focused Core runtime 255 tests, and rebuilt Skill interface 43 tests passed. After the truncation-boundary correction, the complete workspace-git file passed 53 tests. NanoCore/shim dependency build and typecheck completed 14/14 tasks. Frozen dependency installation, focused changed-file Biome, documentation model, specification lifecycle and generated schema checks passed. An isolated production `pnpm deploy --prod --legacy` imported WorkerHarness and both shared protocol dependencies entirely from the deployment output. Evidence logs reside under `/workspace/ok-err-vis-artifacts/`.

Initial regressions observed the missing structured Turn field and opaque 401/403 failure before implementation. Intermediate checks exposed an exact Error-object fixture needing its new explanation asserted and stale Core Client build output in the Skill test; both were corrected and the deciding checks passed. Whole-working-tree Biome encountered an unrelated ignored `.claude/settings.local.json` formatting failure; focused changed-file lint passed without modifying that configuration. Early exploratory commands used ambient Node 24.21.0; deciding checks used the pinned version.

Residual limits: no Docker binary is available, so isolated package deployment is not an image build or real OpenShell enforcement proof. The crash window before durable Turn persistence can leave cause unavailable; after persistence, cold read/list and terminal-event checks retain the explanation. Full enforcement correlation, delegated application and service recovery remain out of scope. PR review and required CI acceptance will be bound to the exact published head in `/workspace/ok-err-vis-artifacts/PEER_ACCEPT.md`; this record does not predeclare those gates.
