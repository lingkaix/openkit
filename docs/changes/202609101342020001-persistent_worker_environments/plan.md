---
type: change-plan
status: in-progress
date: "2026-09-10"
---
# Persistent Worker Environments

## Intent Epoch 1

The engineer requests that Worker sandboxes behave as long-lived lightweight VM-like working resources, while NanoCore remains the backend-independent orchestration center. Persist and remount useful working storage through compatible image replacement and planned or accidental container/epoch restart. NanoCore/Web updates must not routinely restart NanoHost. Desktop and internal Operations Agents must prepare and activate a changed environment through owned operations, including adding software during a long-running task with truthful interruption/continuity semantics. Primary writes Core/key specs personally, consults independent Claude Code, obtains independent review and audit, commits documentation first, then creates a new plan and completely implements the design. Product Vision remains untouched. Source: engineer messages in this thread on 2026-09-10.

## Intent Epoch 2

The engineer clarifies that ignored temp/, native runtime conversations, memory, configuration and similar files are examples, not a complete inventory. Preserve and reuse entire volumes generically; no file-kind allowlist or per-runtime data migration registry. Inherited base-image storage layout should support ordinary derived images adding tools or upgrading software. Mount compatibility does not assert native-format rollback or process-memory continuity. Source: subsequent engineer corrections in this thread.

## Intent Epoch 3

The engineer makes Workspace and work relatedness central to reuse: isolate Workspaces, prefer reusing suitable idle Workers and volumes for related Tasks/Goals within one Workspace, but let Operator/Orchestrator Agents choose dynamically rather than a mechanical similarity algorithm. The settled interpretation permits explicit selection of an idle retained association for new related work after its old Sandbox is gone, with cumulative source-audience admission, contributor/independent-review isolation, compatible layout and fenced writers. No-input admission creates fresh retained storage; a Goal may explicitly select reuse for child work with current eligibility rechecked. Source: latest engineer steering in this thread.

## Owners

- [Persistent Worker Volumes](../../specs/20260910-persistent_worker_volumes.md) owns generic storage, admission, replacement and acceptance.
- [Storage](../../core/storage.md), [Runtime](../../core/runtime-model.md) and [Sandbox](../../core/sandbox.md) own durable boundaries.
- [NanoHost](../../specs/20260802-nanohost_runtime_and_transport.md) owns fixed transport, host effects and epoch fencing.
- [Materialization](../../specs/20260704-session_static_workspace_materialization.md) and the native adapter specifications own retained data versus disposable control.
- [Environment Images](../../specs/20260721-worker_execution_environment_images.md) owns inherited image supply.
- [Thread entry and visibility](../../specs/20260909-thread_visibility_and_sharing.md) owns immutable private administration routing and its record cutover.
- [Internal administration](../../specs/20260704-chat_mode_assistant.md) and [Operator Skill](../../specs/20260910-agent_operator_skill.md) own the two operation entries.

## Decisions And Evidence

Independent Claude session 61f72bec-5abd-48fe-8956-b1f8c22044ca challenged Workspace/user-derived sharing, per-file retention, host UID allocation and an unnecessary combined environment-inspect operation. Settled opaque per-admitted-group storageRef with current cumulative audience checks, explicit Agent selection, transactional attachment CAS, whole volumes, host-owned 0700 parents, inherited OCI Volumes and numeric User, generic seed-once per target, fresh control, bounded image.inspect/storage.inspect/storage.purge on existing carriage, and no new journal. Current read requests do not inherit effect recovery; partial purge fences only its ref. The readonly bootstrap candidate was refuted by the pinned OpenShell actual write probe and corrected to writable ephemeral /tmp/openkit-bootstrap; immutable base Python supply moves under /opt. Pinned upstream source is OpenShell 0.0.99 at 8c7dd148a9e6360c9d5b2830e339a0dc4b3f3032. Temporary source, consultations and independent doc findings are under temp/persistent-worker-environment/.

## Checkpoint

The physical Epoch source, converter, guide corrections and nine additional fixture files were independently accepted and committed as `0e63730b`. Observed checks: 443 affected-fixture tests pass; full NanoCore 2,896/2,897 pass with only the unchanged macOS `/proc/self/fd` failure, whose owning file passes 31/31 in the canonical Linux ARM64 image; TypeScript, lint and build pass. Linux NanoHost has 158 unit and two integration passes plus format, Clippy and release verification. The exact App archive and Host release were staged on A1 and hash-verified. A1's classic Docker store uses the archive's image-config digest as its local image ID; the initial manifest-addressed staging failed before creating a container, and corrected exact-config staging preserved environment and mounts. Original Core and Host were stopped with independent predecessor absence evidence. The cold converter succeeded, and an independent native SQLite comparison confirmed all 42 table row multisets preserved except the accepted witness transformation and single ledger addition. New Core `cc61e82d` is healthy on config digest `d327a55a`; installed Host bytes are `abd323d5`. Host startup then failed: admission reached generation 11, but its first `sandbox.create` poll received `409` under the existing unknown-outcome successor fence. It is now stopped/nonready; no new Task or restart retry has occurred. The reproduced defect is startup result-only cleanup registration without the stored target origin. The narrow correction carries that origin and retires different-Epoch expectations without closing the replacement connection; same-Epoch and origin-less Image Store fences remain intact. Claude independently confirmed the cleanup-only correction and withdrew a broader image-recovery change because durable Image Store effects survive Epoch replacement. The owning and adjacent recovery suites pass 45/45 and 48/48, with TypeScript, focused lint and build passing; independent review also requires homogeneous groups to be validated before retained-settlement lookup. Final source acceptance precedes a corrected App-only deployment, without conversion, Host reinstallation, marker reset or Task replay. Fresh-context direction scrutiny returned Continue for that route; healthy Host readiness and ordinary cleanup settlement are prerequisites to the new readback Task. The rebuild-fence marker is absent, not reset. Post-attempt observations preserve the seven original files/Git state, known native Codex history, all unrelated containers, bridge and `docker0`. The actual cutover preserved nft unchanged. Earlier unequal pre-stop snapshots remain retained; their independently inspected delta was confined to elements of the existing Fail2ban set without proving its causal writer. Evidence lives under `host/physical-epoch-cutover/`. Successful Host readiness, original-volume readback with one App-only restart, Web prepare/activation, successor work and exact Skill replay remain outstanding. Earlier checkpoint paragraphs below are historical.

Worker9 has now crossed its recorded startup deadline: the lease is `stale` with `startup-timeout` and `recovery-projected`, while the backend remains `launching`, the old Harness inspection remains queued and the public Turn remains `running`. These are direct incomplete-recovery observations, not terminal success or cancellation (`host/worker9-diagnosis/core-state-post-startup-deadline.json`, `host/a1-worker-9-turn-post-startup-deadline.json`). No Task replay, database mutation, reconnect or service action was used to settle it.

Claude withdrew the compatibility-hash shortcut and the advice to retain time-based absence checks after independent audit proved the latter violates same-Epoch cleanup fencing. The agreed owner amendment uses a coordinator-derived explicit physical witness, current RuntimeTarget plus immutable Sandbox/backend-attempt origins, and replaces all temporal absence decisions. The existing cold backup/conversion pattern handles the three record fields with a reserved non-reusable predecessor marker; no runtime legacy fallback or new migration framework. The private Harness wait also acquires an enqueue-side bound so an unpolled old command cannot wait indefinitely or reach a later incarnation. Root has authored the transport and volume owner amendment; independent review/audit must accept it before code. The current A1 Host and data remain unchanged, and Worker9 is not replayed. Consultation: `host/worker9-diagnosis/claude-revised-epoch-verdict.json`; source and consumer evidence remain adjacent.

The factory-only correction was refuted before production edits: both connection generation and `lastFreshReadyAt` advance on a healthy reconnect to the same coordinator, so neither proves a physical epoch replacement. The private readiness body is currently exact `{}` and no existing authenticated/persisted physical-epoch discriminator exists. Independent audit returned Reframe to an explicit physical-epoch witness within the already-authorized continuity/remount outcome; the existing transport owner must be amended and independently accepted before implementation. A new ID alone cannot authorize old-writer cleanup: it must be coupled to the existing authenticated fresh-coordinator and predecessor-fencing proof. Same-epoch reconnect must preserve healthy state; unknown/conflicting evidence fails closed. Claude consultation is resolving the minimal wire/persistence and existing-record treatment; no user trade-off or loss of healthy continuity is inferred.

Worker9 exposed a live post-epoch recovery gap before Worker execution: request `5fdda72b-97fc-44c8-b435-aa2bd9ef8288` entered a new `launching` backend session but queued `session.inspect` against the old persisted open Harness/Sandbox even though RuntimeTarget generation 10 reports a later fresh-empty fence. The packaged Skill wait ended with `connection_failed`; fresh `turn.read` still reports running, so this is neither cancellation nor terminal failure. Preserve that request without replay. Read-only diagnosis and independent Claude consultation now examine the existing reuse/fresh-epoch owner before any correction or next preparation; no manual database reset or concealment by restart is proposed. Evidence: `host/worker9-diagnosis/core-state.json`, `host/a1-worker-9-task-start.json` and `host/a1-worker-9-turn-after-disconnect.json`.

The bounded A1 Host-only upgrade is complete: exact NanoHost `37a12edf` is running as PID 3969437, new invocation `8684c8860991427d86c52d74faa5cc51`, authenticated RuntimeTarget generation 10. Independent audit directly accepted old-writer absence, unchanged retained fixture/native bytes, usable Image Store, unchanged Gateway/unit and exact App/unrelated-container/network comparisons (`host/retained-alias-upgrade/`). One packaged-Skill read-only Worker9 is now submitted against freshly observed selected storage revision 9; its actual materialization, completion and readback remain pending. No manual storage reset, failed-build replay or additional service restart was performed.

Fresh independent direction scrutiny after the next primary compaction returned Continue after reading source Intent, Git `68eac2a8`, actual stop admission/absence evidence and the prepared Web observers. Finish the already admitted Host upgrade; only after authenticated healthy readiness and retained-data/noninterference proof, submit one new ordinary Skill readback using the current storage revision, then new Web preparation, exact-candidate activation and identical-request Skill replay. Inspect configuration, per-storage result, successor Turn and retained native metadata separately. Do not replay the failed preparation or interpret idempotent replay as another activation.

Source increment `68eac2a8` is committed. The real isolated Docker 29.7.2 / Buildx 0.36.1 strict-policy observations now establish retained build/tool execution, exact original layer prefix, alias reuse without rebinding, conflicting-alias refusal without mutation, and wrong-checksum denial before Dockerfile execution with broader policy rules retained. The two incorrect temporary assertions remain recorded and were independently adjudicated against archive hashes and actual BuildKit vertices. A separate tiny-archive S4 follow-up invokes the real backend import: reserved-name content is refused while absent, ordinary naming loads the same verified identity, and the original Worker alias is preserved (`image/product-private-alias-probe/s4-followup/`). This closes the missed probe setup, not the remaining A1 whole-epoch observation.

The locally verified A1 package is `host/private-alias-linux/release/openkit-nanohost-v0.1.0-persistent.20260911-linux-arm64.tar.gz`, SHA-256 `1c69343362e109e31e8867823e2bce6d52a3be21c0aa1d2024479cd8eec39e25`, containing NanoHost SHA-256 `37a12edf7d66470be60938d411cd43eb93ad0cf81ef646c41bc3394e0ef3b42a`. The fresh read-only preflight preserves all seven data samples, Git state, native transcripts and SQLite main data, Image Store content, the App and nine unrelated running containers. Its only historical nftables difference is one Fail2ban-owned set element; independent comparison confirms every other normalized byte. Next action is one bounded Host binary upgrade using the reviewed `host/retained-alias-upgrade/plan.md`: stage and verify the package and predecessor backup, refresh idle/baseline predicates, prove complete old-writer absence before replacement, start one successor without resetting the fence marker, and verify authenticated newer readiness plus unchanged retained data. Any failed prerequisite stops the dependent effect. The App, unrelated services, credentials and persistent stores are not upgrade targets. Successful Host startup must not be described as Worker-image preload or final Task/image-replacement acceptance.

The single packaged-Skill fixture Task `a4f2d31b-408a-4334-a5eb-1636fc0cfdab` completed in 22.807 seconds without a Gate or error; fresh public reads retain storage revision 9 / attachment generation 3 and no active lease or Turn. Independent filesystem observation verified five new tracked/ignored and harmless memory/settings samples plus both unchanged original nonces. Four real native Codex JSONL files and SQLite state were observed; completed transcript bytes supply the later preservation oracle, while ordinary SQLite WAL/checkpoint changes are not data-loss evidence by themselves. Evidence: `host/retained-alias-preflight/result.md` and `a1-worker-8-post-task-facts.json`. This is pre-upgrade fixture/component evidence, not whole-epoch, image-replacement or L6 completion.

Final-source validation found two local corrections before deployment. The first Linux run reports 151 passed, 1 failed and 2 ignored: `attempt_import_verifies_store_before_backend_hit_and_is_failure_isolated` received a Store failure before its intended digest-mismatch branch; its isolated rerun passes. A deterministic Linux pre-exec barrier probe proves that an inherited open file description retains the drop-only directory lock, while explicit unlock restores independent acquisition immediately (`host/private-alias-linux/fork-inherited-lock-probe.log`). The original flattened Store error does not establish its exact cause, and a narrower parallel diagnostic did not reproduce it. The private lexical guard now explicitly unlocks the shared open file description; the clone-descriptor regression and all 19 Store tests pass. The exact final Linux source snapshot passes 154 unit tests and 2 integration tests, with 2 declared ignored tests, plus Clippy and formatting (`host/private-alias-linux/cargo-test-linux-arm64.log`, `cargo-clippy-linux-arm64.log` and `cargo-fmt-check-linux-arm64.log`). This corrected full run does not erase the original failure or overstate its attribution. Independent review also found that retained imports rebased the whole-build deadline through durations; the concrete backend now carries one absolute deadline through archive scanning, child startup and bounded waiting, with 2/2 pinned Rust 1.97.1 focused import checks, 23/23 coordinator checks and all-target/all-feature Clippy passing (`image/private-alias-absolute-deadline.log`, `private-alias-epoch-final.log` and `private-alias-epoch-final-clippy.log`). The fresh post-compaction direction audit returns Continue: finish these existing-owner corrections and exact Linux/strict-backend proof before any A1 Host upgrade. No new architecture or engineer decision is needed, and the plan remains open.

The independently reviewed source increment is frozen at acquisition blob `6b10c8d7`, Store `1a9abadd` and coordinator `71edf30b`. Its Linux ARM64 binary is SHA-256 `37a12edf7d66470be60938d411cd43eb93ad0cf81ef646c41bc3394e0ef3b42a`; complete input maps, gates and build attribution are in `host/private-alias-linux/result.md`. The isolated strict-backend probe has observed a successful actual BuildPlan/binder build, result import, exact original 21-layer prefix plus one added layer, inherited user/workdir/volumes and executable added tool. Its first attempt failed on a temporary driver's pre-created build directory, which was corrected without changing production. A later temporary assertion incorrectly required Docker `.Id` to equal OCI configuration digest; observed `.Id` instead equals the manifest on this backend. Direct normalized archive hashing independently confirms distinct correct manifest/configuration digests, and the product uses `Descriptor.digest`; retain the failed assertion and corrected oracle evidence rather than rewriting the run as an unqualified pass. Remaining negative controls and A1 deployment/retention/activation are pending; committing this verified source increment does not close the plan.

The source owner is committed as `05461a9c`. Implementation uses the existing acquisition, backend and Store verification owners, with cooperative deadline checks preserving stored bytes on timeout. The fixture Task above is complete and must not be resubmitted. Next: independently accept the final source and its Linux/strict-backend evidence, then prepare one bounded A1 Host upgrade after fresh idle and unrelated-service observations. Expected observables are exact new Host bytes, a fresh healthy epoch with the same retained volume data, and a successful ordinary readback; a failed fence/readiness proof stops admission without resetting evidence or replaying a Task. The existing App and all unrelated services remain unchanged.

Intent Epoch 12 resolves the source-addressing choice. The existing NanoHost source amendment has undergone Claude scrutiny and independent review/audit; a documentation commit precedes production changes. Current source inspection disproved an annotation-free assumption: the accepted retained Worker archive contains both OCI naming metadata and Docker RepoTags, so its import needs a bounded name-free projection preserving and reverifying the exact manifest/configuration/layer content. Reuse the existing archive and backend owners, without a new cache or alias lifecycle. Expected outcome: verified retained content builds through a private local alias under exact manifest-checksum policy without registry dependence, while wrong content, alias conflicts and broader-rule bypass fail before Dockerfile execution. Keep the healthy A1 deployment unchanged until the corrected Host is independently accepted and a bounded upgrade is ready. Earlier pending-decision paragraphs are historical and superseded by Epoch 12.

Worker7 completed across the actual App replacement. Independent installed-Skill `turn.read` request `5ce42654-8de4-430a-9baf-607cab571308` confirms completion at 09:47:35.626Z, no error, and final Item `_169` at 09:47:33.884Z reporting the original 24/22-byte files and SHA-256 hashes before and after `sleep 180`. Independent audit accepted the combined exact process/lease/file observations and successful Task outcome. The separately retained control snapshot has one AgentSession/package, unique contiguous heartbeat 0..85 and event-append 0..86, with accepted records on both sides of the new App startup and no recorded rejection; it ends before terminal completion and does not establish every native-protocol guarantee. Evidence: `host/a1-worker-7-app-update.jsonl`, `a1-worker-7-sequence-metadata.json`, `a1-worker-7-sequence-summary.json` and the independent Skill read identified above. This closes the named bounded active-task App-update observation, not full R001, compatible image replacement or this plan. A fresh independent read is retained in `host/a1-worker-7-turn-final-audit.json`, request `af3ad297-f87a-4bcb-9558-59cfac4c98af`; the actual Web DOM contains that final result in `host/a1-web-worker-7-after-update.json`, while its companion screenshot records the conversation shell at the earlier viewport. The 09:49:53Z post-update baseline preserves all nine unrelated containers, normalized bridge/docker0/nft, machine and boot identity, with Host PID 3859012 unchanged (`host/a1-8c927f88-baseline-comparison.json`).

Current frontier is the accepted private-alias source amendment and its implementation. Preserve the healthy updated App, unchanged Host and retained data; do not rebuild the failed authored request or activate an unresolved candidate. Independent local checks and actual update evidence are complete for the Core handoff increment. The source-policy generator must keep exact retained alias/checksum rules outside broader registry/reference allows, and imported archive naming metadata must not overwrite private aliases.

A1 now runs App container `659959af4278cc04e8e9dd3d4f0c249011158e95ed33342e40be6d54a9876903`, source `8c927f88`, platform image configuration `sha256:896517207f71c9babc113a3ea6bb150082cd45691655a8f16a071857a1c443b1`. The replacement occurred during Worker7 request `b849483e-29c5-4233-86f1-09a9417f4921`, Turn `turn_b849483e-29c5-4233-86f1-09a9417f4921_f39156965fcde2e3`, with exact active lease `lease_c65edebb9af5428a`. Independent instrument review accepted SHA-256 `4cd5fcfd3da8801cada4c56d411d8abc333b4b96a797c94f4df1641792601580` after tightening the final lease status to exactly active. The actual pre-effect match was `sleep 180`, age 0.23 seconds. Sleep PID 3913160 and Codex PID 3913021 retained identical start ticks and cgroup across the update, both original nonce hashes/lengths survived, the same lease remained active, health recovered and Host PID 3859012 stayed unchanged. The Skill wait disconnected, which is not a Task cancellation. At this pre-terminal observation the public result was still pending; the completed result and independent adjudication are recorded above. Exact observations are in `host/a1-worker-7-app-update.jsonl`.

The retained-parent build investigation reproduces the Buildx policy-input limitation in a fresh network-disabled Docker 29.7.2 / Buildx 0.36.1 / BuildKit 0.32.2 daemon. OCI-layout fails before Rego; canonical digest lookup without a warmed cache attempts registry resolution. A private local alias with strict exact-name/manifest-checksum matching builds successfully and preserves all parent layers; a deliberately wrong checksum is denied before any Dockerfile RUN and yields no output. Those probes established feasibility but did not themselves authorize the owner change. The engineer subsequently selected the private-alias amendment in Intent Epoch 12; the alternative Buildx patch is not pursued. Implementation follows independent acceptance of the amended owner, and a corrected build will use a new request lineage. Temporary primary evidence is `image/local-derived-probe/strict-docker-image/results.md` and its named traces. Earlier claims that 9784 is the configuration digest are incorrect: the inspected retained archive identifies 9784 as its selected manifest and eb6878 as its configuration; the fresh canonical-source failure is independent of that mistaken hypothesis.

The accepted Core handoff is committed as `8c927f88`; its exact App build and image smoke passed. Archive SHA-256 is `bb6be6e0b08ac4160edd07c92b22323e601e6162b72b8edb26e6a7b18ebd30d1`, 203,707,904 bytes. Claude and fresh direction scrutiny agree to use this required App replacement for the already-authorized active Worker continuity observation rather than add another restart. Stage a stopped replacement with exact environment/mount equality while idle, then admit one read-only Task using selected storage revision 9. The concrete observer must match exact `sleep 180` arguments, later start ticks than the same-cgroup Codex process, sleep age below 60 seconds, the sole exact active lease and more than 200 seconds remaining lease lifetime; otherwise it refuses before replacement. Preserve pre/post process, lease and file facts separately from the later public Task result. Only the owned App changes; the healthy Host, failed build settlement and unrelated services remain. The observer receives independent inspection before its effect.

The accepted generic Core handoff now passes the complete Linux development-image NanoCore suite (2,878/2,878), package lint (634 files) and build, with exact logs under `core-restart-selected-revision/`. The macOS-only procfs failure remains recorded separately; Linux success does not erase it. Independent review/audit accepted the frozen seven-file diff and the runtime guide sentence. The known settled build failure does not block this separate Core update. Next action: commit this accepted increment and current evidence, build one exact App image and replace only the owned A1 App after fresh idle/configuration checks; keep activation paused while the supported strict retained-parent source path is investigated off-host.

Post-prepare observation preserves nine unrelated containers, Docker bridge/docker0, machine and boot identity, with NanoHost still PID 3859012. The normalized nft comparison is false only because the existing sshd Fail2ban set changed; exact service logs attribute the two expired bans and replacement ban. Preserve both the raw difference and attribution in `host/a1-prepare-failure-baseline-comparison.json`, `a1-prepare-failure-nft.diff` and `a1-prepare-failure-fail2ban.log`; do not rewrite the baseline as equal.

The actual Web prepare reached the private internal Agent, created authored Artifact `ar_worker_environment_authored_dcb81b3ac33cb96a62fb0913` and dispatched one Host `BuildImage`, but strict BuildKit rejected the retained-parent OCI context with `failed to build policy input: unsupported source scheme: oci-layout`. Exact failure settlement `8313038e6d8fabf02b7b58e12ceb772387f9e2a0632539246ef896f506c85583` is durable at 09:26:37.230Z with no image digest. The Web shows the authored recovery card; no candidate was activated, the Agent configuration remains its original exact SHA-256 and Host PID 3859012 is unchanged. This disproves the implemented strict retained-parent build route, not the retained bytes. Reframe locally with Claude and the image owner around a supported native source representation; do not repeat the build unchanged or weaken policy. Evidence: `host/a1-web-prepare-response.json`, `a1-web-prepare-after.json`/`.png`, `a1-web-prepare-host.log` and `a1-web-prepare-settlement.json`.

Independent review and audit accepted the generic selected-storage handoff and its runtime guide projection. The complete local NanoCore run passed 2,877 tests and failed one unrelated archive-boundary test because it unconditionally scans Linux `/proc/self/fd` on macOS. Preserve that environment-placement failure; do not skip or change it within this storage correction. The same frozen bytes are undergoing the existing Linux development-image gate and the required build before commit.

Current next action after fresh independent direction scrutiny: prepare only one derived image through the Web private Administration chat, using the exact 298-byte Dockerfile and digest in `image/local-derived-probe/Dockerfile`. Expect an actual settled image result and immutable structured candidate Artifact; a chat summary is insufficient. Do not activate or change the live Agent configuration until the generic idle-session selected-revision handoff is independently accepted and deployed, then recheck candidate, configuration and storage revisions. Keep the healthy Host and unrelated services unchanged. This preparation proves neither unavailable-registry behavior nor complete image replacement. Claude and fresh Auditor agree this bounded preparation is independent of the pending Core correction; neither active Image Store deletion nor registry/firewall manipulation is part of the attempt. The retained-parent projection ceiling is the existing 20 GiB archive constant, and the observed A1 filesystem has 112 GB available.

The generic idle-session handoff correction is frozen for independent review and audit after 162 focused tests, typecheck, Biome and diff checks. Its deciding red demonstrates the original revision conflict through the actual scheduler/factory path; the correction carries only the cleanup-owned exact revision advance into the immediate successor, preserving durable admission and competing-writer CAS refusal. Final diff SHA-256 is `afde8cdc65d2b4f773af7741e554f4ecf12d5a0303e4211ba19e3a2a1e9dfb60`; evidence is under `core-restart-selected-revision/`. Worker6 was independently re-read through the installed Skill and accepted for its bounded completion/readback claim. `host/a1-orca-vendor-only-proof.json` additionally shows that reversing only the vendor field in the exact current configuration bytes reproduces the old public revision.

Worker6 completed successfully on the now-observed bridged route. Turn `turn_bad0ca9b-5adf-4423-ae52-50ae5ac1f867_da2974bdfe4142b6` produced final Item `it_worker_turn_bad0ca9b-5adf-4423-ae52-50ae5ac1f867_da2974bdfe4142b6_9` at 09:10:51.072Z, reporting both original files with lengths 24/22 and exactly the previously observed SHA-256 hashes, after an explicit read-only request forbidding recreation. Its public state and Turn status are `completed` with no error. This establishes actual Worker-side readback and result publication in addition to Worker4's separate bounded App-restart process evidence; it does not retroactively make Worker4 successful or prove image replacement, native sequence recovery or full L6/R001. Evidence: `host/a1-worker-6-task-input.json` and `a1-worker-6-task-start.json`.

The Provider correction required both the public file CAS and the existing restart-required registry lifecycle. CAS changed only vendor identity to `orcarouter`, producing revision `sha256:596461ee5d68aef7632e1b61a6ba7bcda82e51199f53d5223c96294a505063b4`. Worker5 was submitted before loaded configuration was verified and failed while still on native Responses; retain it as an operator precondition error. Safe reload explicitly reported Provider registry restart required, so the same owned App was restarted with no active leases and Host PID 3859012 unchanged. Public diagnostics then showed Chat native / Responses bridged before Worker6 was admitted. The endpoint, secret reference, model and context remained unchanged. Evidence: `host/a1-orca-vendor-config-update.json`, `a1-orca-vendor-config-reload.json`, and the before/after-restart diagnostics files. Earlier no-restart and live-route attribution predictions below are superseded by these observations.

Worker4 terminalized failed at 08:59:23.072Z with the same native `stream closed before response.completed` diagnostic. Its valid App-restart process/lease/storage observations remain bounded; it supplies no successful Worker completion. A subsequent exact configuration read disproved the earlier live-route inference: `acceptance-orca` declares `vendor: openai`, so `gatewayCapabilitiesForProfile` selects native Responses and the uncatalogued model is registered with `openai-responses`. The Chat field-marker regression is a real local correction but was not the live route behind either failed Worker. Correct the earlier attribution rather than treating that regression as proof of the A1 cause. Public usage records remain metadata-only and their generic local-400 classification does not identify the native missing feature.

Next bounded route: independently confirm the existing provider-identity and adapter selection rules, then correct OrcaRouter's actual vendor identity through the existing configuration compare-and-swap, preserving endpoint, secret reference, model and context 111000. The normal third-party Chat-to-Responses path must then complete a short Worker readback before another long task or lifecycle effect. Separately, the generic current-idle predecessor revision gap now has a full scheduler/factory red regression and a passing competing-writer refusal control; production correction remains pending exact owner-aligned handoff review. No additional App or Host restart is planned for provider correction.

Worker4 request `3d5a2229-1318-47a9-a5df-f177af422de3` was admitted after a narrow canonical-file probe confirmed the prior product AgentSession was terminal `failed`; explicit selected storage revision 3 therefore used the existing materialization path. The new sandbox exposed the original nonce bytes without recreation. During its active lease `lease_ae66ec2bf8737d3e`, the exact App container was restarted. Native Codex PID 3890458, start ticks 84122427 and cgroup remained identical, NanoHost PID 3859012 stayed unchanged, health returned, and the public Turn remained running. The original Skill wait disconnected with `connection_failed`; that is not cancellation or terminal task evidence. Web reload showed the retained conversation and active Task. Final Worker output remains pending.

The observer selected container keepalive `sleep infinity` PID 3890364 rather than the requested Task sleep, so its raw record does not establish the intended Task-level wait window or Worker-produced pre-restart readback. The original before/after record is retained with an explicit instrument disposition; it still directly establishes native process, lease and operator-read file continuity. A subsequent primary read-only process inspection found the actual `sleep 240` PID 3890630 in the same cgroup; its exact returned metadata and tool-output provenance are retained in `host/a1-worker-4-sleep-observation.json`, not as an independent pre-restart oracle. Do not repeat the restart merely to improve the narrative, or claim complete task acceptance before the durable outcome. Evidence: `host/a1-worker-4-core-restart.jsonl`, `a1-worker-4-turn-after-restart.json` and `a1-web-worker-4-after-restart.json`/`.png`.

Core correction `4e399c58` is now deployed on A1 as App container `67e228984c6cad995caf0697b715b34f63896e355987b240ef6b5eeb1f16349f`, image `sha256:ff1b5f0d7068d2d8b4e658b181eedf512de18dfccfc023cc40f2b8610ad457b8`, archive SHA-256 `b225913f431ded94b3ae90d27605f8006cd6570f59689e66adc09252dc67a806`. Build, image smoke, protected-environment/mount equality, public health and installed Skill doctor passed. NanoHost PID 3859012 is unchanged and its Core connection is generation 6. A read-only check confirmed both prior nonce hashes unchanged. Evidence: `app-image/build-4e399c58.log`, `smoke-4e399c58.log`, `archive-4e399c58.json` and `host/a1-app-4e399c58-replacement.jsonl`. The original archive extraction attempt failed on local Python 3.9's unsupported tar filter argument; native tar extracted the trusted Git archive before the successful build. This was a packaging setup failure, not product evidence.

Historical pre-Worker4 checkpoint, superseded by the admission and restart observations above: the next readback request was prepared but not submitted. Source inspection correctly requires explicit selected storage after Core restart; same Thread alone does not select the old whole-volume association. A conditional admission gap is under bounded investigation: a still-current idle predecessor can be retired before materialization receives the original selected revision, while a terminal failed predecessor takes a different path. Do not infer the actual product AgentSession state from a failed Turn alone. Verify that state and reduce the affected branch to a local regression before changing code or attributing a live result. No further Worker effect or continuity PASS follows from this checkpoint.

Two bounded Core corrections now precede the next attempt. The real Worker stream failure was reduced without network access to the exact Pi semantic signature mismatch: the Chat adapter carries `reasoning_content` as field-routing metadata, while the Responses bridge treated every truthy signature as a native encrypted reasoning item. The shared native-item decoder now arms terminal encrypted backfill only for a decoded native Responses reasoning object. The regression preserves native backfill and missing-capsule rejection while proving a Chat marker reaches `response.completed`, `[DONE]` and one usage observation without fabricated encrypted content. The deciding red failed at that capsule check; the two focused suites pass 76/76. Public A1 usage and the native terminal error support this diagnosis, but the original private request was not captured, so the local reproduction is not an exact-request replay. Evidence: `inference-stream/red.log`, `green-tests.log`, `reproduce-reasoning-terminal.json` and `host/a1-worker-3-usage.json` under this change's temporary evidence directory.

The same inspection found a separate no-Sandbox recovery gap: after later fresh-ready proof, a cleanup-failed materialization could release its backend lease but leave its exact storage reservation permanently reserved. The correction finds the sole active binding matching Workspace, Thread, AgentSession, deployment and RuntimeTarget, then uses the existing attachment release owner after that proof; multiple matches fail closed and volume bytes remain retained. The deciding regression originally retained revision 2 in `reserved`; the corrected factory suite passes 34/34. Independent review and audit accepted production blob `5fbe6950536d8798dd458a3aa75fada3ab739be1` and test blob `d9b7b358f0e04135627c37578d0559a2e220017f`. Evidence: `bundle/storage-recovery/`. This fixes future recovery, not historical terminal A1 records; no database repair or effect replay is proposed.

Fresh independent direction scrutiny after primary compaction returned Continue against these two actual diffs and the failed Worker evidence. Independent inference review and audit accepted production blob `7a3611697bc762a38e4fdcffb78e8849c22bf0db` and test blob `452765d4c72eeda3f06e41d2c6278f82fcb7cd96`; the reviewer independently reran 72/72 tests and typecheck. Claude withdrew the unused four-way decoding suggestion after inspecting the exact predicate and retained no blocking objection. Next action: commit the combined Core correction, build one exact App snapshot and replace only the owned A1 App while retaining the healthy Host and whole volumes. Prepare the observer before admitting the next bounded Worker; prove it is active immediately before the App restart and observe process identity, retained bytes and terminal outcome separately. No complete L6, retained-parent build, image replacement or R001 result is claimed. Two adjacent Gateway findings remain outside this correction: a provider's redacted opaque non-Responses signature has no established encrypted-continuity mapping, and post-stream local 400 errors can be mislabeled as provider failures by `gateway-routes.ts`. Neither is silently claimed fixed.

Current live frontier: clock correction `a7dace16` passed independent review/audit and 57 focused tests, then replaced only A1 App container with `0d03541577bf0882fef993971d381bb9e08e594a2ec6fbcc99d1b00a177eb258`, image `sha256:4c51080722f149a21f46fa985a189c97ec12e7fcfa6b6c33003ca02a9482dde4`, archive SHA-256 `8a04f1788166ffa986081801f6b4a828be81ee1ff53ee4c48413bcdfb97b24b0`. Health and Skill doctor passed, transport reconnected as generation 5, and NanoHost PID 3859012 remained unchanged. Two precondition-only operator probes refused before mutation because terminal failed leases and Docker Mounts were initially misclassified; corrected probes used the actual scheduler terminal set and exact inspected mounts. The Consultant withdrew the subsequent re-stamping objection after reconciling the old code, deciding red log and current no-effect proof gate; independent audit also rejected re-stamping repeated pre-effect refusals.

New real Task request `513c76b5-d5a8-43c4-8a4c-a2fc4ce11405`, Workspace `ws_2`, Thread `th_4`, reached real Sandbox creation and active native Codex execution. The Worker wrote the two exact nonce files into retained storage with lengths 24 and 22 and SHA-256 values `726c53c4c2152b87b2269f242cbf0b3cd7b354242a7df46318439a78b2a9240c` and `2a9c61fd6cdced50c9fadaff04373ecaef42d01ac6ad9082e37fbfa6ee9595f7`. It nevertheless terminalized failed at 08:16:02.468Z; the native task-complete error reports `stream disconnected before completion: stream closed before response.completed`. Only explicit native terminal-error metadata was inspected; no reasoning extraction supplies evidence. The intended live App restart did not occur because the sleep process had already ended at the pre-effect observation. Preserve this failed attempt and its files; it proves startup and file production, not live restart continuity or a completed Worker task. Next bounded diagnostic: trace the existing native inference/Responses terminal error and its server evidence before selecting the next real attempt. Evidence: `host/a1-app-a7dace16-replacement.jsonl`, `host/a1-worker-3-task-start.json`, `host/a1-worker-3-turn-after-sleep.json` and `host/a1-worker-3-core-restart.jsonl`. Earlier frontier paragraphs below are retained historical observations.

Current frontier after the latest real A1 observation: Host commit `4ae8bd48` is installed as binary SHA-256 `2b127869b2451358f93d0fccce400e24e553fdb4969d269608ad015b40a28e06`; generation 4 is healthy with shared backend/Gateway mount and network namespaces, while App `95980d52` remains deployed. Final Linux validation passed 144 unit tests and two integration tests with two declared ignores, including the privileged namespace test separately passed against the exact accepted source. The exact retained Worker image was imported through the installed CLI. Generation-2 roots and images are retained aside; whole working volumes and the original binary backup remain intact. Empty credential directory identity was preserved through API slot retirement and exact-pair re-enrollment. Evidence: `host/linux-shared-mnt-final-accepted-20260911/`, `host/a1-generation2-retirement.jsonl`, `host/a1-install-4ae8bd48.jsonl`, `host/a1-shared-mount-ready.json`, `host/a1-shared-mount-members.json` and `host/a1-shared-mount-image-import.log`.

The recovery attempt exposed an implementation defect and is not a recovery PASS: the prior backend was marked cleaned at 07:47:12, before generation 4 became ready at 07:48:41. Recovery maintenance passed its 07:46:42 entry timestamp through an awaited cleanup failure, allowing generation 3's 07:46:55 readiness to appear later than its own failure fence. The existing transport owner requires readiness strictly after that failure. A bounded correction in scheduler-restart-recovery passes the actual clock into asynchronous transitions while retaining the entry snapshot for scanning; an advancing-clock regression, independent review and audit precede deployment. Do not rewrite or replay the terminalized historical Task. No new Worker is admitted pending this correction; no Worker nonce, volume continuity, strict retained-parent build or complete Web/Internal Agent acceptance is claimed.

The fresh independent direction check after primary compaction returned Continue: classify the network discrepancy, fix and independently verify the clock, then update only the Core App while preserving healthy Host generation 4. The original network comparison remains false, with its sole difference classified from actual logs as the existing Fail2ban sshd jail adding a banned address at 07:47:35.945. Exact bridge/docker0 state, all nine unrelated containers, machine and boot identity remain equal. Evidence: `host/a1-network-difference-disposition.md` and its named raw observations. No baseline was reset and no firewall change was made. Next action: obtain deciding red/green evidence and independent acceptance for the clock correction, build the exact Core commit, and replace only the App; renewed real Worker execution must then establish the outstanding product observations. The following earlier checkpoint observations are historical and are superseded where they describe a stopped Host, unresolved old Turn or pending Host packaging.

The incomplete-handoff warning was traced through actual code and a bounded A1 database read: the lease-maintenance reconciliation path catches it and still invokes recovery maintenance. The cleanup projection explicitly accepts pending handoff with zero Workspace materialization/handle rows, preserves that truthful missing handoff and can terminalize the Turn after later fresh readiness. It does not block the accepted two-coordinator recovery route and requires no handoff-validation change. The preparing Task checkpoint has no Worker session identity and reports recovery-required separately; inspect its final disposition after real cleanup rather than assuming this warning is settled. Independent Claude and the Core trace confirm exact-pair decommission/re-enrollment preserves Core target, connection-generation, lease, backend and Turn lineage. Retain failed roots aside without raw secret archival; the first new coordinator's poll must establish unknown outcome, and a later fresh coordinator must supply the strictly later readiness proof. Pre-stage that normal second start within the retained 300-second interval, never reset a marker or replay an old effect.

Core recovery correction `95980d52` is independently reviewed and audited, committed and deployed on A1. Its App archive SHA-256 is `62a9f90b3f38864e9f86a755df8e482a2c81b6076c2f226881b5283852be91e8`, runtime image is `sha256:d13503b39720d026a864917fef8609768104468597ef392bbc3d0822cdfae9c0`, and current container is `46012055e58ab4e64b5ce649ba16b8f47e304a4c87a30a9ba6dfbf7a0decb304`. A deployment precondition error omitted three non-secret explicit environment overrides from the first replacement; that container is stopped and retained, and the corrected replacement's complete environment matches its predecessor before start. Public health and installed Skill doctor now pass; NanoHost stayed stopped. Evidence: `host/a1-app-95980d52-replacement.jsonl`. A diagnostic summary is retained as Workspace Artifact `ar_import_147428662026fd3eb29ab69f` version 1 without recovery authority. The failed Turn remains unresolved; source tracing confirms same-pair decommission/re-enrollment preserves its Core target, lease and backend lineage, and the subsequent bounded trace in `host/a1-pending-handoff-recovery-observation.md` distinguishes the incomplete-handoff reconciliation warning from the independent cleanup path.

Incremental audit found and corrected two retained-image boundaries: partial content/index/staging was classified as Missing, and the new archive member-name inventory grew with unrelated archive entries. Final corrected acquisition blob `436734b6c73b46f2dac7ae7eaefce276a9836bd6` and store blob `3424146fcbb5db9ed87d532589a28a5715c40266` preserve incomplete bytes, admit registry fallback only for complete absence, retain bounded graph inventory plus three fixed optional OCI members, and preserve existing proved-corruption cleanup. Audit verified the final 3/3 retained-parent and 17/17 store checks under `image/retained-parent-final/`; the reported large-archive projection has no retained raw terminal stream and is not independently certified. Independent source review and audit accept the corrected production bytes. A redundant source-string assertion was then removed while retaining the deciding runtime regression, which passed again. Final Linux packaging remains required. The prior combined Linux build is retained as superseded intermediate evidence and will not be deployed.

Owner corrections are committed as `ca6b1950` (retained build-parent sources) and `b16bf6c6` (shared private mount ownership), after independent review and audit. The two Host corrections and the Core dispatcher correction are now frozen for independent source acceptance. The real privileged Linux member-spawn regression failed before the mount fix and passes afterward; native Host tests pass 142 unit and two integration tests with one declared ignore. Retained-parent source tests pass 13/13 and independent review accepts image-acquisition blob `4fbd6eb65f11c9a44c868b62815d4f079b288b16`. The Core regression proves cleanup result-only registration orphaned the earlier accepted create on successor poll; the corrected existing transition settles prior accepted uncertainty once while preserving queued work and durable cleanup ownership. Its three focused suites pass 41/41 plus typecheck and formatting. These are local observations, not A1 acceptance. Final combined Linux gates, frozen-source review and exact packaging precede the next deployment.

The fresh primary-context direction check returned Continue after reading Intent, current Git, accepted amendments and the paired mount/OCI probes. Before A1 restart, inspect fresh service, process, namespace, socket, Task/lease/backend, retained-volume and credential state plus the nine unrelated services. The expired generation-2 fence interval remains failed: use the accepted complete decommission and exact-pair reinstall route, never reset its marker or resubmit the timed-out Task. Unlike the earlier retirement, the Image Store now contains verified Worker content and needs explicit retained disposition. Preserve whole working volumes and the original binary backup; upgrade the App before opening the new Host readiness interval. Expected observables are actual shared member mount identity and Worker completion, then a strict supported-BuildKit retained-parent build with registry access unavailable. No new A1 lifecycle effect has occurred, and full-plan acceptance remains open.

Current live frontier: reviewed correction `940d8ee4` is deployed on A1 as App image `sha256:2d8dce574a5ca5312e8ac68b454cc41467ca526a87daac58a2538f309f0b8609`, container `810974f7c73c81cba9a2123f3f91a397733e4948db619f6cc24469161aa7cf96`, preserving DataRoot, protected environment and mounts. Both predecessor App containers remain stopped and retained. Normal fresh NanoHost generation 2 became ready at 2026-09-11T06:11:10.518Z, reclaimed the previous epoch roots without a marker reset, and the previous cleanup-failed backend row became cleaned at 06:11:35.313Z. Evidence beneath the existing temp bundle: `host/a1-app-940d8ee4-replacement.jsonl`, `host/a1-strict-recovery-transition.json`, and `host/a1-strict-recovery-settled.json`. The next real Skill Task on Workspace `ws_2`, Thread `th_3`, request `1d000777-6cd0-454d-8df9-bb0b162277b3` passed image acquisition and inspection but failed during stock Supervisor startup in `sandbox.create`: all four fixed `ip` helper metadata probes failed. The exact image and retained image layer contain the regular executable; this is not evidence of a missing package. NanoHost is stopped after the uncertain create; the Task client timed out, which is not a terminal-outcome proof, and the backend row remains materializing. Investigate effective container root visibility and the separate backend mount namespaces before any retry. Retain the failed epoch and whole volume association; no nonce files, Worker execution or volume continuity success is claimed. `host/a1-worker-2-host-journal.log` and `host/a1-worker-2-container-diagnostics.json` preserve the failure. Independently, `image/local-derived-probe/results.md` disproves the assumption that current Buildx resolves a repository-qualified digest from a local image without registry contact; it is a local resolution probe, not a NanoHost build verdict or permission to publish an image. Fresh independent direction scrutiny permits these bounded investigations with no blind retry, purge, fence reset, host reboot or A2 effect. The previously observed Web administration response uses the current private Workspace; its prose alone remains insufficient proof of an exact Tool call. Full-plan acceptance remains open.

The retained build-parent source gap has a native local proof: an exact named OCI-layout context preserves the authored digest-pinned Dockerfile, produces the same 21 parent layers plus one utility layer, and avoids the previous registry source-resolution path. Evidence: `image/local-derived-probe/oci-override/results.md`. Its strict-policy attempt was rejected by local Docker builder BuildKit v0.30 before source resolution; the authorized resolution-only fallback is not Host acceptance. Read-only A1 system-builder inspection reports BuildKit v0.32.2 and Buildx v0.36.1, so no A1 capability failure is inferred. Independent Claude agrees to extend the existing NanoHost acquisition owner with retained-parent-first resolution, definite-absence-only registry fallback, bounded private OCI layouts and unchanged strict policy; independent document review/audit precede code. The mount-visibility hypothesis remains unproven because the first paired local runtime attempts failed during setup. Preserve those failures and allow one corrected comparison using copied exact A1 runtime binaries and private temporary storage; no A1 lifecycle retry follows from them. At 06:40:03Z the failed Task remains running, but its lease is now stale with `startup-timeout` and `needs-evidence`, and its backend is cleanup-pending without physical cleanup proof (`host/a1-worker-2-timeout-observation.json`). All nine unrelated A1 containers and normalized host-network projections remain equal (`host/a1-second-failure-baseline-comparison.json`).

The corrected local paired mount probe now settles the startup root cause. With exact copied A1 containerd 2.3.4, dockerd 29.7.2 and runc 1.4.3, the exact Worker archive and stock Supervisor-equivalent root user/working-directory settings, the shared namespace arm exposes the full overlay image root and `/usr/bin/ip`; the split arm runs the same injected static helper on only the bare mountpoint tree and has no `/usr/bin/ip`. Both completed in the same 15.2-second run, exit 0. Primary inspected the per-arm helper output, mount identities and root listings; full evidence is `host/mount-visibility-probe/results.md` and `result.json`. Earlier setup failures remain preserved and supply no verdict. No local probe container remains; A1 access for this probe was read-only binary copying and hashes. The probe establishes containerd/dockerd causality only; its earlier recommendation to keep Gateway separate is not adopted. The agreed uniform private-member view includes Gateway because these members already share backend control authority, without adding another namespace branch or changing the host/Sandbox boundary. This defeats the accepted per-member mount split introduced by the DNS correction: reframe that owner to one shared recursively private mount namespace for the trusted backend/Gateway members, retaining the unchanged parent/slirp/host resolver and system Docker socket exclusions. Independent Claude had accepted this bounded correction conditional on the now-observed causal result; independent review/audit of the actual owner amendment precede code. This is neither an A1 corrected-product pass nor full-plan acceptance.

The bounded daemon-DNS correction now projects the once-validated resolver set into read-only, recursively private backend/Gateway mount namespaces while preserving inherited socket masking and host resolver bytes. Independent reviewer and Auditor accept this local correction; actual A1 startup and retained-volume acceptance remain pending. Frozen Linux checks passed 140 unit tests with two declared ignores and two integration tests. The new privileged real `JoinPrivate` spawn/DNS test was separately executed successfully in an isolated network-disabled container with the required namespace/mount permissions; the initial AppArmor-restricted attempt failed at setup and is not product evidence. The release build succeeded with unchanged source SHA-256 `328e71ee41ade1407b636e3d3d1b91e4ff68dd951a4fc3bd073b65aa31ee9471`. Evidence: `host/linux-daemon-dns-final-test.log`, `host/linux-daemon-dns-real-mount-test-unconfined.log`, and `host/linux-daemon-dns-release.log` beneath the existing temp bundle. A1's exact configured upstream answered the GHCR DNS query from the host; private slirp routing still requires the corrected product observation. Web signup plus existing public token issuance established one real canonical test administrator and private Quick Chat Workspace; the Web administration page and Skill entry both admit that user. The model initially lacked an explicit Tool-capability declaration; an actual provider request then returned the expected tool call, supporting its configuration. No complete internal-Agent or Worker outcome is inferred from those setup observations.

Current frontier after live A1 execution on 2026-09-11: commit `42fa7faf` is installed with the exact candidate4 NanoHost and unchanged `e41a2605` App. The isolated App is healthy; the packaged Desktop Skill completed administrator bootstrap and NanoHost safe-slot enrollment. Actual installer execution and the already-installed rerun both report configuration/enrollment/service-start as remaining work. The original NanoHost binary is retained separately and excluded from cleanup. A fresh independent direction check accepted the bounded first start after two equal idle baselines. That start failed: the dedicated Docker daemon used the host stub resolver `127.0.0.53` for the stock Supervisor GHCR pull inside its private network namespace. The existing Docker `--dns` arguments govern containers, not the daemon's own resolver. NanoHost exited and the complete normalized system Docker/network/container baseline and host boot identity remained unchanged. No workload or volume-continuity acceptance is claimed. Correct the private-process resolver projection under the existing fixed-resolver and mount-isolation owner, then obtain focused regression, independent review/audit and an exact rebuild before another controlled attempt. Do not erase or restart around the failed recovery interval. The user authorizes Playwright; it successfully rendered the real server-mode Web account gate. Web administrator setup and end-to-end behavior remain unverified. Evidence is under `temp/changes/202609101342020001-persistent_worker_environments/host/`: `a1-nanohost-install-observation.jsonl`, `a1-skill-bootstrap-final.json`, `a1-skill-nanohost-enroll.json`, `a1-baseline-resumed-before-start.json`, `a1-baseline-resumed-before-start-2.json`, `a1-first-start-failure.log`, `a1-baseline-after-failed-start.json`, and `a1-web-initial.png`. Earlier paragraphs below preserve preceding evidence and are superseded where they describe no A1 effects.

The local Image Store follow-up now removes Worker-image preload from startup, acquires retained images at point of use, exposes exact host-local import/list/remove/capacity commands, and replaces eviction with configurable retain-all capacity. The stock OpenShell Supervisor/GHCR bootstrap remains unchanged and is explicit in the runtime owner, NanoHost guide and operations Skill. Independent source review accepted the final preservation revision after invalid-retry, malformed-index and transient-I/O corrections. Frozen Linux candidate4 passed 140 unit tests (one declared ignore) and two integration tests, and its release build succeeded; before/after source hash maps match. The final binary SHA-256 is `eea790bb96cd35bdbc91cd8cb8eabefbd583c901dfaa2c0b109d893c65d2da58`. An offline actual-CLI observation imported the 478,850,048-byte Worker archive in a 256 MiB container, preserved it after lowering capacity, accepted identical no-growth reimport and removed only the exact stored image without creating service configuration or an epoch. Evidence: `temp/changes/202609101342020001-persistent_worker_environments/host/linux-candidate4-cargo-test.log`, `linux-candidate4-cargo-build-release.log`, `linux-candidate4-source-hashes.before.txt`, `linux-candidate4-source-hashes.after.txt`, and `local-image-cli-final-observation.jsonl`. Independent final audit accepted the local increment for commit after inspecting these source bytes and observations. No A1 lifecycle effect or retained-volume continuity PASS exists. Next action is the coherent Host/guide commit after audit, exact release packaging and the separately controlled A1 observations; this plan remains in progress.

The local-image Core follow-up now dispatches `image.acquire` for bare canonical digests before inspection or storage effects, rejects a returned digest mismatch, and includes local acquisition in result-only recovery. Independent Core review accepted the six-file increment after 46 focused tests, typecheck and formatting. The primary inspected its diff and ran the complete NanoCore Linux suite: 261 files and 2,870 tests passed in `temp/changes/202609101342020001-persistent_worker_environments/core-local-image-full-linux.log`. NanoHost streaming/retention/CLI implementation and exact A1 acceptance remain outstanding; this is not a complete-plan verdict.

Documentation commits `02daa009`, `508c8e78` and `b39274b0` precede their corresponding implementation. Native builders and independent review/audit resumed after quota restoration. The integrated Linux NanoCore check passed 261 files and 2,867 tests; Web passed 889 tests, and the regenerated Desktop Skill passed 20 tests. These observations precede the final active-resident replacement and administration discovery corrections and do not substitute for their focused regressions or real pinned OpenShell acceptance.

Final review corrected mutable image activation to consume the exact resolved Artifact B digest with `pullPolicy: never`, removed raw host handles from public storage status, and rejected an empty final administration response instead of publishing prior history. The running replacement regression demonstrated that interrupt acknowledgment is not terminal cleanup; the correction reuses the existing bounded Turn observer and scheduler lease owner, then accepts only the confirmed association or its exact proved release revision before ordinary successor admission. The corrected real-App fixture passed with a genuinely running predecessor and disk-backed configuration surviving safe reload. Restoring the pre-wait behavior made that same fixture fail; restoring the correction then passed all 104 tests across the 14 environment, administration and internal-Agent files. The initial fixture that seeded `awaiting_human` was invalid for this claim and is not deciding evidence. Goal/scheduler checks passed 25 tests, Turn routes passed 12, NanoCore typecheck/build and OpenAPI validation passed. Independent native review accepted the final preparation/activation bytes with no remaining finding in that scope, including its own 110 NanoCore, 11 schema, 3 client and 9 Web test passes. The final pre-wait falsifier fails on one executor dispatch instead of two, not a transient disposition. Evidence is retained in `temp/changes/202609101342020001-persistent_worker_environments/native-activation-review.md` and `active-replacement-final-falsifier.log`. Independent native intent audit accepted the exact 185-path staged implementation increment after direct boundary inspection, six independent tests and reconciliation of the named evidence. The audit permits an ordinary implementation commit; bootstrap, full R035 and A1 acceptance remain incomplete. Evidence: `temp/changes/202609101342020001-persistent_worker_environments/native-final-intent-audit.md`.

Next action: amend the runtime/storage owners for registry-independent empty-image startup and exact local image acquisition, then independently review/audit and commit those documents before dependent implementation. The independently accepted local increment is committed as `1448671e924f64a6cb5dcc79f079d95c2e9b4f83`; exact ARM64 App and Worker archives have been built locally, with no remote mutation. Independent Claude has resumed and source inspection confirms the mandatory Worker-image preload is an OpenKit startup rule, not a prerequisite for the current Core transport/readiness model. The ready-path exact-digest import gap affects both environment preparation and ordinary Worker creation after epoch restart. The engineer subsequently chose retaining verified images with capacity refusal and explicit cleanup. Claude and primary settled per-transaction native directory locking rather than a service-lifetime lock, so local archive import/removal does not require stopping Workers. A subsequent exact-upstream source inspection refuted the stronger assumption that removing Worker preload alone removes registry startup dependence: stock Gateway resolves and pulls the configured Supervisor image during construction. Claude confirmed the stock dependency, but the engineer then explicitly chose to retain OpenShell's existing behavior. The proposed separate Supervisor binary distribution and pull-policy change were withdrawn before implementation; the startup amendment removes only OpenKit's Worker-image preload prerequisite. Independent source probes also require bounded file-based archive handling and physical content accounting, including interrupted/unindexed bytes; the existing split content/index layout is retained rather than replaced by a new storage model. Pinned Linux Rust 1.97.1 directory-lock exclusion/release is directly proved in `host/image-store-concurrency-crash-probe.md` under the existing temp evidence bundle. Independent native reviewer and auditor accepted the final eleven-document amendment after direct inspection and verification of bounded I/O, physical capacity accounting, interrupted publication, exact cleanup and the accepted stock bootstrap exception. Lifecycle, documentation model (255 documents), generated index and diff checks passed. Operator references retain current preload instructions until implementation changes. Next, commit the owner amendment and implement the existing Host/Core seams; no deployment acceptance is inferred.

A1 acceptance must use the current owner's no-host-reboot boundary. The historical full Unit F runner explicitly reboots the host and is not the current gate; unrelated live services must remain intact. Latest Linux ARM64 Host release SHA-256 is `59d61e5699be4d4a5ca16c50a30ed4a6c363e3bfe7c67d3039ebb48e9ef420c8`, with exact input attribution in `temp/changes/202609101342020001-persistent_worker_environments/host/linux-arm64-build-current.json`. A later Host source change invalidates that binary for the new source. No live mounted-volume continuity PASS exists for this change.

Verification uses the lowest sufficient checks, then an expressly named isolated real-host fixture for destructive restart/replacement. Before host effects record exact target, prerequisites, cleanup and retained evidence; do not routinely restart A2 NanoHost or tear down unrelated services. Local Docker and authorized A1 test-host resources are available. Evidence belongs under the matching temp/changes/ bundle. Core-only backup cannot certify host data coverage; data persistence cannot certify process survival, native resume or all-roadmap completion.

Independent Claude performed the fresh direction observation after primary compaction, reading Intent Epoch 5, the current plan and actual owner diffs. Verdict: Continue with the broad catalog wording narrowed to technical effects and current result settlement separated from new dispatch/apply authorization. Evidence: `temp/changes/202609101342020001-persistent_worker_environments/consultant-admin-direction.txt`. Existing local-mode implicit administration remains with the identity owner; this team/server-mode clarification does not replace local authentication. Candidate preparation reuses immutable authored/resolved Artifacts, private administration Items and payload-bound human command confirmation/receipts; no Worker lease, proposal registry or operation journal is added. Independent reviewer and auditor accepted these authority amendments after correcting the local/server wording, fixed Tool count, ordinary Skill-selection boundary and candidate-build alternative. Document-model validation (255 documents), lifecycle/index checks and 16 root/spec tests passed. Production and live-environment acceptance remain pending.

A subsequent direct code probe refuted two implementation shortcuts: expiring conversation receipts or prose/ID markers cannot own durable administration entry, and create-only Artifact v1 plus MCP-specific Approval resumption cannot implement generic candidate revision and pause/resume without unnecessary expansion. Claude endorsed one immutable server-authored Thread.entryPath and separate authored/resolved v1 Artifacts, with the existing payload-bound human command confirmation/receipt path. Build identity comes from the pre-dispatch authored Artifact. Evidence: `temp/changes/202609101342020001-persistent_worker_environments/consultant-candidate-command.txt`. This reframes the over-specific ApprovalRequest implementation wording, not the user's required human approval, current authority or private audience. Independent reviewer and auditor accepted the corrected authority bytes, including private administration provenance, exact confirmation/receipt semantics and the record feature registry. Lifecycle, documentation-model, generated-index and diff checks passed.

## Current Integration Dependency

The implementation review found integration gaps that remain part of this change: explicit storage selection must survive scheduler admission and reach AEP work-slot planning; artifact export must follow that admitted slot; runtime-file credentials overlapping retained targets must be refused before reservation or mounting; image preparation must recover authenticated results without redispatching an uncertain build; and activation must use the existing drain, configuration and ordinary Turn owners. List/select/status alone do not satisfy environment replacement. Final image evidence must include the final Worker Shim bytes rather than reuse the earlier intermediate image digest.

The bounded private Operator requires the logical model's authored context-management policy and rejects dispatch at its threshold with `context_compaction_unavailable` until the owning compactor is implemented. Required policy validation is included; automatic compaction and migration of ordinary Quick Chat remain explicit implementation gaps under the context-compaction specification. There is no optional-policy compatibility fallback. The shipped configuration and any deployment used for this acceptance must contain a valid policy.

The bounded environment Operator is not full R035 completion. Generic configuration proposals, invalid-source diagnostics and general reload/restart classification remain unavailable under the Assistant owner. Agent discovery, exact Server Agent file identity/revision and image preparation are required for this change and may not depend on a model guessing file names or an optional profile.

A read-only A1 observation found NanoHost inactive and unrelated application containers running. Authorized host verification will use isolated named resources and preserve those services. No host mutation or pinned OpenShell persistence acceptance follows from this observation.

After the next primary compaction, independent Claude again returned Continue against the current Intent, checkpoint and implementation; its concrete recommendations were to parallelize the existing Task/Goal admission producer separately from runtime attachment and to reconcile preparation's effect identity with the authored Artifact before live effects. Evidence: `temp/changes/202609101342020001-persistent_worker_environments/consultant-integration-direction.txt`. Real-host observations must carry machine identity, not hostname alone. The primary's initial Skill-interface run on Node 24.18.0 observed 17 passing tests and one failing catalog-coverage check because the generated public API projection had not yet acquired the seven new operations; the check remains intact pending integration and regeneration.

Regeneration subsequently made the unchanged Skill catalog check pass: 18 interface tests passed, including unified Agent Skill reachability. Further public-contract changes require a fresh generated bundle and focused recheck. Independent review of the bounded internal Gateway path passed 18 tests across two files: below-threshold requests traverse the real dispatcher and PiAi admission with only the provider transport substituted, while threshold refusal occurs before provider contact. This closes the earlier unsupported `context_management` projection finding without claiming automatic compaction.

Two implementation probes require correction before preparation and activation can be accepted. First, image configuration belongs to the Server Agent manifest; neither an Agent profile nor a Thread currently owns an image override. Updating only a running Thread would revert on its next ordinary Turn. The engineer has been asked whether isolated task image choice is required or distinct Server Agent configurations are acceptable; dependent target-contract work is paused, with no implicit decision from elapsed time. Second, NanoHost drops a retained image result after Core acknowledgment, while Core's completed-effect map is process-local. An acknowledgment before durable image settlement can lose the resolved digest during restart. Independent Claude is examining a durable settlement through the existing Artifact owner before acknowledgment; no new job or receipt-metadata registry is authorized by this checkpoint.

The fresh Claude direction check accepted a bounded immutable image-result row in existing Server SQLite after rejecting a Workspace/Turn-dependent Artifact write, expiring conversation receipts, a disposable image-cache index and overloading WorkerStorageBinding. Source deletion must not prevent acknowledgment of an already accepted result. The amended volume and transport owners specify persistence before acknowledgment, exact duplicate/conflict handling, no replay, independent candidate authorization and no command journal. Evidence: `temp/changes/202609101342020001-persistent_worker_environments/consultant-image-settlement.txt`. Residual risk: failure of this small local settlement write follows existing definitive-result rejection semantics and can invalidate the NanoHost epoch, interrupting resident Workers; no availability fallback silently discards the result. Independent amendment review and audit remain required before implementation.

Independent Host re-review refuted the builder's claimed frozen local gate: current `statvfs` field arithmetic failed macOS compilation with nine type mismatches. The Linux release rebuild was interrupted with exit 130 during dependency resolution; no new binary or live result was produced. Corrected platform arithmetic, fresh exact-byte gates and re-review are required before replacing the historical binary evidence.

Native builder/reviewer/auditor contexts exhausted their provider quota during the remaining integration. The primary resumed sole write ownership of their stopped slices. The engineer explicitly authorized two fresh independent Claude Code contexts for review and audit; the existing consultant remains ineligible to accept its co-designed work. The reviewer also falsified the consultant's schema-only late-Artifact proposal against Core protocol and `createArtifact`/`createItem`: a terminal Turn cannot be rewritten. The owner now requires a fresh request and ordinary recovery Turn, proved causation to the original authored candidate, a unique resolved Artifact, no build replay and no automatic baseline rebase. Final documentation review is against these corrected bytes, not the superseded consultation inference.

Direct inspection of mode-entry-routes.ts and the Assistant specification's implementation projection confirms that the shared Internal Agent Loop and private administration entry remain unimplemented; the current fallback calls callQuickChatProvider directly. Independent Claude recommends delivering storage and real public Web/Skill operations here, with Desktop Agents making actual reuse choices, and tracking internal Operator integration under its runtime/privacy owner rather than inventing a bespoke Assistant runtime. The engineer chose to include those prerequisites, as preserved in Intent Epoch 4. A dedicated builder now implements the minimal accepted shared loop and private administration entry; image, host, shim, Core and public operation work continue independently. No internal Operator readiness claim is made before that path is implemented and exercised.

Fresh independent Claude review (`d121de9a-636d-4227-a4e4-03eb5acdcc3a`) and audit (`7e523d7c-b038-47c4-a8d8-63eda40276c7`) found local owner-header, transport-restatement, acceptance, busy-recovery and docs-before-code projection defects. The primary corrected those bytes. Audit also refuted the proposed non-disruptive preparation claim under settlement database failure: definitive non-204 result rejection can fence the complete epoch. That collateral consequence is not engineer-accepted; the previous residual-risk proposal is superseded pending a smaller local failure design. The consultant is comparing actual result carriage and retry behavior; dependent settlement implementation and documentation commit remain pending independent acceptance. No remote mutation has occurred. The smallest reconnect probe was refuted: `closePhysicalConnection` only fences in-memory authority, and database failure may also prevent successor admission/readiness. The engineer is now asked to choose explicit degraded lifecycle deferral with existing Workers retained, or bounded retries followed by epoch termination. Neither choice is inferred before the reply.

The primary added a deciding purge regression proving an unfinished Goal reference previously allowed deletion, then routed purge admission through existing Goal and scheduler reference owners. After correcting Workspace database setup in the fixture, the environment-operations and Turn-factory checks passed 40 tests; NanoCore typecheck and focused formatting passed. Independent source review is pending. The primary subsequently observed local NanoHost `cargo fmt --check` and `cargo clippy --locked --all-targets --all-features -- -D warnings` both exit successfully. The exact Linux ARM64 Host release rebuilt successfully and reports version 0.1.0; its binary hash and source digest are recorded in the temporary host evidence, with independent review and real-host acceptance still pending.

## Intent Epoch 4

The engineer explicitly chooses to implement the internal Agent prerequisites in this change after being informed that the shared Internal Agent Loop and private administration entry are missing. Source: user reply "Implement the internal Agent prerequisites in this change as well" on 2026-09-10. Deliver the minimal shared loop and private administration entry needed for the same environment operations and Agent reuse proposals, under their existing accepted runtime/Assistant/privacy owners. Do not replace the internal runtime with a Worker harness or silently enable unrelated external tools. This supersedes the pending dependency-scope question above; internal Operator integration is required for this plan's completion.

## Intent Epoch 5

The engineer explicitly states that Assistant/Operator system operations depend on the requesting user's valid administrator token. Technical staff or engineers administer the team deployment and decide system changes, including technical configuration affecting system behavior; ordinary users such as lawyers, doctors, marketers or designers ask that administrator rather than authorizing such changes themselves. Workspace membership and ordinary human confirmation do not grant system-management authority. The model never receives the token secret or acts through an independent privileged service identity. Routine work using already admitted resources remains distinct from technical configuration changes. Source: engineer clarification during implementation on 2026-09-10.

## Intent Epoch 6

The engineer accepts using a distinct Agent configuration for an isolated task environment after being informed that the current image owner is the Server Agent manifest and changing it affects every later admission using that Agent. Source: user reply "Use an independent Agent configuration" on 2026-09-11. Keep that existing owner, use exact configuration-file SHA compare-and-swap, state shared Agent impact explicitly, and use ordinary Agent selection with separately authorized retained storage reuse. No per-Thread image override or automatic Agent cloning is required. This resolves the pending granularity question in the checkpoint; the primary amends the existing owner and independent review/audit precede dependent implementation.

Following the restored Claude quota, independent reviewer and auditor both accepted the final six-document amendment against actual bytes, including the explicit image-result delivery-deferral exception and Failure Matrix alignment. The auditor independently ran the purge regression suite: 10/10 passed. Lifecycle/model/index and diff checks pass. This clears the documentation gate only; preparation/activation, final code review and real-host acceptance remain outstanding. The primary also reproduced the handed-off work-slot disagreement in a runnable regression, then unified planner and reservation selection through the existing binding owner; 6/6 storage-binding tests and NanoCore typecheck pass. Review findings on inherited Goal revision, independent-review lineage and failed fresh reservation still require correction or an evidence-backed disposition.

## Intent Epoch 7

The engineer explicitly chooses to pause subsequent NanoHost lifecycle operations and preserve running Workers when an environment-preparation image result cannot be persisted in NanoCore. Source: user reply "Pause lifecycle operations and retain running Workers" on 2026-09-11. This failure alone must not trigger epoch termination after a retry limit; lifecycle creation, closure and output collection may wait for database recovery. The existing independent connection, identity, member-failure and containment boundaries remain strict. The primary amends the existing result-delivery owner with exact empty 503 deferral, same-result one-second redelivery and truthful recovery-required product status, without effect replay or another queue. Independent review/audit must inspect these final owner bytes before the corresponding implementation.

## Intent Epoch 8

The engineer directs NanoHost to start and establish communication without loading Worker images, and clarifies that future public base-image publication through GitHub registry or Docker Hub and future private registry registration must not become prerequisites for system startup. NanoCore or preferably NanoHost should hold image content locally for building and running environments. Source: engineer startup and image-store clarifications on 2026-09-11. Preserve NanoCore orchestration and image declarations while NanoHost owns verified local image bytes; registry access is an acquisition source rather than startup authority. Empty-image readiness does not bypass substrate, authentication, containment or per-workload image verification. The current scope supplies local archives and reuses exact retained content; private-registry credentials and egress configuration remain future work. Image retention and capacity policy await the explicit engineer reply and are not decided by this epoch.

## Intent Epoch 9

The engineer explicitly selects retaining all verified images, refusing new admission at the existing 200 GiB bound and recovering capacity through administrator cleanup, with running Workers unaffected. Source: user reply "Adopt: retain images, explicit cleanup when full" on 2026-09-11. This resolves the retention question recorded in Intent Epoch 8 and replaces automatic image eviction. Exact local image removal does not imply that unavailable registry/build sources can reproduce it and does not remove retained working volumes.

## Intent Epoch 10

The engineer clarifies that the image-store capacity must be user-configurable rather than fixed at 200 GiB. Source: user request "The 200 G limit should be configurable by the user" on 2026-09-11. Preserve the previously accepted retain-all policy and use 200 GiB only as the initial default. Lowering capacity must not delete existing images or interrupt Workers; it controls subsequent image growth.

## Intent Epoch 11

After being informed that stock OpenShell may fetch its Supervisor from GHCR before readiness, the engineer explicitly declines separately distributing the official Supervisor binary and accepts OpenShell's default behavior. Source: user reply "No need to do this; we accept OpenShell's default behavior" on 2026-09-11. Preserve the existing pinned stock Supervisor bootstrap. The earlier registry-independence intent is therefore scoped to OpenKit's own Worker-image preload and local image reuse, not removal of OpenShell's supported external bootstrap dependencies. No additional Supervisor packaging or stock image-pull-policy change is part of this increment.

## Intent Epoch 12

The engineer selects private local aliases plus mandatory digest verification for retained-parent builds instead of maintaining a patched Buildx. Source: user reply "Adopt private local alias + mandatory digest verification" on 2026-09-11. NanoHost exclusively owns digest-derived addressing in its private backend; the verified content digest remains authoritative and a mismatched resolved digest must be rejected before build execution. Preserve unchanged authored Dockerfiles, strict policy, retained data, fixed external authority and distinct preparation/activation. Amend the source-owning specification, independently review and audit it, then implement and verify the selected route; the previously failed build is not replayed.
