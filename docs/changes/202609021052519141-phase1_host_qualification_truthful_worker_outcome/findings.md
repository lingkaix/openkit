# Findings

This report records non-authorizing findings discovered while reconciling the engineer handoff with current repository authority. The 2026-09-06 isolated preservation preparation retains these findings and their historical evidence without treating their old candidate identities as current admission. The finite source corrections are locally preserved on `codex/final-main-preservation`; R110 remains deferred, no A2 recovery or qualification effect is authorized here, and R005 remains pending fresh combined-candidate review and exact-candidate real-use evidence.

## Follow-up Index

- [x] `PHQ-FND-001` [closed] Roadmap was missing the engineer-directed pathway and R109 definition
- [x] `PHQ-FND-002` [closed] Worker command identity can collide and replace another session's durable row
- [ ] `PHQ-FND-003` [open] The proposed R005 support boundary omitted the shipped OpenCode path
- [x] `PHQ-FND-004` [closed] Codex fallback Responses serialize disabled reasoning as null
- [x] `PHQ-FND-005` [closed] Trusted Worker Responses tools were not normalized to the existing message-anchored form
- [x] `PHQ-FND-006` [closed] Unit F conflated an immutable image ID with a Dockerfile-resolvable candidate reference
- [x] `PHQ-FND-007` [closed] Unit F did not reject NanoHost boot activation before dispatching F3
- [x] `PHQ-FND-008` [closed] Unit F attributed post-reboot Docker lazy initialization to F4
- [x] `PHQ-FND-009` [closed] NanoHost certainty-loss report can be immediately pruned across artifact kinds
- [ ] `PHQ-FND-010` [deferred] R110 and R109 lack the Accepted B1 deployment-host owner
- [ ] `PHQ-FND-011` [open] Worker Shim rejects a valid Gateway multi-route AEP
- [ ] `PHQ-FND-012` [open] Unit F creates durable work before checking the candidate Agent Manifest binding
- [ ] `PHQ-FND-013` [open] NanoHost advances the first-fence clock after a hard-bound failure
- [ ] `PHQ-FND-014` [open] A2 retained a NanoHost Token without its IntegrationIdentity row
- [ ] `PHQ-FND-015` [open] Current main has six unrelated local test failures

## [closed] PHQ-FND-001 — Roadmap was missing the engineer-directed pathway and R109 definition

- **Observation:** The engineer handoff directs an immediate sequence containing R110, R109, and R005, but current `docs/roadmap.md`, remote branches, and pull requests contain no Near-Term Track and no R109 or R110 outcome definition.
- **Impact:** The handoff is sufficient user intent to execute defined R110 and R005 work, but implementation cannot invent R109 or silently rewrite the Roadmap's outcome inventory and ordering projection.
- **Evidence:** Direct inspection of current `docs/roadmap.md`, Git refs, and GitHub pull requests found R005 but no Near-Term Track, R109, or R110; the fresh-context direction verifier independently returned `Continue` for R110 and R005 and prohibited inventing R109.
- **Owner:** The engineer and `docs/roadmap.md` own the missing outcome and ordering decision.
- **Next action:** None. Transition history: the fresh-context direction check accepted continuing only defined work and moved the missing outcome outside this plan. Commit `7218069` then added the Accepted Execution Pathway and exact R109 and R110 outcomes to `docs/roadmap.md`; commit `ade57e1` merged those bytes into this branch.
- **Closing verdict:** Closed because the Accepted Roadmap now owns the previously missing outcomes, ordering, and blockers; R110 and R109 remain deferred behind B1 rather than being inferred by this plan.
- **Closure evidence:** `docs/roadmap.md` defines the current Execution Pathway, R109, R110, and B1 at commit `7218069`; the current branch contains that commit through merge `ade57e1`.

## [closed] PHQ-FND-002 — Worker command identity can collide and replace another session's durable row

- **Observation:** The gateway derives `worker-command-${sequence}` independently per registered session, restart rebuild derives the next sequence from only outstanding rows, `worker_control_commands.command_id` is a global primary key, and enqueue uses `INSERT OR REPLACE`.
- **Impact:** One session can replace another session's command row or reuse its own acknowledged identifier after restart before complete-lineage delivery and acknowledgement compare-and-set logic runs, so the accepted CAS direction alone cannot guarantee truthful command ownership.
- **Evidence:** Independent authority review identified the counterexample in `apps/nanocore/src/runtime/worker-control-gateway.ts`, `apps/nanocore/src/runtime/worker-control-commands.ts`, and the `worker_control_commands` schema; direct inspection confirmed all four premises.
- **Owner:** `docs/specs/20260703-worker_control_protocol.md` owns command identity and durable delivery semantics, while the existing scheduler and worker-control rows remain the only transition owners.
- **Next action:** None. Transition history: the Consultant required a domain-separated SHA-256 identity on exact lineage, kind, and sequence, non-replacing exact replay, all-status restart high-watermark, and memory mutation only after the durable winner; the accepted owners and plan carried that direction. The final frozen bytes implement creation, replay, terminalization, restart, and lease-race regressions, and the independent Reviewer and Verifier accepted the actual bytes on 2026-09-02. Resolved; retain the regressions as the executable projection.
- **Closing verdict:** Closed because the complete-lineage command identity, non-replacing exact replay, terminal-aware restart, and lease-serialized state transitions remove the global-key replacement and terminal-revival counterexamples without adding another owner.
- **Closure evidence:** `apps/nanocore/src/runtime/worker-control-gateway.ts`, `apps/nanocore/src/runtime/worker-control-commands.ts`, `apps/nanocore/src/runtime/worker-control-rebuild.ts`, `apps/nanocore/src/scheduler-records.ts`, and their focused regressions prove the corrected identity and transition path; the independent Reviewer and Verifier accepted the final frozen bytes on 2026-09-02.

## [open] PHQ-FND-003 — The proposed R005 support boundary omitted the shipped OpenCode path

- **Observation:** The initial reliability projection limited R005 to OpenShell/Codex and listed OpenCode as deferred, while accepted authority includes OpenCode. The final frozen NanoCore and Harness bytes now use a non-reusable one-Turn binding and durable Worker command interruption, but exact-candidate A2 OpenCode success and restart/reconnect evidence do not yet exist.
- **Impact:** The local supported OpenCode implementation is independently accepted, but R005 cannot truthfully close until the exact candidate proves that behavior on A2.
- **Evidence:** Independent authority review found the original conflict; focused regressions cover the corrected local product bytes, and the independent Reviewer and Verifier accepted the implementation on 2026-09-02 while retaining the A2 closure predicate. The former Unit F admission is now invalid because R110 removal changed the deciding runner bytes and restored the A1-bound host assertion.
- **Owner:** The Worker Turn Reliability Envelope owns the R005 supported surface, while the OpenCode adapter spec owns its native boundary and the Worker Control Protocol owns its bounded-turn command behavior.
- **Next action:** Preserve the corrected product bytes in one pushed candidate. Run the exact-candidate A2 OpenCode successful turn and restart/reconnect durable interrupt checks only after B1/R110 supplies the A2-compatible host-admission path, PHQ-FND-014 has an engineer disposition, and the final deciding bytes receive renewed Harness Admission; close this finding only after independent acceptance of that evidence. Transition history: the Consultant required one non-reusable Turn-private Harness binding, durable command polling as the sole interrupt-delivery owner, and A2 success plus restart/reconnect interrupt evidence; the local mode admission, rejection, cleanup, and focused regressions were independently accepted.

## [closed] PHQ-FND-009 — NanoHost certainty-loss report can be immediately pruned across artifact kinds

- **Observation:** Exact candidate `f75537a1105ce697acf7dd7e6c30798604ec4e99` implements the five specification-owned certainty-loss labels and passed the complete Unit F on A2, but its sole OpenCode attempt reached exact-image container create and start before NanoHost reported terminal `sandbox.create` with no status and fail-stopped without retaining the expected report. The evidence root already contained 20 prior-epoch disposition notes, and `EpochEvidenceWriter::prune` sorts complete names whose kind prefix places every new `epoch-invalidation-*` before every `prior-epoch-disposition-*`, so the just-written report is immediately selected as the oldest artifact and deleted.
- **Impact:** The fail-stop remains truthful and A2 recovered, but the exact Ready failure branch remains unavailable despite the corrected report fields, so PHQ-FND-009 and the OpenCode gate cannot close. Any retention correction again changes NanoHost product bytes and requires a new exact candidate rather than retrying `f75537a1105ce697acf7dd7e6c30798604ec4e99`.
- **Evidence:** Exact-candidate Unit F result `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-f75537a-attempt1.json` is mode `0600`, has SHA-256 `340607e315011c151ec0d42ee439babf14eee8511b48be04969bb896d34c49f8`, and was independently accepted with F1-F4 and Aggregate PASS. The sole ignored OpenCode attempt directory `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-opencode-success-f75537a-attempt1/` retains the generic failure SHA-256 `eb54b2439b16e521bac51fdea997124da21cc085de01f323ac694bd78cbf4310`, bounded exact-image create/start events SHA-256 `77e532b44b403199110c6290063e5a04f48d167cf0009383f9d2b88c3dd9b823`, and fixed monitor status SHA-256 `0b5457131ba9476f26ec6a4957c61ddd3a5d83e80b9bcc92ec46a3d702cd79cb`. The lowest regression filled the shared cap with 20 prior notes and failed because the newly exported invalidation path did not exist; the cross-kind suffix correction now passes both note-to-report and report-to-note directions, the existing retention/redaction test, 90 isolated-root unit tests, both integration tests, all-feature Clippy, release build, formatter, and diff check. A2 recovered through bounded successor fences and is healthy at `80:1:1:1`.
- **Owner:** `docs/specs/20260802-nanohost_runtime_and_transport.md` owns the private Epoch Invalidation Report and already requires uncertain-operation kind, sandbox and attempt lineage, exact certainty-loss point, and elapsed time; `apps/nanohost/src/openshell_client.rs`, `epoch_coordinator.rs`, and `epoch_evidence.rs` are the existing implementation path.
- **Next action:** Commit and push one new exact candidate on the same branch, rebuild and rebind the complete candidate campaign, and use one new A2 OpenCode attempt to read the owned certainty-loss label before deciding whether another product correction is needed. Do not run attempt 6 on the old candidate or change the OpenCode image, Worker, NanoCore, or Gateway pin without evidence from the new report. Transition history: the focused classifier, report-field, and lifecycle structure regressions pass; an isolated-root full NanoHost run passes 89 unit tests plus both integration tests; Clippy with all targets and features, release build with the lockfile, formatter, and diff check pass; six stale source checks were rebound to the existing synchronous process-entry boundary without changing product code; an independent Reviewer accepted the frozen implementation and a fresh Verifier found no counterexample to commit, push, and complete A2 candidate rebuild. The resulting candidate `f75537a1105ce697acf7dd7e6c30798604ec4e99` completed independently accepted Unit F before its sole OpenCode attempt exposed the cross-kind retention counterexample; an independent Consultant accepted the minimal validated `timestamp-sequence` suffix ordering direction and rejected mtime, JSON reads, an index, a persistent counter, or a new retention owner. Transition history: the correction was committed and pushed as `5f6b45b2d677e223bb9a5e9f18caa5db460c50ad`, exact-candidate Unit F passed on A2, and the sole fresh OpenCode-only attempt instead crossed successful container create and start before exposing the distinct pre-heartbeat Worker Shim defect in PHQ-FND-011. None.
- **Closing verdict:** Closed because the shared retention owner now orders both owned artifact kinds by their validated fixed-width timestamp and sequence suffix, its cross-kind regressions and independent acceptance cover the pruning counterexample, and the exact candidate was built and exercised on A2 without recurrence of the old missing-report mechanism.
- **Closure evidence:** Commit `5f6b45b2d677e223bb9a5e9f18caa5db460c50ad` contains the accepted suffix-order correction; exact-candidate Unit F evidence SHA-256 `92eed37199a13d8086509e5b09b72da9fe5b31afe37993695dc7ee4c5fb8f4f1` passed on A2, while the later OpenCode-only attempt retained exact-image create, start, die, and destroy events SHA-256 `d7a8ae3ae212d5771aff1d4fabe719a5c267daf02f51bc03e796ea535ed7b6bd` and was attributed to the separate Worker Shim route rejection rather than certainty-loss retention.

## [closed] PHQ-FND-004 — Codex fallback Responses serialize disabled reasoning as null

- **Observation:** Exact candidate `a35e1075a511a232f57b9f5bc07d885ea5e4b03b` reached Unit F F1 on A2, but Codex 0.144.1 failed before its first command because the Gateway rejected `reasoning: null` as `pi-ai Responses reasoning` even though the Codex fallback model path uses that value to represent disabled reasoning.
- **Impact:** The exact candidate cannot complete the required Codex Unit F matrix until the shared Codex Responses admission boundary treats `null` as semantic omission.
- **Evidence:** Attempt 1 failed before scenarios because its operator input expected an image tag while Docker recorded the same exact image ID as `Config.Image`; the corrected attempt 2 evidence at ignored path `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-a35e107-attempt2.json` has SHA-256 `20c486f809250d15339e0e446aefec9c434220237d668559b0e2f05b5c2054c8`, binds F1 runtime cleanup evidence, and links the restricted raw stream containing the exact rejection. Official Codex tag `rust-v0.144.1` shows fallback metadata disables reasoning while `ResponsesApiRequest.reasoning` serializes its `None` value as JSON `null`.
- **Owner:** The accepted pi-ai unified backend specification owns semantic-loss rejection, the Responses API specification owns supported passthrough, and `assertCodexResponsesRequestAdmission` is their existing shared implementation seam.
- **Next action:** None. Transition history: the one-condition correction and exact regression were locally green and independently accepted, candidate `9570c276e21503a3dcd1d74715f404d62b0bbbb3` was built and deployed on A2, and its Unit F attempt reached the next distinct tool-admission boundary without the prior reasoning rejection.
- **Closing verdict:** Closed because exact-candidate A2 execution proved `reasoning: null` passed the shared Codex Responses admission boundary while every non-null reasoning object remains subject to the existing strict validation.
- **Closure evidence:** Ignored evidence `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-9570c27-attempt1.json` has SHA-256 `c4dd0c8c814897ff988225d21274ef8d5afaf48efcaca34a18526f8bd947e89c`; its F1 runtime reached provider request processing, and restricted raw stream `/home/ubuntu/.openkit/workspaces/ws_16/evidence/backend/evb_ef4fe8e13208d96a0cc04036/raw/stream-0000.jsonl` has SHA-256 `f839188d8d82b43b9c59a7c2e8c3a48d4d45b0c7cfbf6d107e222928a2082efb` and contains the later distinct `pi-ai chat tools` rejection rather than the closed reasoning error.

## [closed] PHQ-FND-005 — Trusted Worker Responses tools were not normalized to the existing message-anchored form

- **Observation:** Exact candidate `9570c276e21503a3dcd1d74715f404d62b0bbbb3` reached Unit F F1 on A2 and passed the prior null-reasoning gate, but Codex 0.144.1 failed before its first command because the trusted Worker route validated its direct local `custom` and `function` tool declarations and then forwarded that top-level list unchanged to the pi-ai adapter path that represents such local tools through the existing `additional_tools` input prefix.
- **Impact:** Unit F cannot reach its requested long-running command until the trusted Worker Responses boundary joins its accepted direct Codex tool vocabulary to the already implemented message-anchored adapter representation.
- **Evidence:** Ignored attempt evidence `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-9570c27-attempt1.json` has SHA-256 `c4dd0c8c814897ff988225d21274ef8d5afaf48efcaca34a18526f8bd947e89c`, binds successful provider-call provenance and cleanup, and identifies F1 as failed; the restricted raw stream named in PHQ-FND-004 has SHA-256 `f839188d8d82b43b9c59a7c2e8c3a48d4d45b0c7cfbf6d107e222928a2082efb` and contains exact `unsupported_gateway_feature` and `pi-ai chat tools` markers. Official Codex tag `rust-v0.144.1` serializes its Responses tool plan directly through `ResponsesApiRequest.tools`, including `custom` freeform tools, while current Worker admission already accepts only the closed local tool grammar.
- **Owner:** The accepted pi-ai unified backend specification owns the message-anchored local-tool mapping, the Worker runtime provenance specification owns the trusted inference boundary, and `registerWorkerInferenceRoutes` is their existing join seam.
- **Next action:** None. Transition history: the one-seam normalization and exact regression passed the full adjacent 72-test slice, NanoCore typecheck, lint, build, and independent review; candidate `11a8d88a2111726e44f55ecd6c19b8d1f76b830b` was built and deployed on A2, and its Unit F coordinator completed F1 and entered the F2 blocked-create fault.
- **Closing verdict:** Closed because exact-candidate A2 execution reached the requested long-running command and the next fault scenario without the prior `pi-ai chat tools` rejection.
- **Closure evidence:** Ignored evidence `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-11a8d88-attempt1.json` has SHA-256 `7832565dbd78a593493cbfdcc3398e5eaf763dc4746097b7175c9e927e4823db`; product-safe A2 journal extract `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-11a8d88-attempt1-log-extract.md` has SHA-256 `2657907381dc783f35b5cdef1c09ffe8a155e3d49a5022d5904da3b7e2868afe` and shows the blocked CreateSandbox request, intentional NanoHost SIGKILL, and two exact terminal `sandbox.create` status 409 successors before a separate final system-Docker smoke failure.

## [closed] PHQ-FND-006 — Unit F conflated an immutable image ID with a Dockerfile-resolvable candidate reference

- **Observation:** Candidate `11a8d88a2111726e44f55ecd6c19b8d1f76b830b` completed the F2 product fault and recovery path, but the final system-Docker smoke interpolated the raw `sha256:90831e8d6f7ebf70260196a06eca8c2c33c9035e37abf0c06930793679794e5f` image ID into `FROM`, so Docker resolved it as a registry name and rejected the pull instead of building from the already admitted local candidate tag.
- **Impact:** The retained aggregate is FAIL because the harness stopped after its own invalid smoke input; it is not a product F2 verdict, and the changed deciding instrument must renew Harness Admission before another A2 gate run.
- **Evidence:** Ignored attempt evidence `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-11a8d88-attempt1.json` has SHA-256 `7832565dbd78a593493cbfdcc3398e5eaf763dc4746097b7175c9e927e4823db`; product-safe A2 journal extract `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-11a8d88-attempt1-log-extract.md` has SHA-256 `2657907381dc783f35b5cdef1c09ffe8a155e3d49a5022d5904da3b7e2868afe` and records machine identity, candidate pointers and image mapping, the F2 fault/recovery sequence, and the later system-Docker resolution of `docker.io/library/sha256:90831e8d6f7ebf70260196a06eca8c2c33c9035e37abf0c06930793679794e5f` followed by `pull access denied` and `insufficient_scope`; later NanoHost recovery was healthy at RuntimeTarget generation 20 with the App restart count still zero.
- **Owner:** `docs/verification-instruments.md` owns deciding-instrument admission and environment identity, while the NanoHost runtime specification owns the exact deployed runtime bytes and system-Docker smoke predicate.
- **Next action:** Commit and push the independently accepted correction on the existing branch, update the ignored build helpers to the new commit and tags, build the exact artifacts, bind the transition and Unit F helpers to their actual hashes, image ID, tag, and new output path, then transition and rerun on A2 without touching A1. None. Transition history: candidate `38eb7eb59c87fab7c18017e997b9e5c51af9e4fb` was built from exact source, deployed with a separately admitted immutable image ID and Dockerfile-resolvable tag, and two later runs crossed the corrected post-F2 smoke into F3 before failing on independent campaign-topology preconditions.
- **Closing verdict:** Closed because the driver now proves the running image ID and candidate tag independently, rejects raw IDs and mismatched tags, and exact-candidate A2 execution crossed the corrected smoke without the prior Docker pull failure.
- **Closure evidence:** The runner and self-check at candidate `38eb7eb59c87fab7c18017e997b9e5c51af9e4fb` had SHA-256 `3eaf3a8536d129a38db20106b6dc2c9afbd48eda11878a06679b76ce7b137865` and `e0ed29fb5e364abe1ea6f99972e9f1c0171035125c920797d901da968ee563ff`; ignored Attempt 1 result SHA-256 `63fc4f66f0886c13dc7c47baba107cb890e13ba535054afa9d081b8141f1d83a` identifies F3 rather than F2, and product-safe extract `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-38eb7eb-attempts1-2-log-extract.md` has SHA-256 `c4598cef775cede11f984df29d4479dac83546502ff49b70134ffacd34aa8c17`.

## [closed] PHQ-FND-007 — Unit F did not reject NanoHost boot activation before dispatching F3

- **Observation:** Candidate `38eb7eb59c87fab7c18017e997b9e5c51af9e4fb` first reached F3 with App policy `unless-stopped` and correctly rejected before reboot; after changing the App to `restart=no`, the next attempt rebooted A2 but a preserved `multi-user.target.wants` link automatically started NanoHost before the required Core-only observation. The runner rejected the active new-boot epoch only after the reboot because it did not preflight the unit's `UnitFileState`.
- **Impact:** The two retained F3 failures are campaign-configuration evidence rather than product F3 verdicts, and the missing preflight allowed an expensive reboot and downstream recovery work even though the required topology was already observably invalid.
- **Evidence:** Attempt 1 result SHA-256 `63fc4f66f0886c13dc7c47baba107cb890e13ba535054afa9d081b8141f1d83a` has no reboot, while Attempt 2 result SHA-256 `5a3bef512f4642df3ae09d85188bb12f6a0d334c427dbffb6ce86c2db6e10b07` records F3 after boot ID changed from `888945f0850143959fd5bcbeafd2206d` to `697a1f8fef1d489f8079eb497d464ce1`; the product-safe extract with SHA-256 `c4598cef775cede11f984df29d4479dac83546502ff49b70134ffacd34aa8c17` records both command sequences and later healthy recovery.
- **Owner:** `docs/verification-instruments.md` owns the observed-violation basis and renewed Harness Admission, while the existing Unit F runner owns this pre-effect topology observation; the NanoHost product unit remains static and does not own the operator's A2 wants link.
- **Next action:** Commit and push the independently reviewed `UnitFileState=static` preflight on the existing branch, rebuild and deploy one exact candidate, prove A2 has App `restart=no` and NanoHost `UnitFileState=static` while both are explicitly running and RuntimeTarget is ready/fresh-empty, then run a fresh complete Unit F attempt and close only from retained evidence. Transition history: the focused regression first produced 76 passed and exactly one intended failure, the minimal observation and early rejection then passed 77 of 77, and renewed Harness Admission passed 9 of 9 on runner SHA-256 `677ce5509d2d0ac591d921553f21f1bfc500c6e2f0fcfe1c38b02481d92494e5` and self-check SHA-256 `1527ef14207d26c7777b27a973100fd4329c6cd952531e579e3f2881c07a273c`. Transition history: candidate `5667f9f7d8b62f63f5dc67dfbe7234fab0848b6a` was built and deployed from exact branch bytes with App restart policy `no` and NanoHost `UnitFileState=static`; the retained A2 attempt completed the real F3 reboot, explicit recovery, cleanup, and adjudication with F3 `PASS`.
- **Closing verdict:** Closed because the runner now rejects boot activation before effects and exact-candidate A2 execution with the required static topology completed and passed F3.
- **Closure evidence:** Ignored result `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-5667f9f-attempt1.json` has SHA-256 `76d2c5c583b83f7470ffbaf45c70353cad5c44c00719c0b03ce1e6c74a43f83f`, binds A2 machine-id `34b95b09f8c1475ea242800759411fea` and candidate `5667f9f7d8b62f63f5dc67dfbe7234fab0848b6a`, and retains F3 `PASS` across distinct complete pre- and post-reboot baselines.

## [closed] PHQ-FND-008 — Unit F attributed post-reboot Docker lazy initialization to F4

- **Observation:** Candidate `5667f9f7d8b62f63f5dc67dfbe7234fab0848b6a` passed F1, F2, F3, and normal lifecycle, but F4 alone failed strict system-Docker equality because its first default-network Docker smoke after the F3 host reboot lazily initialized `docker0` and nftables state before the F4 post-baseline.
- **Impact:** The retained aggregate is FAIL even though every product fault and recovery predicate completed, so Unit F cannot be accepted until the deciding harness stabilizes the owned host baseline after F3 and a new exact candidate passes A2.
- **Evidence:** Ignored result `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-5667f9f-attempt1.json` has SHA-256 `76d2c5c583b83f7470ffbaf45c70353cad5c44c00719c0b03ce1e6c74a43f83f` and records only F4 plus Aggregate as FAIL; ignored product-safe attribution `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-5667f9f-attempt1-baseline-attribution.md` has SHA-256 `d41758c1bf51a525efaacdf33e4e674d5ff07441da37c95bb8acc4c0b227953f` and proves the first post-reboot smoke changed only the normalized `docker0` and `nft` component hashes while a second identical smoke changed nothing.
- **Owner:** `docs/verification-instruments.md` owns deciding-instrument admission and product-safe evidence, while the existing Unit F runner owns the baseline capture, F3 cleanup boundary, Docker smoke, and strict F4 system-Docker equality predicate.
- **Next action:** Retain strict F4 equality, renew Harness Admission for the minimal runner correction that executes the existing smoke once after successful F3 cleanup and retains only four validated component hashes, obtain independent review and verification, then commit and push one new exact candidate on the same branch and rerun complete Unit F on A2 without touching A1; close only from the retained exact-candidate result. Transition history: independent review accepted the exact-own-key and aggregate/component oracle, candidate `995590da873572e7e385b93efdafab47d103ccf1` was built and deployed from the same pushed branch, and its sole A2 attempt passed F1, F2, F3, F4, normal lifecycle, and Aggregate while preserving the F3 post-baseline exactly through F4.
- **Closing verdict:** Closed because the admitted runner stabilizes system Docker after F3 without weakening F4, and exact-candidate A2 execution retained strict aggregate and four-component equality with all Unit F verdicts PASS.
- **Closure evidence:** Ignored result `temp/changes/202609021052519141-phase1_host_qualification_truthful_worker_outcome/a2-unit-f-995590d-attempt1.json` has SHA-256 `937d6f2f3d00442c712f82907268e64df328096be04829602644e62ba09cbc29`, mode `0600`, and binds A2 machine-id `34b95b09f8c1475ea242800759411fea`, candidate `995590da873572e7e385b93efdafab47d103ccf1`, admitted runner SHA-256 `36b763d424facd7687176004a08d7f81526c25ba45f6b604460a178cc86e3b54`, F1-F4 PASS, normal lifecycle PASS, Aggregate PASS, and exact F4 component equality; independent read-only verification recomputed the identities and found A2 healthy at RuntimeTarget `59:1:1:1`.

## [deferred] PHQ-FND-010 — R110 and R109 lack the Accepted B1 deployment-host owner

- **Observation:** The engineer's superseding 2026-09-03 handoff states that no Accepted document owns deployment host requirements and installation ownership, names a new B1 specification as the mandatory blocker, and prohibits beginning or extending R110 or R109 until that artifact is Accepted.
- **Impact:** Intent Epoch 1 combined R110 with R005, but current work may continue only the unblocked R005 correctness surface and must not treat earlier R110 implementation or A2 qualification evidence as authoritative completion.
- **Evidence:** `/home/ubuntu/handoff-a2-testenv-20260903.md` names B1 and its required decisions; direct inspection confirms `docs/specs/20260802-nanohost_runtime_and_transport.md` excludes operator installation and `docs/specs/20260829-release_management.md` excludes deployment automation, while no Accepted B1 specification exists in the repository.
- **Owner:** The engineer and the future Accepted B1 deployment-host-requirements and installation-ownership specification own the missing decisions.
- **Next action:** This finding began deferred when Intent Epoch 2 adopted the superseding handoff and removed blocked R110 and R109 from the executable boundary. Deferred to the engineer and future B1 owner; activate when a new B1 specification reaches Accepted status and explicitly authorizes R110 or R109 implementation. R005 implementation may remain in a pushed candidate, but its required A2 Unit F closeout also waits because the restored exact-host assertion rejects A2 and cannot be bypassed without the B1/R110 authority path.

## [open] PHQ-FND-011 — Worker Shim rejects a valid Gateway multi-route AEP

- **Observation:** Exact candidate `5f6b45b2d677e223bb9a5e9f18caa5db460c50ad` received a valid AEP v4 Gateway package with four allowed routes, but the shared Worker Shim ignored `preferredLogicalModelId` and rejected every package whose route count was not exactly one before supply, Git, Integration, heartbeat, worker control, or native launch.
- **Impact:** The shipped OpenCode bounded-turn path cannot start whenever the allowed logical-model set contains multiple members, so PHQ-FND-003 and R005 cannot close despite truthful cleanup and healthy A2 recovery.
- **Evidence:** Mode-`0600` exact-attempt state SHA-256 `ed0fa4db57b4fd536cc5ca6673d97c47fdf723d95c5498a81cbaf93c1ea9f1` proves one cleaned failed Turn with zero accepted heartbeat and worker-control records; redacted exact-package shape SHA-256 `2d0abd1f0f46db97c434f9a7b757550bac52efe8b0435f744d8b789ea6c21e10` proves `llm.mode=gateway` and route count four; exact-image dry-run reproduced `Worker shim requires exactly one resolved LLM route`; and independent Consultant, Verifier, and post-compaction direction check accepted the causal chain.
- **Owner:** `docs/specs/20260616-agent_environment_package.md` owns the preferred and allowed logical-model contract, while `docs/specs/20260629-worker_runtime_communication_model.md` and the shared Worker Shim own selecting one valid preferred route for an adapter.
- **Next action:** Add the lowest regression for a non-first preferred route in a multi-route Gateway package plus fail-closed missing and ambiguous cases, correct only the shared resolver, synchronize the stale one-route projections, obtain independent acceptance, then rebuild and run a fresh exact-candidate A2 OpenCode success and restart/reconnect campaign without retrying candidate `5f6b45b2d677e223bb9a5e9f18caa5db460c50ad`. Transition history: the valid four-route regression first failed only at the old exact-one guard; the minimal shared selection correction, strict routing-mode, missing, empty, and non-unique preferred rejection, non-Gateway exact-one preservation, required strict-fixture cutover, and synchronized stale projections now pass 196 Worker Shim tests plus focused typecheck, build, lint, shell syntax, documentation-model, specification-lifecycle, and diff checks. Transition history: an independent Reviewer accepted the latest tracked bytes, and ignored exact-package stand-in evidence SHA-256 `4f343b23e5a0b90b22d9473bbbbc79b33631b73e657afc3a9490a3272a8cc4b9` proves the historical four-route package crosses the old failure under the reviewed built Shim with no network or residual container; A2 was restored to active RuntimeTarget `96:1:1:1` without any A1 lifecycle action. Current action: preserve the corrected bytes in one pushed candidate, but keep every A2 product gate frozen until B1/R110 supplies an A2-compatible host-admission path and PHQ-FND-014 has an engineer disposition.

## [open] PHQ-FND-012 — Unit F creates durable work before checking the candidate Agent Manifest binding

- **Observation:** Candidate `c478f4dd984d758772dcd565b963bcf9041b76d1` updated NanoHost's required Worker image set but left the live Codex and OpenCode Agent Manifest files pinned to prior image digests, while Unit F created Workspace `ws_53`, Thread `th_43`, and a running Turn before `waitForTaskLineage` compared the frozen AEP image with the candidate digest.
- **Impact:** The sole c478 Unit F attempt was consumed by campaign setup drift before it exercised the corrected Worker Shim, so it is neither a Worker product verdict nor reusable evidence; the stale Codex AEP also left a running Turn, a created AgentSession, a stale startup-timeout lease, and a cleanup-pending backend after the only permitted recovery successor repeated epoch creation failure.
- **Evidence:** Mode-`0600` Unit F result SHA-256 `deb2ac3259ae6ce8601fe55e118a0103601ee6ce15b236e001883898ea4f9e19` has `identity: null`, no scenario rows, and failure at F1; retained mode-`0600` recovery evidence SHA-256 `6201b3f1cc6bb1e0364122ac6ca47d8097ca4f6542869b05809ae37535648f71` binds A2 machine-id, all four c478 pointers, the revision-CAS Codex rebind and backup, the sole successor invocation, the product-owned `epoch-creation-failure` report SHA-256 `3fe16e28de9cda427d758812e2318d1a02f07b1135b262914f658892258b64b8`, the exact remaining owner states, and the still-healthy restart-zero App; independent review accepted the recovery helper before execution and independent verification required stopping after a repeated epoch creation failure.
- **Owner:** `docs/verification-instruments.md` owns pre-effect instrument admission and truthful setup failure, while the Unit F runner owns its candidate identity checks and the existing Config API owns authored Agent Manifest and active runtime snapshot observations.
- **Next action:** Preserve the pre-effect product checks in one pushed candidate on the same branch, then update the coordinated transition to rebind both Codex and OpenCode manifests with revision CAS, compensation, one safe reload, and exact readback only after the two current blockers clear. Do not retry c478, restart A2 NanoHost again, interrupt the created AgentSession, or repair product databases. Transition history: the focused regression first failed because RuntimeTarget was read before the manifest; the minimal driver preflight rejects both an incorrect JSONC manifest digest and a non-no-op active runtime snapshot with zero host commands and admits the exact JSONC manifest plus strict no-op dry-run to the existing RuntimeTarget seam. The former admission bound runner SHA-256 `1566f00d1705e29a05606e9941cfe40e0e30549099a760f0035d0c12ee328198` and self-check SHA-256 `701ede5b72f62f5d90372aeded2d656ac89d1c83841c7f7e23635c2051c889d5`; R110 removal changed those bytes to `83787cd17123056f401dfb2e67dfa2bc8ca5b2c62e6b9633d016d1f76ff2ead1` and `985a4d6fc6870f377478fdbe2f044e85a858537f07c78aff2e187f09fd32bf0c`, so the admission is invalid. Current action: keep A2 frozen until B1/R110 supplies its host gate, PHQ-FND-014 receives an engineer disposition, and exact final deciding bytes receive renewed Harness Admission.

## [open] PHQ-FND-013 — NanoHost advances the first-fence clock after a hard-bound failure

- **Observation:** The sole permitted c478 NanoHost successor began about 33 minutes after the recorded first fence, correctly exceeded the inclusive 300-second hard bound, emitted one `epoch-creation-failure` report, and stayed non-ready, but its failure path called `record_fence_started(SystemTime::now())`, whose `create + truncate` implementation replaced the original timestamp with `07:34:49`.
- **Impact:** A later start could measure from the younger replacement timestamp and advertise readiness after the original hard-bound violation, contradicting the accepted rule that the defect stays non-ready. Candidate c478 is consumed and cannot be retried; A2's first timestamp is already irrecoverable, no accepted marker-repair path exists, and the accepted resolution is the existing terminal decommission and exact-pair re-enrollment owner.
- **Evidence:** Product report SHA-256 `3fe16e28de9cda427d758812e2318d1a02f07b1135b262914f658892258b64b8` records complete `epoch-creation-failure`; retained recovery evidence SHA-256 `6201b3f1cc6bb1e0364122ac6ca47d8097ca4f6542869b05809ae37535648f71` binds failed NanoHost and RuntimeTarget `97:1:0:0`; direct read-only A2 observation found the mode-`0600` marker value and mtime at `07:34:49`; source inspection proves the hard-bound rejection calls the truncating export path; and an independent Verifier classified this as an in-scope product blocker.
- **Owner:** `docs/specs/20260802-nanohost_runtime_and_transport.md` owns the fence-to-ready interval, the inclusive hard bound, and non-ready failure semantics; `apps/nanohost/src/epoch_evidence.rs` is the existing unique temporary marker owner.
- **Next action:** Preserve the independently accepted create-once marker, absolute readiness deadline, and post-acknowledgement consumption correction in one pushed candidate. Transition history: the marker regression first failed with second-timestamp bytes replacing the first, and the existing WP-3c ordering regression then failed because local Gateway health consumed the marker before authoritative readiness. The minimal correction now uses `create_new`, carries the clock through outer admission, derives an absolute monotonic deadline from the remaining interval, encloses readiness request construction, send, and the complete response within `timeout_at`, rechecks before send and after the response, clears only from the post-`204` dispatch seam, and fail-stops hard-bound or marker-consumption failures through the existing partial-start invalidation owner. Behavioral checks prove rejected or late readiness preserves the marker, an exact in-bound acknowledgement consumes it, and a marker-removal failure retains the marker while the existing `PartialStart` path exports `epoch-creation-failure` before fencing. The complete isolated NanoHost suite passes 93 unit tests plus both integration tests without reading or changing A1 runtime state; independent Reviewer and Verifier accepted the exact correction and found no remaining product blocker. Intent Epoch 3's exact-pair decommission route is superseded by PHQ-FND-014's missing identity fact; current action is to keep A2 failed and frozen until both B1/R110 supplies the A2-compatible host-admission path and the engineer selects a replacement identity and data-recovery boundary.

## [open] PHQ-FND-014 — A2 retained a NanoHost Token without its IntegrationIdentity row

- **Observation:** The pre-effect candidate `3540925b23cda4c16f310893442a202085e44260` transition stopped after its complete backup and before decommission because stopping the already-failed unit did not satisfy the helper's immediate `inactive` assertion. Read-only inspection of the retained backup and live Core database then found the configured pair has no `nanohost_integration_identities` row while Token `01a05800-2e02-787c-b0e6-fb192380113b` remains active; every available candidate-keyed Core snapshot retains the same orphan lineage.
- **Impact:** Intent Epoch 3's exact-pair decommission and re-enrollment route cannot complete under the accepted owner because re-enrollment must reject retained Token history without its identity row. Before the local correction, the orphan Token could still authenticate and update last-use metadata, admission could proceed toward RuntimeTarget allocation and process-local authority, issuance and rotation could expand the orphan lineage, and decommission could revoke Tokens before an unchecked zero-row identity update and return success across the host no-rollback boundary even though no retained identity became decommissioned.
- **Evidence:** A2 backup result SHA-256 `e9a192ec659d8eafe9be1ffc792dfc774589a5dea65371c97bbafa1524877810`, backed-up Core snapshot SHA-256 `adfd6f99dc5c1139842b5c5cc55525918bcd719544763322728fff32f975db68`, and redacted prestate SHA-256 `8b8f4e58cffbd671376d274f95d8465c1fffbd12e277cddb88911219a5e8f61b` bind the stopped pre-effect attempt. Snapshot-to-live comparison found zero decommission audit rows and the same active Token in both databases. The lowest decommission regression first observed HTTP `200` plus revoked orphan Token; two follow-on regressions then observed successful verification with last-use mutation and HTTP `201` issuance. The shared verification and creation owners now require the exact active identity/deployment row, and the decommission transaction requires one exact active row update or rolls back. The five-file focused slice passes 31 of 31 tests, the native HTTP/2 admission test passes 1 of 1, NanoCore typecheck, 557-file lint, and build pass, the documentation/governance suite passes 119 of 119 tests, and `git diff --check` passes. The independent Reviewer accepted the complete slice, and the fresh Verifier returned `Ask Human` while explicitly permitting commit and push before any A2 recovery choice.
- **Owner:** `docs/specs/20260802-nanohost_runtime_and_transport.md` owns exact-pair enrollment, re-enrollment, missing-lineage failure, Token admission, issuance, rotation, and terminal decommission; `apps/nanocore/src/auth/nanohost-transport-token-store.ts`, `nanohost-transport-admission.ts`, `nanohost-transport-lifecycle.ts`, and `nanohost-transport-routes.ts` own their implementation.
- **Next action:** Commit and push the accepted fail-closed correction while preserving A2's old healthy App, active orphan Token, failed NanoHost, and retained backup without another transition attempt. Then ask the engineer to replace Intent Epoch 3 with one explicit choice: abandon exact-pair continuity and use a new identity/deployment after terminally preserving the orphan lineage, restore or rebuild the complete A2 data root under an accepted loss boundary, or accept a new Core lineage-recovery design. This disposition is necessary but not sufficient for A2 closeout: the B1/R110 host-admission path must also exist before Unit F can run on A2. Direct SQL repair, old-Token restoration, and the current helper remain prohibited.

## [open] PHQ-FND-015 — Current main has six unrelated local test failures

- **Observation:** The final-candidate full repository test passed 18 of 20 Turbo tasks but reported four NanoCore failures and two Web timeouts in paths whose bytes do not differ from `origin/main`.
- **Impact:** The focused R005 checks are green, but the repository-wide test gate cannot supply a green acceptance signal until the baseline failures are reconciled independently of this candidate.
- **Evidence:** NanoCore passed 2,530 of 2,534 tests and failed one stale boot-phase expectation plus three filesystem-sync non-file fixture expectations. Web passed 854 of 856 tests and timed out two Knowledge cases at five seconds; those exact cases pass at a 30-second diagnostic budget in 11.62 seconds. `git diff --cached origin/main` is empty for `apps/nanocore/src/bootstrap/phase-order.test.ts`, `apps/nanocore/src/index.ts`, `apps/nanocore/src/runtime/filesystem-workspace-sync.ts`, `apps/nanocore/src/runtime/filesystem-workspace-sync.test.ts`, and every `apps/web` path.
- **Owner:** The existing NanoCore boot, filesystem Workspace synchronization, Web Knowledge, and test-strategy owners own these baseline failures; this R005 plan supplies no authority to change them.
- **Next action:** Keep the failures as an explicit external gate finding and do not absorb unrelated fixes into the R005 candidate. Recompute the full repository test after their owner corrects or accepts the baseline.
