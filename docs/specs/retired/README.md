# Retired Specs

This directory keeps superseded specs that are no longer active guidance but still contain useful field-level details, prior alternatives, edge cases, or implementation notes.

Use the root-level specs in `docs/specs/` as the current entry points. Link to files in this directory only when historical detail is explicitly needed.
