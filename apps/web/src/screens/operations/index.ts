/**
 * Recovery and shell-global search — interrupted workers, scheduler admissions,
 * and `searchApp` projected through existing Core Client methods.
 *
 * Host paths, runtime handles, private error text, and secrets stay out of the DOM.
 */
export { AppSearch } from './AppSearch';
export { RecoveryScreen } from './RecoveryScreen';
