/**
 * Artifacts — selected-Workspace inventory, exact content, import, and introduction.
 *
 * Live Tier-A projection through existing Core Client methods. List rows omit
 * preview bytes; exact content comes from `getArtifact` `content.body` only.
 * Raw paths, runtime handles, and secrets stay out of the DOM.
 */
export { ArtifactsScreen } from './ArtifactsScreen';
