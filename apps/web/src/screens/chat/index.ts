/**
 * Chat & task thread surfaces (WP-4, boards 01-04). The conversation spine of the
 * product: the chat starter, the chat thread (+ optional Side panel for this
 * Thread's Artifact and file-change index), and the task thread with the inline
 * approval-card pattern.
 */
export { ChatStarter } from './ChatStarter';
export { ThreadScreen } from './ThreadScreen';
