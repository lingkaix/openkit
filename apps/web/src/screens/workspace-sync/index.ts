/**
 * Workspace changes — selected-Workspace Sync review-to-apply and recovery.
 *
 * Live Tier-A projection of Workspace Sync collections through existing Core
 * Client methods. Host paths, runtime handles, and secrets stay out of the DOM.
 */
export { WorkspaceChangesScreen } from './WorkspaceChangesScreen';
