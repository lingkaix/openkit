/**
 * Custom-only Drizzle Kit schema pointer.
 *
 * Product tables live in `drizzle/<scope>/*.sql`. TypeScript schema exports mix
 * Core, User, Workspace, and App tables and must not generate into every database.
 */
export {};
